// 自动生成的 LightGBM 预测函数
// 特征列表: super_offer_id, offer_id_mod, is_ios, user_active_days, user_gender, user_age, user_state_id, is_preference_beauty, is_preference_women_fashion, is_preference_kitchen, is_preference_men_fashion, is_preference_gadgets, is_preference_home, is_preference_personal_care, is_preference_kids, is_preference_sports, is_preference_pets, is_preference_household, is_preference_grocery
// 树数量: 201

function calculateTree0(features, node = 0) {
  if (node === 3) return -0.39577797833689626;
  if (node === 79) return -0.757650364418729;
  if (node === 323) return -0.7769781904198341;
  if (node === 324) return -1.0162721861929147;
  if (node === 161) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree0(features, 323);
    } else if (val <= 16.500000000000004) {
      return calculateTree0(features, 323);
    } else {
      return calculateTree0(features, 324);
    }
  }
  if (node === 162) return -0.8003491743630933;
  if (node === 80) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree0(features, 161);
    } else if (val <= 712.5000000000001) {
      return calculateTree0(features, 161);
    } else {
      return calculateTree0(features, 162);
    }
  }
  if (node === 39) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree0(features, 79);
    } else if (val <= 358.50000000000006) {
      return calculateTree0(features, 79);
    } else {
      return calculateTree0(features, 80);
    }
  }
  if (node === 163) return -0.4450719874361973;
  if (node === 164) return -1.0611931224447722;
  if (node === 81) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree0(features, 163);
    } else if (val <= 859.5000000000001) {
      return calculateTree0(features, 163);
    } else {
      return calculateTree0(features, 164);
    }
  }
  if (node === 165) return -0.9201033881545136;
  if (node === 1335) return -0.391109563318128;
  if (node === 1336) return -1.1958133925259626;
  if (node === 667) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree0(features, 1335);
    } else if (val <= 34.00000000000001) {
      return calculateTree0(features, 1335);
    } else {
      return calculateTree0(features, 1336);
    }
  }
  if (node === 668) return -1.018006613012132;
  if (node === 333) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree0(features, 667);
    } else if (val <= 191.50000000000003) {
      return calculateTree0(features, 667);
    } else {
      return calculateTree0(features, 668);
    }
  }
  if (node === 1339) return -1.184580595338278;
  if (node === 1340) return -0.3344790440792927;
  if (node === 669) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree0(features, 1339);
    } else if (val <= 275.50000000000006) {
      return calculateTree0(features, 1339);
    } else {
      return calculateTree0(features, 1340);
    }
  }
  if (node === 1341) return -0.9380946648449969;
  if (node === 1342) return -0.6612727882585041;
  if (node === 670) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree0(features, 1341);
    } else if (val <= 276.00000000000006) {
      return calculateTree0(features, 1341);
    } else {
      return calculateTree0(features, 1342);
    }
  }
  if (node === 334) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree0(features, 669);
    } else if (val <= 203.50000000000003) {
      return calculateTree0(features, 669);
    } else {
      return calculateTree0(features, 670);
    }
  }
  if (node === 166) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree0(features, 333);
    } else if (val <= 181.50000000000003) {
      return calculateTree0(features, 333);
    } else {
      return calculateTree0(features, 334);
    }
  }
  if (node === 82) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree0(features, 165);
    } else if (val <= 188.00000000000003) {
      return calculateTree0(features, 165);
    } else {
      return calculateTree0(features, 166);
    }
  }
  if (node === 40) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree0(features, 81);
    } else if (val <= 164.50000000000003) {
      return calculateTree0(features, 81);
    } else {
      return calculateTree0(features, 82);
    }
  }
  if (node === 19) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree0(features, 39);
    } else if (val <= 148.00000000000003) {
      return calculateTree0(features, 39);
    } else {
      return calculateTree0(features, 40);
    }
  }
  if (node === 83) return -0.9136284985249568;
  if (node === 169) return -0.7283460724976111;
  if (node === 341) return -1.1231396975380517;
  if (node === 342) return -0.8318786242919666;
  if (node === 170) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree0(features, 341);
    } else if (val <= 470.50000000000006) {
      return calculateTree0(features, 341);
    } else {
      return calculateTree0(features, 342);
    }
  }
  if (node === 84) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree0(features, 169);
    } else if (val <= 439.50000000000006) {
      return calculateTree0(features, 169);
    } else {
      return calculateTree0(features, 170);
    }
  }
  if (node === 41) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree0(features, 83);
    } else if (val <= 310.00000000000006) {
      return calculateTree0(features, 83);
    } else {
      return calculateTree0(features, 84);
    }
  }
  if (node === 171) return -0.3802007559934694;
  if (node === 172) return -0.8487776856588858;
  if (node === 85) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree0(features, 171);
    } else if (val <= 668.0000000000001) {
      return calculateTree0(features, 171);
    } else {
      return calculateTree0(features, 172);
    }
  }
  if (node === 347) return -0.896892927261483;
  if (node === 1395) return -0.5295118769391949;
  if (node === 1396) return -0.09582485888021763;
  if (node === 697) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree0(features, 1395);
    } else if (val <= 781.0000000000001) {
      return calculateTree0(features, 1395);
    } else {
      return calculateTree0(features, 1396);
    }
  }
  if (node === 1397) return -0.9547188344756569;
  if (node === 1398) return -0.7913798880247952;
  if (node === 698) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree0(features, 1397);
    } else if (val <= 780.0000000000001) {
      return calculateTree0(features, 1397);
    } else {
      return calculateTree0(features, 1398);
    }
  }
  if (node === 348) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree0(features, 697);
    } else if (val <= 752.5000000000001) {
      return calculateTree0(features, 697);
    } else {
      return calculateTree0(features, 698);
    }
  }
  if (node === 173) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree0(features, 347);
    } else if (val <= 745.5000000000001) {
      return calculateTree0(features, 347);
    } else {
      return calculateTree0(features, 348);
    }
  }
  if (node === 349) return -1.0242213707918053;
  if (node === 701) return -0.03417570326541808;
  if (node === 702) return -1.1919168773718165;
  if (node === 350) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree0(features, 701);
    } else if (val <= 982.5000000000001) {
      return calculateTree0(features, 701);
    } else {
      return calculateTree0(features, 702);
    }
  }
  if (node === 174) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree0(features, 349);
    } else if (val <= 905.5000000000001) {
      return calculateTree0(features, 349);
    } else {
      return calculateTree0(features, 350);
    }
  }
  if (node === 86) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree0(features, 173);
    } else if (val <= 969.5000000000001) {
      return calculateTree0(features, 173);
    } else {
      return calculateTree0(features, 174);
    }
  }
  if (node === 42) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree0(features, 85);
    } else if (val <= 662.5000000000001) {
      return calculateTree0(features, 85);
    } else {
      return calculateTree0(features, 86);
    }
  }
  if (node === 20) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree0(features, 41);
    } else if (val <= 647.0000000000001) {
      return calculateTree0(features, 41);
    } else {
      return calculateTree0(features, 42);
    }
  }
  if (node === 9) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree0(features, 19);
    } else if (val <= 365.50000000000006) {
      return calculateTree0(features, 19);
    } else {
      return calculateTree0(features, 20);
    }
  }
  if (node === 21) return -1.0951462548858173;
  if (node === 22) return -0.5029326107708889;
  if (node === 10) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree0(features, 21);
    } else if (val <= 283.50000000000006) {
      return calculateTree0(features, 21);
    } else {
      return calculateTree0(features, 22);
    }
  }
  if (node === 4) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree0(features, 9);
    } else if (val <= 993.5000000000001) {
      return calculateTree0(features, 9);
    } else {
      return calculateTree0(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree0(features, 3);
    } else if (val <= 3.5000000000000004) {
      return calculateTree0(features, 3);
    } else {
      return calculateTree0(features, 4);
    }
  }
  if (node === 2) return -0.5720131523815511;
  if (node === 0) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree0(features, 1);
    } else if (val <= 20.500000000000004) {
      return calculateTree0(features, 1);
    } else {
      return calculateTree0(features, 2);
    }
  }
}
function calculateTree1(features, node = 0) {
  if (node === 3) return 0.04677225986958966;
  if (node === 4) return 0.19853162789381462;
  if (node === 1) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree1(features, 3);
    } else if (val <= 29.500000000000004) {
      return calculateTree1(features, 3);
    } else {
      return calculateTree1(features, 4);
    }
  }
  if (node === 11) return 0.47037445786318594;
  if (node === 12) return -0.501717522302526;
  if (node === 5) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree1(features, 11);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree1(features, 11);
    } else {
      return calculateTree1(features, 12);
    }
  }
  if (node === 895) return 0.04426896586737556;
  if (node === 896) return -0.15213182805490588;
  if (node === 447) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree1(features, 895);
    } else if (val <= 75.50000000000001) {
      return calculateTree1(features, 895);
    } else {
      return calculateTree1(features, 896);
    }
  }
  if (node === 448) return 0.39006737524567786;
  if (node === 223) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree1(features, 447);
    } else if (val <= 148.00000000000003) {
      return calculateTree1(features, 447);
    } else {
      return calculateTree1(features, 448);
    }
  }
  if (node === 449) return -0.40589645082319553;
  if (node === 1803) return -0.25552406285060003;
  if (node === 1804) return 0.6079431749132529;
  if (node === 901) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree1(features, 1803);
    } else if (val <= 893.0000000000001) {
      return calculateTree1(features, 1803);
    } else {
      return calculateTree1(features, 1804);
    }
  }
  if (node === 1805) return -0.3535834937693147;
  if (node === 1806) return -0.056116650682858836;
  if (node === 902) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree1(features, 1805);
    } else if (val <= 222.50000000000003) {
      return calculateTree1(features, 1805);
    } else {
      return calculateTree1(features, 1806);
    }
  }
  if (node === 450) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree1(features, 901);
    } else if (val <= 195.00000000000003) {
      return calculateTree1(features, 901);
    } else {
      return calculateTree1(features, 902);
    }
  }
  if (node === 224) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree1(features, 449);
    } else if (val <= 191.50000000000003) {
      return calculateTree1(features, 449);
    } else {
      return calculateTree1(features, 450);
    }
  }
  if (node === 111) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree1(features, 223);
    } else if (val <= 166.50000000000003) {
      return calculateTree1(features, 223);
    } else {
      return calculateTree1(features, 224);
    }
  }
  if (node === 903) return 0.35577616532940143;
  if (node === 904) return -0.2808798943463933;
  if (node === 451) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree1(features, 903);
    } else if (val <= 606.5000000000001) {
      return calculateTree1(features, 903);
    } else {
      return calculateTree1(features, 904);
    }
  }
  if (node === 452) return -0.18794096154902334;
  if (node === 225) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree1(features, 451);
    } else if (val <= 14.500000000000002) {
      return calculateTree1(features, 451);
    } else {
      return calculateTree1(features, 452);
    }
  }
  if (node === 1815) return 0.20673321201940656;
  if (node === 1816) return 0.6124562100004055;
  if (node === 907) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree1(features, 1815);
    } else if (val <= 708.5000000000001) {
      return calculateTree1(features, 1815);
    } else {
      return calculateTree1(features, 1816);
    }
  }
  if (node === 908) return -0.443247287234776;
  if (node === 453) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree1(features, 907);
    } else if (val <= 805.0000000000001) {
      return calculateTree1(features, 907);
    } else {
      return calculateTree1(features, 908);
    }
  }
  if (node === 1819) return -0.20856858305332682;
  if (node === 1820) return 0.0365364960709762;
  if (node === 909) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree1(features, 1819);
    } else if (val <= 599.5000000000001) {
      return calculateTree1(features, 1819);
    } else {
      return calculateTree1(features, 1820);
    }
  }
  if (node === 1821) return 0.10847169141643438;
  if (node === 1822) return -0.010388749703927487;
  if (node === 910) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree1(features, 1821);
    } else if (val <= 470.00000000000006) {
      return calculateTree1(features, 1821);
    } else {
      return calculateTree1(features, 1822);
    }
  }
  if (node === 454) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree1(features, 909);
    } else if (val <= 316.50000000000006) {
      return calculateTree1(features, 909);
    } else {
      return calculateTree1(features, 910);
    }
  }
  if (node === 226) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree1(features, 453);
    } else if (val <= 214.00000000000003) {
      return calculateTree1(features, 453);
    } else {
      return calculateTree1(features, 454);
    }
  }
  if (node === 112) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree1(features, 225);
    } else if (val <= 185.00000000000003) {
      return calculateTree1(features, 225);
    } else {
      return calculateTree1(features, 226);
    }
  }
  if (node === 55) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree1(features, 111);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree1(features, 111);
    } else {
      return calculateTree1(features, 112);
    }
  }
  if (node === 113) return -0.5501204727490048;
  if (node === 114) return 0.47169188718828964;
  if (node === 56) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree1(features, 113);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree1(features, 113);
    } else {
      return calculateTree1(features, 114);
    }
  }
  if (node === 27) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree1(features, 55);
    } else if (val <= 905.5000000000001) {
      return calculateTree1(features, 55);
    } else {
      return calculateTree1(features, 56);
    }
  }
  if (node === 57) return 0.08613252861601499;
  if (node === 235) return -0.19238736687672736;
  if (node === 473) return 0.4652149865673627;
  if (node === 949) return 0.0692918747489258;
  if (node === 950) return -0.4112070695367001;
  if (node === 474) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree1(features, 949);
    } else if (val <= 661.0000000000001) {
      return calculateTree1(features, 949);
    } else {
      return calculateTree1(features, 950);
    }
  }
  if (node === 236) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree1(features, 473);
    } else if (val <= 963.5000000000001) {
      return calculateTree1(features, 473);
    } else {
      return calculateTree1(features, 474);
    }
  }
  if (node === 117) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree1(features, 235);
    } else if (val <= 951.5000000000001) {
      return calculateTree1(features, 235);
    } else {
      return calculateTree1(features, 236);
    }
  }
  if (node === 118) return -0.3266599458940786;
  if (node === 58) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree1(features, 117);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree1(features, 117);
    } else {
      return calculateTree1(features, 118);
    }
  }
  if (node === 28) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree1(features, 57);
    } else if (val <= 175.50000000000003) {
      return calculateTree1(features, 57);
    } else {
      return calculateTree1(features, 58);
    }
  }
  if (node === 13) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree1(features, 27);
    } else if (val <= 908.5000000000001) {
      return calculateTree1(features, 27);
    } else {
      return calculateTree1(features, 28);
    }
  }
  if (node === 29) return 0.4139419466380568;
  if (node === 30) return -0.4423447296415782;
  if (node === 14) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree1(features, 29);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree1(features, 29);
    } else {
      return calculateTree1(features, 30);
    }
  }
  if (node === 6) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree1(features, 13);
    } else if (val <= 993.5000000000001) {
      return calculateTree1(features, 13);
    } else {
      return calculateTree1(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree1(features, 5);
    } else if (val <= 3.5000000000000004) {
      return calculateTree1(features, 5);
    } else {
      return calculateTree1(features, 6);
    }
  }
  if (node === 0) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree1(features, 1);
    } else if (val <= 1.5000000000000002) {
      return calculateTree1(features, 1);
    } else {
      return calculateTree1(features, 2);
    }
  }
}
function calculateTree2(features, node = 0) {
  if (node === 15) return 0.032011664461999524;
  if (node === 33) return -0.1789588927764552;
  if (node === 139) return -0.04693705583406508;
  if (node === 281) return 0.1996397472281872;
  if (node === 1131) return 0.017775774020155594;
  if (node === 1132) return 0.3535170619526991;
  if (node === 565) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree2(features, 1131);
    } else if (val <= 251.00000000000003) {
      return calculateTree2(features, 1131);
    } else {
      return calculateTree2(features, 1132);
    }
  }
  if (node === 1133) return -0.2867884961412565;
  if (node === 1134) return 0.4033527396007191;
  if (node === 566) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree2(features, 1133);
    } else if (val <= 361.50000000000006) {
      return calculateTree2(features, 1133);
    } else {
      return calculateTree2(features, 1134);
    }
  }
  if (node === 282) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree2(features, 565);
    } else if (val <= 253.50000000000003) {
      return calculateTree2(features, 565);
    } else {
      return calculateTree2(features, 566);
    }
  }
  if (node === 140) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree2(features, 281);
    } else if (val <= 717.0000000000001) {
      return calculateTree2(features, 281);
    } else {
      return calculateTree2(features, 282);
    }
  }
  if (node === 69) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree2(features, 139);
    } else if (val <= 708.5000000000001) {
      return calculateTree2(features, 139);
    } else {
      return calculateTree2(features, 140);
    }
  }
  if (node === 70) return -0.19331968512411643;
  if (node === 34) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree2(features, 69);
    } else if (val <= 783.5000000000001) {
      return calculateTree2(features, 69);
    } else {
      return calculateTree2(features, 70);
    }
  }
  if (node === 16) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree2(features, 33);
    } else if (val <= 127.00000000000001) {
      return calculateTree2(features, 33);
    } else {
      return calculateTree2(features, 34);
    }
  }
  if (node === 7) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree2(features, 15);
    } else if (val <= 353.00000000000006) {
      return calculateTree2(features, 15);
    } else {
      return calculateTree2(features, 16);
    }
  }
  if (node === 35) return 0.05881191054920973;
  if (node === 36) return -0.16125333983658677;
  if (node === 17) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree2(features, 35);
    } else if (val <= 123.50000000000001) {
      return calculateTree2(features, 35);
    } else {
      return calculateTree2(features, 36);
    }
  }
  if (node === 151) return -0.4470166414724819;
  if (node === 152) return 0.49895199337525986;
  if (node === 75) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree2(features, 151);
    } else if (val <= 969.5000000000001) {
      return calculateTree2(features, 151);
    } else {
      return calculateTree2(features, 152);
    }
  }
  if (node === 153) return 0.6207674759107985;
  if (node === 154) return -0.45363825364009797;
  if (node === 76) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree2(features, 153);
    } else if (val <= 969.5000000000001) {
      return calculateTree2(features, 153);
    } else {
      return calculateTree2(features, 154);
    }
  }
  if (node === 37) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree2(features, 75);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree2(features, 75);
    } else {
      return calculateTree2(features, 76);
    }
  }
  if (node === 155) return 0.33122227036789637;
  if (node === 156) return -0.44699344829378745;
  if (node === 77) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree2(features, 155);
    } else if (val <= 311.50000000000006) {
      return calculateTree2(features, 155);
    } else {
      return calculateTree2(features, 156);
    }
  }
  if (node === 157) return -0.4478527909855446;
  if (node === 158) return 0.22211572121189652;
  if (node === 78) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree2(features, 157);
    } else if (val <= 311.50000000000006) {
      return calculateTree2(features, 157);
    } else {
      return calculateTree2(features, 158);
    }
  }
  if (node === 38) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree2(features, 77);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree2(features, 77);
    } else {
      return calculateTree2(features, 78);
    }
  }
  if (node === 18) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree2(features, 37);
    } else if (val <= 288.50000000000006) {
      return calculateTree2(features, 37);
    } else {
      return calculateTree2(features, 38);
    }
  }
  if (node === 8) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree2(features, 17);
    } else if (val <= 241.50000000000003) {
      return calculateTree2(features, 17);
    } else {
      return calculateTree2(features, 18);
    }
  }
  if (node === 3) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree2(features, 7);
    } else if (val <= 835.5000000000001) {
      return calculateTree2(features, 7);
    } else {
      return calculateTree2(features, 8);
    }
  }
  if (node === 9) return -0.1814281100148676;
  if (node === 21) return 0.13890505670739342;
  if (node === 45) return -0.2115009604361218;
  if (node === 187) return 0.02129195767486248;
  if (node === 377) return 0.004913042079700372;
  if (node === 378) return -0.11148592788049728;
  if (node === 188) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree2(features, 377);
    } else if (val <= 525.0000000000001) {
      return calculateTree2(features, 377);
    } else {
      return calculateTree2(features, 378);
    }
  }
  if (node === 93) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree2(features, 187);
    } else if (val <= 229.50000000000003) {
      return calculateTree2(features, 187);
    } else {
      return calculateTree2(features, 188);
    }
  }
  if (node === 189) return 0.3532527251187249;
  if (node === 763) return 0.20417616931189805;
  if (node === 764) return -0.23925980930434562;
  if (node === 381) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree2(features, 763);
    } else if (val <= 144.50000000000003) {
      return calculateTree2(features, 763);
    } else {
      return calculateTree2(features, 764);
    }
  }
  if (node === 765) return 0.27737878112123515;
  if (node === 766) return -0.012950501157969384;
  if (node === 382) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree2(features, 765);
    } else if (val <= 784.5000000000001) {
      return calculateTree2(features, 765);
    } else {
      return calculateTree2(features, 766);
    }
  }
  if (node === 190) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree2(features, 381);
    } else if (val <= 780.0000000000001) {
      return calculateTree2(features, 381);
    } else {
      return calculateTree2(features, 382);
    }
  }
  if (node === 94) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree2(features, 189);
    } else if (val <= 752.5000000000001) {
      return calculateTree2(features, 189);
    } else {
      return calculateTree2(features, 190);
    }
  }
  if (node === 46) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree2(features, 93);
    } else if (val <= 745.5000000000001) {
      return calculateTree2(features, 93);
    } else {
      return calculateTree2(features, 94);
    }
  }
  if (node === 22) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree2(features, 45);
    } else if (val <= 470.50000000000006) {
      return calculateTree2(features, 45);
    } else {
      return calculateTree2(features, 46);
    }
  }
  if (node === 10) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree2(features, 21);
    } else if (val <= 427.50000000000006) {
      return calculateTree2(features, 21);
    } else {
      return calculateTree2(features, 22);
    }
  }
  if (node === 4) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree2(features, 9);
    } else if (val <= 414.50000000000006) {
      return calculateTree2(features, 9);
    } else {
      return calculateTree2(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree2(features, 3);
    } else if (val <= 365.50000000000006) {
      return calculateTree2(features, 3);
    } else {
      return calculateTree2(features, 4);
    }
  }
  if (node === 5) return 0.030654194214483958;
  if (node === 6) return 0.13504415727745467;
  if (node === 2) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree2(features, 5);
    } else if (val <= 29.500000000000004) {
      return calculateTree2(features, 5);
    } else {
      return calculateTree2(features, 6);
    }
  }
  if (node === 0) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree2(features, 1);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree2(features, 1);
    } else {
      return calculateTree2(features, 2);
    }
  }
}
function calculateTree3(features, node = 0) {
  if (node === 7) return 0.3253965028117188;
  if (node === 8) return -0.4379267305580532;
  if (node === 3) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree3(features, 7);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree3(features, 7);
    } else {
      return calculateTree3(features, 8);
    }
  }
  if (node === 1279) return -0.0075503231525116685;
  if (node === 1280) return 0.2731464781602483;
  if (node === 639) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree3(features, 1279);
    } else if (val <= 148.00000000000003) {
      return calculateTree3(features, 1279);
    } else {
      return calculateTree3(features, 1280);
    }
  }
  if (node === 1281) return -0.07969862504260018;
  if (node === 1282) return 0.10346534989517976;
  if (node === 640) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree3(features, 1281);
    } else if (val <= 893.0000000000001) {
      return calculateTree3(features, 1281);
    } else {
      return calculateTree3(features, 1282);
    }
  }
  if (node === 319) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree3(features, 639);
    } else if (val <= 166.50000000000003) {
      return calculateTree3(features, 639);
    } else {
      return calculateTree3(features, 640);
    }
  }
  if (node === 320) return 0.49286060940629095;
  if (node === 159) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree3(features, 319);
    } else if (val <= 804.5000000000001) {
      return calculateTree3(features, 319);
    } else {
      return calculateTree3(features, 320);
    }
  }
  if (node === 643) return -0.20263863616632674;
  if (node === 644) return 0.45689898203584545;
  if (node === 321) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree3(features, 643);
    } else if (val <= 414.50000000000006) {
      return calculateTree3(features, 643);
    } else {
      return calculateTree3(features, 644);
    }
  }
  if (node === 322) return -0.3649685013246212;
  if (node === 160) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree3(features, 321);
    } else if (val <= 441.00000000000006) {
      return calculateTree3(features, 321);
    } else {
      return calculateTree3(features, 322);
    }
  }
  if (node === 79) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree3(features, 159);
    } else if (val <= 818.0000000000001) {
      return calculateTree3(features, 159);
    } else {
      return calculateTree3(features, 160);
    }
  }
  if (node === 323) return 0.06959381299432565;
  if (node === 324) return -0.15154853720176306;
  if (node === 161) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree3(features, 323);
    } else if (val <= 18.500000000000004) {
      return calculateTree3(features, 323);
    } else {
      return calculateTree3(features, 324);
    }
  }
  if (node === 651) return 0.16938265823537885;
  if (node === 652) return -0.38852006015387963;
  if (node === 325) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree3(features, 651);
    } else if (val <= 805.0000000000001) {
      return calculateTree3(features, 651);
    } else {
      return calculateTree3(features, 652);
    }
  }
  if (node === 1307) return 0.0024787722343426292;
  if (node === 1308) return -0.19006666994820626;
  if (node === 653) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree3(features, 1307);
    } else if (val <= 662.5000000000001) {
      return calculateTree3(features, 1307);
    } else {
      return calculateTree3(features, 1308);
    }
  }
  if (node === 1309) return 0.28933665687853005;
  if (node === 1310) return 0.03436557083951926;
  if (node === 654) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree3(features, 1309);
    } else if (val <= 752.5000000000001) {
      return calculateTree3(features, 1309);
    } else {
      return calculateTree3(features, 1310);
    }
  }
  if (node === 326) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree3(features, 653);
    } else if (val <= 745.5000000000001) {
      return calculateTree3(features, 653);
    } else {
      return calculateTree3(features, 654);
    }
  }
  if (node === 162) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree3(features, 325);
    } else if (val <= 214.00000000000003) {
      return calculateTree3(features, 325);
    } else {
      return calculateTree3(features, 326);
    }
  }
  if (node === 80) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree3(features, 161);
    } else if (val <= 185.00000000000003) {
      return calculateTree3(features, 161);
    } else {
      return calculateTree3(features, 162);
    }
  }
  if (node === 39) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree3(features, 79);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree3(features, 79);
    } else {
      return calculateTree3(features, 80);
    }
  }
  if (node === 40) return 0.2614447575665035;
  if (node === 19) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree3(features, 39);
    } else if (val <= 903.5000000000001) {
      return calculateTree3(features, 39);
    } else {
      return calculateTree3(features, 40);
    }
  }
  if (node === 83) return -0.031726864585921694;
  if (node === 339) return -0.4463755385862929;
  if (node === 340) return 0.40477201583747613;
  if (node === 169) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree3(features, 339);
    } else if (val <= 172.50000000000003) {
      return calculateTree3(features, 339);
    } else {
      return calculateTree3(features, 340);
    }
  }
  if (node === 341) return -0.24569141078397425;
  if (node === 342) return 0.41878003125809454;
  if (node === 170) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree3(features, 341);
    } else if (val <= 957.0000000000001) {
      return calculateTree3(features, 341);
    } else {
      return calculateTree3(features, 342);
    }
  }
  if (node === 84) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree3(features, 169);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree3(features, 169);
    } else {
      return calculateTree3(features, 170);
    }
  }
  if (node === 41) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree3(features, 83);
    } else if (val <= 148.50000000000003) {
      return calculateTree3(features, 83);
    } else {
      return calculateTree3(features, 84);
    }
  }
  if (node === 171) return -0.15021956072082854;
  if (node === 345) return 0.23825581761768908;
  if (node === 346) return -0.11354035391294601;
  if (node === 172) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree3(features, 345);
    } else if (val <= 966.0000000000001) {
      return calculateTree3(features, 345);
    } else {
      return calculateTree3(features, 346);
    }
  }
  if (node === 85) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree3(features, 171);
    } else if (val <= 951.5000000000001) {
      return calculateTree3(features, 171);
    } else {
      return calculateTree3(features, 172);
    }
  }
  if (node === 86) return -0.2767938158620292;
  if (node === 42) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree3(features, 85);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree3(features, 85);
    } else {
      return calculateTree3(features, 86);
    }
  }
  if (node === 20) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree3(features, 41);
    } else if (val <= 175.50000000000003) {
      return calculateTree3(features, 41);
    } else {
      return calculateTree3(features, 42);
    }
  }
  if (node === 9) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree3(features, 19);
    } else if (val <= 908.5000000000001) {
      return calculateTree3(features, 19);
    } else {
      return calculateTree3(features, 20);
    }
  }
  if (node === 43) return -0.4465674121326468;
  if (node === 44) return 0.31675816396565004;
  if (node === 21) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree3(features, 43);
    } else if (val <= 283.50000000000006) {
      return calculateTree3(features, 43);
    } else {
      return calculateTree3(features, 44);
    }
  }
  if (node === 22) return -0.38477264071204687;
  if (node === 10) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree3(features, 21);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree3(features, 21);
    } else {
      return calculateTree3(features, 22);
    }
  }
  if (node === 4) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree3(features, 9);
    } else if (val <= 993.5000000000001) {
      return calculateTree3(features, 9);
    } else {
      return calculateTree3(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree3(features, 3);
    } else if (val <= 3.5000000000000004) {
      return calculateTree3(features, 3);
    } else {
      return calculateTree3(features, 4);
    }
  }
  if (node === 2) return 0.04112709157303929;
  if (node === 0) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree3(features, 1);
    } else if (val <= 6.500000000000001) {
      return calculateTree3(features, 1);
    } else {
      return calculateTree3(features, 2);
    }
  }
}
function calculateTree4(features, node = 0) {
  if (node === 31) return -0.00937090697022168;
  if (node === 131) return -0.23847848943702826;
  if (node === 132) return 0.07280478663981099;
  if (node === 65) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree4(features, 131);
    } else if (val <= 389.00000000000006) {
      return calculateTree4(features, 131);
    } else {
      return calculateTree4(features, 132);
    }
  }
  if (node === 133) return 0.02783703657887151;
  if (node === 134) return -0.24834111974974032;
  if (node === 66) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree4(features, 133);
    } else if (val <= 363.00000000000006) {
      return calculateTree4(features, 133);
    } else {
      return calculateTree4(features, 134);
    }
  }
  if (node === 32) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree4(features, 65);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree4(features, 65);
    } else {
      return calculateTree4(features, 66);
    }
  }
  if (node === 15) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree4(features, 31);
    } else if (val <= 358.50000000000006) {
      return calculateTree4(features, 31);
    } else {
      return calculateTree4(features, 32);
    }
  }
  if (node === 135) return 0.08147329763539064;
  if (node === 273) return -0.25957285208028213;
  if (node === 274) return -0.012357160970794498;
  if (node === 136) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree4(features, 273);
    } else if (val <= 467.50000000000006) {
      return calculateTree4(features, 273);
    } else {
      return calculateTree4(features, 274);
    }
  }
  if (node === 67) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree4(features, 135);
    } else if (val <= 417.00000000000006) {
      return calculateTree4(features, 135);
    } else {
      return calculateTree4(features, 136);
    }
  }
  if (node === 137) return 0.12113264366993942;
  if (node === 277) return -0.3766850981993895;
  if (node === 557) return 0.10878486883740388;
  if (node === 558) return -0.12256873146575215;
  if (node === 278) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree4(features, 557);
    } else if (val <= 474.50000000000006) {
      return calculateTree4(features, 557);
    } else {
      return calculateTree4(features, 558);
    }
  }
  if (node === 138) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree4(features, 277);
    } else if (val <= 470.00000000000006) {
      return calculateTree4(features, 277);
    } else {
      return calculateTree4(features, 278);
    }
  }
  if (node === 68) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree4(features, 137);
    } else if (val <= 467.50000000000006) {
      return calculateTree4(features, 137);
    } else {
      return calculateTree4(features, 138);
    }
  }
  if (node === 33) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree4(features, 67);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree4(features, 67);
    } else {
      return calculateTree4(features, 68);
    }
  }
  if (node === 69) return 0.2153524688277558;
  if (node === 141) return 0.11067242747100006;
  if (node === 142) return -0.18215826647704472;
  if (node === 70) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree4(features, 141);
    } else if (val <= 500.50000000000006) {
      return calculateTree4(features, 141);
    } else {
      return calculateTree4(features, 142);
    }
  }
  if (node === 34) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree4(features, 69);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree4(features, 69);
    } else {
      return calculateTree4(features, 70);
    }
  }
  if (node === 16) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree4(features, 33);
    } else if (val <= 498.50000000000006) {
      return calculateTree4(features, 33);
    } else {
      return calculateTree4(features, 34);
    }
  }
  if (node === 7) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree4(features, 15);
    } else if (val <= 409.50000000000006) {
      return calculateTree4(features, 15);
    } else {
      return calculateTree4(features, 16);
    }
  }
  if (node === 71) return 0.05692956086619212;
  if (node === 72) return -0.08192155811170816;
  if (node === 35) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree4(features, 71);
    } else if (val <= 575.5000000000001) {
      return calculateTree4(features, 71);
    } else {
      return calculateTree4(features, 72);
    }
  }
  if (node === 73) return -0.2980143005077992;
  if (node === 599) return 0.014427160226745119;
  if (node === 600) return -0.3488551827958152;
  if (node === 299) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree4(features, 599);
    } else if (val <= 587.5000000000001) {
      return calculateTree4(features, 599);
    } else {
      return calculateTree4(features, 600);
    }
  }
  if (node === 300) return 0.02968158713558324;
  if (node === 149) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree4(features, 299);
    } else if (val <= 607.5000000000001) {
      return calculateTree4(features, 299);
    } else {
      return calculateTree4(features, 300);
    }
  }
  if (node === 150) return -0.14776721671660337;
  if (node === 74) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree4(features, 149);
    } else if (val <= 659.5000000000001) {
      return calculateTree4(features, 149);
    } else {
      return calculateTree4(features, 150);
    }
  }
  if (node === 36) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree4(features, 73);
    } else if (val <= 575.5000000000001) {
      return calculateTree4(features, 73);
    } else {
      return calculateTree4(features, 74);
    }
  }
  if (node === 17) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree4(features, 35);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree4(features, 35);
    } else {
      return calculateTree4(features, 36);
    }
  }
  if (node === 18) return -0.019176489115065583;
  if (node === 8) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree4(features, 17);
    } else if (val <= 682.5000000000001) {
      return calculateTree4(features, 17);
    } else {
      return calculateTree4(features, 18);
    }
  }
  if (node === 3) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree4(features, 7);
    } else if (val <= 507.00000000000006) {
      return calculateTree4(features, 7);
    } else {
      return calculateTree4(features, 8);
    }
  }
  if (node === 39) return 0.30473177396147727;
  if (node === 40) return -0.3999910240553823;
  if (node === 19) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree4(features, 39);
    } else if (val <= 851.5000000000001) {
      return calculateTree4(features, 39);
    } else {
      return calculateTree4(features, 40);
    }
  }
  if (node === 41) return -0.40432400843234945;
  if (node === 42) return 0.325280506376273;
  if (node === 20) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree4(features, 41);
    } else if (val <= 851.5000000000001) {
      return calculateTree4(features, 41);
    } else {
      return calculateTree4(features, 42);
    }
  }
  if (node === 9) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree4(features, 19);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree4(features, 19);
    } else {
      return calculateTree4(features, 20);
    }
  }
  if (node === 10) return 0.008674155091895057;
  if (node === 4) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree4(features, 9);
    } else if (val <= 859.5000000000001) {
      return calculateTree4(features, 9);
    } else {
      return calculateTree4(features, 10);
    }
  }
  if (node === 1) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree4(features, 3);
    } else if (val <= 850.5000000000001) {
      return calculateTree4(features, 3);
    } else {
      return calculateTree4(features, 4);
    }
  }
  if (node === 11) return 0.02789835836130995;
  if (node === 12) return -0.01670926737143534;
  if (node === 5) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree4(features, 11);
    } else if (val <= 36.50000000000001) {
      return calculateTree4(features, 11);
    } else {
      return calculateTree4(features, 12);
    }
  }
  if (node === 13) return 0.0794856772677308;
  if (node === 14) return 0.006905259567646578;
  if (node === 6) {
    const val = features['is_preference_men_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree4(features, 13);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree4(features, 13);
    } else {
      return calculateTree4(features, 14);
    }
  }
  if (node === 2) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree4(features, 5);
    } else if (val <= 15.500000000000002) {
      return calculateTree4(features, 5);
    } else {
      return calculateTree4(features, 6);
    }
  }
  if (node === 0) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree4(features, 1);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree4(features, 1);
    } else {
      return calculateTree4(features, 2);
    }
  }
}
function calculateTree5(features, node = 0) {
  if (node === 31) return 0.1292739495577106;
  if (node === 131) return 0.02629087969389383;
  if (node === 132) return -0.19024493480859492;
  if (node === 65) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree5(features, 131);
    } else if (val <= 181.50000000000003) {
      return calculateTree5(features, 131);
    } else {
      return calculateTree5(features, 132);
    }
  }
  if (node === 267) return -0.41967006686246405;
  if (node === 268) return 0.3386211473142211;
  if (node === 133) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree5(features, 267);
    } else if (val <= 293.00000000000006) {
      return calculateTree5(features, 267);
    } else {
      return calculateTree5(features, 268);
    }
  }
  if (node === 269) return 0.24782094407898417;
  if (node === 270) return -0.3390188323900158;
  if (node === 134) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree5(features, 269);
    } else if (val <= 284.50000000000006) {
      return calculateTree5(features, 269);
    } else {
      return calculateTree5(features, 270);
    }
  }
  if (node === 66) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree5(features, 133);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree5(features, 133);
    } else {
      return calculateTree5(features, 134);
    }
  }
  if (node === 32) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree5(features, 65);
    } else if (val <= 270.00000000000006) {
      return calculateTree5(features, 65);
    } else {
      return calculateTree5(features, 66);
    }
  }
  if (node === 15) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree5(features, 31);
    } else if (val <= 30.500000000000004) {
      return calculateTree5(features, 31);
    } else {
      return calculateTree5(features, 32);
    }
  }
  if (node === 67) return 0.01708607714033445;
  if (node === 68) return -0.2549854393913123;
  if (node === 33) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree5(features, 67);
    } else if (val <= 16.500000000000004) {
      return calculateTree5(features, 67);
    } else {
      return calculateTree5(features, 68);
    }
  }
  if (node === 279) return 0.3149670568278397;
  if (node === 1123) return 0.23963278964562987;
  if (node === 1124) return -0.24428388529883865;
  if (node === 561) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree5(features, 1123);
    } else if (val <= 358.50000000000006) {
      return calculateTree5(features, 1123);
    } else {
      return calculateTree5(features, 1124);
    }
  }
  if (node === 562) return 0.18889475158303234;
  if (node === 280) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree5(features, 561);
    } else if (val <= 148.00000000000003) {
      return calculateTree5(features, 561);
    } else {
      return calculateTree5(features, 562);
    }
  }
  if (node === 139) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree5(features, 279);
    } else if (val <= 75.50000000000001) {
      return calculateTree5(features, 279);
    } else {
      return calculateTree5(features, 280);
    }
  }
  if (node === 140) return -0.2617809273713581;
  if (node === 69) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree5(features, 139);
    } else if (val <= 166.50000000000003) {
      return calculateTree5(features, 139);
    } else {
      return calculateTree5(features, 140);
    }
  }
  if (node === 141) return -0.30663462605163955;
  if (node === 1143) return 0.037364143667117926;
  if (node === 1144) return 0.20166770295642045;
  if (node === 571) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree5(features, 1143);
    } else if (val <= 708.5000000000001) {
      return calculateTree5(features, 1143);
    } else {
      return calculateTree5(features, 1144);
    }
  }
  if (node === 1145) return -0.44356471366089073;
  if (node === 1146) return 0.015603244446183643;
  if (node === 572) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree5(features, 1145);
    } else if (val <= 808.5000000000001) {
      return calculateTree5(features, 1145);
    } else {
      return calculateTree5(features, 1146);
    }
  }
  if (node === 285) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree5(features, 571);
    } else if (val <= 781.0000000000001) {
      return calculateTree5(features, 571);
    } else {
      return calculateTree5(features, 572);
    }
  }
  if (node === 286) return -0.39971694649324563;
  if (node === 142) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree5(features, 285);
    } else if (val <= 261.50000000000006) {
      return calculateTree5(features, 285);
    } else {
      return calculateTree5(features, 286);
    }
  }
  if (node === 70) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree5(features, 141);
    } else if (val <= 86.50000000000001) {
      return calculateTree5(features, 141);
    } else {
      return calculateTree5(features, 142);
    }
  }
  if (node === 34) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree5(features, 69);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree5(features, 69);
    } else {
      return calculateTree5(features, 70);
    }
  }
  if (node === 16) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree5(features, 33);
    } else if (val <= 57.50000000000001) {
      return calculateTree5(features, 33);
    } else {
      return calculateTree5(features, 34);
    }
  }
  if (node === 7) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree5(features, 15);
    } else if (val <= 305.00000000000006) {
      return calculateTree5(features, 15);
    } else {
      return calculateTree5(features, 16);
    }
  }
  if (node === 143) return -0.35154452370707395;
  if (node === 144) return 0.40195822008375276;
  if (node === 71) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree5(features, 143);
    } else if (val <= 28.500000000000004) {
      return calculateTree5(features, 143);
    } else {
      return calculateTree5(features, 144);
    }
  }
  if (node === 145) return -0.37775190096678585;
  if (node === 587) return 0.4374385378227213;
  if (node === 588) return -0.3857666954616701;
  if (node === 293) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree5(features, 587);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree5(features, 587);
    } else {
      return calculateTree5(features, 588);
    }
  }
  if (node === 589) return -0.4139345358239362;
  if (node === 590) return 0.09607140782281391;
  if (node === 294) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree5(features, 589);
    } else if (val <= 900.5000000000001) {
      return calculateTree5(features, 589);
    } else {
      return calculateTree5(features, 590);
    }
  }
  if (node === 146) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree5(features, 293);
    } else if (val <= 212.50000000000003) {
      return calculateTree5(features, 293);
    } else {
      return calculateTree5(features, 294);
    }
  }
  if (node === 72) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree5(features, 145);
    } else if (val <= 191.50000000000003) {
      return calculateTree5(features, 145);
    } else {
      return calculateTree5(features, 146);
    }
  }
  if (node === 35) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree5(features, 71);
    } else if (val <= 131.50000000000003) {
      return calculateTree5(features, 71);
    } else {
      return calculateTree5(features, 72);
    }
  }
  if (node === 36) return -0.17043637392267932;
  if (node === 17) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree5(features, 35);
    } else if (val <= 911.0000000000001) {
      return calculateTree5(features, 35);
    } else {
      return calculateTree5(features, 36);
    }
  }
  if (node === 18) return 0.31437030691653944;
  if (node === 8) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree5(features, 17);
    } else if (val <= 241.50000000000003) {
      return calculateTree5(features, 17);
    } else {
      return calculateTree5(features, 18);
    }
  }
  if (node === 3) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree5(features, 7);
    } else if (val <= 877.5000000000001) {
      return calculateTree5(features, 7);
    } else {
      return calculateTree5(features, 8);
    }
  }
  if (node === 4) return 0.31626114405841804;
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree5(features, 3);
    } else if (val <= 275.50000000000006) {
      return calculateTree5(features, 3);
    } else {
      return calculateTree5(features, 4);
    }
  }
  if (node === 5) return -0.23275839708083262;
  if (node === 6) return -0.00803019322636768;
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree5(features, 5);
    } else if (val <= 291.50000000000006) {
      return calculateTree5(features, 5);
    } else {
      return calculateTree5(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree5(features, 1);
    } else if (val <= 282.50000000000006) {
      return calculateTree5(features, 1);
    } else {
      return calculateTree5(features, 2);
    }
  }
}
function calculateTree6(features, node = 0) {
  if (node === 127) return -0.01984669913308348;
  if (node === 515) return -0.16489058325197933;
  if (node === 1033) return -0.5147867792699086;
  if (node === 1034) return 0.33213240800294874;
  if (node === 516) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree6(features, 1033);
    } else if (val <= 34.00000000000001) {
      return calculateTree6(features, 1033);
    } else {
      return calculateTree6(features, 1034);
    }
  }
  if (node === 257) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree6(features, 515);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree6(features, 515);
    } else {
      return calculateTree6(features, 516);
    }
  }
  if (node === 258) return -0.013496202123932356;
  if (node === 128) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree6(features, 257);
    } else if (val <= 662.5000000000001) {
      return calculateTree6(features, 257);
    } else {
      return calculateTree6(features, 258);
    }
  }
  if (node === 63) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree6(features, 127);
    } else if (val <= 647.0000000000001) {
      return calculateTree6(features, 127);
    } else {
      return calculateTree6(features, 128);
    }
  }
  if (node === 64) return 0.475723195556315;
  if (node === 31) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree6(features, 63);
    } else if (val <= 834.5000000000001) {
      return calculateTree6(features, 63);
    } else {
      return calculateTree6(features, 64);
    }
  }
  if (node === 527) return 0.39785816573424126;
  if (node === 528) return -0.32694218758553545;
  if (node === 263) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree6(features, 527);
    } else if (val <= 39.50000000000001) {
      return calculateTree6(features, 527);
    } else {
      return calculateTree6(features, 528);
    }
  }
  if (node === 264) return 0.2300259595390915;
  if (node === 131) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree6(features, 263);
    } else if (val <= 305.00000000000006) {
      return calculateTree6(features, 263);
    } else {
      return calculateTree6(features, 264);
    }
  }
  if (node === 132) return -0.3458802244408315;
  if (node === 65) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree6(features, 131);
    } else if (val <= 441.00000000000006) {
      return calculateTree6(features, 131);
    } else {
      return calculateTree6(features, 132);
    }
  }
  if (node === 535) return 0.20442185688099554;
  if (node === 536) return -0.3228216953604562;
  if (node === 267) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree6(features, 535);
    } else if (val <= 225.50000000000003) {
      return calculateTree6(features, 535);
    } else {
      return calculateTree6(features, 536);
    }
  }
  if (node === 537) return -0.47908041643529997;
  if (node === 538) return 0.054031470265207875;
  if (node === 268) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree6(features, 537);
    } else if (val <= 39.50000000000001) {
      return calculateTree6(features, 537);
    } else {
      return calculateTree6(features, 538);
    }
  }
  if (node === 133) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree6(features, 267);
    } else if (val <= 868.0000000000001) {
      return calculateTree6(features, 267);
    } else {
      return calculateTree6(features, 268);
    }
  }
  if (node === 269) return 0.21099445774635786;
  if (node === 270) return -0.3686230131818797;
  if (node === 134) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree6(features, 269);
    } else if (val <= 969.5000000000001) {
      return calculateTree6(features, 269);
    } else {
      return calculateTree6(features, 270);
    }
  }
  if (node === 66) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree6(features, 133);
    } else if (val <= 937.5000000000001) {
      return calculateTree6(features, 133);
    } else {
      return calculateTree6(features, 134);
    }
  }
  if (node === 32) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree6(features, 65);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree6(features, 65);
    } else {
      return calculateTree6(features, 66);
    }
  }
  if (node === 15) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree6(features, 31);
    } else if (val <= 835.5000000000001) {
      return calculateTree6(features, 31);
    } else {
      return calculateTree6(features, 32);
    }
  }
  if (node === 33) return -0.49041253154618547;
  if (node === 34) return 0.30691862785531887;
  if (node === 16) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree6(features, 33);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree6(features, 33);
    } else {
      return calculateTree6(features, 34);
    }
  }
  if (node === 7) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree6(features, 15);
    } else if (val <= 905.5000000000001) {
      return calculateTree6(features, 15);
    } else {
      return calculateTree6(features, 16);
    }
  }
  if (node === 35) return -0.2042342721649871;
  if (node === 36) return 0.04382507829143884;
  if (node === 17) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree6(features, 35);
    } else if (val <= 934.0000000000001) {
      return calculateTree6(features, 35);
    } else {
      return calculateTree6(features, 36);
    }
  }
  if (node === 151) return 0.010782341623835205;
  if (node === 152) return 0.3997603062692927;
  if (node === 75) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree6(features, 151);
    } else if (val <= 672.0000000000001) {
      return calculateTree6(features, 151);
    } else {
      return calculateTree6(features, 152);
    }
  }
  if (node === 153) return -0.3193236360253317;
  if (node === 154) return -0.002800599614695917;
  if (node === 76) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree6(features, 153);
    } else if (val <= 808.5000000000001) {
      return calculateTree6(features, 153);
    } else {
      return calculateTree6(features, 154);
    }
  }
  if (node === 37) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree6(features, 75);
    } else if (val <= 675.0000000000001) {
      return calculateTree6(features, 75);
    } else {
      return calculateTree6(features, 76);
    }
  }
  if (node === 38) return -0.29587554197723764;
  if (node === 18) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree6(features, 37);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree6(features, 37);
    } else {
      return calculateTree6(features, 38);
    }
  }
  if (node === 8) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree6(features, 17);
    } else if (val <= 429.00000000000006) {
      return calculateTree6(features, 17);
    } else {
      return calculateTree6(features, 18);
    }
  }
  if (node === 3) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree6(features, 7);
    } else if (val <= 908.5000000000001) {
      return calculateTree6(features, 7);
    } else {
      return calculateTree6(features, 8);
    }
  }
  if (node === 19) return -0.3924185684318287;
  if (node === 20) return 0.24111406961679208;
  if (node === 9) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree6(features, 19);
    } else if (val <= 283.50000000000006) {
      return calculateTree6(features, 19);
    } else {
      return calculateTree6(features, 20);
    }
  }
  if (node === 21) return 0.3490088185384507;
  if (node === 22) return -0.37940190094781706;
  if (node === 10) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree6(features, 21);
    } else if (val <= 181.50000000000003) {
      return calculateTree6(features, 21);
    } else {
      return calculateTree6(features, 22);
    }
  }
  if (node === 4) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree6(features, 9);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree6(features, 9);
    } else {
      return calculateTree6(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree6(features, 3);
    } else if (val <= 993.5000000000001) {
      return calculateTree6(features, 3);
    } else {
      return calculateTree6(features, 4);
    }
  }
  if (node === 11) return 0.31434291588646196;
  if (node === 12) return -0.46124907978850527;
  if (node === 5) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree6(features, 11);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree6(features, 11);
    } else {
      return calculateTree6(features, 12);
    }
  }
  if (node === 6) return 0.013407518597352038;
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree6(features, 5);
    } else if (val <= 3.5000000000000004) {
      return calculateTree6(features, 5);
    } else {
      return calculateTree6(features, 6);
    }
  }
  if (node === 0) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree6(features, 1);
    } else if (val <= 1.5000000000000002) {
      return calculateTree6(features, 1);
    } else {
      return calculateTree6(features, 2);
    }
  }
}
function calculateTree7(features, node = 0) {
  if (node === 3) return 0.010588660810426942;
  if (node === 9) return -0.4775616072693169;
  if (node === 10) return 0.4419626228796869;
  if (node === 4) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree7(features, 9);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree7(features, 9);
    } else {
      return calculateTree7(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree7(features, 3);
    } else if (val <= 361.50000000000006) {
      return calculateTree7(features, 3);
    } else {
      return calculateTree7(features, 4);
    }
  }
  if (node === 11) return -0.05808085809824648;
  if (node === 12) return -0.32963936051384074;
  if (node === 5) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree7(features, 11);
    } else if (val <= 405.00000000000006) {
      return calculateTree7(features, 11);
    } else {
      return calculateTree7(features, 12);
    }
  }
  if (node === 13) return 0.09625089984667382;
  if (node === 59) return -0.21752010146745157;
  if (node === 121) return 0.42026695829176125;
  if (node === 122) return -0.414354838758038;
  if (node === 60) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree7(features, 121);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree7(features, 121);
    } else {
      return calculateTree7(features, 122);
    }
  }
  if (node === 29) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree7(features, 59);
    } else if (val <= 887.0000000000001) {
      return calculateTree7(features, 59);
    } else {
      return calculateTree7(features, 60);
    }
  }
  if (node === 1983) return -0.18072541768558978;
  if (node === 1984) return 0.2306628181305336;
  if (node === 991) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree7(features, 1983);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree7(features, 1983);
    } else {
      return calculateTree7(features, 1984);
    }
  }
  if (node === 1985) return -0.00818821764088293;
  if (node === 1986) return -0.1178222270377097;
  if (node === 992) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree7(features, 1985);
    } else if (val <= 225.50000000000003) {
      return calculateTree7(features, 1985);
    } else {
      return calculateTree7(features, 1986);
    }
  }
  if (node === 495) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree7(features, 991);
    } else if (val <= 525.0000000000001) {
      return calculateTree7(features, 991);
    } else {
      return calculateTree7(features, 992);
    }
  }
  if (node === 1987) return -0.4262993885868183;
  if (node === 1988) return 0.1842906394945412;
  if (node === 993) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree7(features, 1987);
    } else if (val <= 966.0000000000001) {
      return calculateTree7(features, 1987);
    } else {
      return calculateTree7(features, 1988);
    }
  }
  if (node === 1989) return 0.282403804410263;
  if (node === 1990) return -0.2285892631511648;
  if (node === 994) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree7(features, 1989);
    } else if (val <= 963.5000000000001) {
      return calculateTree7(features, 1989);
    } else {
      return calculateTree7(features, 1990);
    }
  }
  if (node === 496) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree7(features, 993);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree7(features, 993);
    } else {
      return calculateTree7(features, 994);
    }
  }
  if (node === 247) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree7(features, 495);
    } else if (val <= 957.0000000000001) {
      return calculateTree7(features, 495);
    } else {
      return calculateTree7(features, 496);
    }
  }
  if (node === 995) return -0.2184875810060338;
  if (node === 996) return 0.21316659658173548;
  if (node === 497) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree7(features, 995);
    } else if (val <= 576.5000000000001) {
      return calculateTree7(features, 995);
    } else {
      return calculateTree7(features, 996);
    }
  }
  if (node === 1995) return -0.04157961828832645;
  if (node === 1996) return -0.4123199719111801;
  if (node === 997) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree7(features, 1995);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree7(features, 1995);
    } else {
      return calculateTree7(features, 1996);
    }
  }
  if (node === 1997) return 0.1441093996693562;
  if (node === 1998) return -0.027061358103044178;
  if (node === 998) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree7(features, 1997);
    } else if (val <= 821.0000000000001) {
      return calculateTree7(features, 1997);
    } else {
      return calculateTree7(features, 1998);
    }
  }
  if (node === 498) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree7(features, 997);
    } else if (val <= 693.0000000000001) {
      return calculateTree7(features, 997);
    } else {
      return calculateTree7(features, 998);
    }
  }
  if (node === 248) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree7(features, 497);
    } else if (val <= 667.5000000000001) {
      return calculateTree7(features, 497);
    } else {
      return calculateTree7(features, 498);
    }
  }
  if (node === 123) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree7(features, 247);
    } else if (val <= 391.00000000000006) {
      return calculateTree7(features, 247);
    } else {
      return calculateTree7(features, 248);
    }
  }
  if (node === 249) return -0.3119580510718821;
  if (node === 250) return -0.026987918159832567;
  if (node === 124) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree7(features, 249);
    } else if (val <= 556.0000000000001) {
      return calculateTree7(features, 249);
    } else {
      return calculateTree7(features, 250);
    }
  }
  if (node === 61) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree7(features, 123);
    } else if (val <= 507.00000000000006) {
      return calculateTree7(features, 123);
    } else {
      return calculateTree7(features, 124);
    }
  }
  if (node === 251) return -0.3661175103167743;
  if (node === 505) return 0.3114354271101656;
  if (node === 506) return -0.4383393605263097;
  if (node === 252) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree7(features, 505);
    } else if (val <= 896.5000000000001) {
      return calculateTree7(features, 505);
    } else {
      return calculateTree7(features, 506);
    }
  }
  if (node === 125) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree7(features, 251);
    } else if (val <= 799.5000000000001) {
      return calculateTree7(features, 251);
    } else {
      return calculateTree7(features, 252);
    }
  }
  if (node === 507) return 0.25419120880451634;
  if (node === 508) return 0.7698480880615118;
  if (node === 253) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree7(features, 507);
    } else if (val <= 551.5000000000001) {
      return calculateTree7(features, 507);
    } else {
      return calculateTree7(features, 508);
    }
  }
  if (node === 509) return -0.28666408191032994;
  if (node === 510) return 0.320736709083122;
  if (node === 254) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree7(features, 509);
    } else if (val <= 905.5000000000001) {
      return calculateTree7(features, 509);
    } else {
      return calculateTree7(features, 510);
    }
  }
  if (node === 126) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree7(features, 253);
    } else if (val <= 582.5000000000001) {
      return calculateTree7(features, 253);
    } else {
      return calculateTree7(features, 254);
    }
  }
  if (node === 62) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree7(features, 125);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree7(features, 125);
    } else {
      return calculateTree7(features, 126);
    }
  }
  if (node === 30) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree7(features, 61);
    } else if (val <= 963.0000000000001) {
      return calculateTree7(features, 61);
    } else {
      return calculateTree7(features, 62);
    }
  }
  if (node === 14) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree7(features, 29);
    } else if (val <= 470.50000000000006) {
      return calculateTree7(features, 29);
    } else {
      return calculateTree7(features, 30);
    }
  }
  if (node === 6) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree7(features, 13);
    } else if (val <= 427.50000000000006) {
      return calculateTree7(features, 13);
    } else {
      return calculateTree7(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree7(features, 5);
    } else if (val <= 414.50000000000006) {
      return calculateTree7(features, 5);
    } else {
      return calculateTree7(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree7(features, 1);
    } else if (val <= 365.50000000000006) {
      return calculateTree7(features, 1);
    } else {
      return calculateTree7(features, 2);
    }
  }
}
function calculateTree8(features, node = 0) {
  if (node === 15) return 0.015408124837623417;
  if (node === 135) return 0.03475326848208564;
  if (node === 1095) return 0.15441863849033868;
  if (node === 1096) return 0.4999818601024177;
  if (node === 547) {
    const val = features['is_preference_sports'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree8(features, 1095);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree8(features, 1095);
    } else {
      return calculateTree8(features, 1096);
    }
  }
  if (node === 548) return 0.07335064829176753;
  if (node === 273) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree8(features, 547);
    } else if (val <= 234.50000000000003) {
      return calculateTree8(features, 547);
    } else {
      return calculateTree8(features, 548);
    }
  }
  if (node === 549) return 0.13615455644851804;
  if (node === 550) return 0.35915381214618575;
  if (node === 274) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree8(features, 549);
    } else if (val <= 660.0000000000001) {
      return calculateTree8(features, 549);
    } else {
      return calculateTree8(features, 550);
    }
  }
  if (node === 136) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree8(features, 273);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree8(features, 273);
    } else {
      return calculateTree8(features, 274);
    }
  }
  if (node === 67) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree8(features, 135);
    } else if (val <= 155.50000000000003) {
      return calculateTree8(features, 135);
    } else {
      return calculateTree8(features, 136);
    }
  }
  if (node === 68) return 0.03222811712727939;
  if (node === 33) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree8(features, 67);
    } else if (val <= 697.0000000000001) {
      return calculateTree8(features, 67);
    } else {
      return calculateTree8(features, 68);
    }
  }
  if (node === 34) return 0.004155819403879724;
  if (node === 16) {
    const val = features['is_preference_women_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree8(features, 33);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree8(features, 33);
    } else {
      return calculateTree8(features, 34);
    }
  }
  if (node === 7) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree8(features, 15);
    } else if (val <= 4.500000000000001) {
      return calculateTree8(features, 15);
    } else {
      return calculateTree8(features, 16);
    }
  }
  if (node === 17) return 0.012486436744242593;
  if (node === 18) return -0.01808946245363113;
  if (node === 8) {
    const val = features['is_preference_personal_care'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree8(features, 17);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree8(features, 17);
    } else {
      return calculateTree8(features, 18);
    }
  }
  if (node === 3) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree8(features, 7);
    } else if (val <= 29.500000000000004) {
      return calculateTree8(features, 7);
    } else {
      return calculateTree8(features, 8);
    }
  }
  if (node === 39) return -0.10350284776394866;
  if (node === 81) return 0.15556792246974313;
  if (node === 165) return 0.03711378206808486;
  if (node === 667) return -0.005637661711784395;
  if (node === 668) return 0.14783983491062885;
  if (node === 333) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree8(features, 667);
    } else if (val <= 28.500000000000004) {
      return calculateTree8(features, 667);
    } else {
      return calculateTree8(features, 668);
    }
  }
  if (node === 334) return -0.031994659186539195;
  if (node === 166) {
    const val = features['is_preference_sports'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree8(features, 333);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree8(features, 333);
    } else {
      return calculateTree8(features, 334);
    }
  }
  if (node === 82) {
    const val = features['is_preference_household'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree8(features, 165);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree8(features, 165);
    } else {
      return calculateTree8(features, 166);
    }
  }
  if (node === 40) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree8(features, 81);
    } else if (val <= 34.50000000000001) {
      return calculateTree8(features, 81);
    } else {
      return calculateTree8(features, 82);
    }
  }
  if (node === 19) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree8(features, 39);
    } else if (val <= 20.500000000000004) {
      return calculateTree8(features, 39);
    } else {
      return calculateTree8(features, 40);
    }
  }
  if (node === 83) return 0.19273669544960462;
  if (node === 339) return 0.12271883395705793;
  if (node === 340) return -0.04194060676031471;
  if (node === 169) {
    const val = features['is_preference_personal_care'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree8(features, 339);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree8(features, 339);
    } else {
      return calculateTree8(features, 340);
    }
  }
  if (node === 170) return 0.05626249268359239;
  if (node === 84) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree8(features, 169);
    } else if (val <= 673.0000000000001) {
      return calculateTree8(features, 169);
    } else {
      return calculateTree8(features, 170);
    }
  }
  if (node === 41) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree8(features, 83);
    } else if (val <= 23.500000000000004) {
      return calculateTree8(features, 83);
    } else {
      return calculateTree8(features, 84);
    }
  }
  if (node === 42) return 0.1937591903755452;
  if (node === 20) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree8(features, 41);
    } else if (val <= 60.50000000000001) {
      return calculateTree8(features, 41);
    } else {
      return calculateTree8(features, 42);
    }
  }
  if (node === 9) {
    const val = features['is_preference_kids'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree8(features, 19);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree8(features, 19);
    } else {
      return calculateTree8(features, 20);
    }
  }
  if (node === 10) return -0.012623784826764656;
  if (node === 4) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree8(features, 9);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree8(features, 9);
    } else {
      return calculateTree8(features, 10);
    }
  }
  if (node === 1) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree8(features, 3);
    } else if (val <= 26.500000000000004) {
      return calculateTree8(features, 3);
    } else {
      return calculateTree8(features, 4);
    }
  }
  if (node === 23) return -0.004779927480331312;
  if (node === 49) return -0.04123093527495291;
  if (node === 50) return 0.805836606089051;
  if (node === 24) {
    const val = features['is_preference_sports'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree8(features, 49);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree8(features, 49);
    } else {
      return calculateTree8(features, 50);
    }
  }
  if (node === 11) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree8(features, 23);
    } else if (val <= 908.0000000000001) {
      return calculateTree8(features, 23);
    } else {
      return calculateTree8(features, 24);
    }
  }
  if (node === 12) return -0.015262586891827223;
  if (node === 5) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree8(features, 11);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree8(features, 11);
    } else {
      return calculateTree8(features, 12);
    }
  }
  if (node === 55) return 0.003837243195405929;
  if (node === 113) return 0.004120324220290138;
  if (node === 114) return 0.04326829094137771;
  if (node === 56) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree8(features, 113);
    } else if (val <= 15.500000000000002) {
      return calculateTree8(features, 113);
    } else {
      return calculateTree8(features, 114);
    }
  }
  if (node === 27) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree8(features, 55);
    } else if (val <= 3.5000000000000004) {
      return calculateTree8(features, 55);
    } else {
      return calculateTree8(features, 56);
    }
  }
  if (node === 28) return -0.005218567395700582;
  if (node === 13) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree8(features, 27);
    } else if (val <= 34.50000000000001) {
      return calculateTree8(features, 27);
    } else {
      return calculateTree8(features, 28);
    }
  }
  if (node === 14) return -0.02720461238706919;
  if (node === 6) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree8(features, 13);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree8(features, 13);
    } else {
      return calculateTree8(features, 14);
    }
  }
  if (node === 2) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree8(features, 5);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree8(features, 5);
    } else {
      return calculateTree8(features, 6);
    }
  }
  if (node === 0) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree8(features, 1);
    } else if (val <= 1.5000000000000002) {
      return calculateTree8(features, 1);
    } else {
      return calculateTree8(features, 2);
    }
  }
}
function calculateTree9(features, node = 0) {
  if (node === 127) return 0.05180589441471395;
  if (node === 257) return -0.38031604015709897;
  if (node === 258) return 0.13136068580907892;
  if (node === 128) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree9(features, 257);
    } else if (val <= 393.00000000000006) {
      return calculateTree9(features, 257);
    } else {
      return calculateTree9(features, 258);
    }
  }
  if (node === 63) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree9(features, 127);
    } else if (val <= 358.50000000000006) {
      return calculateTree9(features, 127);
    } else {
      return calculateTree9(features, 128);
    }
  }
  if (node === 129) return 0.5742271884556368;
  if (node === 130) return -0.48897159554138925;
  if (node === 64) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree9(features, 129);
    } else if (val <= 205.50000000000003) {
      return calculateTree9(features, 129);
    } else {
      return calculateTree9(features, 130);
    }
  }
  if (node === 31) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree9(features, 63);
    } else if (val <= 409.50000000000006) {
      return calculateTree9(features, 63);
    } else {
      return calculateTree9(features, 64);
    }
  }
  if (node === 65) return -0.34160648330474014;
  if (node === 133) return -0.028177403453006605;
  if (node === 134) return -0.46846986786060624;
  if (node === 66) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree9(features, 133);
    } else if (val <= 261.50000000000006) {
      return calculateTree9(features, 133);
    } else {
      return calculateTree9(features, 134);
    }
  }
  if (node === 32) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree9(features, 65);
    } else if (val <= 432.00000000000006) {
      return calculateTree9(features, 65);
    } else {
      return calculateTree9(features, 66);
    }
  }
  if (node === 15) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree9(features, 31);
    } else if (val <= 417.00000000000006) {
      return calculateTree9(features, 31);
    } else {
      return calculateTree9(features, 32);
    }
  }
  if (node === 135) return -0.26659604617672183;
  if (node === 136) return 0.08727676016244867;
  if (node === 67) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree9(features, 135);
    } else if (val <= 28.500000000000004) {
      return calculateTree9(features, 135);
    } else {
      return calculateTree9(features, 136);
    }
  }
  if (node === 68) return -0.27382228559767374;
  if (node === 33) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree9(features, 67);
    } else if (val <= 150.50000000000003) {
      return calculateTree9(features, 67);
    } else {
      return calculateTree9(features, 68);
    }
  }
  if (node === 69) return 0.3987962176127304;
  if (node === 141) return -0.16780157469593904;
  if (node === 1143) return -0.39105073212702895;
  if (node === 1144) return 0.25760782663803794;
  if (node === 571) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree9(features, 1143);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree9(features, 1143);
    } else {
      return calculateTree9(features, 1144);
    }
  }
  if (node === 572) return -0.20544056730834148;
  if (node === 285) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree9(features, 571);
    } else if (val <= 253.50000000000003) {
      return calculateTree9(features, 571);
    } else {
      return calculateTree9(features, 572);
    }
  }
  if (node === 286) return 0.3166817067875917;
  if (node === 142) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree9(features, 285);
    } else if (val <= 835.5000000000001) {
      return calculateTree9(features, 285);
    } else {
      return calculateTree9(features, 286);
    }
  }
  if (node === 70) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree9(features, 141);
    } else if (val <= 241.50000000000003) {
      return calculateTree9(features, 141);
    } else {
      return calculateTree9(features, 142);
    }
  }
  if (node === 34) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree9(features, 69);
    } else if (val <= 212.50000000000003) {
      return calculateTree9(features, 69);
    } else {
      return calculateTree9(features, 70);
    }
  }
  if (node === 16) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree9(features, 33);
    } else if (val <= 191.50000000000003) {
      return calculateTree9(features, 33);
    } else {
      return calculateTree9(features, 34);
    }
  }
  if (node === 7) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree9(features, 15);
    } else if (val <= 708.5000000000001) {
      return calculateTree9(features, 15);
    } else {
      return calculateTree9(features, 16);
    }
  }
  if (node === 17) return -0.17604209452758976;
  if (node === 18) return 0.0056712452452040134;
  if (node === 8) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree9(features, 17);
    } else if (val <= 291.50000000000006) {
      return calculateTree9(features, 17);
    } else {
      return calculateTree9(features, 18);
    }
  }
  if (node === 3) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree9(features, 7);
    } else if (val <= 282.50000000000006) {
      return calculateTree9(features, 7);
    } else {
      return calculateTree9(features, 8);
    }
  }
  if (node === 19) return -0.005743010182192967;
  if (node === 41) return -0.05317926213903326;
  if (node === 42) return -0.33412337778160567;
  if (node === 20) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree9(features, 41);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree9(features, 41);
    } else {
      return calculateTree9(features, 42);
    }
  }
  if (node === 9) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree9(features, 19);
    } else if (val <= 975.5000000000001) {
      return calculateTree9(features, 19);
    } else {
      return calculateTree9(features, 20);
    }
  }
  if (node === 87) return 0.3140742216827356;
  if (node === 177) return -0.4223579635701762;
  if (node === 178) return 0.12204532776000238;
  if (node === 88) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree9(features, 177);
    } else if (val <= 283.50000000000006) {
      return calculateTree9(features, 177);
    } else {
      return calculateTree9(features, 178);
    }
  }
  if (node === 43) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree9(features, 87);
    } else if (val <= 983.5000000000001) {
      return calculateTree9(features, 87);
    } else {
      return calculateTree9(features, 88);
    }
  }
  if (node === 44) return -0.3616499865338906;
  if (node === 21) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree9(features, 43);
    } else if (val <= 675.0000000000001) {
      return calculateTree9(features, 43);
    } else {
      return calculateTree9(features, 44);
    }
  }
  if (node === 45) return -0.41003917013982466;
  if (node === 187) return -0.41652762107385605;
  if (node === 188) return 0.2169893883394114;
  if (node === 93) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree9(features, 187);
    } else if (val <= 148.50000000000003) {
      return calculateTree9(features, 187);
    } else {
      return calculateTree9(features, 188);
    }
  }
  if (node === 189) return 0.29712604629945355;
  if (node === 190) return -0.34986664778599336;
  if (node === 94) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree9(features, 189);
    } else if (val <= 283.50000000000006) {
      return calculateTree9(features, 189);
    } else {
      return calculateTree9(features, 190);
    }
  }
  if (node === 46) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree9(features, 93);
    } else if (val <= 992.5000000000001) {
      return calculateTree9(features, 93);
    } else {
      return calculateTree9(features, 94);
    }
  }
  if (node === 22) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree9(features, 45);
    } else if (val <= 983.5000000000001) {
      return calculateTree9(features, 45);
    } else {
      return calculateTree9(features, 46);
    }
  }
  if (node === 10) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree9(features, 21);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree9(features, 21);
    } else {
      return calculateTree9(features, 22);
    }
  }
  if (node === 4) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree9(features, 9);
    } else if (val <= 982.5000000000001) {
      return calculateTree9(features, 9);
    } else {
      return calculateTree9(features, 10);
    }
  }
  if (node === 1) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree9(features, 3);
    } else if (val <= 1.5000000000000002) {
      return calculateTree9(features, 3);
    } else {
      return calculateTree9(features, 4);
    }
  }
  if (node === 2) return -0.3731041663471993;
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree9(features, 1);
    } else if (val <= 994.5000000000001) {
      return calculateTree9(features, 1);
    } else {
      return calculateTree9(features, 2);
    }
  }
}
function calculateTree10(features, node = 0) {
  if (node === 127) return 0.3292135164298605;
  if (node === 128) return -0.4224127284248512;
  if (node === 63) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree10(features, 127);
    } else if (val <= 166.50000000000003) {
      return calculateTree10(features, 127);
    } else {
      return calculateTree10(features, 128);
    }
  }
  if (node === 129) return -0.4263854170042535;
  if (node === 130) return 0.20247090998618358;
  if (node === 64) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree10(features, 129);
    } else if (val <= 186.50000000000003) {
      return calculateTree10(features, 129);
    } else {
      return calculateTree10(features, 130);
    }
  }
  if (node === 31) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree10(features, 63);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree10(features, 63);
    } else {
      return calculateTree10(features, 64);
    }
  }
  if (node === 131) return -0.08756592560342984;
  if (node === 132) return -0.3951125960004176;
  if (node === 65) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree10(features, 131);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree10(features, 131);
    } else {
      return calculateTree10(features, 132);
    }
  }
  if (node === 133) return 0.11568043765435335;
  if (node === 134) return -0.18558187829231915;
  if (node === 66) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree10(features, 133);
    } else if (val <= 551.5000000000001) {
      return calculateTree10(features, 133);
    } else {
      return calculateTree10(features, 134);
    }
  }
  if (node === 32) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree10(features, 65);
    } else if (val <= 338.50000000000006) {
      return calculateTree10(features, 65);
    } else {
      return calculateTree10(features, 66);
    }
  }
  if (node === 15) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree10(features, 31);
    } else if (val <= 278.50000000000006) {
      return calculateTree10(features, 31);
    } else {
      return calculateTree10(features, 32);
    }
  }
  if (node === 67) return -0.3911634230549453;
  if (node === 137) return 0.12380180476292217;
  if (node === 138) return -0.27491954445463107;
  if (node === 68) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree10(features, 137);
    } else if (val <= 507.50000000000006) {
      return calculateTree10(features, 137);
    } else {
      return calculateTree10(features, 138);
    }
  }
  if (node === 33) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree10(features, 67);
    } else if (val <= 205.50000000000003) {
      return calculateTree10(features, 67);
    } else {
      return calculateTree10(features, 68);
    }
  }
  if (node === 34) return -0.3325758565214793;
  if (node === 16) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree10(features, 33);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree10(features, 33);
    } else {
      return calculateTree10(features, 34);
    }
  }
  if (node === 7) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree10(features, 15);
    } else if (val <= 34.00000000000001) {
      return calculateTree10(features, 15);
    } else {
      return calculateTree10(features, 16);
    }
  }
  if (node === 17) return 0.2182028387531324;
  if (node === 18) return -0.036085575384074725;
  if (node === 8) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree10(features, 17);
    } else if (val <= 662.5000000000001) {
      return calculateTree10(features, 17);
    } else {
      return calculateTree10(features, 18);
    }
  }
  if (node === 3) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree10(features, 7);
    } else if (val <= 649.5000000000001) {
      return calculateTree10(features, 7);
    } else {
      return calculateTree10(features, 8);
    }
  }
  if (node === 159) return -0.21100472380425905;
  if (node === 321) return 0.04315198955805247;
  if (node === 322) return 0.42980191852020716;
  if (node === 160) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree10(features, 321);
    } else if (val <= 391.50000000000006) {
      return calculateTree10(features, 321);
    } else {
      return calculateTree10(features, 322);
    }
  }
  if (node === 79) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree10(features, 159);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree10(features, 159);
    } else {
      return calculateTree10(features, 160);
    }
  }
  if (node === 80) return -0.389402591943468;
  if (node === 39) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree10(features, 79);
    } else if (val <= 525.0000000000001) {
      return calculateTree10(features, 79);
    } else {
      return calculateTree10(features, 80);
    }
  }
  if (node === 81) return 0.7770044704174349;
  if (node === 82) return -0.41072897564672284;
  if (node === 40) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree10(features, 81);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree10(features, 81);
    } else {
      return calculateTree10(features, 82);
    }
  }
  if (node === 19) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree10(features, 39);
    } else if (val <= 572.0000000000001) {
      return calculateTree10(features, 39);
    } else {
      return calculateTree10(features, 40);
    }
  }
  if (node === 20) return -0.28124853151159074;
  if (node === 9) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree10(features, 19);
    } else if (val <= 589.5000000000001) {
      return calculateTree10(features, 19);
    } else {
      return calculateTree10(features, 20);
    }
  }
  if (node === 175) return 0.0505587383874708;
  if (node === 176) return -0.3308151360796307;
  if (node === 87) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree10(features, 175);
    } else if (val <= 311.50000000000006) {
      return calculateTree10(features, 175);
    } else {
      return calculateTree10(features, 176);
    }
  }
  if (node === 88) return -0.018805520645980976;
  if (node === 43) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree10(features, 87);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree10(features, 87);
    } else {
      return calculateTree10(features, 88);
    }
  }
  if (node === 89) return -0.3009236697590866;
  if (node === 363) return -0.26895949235284355;
  if (node === 364) return 0.41381814848492793;
  if (node === 181) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree10(features, 363);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree10(features, 363);
    } else {
      return calculateTree10(features, 364);
    }
  }
  if (node === 731) return 0.6087892358350628;
  if (node === 732) return -0.4860314784462255;
  if (node === 365) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree10(features, 731);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree10(features, 731);
    } else {
      return calculateTree10(features, 732);
    }
  }
  if (node === 366) return 0.030005674074070152;
  if (node === 182) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree10(features, 365);
    } else if (val <= 225.50000000000003) {
      return calculateTree10(features, 365);
    } else {
      return calculateTree10(features, 366);
    }
  }
  if (node === 90) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree10(features, 181);
    } else if (val <= 320.00000000000006) {
      return calculateTree10(features, 181);
    } else {
      return calculateTree10(features, 182);
    }
  }
  if (node === 44) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree10(features, 89);
    } else if (val <= 181.50000000000003) {
      return calculateTree10(features, 89);
    } else {
      return calculateTree10(features, 90);
    }
  }
  if (node === 21) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree10(features, 43);
    } else if (val <= 314.50000000000006) {
      return calculateTree10(features, 43);
    } else {
      return calculateTree10(features, 44);
    }
  }
  if (node === 22) return -0.009248559655309652;
  if (node === 10) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree10(features, 21);
    } else if (val <= 365.50000000000006) {
      return calculateTree10(features, 21);
    } else {
      return calculateTree10(features, 22);
    }
  }
  if (node === 4) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree10(features, 9);
    } else if (val <= 113.50000000000001) {
      return calculateTree10(features, 9);
    } else {
      return calculateTree10(features, 10);
    }
  }
  if (node === 1) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree10(features, 3);
    } else if (val <= 99.50000000000001) {
      return calculateTree10(features, 3);
    } else {
      return calculateTree10(features, 4);
    }
  }
  if (node === 2) return -0.35547355800804353;
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree10(features, 1);
    } else if (val <= 994.5000000000001) {
      return calculateTree10(features, 1);
    } else {
      return calculateTree10(features, 2);
    }
  }
}
function calculateTree11(features, node = 0) {
  if (node === 3) return 0.0020220718390169022;
  if (node === 4) return -0.07208279900723132;
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree11(features, 3);
    } else if (val <= 525.0000000000001) {
      return calculateTree11(features, 3);
    } else {
      return calculateTree11(features, 4);
    }
  }
  if (node === 47) return -0.4154023132957013;
  if (node === 48) return 0.17384757104005397;
  if (node === 23) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree11(features, 47);
    } else if (val <= 649.5000000000001) {
      return calculateTree11(features, 47);
    } else {
      return calculateTree11(features, 48);
    }
  }
  if (node === 24) return -0.419823441261652;
  if (node === 11) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree11(features, 23);
    } else if (val <= 657.5000000000001) {
      return calculateTree11(features, 23);
    } else {
      return calculateTree11(features, 24);
    }
  }
  if (node === 51) return 0.24948039475141578;
  if (node === 52) return -0.29846970638817316;
  if (node === 25) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree11(features, 51);
    } else if (val <= 649.5000000000001) {
      return calculateTree11(features, 51);
    } else {
      return calculateTree11(features, 52);
    }
  }
  if (node === 26) return 0.2724209513429727;
  if (node === 12) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree11(features, 25);
    } else if (val <= 657.5000000000001) {
      return calculateTree11(features, 25);
    } else {
      return calculateTree11(features, 26);
    }
  }
  if (node === 5) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree11(features, 11);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree11(features, 11);
    } else {
      return calculateTree11(features, 12);
    }
  }
  if (node === 55) return 0.21034396127047897;
  if (node === 56) return -0.3346508561456062;
  if (node === 27) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree11(features, 55);
    } else if (val <= 685.0000000000001) {
      return calculateTree11(features, 55);
    } else {
      return calculateTree11(features, 56);
    }
  }
  if (node === 57) return -0.3342515991473704;
  if (node === 117) return 0.24096921843134722;
  if (node === 118) return -0.34894596429430896;
  if (node === 58) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree11(features, 117);
    } else if (val <= 693.0000000000001) {
      return calculateTree11(features, 117);
    } else {
      return calculateTree11(features, 118);
    }
  }
  if (node === 28) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree11(features, 57);
    } else if (val <= 685.0000000000001) {
      return calculateTree11(features, 57);
    } else {
      return calculateTree11(features, 58);
    }
  }
  if (node === 13) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree11(features, 27);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree11(features, 27);
    } else {
      return calculateTree11(features, 28);
    }
  }
  if (node === 239) return 0.36318977644331624;
  if (node === 481) return -0.33593323853068097;
  if (node === 965) return 0.40066198083078336;
  if (node === 1933) return -0.3953696644183361;
  if (node === 1934) return 0.0909658431948145;
  if (node === 966) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree11(features, 1933);
    } else if (val <= 729.5000000000001) {
      return calculateTree11(features, 1933);
    } else {
      return calculateTree11(features, 1934);
    }
  }
  if (node === 482) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree11(features, 965);
    } else if (val <= 727.5000000000001) {
      return calculateTree11(features, 965);
    } else {
      return calculateTree11(features, 966);
    }
  }
  if (node === 240) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree11(features, 481);
    } else if (val <= 723.5000000000001) {
      return calculateTree11(features, 481);
    } else {
      return calculateTree11(features, 482);
    }
  }
  if (node === 119) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree11(features, 239);
    } else if (val <= 715.5000000000001) {
      return calculateTree11(features, 239);
    } else {
      return calculateTree11(features, 240);
    }
  }
  if (node === 241) return -0.03750652607442965;
  if (node === 242) return -0.34939312224927405;
  if (node === 120) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree11(features, 241);
    } else if (val <= 740.5000000000001) {
      return calculateTree11(features, 241);
    } else {
      return calculateTree11(features, 242);
    }
  }
  if (node === 59) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree11(features, 119);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree11(features, 119);
    } else {
      return calculateTree11(features, 120);
    }
  }
  if (node === 121) return 0.03966439859807203;
  if (node === 122) return 0.26684840763222106;
  if (node === 60) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree11(features, 121);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree11(features, 121);
    } else {
      return calculateTree11(features, 122);
    }
  }
  if (node === 29) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree11(features, 59);
    } else if (val <= 745.5000000000001) {
      return calculateTree11(features, 59);
    } else {
      return calculateTree11(features, 60);
    }
  }
  if (node === 123) return -0.31617531293088064;
  if (node === 499) return -0.0741457597401619;
  if (node === 500) return 0.3072950355525801;
  if (node === 249) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree11(features, 499);
    } else if (val <= 757.5000000000001) {
      return calculateTree11(features, 499);
    } else {
      return calculateTree11(features, 500);
    }
  }
  if (node === 1003) return -0.25977145245957206;
  if (node === 1004) return 0.16444943069875675;
  if (node === 501) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree11(features, 1003);
    } else if (val <= 774.0000000000001) {
      return calculateTree11(features, 1003);
    } else {
      return calculateTree11(features, 1004);
    }
  }
  if (node === 502) return -0.3829137765084851;
  if (node === 250) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree11(features, 501);
    } else if (val <= 777.5000000000001) {
      return calculateTree11(features, 501);
    } else {
      return calculateTree11(features, 502);
    }
  }
  if (node === 124) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree11(features, 249);
    } else if (val <= 762.5000000000001) {
      return calculateTree11(features, 249);
    } else {
      return calculateTree11(features, 250);
    }
  }
  if (node === 61) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree11(features, 123);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree11(features, 123);
    } else {
      return calculateTree11(features, 124);
    }
  }
  if (node === 125) return 0.16979589127507774;
  if (node === 253) return -0.3788613872080254;
  if (node === 509) return 0.06700061968073015;
  if (node === 510) return -0.005520983075936867;
  if (node === 254) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree11(features, 509);
    } else if (val <= 819.5000000000001) {
      return calculateTree11(features, 509);
    } else {
      return calculateTree11(features, 510);
    }
  }
  if (node === 126) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree11(features, 253);
    } else if (val <= 785.5000000000001) {
      return calculateTree11(features, 253);
    } else {
      return calculateTree11(features, 254);
    }
  }
  if (node === 62) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree11(features, 125);
    } else if (val <= 784.5000000000001) {
      return calculateTree11(features, 125);
    } else {
      return calculateTree11(features, 126);
    }
  }
  if (node === 30) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree11(features, 61);
    } else if (val <= 780.0000000000001) {
      return calculateTree11(features, 61);
    } else {
      return calculateTree11(features, 62);
    }
  }
  if (node === 14) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree11(features, 29);
    } else if (val <= 752.5000000000001) {
      return calculateTree11(features, 29);
    } else {
      return calculateTree11(features, 30);
    }
  }
  if (node === 6) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree11(features, 13);
    } else if (val <= 709.5000000000001) {
      return calculateTree11(features, 13);
    } else {
      return calculateTree11(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree11(features, 5);
    } else if (val <= 662.5000000000001) {
      return calculateTree11(features, 5);
    } else {
      return calculateTree11(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree11(features, 1);
    } else if (val <= 647.0000000000001) {
      return calculateTree11(features, 1);
    } else {
      return calculateTree11(features, 2);
    }
  }
}
function calculateTree12(features, node = 0) {
  if (node === 7) return 0.18838409157151995;
  if (node === 35) return -0.01855995874080269;
  if (node === 36) return -0.44181179187361863;
  if (node === 17) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree12(features, 35);
    } else if (val <= 8.500000000000002) {
      return calculateTree12(features, 35);
    } else {
      return calculateTree12(features, 36);
    }
  }
  if (node === 37) return 0.3316497414462289;
  if (node === 77) return -0.15760696933701804;
  if (node === 157) return 0.34470818575493506;
  if (node === 317) return -0.3783307653418485;
  if (node === 318) return 0.04767304260734241;
  if (node === 158) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree12(features, 317);
    } else if (val <= 36.50000000000001) {
      return calculateTree12(features, 317);
    } else {
      return calculateTree12(features, 318);
    }
  }
  if (node === 78) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree12(features, 157);
    } else if (val <= 30.500000000000004) {
      return calculateTree12(features, 157);
    } else {
      return calculateTree12(features, 158);
    }
  }
  if (node === 38) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree12(features, 77);
    } else if (val <= 28.500000000000004) {
      return calculateTree12(features, 77);
    } else {
      return calculateTree12(features, 78);
    }
  }
  if (node === 18) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree12(features, 37);
    } else if (val <= 16.500000000000004) {
      return calculateTree12(features, 37);
    } else {
      return calculateTree12(features, 38);
    }
  }
  if (node === 8) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree12(features, 17);
    } else if (val <= 14.500000000000002) {
      return calculateTree12(features, 17);
    } else {
      return calculateTree12(features, 18);
    }
  }
  if (node === 3) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree12(features, 7);
    } else if (val <= 3.5000000000000004) {
      return calculateTree12(features, 7);
    } else {
      return calculateTree12(features, 8);
    }
  }
  if (node === 19) return -0.3242170834547209;
  if (node === 41) return 0.18391499265989;
  if (node === 85) return -0.39409481379243516;
  if (node === 86) return 0.10931039130996772;
  if (node === 42) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree12(features, 85);
    } else if (val <= 17.500000000000004) {
      return calculateTree12(features, 85);
    } else {
      return calculateTree12(features, 86);
    }
  }
  if (node === 20) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree12(features, 41);
    } else if (val <= 14.500000000000002) {
      return calculateTree12(features, 41);
    } else {
      return calculateTree12(features, 42);
    }
  }
  if (node === 9) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree12(features, 19);
    } else if (val <= 8.500000000000002) {
      return calculateTree12(features, 19);
    } else {
      return calculateTree12(features, 20);
    }
  }
  if (node === 21) return -0.334146002645961;
  if (node === 45) return 0.1822866300469183;
  if (node === 46) return -0.23265141333231903;
  if (node === 22) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree12(features, 45);
    } else if (val <= 57.50000000000001) {
      return calculateTree12(features, 45);
    } else {
      return calculateTree12(features, 46);
    }
  }
  if (node === 10) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree12(features, 21);
    } else if (val <= 51.50000000000001) {
      return calculateTree12(features, 21);
    } else {
      return calculateTree12(features, 22);
    }
  }
  if (node === 4) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree12(features, 9);
    } else if (val <= 26.500000000000004) {
      return calculateTree12(features, 9);
    } else {
      return calculateTree12(features, 10);
    }
  }
  if (node === 1) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree12(features, 3);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree12(features, 3);
    } else {
      return calculateTree12(features, 4);
    }
  }
  if (node === 23) return -0.05393162606290453;
  if (node === 24) return -0.3789403099944444;
  if (node === 11) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree12(features, 23);
    } else if (val <= 123.50000000000001) {
      return calculateTree12(features, 23);
    } else {
      return calculateTree12(features, 24);
    }
  }
  if (node === 25) return 0.08737707976946114;
  if (node === 26) return 0.3413352832106292;
  if (node === 12) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree12(features, 25);
    } else if (val <= 123.50000000000001) {
      return calculateTree12(features, 25);
    } else {
      return calculateTree12(features, 26);
    }
  }
  if (node === 5) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree12(features, 11);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree12(features, 11);
    } else {
      return calculateTree12(features, 12);
    }
  }
  if (node === 111) return -0.38016315353119184;
  if (node === 112) return 0.1567815281315504;
  if (node === 55) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree12(features, 111);
    } else if (val <= 135.50000000000003) {
      return calculateTree12(features, 111);
    } else {
      return calculateTree12(features, 112);
    }
  }
  if (node === 56) return -0.30184877197894433;
  if (node === 27) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree12(features, 55);
    } else if (val <= 166.50000000000003) {
      return calculateTree12(features, 55);
    } else {
      return calculateTree12(features, 56);
    }
  }
  if (node === 115) return -0.14076149035749147;
  if (node === 116) return -0.4229141652368639;
  if (node === 57) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree12(features, 115);
    } else if (val <= 158.50000000000003) {
      return calculateTree12(features, 115);
    } else {
      return calculateTree12(features, 116);
    }
  }
  if (node === 58) return -0.037320816221362664;
  if (node === 28) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree12(features, 57);
    } else if (val <= 166.50000000000003) {
      return calculateTree12(features, 57);
    } else {
      return calculateTree12(features, 58);
    }
  }
  if (node === 13) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree12(features, 27);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree12(features, 27);
    } else {
      return calculateTree12(features, 28);
    }
  }
  if (node === 239) return -0.38817071891521265;
  if (node === 240) return -0.1148150287953743;
  if (node === 119) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree12(features, 239);
    } else if (val <= 191.50000000000003) {
      return calculateTree12(features, 239);
    } else {
      return calculateTree12(features, 240);
    }
  }
  if (node === 241) return 0.4282447517846257;
  if (node === 242) return -0.19497270220765928;
  if (node === 120) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree12(features, 241);
    } else if (val <= 278.50000000000006) {
      return calculateTree12(features, 241);
    } else {
      return calculateTree12(features, 242);
    }
  }
  if (node === 59) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree12(features, 119);
    } else if (val <= 271.00000000000006) {
      return calculateTree12(features, 119);
    } else {
      return calculateTree12(features, 120);
    }
  }
  if (node === 60) return 0.06918255339528374;
  if (node === 29) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree12(features, 59);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree12(features, 59);
    } else {
      return calculateTree12(features, 60);
    }
  }
  if (node === 61) return -0.1816751014681641;
  if (node === 62) return -0.002128247370430547;
  if (node === 30) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree12(features, 61);
    } else if (val <= 291.50000000000006) {
      return calculateTree12(features, 61);
    } else {
      return calculateTree12(features, 62);
    }
  }
  if (node === 14) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree12(features, 29);
    } else if (val <= 282.50000000000006) {
      return calculateTree12(features, 29);
    } else {
      return calculateTree12(features, 30);
    }
  }
  if (node === 6) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree12(features, 13);
    } else if (val <= 188.00000000000003) {
      return calculateTree12(features, 13);
    } else {
      return calculateTree12(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree12(features, 5);
    } else if (val <= 131.50000000000003) {
      return calculateTree12(features, 5);
    } else {
      return calculateTree12(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree12(features, 1);
    } else if (val <= 86.50000000000001) {
      return calculateTree12(features, 1);
    } else {
      return calculateTree12(features, 2);
    }
  }
}
function calculateTree13(features, node = 0) {
  if (node === 31) return -0.39523837642716825;
  if (node === 263) return 0.04188979160306836;
  if (node === 264) return 0.2759949179377482;
  if (node === 131) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree13(features, 263);
    } else if (val <= 327.50000000000006) {
      return calculateTree13(features, 263);
    } else {
      return calculateTree13(features, 264);
    }
  }
  if (node === 265) return -0.29931134650403696;
  if (node === 266) return 0.042124936167741316;
  if (node === 132) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree13(features, 265);
    } else if (val <= 531.0000000000001) {
      return calculateTree13(features, 265);
    } else {
      return calculateTree13(features, 266);
    }
  }
  if (node === 65) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree13(features, 131);
    } else if (val <= 358.50000000000006) {
      return calculateTree13(features, 131);
    } else {
      return calculateTree13(features, 132);
    }
  }
  if (node === 66) return 0.31420209540967753;
  if (node === 32) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree13(features, 65);
    } else if (val <= 898.5000000000001) {
      return calculateTree13(features, 65);
    } else {
      return calculateTree13(features, 66);
    }
  }
  if (node === 15) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree13(features, 31);
    } else if (val <= 115.50000000000001) {
      return calculateTree13(features, 31);
    } else {
      return calculateTree13(features, 32);
    }
  }
  if (node === 16) return -0.3690286147621604;
  if (node === 7) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree13(features, 15);
    } else if (val <= 932.5000000000001) {
      return calculateTree13(features, 15);
    } else {
      return calculateTree13(features, 16);
    }
  }
  if (node === 35) return 0.17042279111442932;
  if (node === 36) return -0.394068041713152;
  if (node === 17) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree13(features, 35);
    } else if (val <= 66.00000000000001) {
      return calculateTree13(features, 35);
    } else {
      return calculateTree13(features, 36);
    }
  }
  if (node === 37) return -0.2888229162210999;
  if (node === 155) return 0.1768575450228705;
  if (node === 156) return -0.34611522247016496;
  if (node === 77) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree13(features, 155);
    } else if (val <= 16.500000000000004) {
      return calculateTree13(features, 155);
    } else {
      return calculateTree13(features, 156);
    }
  }
  if (node === 157) return -0.3009042444228614;
  if (node === 158) return -0.04572844649840485;
  if (node === 78) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree13(features, 157);
    } else if (val <= 41.50000000000001) {
      return calculateTree13(features, 157);
    } else {
      return calculateTree13(features, 158);
    }
  }
  if (node === 38) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree13(features, 77);
    } else if (val <= 491.50000000000006) {
      return calculateTree13(features, 77);
    } else {
      return calculateTree13(features, 78);
    }
  }
  if (node === 18) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree13(features, 37);
    } else if (val <= 470.00000000000006) {
      return calculateTree13(features, 37);
    } else {
      return calculateTree13(features, 38);
    }
  }
  if (node === 8) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree13(features, 17);
    } else if (val <= 175.50000000000003) {
      return calculateTree13(features, 17);
    } else {
      return calculateTree13(features, 18);
    }
  }
  if (node === 3) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree13(features, 7);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree13(features, 7);
    } else {
      return calculateTree13(features, 8);
    }
  }
  if (node === 19) return -0.15682386093897194;
  if (node === 167) return -0.3602422469764083;
  if (node === 168) return 0.2988770371481655;
  if (node === 83) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree13(features, 167);
    } else if (val <= 123.50000000000001) {
      return calculateTree13(features, 167);
    } else {
      return calculateTree13(features, 168);
    }
  }
  if (node === 84) return 0.7244172644877789;
  if (node === 41) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree13(features, 83);
    } else if (val <= 409.50000000000006) {
      return calculateTree13(features, 83);
    } else {
      return calculateTree13(features, 84);
    }
  }
  if (node === 85) return -0.39949212398165507;
  if (node === 173) return 0.26617744437468394;
  if (node === 174) return -0.41465736979196605;
  if (node === 86) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree13(features, 173);
    } else if (val <= 884.0000000000001) {
      return calculateTree13(features, 173);
    } else {
      return calculateTree13(features, 174);
    }
  }
  if (node === 42) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree13(features, 85);
    } else if (val <= 861.5000000000001) {
      return calculateTree13(features, 85);
    } else {
      return calculateTree13(features, 86);
    }
  }
  if (node === 20) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree13(features, 41);
    } else if (val <= 422.00000000000006) {
      return calculateTree13(features, 41);
    } else {
      return calculateTree13(features, 42);
    }
  }
  if (node === 9) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree13(features, 19);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree13(features, 19);
    } else {
      return calculateTree13(features, 20);
    }
  }
  if (node === 43) return -0.022580104791810888;
  if (node === 44) return -0.26461428504887097;
  if (node === 21) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree13(features, 43);
    } else if (val <= 270.00000000000006) {
      return calculateTree13(features, 43);
    } else {
      return calculateTree13(features, 44);
    }
  }
  if (node === 45) return 0.10926179949864052;
  if (node === 93) return -0.1596816994514282;
  if (node === 189) return 0.08492837284924361;
  if (node === 381) return -0.2922359134634275;
  if (node === 1531) return -0.01872877121093212;
  if (node === 1532) return -0.18904782035044054;
  if (node === 765) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree13(features, 1531);
    } else if (val <= 208.50000000000003) {
      return calculateTree13(features, 1531);
    } else {
      return calculateTree13(features, 1532);
    }
  }
  if (node === 1533) return 0.252502423779166;
  if (node === 1534) return -0.0016763712469161942;
  if (node === 766) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree13(features, 1533);
    } else if (val <= 245.00000000000003) {
      return calculateTree13(features, 1533);
    } else {
      return calculateTree13(features, 1534);
    }
  }
  if (node === 382) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree13(features, 765);
    } else if (val <= 241.50000000000003) {
      return calculateTree13(features, 765);
    } else {
      return calculateTree13(features, 766);
    }
  }
  if (node === 190) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree13(features, 381);
    } else if (val <= 346.00000000000006) {
      return calculateTree13(features, 381);
    } else {
      return calculateTree13(features, 382);
    }
  }
  if (node === 94) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree13(features, 189);
    } else if (val <= 327.50000000000006) {
      return calculateTree13(features, 189);
    } else {
      return calculateTree13(features, 190);
    }
  }
  if (node === 46) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree13(features, 93);
    } else if (val <= 293.00000000000006) {
      return calculateTree13(features, 93);
    } else {
      return calculateTree13(features, 94);
    }
  }
  if (node === 22) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree13(features, 45);
    } else if (val <= 284.50000000000006) {
      return calculateTree13(features, 45);
    } else {
      return calculateTree13(features, 46);
    }
  }
  if (node === 10) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree13(features, 21);
    } else if (val <= 283.50000000000006) {
      return calculateTree13(features, 21);
    } else {
      return calculateTree13(features, 22);
    }
  }
  if (node === 4) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree13(features, 9);
    } else if (val <= 131.50000000000003) {
      return calculateTree13(features, 9);
    } else {
      return calculateTree13(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree13(features, 3);
    } else if (val <= 97.50000000000001) {
      return calculateTree13(features, 3);
    } else {
      return calculateTree13(features, 4);
    }
  }
  if (node === 2) return 0.005696899056882769;
  if (node === 0) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree13(features, 1);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree13(features, 1);
    } else {
      return calculateTree13(features, 2);
    }
  }
}
function calculateTree14(features, node = 0) {
  if (node === 31) return -0.0022545334208836656;
  if (node === 131) return 0.2493604496056599;
  if (node === 132) return -0.40391369327013465;
  if (node === 65) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree14(features, 131);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree14(features, 131);
    } else {
      return calculateTree14(features, 132);
    }
  }
  if (node === 66) return -0.22409161321047985;
  if (node === 32) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree14(features, 65);
    } else if (val <= 245.00000000000003) {
      return calculateTree14(features, 65);
    } else {
      return calculateTree14(features, 66);
    }
  }
  if (node === 15) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree14(features, 31);
    } else if (val <= 144.50000000000003) {
      return calculateTree14(features, 31);
    } else {
      return calculateTree14(features, 32);
    }
  }
  if (node === 33) return -0.002329328235902313;
  if (node === 279) return -0.04541470964698716;
  if (node === 280) return 0.08592361370319866;
  if (node === 139) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree14(features, 279);
    } else if (val <= 780.0000000000001) {
      return calculateTree14(features, 279);
    } else {
      return calculateTree14(features, 280);
    }
  }
  if (node === 140) return 0.15598547296991244;
  if (node === 69) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree14(features, 139);
    } else if (val <= 690.5000000000001) {
      return calculateTree14(features, 139);
    } else {
      return calculateTree14(features, 140);
    }
  }
  if (node === 141) return 0.04377191909218045;
  if (node === 285) return -0.05662611239676772;
  if (node === 286) return -0.44037484555125617;
  if (node === 142) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree14(features, 285);
    } else if (val <= 794.0000000000001) {
      return calculateTree14(features, 285);
    } else {
      return calculateTree14(features, 286);
    }
  }
  if (node === 70) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree14(features, 141);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree14(features, 141);
    } else {
      return calculateTree14(features, 142);
    }
  }
  if (node === 34) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree14(features, 69);
    } else if (val <= 800.5000000000001) {
      return calculateTree14(features, 69);
    } else {
      return calculateTree14(features, 70);
    }
  }
  if (node === 16) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree14(features, 33);
    } else if (val <= 709.5000000000001) {
      return calculateTree14(features, 33);
    } else {
      return calculateTree14(features, 34);
    }
  }
  if (node === 7) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree14(features, 15);
    } else if (val <= 175.50000000000003) {
      return calculateTree14(features, 15);
    } else {
      return calculateTree14(features, 16);
    }
  }
  if (node === 17) return -0.30377527617708233;
  if (node === 37) return 0.3499820056411519;
  if (node === 38) return -0.4973175333059727;
  if (node === 18) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree14(features, 37);
    } else if (val <= 859.5000000000001) {
      return calculateTree14(features, 37);
    } else {
      return calculateTree14(features, 38);
    }
  }
  if (node === 8) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree14(features, 17);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree14(features, 17);
    } else {
      return calculateTree14(features, 18);
    }
  }
  if (node === 3) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree14(features, 7);
    } else if (val <= 834.5000000000001) {
      return calculateTree14(features, 7);
    } else {
      return calculateTree14(features, 8);
    }
  }
  if (node === 39) return 0.01748627337312021;
  if (node === 81) return 0.29311926016627027;
  if (node === 165) return 0.0667899425242203;
  if (node === 166) return -0.40483619200379634;
  if (node === 82) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree14(features, 165);
    } else if (val <= 240.00000000000003) {
      return calculateTree14(features, 165);
    } else {
      return calculateTree14(features, 166);
    }
  }
  if (node === 40) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree14(features, 81);
    } else if (val <= 844.5000000000001) {
      return calculateTree14(features, 81);
    } else {
      return calculateTree14(features, 82);
    }
  }
  if (node === 19) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree14(features, 39);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree14(features, 39);
    } else {
      return calculateTree14(features, 40);
    }
  }
  if (node === 20) return 0.0033876919690067862;
  if (node === 9) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree14(features, 19);
    } else if (val <= 864.5000000000001) {
      return calculateTree14(features, 19);
    } else {
      return calculateTree14(features, 20);
    }
  }
  if (node === 21) return -0.3317369562421899;
  if (node === 45) return -0.2687672035773922;
  if (node === 375) return 0.3386722742173147;
  if (node === 376) return -0.3780121351588016;
  if (node === 187) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree14(features, 375);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree14(features, 375);
    } else {
      return calculateTree14(features, 376);
    }
  }
  if (node === 377) return -0.31958754466238426;
  if (node === 757) return 0.04892361103764618;
  if (node === 758) return -0.3390984133076203;
  if (node === 378) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree14(features, 757);
    } else if (val <= 33.50000000000001) {
      return calculateTree14(features, 757);
    } else {
      return calculateTree14(features, 758);
    }
  }
  if (node === 188) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree14(features, 377);
    } else if (val <= 863.0000000000001) {
      return calculateTree14(features, 377);
    } else {
      return calculateTree14(features, 378);
    }
  }
  if (node === 93) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree14(features, 187);
    } else if (val <= 835.5000000000001) {
      return calculateTree14(features, 187);
    } else {
      return calculateTree14(features, 188);
    }
  }
  if (node === 94) return -0.3461547181773703;
  if (node === 46) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree14(features, 93);
    } else if (val <= 966.0000000000001) {
      return calculateTree14(features, 93);
    } else {
      return calculateTree14(features, 94);
    }
  }
  if (node === 22) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree14(features, 45);
    } else if (val <= 808.5000000000001) {
      return calculateTree14(features, 45);
    } else {
      return calculateTree14(features, 46);
    }
  }
  if (node === 10) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree14(features, 21);
    } else if (val <= 859.5000000000001) {
      return calculateTree14(features, 21);
    } else {
      return calculateTree14(features, 22);
    }
  }
  if (node === 4) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree14(features, 9);
    } else if (val <= 781.0000000000001) {
      return calculateTree14(features, 9);
    } else {
      return calculateTree14(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree14(features, 3);
    } else if (val <= 840.0000000000001) {
      return calculateTree14(features, 3);
    } else {
      return calculateTree14(features, 4);
    }
  }
  if (node === 23) return 0.02175995418337941;
  if (node === 24) return -0.37765492200041406;
  if (node === 11) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree14(features, 23);
    } else if (val <= 143.50000000000003) {
      return calculateTree14(features, 23);
    } else {
      return calculateTree14(features, 24);
    }
  }
  if (node === 12) return 0.015802613020637037;
  if (node === 5) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree14(features, 11);
    } else if (val <= 222.50000000000003) {
      return calculateTree14(features, 11);
    } else {
      return calculateTree14(features, 12);
    }
  }
  if (node === 13) return -0.253536740637439;
  if (node === 14) return 0.25674233961591825;
  if (node === 6) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree14(features, 13);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree14(features, 13);
    } else {
      return calculateTree14(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree14(features, 5);
    } else if (val <= 905.5000000000001) {
      return calculateTree14(features, 5);
    } else {
      return calculateTree14(features, 6);
    }
  }
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree14(features, 1);
    } else if (val <= 937.5000000000001) {
      return calculateTree14(features, 1);
    } else {
      return calculateTree14(features, 2);
    }
  }
}
function calculateTree15(features, node = 0) {
  if (node === 15) return 0.034802886963929316;
  if (node === 16) return -0.10661471426808654;
  if (node === 7) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree15(features, 15);
    } else if (val <= 175.50000000000003) {
      return calculateTree15(features, 15);
    } else {
      return calculateTree15(features, 16);
    }
  }
  if (node === 17) return -0.26729556425654444;
  if (node === 18) return -0.03328885932457094;
  if (node === 8) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree15(features, 17);
    } else if (val <= 321.50000000000006) {
      return calculateTree15(features, 17);
    } else {
      return calculateTree15(features, 18);
    }
  }
  if (node === 3) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree15(features, 7);
    } else if (val <= 305.00000000000006) {
      return calculateTree15(features, 7);
    } else {
      return calculateTree15(features, 8);
    }
  }
  if (node === 79) return 0.12309019407633343;
  if (node === 161) return -0.2005420705263443;
  if (node === 325) return -0.008393506076314781;
  if (node === 326) return 0.21736123044405692;
  if (node === 162) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree15(features, 325);
    } else if (val <= 829.0000000000001) {
      return calculateTree15(features, 325);
    } else {
      return calculateTree15(features, 326);
    }
  }
  if (node === 80) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree15(features, 161);
    } else if (val <= 191.50000000000003) {
      return calculateTree15(features, 161);
    } else {
      return calculateTree15(features, 162);
    }
  }
  if (node === 39) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree15(features, 79);
    } else if (val <= 34.00000000000001) {
      return calculateTree15(features, 79);
    } else {
      return calculateTree15(features, 80);
    }
  }
  if (node === 40) return -0.34288811538672004;
  if (node === 19) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree15(features, 39);
    } else if (val <= 205.50000000000003) {
      return calculateTree15(features, 39);
    } else {
      return calculateTree15(features, 40);
    }
  }
  if (node === 20) return 0.29240120310252327;
  if (node === 9) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree15(features, 19);
    } else if (val <= 207.00000000000003) {
      return calculateTree15(features, 19);
    } else {
      return calculateTree15(features, 20);
    }
  }
  if (node === 21) return -0.18233331214669404;
  if (node === 735) return -0.21963683374578527;
  if (node === 1473) return -0.11796145634786424;
  if (node === 1474) return 0.09742406039022387;
  if (node === 736) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree15(features, 1473);
    } else if (val <= 271.00000000000006) {
      return calculateTree15(features, 1473);
    } else {
      return calculateTree15(features, 1474);
    }
  }
  if (node === 367) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree15(features, 735);
    } else if (val <= 39.50000000000001) {
      return calculateTree15(features, 735);
    } else {
      return calculateTree15(features, 736);
    }
  }
  if (node === 368) return -0.3313918276983592;
  if (node === 183) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree15(features, 367);
    } else if (val <= 171.00000000000003) {
      return calculateTree15(features, 367);
    } else {
      return calculateTree15(features, 368);
    }
  }
  if (node === 1479) return -0.03091908544322535;
  if (node === 1480) return 0.03276810841899245;
  if (node === 739) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree15(features, 1479);
    } else if (val <= 241.50000000000003) {
      return calculateTree15(features, 1479);
    } else {
      return calculateTree15(features, 1480);
    }
  }
  if (node === 740) return 0.18212501621719418;
  if (node === 369) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree15(features, 739);
    } else if (val <= 314.50000000000006) {
      return calculateTree15(features, 739);
    } else {
      return calculateTree15(features, 740);
    }
  }
  if (node === 370) return -0.21956756862065455;
  if (node === 184) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree15(features, 369);
    } else if (val <= 322.00000000000006) {
      return calculateTree15(features, 369);
    } else {
      return calculateTree15(features, 370);
    }
  }
  if (node === 91) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree15(features, 183);
    } else if (val <= 201.50000000000003) {
      return calculateTree15(features, 183);
    } else {
      return calculateTree15(features, 184);
    }
  }
  if (node === 185) return 0.36175104627417926;
  if (node === 186) return 0.08410329349819468;
  if (node === 92) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree15(features, 185);
    } else if (val <= 25.500000000000004) {
      return calculateTree15(features, 185);
    } else {
      return calculateTree15(features, 186);
    }
  }
  if (node === 45) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree15(features, 91);
    } else if (val <= 331.50000000000006) {
      return calculateTree15(features, 91);
    } else {
      return calculateTree15(features, 92);
    }
  }
  if (node === 93) return -0.2554415256653688;
  if (node === 379) return 0.2841824868167487;
  if (node === 380) return -0.15484656655652523;
  if (node === 189) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree15(features, 379);
    } else if (val <= 361.50000000000006) {
      return calculateTree15(features, 379);
    } else {
      return calculateTree15(features, 380);
    }
  }
  if (node === 1527) return -0.007311875132305639;
  if (node === 1528) return -0.3964288617928653;
  if (node === 763) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree15(features, 1527);
    } else if (val <= 969.5000000000001) {
      return calculateTree15(features, 1527);
    } else {
      return calculateTree15(features, 1528);
    }
  }
  if (node === 764) return -0.27116958646948347;
  if (node === 381) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree15(features, 763);
    } else if (val <= 405.00000000000006) {
      return calculateTree15(features, 763);
    } else {
      return calculateTree15(features, 764);
    }
  }
  if (node === 1531) return 0.006192903704168719;
  if (node === 1532) return 0.1326866985039354;
  if (node === 765) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree15(features, 1531);
    } else if (val <= 422.50000000000006) {
      return calculateTree15(features, 1531);
    } else {
      return calculateTree15(features, 1532);
    }
  }
  if (node === 1533) return -0.18358578772092857;
  if (node === 1534) return -0.0007609551631971079;
  if (node === 766) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree15(features, 1533);
    } else if (val <= 470.50000000000006) {
      return calculateTree15(features, 1533);
    } else {
      return calculateTree15(features, 1534);
    }
  }
  if (node === 382) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree15(features, 765);
    } else if (val <= 439.50000000000006) {
      return calculateTree15(features, 765);
    } else {
      return calculateTree15(features, 766);
    }
  }
  if (node === 190) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree15(features, 381);
    } else if (val <= 414.50000000000006) {
      return calculateTree15(features, 381);
    } else {
      return calculateTree15(features, 382);
    }
  }
  if (node === 94) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree15(features, 189);
    } else if (val <= 358.50000000000006) {
      return calculateTree15(features, 189);
    } else {
      return calculateTree15(features, 190);
    }
  }
  if (node === 46) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree15(features, 93);
    } else if (val <= 350.50000000000006) {
      return calculateTree15(features, 93);
    } else {
      return calculateTree15(features, 94);
    }
  }
  if (node === 22) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree15(features, 45);
    } else if (val <= 338.50000000000006) {
      return calculateTree15(features, 45);
    } else {
      return calculateTree15(features, 46);
    }
  }
  if (node === 10) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree15(features, 21);
    } else if (val <= 212.50000000000003) {
      return calculateTree15(features, 21);
    } else {
      return calculateTree15(features, 22);
    }
  }
  if (node === 4) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree15(features, 9);
    } else if (val <= 208.50000000000003) {
      return calculateTree15(features, 9);
    } else {
      return calculateTree15(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree15(features, 3);
    } else if (val <= 188.00000000000003) {
      return calculateTree15(features, 3);
    } else {
      return calculateTree15(features, 4);
    }
  }
  if (node === 2) return -0.25654752582397167;
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree15(features, 1);
    } else if (val <= 994.5000000000001) {
      return calculateTree15(features, 1);
    } else {
      return calculateTree15(features, 2);
    }
  }
}
function calculateTree16(features, node = 0) {
  if (node === 3) return -0.0023602353230042106;
  if (node === 79) return -0.3778281942680179;
  if (node === 80) return 0.19876234328138492;
  if (node === 39) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree16(features, 79);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree16(features, 79);
    } else {
      return calculateTree16(features, 80);
    }
  }
  if (node === 40) return -0.2331996238507386;
  if (node === 19) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree16(features, 39);
    } else if (val <= 16.500000000000004) {
      return calculateTree16(features, 39);
    } else {
      return calculateTree16(features, 40);
    }
  }
  if (node === 41) return 0.11711406275806802;
  if (node === 42) return 0.025824632465074108;
  if (node === 20) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree16(features, 41);
    } else if (val <= 325.00000000000006) {
      return calculateTree16(features, 41);
    } else {
      return calculateTree16(features, 42);
    }
  }
  if (node === 9) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree16(features, 19);
    } else if (val <= 127.00000000000001) {
      return calculateTree16(features, 19);
    } else {
      return calculateTree16(features, 20);
    }
  }
  if (node === 10) return -0.25063851654127894;
  if (node === 4) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree16(features, 9);
    } else if (val <= 940.0000000000001) {
      return calculateTree16(features, 9);
    } else {
      return calculateTree16(features, 10);
    }
  }
  if (node === 1) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree16(features, 3);
    } else if (val <= 409.50000000000006) {
      return calculateTree16(features, 3);
    } else {
      return calculateTree16(features, 4);
    }
  }
  if (node === 5) return -0.04111629791203793;
  if (node === 55) return -0.18544656546994612;
  if (node === 227) return 0.33107065484544823;
  if (node === 228) return -0.413953687301733;
  if (node === 113) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree16(features, 227);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree16(features, 227);
    } else {
      return calculateTree16(features, 228);
    }
  }
  if (node === 1839) return -0.2440472732406867;
  if (node === 1840) return 0.09796840019692389;
  if (node === 919) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree16(features, 1839);
    } else if (val <= 708.5000000000001) {
      return calculateTree16(features, 1839);
    } else {
      return calculateTree16(features, 1840);
    }
  }
  if (node === 1841) return 0.40597963039258883;
  if (node === 1842) return -0.06015129195591251;
  if (node === 920) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree16(features, 1841);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree16(features, 1841);
    } else {
      return calculateTree16(features, 1842);
    }
  }
  if (node === 459) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree16(features, 919);
    } else if (val <= 305.50000000000006) {
      return calculateTree16(features, 919);
    } else {
      return calculateTree16(features, 920);
    }
  }
  if (node === 921) return -0.3016914737355944;
  if (node === 1845) return 0.20885678834559934;
  if (node === 1846) return -0.10196143979844072;
  if (node === 922) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree16(features, 1845);
    } else if (val <= 708.5000000000001) {
      return calculateTree16(features, 1845);
    } else {
      return calculateTree16(features, 1846);
    }
  }
  if (node === 460) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree16(features, 921);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree16(features, 921);
    } else {
      return calculateTree16(features, 922);
    }
  }
  if (node === 229) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree16(features, 459);
    } else if (val <= 461.50000000000006) {
      return calculateTree16(features, 459);
    } else {
      return calculateTree16(features, 460);
    }
  }
  if (node === 1847) return 0.3280312932170002;
  if (node === 1848) return -0.3540294070648349;
  if (node === 923) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree16(features, 1847);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree16(features, 1847);
    } else {
      return calculateTree16(features, 1848);
    }
  }
  if (node === 1849) return -0.3821531984463904;
  if (node === 1850) return -0.06405471446859741;
  if (node === 924) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree16(features, 1849);
    } else if (val <= 181.50000000000003) {
      return calculateTree16(features, 1849);
    } else {
      return calculateTree16(features, 1850);
    }
  }
  if (node === 461) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree16(features, 923);
    } else if (val <= 75.50000000000001) {
      return calculateTree16(features, 923);
    } else {
      return calculateTree16(features, 924);
    }
  }
  if (node === 925) return 0.0023141506271281437;
  if (node === 926) return -0.3224372100046012;
  if (node === 462) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree16(features, 925);
    } else if (val <= 896.5000000000001) {
      return calculateTree16(features, 925);
    } else {
      return calculateTree16(features, 926);
    }
  }
  if (node === 230) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree16(features, 461);
    } else if (val <= 778.5000000000001) {
      return calculateTree16(features, 461);
    } else {
      return calculateTree16(features, 462);
    }
  }
  if (node === 114) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree16(features, 229);
    } else if (val <= 717.0000000000001) {
      return calculateTree16(features, 229);
    } else {
      return calculateTree16(features, 230);
    }
  }
  if (node === 56) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree16(features, 113);
    } else if (val <= 30.500000000000004) {
      return calculateTree16(features, 113);
    } else {
      return calculateTree16(features, 114);
    }
  }
  if (node === 27) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree16(features, 55);
    } else if (val <= 28.500000000000004) {
      return calculateTree16(features, 55);
    } else {
      return calculateTree16(features, 56);
    }
  }
  if (node === 115) return -0.3851977347810435;
  if (node === 116) return 0.4583760592720885;
  if (node === 57) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree16(features, 115);
    } else if (val <= 916.5000000000001) {
      return calculateTree16(features, 115);
    } else {
      return calculateTree16(features, 116);
    }
  }
  if (node === 58) return 0.20610519202031985;
  if (node === 28) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree16(features, 57);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree16(features, 57);
    } else {
      return calculateTree16(features, 58);
    }
  }
  if (node === 13) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree16(features, 27);
    } else if (val <= 902.0000000000001) {
      return calculateTree16(features, 27);
    } else {
      return calculateTree16(features, 28);
    }
  }
  if (node === 29) return -0.28701887239573515;
  if (node === 123) return 0.229392798759479;
  if (node === 124) return -0.35825074819473834;
  if (node === 61) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree16(features, 123);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree16(features, 123);
    } else {
      return calculateTree16(features, 124);
    }
  }
  if (node === 125) return -0.3119170499080427;
  if (node === 126) return 0.42681076731230555;
  if (node === 62) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree16(features, 125);
    } else if (val <= 953.5000000000001) {
      return calculateTree16(features, 125);
    } else {
      return calculateTree16(features, 126);
    }
  }
  if (node === 30) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree16(features, 61);
    } else if (val <= 966.0000000000001) {
      return calculateTree16(features, 61);
    } else {
      return calculateTree16(features, 62);
    }
  }
  if (node === 14) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree16(features, 29);
    } else if (val <= 962.5000000000001) {
      return calculateTree16(features, 29);
    } else {
      return calculateTree16(features, 30);
    }
  }
  if (node === 6) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree16(features, 13);
    } else if (val <= 919.5000000000001) {
      return calculateTree16(features, 13);
    } else {
      return calculateTree16(features, 14);
    }
  }
  if (node === 2) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree16(features, 5);
    } else if (val <= 677.5000000000001) {
      return calculateTree16(features, 5);
    } else {
      return calculateTree16(features, 6);
    }
  }
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree16(features, 1);
    } else if (val <= 507.00000000000006) {
      return calculateTree16(features, 1);
    } else {
      return calculateTree16(features, 2);
    }
  }
}
function calculateTree17(features, node = 0) {
  if (node === 15) return -0.000538833252632904;
  if (node === 135) return -0.22614347769650736;
  if (node === 136) return 0.17882431867801035;
  if (node === 67) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree17(features, 135);
    } else if (val <= 414.50000000000006) {
      return calculateTree17(features, 135);
    } else {
      return calculateTree17(features, 136);
    }
  }
  if (node === 68) return -0.25009547686494343;
  if (node === 33) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree17(features, 67);
    } else if (val <= 868.0000000000001) {
      return calculateTree17(features, 67);
    } else {
      return calculateTree17(features, 68);
    }
  }
  if (node === 139) return 0.14924938228604684;
  if (node === 140) return -0.305024432733119;
  if (node === 69) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree17(features, 139);
    } else if (val <= 225.50000000000003) {
      return calculateTree17(features, 139);
    } else {
      return calculateTree17(features, 140);
    }
  }
  if (node === 70) return 0.045187466658638965;
  if (node === 34) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree17(features, 69);
    } else if (val <= 868.0000000000001) {
      return calculateTree17(features, 69);
    } else {
      return calculateTree17(features, 70);
    }
  }
  if (node === 16) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree17(features, 33);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree17(features, 33);
    } else {
      return calculateTree17(features, 34);
    }
  }
  if (node === 7) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree17(features, 15);
    } else if (val <= 852.5000000000001) {
      return calculateTree17(features, 15);
    } else {
      return calculateTree17(features, 16);
    }
  }
  if (node === 8) return 0.09497682871635774;
  if (node === 3) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree17(features, 7);
    } else if (val <= 902.0000000000001) {
      return calculateTree17(features, 7);
    } else {
      return calculateTree17(features, 8);
    }
  }
  if (node === 39) return -0.026111524067834155;
  if (node === 40) return -0.270885644760068;
  if (node === 19) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree17(features, 39);
    } else if (val <= 943.5000000000001) {
      return calculateTree17(features, 39);
    } else {
      return calculateTree17(features, 40);
    }
  }
  if (node === 41) return 0.29346995155424965;
  if (node === 42) return -0.3819045671521;
  if (node === 20) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree17(features, 41);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree17(features, 41);
    } else {
      return calculateTree17(features, 42);
    }
  }
  if (node === 9) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree17(features, 19);
    } else if (val <= 951.5000000000001) {
      return calculateTree17(features, 19);
    } else {
      return calculateTree17(features, 20);
    }
  }
  if (node === 43) return -0.43324734746207566;
  if (node === 179) return -0.20183288011167236;
  if (node === 180) return 0.7569389238297539;
  if (node === 89) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree17(features, 179);
    } else if (val <= 718.5000000000001) {
      return calculateTree17(features, 179);
    } else {
      return calculateTree17(features, 180);
    }
  }
  if (node === 181) return 0.19782356492708741;
  if (node === 182) return -0.23611355874234505;
  if (node === 90) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree17(features, 181);
    } else if (val <= 938.5000000000001) {
      return calculateTree17(features, 181);
    } else {
      return calculateTree17(features, 182);
    }
  }
  if (node === 44) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree17(features, 89);
    } else if (val <= 734.0000000000001) {
      return calculateTree17(features, 89);
    } else {
      return calculateTree17(features, 90);
    }
  }
  if (node === 21) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree17(features, 43);
    } else if (val <= 709.5000000000001) {
      return calculateTree17(features, 43);
    } else {
      return calculateTree17(features, 44);
    }
  }
  if (node === 22) return -0.33743432046129257;
  if (node === 10) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree17(features, 21);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree17(features, 21);
    } else {
      return calculateTree17(features, 22);
    }
  }
  if (node === 4) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree17(features, 9);
    } else if (val <= 665.0000000000001) {
      return calculateTree17(features, 9);
    } else {
      return calculateTree17(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree17(features, 3);
    } else if (val <= 908.5000000000001) {
      return calculateTree17(features, 3);
    } else {
      return calculateTree17(features, 4);
    }
  }
  if (node === 11) return 0.3949670964370916;
  if (node === 25) return -0.3934655545194586;
  if (node === 107) return 0.2753445101993651;
  if (node === 108) return -0.3652389580834076;
  if (node === 53) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree17(features, 107);
    } else if (val <= 993.5000000000001) {
      return calculateTree17(features, 107);
    } else {
      return calculateTree17(features, 108);
    }
  }
  if (node === 109) return -0.41702505045317995;
  if (node === 221) return 0.11555913640553665;
  if (node === 445) return 0.1632526629694548;
  if (node === 446) return -0.3067155728299636;
  if (node === 222) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree17(features, 445);
    } else if (val <= 966.0000000000001) {
      return calculateTree17(features, 445);
    } else {
      return calculateTree17(features, 446);
    }
  }
  if (node === 110) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree17(features, 221);
    } else if (val <= 661.0000000000001) {
      return calculateTree17(features, 221);
    } else {
      return calculateTree17(features, 222);
    }
  }
  if (node === 54) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree17(features, 109);
    } else if (val <= 283.50000000000006) {
      return calculateTree17(features, 109);
    } else {
      return calculateTree17(features, 110);
    }
  }
  if (node === 26) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree17(features, 53);
    } else if (val <= 139.50000000000003) {
      return calculateTree17(features, 53);
    } else {
      return calculateTree17(features, 54);
    }
  }
  if (node === 12) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree17(features, 25);
    } else if (val <= 962.5000000000001) {
      return calculateTree17(features, 25);
    } else {
      return calculateTree17(features, 26);
    }
  }
  if (node === 5) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree17(features, 11);
    } else if (val <= 955.5000000000001) {
      return calculateTree17(features, 11);
    } else {
      return calculateTree17(features, 12);
    }
  }
  if (node === 55) return -0.39588555600419956;
  if (node === 56) return 0.198143835174399;
  if (node === 27) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree17(features, 55);
    } else if (val <= 993.5000000000001) {
      return calculateTree17(features, 55);
    } else {
      return calculateTree17(features, 56);
    }
  }
  if (node === 28) return 0.18857291168053372;
  if (node === 13) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree17(features, 27);
    } else if (val <= 127.50000000000001) {
      return calculateTree17(features, 27);
    } else {
      return calculateTree17(features, 28);
    }
  }
  if (node === 29) return -0.2859213670165241;
  if (node === 61) return -0.36477681672717766;
  if (node === 62) return -0.033006368539476215;
  if (node === 30) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree17(features, 61);
    } else if (val <= 966.0000000000001) {
      return calculateTree17(features, 61);
    } else {
      return calculateTree17(features, 62);
    }
  }
  if (node === 14) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree17(features, 29);
    } else if (val <= 661.0000000000001) {
      return calculateTree17(features, 29);
    } else {
      return calculateTree17(features, 30);
    }
  }
  if (node === 6) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree17(features, 13);
    } else if (val <= 175.50000000000003) {
      return calculateTree17(features, 13);
    } else {
      return calculateTree17(features, 14);
    }
  }
  if (node === 2) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree17(features, 5);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree17(features, 5);
    } else {
      return calculateTree17(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree17(features, 1);
    } else if (val <= 954.5000000000001) {
      return calculateTree17(features, 1);
    } else {
      return calculateTree17(features, 2);
    }
  }
}
function calculateTree18(features, node = 0) {
  if (node === 127) return -0.0008679857269934981;
  if (node === 128) return -0.1063372204724699;
  if (node === 63) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree18(features, 127);
    } else if (val <= 214.00000000000003) {
      return calculateTree18(features, 127);
    } else {
      return calculateTree18(features, 128);
    }
  }
  if (node === 129) return 0.1890627885352153;
  if (node === 130) return 0.005381166236775747;
  if (node === 64) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree18(features, 129);
    } else if (val <= 245.00000000000003) {
      return calculateTree18(features, 129);
    } else {
      return calculateTree18(features, 130);
    }
  }
  if (node === 31) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree18(features, 63);
    } else if (val <= 241.50000000000003) {
      return calculateTree18(features, 63);
    } else {
      return calculateTree18(features, 64);
    }
  }
  if (node === 32) return 0.056867033155763566;
  if (node === 15) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree18(features, 31);
    } else if (val <= 14.500000000000002) {
      return calculateTree18(features, 31);
    } else {
      return calculateTree18(features, 32);
    }
  }
  if (node === 16) return -0.38015407757280234;
  if (node === 7) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree18(features, 15);
    } else if (val <= 978.0000000000001) {
      return calculateTree18(features, 15);
    } else {
      return calculateTree18(features, 16);
    }
  }
  if (node === 17) return 0.04525009155834122;
  if (node === 18) return -0.27072057748471906;
  if (node === 8) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree18(features, 17);
    } else if (val <= 253.00000000000003) {
      return calculateTree18(features, 17);
    } else {
      return calculateTree18(features, 18);
    }
  }
  if (node === 3) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree18(features, 7);
    } else if (val <= 261.50000000000006) {
      return calculateTree18(features, 7);
    } else {
      return calculateTree18(features, 8);
    }
  }
  if (node === 19) return 0.25909307318039015;
  if (node === 41) return -0.45293226790035424;
  if (node === 85) return 0.20197242855598427;
  if (node === 86) return -0.5141515204514572;
  if (node === 42) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree18(features, 85);
    } else if (val <= 816.5000000000001) {
      return calculateTree18(features, 85);
    } else {
      return calculateTree18(features, 86);
    }
  }
  if (node === 20) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree18(features, 41);
    } else if (val <= 758.0000000000001) {
      return calculateTree18(features, 41);
    } else {
      return calculateTree18(features, 42);
    }
  }
  if (node === 9) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree18(features, 19);
    } else if (val <= 556.0000000000001) {
      return calculateTree18(features, 19);
    } else {
      return calculateTree18(features, 20);
    }
  }
  if (node === 10) return 0.22108122335947022;
  if (node === 4) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree18(features, 9);
    } else if (val <= 835.5000000000001) {
      return calculateTree18(features, 9);
    } else {
      return calculateTree18(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree18(features, 3);
    } else if (val <= 275.50000000000006) {
      return calculateTree18(features, 3);
    } else {
      return calculateTree18(features, 4);
    }
  }
  if (node === 47) return 0.062496513283860654;
  if (node === 48) return -0.30903223033147564;
  if (node === 23) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree18(features, 47);
    } else if (val <= 89.00000000000001) {
      return calculateTree18(features, 47);
    } else {
      return calculateTree18(features, 48);
    }
  }
  if (node === 49) return -0.09585633169915658;
  if (node === 50) return 0.19777371323195295;
  if (node === 24) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree18(features, 49);
    } else if (val <= 29.500000000000004) {
      return calculateTree18(features, 49);
    } else {
      return calculateTree18(features, 50);
    }
  }
  if (node === 11) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree18(features, 23);
    } else if (val <= 447.00000000000006) {
      return calculateTree18(features, 23);
    } else {
      return calculateTree18(features, 24);
    }
  }
  if (node === 12) return -0.2684124042987009;
  if (node === 5) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree18(features, 11);
    } else if (val <= 781.0000000000001) {
      return calculateTree18(features, 11);
    } else {
      return calculateTree18(features, 12);
    }
  }
  if (node === 27) return 0.5122608994707122;
  if (node === 28) return -0.10927845714493073;
  if (node === 13) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree18(features, 27);
    } else if (val <= 776.0000000000001) {
      return calculateTree18(features, 27);
    } else {
      return calculateTree18(features, 28);
    }
  }
  if (node === 59) return -0.41363597944553165;
  if (node === 121) return -0.07867375669274464;
  if (node === 245) return 0.1417460632394566;
  if (node === 246) return -0.11046178751166691;
  if (node === 122) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree18(features, 245);
    } else if (val <= 19.500000000000004) {
      return calculateTree18(features, 245);
    } else {
      return calculateTree18(features, 246);
    }
  }
  if (node === 60) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree18(features, 121);
    } else if (val <= 35.50000000000001) {
      return calculateTree18(features, 121);
    } else {
      return calculateTree18(features, 122);
    }
  }
  if (node === 29) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree18(features, 59);
    } else if (val <= 478.50000000000006) {
      return calculateTree18(features, 59);
    } else {
      return calculateTree18(features, 60);
    }
  }
  if (node === 61) return 0.04648689476336842;
  if (node === 125) return -0.14002642573151536;
  if (node === 253) return 0.19285462790496466;
  if (node === 509) return -0.20935546371821384;
  if (node === 2043) return -0.31243374698200216;
  if (node === 2044) return 0.11094165623824707;
  if (node === 1021) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree18(features, 2043);
    } else if (val <= 181.50000000000003) {
      return calculateTree18(features, 2043);
    } else {
      return calculateTree18(features, 2044);
    }
  }
  if (node === 2045) return -0.14337279435899886;
  if (node === 2046) return -0.0020047214743282547;
  if (node === 1022) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree18(features, 2045);
    } else if (val <= 386.50000000000006) {
      return calculateTree18(features, 2045);
    } else {
      return calculateTree18(features, 2046);
    }
  }
  if (node === 510) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree18(features, 1021);
    } else if (val <= 365.50000000000006) {
      return calculateTree18(features, 1021);
    } else {
      return calculateTree18(features, 1022);
    }
  }
  if (node === 254) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree18(features, 509);
    } else if (val <= 350.50000000000006) {
      return calculateTree18(features, 509);
    } else {
      return calculateTree18(features, 510);
    }
  }
  if (node === 126) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree18(features, 253);
    } else if (val <= 338.50000000000006) {
      return calculateTree18(features, 253);
    } else {
      return calculateTree18(features, 254);
    }
  }
  if (node === 62) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree18(features, 125);
    } else if (val <= 331.50000000000006) {
      return calculateTree18(features, 125);
    } else {
      return calculateTree18(features, 126);
    }
  }
  if (node === 30) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree18(features, 61);
    } else if (val <= 322.00000000000006) {
      return calculateTree18(features, 61);
    } else {
      return calculateTree18(features, 62);
    }
  }
  if (node === 14) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree18(features, 29);
    } else if (val <= 311.50000000000006) {
      return calculateTree18(features, 29);
    } else {
      return calculateTree18(features, 30);
    }
  }
  if (node === 6) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree18(features, 13);
    } else if (val <= 294.50000000000006) {
      return calculateTree18(features, 13);
    } else {
      return calculateTree18(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree18(features, 5);
    } else if (val <= 291.50000000000006) {
      return calculateTree18(features, 5);
    } else {
      return calculateTree18(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree18(features, 1);
    } else if (val <= 282.50000000000006) {
      return calculateTree18(features, 1);
    } else {
      return calculateTree18(features, 2);
    }
  }
}
function calculateTree19(features, node = 0) {
  if (node === 7) return -0.0033644808988333333;
  if (node === 35) return 0.4639585900534782;
  if (node === 36) return -0.3613837304321849;
  if (node === 17) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree19(features, 35);
    } else if (val <= 83.50000000000001) {
      return calculateTree19(features, 35);
    } else {
      return calculateTree19(features, 36);
    }
  }
  if (node === 75) return 0.15064664567001676;
  if (node === 76) return -0.381216433709858;
  if (node === 37) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree19(features, 75);
    } else if (val <= 978.0000000000001) {
      return calculateTree19(features, 75);
    } else {
      return calculateTree19(features, 76);
    }
  }
  if (node === 77) return 0.4838283519337802;
  if (node === 157) return 0.2741929348705825;
  if (node === 158) return -0.3160807596346698;
  if (node === 78) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree19(features, 157);
    } else if (val <= 982.5000000000001) {
      return calculateTree19(features, 157);
    } else {
      return calculateTree19(features, 158);
    }
  }
  if (node === 38) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree19(features, 77);
    } else if (val <= 606.5000000000001) {
      return calculateTree19(features, 77);
    } else {
      return calculateTree19(features, 78);
    }
  }
  if (node === 18) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree19(features, 37);
    } else if (val <= 551.5000000000001) {
      return calculateTree19(features, 37);
    } else {
      return calculateTree19(features, 38);
    }
  }
  if (node === 8) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree19(features, 17);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree19(features, 17);
    } else {
      return calculateTree19(features, 18);
    }
  }
  if (node === 3) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree19(features, 7);
    } else if (val <= 973.5000000000001) {
      return calculateTree19(features, 7);
    } else {
      return calculateTree19(features, 8);
    }
  }
  if (node === 9) return -0.02835161837250965;
  if (node === 351) return 0.017149054949765374;
  if (node === 1411) return -0.3934615791223183;
  if (node === 1412) return 0.2614118571508007;
  if (node === 705) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree19(features, 1411);
    } else if (val <= 472.50000000000006) {
      return calculateTree19(features, 1411);
    } else {
      return calculateTree19(features, 1412);
    }
  }
  if (node === 706) return -0.3953067052686631;
  if (node === 352) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree19(features, 705);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree19(features, 705);
    } else {
      return calculateTree19(features, 706);
    }
  }
  if (node === 175) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree19(features, 351);
    } else if (val <= 464.50000000000006) {
      return calculateTree19(features, 351);
    } else {
      return calculateTree19(features, 352);
    }
  }
  if (node === 707) return -0.41681001768379683;
  if (node === 708) return 0.21106116837522254;
  if (node === 353) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree19(features, 707);
    } else if (val <= 525.0000000000001) {
      return calculateTree19(features, 707);
    } else {
      return calculateTree19(features, 708);
    }
  }
  if (node === 709) return 0.29297153857274055;
  if (node === 710) return -0.34348259816196663;
  if (node === 354) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree19(features, 709);
    } else if (val <= 525.0000000000001) {
      return calculateTree19(features, 709);
    } else {
      return calculateTree19(features, 710);
    }
  }
  if (node === 176) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree19(features, 353);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree19(features, 353);
    } else {
      return calculateTree19(features, 354);
    }
  }
  if (node === 87) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree19(features, 175);
    } else if (val <= 481.50000000000006) {
      return calculateTree19(features, 175);
    } else {
      return calculateTree19(features, 176);
    }
  }
  if (node === 177) return -0.24967629457683907;
  if (node === 178) return 0.35723609647468363;
  if (node === 88) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree19(features, 177);
    } else if (val <= 361.50000000000006) {
      return calculateTree19(features, 177);
    } else {
      return calculateTree19(features, 178);
    }
  }
  if (node === 43) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree19(features, 87);
    } else if (val <= 555.5000000000001) {
      return calculateTree19(features, 87);
    } else {
      return calculateTree19(features, 88);
    }
  }
  if (node === 719) return 0.43956332704201817;
  if (node === 720) return -0.06338692405858591;
  if (node === 359) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree19(features, 719);
    } else if (val <= 577.5000000000001) {
      return calculateTree19(features, 719);
    } else {
      return calculateTree19(features, 720);
    }
  }
  if (node === 721) return -0.4465355470001427;
  if (node === 1445) return 0.3601305418368957;
  if (node === 1446) return -0.3732198118646077;
  if (node === 722) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree19(features, 1445);
    } else if (val <= 222.50000000000003) {
      return calculateTree19(features, 1445);
    } else {
      return calculateTree19(features, 1446);
    }
  }
  if (node === 360) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree19(features, 721);
    } else if (val <= 582.5000000000001) {
      return calculateTree19(features, 721);
    } else {
      return calculateTree19(features, 722);
    }
  }
  if (node === 179) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree19(features, 359);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree19(features, 359);
    } else {
      return calculateTree19(features, 360);
    }
  }
  if (node === 361) return -0.41978597254134203;
  if (node === 362) return 0.6236046338455079;
  if (node === 180) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree19(features, 361);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree19(features, 361);
    } else {
      return calculateTree19(features, 362);
    }
  }
  if (node === 89) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree19(features, 179);
    } else if (val <= 599.5000000000001) {
      return calculateTree19(features, 179);
    } else {
      return calculateTree19(features, 180);
    }
  }
  if (node === 181) return -0.24658698035401985;
  if (node === 365) return 0.14991050128685865;
  if (node === 366) return 0.04501606758651909;
  if (node === 182) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree19(features, 365);
    } else if (val <= 667.5000000000001) {
      return calculateTree19(features, 365);
    } else {
      return calculateTree19(features, 366);
    }
  }
  if (node === 90) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree19(features, 181);
    } else if (val <= 628.5000000000001) {
      return calculateTree19(features, 181);
    } else {
      return calculateTree19(features, 182);
    }
  }
  if (node === 44) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree19(features, 89);
    } else if (val <= 602.5000000000001) {
      return calculateTree19(features, 89);
    } else {
      return calculateTree19(features, 90);
    }
  }
  if (node === 21) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree19(features, 43);
    } else if (val <= 572.0000000000001) {
      return calculateTree19(features, 43);
    } else {
      return calculateTree19(features, 44);
    }
  }
  if (node === 45) return 0.00861312280049623;
  if (node === 46) return -0.17795362367973466;
  if (node === 22) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree19(features, 45);
    } else if (val <= 966.0000000000001) {
      return calculateTree19(features, 45);
    } else {
      return calculateTree19(features, 46);
    }
  }
  if (node === 10) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree19(features, 21);
    } else if (val <= 504.50000000000006) {
      return calculateTree19(features, 21);
    } else {
      return calculateTree19(features, 22);
    }
  }
  if (node === 4) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree19(features, 9);
    } else if (val <= 82.50000000000001) {
      return calculateTree19(features, 9);
    } else {
      return calculateTree19(features, 10);
    }
  }
  if (node === 1) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree19(features, 3);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree19(features, 3);
    } else {
      return calculateTree19(features, 4);
    }
  }
  if (node === 2) return -0.016207846992120422;
  if (node === 0) {
    const val = features['is_preference_men_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree19(features, 1);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree19(features, 1);
    } else {
      return calculateTree19(features, 2);
    }
  }
}
function calculateTree20(features, node = 0) {
  if (node === 1) return 0.003606287913570097;
  if (node === 95) return 1.1928214209099581;
  if (node === 96) return -0.37596231635982663;
  if (node === 47) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree20(features, 95);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree20(features, 95);
    } else {
      return calculateTree20(features, 96);
    }
  }
  if (node === 97) return -0.3899175879814622;
  if (node === 197) return -0.4016939466108924;
  if (node === 198) return 0.40155429649079755;
  if (node === 98) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree20(features, 197);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree20(features, 197);
    } else {
      return calculateTree20(features, 198);
    }
  }
  if (node === 48) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree20(features, 97);
    } else if (val <= 511.50000000000006) {
      return calculateTree20(features, 97);
    } else {
      return calculateTree20(features, 98);
    }
  }
  if (node === 23) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree20(features, 47);
    } else if (val <= 232.50000000000003) {
      return calculateTree20(features, 47);
    } else {
      return calculateTree20(features, 48);
    }
  }
  if (node === 24) return -0.011510483395191307;
  if (node === 11) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree20(features, 23);
    } else if (val <= 522.5000000000001) {
      return calculateTree20(features, 23);
    } else {
      return calculateTree20(features, 24);
    }
  }
  if (node === 25) return -0.23351152709311113;
  if (node === 107) return -0.3919440333972161;
  if (node === 108) return 0.3171451863083428;
  if (node === 53) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree20(features, 107);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree20(features, 107);
    } else {
      return calculateTree20(features, 108);
    }
  }
  if (node === 219) return -0.33387240244703725;
  if (node === 441) return 0.8232674656557503;
  if (node === 442) return 0.0849135301293588;
  if (node === 220) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree20(features, 441);
    } else if (val <= 740.5000000000001) {
      return calculateTree20(features, 441);
    } else {
      return calculateTree20(features, 442);
    }
  }
  if (node === 109) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree20(features, 219);
    } else if (val <= 733.5000000000001) {
      return calculateTree20(features, 219);
    } else {
      return calculateTree20(features, 220);
    }
  }
  if (node === 110) return -0.11639072104640295;
  if (node === 54) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree20(features, 109);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree20(features, 109);
    } else {
      return calculateTree20(features, 110);
    }
  }
  if (node === 26) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree20(features, 53);
    } else if (val <= 635.0000000000001) {
      return calculateTree20(features, 53);
    } else {
      return calculateTree20(features, 54);
    }
  }
  if (node === 12) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree20(features, 25);
    } else if (val <= 606.5000000000001) {
      return calculateTree20(features, 25);
    } else {
      return calculateTree20(features, 26);
    }
  }
  if (node === 5) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree20(features, 11);
    } else if (val <= 427.50000000000006) {
      return calculateTree20(features, 11);
    } else {
      return calculateTree20(features, 12);
    }
  }
  if (node === 55) return -0.14359423239395047;
  if (node === 227) return 0.24742468477127144;
  if (node === 228) return -0.38012812676644864;
  if (node === 113) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree20(features, 227);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree20(features, 227);
    } else {
      return calculateTree20(features, 228);
    }
  }
  if (node === 1839) return 0.012150350021104891;
  if (node === 1840) return 0.22244965674566186;
  if (node === 919) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree20(features, 1839);
    } else if (val <= 877.5000000000001) {
      return calculateTree20(features, 1839);
    } else {
      return calculateTree20(features, 1840);
    }
  }
  if (node === 1841) return -0.3573195003031004;
  if (node === 1842) return -0.06867341109180286;
  if (node === 920) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree20(features, 1841);
    } else if (val <= 925.5000000000001) {
      return calculateTree20(features, 1841);
    } else {
      return calculateTree20(features, 1842);
    }
  }
  if (node === 459) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree20(features, 919);
    } else if (val <= 893.0000000000001) {
      return calculateTree20(features, 919);
    } else {
      return calculateTree20(features, 920);
    }
  }
  if (node === 460) return -0.2259516105783002;
  if (node === 229) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree20(features, 459);
    } else if (val <= 150.50000000000003) {
      return calculateTree20(features, 459);
    } else {
      return calculateTree20(features, 460);
    }
  }
  if (node === 461) return 0.14517434354818462;
  if (node === 925) return -0.2224512631307074;
  if (node === 1853) return 0.08189694748887631;
  if (node === 1854) return -0.0039024041728107153;
  if (node === 926) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree20(features, 1853);
    } else if (val <= 708.5000000000001) {
      return calculateTree20(features, 1853);
    } else {
      return calculateTree20(features, 1854);
    }
  }
  if (node === 462) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree20(features, 925);
    } else if (val <= 217.50000000000003) {
      return calculateTree20(features, 925);
    } else {
      return calculateTree20(features, 926);
    }
  }
  if (node === 230) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree20(features, 461);
    } else if (val <= 208.50000000000003) {
      return calculateTree20(features, 461);
    } else {
      return calculateTree20(features, 462);
    }
  }
  if (node === 114) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree20(features, 229);
    } else if (val <= 191.50000000000003) {
      return calculateTree20(features, 229);
    } else {
      return calculateTree20(features, 230);
    }
  }
  if (node === 56) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree20(features, 113);
    } else if (val <= 30.500000000000004) {
      return calculateTree20(features, 113);
    } else {
      return calculateTree20(features, 114);
    }
  }
  if (node === 27) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree20(features, 55);
    } else if (val <= 28.500000000000004) {
      return calculateTree20(features, 55);
    } else {
      return calculateTree20(features, 56);
    }
  }
  if (node === 57) return -0.6089084148213221;
  if (node === 58) return 0.14328574962404853;
  if (node === 28) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree20(features, 57);
    } else if (val <= 925.5000000000001) {
      return calculateTree20(features, 57);
    } else {
      return calculateTree20(features, 58);
    }
  }
  if (node === 13) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree20(features, 27);
    } else if (val <= 905.5000000000001) {
      return calculateTree20(features, 27);
    } else {
      return calculateTree20(features, 28);
    }
  }
  if (node === 59) return -0.3918256649625475;
  if (node === 60) return 0.012536227144653987;
  if (node === 29) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree20(features, 59);
    } else if (val <= 709.5000000000001) {
      return calculateTree20(features, 59);
    } else {
      return calculateTree20(features, 60);
    }
  }
  if (node === 61) return -0.32864907011343675;
  if (node === 62) return 0.12764980966056239;
  if (node === 30) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree20(features, 61);
    } else if (val <= 953.5000000000001) {
      return calculateTree20(features, 61);
    } else {
      return calculateTree20(features, 62);
    }
  }
  if (node === 14) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree20(features, 29);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree20(features, 29);
    } else {
      return calculateTree20(features, 30);
    }
  }
  if (node === 6) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree20(features, 13);
    } else if (val <= 919.5000000000001) {
      return calculateTree20(features, 13);
    } else {
      return calculateTree20(features, 14);
    }
  }
  if (node === 2) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree20(features, 5);
    } else if (val <= 677.5000000000001) {
      return calculateTree20(features, 5);
    } else {
      return calculateTree20(features, 6);
    }
  }
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree20(features, 1);
    } else if (val <= 507.00000000000006) {
      return calculateTree20(features, 1);
    } else {
      return calculateTree20(features, 2);
    }
  }
}
function calculateTree21(features, node = 0) {
  if (node === 15) return 0.0006612587439182132;
  if (node === 67) return -0.1325984223714122;
  if (node === 68) return -0.3470378322376712;
  if (node === 33) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree21(features, 67);
    } else if (val <= 386.00000000000006) {
      return calculateTree21(features, 67);
    } else {
      return calculateTree21(features, 68);
    }
  }
  if (node === 279) return -0.11030566815726904;
  if (node === 280) return 0.11888859540089283;
  if (node === 139) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree21(features, 279);
    } else if (val <= 391.00000000000006) {
      return calculateTree21(features, 279);
    } else {
      return calculateTree21(features, 280);
    }
  }
  if (node === 140) return 0.159769949932366;
  if (node === 69) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree21(features, 139);
    } else if (val <= 26.500000000000004) {
      return calculateTree21(features, 139);
    } else {
      return calculateTree21(features, 140);
    }
  }
  if (node === 70) return -0.11143326038701498;
  if (node === 34) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree21(features, 69);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree21(features, 69);
    } else {
      return calculateTree21(features, 70);
    }
  }
  if (node === 16) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree21(features, 33);
    } else if (val <= 389.00000000000006) {
      return calculateTree21(features, 33);
    } else {
      return calculateTree21(features, 34);
    }
  }
  if (node === 7) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree21(features, 15);
    } else if (val <= 361.50000000000006) {
      return calculateTree21(features, 15);
    } else {
      return calculateTree21(features, 16);
    }
  }
  if (node === 35) return 0.10709412574602029;
  if (node === 36) return -0.015582407568344649;
  if (node === 17) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree21(features, 35);
    } else if (val <= 26.500000000000004) {
      return calculateTree21(features, 35);
    } else {
      return calculateTree21(features, 36);
    }
  }
  if (node === 18) return 0.0063816273456716414;
  if (node === 8) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree21(features, 17);
    } else if (val <= 412.00000000000006) {
      return calculateTree21(features, 17);
    } else {
      return calculateTree21(features, 18);
    }
  }
  if (node === 3) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree21(features, 7);
    } else if (val <= 409.50000000000006) {
      return calculateTree21(features, 7);
    } else {
      return calculateTree21(features, 8);
    }
  }
  if (node === 159) return -0.01590001443617111;
  if (node === 321) return 0.4056491972475388;
  if (node === 645) return -0.411764346170809;
  if (node === 646) return 0.09173904091550407;
  if (node === 322) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree21(features, 645);
    } else if (val <= 799.5000000000001) {
      return calculateTree21(features, 645);
    } else {
      return calculateTree21(features, 646);
    }
  }
  if (node === 160) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree21(features, 321);
    } else if (val <= 795.5000000000001) {
      return calculateTree21(features, 321);
    } else {
      return calculateTree21(features, 322);
    }
  }
  if (node === 79) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree21(features, 159);
    } else if (val <= 783.5000000000001) {
      return calculateTree21(features, 159);
    } else {
      return calculateTree21(features, 160);
    }
  }
  if (node === 161) return -0.37262506557110114;
  if (node === 162) return -0.08820980255615392;
  if (node === 80) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree21(features, 161);
    } else if (val <= 829.0000000000001) {
      return calculateTree21(features, 161);
    } else {
      return calculateTree21(features, 162);
    }
  }
  if (node === 39) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree21(features, 79);
    } else if (val <= 819.0000000000001) {
      return calculateTree21(features, 79);
    } else {
      return calculateTree21(features, 80);
    }
  }
  if (node === 81) return -0.20027250862415216;
  if (node === 165) return 0.044796680503771544;
  if (node === 166) return -0.046432910453390076;
  if (node === 82) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree21(features, 165);
    } else if (val <= 619.5000000000001) {
      return calculateTree21(features, 165);
    } else {
      return calculateTree21(features, 166);
    }
  }
  if (node === 40) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree21(features, 81);
    } else if (val <= 575.5000000000001) {
      return calculateTree21(features, 81);
    } else {
      return calculateTree21(features, 82);
    }
  }
  if (node === 19) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree21(features, 39);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree21(features, 39);
    } else {
      return calculateTree21(features, 40);
    }
  }
  if (node === 167) return 0.2796612370531587;
  if (node === 168) return -0.3616624162049527;
  if (node === 83) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree21(features, 167);
    } else if (val <= 851.5000000000001) {
      return calculateTree21(features, 167);
    } else {
      return calculateTree21(features, 168);
    }
  }
  if (node === 84) return 0.2047802590541145;
  if (node === 41) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree21(features, 83);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree21(features, 83);
    } else {
      return calculateTree21(features, 84);
    }
  }
  if (node === 42) return -0.0010120574654672156;
  if (node === 20) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree21(features, 41);
    } else if (val <= 859.5000000000001) {
      return calculateTree21(features, 41);
    } else {
      return calculateTree21(features, 42);
    }
  }
  if (node === 9) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree21(features, 19);
    } else if (val <= 850.5000000000001) {
      return calculateTree21(features, 19);
    } else {
      return calculateTree21(features, 20);
    }
  }
  if (node === 10) return 0.002504941632978547;
  if (node === 4) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree21(features, 9);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree21(features, 9);
    } else {
      return calculateTree21(features, 10);
    }
  }
  if (node === 1) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree21(features, 3);
    } else if (val <= 507.00000000000006) {
      return calculateTree21(features, 3);
    } else {
      return calculateTree21(features, 4);
    }
  }
  if (node === 11) return -0.01488384109356245;
  if (node === 12) return -0.3635170320058574;
  if (node === 5) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree21(features, 11);
    } else if (val <= 148.50000000000003) {
      return calculateTree21(features, 11);
    } else {
      return calculateTree21(features, 12);
    }
  }
  if (node === 13) return 0.19321378355252405;
  if (node === 119) return 0.022328926049028697;
  if (node === 120) return 0.3973417604826988;
  if (node === 59) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree21(features, 119);
    } else if (val <= 900.5000000000001) {
      return calculateTree21(features, 119);
    } else {
      return calculateTree21(features, 120);
    }
  }
  if (node === 121) return 0.1155889617267433;
  if (node === 122) return -0.17831654009803838;
  if (node === 60) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree21(features, 121);
    } else if (val <= 320.00000000000006) {
      return calculateTree21(features, 121);
    } else {
      return calculateTree21(features, 122);
    }
  }
  if (node === 29) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree21(features, 59);
    } else if (val <= 925.5000000000001) {
      return calculateTree21(features, 59);
    } else {
      return calculateTree21(features, 60);
    }
  }
  if (node === 30) return 0.14068706564219693;
  if (node === 14) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree21(features, 29);
    } else if (val <= 963.0000000000001) {
      return calculateTree21(features, 29);
    } else {
      return calculateTree21(features, 30);
    }
  }
  if (node === 6) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree21(features, 13);
    } else if (val <= 178.50000000000003) {
      return calculateTree21(features, 13);
    } else {
      return calculateTree21(features, 14);
    }
  }
  if (node === 2) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree21(features, 5);
    } else if (val <= 166.00000000000003) {
      return calculateTree21(features, 5);
    } else {
      return calculateTree21(features, 6);
    }
  }
  if (node === 0) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree21(features, 1);
    } else if (val <= 28.500000000000004) {
      return calculateTree21(features, 1);
    } else {
      return calculateTree21(features, 2);
    }
  }
}
function calculateTree22(features, node = 0) {
  if (node === 63) return 0.0001139828118584065;
  if (node === 64) return -0.3133187999401855;
  if (node === 31) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree22(features, 63);
    } else if (val <= 566.0000000000001) {
      return calculateTree22(features, 63);
    } else {
      return calculateTree22(features, 64);
    }
  }
  if (node === 131) return 0.43982946845132226;
  if (node === 132) return -0.2102770444693964;
  if (node === 65) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree22(features, 131);
    } else if (val <= 511.50000000000006) {
      return calculateTree22(features, 131);
    } else {
      return calculateTree22(features, 132);
    }
  }
  if (node === 66) return -0.42807680972763545;
  if (node === 32) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree22(features, 65);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree22(features, 65);
    } else {
      return calculateTree22(features, 66);
    }
  }
  if (node === 15) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree22(features, 31);
    } else if (val <= 572.0000000000001) {
      return calculateTree22(features, 31);
    } else {
      return calculateTree22(features, 32);
    }
  }
  if (node === 67) return -0.2294115418618157;
  if (node === 275) return -0.41889684056817844;
  if (node === 276) return 0.560981063110044;
  if (node === 137) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree22(features, 275);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree22(features, 275);
    } else {
      return calculateTree22(features, 276);
    }
  }
  if (node === 277) return -0.20994889450989987;
  if (node === 557) return -0.4520245687002769;
  if (node === 558) return 0.3798154152530348;
  if (node === 278) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree22(features, 557);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree22(features, 557);
    } else {
      return calculateTree22(features, 558);
    }
  }
  if (node === 138) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree22(features, 277);
    } else if (val <= 504.50000000000006) {
      return calculateTree22(features, 277);
    } else {
      return calculateTree22(features, 278);
    }
  }
  if (node === 68) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree22(features, 137);
    } else if (val <= 222.50000000000003) {
      return calculateTree22(features, 137);
    } else {
      return calculateTree22(features, 138);
    }
  }
  if (node === 33) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree22(features, 67);
    } else if (val <= 175.50000000000003) {
      return calculateTree22(features, 67);
    } else {
      return calculateTree22(features, 68);
    }
  }
  if (node === 34) return -0.29336183569367175;
  if (node === 16) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree22(features, 33);
    } else if (val <= 507.00000000000006) {
      return calculateTree22(features, 33);
    } else {
      return calculateTree22(features, 34);
    }
  }
  if (node === 7) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree22(features, 15);
    } else if (val <= 577.5000000000001) {
      return calculateTree22(features, 15);
    } else {
      return calculateTree22(features, 16);
    }
  }
  if (node === 17) return -0.38797851387646537;
  if (node === 18) return 0.39225983725607244;
  if (node === 8) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree22(features, 17);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree22(features, 17);
    } else {
      return calculateTree22(features, 18);
    }
  }
  if (node === 3) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree22(features, 7);
    } else if (val <= 599.5000000000001) {
      return calculateTree22(features, 7);
    } else {
      return calculateTree22(features, 8);
    }
  }
  if (node === 19) return -0.32908355503698117;
  if (node === 41) return 0.5207787851488807;
  if (node === 42) return -0.37875766618546364;
  if (node === 20) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree22(features, 41);
    } else if (val <= 865.0000000000001) {
      return calculateTree22(features, 41);
    } else {
      return calculateTree22(features, 42);
    }
  }
  if (node === 9) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree22(features, 19);
    } else if (val <= 734.0000000000001) {
      return calculateTree22(features, 19);
    } else {
      return calculateTree22(features, 20);
    }
  }
  if (node === 43) return -0.4264647223689353;
  if (node === 44) return -0.11052268041180556;
  if (node === 21) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree22(features, 43);
    } else if (val <= 606.5000000000001) {
      return calculateTree22(features, 43);
    } else {
      return calculateTree22(features, 44);
    }
  }
  if (node === 45) return 0.29401295525331733;
  if (node === 46) return -0.041790486284375365;
  if (node === 22) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree22(features, 45);
    } else if (val <= 635.0000000000001) {
      return calculateTree22(features, 45);
    } else {
      return calculateTree22(features, 46);
    }
  }
  if (node === 10) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree22(features, 21);
    } else if (val <= 628.5000000000001) {
      return calculateTree22(features, 21);
    } else {
      return calculateTree22(features, 22);
    }
  }
  if (node === 4) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree22(features, 9);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree22(features, 9);
    } else {
      return calculateTree22(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree22(features, 3);
    } else if (val <= 602.5000000000001) {
      return calculateTree22(features, 3);
    } else {
      return calculateTree22(features, 4);
    }
  }
  if (node === 23) return 0.2606340234314548;
  if (node === 24) return -0.32517840769527095;
  if (node === 11) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree22(features, 23);
    } else if (val <= 34.00000000000001) {
      return calculateTree22(features, 23);
    } else {
      return calculateTree22(features, 24);
    }
  }
  if (node === 25) return -0.44869250876154515;
  if (node === 53) return 0.21408075378610536;
  if (node === 54) return -0.005473113117393076;
  if (node === 26) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree22(features, 53);
    } else if (val <= 668.0000000000001) {
      return calculateTree22(features, 53);
    } else {
      return calculateTree22(features, 54);
    }
  }
  if (node === 12) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree22(features, 25);
    } else if (val <= 34.00000000000001) {
      return calculateTree22(features, 25);
    } else {
      return calculateTree22(features, 26);
    }
  }
  if (node === 5) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree22(features, 11);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree22(features, 11);
    } else {
      return calculateTree22(features, 12);
    }
  }
  if (node === 55) return 0.12861930769893856;
  if (node === 56) return -0.29454771391637763;
  if (node === 27) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree22(features, 55);
    } else if (val <= 685.0000000000001) {
      return calculateTree22(features, 55);
    } else {
      return calculateTree22(features, 56);
    }
  }
  if (node === 57) return -0.35223850034156834;
  if (node === 117) return 0.1815116382361008;
  if (node === 118) return -0.26512625222428465;
  if (node === 58) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree22(features, 117);
    } else if (val <= 381.50000000000006) {
      return calculateTree22(features, 117);
    } else {
      return calculateTree22(features, 118);
    }
  }
  if (node === 28) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree22(features, 57);
    } else if (val <= 677.5000000000001) {
      return calculateTree22(features, 57);
    } else {
      return calculateTree22(features, 58);
    }
  }
  if (node === 13) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree22(features, 27);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree22(features, 27);
    } else {
      return calculateTree22(features, 28);
    }
  }
  if (node === 14) return 0.004977604765801268;
  if (node === 6) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree22(features, 13);
    } else if (val <= 709.5000000000001) {
      return calculateTree22(features, 13);
    } else {
      return calculateTree22(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree22(features, 5);
    } else if (val <= 662.5000000000001) {
      return calculateTree22(features, 5);
    } else {
      return calculateTree22(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree22(features, 1);
    } else if (val <= 647.0000000000001) {
      return calculateTree22(features, 1);
    } else {
      return calculateTree22(features, 2);
    }
  }
}
function calculateTree23(features, node = 0) {
  if (node === 3) return -0.4509158221907634;
  if (node === 9) return -0.004209703090714998;
  if (node === 21) return 0.09810494271074521;
  if (node === 22) return -0.34558170983212194;
  if (node === 10) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree23(features, 21);
    } else if (val <= 893.0000000000001) {
      return calculateTree23(features, 21);
    } else {
      return calculateTree23(features, 22);
    }
  }
  if (node === 4) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree23(features, 9);
    } else if (val <= 28.500000000000004) {
      return calculateTree23(features, 9);
    } else {
      return calculateTree23(features, 10);
    }
  }
  if (node === 1) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree23(features, 3);
    } else if (val <= 113.50000000000001) {
      return calculateTree23(features, 3);
    } else {
      return calculateTree23(features, 4);
    }
  }
  if (node === 47) return -0.40494759574859335;
  if (node === 97) return 0.49637198244931036;
  if (node === 1583) return 0.39568912478284163;
  if (node === 1584) return -0.3538704421397269;
  if (node === 791) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree23(features, 1583);
    } else if (val <= 229.50000000000003) {
      return calculateTree23(features, 1583);
    } else {
      return calculateTree23(features, 1584);
    }
  }
  if (node === 792) return 0.18487891109946464;
  if (node === 395) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree23(features, 791);
    } else if (val <= 850.5000000000001) {
      return calculateTree23(features, 791);
    } else {
      return calculateTree23(features, 792);
    }
  }
  if (node === 793) return 0.1375218510743385;
  if (node === 794) return -0.3616407224793605;
  if (node === 396) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree23(features, 793);
    } else if (val <= 778.5000000000001) {
      return calculateTree23(features, 793);
    } else {
      return calculateTree23(features, 794);
    }
  }
  if (node === 197) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree23(features, 395);
    } else if (val <= 41.50000000000001) {
      return calculateTree23(features, 395);
    } else {
      return calculateTree23(features, 396);
    }
  }
  if (node === 397) return -0.2727637503415265;
  if (node === 797) return -0.267995967227468;
  if (node === 798) return 0.14713421776075147;
  if (node === 398) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree23(features, 797);
    } else if (val <= 327.50000000000006) {
      return calculateTree23(features, 797);
    } else {
      return calculateTree23(features, 798);
    }
  }
  if (node === 198) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree23(features, 397);
    } else if (val <= 83.50000000000001) {
      return calculateTree23(features, 397);
    } else {
      return calculateTree23(features, 398);
    }
  }
  if (node === 98) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree23(features, 197);
    } else if (val <= 75.50000000000001) {
      return calculateTree23(features, 197);
    } else {
      return calculateTree23(features, 198);
    }
  }
  if (node === 48) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree23(features, 97);
    } else if (val <= 130.00000000000003) {
      return calculateTree23(features, 97);
    } else {
      return calculateTree23(features, 98);
    }
  }
  if (node === 23) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree23(features, 47);
    } else if (val <= 115.50000000000001) {
      return calculateTree23(features, 47);
    } else {
      return calculateTree23(features, 48);
    }
  }
  if (node === 24) return -0.3455558841409953;
  if (node === 11) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree23(features, 23);
    } else if (val <= 935.5000000000001) {
      return calculateTree23(features, 23);
    } else {
      return calculateTree23(features, 24);
    }
  }
  if (node === 51) return 0.14557405372299392;
  if (node === 105) return -0.3954134174095652;
  if (node === 427) return 0.3595551819909429;
  if (node === 428) return -0.37569397157789414;
  if (node === 213) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree23(features, 427);
    } else if (val <= 66.00000000000001) {
      return calculateTree23(features, 427);
    } else {
      return calculateTree23(features, 428);
    }
  }
  if (node === 429) return -0.28666852674590165;
  if (node === 861) return -0.20154332523528684;
  if (node === 1725) return 0.816274679463755;
  if (node === 1726) return -0.3866740881355976;
  if (node === 862) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree23(features, 1725);
    } else if (val <= 893.0000000000001) {
      return calculateTree23(features, 1725);
    } else {
      return calculateTree23(features, 1726);
    }
  }
  if (node === 430) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree23(features, 861);
    } else if (val <= 778.5000000000001) {
      return calculateTree23(features, 861);
    } else {
      return calculateTree23(features, 862);
    }
  }
  if (node === 214) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree23(features, 429);
    } else if (val <= 70.00000000000001) {
      return calculateTree23(features, 429);
    } else {
      return calculateTree23(features, 430);
    }
  }
  if (node === 106) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree23(features, 213);
    } else if (val <= 240.00000000000003) {
      return calculateTree23(features, 213);
    } else {
      return calculateTree23(features, 214);
    }
  }
  if (node === 52) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree23(features, 105);
    } else if (val <= 229.50000000000003) {
      return calculateTree23(features, 105);
    } else {
      return calculateTree23(features, 106);
    }
  }
  if (node === 25) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree23(features, 51);
    } else if (val <= 115.50000000000001) {
      return calculateTree23(features, 51);
    } else {
      return calculateTree23(features, 52);
    }
  }
  if (node === 26) return 0.15354542654766587;
  if (node === 12) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree23(features, 25);
    } else if (val <= 935.5000000000001) {
      return calculateTree23(features, 25);
    } else {
      return calculateTree23(features, 26);
    }
  }
  if (node === 5) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree23(features, 11);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree23(features, 11);
    } else {
      return calculateTree23(features, 12);
    }
  }
  if (node === 55) return -0.013966652468400632;
  if (node === 113) return 0.13636883401598257;
  if (node === 114) return -0.34955396167733593;
  if (node === 56) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree23(features, 113);
    } else if (val <= 845.5000000000001) {
      return calculateTree23(features, 113);
    } else {
      return calculateTree23(features, 114);
    }
  }
  if (node === 27) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree23(features, 55);
    } else if (val <= 934.0000000000001) {
      return calculateTree23(features, 55);
    } else {
      return calculateTree23(features, 56);
    }
  }
  if (node === 28) return -0.37210558269593247;
  if (node === 13) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree23(features, 27);
    } else if (val <= 975.0000000000001) {
      return calculateTree23(features, 27);
    } else {
      return calculateTree23(features, 28);
    }
  }
  if (node === 29) return 0.008904548229115058;
  if (node === 61) return -0.2746588361731981;
  if (node === 125) return 0.15820201489413702;
  if (node === 126) return -0.29217123222199837;
  if (node === 62) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree23(features, 125);
    } else if (val <= 992.5000000000001) {
      return calculateTree23(features, 125);
    } else {
      return calculateTree23(features, 126);
    }
  }
  if (node === 30) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree23(features, 61);
    } else if (val <= 983.5000000000001) {
      return calculateTree23(features, 61);
    } else {
      return calculateTree23(features, 62);
    }
  }
  if (node === 14) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree23(features, 29);
    } else if (val <= 962.5000000000001) {
      return calculateTree23(features, 29);
    } else {
      return calculateTree23(features, 30);
    }
  }
  if (node === 6) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree23(features, 13);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree23(features, 13);
    } else {
      return calculateTree23(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree23(features, 5);
    } else if (val <= 86.50000000000001) {
      return calculateTree23(features, 5);
    } else {
      return calculateTree23(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree23(features, 1);
    } else if (val <= 18.500000000000004) {
      return calculateTree23(features, 1);
    } else {
      return calculateTree23(features, 2);
    }
  }
}
function calculateTree24(features, node = 0) {
  if (node === 255) return -0.000820166143779532;
  if (node === 256) return -0.050260653448712625;
  if (node === 127) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree24(features, 255);
    } else if (val <= 760.0000000000001) {
      return calculateTree24(features, 255);
    } else {
      return calculateTree24(features, 256);
    }
  }
  if (node === 128) return 0.07439069136461213;
  if (node === 63) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree24(features, 127);
    } else if (val <= 804.5000000000001) {
      return calculateTree24(features, 127);
    } else {
      return calculateTree24(features, 128);
    }
  }
  if (node === 64) return -0.07498409481210688;
  if (node === 31) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree24(features, 63);
    } else if (val <= 819.5000000000001) {
      return calculateTree24(features, 63);
    } else {
      return calculateTree24(features, 64);
    }
  }
  if (node === 131) return -0.28726008473007364;
  if (node === 132) return 0.31782724449500577;
  if (node === 65) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree24(features, 131);
    } else if (val <= 846.5000000000001) {
      return calculateTree24(features, 131);
    } else {
      return calculateTree24(features, 132);
    }
  }
  if (node === 133) return 0.3108035073102609;
  if (node === 539) return 0.04448574794498269;
  if (node === 540) return 0.21299149366522327;
  if (node === 269) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree24(features, 539);
    } else if (val <= 29.500000000000004) {
      return calculateTree24(features, 539);
    } else {
      return calculateTree24(features, 540);
    }
  }
  if (node === 270) return -0.35871468770990206;
  if (node === 134) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree24(features, 269);
    } else if (val <= 846.5000000000001) {
      return calculateTree24(features, 269);
    } else {
      return calculateTree24(features, 270);
    }
  }
  if (node === 66) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree24(features, 133);
    } else if (val <= 835.5000000000001) {
      return calculateTree24(features, 133);
    } else {
      return calculateTree24(features, 134);
    }
  }
  if (node === 32) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree24(features, 65);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree24(features, 65);
    } else {
      return calculateTree24(features, 66);
    }
  }
  if (node === 15) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree24(features, 31);
    } else if (val <= 834.5000000000001) {
      return calculateTree24(features, 31);
    } else {
      return calculateTree24(features, 32);
    }
  }
  if (node === 543) return -0.1482454566998232;
  if (node === 544) return 0.14445300431142988;
  if (node === 271) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree24(features, 543);
    } else if (val <= 866.5000000000001) {
      return calculateTree24(features, 543);
    } else {
      return calculateTree24(features, 544);
    }
  }
  if (node === 272) return -0.3706045290672342;
  if (node === 135) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree24(features, 271);
    } else if (val <= 868.0000000000001) {
      return calculateTree24(features, 271);
    } else {
      return calculateTree24(features, 272);
    }
  }
  if (node === 136) return 0.3112861119193756;
  if (node === 67) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree24(features, 135);
    } else if (val <= 882.5000000000001) {
      return calculateTree24(features, 135);
    } else {
      return calculateTree24(features, 136);
    }
  }
  if (node === 68) return -0.3105911305985709;
  if (node === 33) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree24(features, 67);
    } else if (val <= 888.0000000000001) {
      return calculateTree24(features, 67);
    } else {
      return calculateTree24(features, 68);
    }
  }
  if (node === 69) return -0.13349224105667054;
  if (node === 70) return 0.029589956505895847;
  if (node === 34) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree24(features, 69);
    } else if (val <= 868.0000000000001) {
      return calculateTree24(features, 69);
    } else {
      return calculateTree24(features, 70);
    }
  }
  if (node === 16) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree24(features, 33);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree24(features, 33);
    } else {
      return calculateTree24(features, 34);
    }
  }
  if (node === 7) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree24(features, 15);
    } else if (val <= 852.5000000000001) {
      return calculateTree24(features, 15);
    } else {
      return calculateTree24(features, 16);
    }
  }
  if (node === 17) return 0.03466953545936165;
  if (node === 18) return 0.16772590878832194;
  if (node === 8) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree24(features, 17);
    } else if (val <= 6.500000000000001) {
      return calculateTree24(features, 17);
    } else {
      return calculateTree24(features, 18);
    }
  }
  if (node === 3) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree24(features, 7);
    } else if (val <= 902.0000000000001) {
      return calculateTree24(features, 7);
    } else {
      return calculateTree24(features, 8);
    }
  }
  if (node === 4) return -0.04196367439049984;
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree24(features, 3);
    } else if (val <= 908.5000000000001) {
      return calculateTree24(features, 3);
    } else {
      return calculateTree24(features, 4);
    }
  }
  if (node === 11) return 0.2760999648731418;
  if (node === 12) return -0.3505804555047948;
  if (node === 5) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree24(features, 11);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree24(features, 11);
    } else {
      return calculateTree24(features, 12);
    }
  }
  if (node === 27) return -0.35169074169483266;
  if (node === 231) return 0.10257310726125599;
  if (node === 232) return -0.197742978537044;
  if (node === 115) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree24(features, 231);
    } else if (val <= 966.0000000000001) {
      return calculateTree24(features, 231);
    } else {
      return calculateTree24(features, 232);
    }
  }
  if (node === 116) return 0.30351166363610715;
  if (node === 57) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree24(features, 115);
    } else if (val <= 972.5000000000001) {
      return calculateTree24(features, 115);
    } else {
      return calculateTree24(features, 116);
    }
  }
  if (node === 58) return 0.036786744096314;
  if (node === 28) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree24(features, 57);
    } else if (val <= 973.5000000000001) {
      return calculateTree24(features, 57);
    } else {
      return calculateTree24(features, 58);
    }
  }
  if (node === 13) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree24(features, 27);
    } else if (val <= 962.5000000000001) {
      return calculateTree24(features, 27);
    } else {
      return calculateTree24(features, 28);
    }
  }
  if (node === 59) return 0.05989335914321956;
  if (node === 60) return 0.22285887721158018;
  if (node === 29) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree24(features, 59);
    } else if (val <= 26.500000000000004) {
      return calculateTree24(features, 59);
    } else {
      return calculateTree24(features, 60);
    }
  }
  if (node === 61) return -0.2395955969077564;
  if (node === 125) return 0.09555782005941084;
  if (node === 126) return -0.2566055194897248;
  if (node === 62) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree24(features, 125);
    } else if (val <= 992.5000000000001) {
      return calculateTree24(features, 125);
    } else {
      return calculateTree24(features, 126);
    }
  }
  if (node === 30) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree24(features, 61);
    } else if (val <= 983.5000000000001) {
      return calculateTree24(features, 61);
    } else {
      return calculateTree24(features, 62);
    }
  }
  if (node === 14) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree24(features, 29);
    } else if (val <= 962.5000000000001) {
      return calculateTree24(features, 29);
    } else {
      return calculateTree24(features, 30);
    }
  }
  if (node === 6) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree24(features, 13);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree24(features, 13);
    } else {
      return calculateTree24(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree24(features, 5);
    } else if (val <= 955.5000000000001) {
      return calculateTree24(features, 5);
    } else {
      return calculateTree24(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree24(features, 1);
    } else if (val <= 954.5000000000001) {
      return calculateTree24(features, 1);
    } else {
      return calculateTree24(features, 2);
    }
  }
}
function calculateTree25(features, node = 0) {
  if (node === 63) return -0.00013659939260184215;
  if (node === 519) return -0.2703784567039701;
  if (node === 520) return 0.5767008529454976;
  if (node === 259) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree25(features, 519);
    } else if (val <= 251.00000000000003) {
      return calculateTree25(features, 519);
    } else {
      return calculateTree25(features, 520);
    }
  }
  if (node === 260) return -0.4004781590165735;
  if (node === 129) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree25(features, 259);
    } else if (val <= 511.50000000000006) {
      return calculateTree25(features, 259);
    } else {
      return calculateTree25(features, 260);
    }
  }
  if (node === 261) return -0.0982529956315425;
  if (node === 262) return 0.1878298965364499;
  if (node === 130) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree25(features, 261);
    } else if (val <= 511.50000000000006) {
      return calculateTree25(features, 261);
    } else {
      return calculateTree25(features, 262);
    }
  }
  if (node === 64) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree25(features, 129);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree25(features, 129);
    } else {
      return calculateTree25(features, 130);
    }
  }
  if (node === 31) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree25(features, 63);
    } else if (val <= 241.50000000000003) {
      return calculateTree25(features, 63);
    } else {
      return calculateTree25(features, 64);
    }
  }
  if (node === 32) return -0.2754462629838229;
  if (node === 15) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree25(features, 31);
    } else if (val <= 969.5000000000001) {
      return calculateTree25(features, 31);
    } else {
      return calculateTree25(features, 32);
    }
  }
  if (node === 33) return 0.037491898694116815;
  if (node === 139) return -0.3353753637027613;
  if (node === 140) return 0.19189843142063798;
  if (node === 69) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree25(features, 139);
    } else if (val <= 969.5000000000001) {
      return calculateTree25(features, 139);
    } else {
      return calculateTree25(features, 140);
    }
  }
  if (node === 70) return -0.3888730974647037;
  if (node === 34) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree25(features, 69);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree25(features, 69);
    } else {
      return calculateTree25(features, 70);
    }
  }
  if (node === 16) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree25(features, 33);
    } else if (val <= 659.5000000000001) {
      return calculateTree25(features, 33);
    } else {
      return calculateTree25(features, 34);
    }
  }
  if (node === 7) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree25(features, 15);
    } else if (val <= 253.50000000000003) {
      return calculateTree25(features, 15);
    } else {
      return calculateTree25(features, 16);
    }
  }
  if (node === 8) return 0.11692634479870417;
  if (node === 3) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree25(features, 7);
    } else if (val <= 275.50000000000006) {
      return calculateTree25(features, 7);
    } else {
      return calculateTree25(features, 8);
    }
  }
  if (node === 4) return -0.006174510315158885;
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree25(features, 3);
    } else if (val <= 282.50000000000006) {
      return calculateTree25(features, 3);
    } else {
      return calculateTree25(features, 4);
    }
  }
  if (node === 23) return 0.1613718880560043;
  if (node === 24) return -0.3479584083908959;
  if (node === 11) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree25(features, 23);
    } else if (val <= 993.5000000000001) {
      return calculateTree25(features, 23);
    } else {
      return calculateTree25(features, 24);
    }
  }
  if (node === 25) return -0.361450473479574;
  if (node === 107) return 0.07637721711553126;
  if (node === 108) return 0.3095948786731594;
  if (node === 53) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree25(features, 107);
    } else if (val <= 21.500000000000004) {
      return calculateTree25(features, 107);
    } else {
      return calculateTree25(features, 108);
    }
  }
  if (node === 109) return -0.2899442735187848;
  if (node === 221) return 0.061643103318269145;
  if (node === 445) return 0.07998786052072802;
  if (node === 446) return -0.29012764990323137;
  if (node === 222) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree25(features, 445);
    } else if (val <= 966.0000000000001) {
      return calculateTree25(features, 445);
    } else {
      return calculateTree25(features, 446);
    }
  }
  if (node === 110) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree25(features, 221);
    } else if (val <= 661.0000000000001) {
      return calculateTree25(features, 221);
    } else {
      return calculateTree25(features, 222);
    }
  }
  if (node === 54) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree25(features, 109);
    } else if (val <= 962.5000000000001) {
      return calculateTree25(features, 109);
    } else {
      return calculateTree25(features, 110);
    }
  }
  if (node === 26) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree25(features, 53);
    } else if (val <= 957.0000000000001) {
      return calculateTree25(features, 53);
    } else {
      return calculateTree25(features, 54);
    }
  }
  if (node === 12) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree25(features, 25);
    } else if (val <= 283.50000000000006) {
      return calculateTree25(features, 25);
    } else {
      return calculateTree25(features, 26);
    }
  }
  if (node === 5) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree25(features, 11);
    } else if (val <= 127.50000000000001) {
      return calculateTree25(features, 11);
    } else {
      return calculateTree25(features, 12);
    }
  }
  if (node === 27) return -0.33554445221957047;
  if (node === 28) return 0.07121542584519368;
  if (node === 13) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree25(features, 27);
    } else if (val <= 69.50000000000001) {
      return calculateTree25(features, 27);
    } else {
      return calculateTree25(features, 28);
    }
  }
  if (node === 29) return -0.335652544106642;
  if (node === 61) return -0.34024847736194525;
  if (node === 503) return -0.3196123769555161;
  if (node === 504) return 0.6222904601491583;
  if (node === 251) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree25(features, 503);
    } else if (val <= 953.5000000000001) {
      return calculateTree25(features, 503);
    } else {
      return calculateTree25(features, 504);
    }
  }
  if (node === 252) return 0.6322790101380931;
  if (node === 125) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree25(features, 251);
    } else if (val <= 973.5000000000001) {
      return calculateTree25(features, 251);
    } else {
      return calculateTree25(features, 252);
    }
  }
  if (node === 253) return -0.304722489853627;
  if (node === 509) return 0.2195027722119369;
  if (node === 510) return -0.012788491335816998;
  if (node === 254) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree25(features, 509);
    } else if (val <= 989.5000000000001) {
      return calculateTree25(features, 509);
    } else {
      return calculateTree25(features, 510);
    }
  }
  if (node === 126) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree25(features, 253);
    } else if (val <= 983.5000000000001) {
      return calculateTree25(features, 253);
    } else {
      return calculateTree25(features, 254);
    }
  }
  if (node === 62) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree25(features, 125);
    } else if (val <= 975.5000000000001) {
      return calculateTree25(features, 125);
    } else {
      return calculateTree25(features, 126);
    }
  }
  if (node === 30) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree25(features, 61);
    } else if (val <= 966.0000000000001) {
      return calculateTree25(features, 61);
    } else {
      return calculateTree25(features, 62);
    }
  }
  if (node === 14) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree25(features, 29);
    } else if (val <= 361.50000000000006) {
      return calculateTree25(features, 29);
    } else {
      return calculateTree25(features, 30);
    }
  }
  if (node === 6) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree25(features, 13);
    } else if (val <= 181.50000000000003) {
      return calculateTree25(features, 13);
    } else {
      return calculateTree25(features, 14);
    }
  }
  if (node === 2) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree25(features, 5);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree25(features, 5);
    } else {
      return calculateTree25(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree25(features, 1);
    } else if (val <= 954.5000000000001) {
      return calculateTree25(features, 1);
    } else {
      return calculateTree25(features, 2);
    }
  }
}
function calculateTree26(features, node = 0) {
  if (node === 15) return -0.0035605320732813514;
  if (node === 135) return 0.012781583211046107;
  if (node === 136) return 0.30260861096875913;
  if (node === 67) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree26(features, 135);
    } else if (val <= 275.50000000000006) {
      return calculateTree26(features, 135);
    } else {
      return calculateTree26(features, 136);
    }
  }
  if (node === 137) return -0.2660401436998973;
  if (node === 138) return 0.12462164510326405;
  if (node === 68) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree26(features, 137);
    } else if (val <= 206.00000000000003) {
      return calculateTree26(features, 137);
    } else {
      return calculateTree26(features, 138);
    }
  }
  if (node === 33) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree26(features, 67);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree26(features, 67);
    } else {
      return calculateTree26(features, 68);
    }
  }
  if (node === 69) return -0.33884801156275246;
  if (node === 141) return -0.41511786904712583;
  if (node === 285) return 0.4211630634086235;
  if (node === 286) return -0.29116987746001266;
  if (node === 142) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree26(features, 285);
    } else if (val <= 810.5000000000001) {
      return calculateTree26(features, 285);
    } else {
      return calculateTree26(features, 286);
    }
  }
  if (node === 70) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree26(features, 141);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree26(features, 141);
    } else {
      return calculateTree26(features, 142);
    }
  }
  if (node === 34) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree26(features, 69);
    } else if (val <= 677.5000000000001) {
      return calculateTree26(features, 69);
    } else {
      return calculateTree26(features, 70);
    }
  }
  if (node === 16) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree26(features, 33);
    } else if (val <= 282.50000000000006) {
      return calculateTree26(features, 33);
    } else {
      return calculateTree26(features, 34);
    }
  }
  if (node === 7) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree26(features, 15);
    } else if (val <= 194.00000000000003) {
      return calculateTree26(features, 15);
    } else {
      return calculateTree26(features, 16);
    }
  }
  if (node === 8) return 0.0027473196865055614;
  if (node === 3) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree26(features, 7);
    } else if (val <= 210.00000000000003) {
      return calculateTree26(features, 7);
    } else {
      return calculateTree26(features, 8);
    }
  }
  if (node === 39) return -0.2796464196301965;
  if (node === 163) return 0.6920621700747849;
  if (node === 164) return 0.18682759855663178;
  if (node === 81) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree26(features, 163);
    } else if (val <= 16.500000000000004) {
      return calculateTree26(features, 163);
    } else {
      return calculateTree26(features, 164);
    }
  }
  if (node === 165) return 0.10325727275413589;
  if (node === 166) return -0.4512098349747559;
  if (node === 82) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree26(features, 165);
    } else if (val <= 957.5000000000001) {
      return calculateTree26(features, 165);
    } else {
      return calculateTree26(features, 166);
    }
  }
  if (node === 40) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree26(features, 81);
    } else if (val <= 160.50000000000003) {
      return calculateTree26(features, 81);
    } else {
      return calculateTree26(features, 82);
    }
  }
  if (node === 19) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree26(features, 39);
    } else if (val <= 135.50000000000003) {
      return calculateTree26(features, 39);
    } else {
      return calculateTree26(features, 40);
    }
  }
  if (node === 167) return -0.3594592423916908;
  if (node === 168) return 0.1772236037825878;
  if (node === 83) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree26(features, 167);
    } else if (val <= 253.50000000000003) {
      return calculateTree26(features, 167);
    } else {
      return calculateTree26(features, 168);
    }
  }
  if (node === 84) return -0.32976486436906133;
  if (node === 41) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree26(features, 83);
    } else if (val <= 387.50000000000006) {
      return calculateTree26(features, 83);
    } else {
      return calculateTree26(features, 84);
    }
  }
  if (node === 85) return 0.1527173954452067;
  if (node === 86) return -0.353497303170146;
  if (node === 42) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree26(features, 85);
    } else if (val <= 896.5000000000001) {
      return calculateTree26(features, 85);
    } else {
      return calculateTree26(features, 86);
    }
  }
  if (node === 20) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree26(features, 41);
    } else if (val <= 799.5000000000001) {
      return calculateTree26(features, 41);
    } else {
      return calculateTree26(features, 42);
    }
  }
  if (node === 9) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree26(features, 19);
    } else if (val <= 963.0000000000001) {
      return calculateTree26(features, 19);
    } else {
      return calculateTree26(features, 20);
    }
  }
  if (node === 87) return -0.3698583200774084;
  if (node === 177) return 0.2456858806201721;
  if (node === 178) return -0.07174952529362962;
  if (node === 88) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree26(features, 177);
    } else if (val <= 31.500000000000004) {
      return calculateTree26(features, 177);
    } else {
      return calculateTree26(features, 178);
    }
  }
  if (node === 43) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree26(features, 87);
    } else if (val <= 485.50000000000006) {
      return calculateTree26(features, 87);
    } else {
      return calculateTree26(features, 88);
    }
  }
  if (node === 44) return -0.37119667992805705;
  if (node === 21) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree26(features, 43);
    } else if (val <= 742.5000000000001) {
      return calculateTree26(features, 43);
    } else {
      return calculateTree26(features, 44);
    }
  }
  if (node === 45) return 0.26893951587566906;
  if (node === 46) return 0.07819911878427331;
  if (node === 22) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree26(features, 45);
    } else if (val <= 1.5000000000000002) {
      return calculateTree26(features, 45);
    } else {
      return calculateTree26(features, 46);
    }
  }
  if (node === 10) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree26(features, 21);
    } else if (val <= 905.5000000000001) {
      return calculateTree26(features, 21);
    } else {
      return calculateTree26(features, 22);
    }
  }
  if (node === 4) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree26(features, 9);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree26(features, 9);
    } else {
      return calculateTree26(features, 10);
    }
  }
  if (node === 1) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree26(features, 3);
    } else if (val <= 948.0000000000001) {
      return calculateTree26(features, 3);
    } else {
      return calculateTree26(features, 4);
    }
  }
  if (node === 11) return 0.2574726649894148;
  if (node === 12) return -0.34046169125289544;
  if (node === 5) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree26(features, 11);
    } else if (val <= 975.0000000000001) {
      return calculateTree26(features, 11);
    } else {
      return calculateTree26(features, 12);
    }
  }
  if (node === 55) return 0.10541433013492735;
  if (node === 56) return -0.36716067423785154;
  if (node === 27) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree26(features, 55);
    } else if (val <= 978.0000000000001) {
      return calculateTree26(features, 55);
    } else {
      return calculateTree26(features, 56);
    }
  }
  if (node === 28) return 0.35903689859184806;
  if (node === 13) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree26(features, 27);
    } else if (val <= 551.5000000000001) {
      return calculateTree26(features, 27);
    } else {
      return calculateTree26(features, 28);
    }
  }
  if (node === 14) return -0.2152759894621512;
  if (node === 6) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree26(features, 13);
    } else if (val <= 606.5000000000001) {
      return calculateTree26(features, 13);
    } else {
      return calculateTree26(features, 14);
    }
  }
  if (node === 2) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree26(features, 5);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree26(features, 5);
    } else {
      return calculateTree26(features, 6);
    }
  }
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree26(features, 1);
    } else if (val <= 973.5000000000001) {
      return calculateTree26(features, 1);
    } else {
      return calculateTree26(features, 2);
    }
  }
}
function calculateTree27(features, node = 0) {
  if (node === 3) return -0.3786352157331418;
  if (node === 9) return 0.03231342192402596;
  if (node === 10) return -0.17769818107382437;
  if (node === 4) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree27(features, 9);
    } else if (val <= 893.0000000000001) {
      return calculateTree27(features, 9);
    } else {
      return calculateTree27(features, 10);
    }
  }
  if (node === 1) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree27(features, 3);
    } else if (val <= 113.50000000000001) {
      return calculateTree27(features, 3);
    } else {
      return calculateTree27(features, 4);
    }
  }
  if (node === 191) return 0.013459320863358585;
  if (node === 192) return -0.18699398762211858;
  if (node === 95) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree27(features, 191);
    } else if (val <= 310.00000000000006) {
      return calculateTree27(features, 191);
    } else {
      return calculateTree27(features, 192);
    }
  }
  if (node === 193) return 0.405795080322825;
  if (node === 389) return -0.2241697320459455;
  if (node === 390) return 0.12258003401694971;
  if (node === 194) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree27(features, 389);
    } else if (val <= 348.50000000000006) {
      return calculateTree27(features, 389);
    } else {
      return calculateTree27(features, 390);
    }
  }
  if (node === 96) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree27(features, 193);
    } else if (val <= 338.50000000000006) {
      return calculateTree27(features, 193);
    } else {
      return calculateTree27(features, 194);
    }
  }
  if (node === 47) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree27(features, 95);
    } else if (val <= 330.00000000000006) {
      return calculateTree27(features, 95);
    } else {
      return calculateTree27(features, 96);
    }
  }
  if (node === 97) return -0.2728910119551125;
  if (node === 395) return 0.032294384998066736;
  if (node === 396) return -0.32648908612467414;
  if (node === 197) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree27(features, 395);
    } else if (val <= 673.0000000000001) {
      return calculateTree27(features, 395);
    } else {
      return calculateTree27(features, 396);
    }
  }
  if (node === 198) return -0.2821092150873713;
  if (node === 98) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree27(features, 197);
    } else if (val <= 70.00000000000001) {
      return calculateTree27(features, 197);
    } else {
      return calculateTree27(features, 198);
    }
  }
  if (node === 48) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree27(features, 97);
    } else if (val <= 57.50000000000001) {
      return calculateTree27(features, 97);
    } else {
      return calculateTree27(features, 98);
    }
  }
  if (node === 23) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree27(features, 47);
    } else if (val <= 377.50000000000006) {
      return calculateTree27(features, 47);
    } else {
      return calculateTree27(features, 48);
    }
  }
  if (node === 24) return 0.04617768239607071;
  if (node === 11) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree27(features, 23);
    } else if (val <= 718.5000000000001) {
      return calculateTree27(features, 23);
    } else {
      return calculateTree27(features, 24);
    }
  }
  if (node === 25) return -0.23232174061095734;
  if (node === 53) return -0.362522006938587;
  if (node === 54) return -0.037188143246623694;
  if (node === 26) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree27(features, 53);
    } else if (val <= 215.00000000000003) {
      return calculateTree27(features, 53);
    } else {
      return calculateTree27(features, 54);
    }
  }
  if (node === 12) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree27(features, 25);
    } else if (val <= 83.50000000000001) {
      return calculateTree27(features, 25);
    } else {
      return calculateTree27(features, 26);
    }
  }
  if (node === 5) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree27(features, 11);
    } else if (val <= 75.50000000000001) {
      return calculateTree27(features, 11);
    } else {
      return calculateTree27(features, 12);
    }
  }
  if (node === 27) return 0.1509496806991493;
  if (node === 57) return -0.360706960672416;
  if (node === 117) return 0.22153449627713437;
  if (node === 237) return -0.31430979432644063;
  if (node === 477) return -0.0276035076379476;
  if (node === 957) return 0.14505245503348949;
  if (node === 958) return -0.2056194510801101;
  if (node === 478) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree27(features, 957);
    } else if (val <= 911.0000000000001) {
      return calculateTree27(features, 957);
    } else {
      return calculateTree27(features, 958);
    }
  }
  if (node === 238) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree27(features, 477);
    } else if (val <= 105.50000000000001) {
      return calculateTree27(features, 477);
    } else {
      return calculateTree27(features, 478);
    }
  }
  if (node === 118) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree27(features, 237);
    } else if (val <= 861.5000000000001) {
      return calculateTree27(features, 237);
    } else {
      return calculateTree27(features, 238);
    }
  }
  if (node === 58) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree27(features, 117);
    } else if (val <= 678.5000000000001) {
      return calculateTree27(features, 117);
    } else {
      return calculateTree27(features, 118);
    }
  }
  if (node === 28) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree27(features, 57);
    } else if (val <= 478.50000000000006) {
      return calculateTree27(features, 57);
    } else {
      return calculateTree27(features, 58);
    }
  }
  if (node === 13) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree27(features, 27);
    } else if (val <= 305.00000000000006) {
      return calculateTree27(features, 27);
    } else {
      return calculateTree27(features, 28);
    }
  }
  if (node === 119) return -0.44721821765333464;
  if (node === 483) return -0.023412261116122428;
  if (node === 969) return 0.20489523676482377;
  if (node === 1941) return -0.2689723343796513;
  if (node === 1942) return 0.056424597268195584;
  if (node === 970) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree27(features, 1941);
    } else if (val <= 539.5000000000001) {
      return calculateTree27(features, 1941);
    } else {
      return calculateTree27(features, 1942);
    }
  }
  if (node === 484) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree27(features, 969);
    } else if (val <= 507.50000000000006) {
      return calculateTree27(features, 969);
    } else {
      return calculateTree27(features, 970);
    }
  }
  if (node === 241) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree27(features, 483);
    } else if (val <= 455.50000000000006) {
      return calculateTree27(features, 483);
    } else {
      return calculateTree27(features, 484);
    }
  }
  if (node === 242) return -0.026508776231613247;
  if (node === 120) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree27(features, 241);
    } else if (val <= 662.5000000000001) {
      return calculateTree27(features, 241);
    } else {
      return calculateTree27(features, 242);
    }
  }
  if (node === 59) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree27(features, 119);
    } else if (val <= 160.50000000000003) {
      return calculateTree27(features, 119);
    } else {
      return calculateTree27(features, 120);
    }
  }
  if (node === 121) return -0.3120801232576887;
  if (node === 122) return -0.05875259557357935;
  if (node === 60) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree27(features, 121);
    } else if (val <= 905.5000000000001) {
      return calculateTree27(features, 121);
    } else {
      return calculateTree27(features, 122);
    }
  }
  if (node === 29) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree27(features, 59);
    } else if (val <= 151.50000000000003) {
      return calculateTree27(features, 59);
    } else {
      return calculateTree27(features, 60);
    }
  }
  if (node === 30) return 0.0014646841702407733;
  if (node === 14) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree27(features, 29);
    } else if (val <= 166.00000000000003) {
      return calculateTree27(features, 29);
    } else {
      return calculateTree27(features, 30);
    }
  }
  if (node === 6) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree27(features, 13);
    } else if (val <= 127.00000000000001) {
      return calculateTree27(features, 13);
    } else {
      return calculateTree27(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree27(features, 5);
    } else if (val <= 86.50000000000001) {
      return calculateTree27(features, 5);
    } else {
      return calculateTree27(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree27(features, 1);
    } else if (val <= 18.500000000000004) {
      return calculateTree27(features, 1);
    } else {
      return calculateTree27(features, 2);
    }
  }
}
function calculateTree28(features, node = 0) {
  if (node === 31) return -0.00043831758231325217;
  if (node === 32) return -0.3046408432325991;
  if (node === 15) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree28(features, 31);
    } else if (val <= 978.0000000000001) {
      return calculateTree28(features, 31);
    } else {
      return calculateTree28(features, 32);
    }
  }
  if (node === 16) return -0.3652698050830603;
  if (node === 7) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree28(features, 15);
    } else if (val <= 548.5000000000001) {
      return calculateTree28(features, 15);
    } else {
      return calculateTree28(features, 16);
    }
  }
  if (node === 17) return -0.3147808520876909;
  if (node === 37) return -0.3841397849268717;
  if (node === 38) return 0.3776711209981933;
  if (node === 18) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree28(features, 37);
    } else if (val <= 705.5000000000001) {
      return calculateTree28(features, 37);
    } else {
      return calculateTree28(features, 38);
    }
  }
  if (node === 8) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree28(features, 17);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree28(features, 17);
    } else {
      return calculateTree28(features, 18);
    }
  }
  if (node === 3) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree28(features, 7);
    } else if (val <= 551.5000000000001) {
      return calculateTree28(features, 7);
    } else {
      return calculateTree28(features, 8);
    }
  }
  if (node === 159) return -0.368948416377688;
  if (node === 160) return 0.3184219412937959;
  if (node === 79) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree28(features, 159);
    } else if (val <= 132.50000000000003) {
      return calculateTree28(features, 159);
    } else {
      return calculateTree28(features, 160);
    }
  }
  if (node === 80) return -0.36321478808240043;
  if (node === 39) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree28(features, 79);
    } else if (val <= 288.00000000000006) {
      return calculateTree28(features, 79);
    } else {
      return calculateTree28(features, 80);
    }
  }
  if (node === 81) return -0.2859837049598265;
  if (node === 82) return 0.479296140707472;
  if (node === 40) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree28(features, 81);
    } else if (val <= 361.50000000000006) {
      return calculateTree28(features, 81);
    } else {
      return calculateTree28(features, 82);
    }
  }
  if (node === 19) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree28(features, 39);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree28(features, 39);
    } else {
      return calculateTree28(features, 40);
    }
  }
  if (node === 83) return -0.20362941940324933;
  if (node === 339) return 0.27668188646705844;
  if (node === 340) return -0.34833442510354135;
  if (node === 169) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree28(features, 339);
    } else if (val <= 589.5000000000001) {
      return calculateTree28(features, 339);
    } else {
      return calculateTree28(features, 340);
    }
  }
  if (node === 341) return -0.38329710518337107;
  if (node === 342) return 0.18548656883191447;
  if (node === 170) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree28(features, 341);
    } else if (val <= 589.5000000000001) {
      return calculateTree28(features, 341);
    } else {
      return calculateTree28(features, 342);
    }
  }
  if (node === 84) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree28(features, 169);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree28(features, 169);
    } else {
      return calculateTree28(features, 170);
    }
  }
  if (node === 41) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree28(features, 83);
    } else if (val <= 112.00000000000001) {
      return calculateTree28(features, 83);
    } else {
      return calculateTree28(features, 84);
    }
  }
  if (node === 85) return -0.28349368791813956;
  if (node === 347) return 0.13441406609443268;
  if (node === 697) return -0.3904367052359963;
  if (node === 698) return -0.10752388574856722;
  if (node === 348) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree28(features, 697);
    } else if (val <= 625.0000000000001) {
      return calculateTree28(features, 697);
    } else {
      return calculateTree28(features, 698);
    }
  }
  if (node === 173) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree28(features, 347);
    } else if (val <= 151.50000000000003) {
      return calculateTree28(features, 347);
    } else {
      return calculateTree28(features, 348);
    }
  }
  if (node === 174) return 0.1496155787705776;
  if (node === 86) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree28(features, 173);
    } else if (val <= 435.00000000000006) {
      return calculateTree28(features, 173);
    } else {
      return calculateTree28(features, 174);
    }
  }
  if (node === 42) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree28(features, 85);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree28(features, 85);
    } else {
      return calculateTree28(features, 86);
    }
  }
  if (node === 20) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree28(features, 41);
    } else if (val <= 602.5000000000001) {
      return calculateTree28(features, 41);
    } else {
      return calculateTree28(features, 42);
    }
  }
  if (node === 9) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree28(features, 19);
    } else if (val <= 572.0000000000001) {
      return calculateTree28(features, 19);
    } else {
      return calculateTree28(features, 20);
    }
  }
  if (node === 87) return -0.33673124853662456;
  if (node === 88) return 0.1503406098766859;
  if (node === 43) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree28(features, 87);
    } else if (val <= 776.0000000000001) {
      return calculateTree28(features, 87);
    } else {
      return calculateTree28(features, 88);
    }
  }
  if (node === 44) return -0.34816653017257565;
  if (node === 21) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree28(features, 43);
    } else if (val <= 859.5000000000001) {
      return calculateTree28(features, 43);
    } else {
      return calculateTree28(features, 44);
    }
  }
  if (node === 45) return -0.3922650370110721;
  if (node === 93) return 0.2208353749073459;
  if (node === 94) return -0.258463970897359;
  if (node === 46) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree28(features, 93);
    } else if (val <= 865.0000000000001) {
      return calculateTree28(features, 93);
    } else {
      return calculateTree28(features, 94);
    }
  }
  if (node === 22) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree28(features, 45);
    } else if (val <= 859.5000000000001) {
      return calculateTree28(features, 45);
    } else {
      return calculateTree28(features, 46);
    }
  }
  if (node === 10) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree28(features, 21);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree28(features, 21);
    } else {
      return calculateTree28(features, 22);
    }
  }
  if (node === 4) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree28(features, 9);
    } else if (val <= 672.0000000000001) {
      return calculateTree28(features, 9);
    } else {
      return calculateTree28(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree28(features, 3);
    } else if (val <= 555.5000000000001) {
      return calculateTree28(features, 3);
    } else {
      return calculateTree28(features, 4);
    }
  }
  if (node === 11) return 0.11550690688893867;
  if (node === 25) return -0.2768176440236397;
  if (node === 26) return 0.06785428251986458;
  if (node === 12) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree28(features, 25);
    } else if (val <= 422.00000000000006) {
      return calculateTree28(features, 25);
    } else {
      return calculateTree28(features, 26);
    }
  }
  if (node === 5) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree28(features, 11);
    } else if (val <= 240.00000000000003) {
      return calculateTree28(features, 11);
    } else {
      return calculateTree28(features, 12);
    }
  }
  if (node === 13) return -0.2656858265238353;
  if (node === 14) return 0.0016726797707411352;
  if (node === 6) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree28(features, 13);
    } else if (val <= 670.5000000000001) {
      return calculateTree28(features, 13);
    } else {
      return calculateTree28(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree28(features, 5);
    } else if (val <= 667.5000000000001) {
      return calculateTree28(features, 5);
    } else {
      return calculateTree28(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree28(features, 1);
    } else if (val <= 647.0000000000001) {
      return calculateTree28(features, 1);
    } else {
      return calculateTree28(features, 2);
    }
  }
}
function calculateTree29(features, node = 0) {
  if (node === 1) return -0.0039026963311067126;
  if (node === 47) return 0.4268809176444607;
  if (node === 48) return -0.3610004042205267;
  if (node === 23) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree29(features, 47);
    } else if (val <= 588.5000000000001) {
      return calculateTree29(features, 47);
    } else {
      return calculateTree29(features, 48);
    }
  }
  if (node === 24) return -0.49503971661183344;
  if (node === 11) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree29(features, 23);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree29(features, 23);
    } else {
      return calculateTree29(features, 24);
    }
  }
  if (node === 25) return -0.6606517527479472;
  if (node === 26) return 0.2710686448785149;
  if (node === 12) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree29(features, 25);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree29(features, 25);
    } else {
      return calculateTree29(features, 26);
    }
  }
  if (node === 5) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree29(features, 11);
    } else if (val <= 781.0000000000001) {
      return calculateTree29(features, 11);
    } else {
      return calculateTree29(features, 12);
    }
  }
  if (node === 55) return -0.3686191364466736;
  if (node === 113) return 0.6329786332479727;
  if (node === 229) return 0.6124777589663117;
  if (node === 461) return -0.4792226082937161;
  if (node === 462) return 0.21716755949785652;
  if (node === 230) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree29(features, 461);
    } else if (val <= 127.50000000000001) {
      return calculateTree29(features, 461);
    } else {
      return calculateTree29(features, 462);
    }
  }
  if (node === 114) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree29(features, 229);
    } else if (val <= 89.00000000000001) {
      return calculateTree29(features, 229);
    } else {
      return calculateTree29(features, 230);
    }
  }
  if (node === 56) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree29(features, 113);
    } else if (val <= 757.5000000000001) {
      return calculateTree29(features, 113);
    } else {
      return calculateTree29(features, 114);
    }
  }
  if (node === 27) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree29(features, 55);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree29(features, 55);
    } else {
      return calculateTree29(features, 56);
    }
  }
  if (node === 57) return -0.35960826034330834;
  if (node === 235) return -0.11204201540599994;
  if (node === 473) return 0.40865003089090296;
  if (node === 949) return -0.3808016605032607;
  if (node === 1901) return -0.29895434588373954;
  if (node === 1902) return 0.5489781222786885;
  if (node === 950) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree29(features, 1901);
    } else if (val <= 690.5000000000001) {
      return calculateTree29(features, 1901);
    } else {
      return calculateTree29(features, 1902);
    }
  }
  if (node === 474) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree29(features, 949);
    } else if (val <= 774.0000000000001) {
      return calculateTree29(features, 949);
    } else {
      return calculateTree29(features, 950);
    }
  }
  if (node === 236) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree29(features, 473);
    } else if (val <= 762.5000000000001) {
      return calculateTree29(features, 473);
    } else {
      return calculateTree29(features, 474);
    }
  }
  if (node === 117) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree29(features, 235);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree29(features, 235);
    } else {
      return calculateTree29(features, 236);
    }
  }
  if (node === 118) return -0.3241808285260312;
  if (node === 58) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree29(features, 117);
    } else if (val <= 835.5000000000001) {
      return calculateTree29(features, 117);
    } else {
      return calculateTree29(features, 118);
    }
  }
  if (node === 28) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree29(features, 57);
    } else if (val <= 556.0000000000001) {
      return calculateTree29(features, 57);
    } else {
      return calculateTree29(features, 58);
    }
  }
  if (node === 13) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree29(features, 27);
    } else if (val <= 144.50000000000003) {
      return calculateTree29(features, 27);
    } else {
      return calculateTree29(features, 28);
    }
  }
  if (node === 59) return 0.04404799426417159;
  if (node === 60) return 0.22729642202096031;
  if (node === 29) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree29(features, 59);
    } else if (val <= 606.5000000000001) {
      return calculateTree29(features, 59);
    } else {
      return calculateTree29(features, 60);
    }
  }
  if (node === 61) return -0.3360380540674661;
  if (node === 1007) return -0.18055862048452873;
  if (node === 2017) return 0.12099085774904732;
  if (node === 2018) return 0.03131497344858092;
  if (node === 1008) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree29(features, 2017);
    } else if (val <= 810.5000000000001) {
      return calculateTree29(features, 2017);
    } else {
      return calculateTree29(features, 2018);
    }
  }
  if (node === 503) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree29(features, 1007);
    } else if (val <= 181.50000000000003) {
      return calculateTree29(features, 1007);
    } else {
      return calculateTree29(features, 1008);
    }
  }
  if (node === 504) return -0.004900800818803009;
  if (node === 251) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree29(features, 503);
    } else if (val <= 819.5000000000001) {
      return calculateTree29(features, 503);
    } else {
      return calculateTree29(features, 504);
    }
  }
  if (node === 505) return -0.3909020710118906;
  if (node === 1013) return -0.39555671458943914;
  if (node === 1014) return -0.12246471809305137;
  if (node === 506) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree29(features, 1013);
    } else if (val <= 799.5000000000001) {
      return calculateTree29(features, 1013);
    } else {
      return calculateTree29(features, 1014);
    }
  }
  if (node === 252) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree29(features, 505);
    } else if (val <= 893.0000000000001) {
      return calculateTree29(features, 505);
    } else {
      return calculateTree29(features, 506);
    }
  }
  if (node === 125) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree29(features, 251);
    } else if (val <= 884.0000000000001) {
      return calculateTree29(features, 251);
    } else {
      return calculateTree29(features, 252);
    }
  }
  if (node === 507) return -0.3515001323196641;
  if (node === 508) return 0.0816148472275393;
  if (node === 253) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree29(features, 507);
    } else if (val <= 944.5000000000001) {
      return calculateTree29(features, 507);
    } else {
      return calculateTree29(features, 508);
    }
  }
  if (node === 509) return -0.37179796032277274;
  if (node === 510) return 0.13132062273023767;
  if (node === 254) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree29(features, 509);
    } else if (val <= 845.5000000000001) {
      return calculateTree29(features, 509);
    } else {
      return calculateTree29(features, 510);
    }
  }
  if (node === 126) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree29(features, 253);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree29(features, 253);
    } else {
      return calculateTree29(features, 254);
    }
  }
  if (node === 62) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree29(features, 125);
    } else if (val <= 937.5000000000001) {
      return calculateTree29(features, 125);
    } else {
      return calculateTree29(features, 126);
    }
  }
  if (node === 30) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree29(features, 61);
    } else if (val <= 785.5000000000001) {
      return calculateTree29(features, 61);
    } else {
      return calculateTree29(features, 62);
    }
  }
  if (node === 14) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree29(features, 29);
    } else if (val <= 784.5000000000001) {
      return calculateTree29(features, 29);
    } else {
      return calculateTree29(features, 30);
    }
  }
  if (node === 6) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree29(features, 13);
    } else if (val <= 780.0000000000001) {
      return calculateTree29(features, 13);
    } else {
      return calculateTree29(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree29(features, 5);
    } else if (val <= 752.5000000000001) {
      return calculateTree29(features, 5);
    } else {
      return calculateTree29(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree29(features, 1);
    } else if (val <= 745.5000000000001) {
      return calculateTree29(features, 1);
    } else {
      return calculateTree29(features, 2);
    }
  }
}
function calculateTree30(features, node = 0) {
  if (node === 3) return -0.00010032243459875095;
  if (node === 79) return -0.26061250094413463;
  if (node === 161) return 0.2830295655116992;
  if (node === 325) return -0.3411069313831138;
  if (node === 326) return 0.38442409374719844;
  if (node === 162) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree30(features, 325);
    } else if (val <= 948.5000000000001) {
      return calculateTree30(features, 325);
    } else {
      return calculateTree30(features, 326);
    }
  }
  if (node === 80) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree30(features, 161);
    } else if (val <= 647.0000000000001) {
      return calculateTree30(features, 161);
    } else {
      return calculateTree30(features, 162);
    }
  }
  if (node === 39) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree30(features, 79);
    } else if (val <= 282.50000000000006) {
      return calculateTree30(features, 79);
    } else {
      return calculateTree30(features, 80);
    }
  }
  if (node === 40) return -0.39178710093683106;
  if (node === 19) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree30(features, 39);
    } else if (val <= 723.5000000000001) {
      return calculateTree30(features, 39);
    } else {
      return calculateTree30(features, 40);
    }
  }
  if (node === 167) return 0.12760136167278274;
  if (node === 168) return -0.44381780209006866;
  if (node === 83) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree30(features, 167);
    } else if (val <= 282.50000000000006) {
      return calculateTree30(features, 167);
    } else {
      return calculateTree30(features, 168);
    }
  }
  if (node === 84) return -0.0008867023514514253;
  if (node === 41) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree30(features, 83);
    } else if (val <= 647.0000000000001) {
      return calculateTree30(features, 83);
    } else {
      return calculateTree30(features, 84);
    }
  }
  if (node === 85) return 0.17534323187989415;
  if (node === 173) return -0.38872785039813484;
  if (node === 174) return 0.0841084147383431;
  if (node === 86) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree30(features, 173);
    } else if (val <= 723.5000000000001) {
      return calculateTree30(features, 173);
    } else {
      return calculateTree30(features, 174);
    }
  }
  if (node === 42) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree30(features, 85);
    } else if (val <= 542.5000000000001) {
      return calculateTree30(features, 85);
    } else {
      return calculateTree30(features, 86);
    }
  }
  if (node === 20) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree30(features, 41);
    } else if (val <= 717.0000000000001) {
      return calculateTree30(features, 41);
    } else {
      return calculateTree30(features, 42);
    }
  }
  if (node === 9) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree30(features, 19);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree30(features, 19);
    } else {
      return calculateTree30(features, 20);
    }
  }
  if (node === 10) return 0.13891709692041762;
  if (node === 4) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree30(features, 9);
    } else if (val <= 37.50000000000001) {
      return calculateTree30(features, 9);
    } else {
      return calculateTree30(features, 10);
    }
  }
  if (node === 1) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree30(features, 3);
    } else if (val <= 712.5000000000001) {
      return calculateTree30(features, 3);
    } else {
      return calculateTree30(features, 4);
    }
  }
  if (node === 1535) return 0.14397239713546744;
  if (node === 1536) return 1.0510729164492922;
  if (node === 767) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree30(features, 1535);
    } else if (val <= 181.50000000000003) {
      return calculateTree30(features, 1535);
    } else {
      return calculateTree30(features, 1536);
    }
  }
  if (node === 768) return -0.3794398073127225;
  if (node === 383) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree30(features, 767);
    } else if (val <= 191.50000000000003) {
      return calculateTree30(features, 767);
    } else {
      return calculateTree30(features, 768);
    }
  }
  if (node === 384) return 0.33714729908542657;
  if (node === 191) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree30(features, 383);
    } else if (val <= 275.50000000000006) {
      return calculateTree30(features, 383);
    } else {
      return calculateTree30(features, 384);
    }
  }
  if (node === 192) return -0.3996477048762311;
  if (node === 95) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree30(features, 191);
    } else if (val <= 291.50000000000006) {
      return calculateTree30(features, 191);
    } else {
      return calculateTree30(features, 192);
    }
  }
  if (node === 96) return -0.16853935209934343;
  if (node === 47) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree30(features, 95);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree30(features, 95);
    } else {
      return calculateTree30(features, 96);
    }
  }
  if (node === 97) return -0.42619827078322975;
  if (node === 98) return 0.2958986501172403;
  if (node === 48) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree30(features, 97);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree30(features, 97);
    } else {
      return calculateTree30(features, 98);
    }
  }
  if (node === 23) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree30(features, 47);
    } else if (val <= 361.50000000000006) {
      return calculateTree30(features, 47);
    } else {
      return calculateTree30(features, 48);
    }
  }
  if (node === 49) return -0.3903001289373372;
  if (node === 203) return -0.24578643731497885;
  if (node === 409) return 0.6367001399280836;
  if (node === 410) return -0.12252732730880425;
  if (node === 204) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree30(features, 409);
    } else if (val <= 776.0000000000001) {
      return calculateTree30(features, 409);
    } else {
      return calculateTree30(features, 410);
    }
  }
  if (node === 101) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree30(features, 203);
    } else if (val <= 771.0000000000001) {
      return calculateTree30(features, 203);
    } else {
      return calculateTree30(features, 204);
    }
  }
  if (node === 205) return 0.042370967888257054;
  if (node === 206) return -0.37831279132057083;
  if (node === 102) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree30(features, 205);
    } else if (val <= 522.5000000000001) {
      return calculateTree30(features, 205);
    } else {
      return calculateTree30(features, 206);
    }
  }
  if (node === 50) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree30(features, 101);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree30(features, 101);
    } else {
      return calculateTree30(features, 102);
    }
  }
  if (node === 24) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree30(features, 49);
    } else if (val <= 461.50000000000006) {
      return calculateTree30(features, 49);
    } else {
      return calculateTree30(features, 50);
    }
  }
  if (node === 11) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree30(features, 23);
    } else if (val <= 365.50000000000006) {
      return calculateTree30(features, 23);
    } else {
      return calculateTree30(features, 24);
    }
  }
  if (node === 25) return 0.17910739308635942;
  if (node === 53) return -0.33492072439059606;
  if (node === 109) return -0.3770309276646997;
  if (node === 110) return 0.10564055470615634;
  if (node === 54) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree30(features, 109);
    } else if (val <= 877.5000000000001) {
      return calculateTree30(features, 109);
    } else {
      return calculateTree30(features, 110);
    }
  }
  if (node === 26) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree30(features, 53);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree30(features, 53);
    } else {
      return calculateTree30(features, 54);
    }
  }
  if (node === 12) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree30(features, 25);
    } else if (val <= 784.5000000000001) {
      return calculateTree30(features, 25);
    } else {
      return calculateTree30(features, 26);
    }
  }
  if (node === 5) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree30(features, 11);
    } else if (val <= 723.5000000000001) {
      return calculateTree30(features, 11);
    } else {
      return calculateTree30(features, 12);
    }
  }
  if (node === 6) return -0.004162139133436144;
  if (node === 2) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree30(features, 5);
    } else if (val <= 778.5000000000001) {
      return calculateTree30(features, 5);
    } else {
      return calculateTree30(features, 6);
    }
  }
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree30(features, 1);
    } else if (val <= 734.0000000000001) {
      return calculateTree30(features, 1);
    } else {
      return calculateTree30(features, 2);
    }
  }
}
function calculateTree31(features, node = 0) {
  if (node === 7) return 0.0357941735042934;
  if (node === 17) return -0.1600607947777944;
  if (node === 18) return 0.05905325529178222;
  if (node === 8) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree31(features, 17);
    } else if (val <= 963.0000000000001) {
      return calculateTree31(features, 17);
    } else {
      return calculateTree31(features, 18);
    }
  }
  if (node === 3) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree31(features, 7);
    } else if (val <= 897.5000000000001) {
      return calculateTree31(features, 7);
    } else {
      return calculateTree31(features, 8);
    }
  }
  if (node === 19) return 0.18963116146658987;
  if (node === 41) return -0.40962543065390256;
  if (node === 85) return 0.16587061647822468;
  if (node === 86) return -0.4291603772276667;
  if (node === 42) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree31(features, 85);
    } else if (val <= 14.500000000000002) {
      return calculateTree31(features, 85);
    } else {
      return calculateTree31(features, 86);
    }
  }
  if (node === 20) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree31(features, 41);
    } else if (val <= 3.5000000000000004) {
      return calculateTree31(features, 41);
    } else {
      return calculateTree31(features, 42);
    }
  }
  if (node === 9) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree31(features, 19);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree31(features, 19);
    } else {
      return calculateTree31(features, 20);
    }
  }
  if (node === 175) return 0.019652303789658404;
  if (node === 707) return 0.33245124141672977;
  if (node === 708) return -0.13442669022797785;
  if (node === 353) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree31(features, 707);
    } else if (val <= 175.50000000000003) {
      return calculateTree31(features, 707);
    } else {
      return calculateTree31(features, 708);
    }
  }
  if (node === 709) return -0.4136570826667206;
  if (node === 710) return 0.3801370202505056;
  if (node === 354) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree31(features, 709);
    } else if (val <= 175.50000000000003) {
      return calculateTree31(features, 709);
    } else {
      return calculateTree31(features, 710);
    }
  }
  if (node === 176) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree31(features, 353);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree31(features, 353);
    } else {
      return calculateTree31(features, 354);
    }
  }
  if (node === 87) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree31(features, 175);
    } else if (val <= 171.00000000000003) {
      return calculateTree31(features, 175);
    } else {
      return calculateTree31(features, 176);
    }
  }
  if (node === 177) return -0.2947551509039406;
  if (node === 178) return -0.025934796719544406;
  if (node === 88) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree31(features, 177);
    } else if (val <= 210.00000000000003) {
      return calculateTree31(features, 177);
    } else {
      return calculateTree31(features, 178);
    }
  }
  if (node === 43) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree31(features, 87);
    } else if (val <= 181.50000000000003) {
      return calculateTree31(features, 87);
    } else {
      return calculateTree31(features, 88);
    }
  }
  if (node === 719) return -0.3273628852521138;
  if (node === 720) return 0.3062668154924229;
  if (node === 359) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree31(features, 719);
    } else if (val <= 240.00000000000003) {
      return calculateTree31(features, 719);
    } else {
      return calculateTree31(features, 720);
    }
  }
  if (node === 360) return -0.46050209822444993;
  if (node === 179) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree31(features, 359);
    } else if (val <= 409.50000000000006) {
      return calculateTree31(features, 359);
    } else {
      return calculateTree31(features, 360);
    }
  }
  if (node === 180) return 0.2536588604511881;
  if (node === 89) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree31(features, 179);
    } else if (val <= 829.0000000000001) {
      return calculateTree31(features, 179);
    } else {
      return calculateTree31(features, 180);
    }
  }
  if (node === 181) return 0.2146908813512776;
  if (node === 182) return -0.5670194421901804;
  if (node === 90) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree31(features, 181);
    } else if (val <= 829.0000000000001) {
      return calculateTree31(features, 181);
    } else {
      return calculateTree31(features, 182);
    }
  }
  if (node === 44) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree31(features, 89);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree31(features, 89);
    } else {
      return calculateTree31(features, 90);
    }
  }
  if (node === 21) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree31(features, 43);
    } else if (val <= 191.50000000000003) {
      return calculateTree31(features, 43);
    } else {
      return calculateTree31(features, 44);
    }
  }
  if (node === 45) return -0.17236969378100903;
  if (node === 751) return 0.08075778136732444;
  if (node === 752) return -0.40831292444950945;
  if (node === 375) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree31(features, 751);
    } else if (val <= 391.50000000000006) {
      return calculateTree31(features, 751);
    } else {
      return calculateTree31(features, 752);
    }
  }
  if (node === 753) return -0.39450652887928433;
  if (node === 1509) return 0.19912116071287192;
  if (node === 1510) return -0.35426628953506106;
  if (node === 754) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree31(features, 1509);
    } else if (val <= 398.00000000000006) {
      return calculateTree31(features, 1509);
    } else {
      return calculateTree31(features, 1510);
    }
  }
  if (node === 376) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree31(features, 753);
    } else if (val <= 391.50000000000006) {
      return calculateTree31(features, 753);
    } else {
      return calculateTree31(features, 754);
    }
  }
  if (node === 187) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree31(features, 375);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree31(features, 375);
    } else {
      return calculateTree31(features, 376);
    }
  }
  if (node === 1511) return 0.020157087883585414;
  if (node === 1512) return -0.44985160767010807;
  if (node === 755) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree31(features, 1511);
    } else if (val <= 6.000000000000001) {
      return calculateTree31(features, 1511);
    } else {
      return calculateTree31(features, 1512);
    }
  }
  if (node === 756) return 0.22277922073813447;
  if (node === 377) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree31(features, 755);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree31(features, 755);
    } else {
      return calculateTree31(features, 756);
    }
  }
  if (node === 378) return -0.044127070344444776;
  if (node === 188) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree31(features, 377);
    } else if (val <= 12.000000000000002) {
      return calculateTree31(features, 377);
    } else {
      return calculateTree31(features, 378);
    }
  }
  if (node === 93) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree31(features, 187);
    } else if (val <= 427.50000000000006) {
      return calculateTree31(features, 187);
    } else {
      return calculateTree31(features, 188);
    }
  }
  if (node === 94) return 0.0010815188633044068;
  if (node === 46) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree31(features, 93);
    } else if (val <= 166.00000000000003) {
      return calculateTree31(features, 93);
    } else {
      return calculateTree31(features, 94);
    }
  }
  if (node === 22) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree31(features, 45);
    } else if (val <= 217.50000000000003) {
      return calculateTree31(features, 45);
    } else {
      return calculateTree31(features, 46);
    }
  }
  if (node === 10) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree31(features, 21);
    } else if (val <= 212.50000000000003) {
      return calculateTree31(features, 21);
    } else {
      return calculateTree31(features, 22);
    }
  }
  if (node === 4) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree31(features, 9);
    } else if (val <= 16.500000000000004) {
      return calculateTree31(features, 9);
    } else {
      return calculateTree31(features, 10);
    }
  }
  if (node === 1) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree31(features, 3);
    } else if (val <= 27.500000000000004) {
      return calculateTree31(features, 3);
    } else {
      return calculateTree31(features, 4);
    }
  }
  if (node === 2) return -0.002516833691912489;
  if (node === 0) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree31(features, 1);
    } else if (val <= 1.5000000000000002) {
      return calculateTree31(features, 1);
    } else {
      return calculateTree31(features, 2);
    }
  }
}
function calculateTree32(features, node = 0) {
  if (node === 15) return 0.025432873079225798;
  if (node === 67) return -0.0066889049869709675;
  if (node === 137) return 0.0903011554730359;
  if (node === 277) return -0.27276877141980965;
  if (node === 278) return -0.03416844454184664;
  if (node === 138) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree32(features, 277);
    } else if (val <= 158.50000000000003) {
      return calculateTree32(features, 277);
    } else {
      return calculateTree32(features, 278);
    }
  }
  if (node === 68) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree32(features, 137);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree32(features, 137);
    } else {
      return calculateTree32(features, 138);
    }
  }
  if (node === 33) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree32(features, 67);
    } else if (val <= 141.50000000000003) {
      return calculateTree32(features, 67);
    } else {
      return calculateTree32(features, 68);
    }
  }
  if (node === 34) return -0.33250189804396363;
  if (node === 16) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree32(features, 33);
    } else if (val <= 171.00000000000003) {
      return calculateTree32(features, 33);
    } else {
      return calculateTree32(features, 34);
    }
  }
  if (node === 7) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree32(features, 15);
    } else if (val <= 191.50000000000003) {
      return calculateTree32(features, 15);
    } else {
      return calculateTree32(features, 16);
    }
  }
  if (node === 8) return 0.0006177993553413345;
  if (node === 3) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree32(features, 7);
    } else if (val <= 172.50000000000003) {
      return calculateTree32(features, 7);
    } else {
      return calculateTree32(features, 8);
    }
  }
  if (node === 19) return 0.18549091689336264;
  if (node === 41) return -0.36521201988458674;
  if (node === 85) return 0.2413911747996217;
  if (node === 86) return -0.12881772218025173;
  if (node === 42) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree32(features, 85);
    } else if (val <= 734.0000000000001) {
      return calculateTree32(features, 85);
    } else {
      return calculateTree32(features, 86);
    }
  }
  if (node === 20) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree32(features, 41);
    } else if (val <= 718.5000000000001) {
      return calculateTree32(features, 41);
    } else {
      return calculateTree32(features, 42);
    }
  }
  if (node === 9) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree32(features, 19);
    } else if (val <= 478.50000000000006) {
      return calculateTree32(features, 19);
    } else {
      return calculateTree32(features, 20);
    }
  }
  if (node === 10) return -0.30508581957740283;
  if (node === 4) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree32(features, 9);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree32(features, 9);
    } else {
      return calculateTree32(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree32(features, 3);
    } else if (val <= 943.5000000000001) {
      return calculateTree32(features, 3);
    } else {
      return calculateTree32(features, 4);
    }
  }
  if (node === 23) return 0.08677710031303741;
  if (node === 24) return 0.27120751665567383;
  if (node === 11) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree32(features, 23);
    } else if (val <= 2.5000000000000004) {
      return calculateTree32(features, 23);
    } else {
      return calculateTree32(features, 24);
    }
  }
  if (node === 25) return -0.30195063772830666;
  if (node === 107) return 0.06921732138954886;
  if (node === 108) return 0.2023781380417055;
  if (node === 53) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree32(features, 107);
    } else if (val <= 82.50000000000001) {
      return calculateTree32(features, 107);
    } else {
      return calculateTree32(features, 108);
    }
  }
  if (node === 109) return -0.3438866314765748;
  if (node === 221) return 0.012751543756579971;
  if (node === 222) return 0.10261315764928838;
  if (node === 110) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree32(features, 221);
    } else if (val <= 4.500000000000001) {
      return calculateTree32(features, 221);
    } else {
      return calculateTree32(features, 222);
    }
  }
  if (node === 54) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree32(features, 109);
    } else if (val <= 283.50000000000006) {
      return calculateTree32(features, 109);
    } else {
      return calculateTree32(features, 110);
    }
  }
  if (node === 26) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree32(features, 53);
    } else if (val <= 139.50000000000003) {
      return calculateTree32(features, 53);
    } else {
      return calculateTree32(features, 54);
    }
  }
  if (node === 12) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree32(features, 25);
    } else if (val <= 962.5000000000001) {
      return calculateTree32(features, 25);
    } else {
      return calculateTree32(features, 26);
    }
  }
  if (node === 5) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree32(features, 11);
    } else if (val <= 955.5000000000001) {
      return calculateTree32(features, 11);
    } else {
      return calculateTree32(features, 12);
    }
  }
  if (node === 55) return -0.33553432103266956;
  if (node === 56) return 0.302372610700893;
  if (node === 27) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree32(features, 55);
    } else if (val <= 993.5000000000001) {
      return calculateTree32(features, 55);
    } else {
      return calculateTree32(features, 56);
    }
  }
  if (node === 57) return 0.04754463850118972;
  if (node === 58) return 0.16794362806099794;
  if (node === 28) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree32(features, 57);
    } else if (val <= 6.500000000000001) {
      return calculateTree32(features, 57);
    } else {
      return calculateTree32(features, 58);
    }
  }
  if (node === 13) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree32(features, 27);
    } else if (val <= 127.50000000000001) {
      return calculateTree32(features, 27);
    } else {
      return calculateTree32(features, 28);
    }
  }
  if (node === 29) return -0.3260586130930825;
  if (node === 61) return -0.32978735755701044;
  if (node === 125) return 0.1302987880964116;
  if (node === 253) return -0.32249264839200614;
  if (node === 509) return 0.24934349028610395;
  if (node === 2043) return 0.007388611455465085;
  if (node === 2044) return -0.3001382147479335;
  if (node === 1021) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree32(features, 2043);
    } else if (val <= 678.5000000000001) {
      return calculateTree32(features, 2043);
    } else {
      return calculateTree32(features, 2044);
    }
  }
  if (node === 1022) return 0.24891247974982927;
  if (node === 510) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree32(features, 1021);
    } else if (val <= 953.5000000000001) {
      return calculateTree32(features, 1021);
    } else {
      return calculateTree32(features, 1022);
    }
  }
  if (node === 254) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree32(features, 509);
    } else if (val <= 675.0000000000001) {
      return calculateTree32(features, 509);
    } else {
      return calculateTree32(features, 510);
    }
  }
  if (node === 126) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree32(features, 253);
    } else if (val <= 661.0000000000001) {
      return calculateTree32(features, 253);
    } else {
      return calculateTree32(features, 254);
    }
  }
  if (node === 62) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree32(features, 125);
    } else if (val <= 449.50000000000006) {
      return calculateTree32(features, 125);
    } else {
      return calculateTree32(features, 126);
    }
  }
  if (node === 30) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree32(features, 61);
    } else if (val <= 966.0000000000001) {
      return calculateTree32(features, 61);
    } else {
      return calculateTree32(features, 62);
    }
  }
  if (node === 14) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree32(features, 29);
    } else if (val <= 361.50000000000006) {
      return calculateTree32(features, 29);
    } else {
      return calculateTree32(features, 30);
    }
  }
  if (node === 6) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree32(features, 13);
    } else if (val <= 181.50000000000003) {
      return calculateTree32(features, 13);
    } else {
      return calculateTree32(features, 14);
    }
  }
  if (node === 2) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree32(features, 5);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree32(features, 5);
    } else {
      return calculateTree32(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree32(features, 1);
    } else if (val <= 954.5000000000001) {
      return calculateTree32(features, 1);
    } else {
      return calculateTree32(features, 2);
    }
  }
}
function calculateTree33(features, node = 0) {
  if (node === 3) return 0.0007133785064649445;
  if (node === 39) return 0.010762408696982396;
  if (node === 40) return -0.3915660721558407;
  if (node === 19) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree33(features, 39);
    } else if (val <= 185.00000000000003) {
      return calculateTree33(features, 39);
    } else {
      return calculateTree33(features, 40);
    }
  }
  if (node === 20) return 0.24583571633435175;
  if (node === 9) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree33(features, 19);
    } else if (val <= 670.5000000000001) {
      return calculateTree33(features, 19);
    } else {
      return calculateTree33(features, 20);
    }
  }
  if (node === 43) return -0.33217992497544113;
  if (node === 44) return 0.18903155674314465;
  if (node === 21) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree33(features, 43);
    } else if (val <= 185.00000000000003) {
      return calculateTree33(features, 43);
    } else {
      return calculateTree33(features, 44);
    }
  }
  if (node === 22) return -0.35385249818013437;
  if (node === 10) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree33(features, 21);
    } else if (val <= 670.5000000000001) {
      return calculateTree33(features, 21);
    } else {
      return calculateTree33(features, 22);
    }
  }
  if (node === 4) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree33(features, 9);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree33(features, 9);
    } else {
      return calculateTree33(features, 10);
    }
  }
  if (node === 1) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree33(features, 3);
    } else if (val <= 316.50000000000006) {
      return calculateTree33(features, 3);
    } else {
      return calculateTree33(features, 4);
    }
  }
  if (node === 23) return 0.29692002260689987;
  if (node === 24) return -0.3604961760597258;
  if (node === 11) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree33(features, 23);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree33(features, 23);
    } else {
      return calculateTree33(features, 24);
    }
  }
  if (node === 103) return 0.039657347567186925;
  if (node === 104) return -0.34542185366883316;
  if (node === 51) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree33(features, 103);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree33(features, 103);
    } else {
      return calculateTree33(features, 104);
    }
  }
  if (node === 211) return -0.35741954434588413;
  if (node === 212) return -0.12283952770745352;
  if (node === 105) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree33(features, 211);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree33(features, 211);
    } else {
      return calculateTree33(features, 212);
    }
  }
  if (node === 106) return -0.38578573932708143;
  if (node === 52) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree33(features, 105);
    } else if (val <= 346.00000000000006) {
      return calculateTree33(features, 105);
    } else {
      return calculateTree33(features, 106);
    }
  }
  if (node === 25) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree33(features, 51);
    } else if (val <= 180.50000000000003) {
      return calculateTree33(features, 51);
    } else {
      return calculateTree33(features, 52);
    }
  }
  if (node === 53) return -0.313493996406646;
  if (node === 109) return -0.3516962401038629;
  if (node === 110) return 0.4228752405209704;
  if (node === 54) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree33(features, 109);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree33(features, 109);
    } else {
      return calculateTree33(features, 110);
    }
  }
  if (node === 26) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree33(features, 53);
    } else if (val <= 346.00000000000006) {
      return calculateTree33(features, 53);
    } else {
      return calculateTree33(features, 54);
    }
  }
  if (node === 12) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree33(features, 25);
    } else if (val <= 688.5000000000001) {
      return calculateTree33(features, 25);
    } else {
      return calculateTree33(features, 26);
    }
  }
  if (node === 5) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree33(features, 11);
    } else if (val <= 60.00000000000001) {
      return calculateTree33(features, 11);
    } else {
      return calculateTree33(features, 12);
    }
  }
  if (node === 55) return 0.13243607302040192;
  if (node === 227) return -0.35510116060133956;
  if (node === 228) return 0.06347205917635688;
  if (node === 113) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree33(features, 227);
    } else if (val <= 189.50000000000003) {
      return calculateTree33(features, 227);
    } else {
      return calculateTree33(features, 228);
    }
  }
  if (node === 114) return -0.42791301867808373;
  if (node === 56) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree33(features, 113);
    } else if (val <= 325.00000000000006) {
      return calculateTree33(features, 113);
    } else {
      return calculateTree33(features, 114);
    }
  }
  if (node === 27) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree33(features, 55);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree33(features, 55);
    } else {
      return calculateTree33(features, 56);
    }
  }
  if (node === 115) return -0.5260382140728971;
  if (node === 116) return 0.02180983818507439;
  if (node === 57) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree33(features, 115);
    } else if (val <= 973.5000000000001) {
      return calculateTree33(features, 115);
    } else {
      return calculateTree33(features, 116);
    }
  }
  if (node === 117) return 0.2641589067525115;
  if (node === 118) return -0.3208617510624317;
  if (node === 58) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree33(features, 117);
    } else if (val <= 973.5000000000001) {
      return calculateTree33(features, 117);
    } else {
      return calculateTree33(features, 118);
    }
  }
  if (node === 28) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree33(features, 57);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree33(features, 57);
    } else {
      return calculateTree33(features, 58);
    }
  }
  if (node === 13) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree33(features, 27);
    } else if (val <= 331.50000000000006) {
      return calculateTree33(features, 27);
    } else {
      return calculateTree33(features, 28);
    }
  }
  if (node === 59) return -0.26554502517099143;
  if (node === 243) return -0.3699371869032518;
  if (node === 244) return 0.39071407011823894;
  if (node === 121) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree33(features, 243);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree33(features, 243);
    } else {
      return calculateTree33(features, 244);
    }
  }
  if (node === 245) return -0.3295959349190499;
  if (node === 493) return -0.01179508751329515;
  if (node === 494) return -0.18522130313103066;
  if (node === 246) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree33(features, 493);
    } else if (val <= 185.00000000000003) {
      return calculateTree33(features, 493);
    } else {
      return calculateTree33(features, 494);
    }
  }
  if (node === 122) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree33(features, 245);
    } else if (val <= 432.00000000000006) {
      return calculateTree33(features, 245);
    } else {
      return calculateTree33(features, 246);
    }
  }
  if (node === 60) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree33(features, 121);
    } else if (val <= 422.00000000000006) {
      return calculateTree33(features, 121);
    } else {
      return calculateTree33(features, 122);
    }
  }
  if (node === 29) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree33(features, 59);
    } else if (val <= 409.50000000000006) {
      return calculateTree33(features, 59);
    } else {
      return calculateTree33(features, 60);
    }
  }
  if (node === 30) return -0.0006532187264574555;
  if (node === 14) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree33(features, 29);
    } else if (val <= 191.50000000000003) {
      return calculateTree33(features, 29);
    } else {
      return calculateTree33(features, 30);
    }
  }
  if (node === 6) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree33(features, 13);
    } else if (val <= 353.00000000000006) {
      return calculateTree33(features, 13);
    } else {
      return calculateTree33(features, 14);
    }
  }
  if (node === 2) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree33(features, 5);
    } else if (val <= 348.50000000000006) {
      return calculateTree33(features, 5);
    } else {
      return calculateTree33(features, 6);
    }
  }
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree33(features, 1);
    } else if (val <= 327.50000000000006) {
      return calculateTree33(features, 1);
    } else {
      return calculateTree33(features, 2);
    }
  }
}
function calculateTree34(features, node = 0) {
  if (node === 1) return 0.002070710191490986;
  if (node === 23) return -0.0003181289375914579;
  if (node === 49) return -0.30823979148252145;
  if (node === 101) return -0.41449761458207657;
  if (node === 102) return -0.009299839684465048;
  if (node === 50) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree34(features, 101);
    } else if (val <= 800.5000000000001) {
      return calculateTree34(features, 101);
    } else {
      return calculateTree34(features, 102);
    }
  }
  if (node === 24) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree34(features, 49);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree34(features, 49);
    } else {
      return calculateTree34(features, 50);
    }
  }
  if (node === 11) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree34(features, 23);
    } else if (val <= 795.5000000000001) {
      return calculateTree34(features, 23);
    } else {
      return calculateTree34(features, 24);
    }
  }
  if (node === 1663) return 0.020764710688257956;
  if (node === 1664) return -0.4156735724063068;
  if (node === 831) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree34(features, 1663);
    } else if (val <= 861.5000000000001) {
      return calculateTree34(features, 1663);
    } else {
      return calculateTree34(features, 1664);
    }
  }
  if (node === 832) return 0.1270063167967299;
  if (node === 415) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree34(features, 831);
    } else if (val <= 884.0000000000001) {
      return calculateTree34(features, 831);
    } else {
      return calculateTree34(features, 832);
    }
  }
  if (node === 416) return -0.3469227026454205;
  if (node === 207) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree34(features, 415);
    } else if (val <= 212.50000000000003) {
      return calculateTree34(features, 415);
    } else {
      return calculateTree34(features, 416);
    }
  }
  if (node === 208) return 0.30883821116423066;
  if (node === 103) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree34(features, 207);
    } else if (val <= 387.50000000000006) {
      return calculateTree34(features, 207);
    } else {
      return calculateTree34(features, 208);
    }
  }
  if (node === 104) return -0.40399191500662796;
  if (node === 51) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree34(features, 103);
    } else if (val <= 472.50000000000006) {
      return calculateTree34(features, 103);
    } else {
      return calculateTree34(features, 104);
    }
  }
  if (node === 211) return 0.06556041997142732;
  if (node === 212) return -0.3302773570629036;
  if (node === 105) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree34(features, 211);
    } else if (val <= 123.50000000000001) {
      return calculateTree34(features, 211);
    } else {
      return calculateTree34(features, 212);
    }
  }
  if (node === 106) return 0.1554408462605789;
  if (node === 52) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree34(features, 105);
    } else if (val <= 217.50000000000003) {
      return calculateTree34(features, 105);
    } else {
      return calculateTree34(features, 106);
    }
  }
  if (node === 25) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree34(features, 51);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree34(features, 51);
    } else {
      return calculateTree34(features, 52);
    }
  }
  if (node === 215) return -0.3861304342324848;
  if (node === 216) return 0.11743972668019767;
  if (node === 107) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree34(features, 215);
    } else if (val <= 57.50000000000001) {
      return calculateTree34(features, 215);
    } else {
      return calculateTree34(features, 216);
    }
  }
  if (node === 108) return -0.3447257528448973;
  if (node === 53) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree34(features, 107);
    } else if (val <= 311.50000000000006) {
      return calculateTree34(features, 107);
    } else {
      return calculateTree34(features, 108);
    }
  }
  if (node === 219) return 0.08001141069799798;
  if (node === 220) return -0.3923397397941832;
  if (node === 109) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree34(features, 219);
    } else if (val <= 97.50000000000001) {
      return calculateTree34(features, 219);
    } else {
      return calculateTree34(features, 220);
    }
  }
  if (node === 110) return 0.07276354934085447;
  if (node === 54) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree34(features, 109);
    } else if (val <= 311.50000000000006) {
      return calculateTree34(features, 109);
    } else {
      return calculateTree34(features, 110);
    }
  }
  if (node === 26) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree34(features, 53);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree34(features, 53);
    } else {
      return calculateTree34(features, 54);
    }
  }
  if (node === 12) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree34(features, 25);
    } else if (val <= 911.0000000000001) {
      return calculateTree34(features, 25);
    } else {
      return calculateTree34(features, 26);
    }
  }
  if (node === 5) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree34(features, 11);
    } else if (val <= 835.5000000000001) {
      return calculateTree34(features, 11);
    } else {
      return calculateTree34(features, 12);
    }
  }
  if (node === 447) return -0.3242146400716877;
  if (node === 448) return 0.20093724888060732;
  if (node === 223) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree34(features, 447);
    } else if (val <= 539.5000000000001) {
      return calculateTree34(features, 447);
    } else {
      return calculateTree34(features, 448);
    }
  }
  if (node === 224) return 0.5446512413138457;
  if (node === 111) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree34(features, 223);
    } else if (val <= 800.5000000000001) {
      return calculateTree34(features, 223);
    } else {
      return calculateTree34(features, 224);
    }
  }
  if (node === 112) return -0.39568670251225696;
  if (node === 55) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree34(features, 111);
    } else if (val <= 877.5000000000001) {
      return calculateTree34(features, 111);
    } else {
      return calculateTree34(features, 112);
    }
  }
  if (node === 56) return -0.34468110722603634;
  if (node === 27) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree34(features, 55);
    } else if (val <= 548.5000000000001) {
      return calculateTree34(features, 55);
    } else {
      return calculateTree34(features, 56);
    }
  }
  if (node === 28) return -0.37396919889031766;
  if (node === 13) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree34(features, 27);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree34(features, 27);
    } else {
      return calculateTree34(features, 28);
    }
  }
  if (node === 59) return -0.351978847823752;
  if (node === 60) return 0.2600482827479233;
  if (node === 29) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree34(features, 59);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree34(features, 59);
    } else {
      return calculateTree34(features, 60);
    }
  }
  if (node === 247) return 0.09693591462979273;
  if (node === 248) return -0.2982647443828121;
  if (node === 123) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree34(features, 247);
    } else if (val <= 566.0000000000001) {
      return calculateTree34(features, 247);
    } else {
      return calculateTree34(features, 248);
    }
  }
  if (node === 124) return -0.33928012200154417;
  if (node === 61) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree34(features, 123);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree34(features, 123);
    } else {
      return calculateTree34(features, 124);
    }
  }
  if (node === 62) return -0.00525056106152392;
  if (node === 30) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree34(features, 61);
    } else if (val <= 597.5000000000001) {
      return calculateTree34(features, 61);
    } else {
      return calculateTree34(features, 62);
    }
  }
  if (node === 14) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree34(features, 29);
    } else if (val <= 555.5000000000001) {
      return calculateTree34(features, 29);
    } else {
      return calculateTree34(features, 30);
    }
  }
  if (node === 6) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree34(features, 13);
    } else if (val <= 551.5000000000001) {
      return calculateTree34(features, 13);
    } else {
      return calculateTree34(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree34(features, 5);
    } else if (val <= 518.5000000000001) {
      return calculateTree34(features, 5);
    } else {
      return calculateTree34(features, 6);
    }
  }
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree34(features, 1);
    } else if (val <= 500.50000000000006) {
      return calculateTree34(features, 1);
    } else {
      return calculateTree34(features, 2);
    }
  }
}
function calculateTree35(features, node = 0) {
  if (node === 3) return -0.0012840187193409308;
  if (node === 39) return 0.10953036306131358;
  if (node === 40) return -0.40717993559267585;
  if (node === 19) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree35(features, 39);
    } else if (val <= 731.5000000000001) {
      return calculateTree35(features, 39);
    } else {
      return calculateTree35(features, 40);
    }
  }
  if (node === 20) return 0.09626740224994462;
  if (node === 9) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree35(features, 19);
    } else if (val <= 459.50000000000006) {
      return calculateTree35(features, 19);
    } else {
      return calculateTree35(features, 20);
    }
  }
  if (node === 43) return -0.4071135836999078;
  if (node === 44) return 0.05026568193538923;
  if (node === 21) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree35(features, 43);
    } else if (val <= 731.5000000000001) {
      return calculateTree35(features, 43);
    } else {
      return calculateTree35(features, 44);
    }
  }
  if (node === 45) return -0.35617258842446375;
  if (node === 46) return 0.4422238156101324;
  if (node === 22) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree35(features, 45);
    } else if (val <= 973.5000000000001) {
      return calculateTree35(features, 45);
    } else {
      return calculateTree35(features, 46);
    }
  }
  if (node === 10) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree35(features, 21);
    } else if (val <= 459.50000000000006) {
      return calculateTree35(features, 21);
    } else {
      return calculateTree35(features, 22);
    }
  }
  if (node === 4) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree35(features, 9);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree35(features, 9);
    } else {
      return calculateTree35(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree35(features, 3);
    } else if (val <= 729.5000000000001) {
      return calculateTree35(features, 3);
    } else {
      return calculateTree35(features, 4);
    }
  }
  if (node === 47) return 0.316425716072712;
  if (node === 48) return -0.28129256234284983;
  if (node === 23) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree35(features, 47);
    } else if (val <= 588.5000000000001) {
      return calculateTree35(features, 47);
    } else {
      return calculateTree35(features, 48);
    }
  }
  if (node === 24) return -0.35909768051983365;
  if (node === 11) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree35(features, 23);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree35(features, 23);
    } else {
      return calculateTree35(features, 24);
    }
  }
  if (node === 25) return -0.4852360159990269;
  if (node === 26) return 0.21159645030991933;
  if (node === 12) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree35(features, 25);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree35(features, 25);
    } else {
      return calculateTree35(features, 26);
    }
  }
  if (node === 5) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree35(features, 11);
    } else if (val <= 781.0000000000001) {
      return calculateTree35(features, 11);
    } else {
      return calculateTree35(features, 12);
    }
  }
  if (node === 895) return -0.3276178799520625;
  if (node === 896) return 0.20412758980296963;
  if (node === 447) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree35(features, 895);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree35(features, 895);
    } else {
      return calculateTree35(features, 896);
    }
  }
  if (node === 897) return -0.3315741261611385;
  if (node === 898) return 0.023206280849544486;
  if (node === 448) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree35(features, 897);
    } else if (val <= 556.0000000000001) {
      return calculateTree35(features, 897);
    } else {
      return calculateTree35(features, 898);
    }
  }
  if (node === 223) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree35(features, 447);
    } else if (val <= 144.50000000000003) {
      return calculateTree35(features, 447);
    } else {
      return calculateTree35(features, 448);
    }
  }
  if (node === 899) return 0.02677512111175642;
  if (node === 1801) return 0.428856778835634;
  if (node === 1802) return -0.47651548712026187;
  if (node === 900) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree35(features, 1801);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree35(features, 1801);
    } else {
      return calculateTree35(features, 1802);
    }
  }
  if (node === 449) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree35(features, 899);
    } else if (val <= 606.5000000000001) {
      return calculateTree35(features, 899);
    } else {
      return calculateTree35(features, 900);
    }
  }
  if (node === 1803) return -0.40023233049116824;
  if (node === 1804) return 0.21997445086757914;
  if (node === 901) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree35(features, 1803);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree35(features, 1803);
    } else {
      return calculateTree35(features, 1804);
    }
  }
  if (node === 902) return -0.3975153505803124;
  if (node === 450) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree35(features, 901);
    } else if (val <= 236.00000000000003) {
      return calculateTree35(features, 901);
    } else {
      return calculateTree35(features, 902);
    }
  }
  if (node === 224) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree35(features, 449);
    } else if (val <= 789.5000000000001) {
      return calculateTree35(features, 449);
    } else {
      return calculateTree35(features, 450);
    }
  }
  if (node === 111) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree35(features, 223);
    } else if (val <= 780.0000000000001) {
      return calculateTree35(features, 223);
    } else {
      return calculateTree35(features, 224);
    }
  }
  if (node === 112) return -0.2912057511819607;
  if (node === 55) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree35(features, 111);
    } else if (val <= 619.5000000000001) {
      return calculateTree35(features, 111);
    } else {
      return calculateTree35(features, 112);
    }
  }
  if (node === 113) return -0.21413202706939477;
  if (node === 229) return 0.3500544770494118;
  if (node === 461) return -0.335175735112648;
  if (node === 462) return 0.2065279905966641;
  if (node === 230) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree35(features, 461);
    } else if (val <= 758.0000000000001) {
      return calculateTree35(features, 461);
    } else {
      return calculateTree35(features, 462);
    }
  }
  if (node === 114) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree35(features, 229);
    } else if (val <= 695.5000000000001) {
      return calculateTree35(features, 229);
    } else {
      return calculateTree35(features, 230);
    }
  }
  if (node === 56) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree35(features, 113);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree35(features, 113);
    } else {
      return calculateTree35(features, 114);
    }
  }
  if (node === 27) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree35(features, 55);
    } else if (val <= 690.5000000000001) {
      return calculateTree35(features, 55);
    } else {
      return calculateTree35(features, 56);
    }
  }
  if (node === 57) return -0.289063580750056;
  if (node === 117) return -0.44061669950449633;
  if (node === 118) return 0.46091312121093886;
  if (node === 58) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree35(features, 117);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree35(features, 117);
    } else {
      return calculateTree35(features, 118);
    }
  }
  if (node === 28) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree35(features, 57);
    } else if (val <= 789.5000000000001) {
      return calculateTree35(features, 57);
    } else {
      return calculateTree35(features, 58);
    }
  }
  if (node === 13) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree35(features, 27);
    } else if (val <= 776.0000000000001) {
      return calculateTree35(features, 27);
    } else {
      return calculateTree35(features, 28);
    }
  }
  if (node === 14) return 0.006214550163798161;
  if (node === 6) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree35(features, 13);
    } else if (val <= 794.0000000000001) {
      return calculateTree35(features, 13);
    } else {
      return calculateTree35(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree35(features, 5);
    } else if (val <= 752.5000000000001) {
      return calculateTree35(features, 5);
    } else {
      return calculateTree35(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree35(features, 1);
    } else if (val <= 745.5000000000001) {
      return calculateTree35(features, 1);
    } else {
      return calculateTree35(features, 2);
    }
  }
}
function calculateTree36(features, node = 0) {
  if (node === 63) return -0.39388164048465074;
  if (node === 129) return 0.3094023799340429;
  if (node === 261) return -0.4834833369627368;
  if (node === 525) return 0.05285226611378454;
  if (node === 526) return -0.10865297417576615;
  if (node === 262) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree36(features, 525);
    } else if (val <= 75.50000000000001) {
      return calculateTree36(features, 525);
    } else {
      return calculateTree36(features, 526);
    }
  }
  if (node === 130) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree36(features, 261);
    } else if (val <= 175.50000000000003) {
      return calculateTree36(features, 261);
    } else {
      return calculateTree36(features, 262);
    }
  }
  if (node === 64) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree36(features, 129);
    } else if (val <= 132.50000000000003) {
      return calculateTree36(features, 129);
    } else {
      return calculateTree36(features, 130);
    }
  }
  if (node === 31) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree36(features, 63);
    } else if (val <= 115.50000000000001) {
      return calculateTree36(features, 63);
    } else {
      return calculateTree36(features, 64);
    }
  }
  if (node === 32) return -0.3484925001193645;
  if (node === 15) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree36(features, 31);
    } else if (val <= 123.50000000000001) {
      return calculateTree36(features, 31);
    } else {
      return calculateTree36(features, 32);
    }
  }
  if (node === 16) return 0.09543590083930437;
  if (node === 7) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree36(features, 15);
    } else if (val <= 135.50000000000003) {
      return calculateTree36(features, 15);
    } else {
      return calculateTree36(features, 16);
    }
  }
  if (node === 17) return -0.2884668815743474;
  if (node === 37) return 0.14307821455991623;
  if (node === 77) return -0.22134675255229588;
  if (node === 631) return 0.35231716295600696;
  if (node === 632) return -0.224570803445907;
  if (node === 315) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree36(features, 631);
    } else if (val <= 55.50000000000001) {
      return calculateTree36(features, 631);
    } else {
      return calculateTree36(features, 632);
    }
  }
  if (node === 316) return 0.2604191046906824;
  if (node === 157) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree36(features, 315);
    } else if (val <= 504.50000000000006) {
      return calculateTree36(features, 315);
    } else {
      return calculateTree36(features, 316);
    }
  }
  if (node === 158) return -0.2886143790738369;
  if (node === 78) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree36(features, 157);
    } else if (val <= 679.5000000000001) {
      return calculateTree36(features, 157);
    } else {
      return calculateTree36(features, 158);
    }
  }
  if (node === 38) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree36(features, 77);
    } else if (val <= 222.50000000000003) {
      return calculateTree36(features, 77);
    } else {
      return calculateTree36(features, 78);
    }
  }
  if (node === 18) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree36(features, 37);
    } else if (val <= 195.00000000000003) {
      return calculateTree36(features, 37);
    } else {
      return calculateTree36(features, 38);
    }
  }
  if (node === 8) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree36(features, 17);
    } else if (val <= 191.50000000000003) {
      return calculateTree36(features, 17);
    } else {
      return calculateTree36(features, 18);
    }
  }
  if (node === 3) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree36(features, 7);
    } else if (val <= 166.50000000000003) {
      return calculateTree36(features, 7);
    } else {
      return calculateTree36(features, 8);
    }
  }
  if (node === 19) return -0.35082496913333727;
  if (node === 335) return 0.1280198334267419;
  if (node === 336) return -0.3761744384997711;
  if (node === 167) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree36(features, 335);
    } else if (val <= 66.00000000000001) {
      return calculateTree36(features, 335);
    } else {
      return calculateTree36(features, 336);
    }
  }
  if (node === 1351) return 0.040038797942090185;
  if (node === 1352) return -0.3568901241705639;
  if (node === 675) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree36(features, 1351);
    } else if (val <= 531.0000000000001) {
      return calculateTree36(features, 1351);
    } else {
      return calculateTree36(features, 1352);
    }
  }
  if (node === 676) return -0.2609584607368178;
  if (node === 337) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree36(features, 675);
    } else if (val <= 36.50000000000001) {
      return calculateTree36(features, 675);
    } else {
      return calculateTree36(features, 676);
    }
  }
  if (node === 677) return 0.2419571401505264;
  if (node === 1357) return -0.3306363298921859;
  if (node === 1358) return 0.00995856580610168;
  if (node === 678) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree36(features, 1357);
    } else if (val <= 778.5000000000001) {
      return calculateTree36(features, 1357);
    } else {
      return calculateTree36(features, 1358);
    }
  }
  if (node === 338) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree36(features, 677);
    } else if (val <= 705.5000000000001) {
      return calculateTree36(features, 677);
    } else {
      return calculateTree36(features, 678);
    }
  }
  if (node === 168) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree36(features, 337);
    } else if (val <= 672.0000000000001) {
      return calculateTree36(features, 337);
    } else {
      return calculateTree36(features, 338);
    }
  }
  if (node === 83) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree36(features, 167);
    } else if (val <= 175.50000000000003) {
      return calculateTree36(features, 167);
    } else {
      return calculateTree36(features, 168);
    }
  }
  if (node === 84) return 0.08287071494042281;
  if (node === 41) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree36(features, 83);
    } else if (val <= 97.50000000000001) {
      return calculateTree36(features, 83);
    } else {
      return calculateTree36(features, 84);
    }
  }
  if (node === 171) return -0.12835254543603733;
  if (node === 172) return -0.36308972873757966;
  if (node === 85) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree36(features, 171);
    } else if (val <= 158.50000000000003) {
      return calculateTree36(features, 171);
    } else {
      return calculateTree36(features, 172);
    }
  }
  if (node === 86) return 0.004271480454002712;
  if (node === 42) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree36(features, 85);
    } else if (val <= 166.50000000000003) {
      return calculateTree36(features, 85);
    } else {
      return calculateTree36(features, 86);
    }
  }
  if (node === 20) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree36(features, 41);
    } else if (val <= 135.50000000000003) {
      return calculateTree36(features, 41);
    } else {
      return calculateTree36(features, 42);
    }
  }
  if (node === 9) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree36(features, 19);
    } else if (val <= 3.5000000000000004) {
      return calculateTree36(features, 19);
    } else {
      return calculateTree36(features, 20);
    }
  }
  if (node === 21) return 0.1522150783544142;
  if (node === 22) return 0.04229761793472002;
  if (node === 10) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree36(features, 21);
    } else if (val <= 34.00000000000001) {
      return calculateTree36(features, 21);
    } else {
      return calculateTree36(features, 22);
    }
  }
  if (node === 4) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree36(features, 9);
    } else if (val <= 188.00000000000003) {
      return calculateTree36(features, 9);
    } else {
      return calculateTree36(features, 10);
    }
  }
  if (node === 1) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree36(features, 3);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree36(features, 3);
    } else {
      return calculateTree36(features, 4);
    }
  }
  if (node === 11) return -0.36053849785437325;
  if (node === 12) return -0.004478612698272436;
  if (node === 5) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree36(features, 11);
    } else if (val <= 839.5000000000001) {
      return calculateTree36(features, 11);
    } else {
      return calculateTree36(features, 12);
    }
  }
  if (node === 6) return -0.0021148160567585083;
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree36(features, 5);
    } else if (val <= 258.50000000000006) {
      return calculateTree36(features, 5);
    } else {
      return calculateTree36(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree36(features, 1);
    } else if (val <= 253.50000000000003) {
      return calculateTree36(features, 1);
    } else {
      return calculateTree36(features, 2);
    }
  }
}
function calculateTree37(features, node = 0) {
  if (node === 3) return -0.0035744070463381698;
  if (node === 79) return 0.010894957121900353;
  if (node === 80) return -0.09463142254597395;
  if (node === 39) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree37(features, 79);
    } else if (val <= 611.0000000000001) {
      return calculateTree37(features, 79);
    } else {
      return calculateTree37(features, 80);
    }
  }
  if (node === 655) return 0.39736244168296836;
  if (node === 656) return 0.04121266862858421;
  if (node === 327) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree37(features, 655);
    } else if (val <= 398.00000000000006) {
      return calculateTree37(features, 655);
    } else {
      return calculateTree37(features, 656);
    }
  }
  if (node === 657) return 0.04563823808355279;
  if (node === 658) return -0.13847908288717947;
  if (node === 328) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree37(features, 657);
    } else if (val <= 816.0000000000001) {
      return calculateTree37(features, 657);
    } else {
      return calculateTree37(features, 658);
    }
  }
  if (node === 163) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree37(features, 327);
    } else if (val <= 659.5000000000001) {
      return calculateTree37(features, 327);
    } else {
      return calculateTree37(features, 328);
    }
  }
  if (node === 164) return 0.3096713766122909;
  if (node === 81) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree37(features, 163);
    } else if (val <= 822.5000000000001) {
      return calculateTree37(features, 163);
    } else {
      return calculateTree37(features, 164);
    }
  }
  if (node === 165) return -0.36073271968317844;
  if (node === 166) return -0.043400565324375896;
  if (node === 82) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree37(features, 165);
    } else if (val <= 852.5000000000001) {
      return calculateTree37(features, 165);
    } else {
      return calculateTree37(features, 166);
    }
  }
  if (node === 40) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree37(features, 81);
    } else if (val <= 844.5000000000001) {
      return calculateTree37(features, 81);
    } else {
      return calculateTree37(features, 82);
    }
  }
  if (node === 19) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree37(features, 39);
    } else if (val <= 656.5000000000001) {
      return calculateTree37(features, 39);
    } else {
      return calculateTree37(features, 40);
    }
  }
  if (node === 83) return -0.09240910328849229;
  if (node === 84) return 0.0058851285529483;
  if (node === 41) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree37(features, 83);
    } else if (val <= 957.5000000000001) {
      return calculateTree37(features, 83);
    } else {
      return calculateTree37(features, 84);
    }
  }
  if (node === 42) return 0.06654121106746062;
  if (node === 20) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree37(features, 41);
    } else if (val <= 845.5000000000001) {
      return calculateTree37(features, 41);
    } else {
      return calculateTree37(features, 42);
    }
  }
  if (node === 9) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree37(features, 19);
    } else if (val <= 897.5000000000001) {
      return calculateTree37(features, 19);
    } else {
      return calculateTree37(features, 20);
    }
  }
  if (node === 21) return 0.12169370836087552;
  if (node === 1471) return -0.041564695973593434;
  if (node === 1472) return 0.00036660869922726835;
  if (node === 735) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree37(features, 1471);
    } else if (val <= 311.50000000000006) {
      return calculateTree37(features, 1471);
    } else {
      return calculateTree37(features, 1472);
    }
  }
  if (node === 736) return -0.15640178117959644;
  if (node === 367) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree37(features, 735);
    } else if (val <= 429.00000000000006) {
      return calculateTree37(features, 735);
    } else {
      return calculateTree37(features, 736);
    }
  }
  if (node === 1475) return 0.09282847468767322;
  if (node === 1476) return 0.31351548754809155;
  if (node === 737) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree37(features, 1475);
    } else if (val <= 474.50000000000006) {
      return calculateTree37(features, 1475);
    } else {
      return calculateTree37(features, 1476);
    }
  }
  if (node === 1477) return -0.0027018067270138937;
  if (node === 1478) return 0.08575476728611794;
  if (node === 738) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree37(features, 1477);
    } else if (val <= 954.5000000000001) {
      return calculateTree37(features, 1477);
    } else {
      return calculateTree37(features, 1478);
    }
  }
  if (node === 368) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree37(features, 737);
    } else if (val <= 478.50000000000006) {
      return calculateTree37(features, 737);
    } else {
      return calculateTree37(features, 738);
    }
  }
  if (node === 183) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree37(features, 367);
    } else if (val <= 447.00000000000006) {
      return calculateTree37(features, 367);
    } else {
      return calculateTree37(features, 368);
    }
  }
  if (node === 1479) return -0.22013107843577753;
  if (node === 1480) return 0.00048030902814532765;
  if (node === 739) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree37(features, 1479);
    } else if (val <= 821.0000000000001) {
      return calculateTree37(features, 1479);
    } else {
      return calculateTree37(features, 1480);
    }
  }
  if (node === 740) return 0.20530363567977908;
  if (node === 369) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree37(features, 739);
    } else if (val <= 427.50000000000006) {
      return calculateTree37(features, 739);
    } else {
      return calculateTree37(features, 740);
    }
  }
  if (node === 741) return -0.18380900634086897;
  if (node === 1485) return 0.041249722264150836;
  if (node === 1486) return -0.4073988888651922;
  if (node === 742) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree37(features, 1485);
    } else if (val <= 906.0000000000001) {
      return calculateTree37(features, 1485);
    } else {
      return calculateTree37(features, 1486);
    }
  }
  if (node === 370) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree37(features, 741);
    } else if (val <= 897.5000000000001) {
      return calculateTree37(features, 741);
    } else {
      return calculateTree37(features, 742);
    }
  }
  if (node === 184) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree37(features, 369);
    } else if (val <= 475.50000000000006) {
      return calculateTree37(features, 369);
    } else {
      return calculateTree37(features, 370);
    }
  }
  if (node === 91) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree37(features, 183);
    } else if (val <= 816.5000000000001) {
      return calculateTree37(features, 183);
    } else {
      return calculateTree37(features, 184);
    }
  }
  if (node === 185) return 0.13572409216155243;
  if (node === 186) return -0.054809271928262474;
  if (node === 92) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree37(features, 185);
    } else if (val <= 19.500000000000004) {
      return calculateTree37(features, 185);
    } else {
      return calculateTree37(features, 186);
    }
  }
  if (node === 45) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree37(features, 91);
    } else if (val <= 934.0000000000001) {
      return calculateTree37(features, 91);
    } else {
      return calculateTree37(features, 92);
    }
  }
  if (node === 93) return -0.10950138862593842;
  if (node === 94) return 0.035348027490500644;
  if (node === 46) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree37(features, 93);
    } else if (val <= 973.5000000000001) {
      return calculateTree37(features, 93);
    } else {
      return calculateTree37(features, 94);
    }
  }
  if (node === 22) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree37(features, 45);
    } else if (val <= 948.0000000000001) {
      return calculateTree37(features, 45);
    } else {
      return calculateTree37(features, 46);
    }
  }
  if (node === 10) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree37(features, 21);
    } else if (val <= 3.5000000000000004) {
      return calculateTree37(features, 21);
    } else {
      return calculateTree37(features, 22);
    }
  }
  if (node === 4) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree37(features, 9);
    } else if (val <= 33.50000000000001) {
      return calculateTree37(features, 9);
    } else {
      return calculateTree37(features, 10);
    }
  }
  if (node === 1) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree37(features, 3);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree37(features, 3);
    } else {
      return calculateTree37(features, 4);
    }
  }
  if (node === 2) return -0.013877794983456338;
  if (node === 0) {
    const val = features['is_preference_kids'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree37(features, 1);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree37(features, 1);
    } else {
      return calculateTree37(features, 2);
    }
  }
}
function calculateTree38(features, node = 0) {
  if (node === 3) return -0.006193393935634404;
  if (node === 1279) return 0.28539665983986173;
  if (node === 1280) return -0.04088159583628912;
  if (node === 639) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree38(features, 1279);
    } else if (val <= 225.50000000000003) {
      return calculateTree38(features, 1279);
    } else {
      return calculateTree38(features, 1280);
    }
  }
  if (node === 1281) return 0.804268481743271;
  if (node === 1282) return -0.348609735092662;
  if (node === 640) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree38(features, 1281);
    } else if (val <= 409.50000000000006) {
      return calculateTree38(features, 1281);
    } else {
      return calculateTree38(features, 1282);
    }
  }
  if (node === 319) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree38(features, 639);
    } else if (val <= 203.50000000000003) {
      return calculateTree38(features, 639);
    } else {
      return calculateTree38(features, 640);
    }
  }
  if (node === 1283) return -0.13994064809826703;
  if (node === 1284) return 0.4522464055863782;
  if (node === 641) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree38(features, 1283);
    } else if (val <= 240.00000000000003) {
      return calculateTree38(features, 1283);
    } else {
      return calculateTree38(features, 1284);
    }
  }
  if (node === 1285) return -0.37595152564727913;
  if (node === 1286) return -0.15071676333101203;
  if (node === 642) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree38(features, 1285);
    } else if (val <= 325.00000000000006) {
      return calculateTree38(features, 1285);
    } else {
      return calculateTree38(features, 1286);
    }
  }
  if (node === 320) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree38(features, 641);
    } else if (val <= 253.00000000000003) {
      return calculateTree38(features, 641);
    } else {
      return calculateTree38(features, 642);
    }
  }
  if (node === 159) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree38(features, 319);
    } else if (val <= 212.50000000000003) {
      return calculateTree38(features, 319);
    } else {
      return calculateTree38(features, 320);
    }
  }
  if (node === 643) return 0.08061361716167291;
  if (node === 644) return -0.38989687416623314;
  if (node === 321) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree38(features, 643);
    } else if (val <= 422.00000000000006) {
      return calculateTree38(features, 643);
    } else {
      return calculateTree38(features, 644);
    }
  }
  if (node === 322) return 0.2787596116948467;
  if (node === 160) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree38(features, 321);
    } else if (val <= 693.0000000000001) {
      return calculateTree38(features, 321);
    } else {
      return calculateTree38(features, 322);
    }
  }
  if (node === 79) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree38(features, 159);
    } else if (val <= 525.0000000000001) {
      return calculateTree38(features, 159);
    } else {
      return calculateTree38(features, 160);
    }
  }
  if (node === 161) return -0.3735788448661296;
  if (node === 325) return 0.319386495848107;
  if (node === 326) return -0.1672925156211765;
  if (node === 162) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree38(features, 325);
    } else if (val <= 464.50000000000006) {
      return calculateTree38(features, 325);
    } else {
      return calculateTree38(features, 326);
    }
  }
  if (node === 80) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree38(features, 161);
    } else if (val <= 300.50000000000006) {
      return calculateTree38(features, 161);
    } else {
      return calculateTree38(features, 162);
    }
  }
  if (node === 39) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree38(features, 79);
    } else if (val <= 474.50000000000006) {
      return calculateTree38(features, 79);
    } else {
      return calculateTree38(features, 80);
    }
  }
  if (node === 40) return -0.4424017867658722;
  if (node === 19) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree38(features, 39);
    } else if (val <= 733.5000000000001) {
      return calculateTree38(features, 39);
    } else {
      return calculateTree38(features, 40);
    }
  }
  if (node === 20) return 0.05810539060248726;
  if (node === 9) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree38(features, 19);
    } else if (val <= 846.5000000000001) {
      return calculateTree38(features, 19);
    } else {
      return calculateTree38(features, 20);
    }
  }
  if (node === 43) return -0.39030946446693615;
  if (node === 179) return 0.05551172678049223;
  if (node === 180) return -0.236849957388197;
  if (node === 89) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree38(features, 179);
    } else if (val <= 36.50000000000001) {
      return calculateTree38(features, 179);
    } else {
      return calculateTree38(features, 180);
    }
  }
  if (node === 90) return 0.09909529777661767;
  if (node === 44) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree38(features, 89);
    } else if (val <= 123.50000000000001) {
      return calculateTree38(features, 89);
    } else {
      return calculateTree38(features, 90);
    }
  }
  if (node === 21) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree38(features, 43);
    } else if (val <= 225.50000000000003) {
      return calculateTree38(features, 43);
    } else {
      return calculateTree38(features, 44);
    }
  }
  if (node === 45) return -0.3912753631226869;
  if (node === 187) return -0.42364431549605536;
  if (node === 188) return 0.2512686759787832;
  if (node === 93) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree38(features, 187);
    } else if (val <= 253.00000000000003) {
      return calculateTree38(features, 187);
    } else {
      return calculateTree38(features, 188);
    }
  }
  if (node === 759) return -0.35842292481853083;
  if (node === 760) return 0.7533351084201086;
  if (node === 379) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree38(features, 759);
    } else if (val <= 236.00000000000003) {
      return calculateTree38(features, 759);
    } else {
      return calculateTree38(features, 760);
    }
  }
  if (node === 380) return -0.3486008947639541;
  if (node === 189) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree38(features, 379);
    } else if (val <= 284.50000000000006) {
      return calculateTree38(features, 379);
    } else {
      return calculateTree38(features, 380);
    }
  }
  if (node === 1527) return 0.2110238302341396;
  if (node === 1528) return 0.0013346520496634311;
  if (node === 763) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree38(features, 1527);
    } else if (val <= 229.50000000000003) {
      return calculateTree38(features, 1527);
    } else {
      return calculateTree38(features, 1528);
    }
  }
  if (node === 764) return -0.3744504153616503;
  if (node === 381) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree38(features, 763);
    } else if (val <= 467.50000000000006) {
      return calculateTree38(features, 763);
    } else {
      return calculateTree38(features, 764);
    }
  }
  if (node === 382) return 0.1918997732177052;
  if (node === 190) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree38(features, 381);
    } else if (val <= 476.00000000000006) {
      return calculateTree38(features, 381);
    } else {
      return calculateTree38(features, 382);
    }
  }
  if (node === 94) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree38(features, 189);
    } else if (val <= 522.5000000000001) {
      return calculateTree38(features, 189);
    } else {
      return calculateTree38(features, 190);
    }
  }
  if (node === 46) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree38(features, 93);
    } else if (val <= 427.50000000000006) {
      return calculateTree38(features, 93);
    } else {
      return calculateTree38(features, 94);
    }
  }
  if (node === 22) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree38(features, 45);
    } else if (val <= 417.00000000000006) {
      return calculateTree38(features, 45);
    } else {
      return calculateTree38(features, 46);
    }
  }
  if (node === 10) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree38(features, 21);
    } else if (val <= 338.50000000000006) {
      return calculateTree38(features, 21);
    } else {
      return calculateTree38(features, 22);
    }
  }
  if (node === 4) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree38(features, 9);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree38(features, 9);
    } else {
      return calculateTree38(features, 10);
    }
  }
  if (node === 1) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree38(features, 3);
    } else if (val <= 210.00000000000003) {
      return calculateTree38(features, 3);
    } else {
      return calculateTree38(features, 4);
    }
  }
  if (node === 2) return -0.004944926935372012;
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree38(features, 1);
    } else if (val <= 500.50000000000006) {
      return calculateTree38(features, 1);
    } else {
      return calculateTree38(features, 2);
    }
  }
}
function calculateTree39(features, node = 0) {
  if (node === 1) return -0.0040685115256415124;
  if (node === 5) return -0.02267018640658581;
  if (node === 13) return 0.08112970007773673;
  if (node === 1919) return -0.3974515282619479;
  if (node === 1920) return 0.052564424586827536;
  if (node === 959) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree39(features, 1919);
    } else if (val <= 171.00000000000003) {
      return calculateTree39(features, 1919);
    } else {
      return calculateTree39(features, 1920);
    }
  }
  if (node === 1921) return -0.14061215778961747;
  if (node === 1922) return 0.0026011654176351716;
  if (node === 960) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree39(features, 1921);
    } else if (val <= 222.50000000000003) {
      return calculateTree39(features, 1921);
    } else {
      return calculateTree39(features, 1922);
    }
  }
  if (node === 479) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree39(features, 959);
    } else if (val <= 166.50000000000003) {
      return calculateTree39(features, 959);
    } else {
      return calculateTree39(features, 960);
    }
  }
  if (node === 1923) return 0.2617989896094269;
  if (node === 1924) return 0.08145327018772988;
  if (node === 961) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree39(features, 1923);
    } else if (val <= 750.0000000000001) {
      return calculateTree39(features, 1923);
    } else {
      return calculateTree39(features, 1924);
    }
  }
  if (node === 1925) return -0.3341244974072919;
  if (node === 1926) return 0.07301208184375266;
  if (node === 962) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree39(features, 1925);
    } else if (val <= 325.00000000000006) {
      return calculateTree39(features, 1925);
    } else {
      return calculateTree39(features, 1926);
    }
  }
  if (node === 480) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree39(features, 961);
    } else if (val <= 312.50000000000006) {
      return calculateTree39(features, 961);
    } else {
      return calculateTree39(features, 962);
    }
  }
  if (node === 239) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree39(features, 479);
    } else if (val <= 294.50000000000006) {
      return calculateTree39(features, 479);
    } else {
      return calculateTree39(features, 480);
    }
  }
  if (node === 240) return -0.33224659332431355;
  if (node === 119) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree39(features, 239);
    } else if (val <= 975.0000000000001) {
      return calculateTree39(features, 239);
    } else {
      return calculateTree39(features, 240);
    }
  }
  if (node === 1935) return -0.34628429986520126;
  if (node === 1936) return 0.016562658497619648;
  if (node === 967) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree39(features, 1935);
    } else if (val <= 3.5000000000000004) {
      return calculateTree39(features, 1935);
    } else {
      return calculateTree39(features, 1936);
    }
  }
  if (node === 968) return -0.3817164266877619;
  if (node === 483) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree39(features, 967);
    } else if (val <= 294.50000000000006) {
      return calculateTree39(features, 967);
    } else {
      return calculateTree39(features, 968);
    }
  }
  if (node === 969) return -0.21437386171560527;
  if (node === 970) return 0.08331529389377053;
  if (node === 484) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree39(features, 969);
    } else if (val <= 225.50000000000003) {
      return calculateTree39(features, 969);
    } else {
      return calculateTree39(features, 970);
    }
  }
  if (node === 241) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree39(features, 483);
    } else if (val <= 311.50000000000006) {
      return calculateTree39(features, 483);
    } else {
      return calculateTree39(features, 484);
    }
  }
  if (node === 485) return -0.3671191826605235;
  if (node === 973) return 0.4611831663826037;
  if (node === 974) return -0.4175884889994835;
  if (node === 486) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree39(features, 973);
    } else if (val <= 758.0000000000001) {
      return calculateTree39(features, 973);
    } else {
      return calculateTree39(features, 974);
    }
  }
  if (node === 242) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree39(features, 485);
    } else if (val <= 619.5000000000001) {
      return calculateTree39(features, 485);
    } else {
      return calculateTree39(features, 486);
    }
  }
  if (node === 120) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree39(features, 241);
    } else if (val <= 387.50000000000006) {
      return calculateTree39(features, 241);
    } else {
      return calculateTree39(features, 242);
    }
  }
  if (node === 59) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree39(features, 119);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree39(features, 119);
    } else {
      return calculateTree39(features, 120);
    }
  }
  if (node === 243) return -0.46100929021742904;
  if (node === 489) return 0.404787671678703;
  if (node === 490) return -0.42278184027823723;
  if (node === 244) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree39(features, 489);
    } else if (val <= 734.0000000000001) {
      return calculateTree39(features, 489);
    } else {
      return calculateTree39(features, 490);
    }
  }
  if (node === 121) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree39(features, 243);
    } else if (val <= 682.5000000000001) {
      return calculateTree39(features, 243);
    } else {
      return calculateTree39(features, 244);
    }
  }
  if (node === 491) return 0.11527794309132962;
  if (node === 492) return 0.3607491095825402;
  if (node === 245) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree39(features, 491);
    } else if (val <= 607.5000000000001) {
      return calculateTree39(features, 491);
    } else {
      return calculateTree39(features, 492);
    }
  }
  if (node === 493) return -0.48857953736501386;
  if (node === 494) return 0.1313597285414679;
  if (node === 246) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree39(features, 493);
    } else if (val <= 800.5000000000001) {
      return calculateTree39(features, 493);
    } else {
      return calculateTree39(features, 494);
    }
  }
  if (node === 122) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree39(features, 245);
    } else if (val <= 682.5000000000001) {
      return calculateTree39(features, 245);
    } else {
      return calculateTree39(features, 246);
    }
  }
  if (node === 60) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree39(features, 121);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree39(features, 121);
    } else {
      return calculateTree39(features, 122);
    }
  }
  if (node === 29) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree39(features, 59);
    } else if (val <= 417.00000000000006) {
      return calculateTree39(features, 59);
    } else {
      return calculateTree39(features, 60);
    }
  }
  if (node === 247) return -0.33106631168766437;
  if (node === 248) return 0.11506089573098036;
  if (node === 123) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree39(features, 247);
    } else if (val <= 672.0000000000001) {
      return calculateTree39(features, 247);
    } else {
      return calculateTree39(features, 248);
    }
  }
  if (node === 249) return 0.26844162679779776;
  if (node === 250) return -0.3779324975925768;
  if (node === 124) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree39(features, 249);
    } else if (val <= 393.00000000000006) {
      return calculateTree39(features, 249);
    } else {
      return calculateTree39(features, 250);
    }
  }
  if (node === 61) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree39(features, 123);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree39(features, 123);
    } else {
      return calculateTree39(features, 124);
    }
  }
  if (node === 62) return -0.002710591602392366;
  if (node === 30) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree39(features, 61);
    } else if (val <= 455.50000000000006) {
      return calculateTree39(features, 61);
    } else {
      return calculateTree39(features, 62);
    }
  }
  if (node === 14) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree39(features, 29);
    } else if (val <= 427.50000000000006) {
      return calculateTree39(features, 29);
    } else {
      return calculateTree39(features, 30);
    }
  }
  if (node === 6) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree39(features, 13);
    } else if (val <= 132.50000000000003) {
      return calculateTree39(features, 13);
    } else {
      return calculateTree39(features, 14);
    }
  }
  if (node === 2) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree39(features, 5);
    } else if (val <= 125.50000000000001) {
      return calculateTree39(features, 5);
    } else {
      return calculateTree39(features, 6);
    }
  }
  if (node === 0) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree39(features, 1);
    } else if (val <= 18.500000000000004) {
      return calculateTree39(features, 1);
    } else {
      return calculateTree39(features, 2);
    }
  }
}
function calculateTree40(features, node = 0) {
  if (node === 15) return 0.004471225536214719;
  if (node === 33) return -0.007058048323859655;
  if (node === 34) return 0.006446946580059672;
  if (node === 16) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree40(features, 33);
    } else if (val <= 1.5000000000000002) {
      return calculateTree40(features, 33);
    } else {
      return calculateTree40(features, 34);
    }
  }
  if (node === 7) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree40(features, 15);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree40(features, 15);
    } else {
      return calculateTree40(features, 16);
    }
  }
  if (node === 143) return 0.006147845914735742;
  if (node === 144) return -0.20654919228534765;
  if (node === 71) {
    const val = features['is_preference_grocery'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree40(features, 143);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree40(features, 143);
    } else {
      return calculateTree40(features, 144);
    }
  }
  if (node === 72) return 0.03125654665884005;
  if (node === 35) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree40(features, 71);
    } else if (val <= 13.500000000000002) {
      return calculateTree40(features, 71);
    } else {
      return calculateTree40(features, 72);
    }
  }
  if (node === 36) return -0.013775370975518413;
  if (node === 17) {
    const val = features['is_preference_kitchen'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree40(features, 35);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree40(features, 35);
    } else {
      return calculateTree40(features, 36);
    }
  }
  if (node === 18) return -0.01327427366206546;
  if (node === 8) {
    const val = features['is_preference_women_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree40(features, 17);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree40(features, 17);
    } else {
      return calculateTree40(features, 18);
    }
  }
  if (node === 3) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree40(features, 7);
    } else if (val <= 4.500000000000001) {
      return calculateTree40(features, 7);
    } else {
      return calculateTree40(features, 8);
    }
  }
  if (node === 4) return -0.008291153506236052;
  if (node === 1) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree40(features, 3);
    } else if (val <= 38.50000000000001) {
      return calculateTree40(features, 3);
    } else {
      return calculateTree40(features, 4);
    }
  }
  if (node === 47) return 0.059062056339586555;
  if (node === 195) return 0.19702678752197628;
  if (node === 393) return -0.17317156527432012;
  if (node === 394) return 0.11871124056830129;
  if (node === 196) {
    const val = features['is_preference_personal_care'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree40(features, 393);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree40(features, 393);
    } else {
      return calculateTree40(features, 394);
    }
  }
  if (node === 97) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree40(features, 195);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree40(features, 195);
    } else {
      return calculateTree40(features, 196);
    }
  }
  if (node === 98) return -0.12628971571522568;
  if (node === 48) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree40(features, 97);
    } else if (val <= 45.50000000000001) {
      return calculateTree40(features, 97);
    } else {
      return calculateTree40(features, 98);
    }
  }
  if (node === 23) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree40(features, 47);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree40(features, 47);
    } else {
      return calculateTree40(features, 48);
    }
  }
  if (node === 49) return -0.05661250241613018;
  if (node === 101) return -0.10591055856419287;
  if (node === 102) return 0.23247149225694608;
  if (node === 50) {
    const val = features['is_preference_household'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree40(features, 101);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree40(features, 101);
    } else {
      return calculateTree40(features, 102);
    }
  }
  if (node === 24) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree40(features, 49);
    } else if (val <= 54.50000000000001) {
      return calculateTree40(features, 49);
    } else {
      return calculateTree40(features, 50);
    }
  }
  if (node === 11) {
    const val = features['is_preference_sports'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree40(features, 23);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree40(features, 23);
    } else {
      return calculateTree40(features, 24);
    }
  }
  if (node === 12) return -0.05838386981643733;
  if (node === 5) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree40(features, 11);
    } else if (val <= 540.0000000000001) {
      return calculateTree40(features, 11);
    } else {
      return calculateTree40(features, 12);
    }
  }
  if (node === 55) return 0.08049257697006486;
  if (node === 56) return 0.3053746885672323;
  if (node === 27) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree40(features, 55);
    } else if (val <= 45.50000000000001) {
      return calculateTree40(features, 55);
    } else {
      return calculateTree40(features, 56);
    }
  }
  if (node === 57) return -0.25734173948288164;
  if (node === 58) return 0.023901646509938847;
  if (node === 28) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree40(features, 57);
    } else if (val <= 749.0000000000001) {
      return calculateTree40(features, 57);
    } else {
      return calculateTree40(features, 58);
    }
  }
  if (node === 13) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree40(features, 27);
    } else if (val <= 51.50000000000001) {
      return calculateTree40(features, 27);
    } else {
      return calculateTree40(features, 28);
    }
  }
  if (node === 59) return -0.11038110612061883;
  if (node === 121) return -0.020507939973909536;
  if (node === 245) return -0.1251226847247586;
  if (node === 246) return 0.290158264392514;
  if (node === 122) {
    const val = features['is_preference_household'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree40(features, 245);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree40(features, 245);
    } else {
      return calculateTree40(features, 246);
    }
  }
  if (node === 60) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree40(features, 121);
    } else if (val <= 52.50000000000001) {
      return calculateTree40(features, 121);
    } else {
      return calculateTree40(features, 122);
    }
  }
  if (node === 29) {
    const val = features['is_preference_kitchen'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree40(features, 59);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree40(features, 59);
    } else {
      return calculateTree40(features, 60);
    }
  }
  if (node === 247) return 0.05612470324632561;
  if (node === 497) return -0.26535016750686435;
  if (node === 498) return 0.033102255197113566;
  if (node === 248) {
    const val = features['is_preference_pets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree40(features, 497);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree40(features, 497);
    } else {
      return calculateTree40(features, 498);
    }
  }
  if (node === 123) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree40(features, 247);
    } else if (val <= 960.0000000000001) {
      return calculateTree40(features, 247);
    } else {
      return calculateTree40(features, 248);
    }
  }
  if (node === 249) return -0.027712731567352576;
  if (node === 250) return 0.3874201882002235;
  if (node === 124) {
    const val = features['is_preference_household'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree40(features, 249);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree40(features, 249);
    } else {
      return calculateTree40(features, 250);
    }
  }
  if (node === 61) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree40(features, 123);
    } else if (val <= 59.50000000000001) {
      return calculateTree40(features, 123);
    } else {
      return calculateTree40(features, 124);
    }
  }
  if (node === 62) return -0.22158344282272338;
  if (node === 30) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree40(features, 61);
    } else if (val <= 62.50000000000001) {
      return calculateTree40(features, 61);
    } else {
      return calculateTree40(features, 62);
    }
  }
  if (node === 14) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree40(features, 29);
    } else if (val <= 897.0000000000001) {
      return calculateTree40(features, 29);
    } else {
      return calculateTree40(features, 30);
    }
  }
  if (node === 6) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree40(features, 13);
    } else if (val <= 817.5000000000001) {
      return calculateTree40(features, 13);
    } else {
      return calculateTree40(features, 14);
    }
  }
  if (node === 2) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree40(features, 5);
    } else if (val <= 673.0000000000001) {
      return calculateTree40(features, 5);
    } else {
      return calculateTree40(features, 6);
    }
  }
  if (node === 0) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree40(features, 1);
    } else if (val <= 28.500000000000004) {
      return calculateTree40(features, 1);
    } else {
      return calculateTree40(features, 2);
    }
  }
}
function calculateTree41(features, node = 0) {
  if (node === 31) return -0.003866348764976532;
  if (node === 65) return -0.2863834370553677;
  if (node === 133) return 0.34330587613992924;
  if (node === 134) return 0.11108026282857376;
  if (node === 66) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree41(features, 133);
    } else if (val <= 312.50000000000006) {
      return calculateTree41(features, 133);
    } else {
      return calculateTree41(features, 134);
    }
  }
  if (node === 32) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree41(features, 65);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree41(features, 65);
    } else {
      return calculateTree41(features, 66);
    }
  }
  if (node === 15) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree41(features, 31);
    } else if (val <= 481.50000000000006) {
      return calculateTree41(features, 31);
    } else {
      return calculateTree41(features, 32);
    }
  }
  if (node === 16) return -0.03110882148838457;
  if (node === 7) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree41(features, 15);
    } else if (val <= 525.0000000000001) {
      return calculateTree41(features, 15);
    } else {
      return calculateTree41(features, 16);
    }
  }
  if (node === 8) return 0.05008905335345405;
  if (node === 3) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree41(features, 7);
    } else if (val <= 647.0000000000001) {
      return calculateTree41(features, 7);
    } else {
      return calculateTree41(features, 8);
    }
  }
  if (node === 19) return -0.27034349185411377;
  if (node === 335) return 0.4796922390118672;
  if (node === 336) return 0.12448634138429987;
  if (node === 167) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree41(features, 335);
    } else if (val <= 305.00000000000006) {
      return calculateTree41(features, 335);
    } else {
      return calculateTree41(features, 336);
    }
  }
  if (node === 337) return 0.21207757318810963;
  if (node === 677) return -0.37969800413439475;
  if (node === 678) return 0.12722721559087521;
  if (node === 338) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree41(features, 677);
    } else if (val <= 381.50000000000006) {
      return calculateTree41(features, 677);
    } else {
      return calculateTree41(features, 678);
    }
  }
  if (node === 168) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree41(features, 337);
    } else if (val <= 194.00000000000003) {
      return calculateTree41(features, 337);
    } else {
      return calculateTree41(features, 338);
    }
  }
  if (node === 83) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree41(features, 167);
    } else if (val <= 675.5000000000001) {
      return calculateTree41(features, 167);
    } else {
      return calculateTree41(features, 168);
    }
  }
  if (node === 84) return 0.4086604874107096;
  if (node === 41) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree41(features, 83);
    } else if (val <= 412.00000000000006) {
      return calculateTree41(features, 83);
    } else {
      return calculateTree41(features, 84);
    }
  }
  if (node === 343) return 0.0028152674912905293;
  if (node === 344) return -0.30591548810838376;
  if (node === 171) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree41(features, 343);
    } else if (val <= 447.00000000000006) {
      return calculateTree41(features, 343);
    } else {
      return calculateTree41(features, 344);
    }
  }
  if (node === 172) return 0.05581456370865908;
  if (node === 85) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree41(features, 171);
    } else if (val <= 729.5000000000001) {
      return calculateTree41(features, 171);
    } else {
      return calculateTree41(features, 172);
    }
  }
  if (node === 173) return 0.422663876883559;
  if (node === 349) return -0.35581823685142017;
  if (node === 350) return 0.11992921071355021;
  if (node === 174) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree41(features, 349);
    } else if (val <= 783.5000000000001) {
      return calculateTree41(features, 349);
    } else {
      return calculateTree41(features, 350);
    }
  }
  if (node === 86) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree41(features, 173);
    } else if (val <= 781.0000000000001) {
      return calculateTree41(features, 173);
    } else {
      return calculateTree41(features, 174);
    }
  }
  if (node === 42) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree41(features, 85);
    } else if (val <= 776.0000000000001) {
      return calculateTree41(features, 85);
    } else {
      return calculateTree41(features, 86);
    }
  }
  if (node === 20) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree41(features, 41);
    } else if (val <= 414.50000000000006) {
      return calculateTree41(features, 41);
    } else {
      return calculateTree41(features, 42);
    }
  }
  if (node === 9) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree41(features, 19);
    } else if (val <= 127.50000000000001) {
      return calculateTree41(features, 19);
    } else {
      return calculateTree41(features, 20);
    }
  }
  if (node === 21) return -0.34057357352937373;
  if (node === 183) return 0.09186783596257891;
  if (node === 369) return -0.29033702353967217;
  if (node === 370) return 0.017796904329262807;
  if (node === 184) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree41(features, 369);
    } else if (val <= 731.5000000000001) {
      return calculateTree41(features, 369);
    } else {
      return calculateTree41(features, 370);
    }
  }
  if (node === 91) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree41(features, 183);
    } else if (val <= 693.0000000000001) {
      return calculateTree41(features, 183);
    } else {
      return calculateTree41(features, 184);
    }
  }
  if (node === 185) return 0.8029796752806952;
  if (node === 186) return 0.19582898636406473;
  if (node === 92) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree41(features, 185);
    } else if (val <= 312.50000000000006) {
      return calculateTree41(features, 185);
    } else {
      return calculateTree41(features, 186);
    }
  }
  if (node === 45) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree41(features, 91);
    } else if (val <= 305.00000000000006) {
      return calculateTree41(features, 91);
    } else {
      return calculateTree41(features, 92);
    }
  }
  if (node === 93) return -0.38174882768575763;
  if (node === 759) return -0.275648689663853;
  if (node === 760) return 0.21261582853818103;
  if (node === 379) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree41(features, 759);
    } else if (val <= 969.5000000000001) {
      return calculateTree41(features, 759);
    } else {
      return calculateTree41(features, 760);
    }
  }
  if (node === 380) return 0.15328014005034604;
  if (node === 189) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree41(features, 379);
    } else if (val <= 715.5000000000001) {
      return calculateTree41(features, 379);
    } else {
      return calculateTree41(features, 380);
    }
  }
  if (node === 190) return -0.2711411908918441;
  if (node === 94) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree41(features, 189);
    } else if (val <= 733.5000000000001) {
      return calculateTree41(features, 189);
    } else {
      return calculateTree41(features, 190);
    }
  }
  if (node === 46) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree41(features, 93);
    } else if (val <= 476.00000000000006) {
      return calculateTree41(features, 93);
    } else {
      return calculateTree41(features, 94);
    }
  }
  if (node === 22) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree41(features, 45);
    } else if (val <= 381.50000000000006) {
      return calculateTree41(features, 45);
    } else {
      return calculateTree41(features, 46);
    }
  }
  if (node === 10) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree41(features, 21);
    } else if (val <= 677.5000000000001) {
      return calculateTree41(features, 21);
    } else {
      return calculateTree41(features, 22);
    }
  }
  if (node === 4) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree41(features, 9);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree41(features, 9);
    } else {
      return calculateTree41(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree41(features, 3);
    } else if (val <= 667.5000000000001) {
      return calculateTree41(features, 3);
    } else {
      return calculateTree41(features, 4);
    }
  }
  if (node === 5) return 0.0872647581188121;
  if (node === 6) return 0.002622607409836396;
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree41(features, 5);
    } else if (val <= 752.5000000000001) {
      return calculateTree41(features, 5);
    } else {
      return calculateTree41(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree41(features, 1);
    } else if (val <= 745.5000000000001) {
      return calculateTree41(features, 1);
    } else {
      return calculateTree41(features, 2);
    }
  }
}
function calculateTree42(features, node = 0) {
  if (node === 3) return 0.0015440345652021985;
  if (node === 19) return 0.43425747197977543;
  if (node === 20) return -0.05366474062952618;
  if (node === 9) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree42(features, 19);
    } else if (val <= 476.00000000000006) {
      return calculateTree42(features, 19);
    } else {
      return calculateTree42(features, 20);
    }
  }
  if (node === 21) return -0.3498790838293519;
  if (node === 45) return 0.0021329594675553332;
  if (node === 46) return 0.2685524913288406;
  if (node === 22) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree42(features, 45);
    } else if (val <= 25.500000000000004) {
      return calculateTree42(features, 45);
    } else {
      return calculateTree42(features, 46);
    }
  }
  if (node === 10) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree42(features, 21);
    } else if (val <= 476.00000000000006) {
      return calculateTree42(features, 21);
    } else {
      return calculateTree42(features, 22);
    }
  }
  if (node === 4) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree42(features, 9);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree42(features, 9);
    } else {
      return calculateTree42(features, 10);
    }
  }
  if (node === 1) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree42(features, 3);
    } else if (val <= 474.50000000000006) {
      return calculateTree42(features, 3);
    } else {
      return calculateTree42(features, 4);
    }
  }
  if (node === 23) return 0.054759836756961756;
  if (node === 199) return -0.06025395996383978;
  if (node === 401) return 0.14345390540485775;
  if (node === 402) return -0.08776878869812575;
  if (node === 200) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree42(features, 401);
    } else if (val <= 695.5000000000001) {
      return calculateTree42(features, 401);
    } else {
      return calculateTree42(features, 402);
    }
  }
  if (node === 99) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree42(features, 199);
    } else if (val <= 682.5000000000001) {
      return calculateTree42(features, 199);
    } else {
      return calculateTree42(features, 200);
    }
  }
  if (node === 201) return -0.37117664970532344;
  if (node === 202) return -0.05630882649223995;
  if (node === 100) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree42(features, 201);
    } else if (val <= 709.5000000000001) {
      return calculateTree42(features, 201);
    } else {
      return calculateTree42(features, 202);
    }
  }
  if (node === 49) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree42(features, 99);
    } else if (val <= 708.5000000000001) {
      return calculateTree42(features, 99);
    } else {
      return calculateTree42(features, 100);
    }
  }
  if (node === 407) return 0.10919807206024172;
  if (node === 817) return -0.16300401371414378;
  if (node === 818) return 0.011459908831190168;
  if (node === 408) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree42(features, 817);
    } else if (val <= 734.0000000000001) {
      return calculateTree42(features, 817);
    } else {
      return calculateTree42(features, 818);
    }
  }
  if (node === 203) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree42(features, 407);
    } else if (val <= 717.0000000000001) {
      return calculateTree42(features, 407);
    } else {
      return calculateTree42(features, 408);
    }
  }
  if (node === 409) return 0.2487679650945873;
  if (node === 821) return -0.3937186994209836;
  if (node === 822) return 0.08369344583187073;
  if (node === 410) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree42(features, 821);
    } else if (val <= 799.5000000000001) {
      return calculateTree42(features, 821);
    } else {
      return calculateTree42(features, 822);
    }
  }
  if (node === 204) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree42(features, 409);
    } else if (val <= 795.5000000000001) {
      return calculateTree42(features, 409);
    } else {
      return calculateTree42(features, 410);
    }
  }
  if (node === 101) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree42(features, 203);
    } else if (val <= 783.5000000000001) {
      return calculateTree42(features, 203);
    } else {
      return calculateTree42(features, 204);
    }
  }
  if (node === 205) return -0.07469304528160615;
  if (node === 206) return 0.02198529895938475;
  if (node === 102) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree42(features, 205);
    } else if (val <= 893.0000000000001) {
      return calculateTree42(features, 205);
    } else {
      return calculateTree42(features, 206);
    }
  }
  if (node === 50) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree42(features, 101);
    } else if (val <= 816.5000000000001) {
      return calculateTree42(features, 101);
    } else {
      return calculateTree42(features, 102);
    }
  }
  if (node === 24) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree42(features, 49);
    } else if (val <= 712.5000000000001) {
      return calculateTree42(features, 49);
    } else {
      return calculateTree42(features, 50);
    }
  }
  if (node === 11) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree42(features, 23);
    } else if (val <= 575.5000000000001) {
      return calculateTree42(features, 23);
    } else {
      return calculateTree42(features, 24);
    }
  }
  if (node === 12) return -0.29210689137102125;
  if (node === 5) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree42(features, 11);
    } else if (val <= 975.0000000000001) {
      return calculateTree42(features, 11);
    } else {
      return calculateTree42(features, 12);
    }
  }
  if (node === 27) return -0.01020589076339056;
  if (node === 28) return -0.1895121587993934;
  if (node === 13) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree42(features, 27);
    } else if (val <= 504.50000000000006) {
      return calculateTree42(features, 27);
    } else {
      return calculateTree42(features, 28);
    }
  }
  if (node === 119) return 0.0493944605964973;
  if (node === 120) return -0.3820454463998786;
  if (node === 59) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree42(features, 119);
    } else if (val <= 606.5000000000001) {
      return calculateTree42(features, 119);
    } else {
      return calculateTree42(features, 120);
    }
  }
  if (node === 60) return 0.100374234897415;
  if (node === 29) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree42(features, 59);
    } else if (val <= 607.5000000000001) {
      return calculateTree42(features, 59);
    } else {
      return calculateTree42(features, 60);
    }
  }
  if (node === 123) return -0.01008486606432968;
  if (node === 249) return -0.3620570473717411;
  if (node === 501) return 0.016338756715796352;
  if (node === 1005) return -0.38302044413653485;
  if (node === 1006) return -0.08102354234415803;
  if (node === 502) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree42(features, 1005);
    } else if (val <= 816.5000000000001) {
      return calculateTree42(features, 1005);
    } else {
      return calculateTree42(features, 1006);
    }
  }
  if (node === 250) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree42(features, 501);
    } else if (val <= 811.5000000000001) {
      return calculateTree42(features, 501);
    } else {
      return calculateTree42(features, 502);
    }
  }
  if (node === 124) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree42(features, 249);
    } else if (val <= 799.5000000000001) {
      return calculateTree42(features, 249);
    } else {
      return calculateTree42(features, 250);
    }
  }
  if (node === 61) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree42(features, 123);
    } else if (val <= 783.5000000000001) {
      return calculateTree42(features, 123);
    } else {
      return calculateTree42(features, 124);
    }
  }
  if (node === 62) return 0.011357957414630077;
  if (node === 30) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree42(features, 61);
    } else if (val <= 821.0000000000001) {
      return calculateTree42(features, 61);
    } else {
      return calculateTree42(features, 62);
    }
  }
  if (node === 14) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree42(features, 29);
    } else if (val <= 619.5000000000001) {
      return calculateTree42(features, 29);
    } else {
      return calculateTree42(features, 30);
    }
  }
  if (node === 6) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree42(features, 13);
    } else if (val <= 575.5000000000001) {
      return calculateTree42(features, 13);
    } else {
      return calculateTree42(features, 14);
    }
  }
  if (node === 2) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree42(features, 5);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree42(features, 5);
    } else {
      return calculateTree42(features, 6);
    }
  }
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree42(features, 1);
    } else if (val <= 478.50000000000006) {
      return calculateTree42(features, 1);
    } else {
      return calculateTree42(features, 2);
    }
  }
}
function calculateTree43(features, node = 0) {
  if (node === 7) return -0.0026619494272632346;
  if (node === 8) return 0.05110257938991104;
  if (node === 3) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree43(features, 7);
    } else if (val <= 657.5000000000001) {
      return calculateTree43(features, 7);
    } else {
      return calculateTree43(features, 8);
    }
  }
  if (node === 79) return 0.4761474500770038;
  if (node === 80) return -0.40720984307265784;
  if (node === 39) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree43(features, 79);
    } else if (val <= 69.50000000000001) {
      return calculateTree43(features, 79);
    } else {
      return calculateTree43(features, 80);
    }
  }
  if (node === 40) return -0.33183305625893234;
  if (node === 19) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree43(features, 39);
    } else if (val <= 685.0000000000001) {
      return calculateTree43(features, 39);
    } else {
      return calculateTree43(features, 40);
    }
  }
  if (node === 83) return 0.07330539235504437;
  if (node === 84) return 0.27599425237285446;
  if (node === 41) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree43(features, 83);
    } else if (val <= 412.00000000000006) {
      return calculateTree43(features, 83);
    } else {
      return calculateTree43(features, 84);
    }
  }
  if (node === 687) return 0.13776069688696113;
  if (node === 1377) return -0.0492365760941235;
  if (node === 1378) return -0.3575983880745033;
  if (node === 688) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree43(features, 1377);
    } else if (val <= 447.00000000000006) {
      return calculateTree43(features, 1377);
    } else {
      return calculateTree43(features, 1378);
    }
  }
  if (node === 343) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree43(features, 687);
    } else if (val <= 675.5000000000001) {
      return calculateTree43(features, 687);
    } else {
      return calculateTree43(features, 688);
    }
  }
  if (node === 344) return -0.3427334279048961;
  if (node === 171) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree43(features, 343);
    } else if (val <= 504.50000000000006) {
      return calculateTree43(features, 343);
    } else {
      return calculateTree43(features, 344);
    }
  }
  if (node === 1383) return 0.05421209697387405;
  if (node === 1384) return -0.3490348204252193;
  if (node === 691) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree43(features, 1383);
    } else if (val <= 498.50000000000006) {
      return calculateTree43(features, 1383);
    } else {
      return calculateTree43(features, 1384);
    }
  }
  if (node === 692) return 0.4477833105425989;
  if (node === 345) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree43(features, 691);
    } else if (val <= 643.5000000000001) {
      return calculateTree43(features, 691);
    } else {
      return calculateTree43(features, 692);
    }
  }
  if (node === 346) return -0.2474178510726021;
  if (node === 172) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree43(features, 345);
    } else if (val <= 656.5000000000001) {
      return calculateTree43(features, 345);
    } else {
      return calculateTree43(features, 346);
    }
  }
  if (node === 85) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree43(features, 171);
    } else if (val <= 723.5000000000001) {
      return calculateTree43(features, 171);
    } else {
      return calculateTree43(features, 172);
    }
  }
  if (node === 173) return 0.0677017626158924;
  if (node === 174) return -0.3540422063577945;
  if (node === 86) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree43(features, 173);
    } else if (val <= 969.5000000000001) {
      return calculateTree43(features, 173);
    } else {
      return calculateTree43(features, 174);
    }
  }
  if (node === 42) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree43(features, 85);
    } else if (val <= 776.0000000000001) {
      return calculateTree43(features, 85);
    } else {
      return calculateTree43(features, 86);
    }
  }
  if (node === 20) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree43(features, 41);
    } else if (val <= 414.50000000000006) {
      return calculateTree43(features, 41);
    } else {
      return calculateTree43(features, 42);
    }
  }
  if (node === 9) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree43(features, 19);
    } else if (val <= 127.50000000000001) {
      return calculateTree43(features, 19);
    } else {
      return calculateTree43(features, 20);
    }
  }
  if (node === 21) return -0.32687221083367485;
  if (node === 183) return 0.05395296900673757;
  if (node === 369) return -0.29726635218820474;
  if (node === 370) return -0.0022771495930090035;
  if (node === 184) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree43(features, 369);
    } else if (val <= 731.5000000000001) {
      return calculateTree43(features, 369);
    } else {
      return calculateTree43(features, 370);
    }
  }
  if (node === 91) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree43(features, 183);
    } else if (val <= 693.0000000000001) {
      return calculateTree43(features, 183);
    } else {
      return calculateTree43(features, 184);
    }
  }
  if (node === 92) return 0.17358993233698;
  if (node === 45) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree43(features, 91);
    } else if (val <= 194.00000000000003) {
      return calculateTree43(features, 91);
    } else {
      return calculateTree43(features, 92);
    }
  }
  if (node === 93) return -0.35296365250094036;
  if (node === 189) return 0.0864827155024734;
  if (node === 1527) return -0.2863203209972101;
  if (node === 1528) return 0.1075159019432303;
  if (node === 763) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree43(features, 1527);
    } else if (val <= 857.5000000000001) {
      return calculateTree43(features, 1527);
    } else {
      return calculateTree43(features, 1528);
    }
  }
  if (node === 764) return -0.3425560441041715;
  if (node === 381) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree43(features, 763);
    } else if (val <= 900.5000000000001) {
      return calculateTree43(features, 763);
    } else {
      return calculateTree43(features, 764);
    }
  }
  if (node === 382) return 0.1309455510571082;
  if (node === 190) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree43(features, 381);
    } else if (val <= 969.5000000000001) {
      return calculateTree43(features, 381);
    } else {
      return calculateTree43(features, 382);
    }
  }
  if (node === 94) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree43(features, 189);
    } else if (val <= 705.5000000000001) {
      return calculateTree43(features, 189);
    } else {
      return calculateTree43(features, 190);
    }
  }
  if (node === 46) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree43(features, 93);
    } else if (val <= 476.00000000000006) {
      return calculateTree43(features, 93);
    } else {
      return calculateTree43(features, 94);
    }
  }
  if (node === 22) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree43(features, 45);
    } else if (val <= 381.50000000000006) {
      return calculateTree43(features, 45);
    } else {
      return calculateTree43(features, 46);
    }
  }
  if (node === 10) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree43(features, 21);
    } else if (val <= 677.5000000000001) {
      return calculateTree43(features, 21);
    } else {
      return calculateTree43(features, 22);
    }
  }
  if (node === 4) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree43(features, 9);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree43(features, 9);
    } else {
      return calculateTree43(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree43(features, 3);
    } else if (val <= 667.5000000000001) {
      return calculateTree43(features, 3);
    } else {
      return calculateTree43(features, 4);
    }
  }
  if (node === 11) return -0.025399807808371425;
  if (node === 25) return -0.4255962620672116;
  if (node === 26) return 0.16422594724891437;
  if (node === 12) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree43(features, 25);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree43(features, 25);
    } else {
      return calculateTree43(features, 26);
    }
  }
  if (node === 5) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree43(features, 11);
    } else if (val <= 781.0000000000001) {
      return calculateTree43(features, 11);
    } else {
      return calculateTree43(features, 12);
    }
  }
  if (node === 6) return 0.0019829852589646883;
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree43(features, 5);
    } else if (val <= 752.5000000000001) {
      return calculateTree43(features, 5);
    } else {
      return calculateTree43(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree43(features, 1);
    } else if (val <= 745.5000000000001) {
      return calculateTree43(features, 1);
    } else {
      return calculateTree43(features, 2);
    }
  }
}
function calculateTree44(features, node = 0) {
  if (node === 31) return 0.019905101670042894;
  if (node === 131) return -0.212508995668679;
  if (node === 531) return 0.10716208194981018;
  if (node === 532) return -0.2707538510084734;
  if (node === 265) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree44(features, 531);
    } else if (val <= 253.50000000000003) {
      return calculateTree44(features, 531);
    } else {
      return calculateTree44(features, 532);
    }
  }
  if (node === 266) return -0.3087122096193643;
  if (node === 132) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree44(features, 265);
    } else if (val <= 717.0000000000001) {
      return calculateTree44(features, 265);
    } else {
      return calculateTree44(features, 266);
    }
  }
  if (node === 65) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree44(features, 131);
    } else if (val <= 214.00000000000003) {
      return calculateTree44(features, 131);
    } else {
      return calculateTree44(features, 132);
    }
  }
  if (node === 66) return 0.0512101078026994;
  if (node === 32) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree44(features, 65);
    } else if (val <= 829.0000000000001) {
      return calculateTree44(features, 65);
    } else {
      return calculateTree44(features, 66);
    }
  }
  if (node === 15) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree44(features, 31);
    } else if (val <= 166.50000000000003) {
      return calculateTree44(features, 31);
    } else {
      return calculateTree44(features, 32);
    }
  }
  if (node === 135) return -0.3450536059892347;
  if (node === 136) return 0.21663667301923573;
  if (node === 67) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree44(features, 135);
    } else if (val <= 12.000000000000002) {
      return calculateTree44(features, 135);
    } else {
      return calculateTree44(features, 136);
    }
  }
  if (node === 68) return -0.14219997333893028;
  if (node === 33) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree44(features, 67);
    } else if (val <= 358.50000000000006) {
      return calculateTree44(features, 67);
    } else {
      return calculateTree44(features, 68);
    }
  }
  if (node === 139) return -0.23100272381206488;
  if (node === 281) return 0.10964137342316958;
  if (node === 565) return -0.28127680359384477;
  if (node === 1133) return 0.0956712250888651;
  if (node === 1134) return -0.3269240922077469;
  if (node === 566) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree44(features, 1133);
    } else if (val <= 311.50000000000006) {
      return calculateTree44(features, 1133);
    } else {
      return calculateTree44(features, 1134);
    }
  }
  if (node === 282) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree44(features, 565);
    } else if (val <= 877.5000000000001) {
      return calculateTree44(features, 565);
    } else {
      return calculateTree44(features, 566);
    }
  }
  if (node === 140) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree44(features, 281);
    } else if (val <= 763.0000000000001) {
      return calculateTree44(features, 281);
    } else {
      return calculateTree44(features, 282);
    }
  }
  if (node === 69) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree44(features, 139);
    } else if (val <= 391.00000000000006) {
      return calculateTree44(features, 139);
    } else {
      return calculateTree44(features, 140);
    }
  }
  if (node === 70) return 0.28516612447710005;
  if (node === 34) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree44(features, 69);
    } else if (val <= 387.50000000000006) {
      return calculateTree44(features, 69);
    } else {
      return calculateTree44(features, 70);
    }
  }
  if (node === 16) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree44(features, 33);
    } else if (val <= 225.50000000000003) {
      return calculateTree44(features, 33);
    } else {
      return calculateTree44(features, 34);
    }
  }
  if (node === 7) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree44(features, 15);
    } else if (val <= 271.00000000000006) {
      return calculateTree44(features, 15);
    } else {
      return calculateTree44(features, 16);
    }
  }
  if (node === 287) return 0.025166006809739558;
  if (node === 577) return -0.35321676062128027;
  if (node === 578) return -0.03286362133202752;
  if (node === 288) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree44(features, 577);
    } else if (val <= 195.00000000000003) {
      return calculateTree44(features, 577);
    } else {
      return calculateTree44(features, 578);
    }
  }
  if (node === 143) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree44(features, 287);
    } else if (val <= 181.50000000000003) {
      return calculateTree44(features, 287);
    } else {
      return calculateTree44(features, 288);
    }
  }
  if (node === 144) return -0.2971086584262627;
  if (node === 71) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree44(features, 143);
    } else if (val <= 217.50000000000003) {
      return calculateTree44(features, 143);
    } else {
      return calculateTree44(features, 144);
    }
  }
  if (node === 145) return 0.019010395418191727;
  if (node === 293) return -0.3261374012824295;
  if (node === 294) return 0.05516879941463484;
  if (node === 146) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree44(features, 293);
    } else if (val <= 311.50000000000006) {
      return calculateTree44(features, 293);
    } else {
      return calculateTree44(features, 294);
    }
  }
  if (node === 72) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree44(features, 145);
    } else if (val <= 937.5000000000001) {
      return calculateTree44(features, 145);
    } else {
      return calculateTree44(features, 146);
    }
  }
  if (node === 35) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree44(features, 71);
    } else if (val <= 225.50000000000003) {
      return calculateTree44(features, 71);
    } else {
      return calculateTree44(features, 72);
    }
  }
  if (node === 36) return -0.38078612095314224;
  if (node === 17) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree44(features, 35);
    } else if (val <= 387.50000000000006) {
      return calculateTree44(features, 35);
    } else {
      return calculateTree44(features, 36);
    }
  }
  if (node === 18) return 0.30954985124224155;
  if (node === 8) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree44(features, 17);
    } else if (val <= 391.50000000000006) {
      return calculateTree44(features, 17);
    } else {
      return calculateTree44(features, 18);
    }
  }
  if (node === 3) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree44(features, 7);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree44(features, 7);
    } else {
      return calculateTree44(features, 8);
    }
  }
  if (node === 39) return -0.2544024559398889;
  if (node === 81) return -0.4706049473969024;
  if (node === 165) return 0.6605497267550656;
  if (node === 166) return -0.16313853700120318;
  if (node === 82) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree44(features, 165);
    } else if (val <= 40.50000000000001) {
      return calculateTree44(features, 165);
    } else {
      return calculateTree44(features, 166);
    }
  }
  if (node === 40) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree44(features, 81);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree44(features, 81);
    } else {
      return calculateTree44(features, 82);
    }
  }
  if (node === 19) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree44(features, 39);
    } else if (val <= 677.5000000000001) {
      return calculateTree44(features, 39);
    } else {
      return calculateTree44(features, 40);
    }
  }
  if (node === 20) return -0.3630293014872045;
  if (node === 9) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree44(features, 19);
    } else if (val <= 778.5000000000001) {
      return calculateTree44(features, 19);
    } else {
      return calculateTree44(features, 20);
    }
  }
  if (node === 10) return -0.0011055513685790449;
  if (node === 4) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree44(features, 9);
    } else if (val <= 409.50000000000006) {
      return calculateTree44(features, 9);
    } else {
      return calculateTree44(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree44(features, 3);
    } else if (val <= 398.00000000000006) {
      return calculateTree44(features, 3);
    } else {
      return calculateTree44(features, 4);
    }
  }
  if (node === 5) return -0.2871669818406601;
  if (node === 6) return -0.0133787546153875;
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree44(features, 5);
    } else if (val <= 424.00000000000006) {
      return calculateTree44(features, 5);
    } else {
      return calculateTree44(features, 6);
    }
  }
  if (node === 0) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree44(features, 1);
    } else if (val <= 92.50000000000001) {
      return calculateTree44(features, 1);
    } else {
      return calculateTree44(features, 2);
    }
  }
}
function calculateTree45(features, node = 0) {
  if (node === 31) return -0.37244420112950366;
  if (node === 32) return 0.1146507573496412;
  if (node === 15) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree45(features, 31);
    } else if (val <= 649.5000000000001) {
      return calculateTree45(features, 31);
    } else {
      return calculateTree45(features, 32);
    }
  }
  if (node === 16) return -0.41695058351441366;
  if (node === 7) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree45(features, 15);
    } else if (val <= 6.000000000000001) {
      return calculateTree45(features, 15);
    } else {
      return calculateTree45(features, 16);
    }
  }
  if (node === 17) return 0.21253456060373996;
  if (node === 37) return -0.326514461161226;
  if (node === 155) return 0.4876649159610674;
  if (node === 156) return 0.09826919020579679;
  if (node === 77) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree45(features, 155);
    } else if (val <= 25.000000000000004) {
      return calculateTree45(features, 155);
    } else {
      return calculateTree45(features, 156);
    }
  }
  if (node === 157) return -0.3197136041408784;
  if (node === 317) return 0.09517013149768525;
  if (node === 318) return -0.3076698868960607;
  if (node === 158) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree45(features, 317);
    } else if (val <= 19.500000000000004) {
      return calculateTree45(features, 317);
    } else {
      return calculateTree45(features, 318);
    }
  }
  if (node === 78) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree45(features, 157);
    } else if (val <= 982.5000000000001) {
      return calculateTree45(features, 157);
    } else {
      return calculateTree45(features, 158);
    }
  }
  if (node === 38) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree45(features, 77);
    } else if (val <= 551.5000000000001) {
      return calculateTree45(features, 77);
    } else {
      return calculateTree45(features, 78);
    }
  }
  if (node === 18) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree45(features, 37);
    } else if (val <= 322.00000000000006) {
      return calculateTree45(features, 37);
    } else {
      return calculateTree45(features, 38);
    }
  }
  if (node === 8) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree45(features, 17);
    } else if (val <= 166.50000000000003) {
      return calculateTree45(features, 17);
    } else {
      return calculateTree45(features, 18);
    }
  }
  if (node === 3) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree45(features, 7);
    } else if (val <= 12.000000000000002) {
      return calculateTree45(features, 7);
    } else {
      return calculateTree45(features, 8);
    }
  }
  if (node === 39) return -0.35766308982333145;
  if (node === 163) return 0.08588256938763435;
  if (node === 164) return -0.32769198154047136;
  if (node === 81) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree45(features, 163);
    } else if (val <= 296.50000000000006) {
      return calculateTree45(features, 163);
    } else {
      return calculateTree45(features, 164);
    }
  }
  if (node === 331) return 0.35560844439384437;
  if (node === 332) return -0.028012834046064868;
  if (node === 165) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree45(features, 331);
    } else if (val <= 42.50000000000001) {
      return calculateTree45(features, 331);
    } else {
      return calculateTree45(features, 332);
    }
  }
  if (node === 667) return -0.013514117870384812;
  if (node === 1337) return 0.46934018969654917;
  if (node === 1338) return 0.042069557051205436;
  if (node === 668) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree45(features, 1337);
    } else if (val <= 5.500000000000001) {
      return calculateTree45(features, 1337);
    } else {
      return calculateTree45(features, 1338);
    }
  }
  if (node === 333) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree45(features, 667);
    } else if (val <= 36.50000000000001) {
      return calculateTree45(features, 667);
    } else {
      return calculateTree45(features, 668);
    }
  }
  if (node === 334) return -0.3514280964304984;
  if (node === 166) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree45(features, 333);
    } else if (val <= 19.000000000000004) {
      return calculateTree45(features, 333);
    } else {
      return calculateTree45(features, 334);
    }
  }
  if (node === 82) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree45(features, 165);
    } else if (val <= 12.000000000000002) {
      return calculateTree45(features, 165);
    } else {
      return calculateTree45(features, 166);
    }
  }
  if (node === 40) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree45(features, 81);
    } else if (val <= 542.5000000000001) {
      return calculateTree45(features, 81);
    } else {
      return calculateTree45(features, 82);
    }
  }
  if (node === 19) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree45(features, 39);
    } else if (val <= 186.50000000000003) {
      return calculateTree45(features, 39);
    } else {
      return calculateTree45(features, 40);
    }
  }
  if (node === 41) return -0.41042280875660053;
  if (node === 42) return 0.004103239003988174;
  if (node === 20) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree45(features, 41);
    } else if (val <= 785.5000000000001) {
      return calculateTree45(features, 41);
    } else {
      return calculateTree45(features, 42);
    }
  }
  if (node === 9) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree45(features, 19);
    } else if (val <= 649.5000000000001) {
      return calculateTree45(features, 19);
    } else {
      return calculateTree45(features, 20);
    }
  }
  if (node === 21) return 0.2329785780133235;
  if (node === 22) return -0.31757714277049365;
  if (node === 10) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree45(features, 21);
    } else if (val <= 12.000000000000002) {
      return calculateTree45(features, 21);
    } else {
      return calculateTree45(features, 22);
    }
  }
  if (node === 4) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree45(features, 9);
    } else if (val <= 942.0000000000001) {
      return calculateTree45(features, 9);
    } else {
      return calculateTree45(features, 10);
    }
  }
  if (node === 1) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree45(features, 3);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree45(features, 3);
    } else {
      return calculateTree45(features, 4);
    }
  }
  if (node === 47) return 0.10931250442214813;
  if (node === 97) return -0.3155387934417488;
  if (node === 197) return 0.2890985286451452;
  if (node === 198) return -0.3263354802479775;
  if (node === 98) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree45(features, 197);
    } else if (val <= 507.50000000000006) {
      return calculateTree45(features, 197);
    } else {
      return calculateTree45(features, 198);
    }
  }
  if (node === 48) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree45(features, 97);
    } else if (val <= 472.50000000000006) {
      return calculateTree45(features, 97);
    } else {
      return calculateTree45(features, 98);
    }
  }
  if (node === 23) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree45(features, 47);
    } else if (val <= 55.50000000000001) {
      return calculateTree45(features, 47);
    } else {
      return calculateTree45(features, 48);
    }
  }
  if (node === 24) return -0.2561058000014421;
  if (node === 11) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree45(features, 23);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree45(features, 23);
    } else {
      return calculateTree45(features, 24);
    }
  }
  if (node === 25) return 0.03347086779361748;
  if (node === 53) return -0.33935400845954594;
  if (node === 54) return -0.01842154461332904;
  if (node === 26) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree45(features, 53);
    } else if (val <= 931.5000000000001) {
      return calculateTree45(features, 53);
    } else {
      return calculateTree45(features, 54);
    }
  }
  if (node === 12) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree45(features, 25);
    } else if (val <= 908.5000000000001) {
      return calculateTree45(features, 25);
    } else {
      return calculateTree45(features, 26);
    }
  }
  if (node === 5) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree45(features, 11);
    } else if (val <= 658.5000000000001) {
      return calculateTree45(features, 11);
    } else {
      return calculateTree45(features, 12);
    }
  }
  if (node === 6) return -0.0011521907295677747;
  if (node === 2) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree45(features, 5);
    } else if (val <= 99.50000000000001) {
      return calculateTree45(features, 5);
    } else {
      return calculateTree45(features, 6);
    }
  }
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree45(features, 1);
    } else if (val <= 34.00000000000001) {
      return calculateTree45(features, 1);
    } else {
      return calculateTree45(features, 2);
    }
  }
}
function calculateTree46(features, node = 0) {
  if (node === 31) return 0.03242294768160325;
  if (node === 32) return -0.07167861374425831;
  if (node === 15) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree46(features, 31);
    } else if (val <= 205.50000000000003) {
      return calculateTree46(features, 31);
    } else {
      return calculateTree46(features, 32);
    }
  }
  if (node === 33) return 0.1124002996803774;
  if (node === 69) return -0.03329630035714449;
  if (node === 70) return 0.05337191452094047;
  if (node === 34) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree46(features, 69);
    } else if (val <= 934.0000000000001) {
      return calculateTree46(features, 69);
    } else {
      return calculateTree46(features, 70);
    }
  }
  if (node === 16) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree46(features, 33);
    } else if (val <= 649.5000000000001) {
      return calculateTree46(features, 33);
    } else {
      return calculateTree46(features, 34);
    }
  }
  if (node === 7) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree46(features, 15);
    } else if (val <= 572.0000000000001) {
      return calculateTree46(features, 15);
    } else {
      return calculateTree46(features, 16);
    }
  }
  if (node === 1151) return 0.08703061156614941;
  if (node === 1152) return 0.008773943465361402;
  if (node === 575) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree46(features, 1151);
    } else if (val <= 327.50000000000006) {
      return calculateTree46(features, 1151);
    } else {
      return calculateTree46(features, 1152);
    }
  }
  if (node === 1153) return -0.34903563328371384;
  if (node === 1154) return 0.1466034564378773;
  if (node === 576) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree46(features, 1153);
    } else if (val <= 195.00000000000003) {
      return calculateTree46(features, 1153);
    } else {
      return calculateTree46(features, 1154);
    }
  }
  if (node === 287) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree46(features, 575);
    } else if (val <= 829.0000000000001) {
      return calculateTree46(features, 575);
    } else {
      return calculateTree46(features, 576);
    }
  }
  if (node === 288) return -0.36393921902362053;
  if (node === 143) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree46(features, 287);
    } else if (val <= 763.5000000000001) {
      return calculateTree46(features, 287);
    } else {
      return calculateTree46(features, 288);
    }
  }
  if (node === 144) return 0.18675338344225442;
  if (node === 71) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree46(features, 143);
    } else if (val <= 780.0000000000001) {
      return calculateTree46(features, 143);
    } else {
      return calculateTree46(features, 144);
    }
  }
  if (node === 72) return -0.3471835961837228;
  if (node === 35) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree46(features, 71);
    } else if (val <= 784.5000000000001) {
      return calculateTree46(features, 71);
    } else {
      return calculateTree46(features, 72);
    }
  }
  if (node === 591) return -0.38084526362188276;
  if (node === 592) return 0.31557403597988215;
  if (node === 295) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree46(features, 591);
    } else if (val <= 846.5000000000001) {
      return calculateTree46(features, 591);
    } else {
      return calculateTree46(features, 592);
    }
  }
  if (node === 593) return 0.023290144995665472;
  if (node === 594) return -0.419243162036852;
  if (node === 296) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree46(features, 593);
    } else if (val <= 822.5000000000001) {
      return calculateTree46(features, 593);
    } else {
      return calculateTree46(features, 594);
    }
  }
  if (node === 147) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree46(features, 295);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree46(features, 295);
    } else {
      return calculateTree46(features, 296);
    }
  }
  if (node === 595) return -0.4507907179117467;
  if (node === 1193) return -0.05344439053393658;
  if (node === 1194) return 0.6541770233427912;
  if (node === 596) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree46(features, 1193);
    } else if (val <= 500.50000000000006) {
      return calculateTree46(features, 1193);
    } else {
      return calculateTree46(features, 1194);
    }
  }
  if (node === 297) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree46(features, 595);
    } else if (val <= 859.5000000000001) {
      return calculateTree46(features, 595);
    } else {
      return calculateTree46(features, 596);
    }
  }
  if (node === 298) return 0.261207321044573;
  if (node === 148) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree46(features, 297);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree46(features, 297);
    } else {
      return calculateTree46(features, 298);
    }
  }
  if (node === 73) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree46(features, 147);
    } else if (val <= 426.00000000000006) {
      return calculateTree46(features, 147);
    } else {
      return calculateTree46(features, 148);
    }
  }
  if (node === 74) return 0.02780447044468213;
  if (node === 36) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree46(features, 73);
    } else if (val <= 563.5000000000001) {
      return calculateTree46(features, 73);
    } else {
      return calculateTree46(features, 74);
    }
  }
  if (node === 17) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree46(features, 35);
    } else if (val <= 794.0000000000001) {
      return calculateTree46(features, 35);
    } else {
      return calculateTree46(features, 36);
    }
  }
  if (node === 75) return -0.33612394622036396;
  if (node === 76) return 0.1852421015752957;
  if (node === 37) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree46(features, 75);
    } else if (val <= 358.50000000000006) {
      return calculateTree46(features, 75);
    } else {
      return calculateTree46(features, 76);
    }
  }
  if (node === 77) return 0.009740118430769822;
  if (node === 78) return -0.3154808942407237;
  if (node === 38) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree46(features, 77);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree46(features, 77);
    } else {
      return calculateTree46(features, 78);
    }
  }
  if (node === 18) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree46(features, 37);
    } else if (val <= 391.00000000000006) {
      return calculateTree46(features, 37);
    } else {
      return calculateTree46(features, 38);
    }
  }
  if (node === 8) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree46(features, 17);
    } else if (val <= 916.5000000000001) {
      return calculateTree46(features, 17);
    } else {
      return calculateTree46(features, 18);
    }
  }
  if (node === 3) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree46(features, 7);
    } else if (val <= 297.00000000000006) {
      return calculateTree46(features, 7);
    } else {
      return calculateTree46(features, 8);
    }
  }
  if (node === 9) return -0.058649616775036544;
  if (node === 21) return 0.25618422094075555;
  if (node === 91) return -0.12360821318618051;
  if (node === 92) return 0.04793666910153924;
  if (node === 45) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree46(features, 91);
    } else if (val <= 19.500000000000004) {
      return calculateTree46(features, 91);
    } else {
      return calculateTree46(features, 92);
    }
  }
  if (node === 93) return -0.16169764371401285;
  if (node === 94) return 0.026295146631105302;
  if (node === 46) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree46(features, 93);
    } else if (val <= 963.0000000000001) {
      return calculateTree46(features, 93);
    } else {
      return calculateTree46(features, 94);
    }
  }
  if (node === 22) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree46(features, 45);
    } else if (val <= 758.5000000000001) {
      return calculateTree46(features, 45);
    } else {
      return calculateTree46(features, 46);
    }
  }
  if (node === 10) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree46(features, 21);
    } else if (val <= 906.0000000000001) {
      return calculateTree46(features, 21);
    } else {
      return calculateTree46(features, 22);
    }
  }
  if (node === 4) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree46(features, 9);
    } else if (val <= 900.5000000000001) {
      return calculateTree46(features, 9);
    } else {
      return calculateTree46(features, 10);
    }
  }
  if (node === 1) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree46(features, 3);
    } else if (val <= 839.5000000000001) {
      return calculateTree46(features, 3);
    } else {
      return calculateTree46(features, 4);
    }
  }
  if (node === 2) return -0.0029528900572788753;
  if (node === 0) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree46(features, 1);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree46(features, 1);
    } else {
      return calculateTree46(features, 2);
    }
  }
}
function calculateTree47(features, node = 0) {
  if (node === 1) return -0.0024988828860434817;
  if (node === 95) return 0.21917223390619536;
  if (node === 96) return -0.2999736914625685;
  if (node === 47) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree47(features, 95);
    } else if (val <= 194.00000000000003) {
      return calculateTree47(features, 95);
    } else {
      return calculateTree47(features, 96);
    }
  }
  if (node === 48) return -0.3759838934670185;
  if (node === 23) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree47(features, 47);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree47(features, 47);
    } else {
      return calculateTree47(features, 48);
    }
  }
  if (node === 199) return -0.43633871289488313;
  if (node === 200) return 0.5547033235414295;
  if (node === 99) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree47(features, 199);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree47(features, 199);
    } else {
      return calculateTree47(features, 200);
    }
  }
  if (node === 807) return -0.3850229995600928;
  if (node === 808) return 0.352104028860619;
  if (node === 403) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree47(features, 807);
    } else if (val <= 778.5000000000001) {
      return calculateTree47(features, 807);
    } else {
      return calculateTree47(features, 808);
    }
  }
  if (node === 809) return 0.11937180828968284;
  if (node === 810) return -0.47646073693038365;
  if (node === 404) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree47(features, 809);
    } else if (val <= 718.5000000000001) {
      return calculateTree47(features, 809);
    } else {
      return calculateTree47(features, 810);
    }
  }
  if (node === 201) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree47(features, 403);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree47(features, 403);
    } else {
      return calculateTree47(features, 404);
    }
  }
  if (node === 202) return 0.19051670956039657;
  if (node === 100) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree47(features, 201);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree47(features, 201);
    } else {
      return calculateTree47(features, 202);
    }
  }
  if (node === 49) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree47(features, 99);
    } else if (val <= 799.5000000000001) {
      return calculateTree47(features, 99);
    } else {
      return calculateTree47(features, 100);
    }
  }
  if (node === 203) return -0.3932666479430202;
  if (node === 409) return 0.41389582661471497;
  if (node === 821) return -0.3828569200048057;
  if (node === 822) return 0.1141283876615356;
  if (node === 410) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree47(features, 821);
    } else if (val <= 813.0000000000001) {
      return calculateTree47(features, 821);
    } else {
      return calculateTree47(features, 822);
    }
  }
  if (node === 204) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree47(features, 409);
    } else if (val <= 776.0000000000001) {
      return calculateTree47(features, 409);
    } else {
      return calculateTree47(features, 410);
    }
  }
  if (node === 101) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree47(features, 203);
    } else if (val <= 656.5000000000001) {
      return calculateTree47(features, 203);
    } else {
      return calculateTree47(features, 204);
    }
  }
  if (node === 205) return -0.38327576809705216;
  if (node === 413) return 0.15720329300585906;
  if (node === 414) return -0.0072855139728285065;
  if (node === 206) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree47(features, 413);
    } else if (val <= 34.50000000000001) {
      return calculateTree47(features, 413);
    } else {
      return calculateTree47(features, 414);
    }
  }
  if (node === 102) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree47(features, 205);
    } else if (val <= 818.0000000000001) {
      return calculateTree47(features, 205);
    } else {
      return calculateTree47(features, 206);
    }
  }
  if (node === 50) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree47(features, 101);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree47(features, 101);
    } else {
      return calculateTree47(features, 102);
    }
  }
  if (node === 24) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree47(features, 49);
    } else if (val <= 810.5000000000001) {
      return calculateTree47(features, 49);
    } else {
      return calculateTree47(features, 50);
    }
  }
  if (node === 11) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree47(features, 23);
    } else if (val <= 412.00000000000006) {
      return calculateTree47(features, 23);
    } else {
      return calculateTree47(features, 24);
    }
  }
  if (node === 51) return -0.3941700095639464;
  if (node === 52) return 0.09190558810480799;
  if (node === 25) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree47(features, 51);
    } else if (val <= 799.5000000000001) {
      return calculateTree47(features, 51);
    } else {
      return calculateTree47(features, 52);
    }
  }
  if (node === 26) return -0.3610806788888597;
  if (node === 12) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree47(features, 25);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree47(features, 25);
    } else {
      return calculateTree47(features, 26);
    }
  }
  if (node === 5) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree47(features, 11);
    } else if (val <= 859.5000000000001) {
      return calculateTree47(features, 11);
    } else {
      return calculateTree47(features, 12);
    }
  }
  if (node === 55) return 0.03820614640886886;
  if (node === 227) return -0.3678243148457869;
  if (node === 457) return 0.10960273555392212;
  if (node === 917) return -0.3788773603102088;
  if (node === 918) return 0.22012147070835283;
  if (node === 458) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree47(features, 917);
    } else if (val <= 993.5000000000001) {
      return calculateTree47(features, 917);
    } else {
      return calculateTree47(features, 918);
    }
  }
  if (node === 228) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree47(features, 457);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree47(features, 457);
    } else {
      return calculateTree47(features, 458);
    }
  }
  if (node === 113) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree47(features, 227);
    } else if (val <= 931.5000000000001) {
      return calculateTree47(features, 227);
    } else {
      return calculateTree47(features, 228);
    }
  }
  if (node === 114) return -0.010001300151428751;
  if (node === 56) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree47(features, 113);
    } else if (val <= 127.50000000000001) {
      return calculateTree47(features, 113);
    } else {
      return calculateTree47(features, 114);
    }
  }
  if (node === 27) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree47(features, 55);
    } else if (val <= 55.50000000000001) {
      return calculateTree47(features, 55);
    } else {
      return calculateTree47(features, 56);
    }
  }
  if (node === 28) return -0.23431201874895524;
  if (node === 13) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree47(features, 27);
    } else if (val <= 884.0000000000001) {
      return calculateTree47(features, 27);
    } else {
      return calculateTree47(features, 28);
    }
  }
  if (node === 59) return 0.17815548913033113;
  if (node === 121) return -0.33975990143015744;
  if (node === 122) return 0.1770058575788778;
  if (node === 60) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree47(features, 121);
    } else if (val <= 916.5000000000001) {
      return calculateTree47(features, 121);
    } else {
      return calculateTree47(features, 122);
    }
  }
  if (node === 29) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree47(features, 59);
    } else if (val <= 845.5000000000001) {
      return calculateTree47(features, 59);
    } else {
      return calculateTree47(features, 60);
    }
  }
  if (node === 30) return 0.08626417951159929;
  if (node === 14) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree47(features, 29);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree47(features, 29);
    } else {
      return calculateTree47(features, 30);
    }
  }
  if (node === 6) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree47(features, 13);
    } else if (val <= 906.0000000000001) {
      return calculateTree47(features, 13);
    } else {
      return calculateTree47(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree47(features, 5);
    } else if (val <= 835.5000000000001) {
      return calculateTree47(features, 5);
    } else {
      return calculateTree47(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree47(features, 1);
    } else if (val <= 794.0000000000001) {
      return calculateTree47(features, 1);
    } else {
      return calculateTree47(features, 2);
    }
  }
}
function calculateTree48(features, node = 0) {
  if (node === 15) return 0.0019045702998509266;
  if (node === 67) return -0.3923316583724187;
  if (node === 68) return 0.24859513307345557;
  if (node === 33) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree48(features, 67);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree48(features, 67);
    } else {
      return calculateTree48(features, 68);
    }
  }
  if (node === 34) return 0.011889445003166575;
  if (node === 16) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree48(features, 33);
    } else if (val <= 649.5000000000001) {
      return calculateTree48(features, 33);
    } else {
      return calculateTree48(features, 34);
    }
  }
  if (node === 7) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree48(features, 15);
    } else if (val <= 647.0000000000001) {
      return calculateTree48(features, 15);
    } else {
      return calculateTree48(features, 16);
    }
  }
  if (node === 143) return -0.011763646791049662;
  if (node === 144) return -0.3206717969323432;
  if (node === 71) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree48(features, 143);
    } else if (val <= 731.5000000000001) {
      return calculateTree48(features, 143);
    } else {
      return calculateTree48(features, 144);
    }
  }
  if (node === 145) return -0.3410913990177978;
  if (node === 146) return 0.09190583061692789;
  if (node === 72) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree48(features, 145);
    } else if (val <= 731.5000000000001) {
      return calculateTree48(features, 145);
    } else {
      return calculateTree48(features, 146);
    }
  }
  if (node === 35) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree48(features, 71);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree48(features, 71);
    } else {
      return calculateTree48(features, 72);
    }
  }
  if (node === 147) return 0.004420240570838674;
  if (node === 297) return -0.3600300750344904;
  if (node === 298) return -0.0007806093881750193;
  if (node === 148) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree48(features, 297);
    } else if (val <= 733.5000000000001) {
      return calculateTree48(features, 297);
    } else {
      return calculateTree48(features, 298);
    }
  }
  if (node === 73) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree48(features, 147);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree48(features, 147);
    } else {
      return calculateTree48(features, 148);
    }
  }
  if (node === 74) return -0.30320004153529995;
  if (node === 36) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree48(features, 73);
    } else if (val <= 752.5000000000001) {
      return calculateTree48(features, 73);
    } else {
      return calculateTree48(features, 74);
    }
  }
  if (node === 17) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree48(features, 35);
    } else if (val <= 144.50000000000003) {
      return calculateTree48(features, 35);
    } else {
      return calculateTree48(features, 36);
    }
  }
  if (node === 37) return 0.0007583666505260475;
  if (node === 77) return 0.07932194387796984;
  if (node === 78) return -0.35751374837275324;
  if (node === 38) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree48(features, 77);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree48(features, 77);
    } else {
      return calculateTree48(features, 78);
    }
  }
  if (node === 18) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree48(features, 37);
    } else if (val <= 417.00000000000006) {
      return calculateTree48(features, 37);
    } else {
      return calculateTree48(features, 38);
    }
  }
  if (node === 8) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree48(features, 17);
    } else if (val <= 780.0000000000001) {
      return calculateTree48(features, 17);
    } else {
      return calculateTree48(features, 18);
    }
  }
  if (node === 3) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree48(features, 7);
    } else if (val <= 723.5000000000001) {
      return calculateTree48(features, 7);
    } else {
      return calculateTree48(features, 8);
    }
  }
  if (node === 79) return 0.03978016432334331;
  if (node === 161) return -0.23658845595896852;
  if (node === 162) return 0.050561250531139565;
  if (node === 80) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree48(features, 161);
    } else if (val <= 476.00000000000006) {
      return calculateTree48(features, 161);
    } else {
      return calculateTree48(features, 162);
    }
  }
  if (node === 39) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree48(features, 79);
    } else if (val <= 531.5000000000001) {
      return calculateTree48(features, 79);
    } else {
      return calculateTree48(features, 80);
    }
  }
  if (node === 40) return -0.1997214188608016;
  if (node === 19) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree48(features, 39);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree48(features, 39);
    } else {
      return calculateTree48(features, 40);
    }
  }
  if (node === 83) return -0.3473405908112597;
  if (node === 84) return 0.08869036252137423;
  if (node === 41) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree48(features, 83);
    } else if (val <= 919.5000000000001) {
      return calculateTree48(features, 83);
    } else {
      return calculateTree48(features, 84);
    }
  }
  if (node === 85) return 0.119738158054588;
  if (node === 86) return -0.33460679336094207;
  if (node === 42) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree48(features, 85);
    } else if (val <= 467.50000000000006) {
      return calculateTree48(features, 85);
    } else {
      return calculateTree48(features, 86);
    }
  }
  if (node === 20) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree48(features, 41);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree48(features, 41);
    } else {
      return calculateTree48(features, 42);
    }
  }
  if (node === 9) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree48(features, 19);
    } else if (val <= 799.5000000000001) {
      return calculateTree48(features, 19);
    } else {
      return calculateTree48(features, 20);
    }
  }
  if (node === 21) return 0.10105861010889895;
  if (node === 22) return 0.007532254731891436;
  if (node === 10) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree48(features, 21);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree48(features, 21);
    } else {
      return calculateTree48(features, 22);
    }
  }
  if (node === 4) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree48(features, 9);
    } else if (val <= 24.500000000000004) {
      return calculateTree48(features, 9);
    } else {
      return calculateTree48(features, 10);
    }
  }
  if (node === 1) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree48(features, 3);
    } else if (val <= 441.00000000000006) {
      return calculateTree48(features, 3);
    } else {
      return calculateTree48(features, 4);
    }
  }
  if (node === 11) return 0.01810942176847643;
  if (node === 51) return 0.058059589725751204;
  if (node === 52) return -0.3526705226346711;
  if (node === 25) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree48(features, 51);
    } else if (val <= 481.50000000000006) {
      return calculateTree48(features, 51);
    } else {
      return calculateTree48(features, 52);
    }
  }
  if (node === 26) return -0.38498671602708817;
  if (node === 12) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree48(features, 25);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree48(features, 25);
    } else {
      return calculateTree48(features, 26);
    }
  }
  if (node === 5) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree48(features, 11);
    } else if (val <= 300.50000000000006) {
      return calculateTree48(features, 11);
    } else {
      return calculateTree48(features, 12);
    }
  }
  if (node === 13) return -0.003378973004906464;
  if (node === 29) return -0.27669938719973813;
  if (node === 30) return -0.004522325861797157;
  if (node === 14) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree48(features, 29);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree48(features, 29);
    } else {
      return calculateTree48(features, 30);
    }
  }
  if (node === 6) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree48(features, 13);
    } else if (val <= 989.5000000000001) {
      return calculateTree48(features, 13);
    } else {
      return calculateTree48(features, 14);
    }
  }
  if (node === 2) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree48(features, 5);
    } else if (val <= 485.50000000000006) {
      return calculateTree48(features, 5);
    } else {
      return calculateTree48(features, 6);
    }
  }
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree48(features, 1);
    } else if (val <= 478.50000000000006) {
      return calculateTree48(features, 1);
    } else {
      return calculateTree48(features, 2);
    }
  }
}
function calculateTree49(features, node = 0) {
  if (node === 3) return -0.004420677806809952;
  if (node === 4) return -0.2826314285696202;
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree49(features, 3);
    } else if (val <= 205.50000000000003) {
      return calculateTree49(features, 3);
    } else {
      return calculateTree49(features, 4);
    }
  }
  if (node === 11) return -0.3693555498553517;
  if (node === 25) return 0.14146442581416957;
  if (node === 26) return 0.30509802897617194;
  if (node === 12) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree49(features, 25);
    } else if (val <= 24.500000000000004) {
      return calculateTree49(features, 25);
    } else {
      return calculateTree49(features, 26);
    }
  }
  if (node === 5) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree49(features, 11);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree49(features, 11);
    } else {
      return calculateTree49(features, 12);
    }
  }
  if (node === 111) return -0.15759082973487187;
  if (node === 112) return 0.04202142632326563;
  if (node === 55) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree49(features, 111);
    } else if (val <= 26.500000000000004) {
      return calculateTree49(features, 111);
    } else {
      return calculateTree49(features, 112);
    }
  }
  if (node === 56) return -0.3685451861245745;
  if (node === 27) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree49(features, 55);
    } else if (val <= 491.50000000000006) {
      return calculateTree49(features, 55);
    } else {
      return calculateTree49(features, 56);
    }
  }
  if (node === 28) return 0.14372213459623023;
  if (node === 13) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree49(features, 27);
    } else if (val <= 718.5000000000001) {
      return calculateTree49(features, 27);
    } else {
      return calculateTree49(features, 28);
    }
  }
  if (node === 59) return -0.3290787828373722;
  if (node === 243) return 0.1451104217895758;
  if (node === 244) return -0.32026169286707745;
  if (node === 121) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree49(features, 243);
    } else if (val <= 898.5000000000001) {
      return calculateTree49(features, 243);
    } else {
      return calculateTree49(features, 244);
    }
  }
  if (node === 122) return -0.07897020536141976;
  if (node === 60) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree49(features, 121);
    } else if (val <= 21.500000000000004) {
      return calculateTree49(features, 121);
    } else {
      return calculateTree49(features, 122);
    }
  }
  if (node === 29) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree49(features, 59);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree49(features, 59);
    } else {
      return calculateTree49(features, 60);
    }
  }
  if (node === 247) return 0.4605666484053124;
  if (node === 248) return -0.21970851377646267;
  if (node === 123) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree49(features, 247);
    } else if (val <= 771.0000000000001) {
      return calculateTree49(features, 247);
    } else {
      return calculateTree49(features, 248);
    }
  }
  if (node === 124) return -0.2782584997657107;
  if (node === 61) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree49(features, 123);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree49(features, 123);
    } else {
      return calculateTree49(features, 124);
    }
  }
  if (node === 1007) return 0.1347157319822856;
  if (node === 1008) return -0.01064451813966899;
  if (node === 503) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree49(features, 1007);
    } else if (val <= 232.50000000000003) {
      return calculateTree49(features, 1007);
    } else {
      return calculateTree49(features, 1008);
    }
  }
  if (node === 1009) return -0.25591417643798936;
  if (node === 2021) return 0.5506758730653031;
  if (node === 2022) return -0.01769209327480817;
  if (node === 1010) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree49(features, 2021);
    } else if (val <= 148.50000000000003) {
      return calculateTree49(features, 2021);
    } else {
      return calculateTree49(features, 2022);
    }
  }
  if (node === 504) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree49(features, 1009);
    } else if (val <= 245.00000000000003) {
      return calculateTree49(features, 1009);
    } else {
      return calculateTree49(features, 1010);
    }
  }
  if (node === 251) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree49(features, 503);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree49(features, 503);
    } else {
      return calculateTree49(features, 504);
    }
  }
  if (node === 1011) return 0.36061964958402254;
  if (node === 2025) return -0.30677108063516095;
  if (node === 2026) return 0.15745717292422154;
  if (node === 1012) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree49(features, 2025);
    } else if (val <= 953.5000000000001) {
      return calculateTree49(features, 2025);
    } else {
      return calculateTree49(features, 2026);
    }
  }
  if (node === 505) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree49(features, 1011);
    } else if (val <= 679.5000000000001) {
      return calculateTree49(features, 1011);
    } else {
      return calculateTree49(features, 1012);
    }
  }
  if (node === 2027) return -0.43179053710262977;
  if (node === 2028) return 0.19869938698991174;
  if (node === 1013) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree49(features, 2027);
    } else if (val <= 679.5000000000001) {
      return calculateTree49(features, 2027);
    } else {
      return calculateTree49(features, 2028);
    }
  }
  if (node === 2029) return -0.11567284816834958;
  if (node === 2030) return 0.16008527669726677;
  if (node === 1014) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree49(features, 2029);
    } else if (val <= 893.0000000000001) {
      return calculateTree49(features, 2029);
    } else {
      return calculateTree49(features, 2030);
    }
  }
  if (node === 506) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree49(features, 1013);
    } else if (val <= 799.5000000000001) {
      return calculateTree49(features, 1013);
    } else {
      return calculateTree49(features, 1014);
    }
  }
  if (node === 252) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree49(features, 505);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree49(features, 505);
    } else {
      return calculateTree49(features, 506);
    }
  }
  if (node === 125) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree49(features, 251);
    } else if (val <= 665.0000000000001) {
      return calculateTree49(features, 251);
    } else {
      return calculateTree49(features, 252);
    }
  }
  if (node === 507) return -0.31809313820074075;
  if (node === 1017) return 0.08756125638892177;
  if (node === 1018) return -0.21604968730840005;
  if (node === 508) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree49(features, 1017);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree49(features, 1017);
    } else {
      return calculateTree49(features, 1018);
    }
  }
  if (node === 253) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree49(features, 507);
    } else if (val <= 839.5000000000001) {
      return calculateTree49(features, 507);
    } else {
      return calculateTree49(features, 508);
    }
  }
  if (node === 254) return -0.0014166948988281174;
  if (node === 126) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree49(features, 253);
    } else if (val <= 258.50000000000006) {
      return calculateTree49(features, 253);
    } else {
      return calculateTree49(features, 254);
    }
  }
  if (node === 62) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree49(features, 125);
    } else if (val <= 253.50000000000003) {
      return calculateTree49(features, 125);
    } else {
      return calculateTree49(features, 126);
    }
  }
  if (node === 30) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree49(features, 61);
    } else if (val <= 217.50000000000003) {
      return calculateTree49(features, 61);
    } else {
      return calculateTree49(features, 62);
    }
  }
  if (node === 14) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree49(features, 29);
    } else if (val <= 214.00000000000003) {
      return calculateTree49(features, 29);
    } else {
      return calculateTree49(features, 30);
    }
  }
  if (node === 6) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree49(features, 13);
    } else if (val <= 212.50000000000003) {
      return calculateTree49(features, 13);
    } else {
      return calculateTree49(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree49(features, 5);
    } else if (val <= 208.50000000000003) {
      return calculateTree49(features, 5);
    } else {
      return calculateTree49(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree49(features, 1);
    } else if (val <= 207.00000000000003) {
      return calculateTree49(features, 1);
    } else {
      return calculateTree49(features, 2);
    }
  }
}
function calculateTree50(features, node = 0) {
  if (node === 15) return 0.07961772474659426;
  if (node === 67) return 0.07348592464066449;
  if (node === 68) return -0.6037073740994647;
  if (node === 33) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree50(features, 67);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree50(features, 67);
    } else {
      return calculateTree50(features, 68);
    }
  }
  if (node === 34) return 0.055588337771998286;
  if (node === 16) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree50(features, 33);
    } else if (val <= 30.500000000000004) {
      return calculateTree50(features, 33);
    } else {
      return calculateTree50(features, 34);
    }
  }
  if (node === 7) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree50(features, 15);
    } else if (val <= 1.5000000000000002) {
      return calculateTree50(features, 15);
    } else {
      return calculateTree50(features, 16);
    }
  }
  if (node === 17) return 0.2464153808809201;
  if (node === 18) return -0.08795670309989391;
  if (node === 8) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree50(features, 17);
    } else if (val <= 926.0000000000001) {
      return calculateTree50(features, 17);
    } else {
      return calculateTree50(features, 18);
    }
  }
  if (node === 3) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree50(features, 7);
    } else if (val <= 197.50000000000003) {
      return calculateTree50(features, 7);
    } else {
      return calculateTree50(features, 8);
    }
  }
  if (node === 4) return -0.3214729744165072;
  if (node === 1) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree50(features, 3);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree50(features, 3);
    } else {
      return calculateTree50(features, 4);
    }
  }
  if (node === 95) return 0.4069094915183258;
  if (node === 193) return 0.22707632602962524;
  if (node === 194) return -0.011342525961288636;
  if (node === 96) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree50(features, 193);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree50(features, 193);
    } else {
      return calculateTree50(features, 194);
    }
  }
  if (node === 47) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree50(features, 95);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree50(features, 95);
    } else {
      return calculateTree50(features, 96);
    }
  }
  if (node === 48) return -0.40052378689649404;
  if (node === 23) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree50(features, 47);
    } else if (val <= 695.5000000000001) {
      return calculateTree50(features, 47);
    } else {
      return calculateTree50(features, 48);
    }
  }
  if (node === 24) return -0.3625747065184794;
  if (node === 11) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree50(features, 23);
    } else if (val <= 8.500000000000002) {
      return calculateTree50(features, 23);
    } else {
      return calculateTree50(features, 24);
    }
  }
  if (node === 25) return -0.3948964728705016;
  if (node === 215) return 0.2595142185540448;
  if (node === 216) return 0.044124851051416274;
  if (node === 107) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree50(features, 215);
    } else if (val <= 1.5000000000000002) {
      return calculateTree50(features, 215);
    } else {
      return calculateTree50(features, 216);
    }
  }
  if (node === 217) return 0.11981750183420647;
  if (node === 218) return -0.13174766445683497;
  if (node === 108) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree50(features, 217);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree50(features, 217);
    } else {
      return calculateTree50(features, 218);
    }
  }
  if (node === 53) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree50(features, 107);
    } else if (val <= 29.500000000000004) {
      return calculateTree50(features, 107);
    } else {
      return calculateTree50(features, 108);
    }
  }
  if (node === 109) return -0.3558757648271983;
  if (node === 110) return 0.11577924646933559;
  if (node === 54) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree50(features, 109);
    } else if (val <= 695.5000000000001) {
      return calculateTree50(features, 109);
    } else {
      return calculateTree50(features, 110);
    }
  }
  if (node === 26) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree50(features, 53);
    } else if (val <= 363.00000000000006) {
      return calculateTree50(features, 53);
    } else {
      return calculateTree50(features, 54);
    }
  }
  if (node === 12) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree50(features, 25);
    } else if (val <= 113.50000000000001) {
      return calculateTree50(features, 25);
    } else {
      return calculateTree50(features, 26);
    }
  }
  if (node === 5) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree50(features, 11);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree50(features, 11);
    } else {
      return calculateTree50(features, 12);
    }
  }
  if (node === 55) return 0.0490255198642751;
  if (node === 113) return -0.25810948419876384;
  if (node === 229) return 0.072081924377326;
  if (node === 230) return -0.2137565249121526;
  if (node === 114) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree50(features, 229);
    } else if (val <= 14.500000000000002) {
      return calculateTree50(features, 229);
    } else {
      return calculateTree50(features, 230);
    }
  }
  if (node === 56) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree50(features, 113);
    } else if (val <= 20.500000000000004) {
      return calculateTree50(features, 113);
    } else {
      return calculateTree50(features, 114);
    }
  }
  if (node === 27) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree50(features, 55);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree50(features, 55);
    } else {
      return calculateTree50(features, 56);
    }
  }
  if (node === 57) return 0.07368730537262182;
  if (node === 58) return 0.23146546078906127;
  if (node === 28) {
    const val = features['is_preference_personal_care'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree50(features, 57);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree50(features, 57);
    } else {
      return calculateTree50(features, 58);
    }
  }
  if (node === 13) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree50(features, 27);
    } else if (val <= 32.50000000000001) {
      return calculateTree50(features, 27);
    } else {
      return calculateTree50(features, 28);
    }
  }
  if (node === 29) return 0.003702215725252318;
  if (node === 495) return 0.17696653646413527;
  if (node === 496) return -0.15184756231203014;
  if (node === 247) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree50(features, 495);
    } else if (val <= 338.50000000000006) {
      return calculateTree50(features, 495);
    } else {
      return calculateTree50(features, 496);
    }
  }
  if (node === 248) return -0.3220864749945529;
  if (node === 123) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree50(features, 247);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree50(features, 247);
    } else {
      return calculateTree50(features, 248);
    }
  }
  if (node === 249) return -0.28375584441000523;
  if (node === 250) return -0.08535714089188405;
  if (node === 124) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree50(features, 249);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree50(features, 249);
    } else {
      return calculateTree50(features, 250);
    }
  }
  if (node === 61) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree50(features, 123);
    } else if (val <= 180.50000000000003) {
      return calculateTree50(features, 123);
    } else {
      return calculateTree50(features, 124);
    }
  }
  if (node === 62) return -0.001607258685904556;
  if (node === 30) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree50(features, 61);
    } else if (val <= 348.50000000000006) {
      return calculateTree50(features, 61);
    } else {
      return calculateTree50(features, 62);
    }
  }
  if (node === 14) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree50(features, 29);
    } else if (val <= 327.50000000000006) {
      return calculateTree50(features, 29);
    } else {
      return calculateTree50(features, 30);
    }
  }
  if (node === 6) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree50(features, 13);
    } else if (val <= 16.500000000000004) {
      return calculateTree50(features, 13);
    } else {
      return calculateTree50(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree50(features, 5);
    } else if (val <= 10.000000000000002) {
      return calculateTree50(features, 5);
    } else {
      return calculateTree50(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree50(features, 1);
    } else if (val <= 3.5000000000000004) {
      return calculateTree50(features, 1);
    } else {
      return calculateTree50(features, 2);
    }
  }
}
function calculateTree51(features, node = 0) {
  if (node === 1) return 0.001497663870328766;
  if (node === 5) return -0.23853691741497315;
  if (node === 55) return 0.14353363656952045;
  if (node === 227) return 0.0027185517722871553;
  if (node === 228) return -0.34827963106864007;
  if (node === 113) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree51(features, 227);
    } else if (val <= 575.5000000000001) {
      return calculateTree51(features, 227);
    } else {
      return calculateTree51(features, 228);
    }
  }
  if (node === 919) return 0.02140987236901431;
  if (node === 920) return 0.2622535138639262;
  if (node === 459) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree51(features, 919);
    } else if (val <= 325.00000000000006) {
      return calculateTree51(features, 919);
    } else {
      return calculateTree51(features, 920);
    }
  }
  if (node === 921) return -0.31341595647108667;
  if (node === 1845) return 0.18199028797251407;
  if (node === 1846) return -0.3260358560992992;
  if (node === 922) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree51(features, 1845);
    } else if (val <= 838.5000000000001) {
      return calculateTree51(features, 1845);
    } else {
      return calculateTree51(features, 1846);
    }
  }
  if (node === 460) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree51(features, 921);
    } else if (val <= 835.5000000000001) {
      return calculateTree51(features, 921);
    } else {
      return calculateTree51(features, 922);
    }
  }
  if (node === 229) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree51(features, 459);
    } else if (val <= 358.50000000000006) {
      return calculateTree51(features, 459);
    } else {
      return calculateTree51(features, 460);
    }
  }
  if (node === 230) return 0.1803150992200403;
  if (node === 114) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree51(features, 229);
    } else if (val <= 424.00000000000006) {
      return calculateTree51(features, 229);
    } else {
      return calculateTree51(features, 230);
    }
  }
  if (node === 56) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree51(features, 113);
    } else if (val <= 656.5000000000001) {
      return calculateTree51(features, 113);
    } else {
      return calculateTree51(features, 114);
    }
  }
  if (node === 27) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree51(features, 55);
    } else if (val <= 558.0000000000001) {
      return calculateTree51(features, 55);
    } else {
      return calculateTree51(features, 56);
    }
  }
  if (node === 57) return -0.34265055265561034;
  if (node === 117) return -0.3763617483384253;
  if (node === 1903) return -0.0034936599244685834;
  if (node === 1904) return 0.1939878452287212;
  if (node === 951) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree51(features, 1903);
    } else if (val <= 782.0000000000001) {
      return calculateTree51(features, 1903);
    } else {
      return calculateTree51(features, 1904);
    }
  }
  if (node === 952) return -0.34932602255927236;
  if (node === 475) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree51(features, 951);
    } else if (val <= 785.5000000000001) {
      return calculateTree51(features, 951);
    } else {
      return calculateTree51(features, 952);
    }
  }
  if (node === 953) return 0.2450182160633877;
  if (node === 954) return 0.06892317689614268;
  if (node === 476) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree51(features, 953);
    } else if (val <= 799.5000000000001) {
      return calculateTree51(features, 953);
    } else {
      return calculateTree51(features, 954);
    }
  }
  if (node === 237) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree51(features, 475);
    } else if (val <= 799.5000000000001) {
      return calculateTree51(features, 475);
    } else {
      return calculateTree51(features, 476);
    }
  }
  if (node === 477) return -0.3181426348881477;
  if (node === 478) return 0.028668076389410345;
  if (node === 238) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree51(features, 477);
    } else if (val <= 908.5000000000001) {
      return calculateTree51(features, 477);
    } else {
      return calculateTree51(features, 478);
    }
  }
  if (node === 118) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree51(features, 237);
    } else if (val <= 840.0000000000001) {
      return calculateTree51(features, 237);
    } else {
      return calculateTree51(features, 238);
    }
  }
  if (node === 58) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree51(features, 117);
    } else if (val <= 500.50000000000006) {
      return calculateTree51(features, 117);
    } else {
      return calculateTree51(features, 118);
    }
  }
  if (node === 28) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree51(features, 57);
    } else if (val <= 518.5000000000001) {
      return calculateTree51(features, 57);
    } else {
      return calculateTree51(features, 58);
    }
  }
  if (node === 13) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree51(features, 27);
    } else if (val <= 472.50000000000006) {
      return calculateTree51(features, 27);
    } else {
      return calculateTree51(features, 28);
    }
  }
  if (node === 119) return -0.1908625187023466;
  if (node === 120) return -0.39085487322256335;
  if (node === 59) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree51(features, 119);
    } else if (val <= 278.50000000000006) {
      return calculateTree51(features, 119);
    } else {
      return calculateTree51(features, 120);
    }
  }
  if (node === 121) return 0.9656387221676368;
  if (node === 983) return 0.09430394052024808;
  if (node === 984) return -0.12845710371419738;
  if (node === 491) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree51(features, 983);
    } else if (val <= 504.50000000000006) {
      return calculateTree51(features, 983);
    } else {
      return calculateTree51(features, 984);
    }
  }
  if (node === 492) return 0.32150834221493857;
  if (node === 245) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree51(features, 491);
    } else if (val <= 542.0000000000001) {
      return calculateTree51(features, 491);
    } else {
      return calculateTree51(features, 492);
    }
  }
  if (node === 246) return -0.38682252931378436;
  if (node === 122) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree51(features, 245);
    } else if (val <= 846.5000000000001) {
      return calculateTree51(features, 245);
    } else {
      return calculateTree51(features, 246);
    }
  }
  if (node === 60) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree51(features, 121);
    } else if (val <= 548.5000000000001) {
      return calculateTree51(features, 121);
    } else {
      return calculateTree51(features, 122);
    }
  }
  if (node === 29) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree51(features, 59);
    } else if (val <= 475.50000000000006) {
      return calculateTree51(features, 59);
    } else {
      return calculateTree51(features, 60);
    }
  }
  if (node === 247) return 0.0008064415606982699;
  if (node === 248) return -0.39508210480285993;
  if (node === 123) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree51(features, 247);
    } else if (val <= 799.5000000000001) {
      return calculateTree51(features, 247);
    } else {
      return calculateTree51(features, 248);
    }
  }
  if (node === 124) return 0.05265900577336733;
  if (node === 61) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree51(features, 123);
    } else if (val <= 834.5000000000001) {
      return calculateTree51(features, 123);
    } else {
      return calculateTree51(features, 124);
    }
  }
  if (node === 125) return -0.291320234687021;
  if (node === 126) return -0.015215862094780467;
  if (node === 62) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree51(features, 125);
    } else if (val <= 966.0000000000001) {
      return calculateTree51(features, 125);
    } else {
      return calculateTree51(features, 126);
    }
  }
  if (node === 30) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree51(features, 61);
    } else if (val <= 916.5000000000001) {
      return calculateTree51(features, 61);
    } else {
      return calculateTree51(features, 62);
    }
  }
  if (node === 14) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree51(features, 29);
    } else if (val <= 575.5000000000001) {
      return calculateTree51(features, 29);
    } else {
      return calculateTree51(features, 30);
    }
  }
  if (node === 6) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree51(features, 13);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree51(features, 13);
    } else {
      return calculateTree51(features, 14);
    }
  }
  if (node === 2) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree51(features, 5);
    } else if (val <= 498.50000000000006) {
      return calculateTree51(features, 5);
    } else {
      return calculateTree51(features, 6);
    }
  }
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree51(features, 1);
    } else if (val <= 491.50000000000006) {
      return calculateTree51(features, 1);
    } else {
      return calculateTree51(features, 2);
    }
  }
}
function calculateTree52(features, node = 0) {
  if (node === 3) return 0.0008548422474314396;
  if (node === 79) return 0.20696334351350204;
  if (node === 80) return -0.35797360813215207;
  if (node === 39) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree52(features, 79);
    } else if (val <= 443.50000000000006) {
      return calculateTree52(features, 79);
    } else {
      return calculateTree52(features, 80);
    }
  }
  if (node === 81) return -0.3740907116984706;
  if (node === 82) return 0.19987369134407768;
  if (node === 40) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree52(features, 81);
    } else if (val <= 443.50000000000006) {
      return calculateTree52(features, 81);
    } else {
      return calculateTree52(features, 82);
    }
  }
  if (node === 19) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree52(features, 39);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree52(features, 39);
    } else {
      return calculateTree52(features, 40);
    }
  }
  if (node === 20) return -0.33410308055736065;
  if (node === 9) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree52(features, 19);
    } else if (val <= 464.50000000000006) {
      return calculateTree52(features, 19);
    } else {
      return calculateTree52(features, 20);
    }
  }
  if (node === 43) return -0.3086724661092339;
  if (node === 89) return 0.25233595900676514;
  if (node === 181) return -0.2019530148124863;
  if (node === 182) return 0.058486507841689676;
  if (node === 90) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree52(features, 181);
    } else if (val <= 887.0000000000001) {
      return calculateTree52(features, 181);
    } else {
      return calculateTree52(features, 182);
    }
  }
  if (node === 44) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree52(features, 89);
    } else if (val <= 491.50000000000006) {
      return calculateTree52(features, 89);
    } else {
      return calculateTree52(features, 90);
    }
  }
  if (node === 21) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree52(features, 43);
    } else if (val <= 474.50000000000006) {
      return calculateTree52(features, 43);
    } else {
      return calculateTree52(features, 44);
    }
  }
  if (node === 22) return -0.308260935205904;
  if (node === 10) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree52(features, 21);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree52(features, 21);
    } else {
      return calculateTree52(features, 22);
    }
  }
  if (node === 4) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree52(features, 9);
    } else if (val <= 284.50000000000006) {
      return calculateTree52(features, 9);
    } else {
      return calculateTree52(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree52(features, 3);
    } else if (val <= 427.50000000000006) {
      return calculateTree52(features, 3);
    } else {
      return calculateTree52(features, 4);
    }
  }
  if (node === 95) return 0.0820164177520072;
  if (node === 96) return 0.31430391360061843;
  if (node === 47) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree52(features, 95);
    } else if (val <= 480.00000000000006) {
      return calculateTree52(features, 95);
    } else {
      return calculateTree52(features, 96);
    }
  }
  if (node === 48) return -0.34118897454;
  if (node === 23) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree52(features, 47);
    } else if (val <= 829.0000000000001) {
      return calculateTree52(features, 47);
    } else {
      return calculateTree52(features, 48);
    }
  }
  if (node === 49) return 0.09371708365137267;
  if (node === 101) return -0.32238889658306935;
  if (node === 205) return -0.3287756162731342;
  if (node === 206) return 0.42455950828082595;
  if (node === 102) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree52(features, 205);
    } else if (val <= 800.5000000000001) {
      return calculateTree52(features, 205);
    } else {
      return calculateTree52(features, 206);
    }
  }
  if (node === 50) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree52(features, 101);
    } else if (val <= 518.5000000000001) {
      return calculateTree52(features, 101);
    } else {
      return calculateTree52(features, 102);
    }
  }
  if (node === 24) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree52(features, 49);
    } else if (val <= 117.50000000000001) {
      return calculateTree52(features, 49);
    } else {
      return calculateTree52(features, 50);
    }
  }
  if (node === 11) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree52(features, 23);
    } else if (val <= 481.50000000000006) {
      return calculateTree52(features, 23);
    } else {
      return calculateTree52(features, 24);
    }
  }
  if (node === 25) return -0.3011696298168897;
  if (node === 53) return -0.3766853738927505;
  if (node === 219) return -0.43790300915136654;
  if (node === 220) return 0.2937301937485115;
  if (node === 109) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree52(features, 219);
    } else if (val <= 481.50000000000006) {
      return calculateTree52(features, 219);
    } else {
      return calculateTree52(features, 220);
    }
  }
  if (node === 221) return -0.37413654913161226;
  if (node === 1783) return -0.04057096980825959;
  if (node === 1784) return 0.10417768411382923;
  if (node === 891) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree52(features, 1783);
    } else if (val <= 503.50000000000006) {
      return calculateTree52(features, 1783);
    } else {
      return calculateTree52(features, 1784);
    }
  }
  if (node === 892) return -0.41127162964140335;
  if (node === 445) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree52(features, 891);
    } else if (val <= 799.5000000000001) {
      return calculateTree52(features, 891);
    } else {
      return calculateTree52(features, 892);
    }
  }
  if (node === 893) return 0.25716864006885787;
  if (node === 1789) return 0.12874651161583248;
  if (node === 1790) return -0.028319083046027625;
  if (node === 894) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree52(features, 1789);
    } else if (val <= 29.500000000000004) {
      return calculateTree52(features, 1789);
    } else {
      return calculateTree52(features, 1790);
    }
  }
  if (node === 446) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree52(features, 893);
    } else if (val <= 485.50000000000006) {
      return calculateTree52(features, 893);
    } else {
      return calculateTree52(features, 894);
    }
  }
  if (node === 222) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree52(features, 445);
    } else if (val <= 851.5000000000001) {
      return calculateTree52(features, 445);
    } else {
      return calculateTree52(features, 446);
    }
  }
  if (node === 110) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree52(features, 221);
    } else if (val <= 316.50000000000006) {
      return calculateTree52(features, 221);
    } else {
      return calculateTree52(features, 222);
    }
  }
  if (node === 54) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree52(features, 109);
    } else if (val <= 290.50000000000006) {
      return calculateTree52(features, 109);
    } else {
      return calculateTree52(features, 110);
    }
  }
  if (node === 26) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree52(features, 53);
    } else if (val <= 117.50000000000001) {
      return calculateTree52(features, 53);
    } else {
      return calculateTree52(features, 54);
    }
  }
  if (node === 12) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree52(features, 25);
    } else if (val <= 472.50000000000006) {
      return calculateTree52(features, 25);
    } else {
      return calculateTree52(features, 26);
    }
  }
  if (node === 5) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree52(features, 11);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree52(features, 11);
    } else {
      return calculateTree52(features, 12);
    }
  }
  if (node === 27) return 0.02453712410925985;
  if (node === 28) return -0.3462785555861356;
  if (node === 13) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree52(features, 27);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree52(features, 27);
    } else {
      return calculateTree52(features, 28);
    }
  }
  if (node === 14) return -0.0012374164072115996;
  if (node === 6) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree52(features, 13);
    } else if (val <= 539.5000000000001) {
      return calculateTree52(features, 13);
    } else {
      return calculateTree52(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree52(features, 5);
    } else if (val <= 525.0000000000001) {
      return calculateTree52(features, 5);
    } else {
      return calculateTree52(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree52(features, 1);
    } else if (val <= 470.50000000000006) {
      return calculateTree52(features, 1);
    } else {
      return calculateTree52(features, 2);
    }
  }
}
function calculateTree53(features, node = 0) {
  if (node === 1) return 0.0031712272792252913;
  if (node === 47) return 0.15907130101804082;
  if (node === 48) return -0.3646096487442529;
  if (node === 23) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree53(features, 47);
    } else if (val <= 171.00000000000003) {
      return calculateTree53(features, 47);
    } else {
      return calculateTree53(features, 48);
    }
  }
  if (node === 49) return 0.05177303302805168;
  if (node === 50) return 0.3848219874431249;
  if (node === 24) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree53(features, 49);
    } else if (val <= 877.5000000000001) {
      return calculateTree53(features, 49);
    } else {
      return calculateTree53(features, 50);
    }
  }
  if (node === 11) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree53(features, 23);
    } else if (val <= 712.5000000000001) {
      return calculateTree53(features, 23);
    } else {
      return calculateTree53(features, 24);
    }
  }
  if (node === 51) return -0.3153546412232187;
  if (node === 105) return 0.21310988856069366;
  if (node === 106) return -0.15293961567532446;
  if (node === 52) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree53(features, 105);
    } else if (val <= 677.5000000000001) {
      return calculateTree53(features, 105);
    } else {
      return calculateTree53(features, 106);
    }
  }
  if (node === 25) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree53(features, 51);
    } else if (val <= 447.00000000000006) {
      return calculateTree53(features, 51);
    } else {
      return calculateTree53(features, 52);
    }
  }
  if (node === 26) return -0.3570189228797618;
  if (node === 12) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree53(features, 25);
    } else if (val <= 682.5000000000001) {
      return calculateTree53(features, 25);
    } else {
      return calculateTree53(features, 26);
    }
  }
  if (node === 5) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree53(features, 11);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree53(features, 11);
    } else {
      return calculateTree53(features, 12);
    }
  }
  if (node === 55) return -0.5237736183463223;
  if (node === 56) return 0.4320839691284683;
  if (node === 27) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree53(features, 55);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree53(features, 55);
    } else {
      return calculateTree53(features, 56);
    }
  }
  if (node === 28) return -0.02011301230630071;
  if (node === 13) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree53(features, 27);
    } else if (val <= 776.0000000000001) {
      return calculateTree53(features, 27);
    } else {
      return calculateTree53(features, 28);
    }
  }
  if (node === 119) return -0.3899280353740643;
  if (node === 241) return 0.19913362902727474;
  if (node === 485) return -0.25830191210729386;
  if (node === 486) return 0.06631720415971355;
  if (node === 242) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree53(features, 485);
    } else if (val <= 715.5000000000001) {
      return calculateTree53(features, 485);
    } else {
      return calculateTree53(features, 486);
    }
  }
  if (node === 120) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree53(features, 241);
    } else if (val <= 587.5000000000001) {
      return calculateTree53(features, 241);
    } else {
      return calculateTree53(features, 242);
    }
  }
  if (node === 59) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree53(features, 119);
    } else if (val <= 470.00000000000006) {
      return calculateTree53(features, 119);
    } else {
      return calculateTree53(features, 120);
    }
  }
  if (node === 60) return -0.32937594114069735;
  if (node === 29) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree53(features, 59);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree53(features, 59);
    } else {
      return calculateTree53(features, 60);
    }
  }
  if (node === 495) return -0.3762312917981821;
  if (node === 496) return 0.23718223555016377;
  if (node === 247) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree53(features, 495);
    } else if (val <= 175.50000000000003) {
      return calculateTree53(features, 495);
    } else {
      return calculateTree53(features, 496);
    }
  }
  if (node === 248) return -0.31523142883239413;
  if (node === 123) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree53(features, 247);
    } else if (val <= 225.50000000000003) {
      return calculateTree53(features, 247);
    } else {
      return calculateTree53(features, 248);
    }
  }
  if (node === 249) return 0.5516807907516269;
  if (node === 501) return -0.34811962927300116;
  if (node === 1005) return 0.039135258927994486;
  if (node === 2013) return 0.15445263654129943;
  if (node === 2014) return -0.319129616632258;
  if (node === 1006) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree53(features, 2013);
    } else if (val <= 678.5000000000001) {
      return calculateTree53(features, 2013);
    } else {
      return calculateTree53(features, 2014);
    }
  }
  if (node === 502) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree53(features, 1005);
    } else if (val <= 314.50000000000006) {
      return calculateTree53(features, 1005);
    } else {
      return calculateTree53(features, 1006);
    }
  }
  if (node === 250) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree53(features, 501);
    } else if (val <= 225.50000000000003) {
      return calculateTree53(features, 501);
    } else {
      return calculateTree53(features, 502);
    }
  }
  if (node === 124) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree53(features, 249);
    } else if (val <= 175.50000000000003) {
      return calculateTree53(features, 249);
    } else {
      return calculateTree53(features, 250);
    }
  }
  if (node === 61) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree53(features, 123);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree53(features, 123);
    } else {
      return calculateTree53(features, 124);
    }
  }
  if (node === 251) return 0.08643219750838922;
  if (node === 252) return -0.3722598789809644;
  if (node === 125) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree53(features, 251);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree53(features, 251);
    } else {
      return calculateTree53(features, 252);
    }
  }
  if (node === 507) return -0.11942450434235674;
  if (node === 2035) return 0.08177878191358644;
  if (node === 2036) return -0.17717784196001782;
  if (node === 1017) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree53(features, 2035);
    } else if (val <= 338.50000000000006) {
      return calculateTree53(features, 2035);
    } else {
      return calculateTree53(features, 2036);
    }
  }
  if (node === 1018) return 0.10195096828351599;
  if (node === 508) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree53(features, 1017);
    } else if (val <= 350.50000000000006) {
      return calculateTree53(features, 1017);
    } else {
      return calculateTree53(features, 1018);
    }
  }
  if (node === 253) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree53(features, 507);
    } else if (val <= 181.50000000000003) {
      return calculateTree53(features, 507);
    } else {
      return calculateTree53(features, 508);
    }
  }
  if (node === 254) return -0.0022540697486261026;
  if (node === 126) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree53(features, 253);
    } else if (val <= 365.50000000000006) {
      return calculateTree53(features, 253);
    } else {
      return calculateTree53(features, 254);
    }
  }
  if (node === 62) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree53(features, 125);
    } else if (val <= 331.50000000000006) {
      return calculateTree53(features, 125);
    } else {
      return calculateTree53(features, 126);
    }
  }
  if (node === 30) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree53(features, 61);
    } else if (val <= 322.00000000000006) {
      return calculateTree53(features, 61);
    } else {
      return calculateTree53(features, 62);
    }
  }
  if (node === 14) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree53(features, 29);
    } else if (val <= 311.50000000000006) {
      return calculateTree53(features, 29);
    } else {
      return calculateTree53(features, 30);
    }
  }
  if (node === 6) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree53(features, 13);
    } else if (val <= 294.50000000000006) {
      return calculateTree53(features, 13);
    } else {
      return calculateTree53(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree53(features, 5);
    } else if (val <= 291.50000000000006) {
      return calculateTree53(features, 5);
    } else {
      return calculateTree53(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree53(features, 1);
    } else if (val <= 282.50000000000006) {
      return calculateTree53(features, 1);
    } else {
      return calculateTree53(features, 2);
    }
  }
}
function calculateTree54(features, node = 0) {
  if (node === 3) return -0.0010041334968101943;
  if (node === 79) return 0.03750635878202489;
  if (node === 80) return 0.18136224843371432;
  if (node === 39) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree54(features, 79);
    } else if (val <= 143.50000000000003) {
      return calculateTree54(features, 79);
    } else {
      return calculateTree54(features, 80);
    }
  }
  if (node === 40) return -0.1363284226681446;
  if (node === 19) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree54(features, 39);
    } else if (val <= 897.0000000000001) {
      return calculateTree54(features, 39);
    } else {
      return calculateTree54(features, 40);
    }
  }
  if (node === 20) return -0.32743181665843685;
  if (node === 9) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree54(features, 19);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree54(features, 19);
    } else {
      return calculateTree54(features, 20);
    }
  }
  if (node === 21) return -0.32119524664873805;
  if (node === 367) return 0.058909731837270655;
  if (node === 368) return 0.39963340621350313;
  if (node === 183) {
    const val = features['is_preference_men_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree54(features, 367);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree54(features, 367);
    } else {
      return calculateTree54(features, 368);
    }
  }
  if (node === 369) return -0.1377423517696395;
  if (node === 370) return 0.038196123471365286;
  if (node === 184) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree54(features, 369);
    } else if (val <= 25.500000000000004) {
      return calculateTree54(features, 369);
    } else {
      return calculateTree54(features, 370);
    }
  }
  if (node === 91) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree54(features, 183);
    } else if (val <= 13.500000000000002) {
      return calculateTree54(features, 183);
    } else {
      return calculateTree54(features, 184);
    }
  }
  if (node === 92) return 0.6286461479398671;
  if (node === 45) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree54(features, 91);
    } else if (val <= 941.5000000000001) {
      return calculateTree54(features, 91);
    } else {
      return calculateTree54(features, 92);
    }
  }
  if (node === 93) return 0.21384183050370528;
  if (node === 94) return 0.053611278443721024;
  if (node === 46) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree54(features, 93);
    } else if (val <= 9.500000000000002) {
      return calculateTree54(features, 93);
    } else {
      return calculateTree54(features, 94);
    }
  }
  if (node === 22) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree54(features, 45);
    } else if (val <= 34.50000000000001) {
      return calculateTree54(features, 45);
    } else {
      return calculateTree54(features, 46);
    }
  }
  if (node === 10) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree54(features, 21);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree54(features, 21);
    } else {
      return calculateTree54(features, 22);
    }
  }
  if (node === 4) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree54(features, 9);
    } else if (val <= 365.50000000000006) {
      return calculateTree54(features, 9);
    } else {
      return calculateTree54(features, 10);
    }
  }
  if (node === 1) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree54(features, 3);
    } else if (val <= 937.5000000000001) {
      return calculateTree54(features, 3);
    } else {
      return calculateTree54(features, 4);
    }
  }
  if (node === 23) return -0.019437075372070046;
  if (node === 24) return -0.32339289731362303;
  if (node === 11) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree54(features, 23);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree54(features, 23);
    } else {
      return calculateTree54(features, 24);
    }
  }
  if (node === 25) return -0.0011562877250930084;
  if (node === 53) return 0.23645781232071195;
  if (node === 54) return 0.04728341964755625;
  if (node === 26) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree54(features, 53);
    } else if (val <= 17.500000000000004) {
      return calculateTree54(features, 53);
    } else {
      return calculateTree54(features, 54);
    }
  }
  if (node === 12) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree54(features, 25);
    } else if (val <= 888.5000000000001) {
      return calculateTree54(features, 25);
    } else {
      return calculateTree54(features, 26);
    }
  }
  if (node === 5) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree54(features, 11);
    } else if (val <= 953.5000000000001) {
      return calculateTree54(features, 11);
    } else {
      return calculateTree54(features, 12);
    }
  }
  if (node === 447) return -0.08384845187846804;
  if (node === 448) return 0.17923682689732615;
  if (node === 223) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree54(features, 447);
    } else if (val <= 763.5000000000001) {
      return calculateTree54(features, 447);
    } else {
      return calculateTree54(features, 448);
    }
  }
  if (node === 224) return 0.11682687564187871;
  if (node === 111) {
    const val = features['is_preference_women_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree54(features, 223);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree54(features, 223);
    } else {
      return calculateTree54(features, 224);
    }
  }
  if (node === 112) return -0.26738582156323565;
  if (node === 55) {
    const val = features['is_preference_sports'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree54(features, 111);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree54(features, 111);
    } else {
      return calculateTree54(features, 112);
    }
  }
  if (node === 113) return -0.04011448669254938;
  if (node === 459) return -0.28103291910260336;
  if (node === 460) return 0.03476753311523384;
  if (node === 229) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree54(features, 459);
    } else if (val <= 61.50000000000001) {
      return calculateTree54(features, 459);
    } else {
      return calculateTree54(features, 460);
    }
  }
  if (node === 230) return 0.101050652850153;
  if (node === 114) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree54(features, 229);
    } else if (val <= 24.500000000000004) {
      return calculateTree54(features, 229);
    } else {
      return calculateTree54(features, 230);
    }
  }
  if (node === 56) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree54(features, 113);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree54(features, 113);
    } else {
      return calculateTree54(features, 114);
    }
  }
  if (node === 27) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree54(features, 55);
    } else if (val <= 963.0000000000001) {
      return calculateTree54(features, 55);
    } else {
      return calculateTree54(features, 56);
    }
  }
  if (node === 28) return -0.005227790702607439;
  if (node === 13) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree54(features, 27);
    } else if (val <= 973.5000000000001) {
      return calculateTree54(features, 27);
    } else {
      return calculateTree54(features, 28);
    }
  }
  if (node === 59) return -0.22622193758362957;
  if (node === 121) return 0.0033265217158001546;
  if (node === 122) return -0.14766585566980117;
  if (node === 60) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree54(features, 121);
    } else if (val <= 835.5000000000001) {
      return calculateTree54(features, 121);
    } else {
      return calculateTree54(features, 122);
    }
  }
  if (node === 29) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree54(features, 59);
    } else if (val <= 32.50000000000001) {
      return calculateTree54(features, 59);
    } else {
      return calculateTree54(features, 60);
    }
  }
  if (node === 61) return 0.22716984884451208;
  if (node === 62) return 0.04372069548983578;
  if (node === 30) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree54(features, 61);
    } else if (val <= 311.50000000000006) {
      return calculateTree54(features, 61);
    } else {
      return calculateTree54(features, 62);
    }
  }
  if (node === 14) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree54(features, 29);
    } else if (val <= 25.500000000000004) {
      return calculateTree54(features, 29);
    } else {
      return calculateTree54(features, 30);
    }
  }
  if (node === 6) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree54(features, 13);
    } else if (val <= 197.50000000000003) {
      return calculateTree54(features, 13);
    } else {
      return calculateTree54(features, 14);
    }
  }
  if (node === 2) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree54(features, 5);
    } else if (val <= 31.500000000000004) {
      return calculateTree54(features, 5);
    } else {
      return calculateTree54(features, 6);
    }
  }
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree54(features, 1);
    } else if (val <= 944.5000000000001) {
      return calculateTree54(features, 1);
    } else {
      return calculateTree54(features, 2);
    }
  }
}
function calculateTree55(features, node = 0) {
  if (node === 63) return 0.05412000036875242;
  if (node === 129) return -0.2615326592815967;
  if (node === 130) return -0.03444790225348719;
  if (node === 64) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree55(features, 129);
    } else if (val <= 19.000000000000004) {
      return calculateTree55(features, 129);
    } else {
      return calculateTree55(features, 130);
    }
  }
  if (node === 31) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree55(features, 63);
    } else if (val <= 30.500000000000004) {
      return calculateTree55(features, 63);
    } else {
      return calculateTree55(features, 64);
    }
  }
  if (node === 131) return 0.004337453535637753;
  if (node === 132) return -0.3405027888501276;
  if (node === 65) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree55(features, 131);
    } else if (val <= 188.00000000000003) {
      return calculateTree55(features, 131);
    } else {
      return calculateTree55(features, 132);
    }
  }
  if (node === 133) return -0.053384723126597575;
  if (node === 134) return -0.005079518048209743;
  if (node === 66) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree55(features, 133);
    } else if (val <= 141.50000000000003) {
      return calculateTree55(features, 133);
    } else {
      return calculateTree55(features, 134);
    }
  }
  if (node === 32) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree55(features, 65);
    } else if (val <= 82.50000000000001) {
      return calculateTree55(features, 65);
    } else {
      return calculateTree55(features, 66);
    }
  }
  if (node === 15) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree55(features, 31);
    } else if (val <= 34.00000000000001) {
      return calculateTree55(features, 31);
    } else {
      return calculateTree55(features, 32);
    }
  }
  if (node === 67) return 0.07076973827118109;
  if (node === 137) return -0.1301950724407413;
  if (node === 138) return 0.07296975962480824;
  if (node === 68) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree55(features, 137);
    } else if (val <= 877.5000000000001) {
      return calculateTree55(features, 137);
    } else {
      return calculateTree55(features, 138);
    }
  }
  if (node === 33) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree55(features, 67);
    } else if (val <= 330.00000000000006) {
      return calculateTree55(features, 67);
    } else {
      return calculateTree55(features, 68);
    }
  }
  if (node === 34) return 0.10260833801181461;
  if (node === 16) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree55(features, 33);
    } else if (val <= 135.50000000000003) {
      return calculateTree55(features, 33);
    } else {
      return calculateTree55(features, 34);
    }
  }
  if (node === 7) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree55(features, 15);
    } else if (val <= 20.500000000000004) {
      return calculateTree55(features, 15);
    } else {
      return calculateTree55(features, 16);
    }
  }
  if (node === 71) return 0.8582487888004291;
  if (node === 72) return -0.26950234082051694;
  if (node === 35) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree55(features, 71);
    } else if (val <= 181.50000000000003) {
      return calculateTree55(features, 71);
    } else {
      return calculateTree55(features, 72);
    }
  }
  if (node === 36) return -0.39686013023792743;
  if (node === 17) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree55(features, 35);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree55(features, 35);
    } else {
      return calculateTree55(features, 36);
    }
  }
  if (node === 18) return -0.054124305544688375;
  if (node === 8) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree55(features, 17);
    } else if (val <= 705.5000000000001) {
      return calculateTree55(features, 17);
    } else {
      return calculateTree55(features, 18);
    }
  }
  if (node === 3) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree55(features, 7);
    } else if (val <= 205.50000000000003) {
      return calculateTree55(features, 7);
    } else {
      return calculateTree55(features, 8);
    }
  }
  if (node === 19) return -0.3478641322409293;
  if (node === 41) return 0.11666447143121235;
  if (node === 42) return 0.25576879445017514;
  if (node === 20) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree55(features, 41);
    } else if (val <= 24.500000000000004) {
      return calculateTree55(features, 41);
    } else {
      return calculateTree55(features, 42);
    }
  }
  if (node === 9) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree55(features, 19);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree55(features, 19);
    } else {
      return calculateTree55(features, 20);
    }
  }
  if (node === 10) return 0.0005973439815868894;
  if (node === 4) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree55(features, 9);
    } else if (val <= 208.50000000000003) {
      return calculateTree55(features, 9);
    } else {
      return calculateTree55(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree55(features, 3);
    } else if (val <= 207.00000000000003) {
      return calculateTree55(features, 3);
    } else {
      return calculateTree55(features, 4);
    }
  }
  if (node === 11) return -0.29610001391282187;
  if (node === 12) return 0.0588419118116756;
  if (node === 5) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree55(features, 11);
    } else if (val <= 973.5000000000001) {
      return calculateTree55(features, 11);
    } else {
      return calculateTree55(features, 12);
    }
  }
  if (node === 27) return 0.0081248837314889;
  if (node === 57) return -0.31622721210066895;
  if (node === 117) return 0.1233480195062803;
  if (node === 475) return -0.30858570393199786;
  if (node === 476) return 0.0711025628373014;
  if (node === 237) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree55(features, 475);
    } else if (val <= 725.5000000000001) {
      return calculateTree55(features, 475);
    } else {
      return calculateTree55(features, 476);
    }
  }
  if (node === 1911) return 0.050334501611284284;
  if (node === 1912) return 0.22994915737810445;
  if (node === 955) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree55(features, 1911);
    } else if (val <= 905.5000000000001) {
      return calculateTree55(features, 1911);
    } else {
      return calculateTree55(features, 1912);
    }
  }
  if (node === 956) return -0.019472006742099673;
  if (node === 477) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree55(features, 955);
    } else if (val <= 1.5000000000000002) {
      return calculateTree55(features, 955);
    } else {
      return calculateTree55(features, 956);
    }
  }
  if (node === 957) return -0.20293874286995806;
  if (node === 958) return 0.06382066791025012;
  if (node === 478) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree55(features, 957);
    } else if (val <= 25.500000000000004) {
      return calculateTree55(features, 957);
    } else {
      return calculateTree55(features, 958);
    }
  }
  if (node === 238) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree55(features, 477);
    } else if (val <= 37.50000000000001) {
      return calculateTree55(features, 477);
    } else {
      return calculateTree55(features, 478);
    }
  }
  if (node === 118) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree55(features, 237);
    } else if (val <= 654.0000000000001) {
      return calculateTree55(features, 237);
    } else {
      return calculateTree55(features, 238);
    }
  }
  if (node === 58) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree55(features, 117);
    } else if (val <= 582.5000000000001) {
      return calculateTree55(features, 117);
    } else {
      return calculateTree55(features, 118);
    }
  }
  if (node === 28) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree55(features, 57);
    } else if (val <= 551.5000000000001) {
      return calculateTree55(features, 57);
    } else {
      return calculateTree55(features, 58);
    }
  }
  if (node === 13) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree55(features, 27);
    } else if (val <= 325.00000000000006) {
      return calculateTree55(features, 27);
    } else {
      return calculateTree55(features, 28);
    }
  }
  if (node === 14) return -0.30664486064404817;
  if (node === 6) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree55(features, 13);
    } else if (val <= 908.5000000000001) {
      return calculateTree55(features, 13);
    } else {
      return calculateTree55(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree55(features, 5);
    } else if (val <= 253.50000000000003) {
      return calculateTree55(features, 5);
    } else {
      return calculateTree55(features, 6);
    }
  }
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree55(features, 1);
    } else if (val <= 969.5000000000001) {
      return calculateTree55(features, 1);
    } else {
      return calculateTree55(features, 2);
    }
  }
}
function calculateTree56(features, node = 0) {
  if (node === 127) return 0.017530178303703518;
  if (node === 128) return -0.2925843700956773;
  if (node === 63) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree56(features, 127);
    } else if (val <= 358.50000000000006) {
      return calculateTree56(features, 127);
    } else {
      return calculateTree56(features, 128);
    }
  }
  if (node === 129) return 0.21573742049179243;
  if (node === 130) return -0.26185237857637206;
  if (node === 64) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree56(features, 129);
    } else if (val <= 44.50000000000001) {
      return calculateTree56(features, 129);
    } else {
      return calculateTree56(features, 130);
    }
  }
  if (node === 31) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree56(features, 63);
    } else if (val <= 409.50000000000006) {
      return calculateTree56(features, 63);
    } else {
      return calculateTree56(features, 64);
    }
  }
  if (node === 65) return -0.31990150050495764;
  if (node === 66) return -0.0022075371896978857;
  if (node === 32) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree56(features, 65);
    } else if (val <= 432.00000000000006) {
      return calculateTree56(features, 65);
    } else {
      return calculateTree56(features, 66);
    }
  }
  if (node === 15) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree56(features, 31);
    } else if (val <= 422.00000000000006) {
      return calculateTree56(features, 31);
    } else {
      return calculateTree56(features, 32);
    }
  }
  if (node === 16) return 0.10251063980109905;
  if (node === 7) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree56(features, 15);
    } else if (val <= 877.5000000000001) {
      return calculateTree56(features, 15);
    } else {
      return calculateTree56(features, 16);
    }
  }
  if (node === 71) return -0.013710876036698228;
  if (node === 145) return -0.366869330306685;
  if (node === 146) return 0.05193156050485415;
  if (node === 72) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree56(features, 145);
    } else if (val <= 771.0000000000001) {
      return calculateTree56(features, 145);
    } else {
      return calculateTree56(features, 146);
    }
  }
  if (node === 35) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree56(features, 71);
    } else if (val <= 679.5000000000001) {
      return calculateTree56(features, 71);
    } else {
      return calculateTree56(features, 72);
    }
  }
  if (node === 36) return 0.2976376291969439;
  if (node === 17) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree56(features, 35);
    } else if (val <= 808.5000000000001) {
      return calculateTree56(features, 35);
    } else {
      return calculateTree56(features, 36);
    }
  }
  if (node === 18) return -0.31034267819139055;
  if (node === 8) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree56(features, 17);
    } else if (val <= 835.5000000000001) {
      return calculateTree56(features, 17);
    } else {
      return calculateTree56(features, 18);
    }
  }
  if (node === 3) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree56(features, 7);
    } else if (val <= 164.50000000000003) {
      return calculateTree56(features, 7);
    } else {
      return calculateTree56(features, 8);
    }
  }
  if (node === 9) return -0.24594797967071946;
  if (node === 43) return -0.04811823195242046;
  if (node === 44) return 0.07917000111133417;
  if (node === 21) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree56(features, 43);
    } else if (val <= 35.50000000000001) {
      return calculateTree56(features, 43);
    } else {
      return calculateTree56(features, 44);
    }
  }
  if (node === 22) return -0.3281506441301551;
  if (node === 10) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree56(features, 21);
    } else if (val <= 143.50000000000003) {
      return calculateTree56(features, 21);
    } else {
      return calculateTree56(features, 22);
    }
  }
  if (node === 4) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree56(features, 9);
    } else if (val <= 898.5000000000001) {
      return calculateTree56(features, 9);
    } else {
      return calculateTree56(features, 10);
    }
  }
  if (node === 1) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree56(features, 3);
    } else if (val <= 893.0000000000001) {
      return calculateTree56(features, 3);
    } else {
      return calculateTree56(features, 4);
    }
  }
  if (node === 191) return -0.058406626056831175;
  if (node === 192) return 0.09445163276827037;
  if (node === 95) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree56(features, 191);
    } else if (val <= 271.00000000000006) {
      return calculateTree56(features, 191);
    } else {
      return calculateTree56(features, 192);
    }
  }
  if (node === 96) return -0.28577705114153684;
  if (node === 47) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree56(features, 95);
    } else if (val <= 240.00000000000003) {
      return calculateTree56(features, 95);
    } else {
      return calculateTree56(features, 96);
    }
  }
  if (node === 48) return 0.041427836151660596;
  if (node === 23) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree56(features, 47);
    } else if (val <= 276.00000000000006) {
      return calculateTree56(features, 47);
    } else {
      return calculateTree56(features, 48);
    }
  }
  if (node === 49) return -0.024057975588529224;
  if (node === 50) return -0.3301850619792423;
  if (node === 24) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree56(features, 49);
    } else if (val <= 253.50000000000003) {
      return calculateTree56(features, 49);
    } else {
      return calculateTree56(features, 50);
    }
  }
  if (node === 11) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree56(features, 23);
    } else if (val <= 542.0000000000001) {
      return calculateTree56(features, 23);
    } else {
      return calculateTree56(features, 24);
    }
  }
  if (node === 103) return 0.09485329226613551;
  if (node === 419) return -0.01972338749700638;
  if (node === 420) return -0.4056008061896396;
  if (node === 209) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree56(features, 419);
    } else if (val <= 839.5000000000001) {
      return calculateTree56(features, 419);
    } else {
      return calculateTree56(features, 420);
    }
  }
  if (node === 210) return 0.05245820097136688;
  if (node === 104) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree56(features, 209);
    } else if (val <= 893.0000000000001) {
      return calculateTree56(features, 209);
    } else {
      return calculateTree56(features, 210);
    }
  }
  if (node === 51) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree56(features, 103);
    } else if (val <= 799.5000000000001) {
      return calculateTree56(features, 103);
    } else {
      return calculateTree56(features, 104);
    }
  }
  if (node === 52) return -0.08528136470927188;
  if (node === 25) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree56(features, 51);
    } else if (val <= 253.50000000000003) {
      return calculateTree56(features, 51);
    } else {
      return calculateTree56(features, 52);
    }
  }
  if (node === 26) return 0.12207812321917169;
  if (node === 12) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree56(features, 25);
    } else if (val <= 278.50000000000006) {
      return calculateTree56(features, 25);
    } else {
      return calculateTree56(features, 26);
    }
  }
  if (node === 5) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree56(features, 11);
    } else if (val <= 708.5000000000001) {
      return calculateTree56(features, 11);
    } else {
      return calculateTree56(features, 12);
    }
  }
  if (node === 13) return -0.07193326156423596;
  if (node === 59) return 0.2279039833799503;
  if (node === 60) return -0.07401385889082931;
  if (node === 29) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree56(features, 59);
    } else if (val <= 26.500000000000004) {
      return calculateTree56(features, 59);
    } else {
      return calculateTree56(features, 60);
    }
  }
  if (node === 30) return -0.0008880467536282288;
  if (node === 14) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree56(features, 29);
    } else if (val <= 294.50000000000006) {
      return calculateTree56(features, 29);
    } else {
      return calculateTree56(features, 30);
    }
  }
  if (node === 6) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree56(features, 13);
    } else if (val <= 291.50000000000006) {
      return calculateTree56(features, 13);
    } else {
      return calculateTree56(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree56(features, 5);
    } else if (val <= 282.50000000000006) {
      return calculateTree56(features, 5);
    } else {
      return calculateTree56(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree56(features, 1);
    } else if (val <= 191.50000000000003) {
      return calculateTree56(features, 1);
    } else {
      return calculateTree56(features, 2);
    }
  }
}
function calculateTree57(features, node = 0) {
  if (node === 1) return 0.0013223528505149533;
  if (node === 191) return 0.1380171393054239;
  if (node === 192) return -0.35997180122180455;
  if (node === 95) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree57(features, 191);
    } else if (val <= 127.00000000000001) {
      return calculateTree57(features, 191);
    } else {
      return calculateTree57(features, 192);
    }
  }
  if (node === 193) return -0.11990552396080602;
  if (node === 194) return 0.05562750948369279;
  if (node === 96) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree57(features, 193);
    } else if (val <= 387.50000000000006) {
      return calculateTree57(features, 193);
    } else {
      return calculateTree57(features, 194);
    }
  }
  if (node === 47) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree57(features, 95);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree57(features, 95);
    } else {
      return calculateTree57(features, 96);
    }
  }
  if (node === 48) return -0.3277770129906996;
  if (node === 23) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree57(features, 47);
    } else if (val <= 455.50000000000006) {
      return calculateTree57(features, 47);
    } else {
      return calculateTree57(features, 48);
    }
  }
  if (node === 49) return 0.11713654050935324;
  if (node === 50) return -0.21812261501379726;
  if (node === 24) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree57(features, 49);
    } else if (val <= 960.0000000000001) {
      return calculateTree57(features, 49);
    } else {
      return calculateTree57(features, 50);
    }
  }
  if (node === 11) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree57(features, 23);
    } else if (val <= 733.5000000000001) {
      return calculateTree57(features, 23);
    } else {
      return calculateTree57(features, 24);
    }
  }
  if (node === 51) return 0.08748436487406536;
  if (node === 52) return 0.47094545980415065;
  if (node === 25) {
    const val = features['is_preference_household'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree57(features, 51);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree57(features, 51);
    } else {
      return calculateTree57(features, 52);
    }
  }
  if (node === 26) return -0.13460301762134294;
  if (node === 12) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree57(features, 25);
    } else if (val <= 455.50000000000006) {
      return calculateTree57(features, 25);
    } else {
      return calculateTree57(features, 26);
    }
  }
  if (node === 5) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree57(features, 11);
    } else if (val <= 17.500000000000004) {
      return calculateTree57(features, 11);
    } else {
      return calculateTree57(features, 12);
    }
  }
  if (node === 223) return -0.004049995038549045;
  if (node === 224) return -0.24941457638918063;
  if (node === 111) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree57(features, 223);
    } else if (val <= 443.50000000000006) {
      return calculateTree57(features, 223);
    } else {
      return calculateTree57(features, 224);
    }
  }
  if (node === 451) return 0.18847376671591767;
  if (node === 905) return -0.31549128896825235;
  if (node === 906) return 0.1645925262421812;
  if (node === 452) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree57(features, 905);
    } else if (val <= 518.5000000000001) {
      return calculateTree57(features, 905);
    } else {
      return calculateTree57(features, 906);
    }
  }
  if (node === 225) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree57(features, 451);
    } else if (val <= 472.50000000000006) {
      return calculateTree57(features, 451);
    } else {
      return calculateTree57(features, 452);
    }
  }
  if (node === 453) return -0.3642735327230112;
  if (node === 909) return 0.07740892704510337;
  if (node === 910) return -0.36409835337795793;
  if (node === 454) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree57(features, 909);
    } else if (val <= 518.5000000000001) {
      return calculateTree57(features, 909);
    } else {
      return calculateTree57(features, 910);
    }
  }
  if (node === 226) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree57(features, 453);
    } else if (val <= 472.50000000000006) {
      return calculateTree57(features, 453);
    } else {
      return calculateTree57(features, 454);
    }
  }
  if (node === 112) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree57(features, 225);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree57(features, 225);
    } else {
      return calculateTree57(features, 226);
    }
  }
  if (node === 55) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree57(features, 111);
    } else if (val <= 470.50000000000006) {
      return calculateTree57(features, 111);
    } else {
      return calculateTree57(features, 112);
    }
  }
  if (node === 227) return 0.529868700460889;
  if (node === 457) return -0.35594460081820556;
  if (node === 917) return -0.28733287076219544;
  if (node === 918) return 0.1601609993259395;
  if (node === 458) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree57(features, 917);
    } else if (val <= 531.5000000000001) {
      return calculateTree57(features, 917);
    } else {
      return calculateTree57(features, 918);
    }
  }
  if (node === 228) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree57(features, 457);
    } else if (val <= 718.5000000000001) {
      return calculateTree57(features, 457);
    } else {
      return calculateTree57(features, 458);
    }
  }
  if (node === 113) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree57(features, 227);
    } else if (val <= 708.5000000000001) {
      return calculateTree57(features, 227);
    } else {
      return calculateTree57(features, 228);
    }
  }
  if (node === 114) return -0.3199890501952109;
  if (node === 56) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree57(features, 113);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree57(features, 113);
    } else {
      return calculateTree57(features, 114);
    }
  }
  if (node === 27) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree57(features, 55);
    } else if (val <= 522.5000000000001) {
      return calculateTree57(features, 55);
    } else {
      return calculateTree57(features, 56);
    }
  }
  if (node === 57) return -0.3336433423996075;
  if (node === 58) return 0.19771829710715683;
  if (node === 28) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree57(features, 57);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree57(features, 57);
    } else {
      return calculateTree57(features, 58);
    }
  }
  if (node === 13) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree57(features, 27);
    } else if (val <= 551.5000000000001) {
      return calculateTree57(features, 27);
    } else {
      return calculateTree57(features, 28);
    }
  }
  if (node === 119) return 0.08610876430459649;
  if (node === 120) return -0.30680057549796463;
  if (node === 59) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree57(features, 119);
    } else if (val <= 566.0000000000001) {
      return calculateTree57(features, 119);
    } else {
      return calculateTree57(features, 120);
    }
  }
  if (node === 60) return -0.2518502531247139;
  if (node === 29) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree57(features, 59);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree57(features, 59);
    } else {
      return calculateTree57(features, 60);
    }
  }
  if (node === 61) return 0.13546764157959423;
  if (node === 125) return -0.33082234483470707;
  if (node === 126) return -0.007532826968012753;
  if (node === 62) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree57(features, 125);
    } else if (val <= 635.0000000000001) {
      return calculateTree57(features, 125);
    } else {
      return calculateTree57(features, 126);
    }
  }
  if (node === 30) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree57(features, 61);
    } else if (val <= 625.0000000000001) {
      return calculateTree57(features, 61);
    } else {
      return calculateTree57(features, 62);
    }
  }
  if (node === 14) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree57(features, 29);
    } else if (val <= 606.5000000000001) {
      return calculateTree57(features, 29);
    } else {
      return calculateTree57(features, 30);
    }
  }
  if (node === 6) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree57(features, 13);
    } else if (val <= 555.5000000000001) {
      return calculateTree57(features, 13);
    } else {
      return calculateTree57(features, 14);
    }
  }
  if (node === 2) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree57(features, 5);
    } else if (val <= 656.5000000000001) {
      return calculateTree57(features, 5);
    } else {
      return calculateTree57(features, 6);
    }
  }
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree57(features, 1);
    } else if (val <= 619.5000000000001) {
      return calculateTree57(features, 1);
    } else {
      return calculateTree57(features, 2);
    }
  }
}
function calculateTree58(features, node = 0) {
  if (node === 3) return 0.0002557684310216296;
  if (node === 79) return 0.04881152713583803;
  if (node === 161) return -0.0159702694195279;
  if (node === 162) return 0.20223011700732058;
  if (node === 80) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree58(features, 161);
    } else if (val <= 207.00000000000003) {
      return calculateTree58(features, 161);
    } else {
      return calculateTree58(features, 162);
    }
  }
  if (node === 39) {
    const val = features['is_preference_household'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree58(features, 79);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree58(features, 79);
    } else {
      return calculateTree58(features, 80);
    }
  }
  if (node === 163) return -0.11719926740140284;
  if (node === 164) return 0.03048477504948987;
  if (node === 81) {
    const val = features['is_preference_men_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree58(features, 163);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree58(features, 163);
    } else {
      return calculateTree58(features, 164);
    }
  }
  if (node === 82) return -0.2357554185269902;
  if (node === 40) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree58(features, 81);
    } else if (val <= 43.50000000000001) {
      return calculateTree58(features, 81);
    } else {
      return calculateTree58(features, 82);
    }
  }
  if (node === 19) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree58(features, 39);
    } else if (val <= 208.50000000000003) {
      return calculateTree58(features, 39);
    } else {
      return calculateTree58(features, 40);
    }
  }
  if (node === 83) return 0.1336730999807804;
  if (node === 169) return 0.029031004672333695;
  if (node === 170) return -0.19290914514274596;
  if (node === 84) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree58(features, 169);
    } else if (val <= 800.5000000000001) {
      return calculateTree58(features, 169);
    } else {
      return calculateTree58(features, 170);
    }
  }
  if (node === 41) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree58(features, 83);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree58(features, 83);
    } else {
      return calculateTree58(features, 84);
    }
  }
  if (node === 85) return 0.24368230551495607;
  if (node === 86) return -0.16531477197616143;
  if (node === 42) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree58(features, 85);
    } else if (val <= 46.50000000000001) {
      return calculateTree58(features, 85);
    } else {
      return calculateTree58(features, 86);
    }
  }
  if (node === 20) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree58(features, 41);
    } else if (val <= 839.5000000000001) {
      return calculateTree58(features, 41);
    } else {
      return calculateTree58(features, 42);
    }
  }
  if (node === 9) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree58(features, 19);
    } else if (val <= 241.50000000000003) {
      return calculateTree58(features, 19);
    } else {
      return calculateTree58(features, 20);
    }
  }
  if (node === 351) return 0.38994958601962854;
  if (node === 352) return -0.00999035657103363;
  if (node === 175) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree58(features, 351);
    } else if (val <= 14.500000000000002) {
      return calculateTree58(features, 351);
    } else {
      return calculateTree58(features, 352);
    }
  }
  if (node === 176) return -0.015231674350883007;
  if (node === 87) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree58(features, 175);
    } else if (val <= 649.5000000000001) {
      return calculateTree58(features, 175);
    } else {
      return calculateTree58(features, 176);
    }
  }
  if (node === 177) return -0.014630668262667625;
  if (node === 1431) return -0.36503514449710567;
  if (node === 1432) return -0.04438156184757929;
  if (node === 715) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree58(features, 1431);
    } else if (val <= 25.000000000000004) {
      return calculateTree58(features, 1431);
    } else {
      return calculateTree58(features, 1432);
    }
  }
  if (node === 716) return 0.13280374239597514;
  if (node === 357) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree58(features, 715);
    } else if (val <= 835.5000000000001) {
      return calculateTree58(features, 715);
    } else {
      return calculateTree58(features, 716);
    }
  }
  if (node === 358) return -0.11906584859601453;
  if (node === 178) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree58(features, 357);
    } else if (val <= 839.5000000000001) {
      return calculateTree58(features, 357);
    } else {
      return calculateTree58(features, 358);
    }
  }
  if (node === 88) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree58(features, 177);
    } else if (val <= 8.500000000000002) {
      return calculateTree58(features, 177);
    } else {
      return calculateTree58(features, 178);
    }
  }
  if (node === 43) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree58(features, 87);
    } else if (val <= 12.000000000000002) {
      return calculateTree58(features, 87);
    } else {
      return calculateTree58(features, 88);
    }
  }
  if (node === 44) return -0.20123261680788843;
  if (node === 21) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree58(features, 43);
    } else if (val <= 727.5000000000001) {
      return calculateTree58(features, 43);
    } else {
      return calculateTree58(features, 44);
    }
  }
  if (node === 183) return 0.05507129273028507;
  if (node === 184) return -0.3402540467037443;
  if (node === 91) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree58(features, 183);
    } else if (val <= 588.5000000000001) {
      return calculateTree58(features, 183);
    } else {
      return calculateTree58(features, 184);
    }
  }
  if (node === 92) return 0.13984225401254793;
  if (node === 45) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree58(features, 91);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree58(features, 91);
    } else {
      return calculateTree58(features, 92);
    }
  }
  if (node === 93) return -0.027044078049727926;
  if (node === 189) return -0.13721258326950203;
  if (node === 381) return 0.07377489169456461;
  if (node === 382) return -0.013893256163582711;
  if (node === 190) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree58(features, 381);
    } else if (val <= 467.50000000000006) {
      return calculateTree58(features, 381);
    } else {
      return calculateTree58(features, 382);
    }
  }
  if (node === 94) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree58(features, 189);
    } else if (val <= 39.50000000000001) {
      return calculateTree58(features, 189);
    } else {
      return calculateTree58(features, 190);
    }
  }
  if (node === 46) {
    const val = features['is_preference_kids'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree58(features, 93);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree58(features, 93);
    } else {
      return calculateTree58(features, 94);
    }
  }
  if (node === 22) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree58(features, 45);
    } else if (val <= 760.0000000000001) {
      return calculateTree58(features, 45);
    } else {
      return calculateTree58(features, 46);
    }
  }
  if (node === 10) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree58(features, 21);
    } else if (val <= 745.5000000000001) {
      return calculateTree58(features, 21);
    } else {
      return calculateTree58(features, 22);
    }
  }
  if (node === 4) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree58(features, 9);
    } else if (val <= 261.50000000000006) {
      return calculateTree58(features, 9);
    } else {
      return calculateTree58(features, 10);
    }
  }
  if (node === 1) {
    const val = features['is_preference_sports'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree58(features, 3);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree58(features, 3);
    } else {
      return calculateTree58(features, 4);
    }
  }
  if (node === 23) return 0.19345453302185256;
  if (node === 24) return -0.3028664865420117;
  if (node === 11) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree58(features, 23);
    } else if (val <= 253.50000000000003) {
      return calculateTree58(features, 23);
    } else {
      return calculateTree58(features, 24);
    }
  }
  if (node === 12) return 0.03716238008536711;
  if (node === 5) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree58(features, 11);
    } else if (val <= 551.5000000000001) {
      return calculateTree58(features, 11);
    } else {
      return calculateTree58(features, 12);
    }
  }
  if (node === 6) return -0.27063138235115736;
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree58(features, 5);
    } else if (val <= 606.5000000000001) {
      return calculateTree58(features, 5);
    } else {
      return calculateTree58(features, 6);
    }
  }
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree58(features, 1);
    } else if (val <= 978.0000000000001) {
      return calculateTree58(features, 1);
    } else {
      return calculateTree58(features, 2);
    }
  }
}
function calculateTree59(features, node = 0) {
  if (node === 15) return 0.0026978479946754112;
  if (node === 16) return -0.28715413261270833;
  if (node === 7) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree59(features, 15);
    } else if (val <= 143.50000000000003) {
      return calculateTree59(features, 15);
    } else {
      return calculateTree59(features, 16);
    }
  }
  if (node === 17) return 0.028258460061735765;
  if (node === 18) return 0.11408555943509129;
  if (node === 8) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree59(features, 17);
    } else if (val <= 29.500000000000004) {
      return calculateTree59(features, 17);
    } else {
      return calculateTree59(features, 18);
    }
  }
  if (node === 3) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree59(features, 7);
    } else if (val <= 148.00000000000003) {
      return calculateTree59(features, 7);
    } else {
      return calculateTree59(features, 8);
    }
  }
  if (node === 39) return -0.026372299343646738;
  if (node === 163) return 0.10224518507705613;
  if (node === 164) return -0.39614849847586175;
  if (node === 81) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree59(features, 163);
    } else if (val <= 308.00000000000006) {
      return calculateTree59(features, 163);
    } else {
      return calculateTree59(features, 164);
    }
  }
  if (node === 82) return -0.3731219706155176;
  if (node === 40) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree59(features, 81);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree59(features, 81);
    } else {
      return calculateTree59(features, 82);
    }
  }
  if (node === 19) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree59(features, 39);
    } else if (val <= 201.50000000000003) {
      return calculateTree59(features, 39);
    } else {
      return calculateTree59(features, 40);
    }
  }
  if (node === 1343) return -0.06176442065589413;
  if (node === 1344) return -0.37586833574564255;
  if (node === 671) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree59(features, 1343);
    } else if (val <= 409.50000000000006) {
      return calculateTree59(features, 1343);
    } else {
      return calculateTree59(features, 1344);
    }
  }
  if (node === 1345) return 0.3939568894311055;
  if (node === 1346) return 0.029505293768337108;
  if (node === 672) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree59(features, 1345);
    } else if (val <= 558.5000000000001) {
      return calculateTree59(features, 1345);
    } else {
      return calculateTree59(features, 1346);
    }
  }
  if (node === 335) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree59(features, 671);
    } else if (val <= 525.0000000000001) {
      return calculateTree59(features, 671);
    } else {
      return calculateTree59(features, 672);
    }
  }
  if (node === 336) return -0.32452744759659696;
  if (node === 167) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree59(features, 335);
    } else if (val <= 589.5000000000001) {
      return calculateTree59(features, 335);
    } else {
      return calculateTree59(features, 336);
    }
  }
  if (node === 337) return 0.10327071053411895;
  if (node === 677) return -0.17564335738380435;
  if (node === 1357) return 0.1981173017688179;
  if (node === 1358) return -0.014725993953967625;
  if (node === 678) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree59(features, 1357);
    } else if (val <= 587.5000000000001) {
      return calculateTree59(features, 1357);
    } else {
      return calculateTree59(features, 1358);
    }
  }
  if (node === 338) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree59(features, 677);
    } else if (val <= 556.0000000000001) {
      return calculateTree59(features, 677);
    } else {
      return calculateTree59(features, 678);
    }
  }
  if (node === 168) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree59(features, 337);
    } else if (val <= 500.50000000000006) {
      return calculateTree59(features, 337);
    } else {
      return calculateTree59(features, 338);
    }
  }
  if (node === 83) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree59(features, 167);
    } else if (val <= 449.50000000000006) {
      return calculateTree59(features, 167);
    } else {
      return calculateTree59(features, 168);
    }
  }
  if (node === 84) return 0.019556994359042186;
  if (node === 41) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree59(features, 83);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree59(features, 83);
    } else {
      return calculateTree59(features, 84);
    }
  }
  if (node === 42) return -0.03853208727084927;
  if (node === 20) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree59(features, 41);
    } else if (val <= 667.5000000000001) {
      return calculateTree59(features, 41);
    } else {
      return calculateTree59(features, 42);
    }
  }
  if (node === 9) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree59(features, 19);
    } else if (val <= 215.00000000000003) {
      return calculateTree59(features, 19);
    } else {
      return calculateTree59(features, 20);
    }
  }
  if (node === 43) return 0.03973263051435335;
  if (node === 44) return 0.2907716150225574;
  if (node === 21) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree59(features, 43);
    } else if (val <= 19.500000000000004) {
      return calculateTree59(features, 43);
    } else {
      return calculateTree59(features, 44);
    }
  }
  if (node === 22) return 0.00986580613901551;
  if (node === 10) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree59(features, 21);
    } else if (val <= 752.5000000000001) {
      return calculateTree59(features, 21);
    } else {
      return calculateTree59(features, 22);
    }
  }
  if (node === 4) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree59(features, 9);
    } else if (val <= 745.5000000000001) {
      return calculateTree59(features, 9);
    } else {
      return calculateTree59(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree59(features, 3);
    } else if (val <= 212.50000000000003) {
      return calculateTree59(features, 3);
    } else {
      return calculateTree59(features, 4);
    }
  }
  if (node === 11) return -0.002185638714875938;
  if (node === 12) return -0.055755209442159774;
  if (node === 5) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree59(features, 11);
    } else if (val <= 21.500000000000004) {
      return calculateTree59(features, 11);
    } else {
      return calculateTree59(features, 12);
    }
  }
  if (node === 55) return 0.02527823386711774;
  if (node === 113) return 0.14549794786028647;
  if (node === 114) return 0.7218324987984642;
  if (node === 56) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree59(features, 113);
    } else if (val <= 13.500000000000002) {
      return calculateTree59(features, 113);
    } else {
      return calculateTree59(features, 114);
    }
  }
  if (node === 27) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree59(features, 55);
    } else if (val <= 775.5000000000001) {
      return calculateTree59(features, 55);
    } else {
      return calculateTree59(features, 56);
    }
  }
  if (node === 115) return 0.025373766697041455;
  if (node === 233) return 0.4468051913017219;
  if (node === 234) return 0.17604300392566666;
  if (node === 116) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree59(features, 233);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree59(features, 233);
    } else {
      return calculateTree59(features, 234);
    }
  }
  if (node === 57) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree59(features, 115);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree59(features, 115);
    } else {
      return calculateTree59(features, 116);
    }
  }
  if (node === 58) return 0.006300165198895913;
  if (node === 28) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree59(features, 57);
    } else if (val <= 13.500000000000002) {
      return calculateTree59(features, 57);
    } else {
      return calculateTree59(features, 58);
    }
  }
  if (node === 13) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree59(features, 27);
    } else if (val <= 794.0000000000001) {
      return calculateTree59(features, 27);
    } else {
      return calculateTree59(features, 28);
    }
  }
  if (node === 14) return -0.14714501267109836;
  if (node === 6) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree59(features, 13);
    } else if (val <= 980.5000000000001) {
      return calculateTree59(features, 13);
    } else {
      return calculateTree59(features, 14);
    }
  }
  if (node === 2) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree59(features, 5);
    } else if (val <= 758.5000000000001) {
      return calculateTree59(features, 5);
    } else {
      return calculateTree59(features, 6);
    }
  }
  if (node === 0) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree59(features, 1);
    } else if (val <= 1.5000000000000002) {
      return calculateTree59(features, 1);
    } else {
      return calculateTree59(features, 2);
    }
  }
}
function calculateTree60(features, node = 0) {
  if (node === 1) return 0.002339671977796382;
  if (node === 11) return 0.04218749245273122;
  if (node === 25) return -0.0736194151356613;
  if (node === 26) return -0.3279505780436832;
  if (node === 12) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree60(features, 25);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree60(features, 25);
    } else {
      return calculateTree60(features, 26);
    }
  }
  if (node === 5) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree60(features, 11);
    } else if (val <= 300.50000000000006) {
      return calculateTree60(features, 11);
    } else {
      return calculateTree60(features, 12);
    }
  }
  if (node === 895) return -0.42248151147853;
  if (node === 896) return 0.2113001396659844;
  if (node === 447) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree60(features, 895);
    } else if (val <= 507.00000000000006) {
      return calculateTree60(features, 895);
    } else {
      return calculateTree60(features, 896);
    }
  }
  if (node === 897) return 0.28050337036419;
  if (node === 898) return 0.07217291211023275;
  if (node === 448) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree60(features, 897);
    } else if (val <= 258.50000000000006) {
      return calculateTree60(features, 897);
    } else {
      return calculateTree60(features, 898);
    }
  }
  if (node === 223) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree60(features, 447);
    } else if (val <= 251.00000000000003) {
      return calculateTree60(features, 447);
    } else {
      return calculateTree60(features, 448);
    }
  }
  if (node === 224) return 0.0023330472986754147;
  if (node === 111) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree60(features, 223);
    } else if (val <= 511.50000000000006) {
      return calculateTree60(features, 223);
    } else {
      return calculateTree60(features, 224);
    }
  }
  if (node === 112) return -0.3195598142214111;
  if (node === 55) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree60(features, 111);
    } else if (val <= 785.5000000000001) {
      return calculateTree60(features, 111);
    } else {
      return calculateTree60(features, 112);
    }
  }
  if (node === 113) return 0.13053561987729242;
  if (node === 229) return -0.5117349580609749;
  if (node === 230) return 0.019805082883570118;
  if (node === 114) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree60(features, 229);
    } else if (val <= 963.0000000000001) {
      return calculateTree60(features, 229);
    } else {
      return calculateTree60(features, 230);
    }
  }
  if (node === 56) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree60(features, 113);
    } else if (val <= 925.5000000000001) {
      return calculateTree60(features, 113);
    } else {
      return calculateTree60(features, 114);
    }
  }
  if (node === 27) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree60(features, 55);
    } else if (val <= 799.5000000000001) {
      return calculateTree60(features, 55);
    } else {
      return calculateTree60(features, 56);
    }
  }
  if (node === 231) return -0.35357910073687887;
  if (node === 232) return 0.1399584328573795;
  if (node === 115) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree60(features, 231);
    } else if (val <= 822.5000000000001) {
      return calculateTree60(features, 231);
    } else {
      return calculateTree60(features, 232);
    }
  }
  if (node === 233) return 0.13620286501915194;
  if (node === 234) return -0.31037336231834184;
  if (node === 116) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree60(features, 233);
    } else if (val <= 606.5000000000001) {
      return calculateTree60(features, 233);
    } else {
      return calculateTree60(features, 234);
    }
  }
  if (node === 57) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree60(features, 115);
    } else if (val <= 840.0000000000001) {
      return calculateTree60(features, 115);
    } else {
      return calculateTree60(features, 116);
    }
  }
  if (node === 117) return 0.04028032867316159;
  if (node === 118) return -0.24710896126647908;
  if (node === 58) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree60(features, 117);
    } else if (val <= 983.5000000000001) {
      return calculateTree60(features, 117);
    } else {
      return calculateTree60(features, 118);
    }
  }
  if (node === 28) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree60(features, 57);
    } else if (val <= 916.5000000000001) {
      return calculateTree60(features, 57);
    } else {
      return calculateTree60(features, 58);
    }
  }
  if (node === 13) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree60(features, 27);
    } else if (val <= 818.0000000000001) {
      return calculateTree60(features, 27);
    } else {
      return calculateTree60(features, 28);
    }
  }
  if (node === 119) return -0.3398502912894646;
  if (node === 120) return 0.20377682226211366;
  if (node === 59) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree60(features, 119);
    } else if (val <= 695.5000000000001) {
      return calculateTree60(features, 119);
    } else {
      return calculateTree60(features, 120);
    }
  }
  if (node === 60) return -0.2639852119645816;
  if (node === 29) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree60(features, 59);
    } else if (val <= 14.500000000000002) {
      return calculateTree60(features, 59);
    } else {
      return calculateTree60(features, 60);
    }
  }
  if (node === 123) return -0.34561490323390603;
  if (node === 124) return 0.05637856281818348;
  if (node === 61) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree60(features, 123);
    } else if (val <= 575.5000000000001) {
      return calculateTree60(features, 123);
    } else {
      return calculateTree60(features, 124);
    }
  }
  if (node === 125) return -0.3168678203632281;
  if (node === 253) return 0.29686919191184563;
  if (node === 2039) return -0.17654274292285652;
  if (node === 2040) return 0.2933554148588209;
  if (node === 1019) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree60(features, 2039);
    } else if (val <= 808.5000000000001) {
      return calculateTree60(features, 2039);
    } else {
      return calculateTree60(features, 2040);
    }
  }
  if (node === 1020) return -0.3571910916674808;
  if (node === 509) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree60(features, 1019);
    } else if (val <= 829.0000000000001) {
      return calculateTree60(features, 1019);
    } else {
      return calculateTree60(features, 1020);
    }
  }
  if (node === 1021) return 0.05969617634090202;
  if (node === 2045) return -0.03367930937983889;
  if (node === 2046) return 0.004706587599436763;
  if (node === 1022) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree60(features, 2045);
    } else if (val <= 472.50000000000006) {
      return calculateTree60(features, 2045);
    } else {
      return calculateTree60(features, 2046);
    }
  }
  if (node === 510) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree60(features, 1021);
    } else if (val <= 208.50000000000003) {
      return calculateTree60(features, 1021);
    } else {
      return calculateTree60(features, 1022);
    }
  }
  if (node === 254) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree60(features, 509);
    } else if (val <= 203.50000000000003) {
      return calculateTree60(features, 509);
    } else {
      return calculateTree60(features, 510);
    }
  }
  if (node === 126) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree60(features, 253);
    } else if (val <= 180.50000000000003) {
      return calculateTree60(features, 253);
    } else {
      return calculateTree60(features, 254);
    }
  }
  if (node === 62) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree60(features, 125);
    } else if (val <= 175.50000000000003) {
      return calculateTree60(features, 125);
    } else {
      return calculateTree60(features, 126);
    }
  }
  if (node === 30) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree60(features, 61);
    } else if (val <= 143.50000000000003) {
      return calculateTree60(features, 61);
    } else {
      return calculateTree60(features, 62);
    }
  }
  if (node === 14) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree60(features, 29);
    } else if (val <= 41.50000000000001) {
      return calculateTree60(features, 29);
    } else {
      return calculateTree60(features, 30);
    }
  }
  if (node === 6) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree60(features, 13);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree60(features, 13);
    } else {
      return calculateTree60(features, 14);
    }
  }
  if (node === 2) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree60(features, 5);
    } else if (val <= 485.50000000000006) {
      return calculateTree60(features, 5);
    } else {
      return calculateTree60(features, 6);
    }
  }
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree60(features, 1);
    } else if (val <= 478.50000000000006) {
      return calculateTree60(features, 1);
    } else {
      return calculateTree60(features, 2);
    }
  }
}
function calculateTree61(features, node = 0) {
  if (node === 1) return 0.0007742375485489927;
  if (node === 47) return 0.0031069640093175635;
  if (node === 97) return 0.07119222117569712;
  if (node === 98) return -0.12134586038699434;
  if (node === 48) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree61(features, 97);
    } else if (val <= 467.50000000000006) {
      return calculateTree61(features, 97);
    } else {
      return calculateTree61(features, 98);
    }
  }
  if (node === 23) {
    const val = features['is_preference_men_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree61(features, 47);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree61(features, 47);
    } else {
      return calculateTree61(features, 48);
    }
  }
  if (node === 199) return -0.10179499232320811;
  if (node === 401) return 0.12986578996897283;
  if (node === 402) return -0.1685525867348529;
  if (node === 200) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree61(features, 401);
    } else if (val <= 44.50000000000001) {
      return calculateTree61(features, 401);
    } else {
      return calculateTree61(features, 402);
    }
  }
  if (node === 99) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree61(features, 199);
    } else if (val <= 905.5000000000001) {
      return calculateTree61(features, 199);
    } else {
      return calculateTree61(features, 200);
    }
  }
  if (node === 1615) return -0.026023356431005052;
  if (node === 1616) return 0.08177982031908519;
  if (node === 807) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree61(features, 1615);
    } else if (val <= 899.5000000000001) {
      return calculateTree61(features, 1615);
    } else {
      return calculateTree61(features, 1616);
    }
  }
  if (node === 1617) return 0.049828435969371206;
  if (node === 1618) return 0.32556375054996284;
  if (node === 808) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree61(features, 1617);
    } else if (val <= 215.00000000000003) {
      return calculateTree61(features, 1617);
    } else {
      return calculateTree61(features, 1618);
    }
  }
  if (node === 403) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree61(features, 807);
    } else if (val <= 697.0000000000001) {
      return calculateTree61(features, 807);
    } else {
      return calculateTree61(features, 808);
    }
  }
  if (node === 404) return -0.3867101215796759;
  if (node === 201) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree61(features, 403);
    } else if (val <= 983.5000000000001) {
      return calculateTree61(features, 403);
    } else {
      return calculateTree61(features, 404);
    }
  }
  if (node === 405) return -0.04055357010839536;
  if (node === 406) return -0.1837811230282086;
  if (node === 202) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree61(features, 405);
    } else if (val <= 27.500000000000004) {
      return calculateTree61(features, 405);
    } else {
      return calculateTree61(features, 406);
    }
  }
  if (node === 100) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree61(features, 201);
    } else if (val <= 229.50000000000003) {
      return calculateTree61(features, 201);
    } else {
      return calculateTree61(features, 202);
    }
  }
  if (node === 49) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree61(features, 99);
    } else if (val <= 1.5000000000000002) {
      return calculateTree61(features, 99);
    } else {
      return calculateTree61(features, 100);
    }
  }
  if (node === 815) return 0.040970548026676365;
  if (node === 816) return 0.19708529202753133;
  if (node === 407) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree61(features, 815);
    } else if (val <= 447.00000000000006) {
      return calculateTree61(features, 815);
    } else {
      return calculateTree61(features, 816);
    }
  }
  if (node === 408) return 0.29894067832613025;
  if (node === 203) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree61(features, 407);
    } else if (val <= 880.0000000000001) {
      return calculateTree61(features, 407);
    } else {
      return calculateTree61(features, 408);
    }
  }
  if (node === 204) return -0.056849012959859044;
  if (node === 101) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree61(features, 203);
    } else if (val <= 18.500000000000004) {
      return calculateTree61(features, 203);
    } else {
      return calculateTree61(features, 204);
    }
  }
  if (node === 102) return 0.00270985165840739;
  if (node === 50) {
    const val = features['is_preference_kitchen'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree61(features, 101);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree61(features, 101);
    } else {
      return calculateTree61(features, 102);
    }
  }
  if (node === 24) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree61(features, 49);
    } else if (val <= 300.50000000000006) {
      return calculateTree61(features, 49);
    } else {
      return calculateTree61(features, 50);
    }
  }
  if (node === 11) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree61(features, 23);
    } else if (val <= 24.500000000000004) {
      return calculateTree61(features, 23);
    } else {
      return calculateTree61(features, 24);
    }
  }
  if (node === 51) return -0.09204952141688331;
  if (node === 211) return -0.012497799960792531;
  if (node === 212) return -0.24503890571094797;
  if (node === 105) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree61(features, 211);
    } else if (val <= 14.500000000000002) {
      return calculateTree61(features, 211);
    } else {
      return calculateTree61(features, 212);
    }
  }
  if (node === 213) return -0.1403509505957595;
  if (node === 214) return 0.1012488371140012;
  if (node === 106) {
    const val = features['is_preference_home'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree61(features, 213);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree61(features, 213);
    } else {
      return calculateTree61(features, 214);
    }
  }
  if (node === 52) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree61(features, 105);
    } else if (val <= 31.500000000000004) {
      return calculateTree61(features, 105);
    } else {
      return calculateTree61(features, 106);
    }
  }
  if (node === 25) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree61(features, 51);
    } else if (val <= 427.50000000000006) {
      return calculateTree61(features, 51);
    } else {
      return calculateTree61(features, 52);
    }
  }
  if (node === 26) return -0.20883887141540822;
  if (node === 12) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree61(features, 25);
    } else if (val <= 51.50000000000001) {
      return calculateTree61(features, 25);
    } else {
      return calculateTree61(features, 26);
    }
  }
  if (node === 5) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree61(features, 11);
    } else if (val <= 926.0000000000001) {
      return calculateTree61(features, 11);
    } else {
      return calculateTree61(features, 12);
    }
  }
  if (node === 27) return -0.2493916386543699;
  if (node === 115) return 0.3509105408789168;
  if (node === 116) return -0.04026425615925421;
  if (node === 57) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree61(features, 115);
    } else if (val <= 27.500000000000004) {
      return calculateTree61(features, 115);
    } else {
      return calculateTree61(features, 116);
    }
  }
  if (node === 58) return -0.05818067067913685;
  if (node === 28) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree61(features, 57);
    } else if (val <= 588.5000000000001) {
      return calculateTree61(features, 57);
    } else {
      return calculateTree61(features, 58);
    }
  }
  if (node === 13) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree61(features, 27);
    } else if (val <= 575.5000000000001) {
      return calculateTree61(features, 27);
    } else {
      return calculateTree61(features, 28);
    }
  }
  if (node === 29) return -0.03401757556286067;
  if (node === 61) return 0.0042485336135963305;
  if (node === 62) return -0.22877569772136075;
  if (node === 30) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree61(features, 61);
    } else if (val <= 982.5000000000001) {
      return calculateTree61(features, 61);
    } else {
      return calculateTree61(features, 62);
    }
  }
  if (node === 14) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree61(features, 29);
    } else if (val <= 2.5000000000000004) {
      return calculateTree61(features, 29);
    } else {
      return calculateTree61(features, 30);
    }
  }
  if (node === 6) {
    const val = features['is_preference_household'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree61(features, 13);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree61(features, 13);
    } else {
      return calculateTree61(features, 14);
    }
  }
  if (node === 2) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree61(features, 5);
    } else if (val <= 504.50000000000006) {
      return calculateTree61(features, 5);
    } else {
      return calculateTree61(features, 6);
    }
  }
  if (node === 0) {
    const val = features['is_preference_kids'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree61(features, 1);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree61(features, 1);
    } else {
      return calculateTree61(features, 2);
    }
  }
}
function calculateTree62(features, node = 0) {
  if (node === 1) return -0.001026765074817309;
  if (node === 95) return 0.058723122257265115;
  if (node === 193) return -0.22343206149610673;
  if (node === 194) return 0.06359114430804161;
  if (node === 96) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree62(features, 193);
    } else if (val <= 135.50000000000003) {
      return calculateTree62(features, 193);
    } else {
      return calculateTree62(features, 194);
    }
  }
  if (node === 47) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree62(features, 95);
    } else if (val <= 51.50000000000001) {
      return calculateTree62(features, 95);
    } else {
      return calculateTree62(features, 96);
    }
  }
  if (node === 97) return -0.34905751594987416;
  if (node === 98) return 0.034914951807396566;
  if (node === 48) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree62(features, 97);
    } else if (val <= 861.5000000000001) {
      return calculateTree62(features, 97);
    } else {
      return calculateTree62(features, 98);
    }
  }
  if (node === 23) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree62(features, 47);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree62(features, 47);
    } else {
      return calculateTree62(features, 48);
    }
  }
  if (node === 24) return -0.2762085404870872;
  if (node === 11) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree62(features, 23);
    } else if (val <= 160.50000000000003) {
      return calculateTree62(features, 23);
    } else {
      return calculateTree62(features, 24);
    }
  }
  if (node === 207) return 0.49795004294592526;
  if (node === 208) return -0.3797278279343192;
  if (node === 103) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree62(features, 207);
    } else if (val <= 278.50000000000006) {
      return calculateTree62(features, 207);
    } else {
      return calculateTree62(features, 208);
    }
  }
  if (node === 104) return 0.16909877822673836;
  if (node === 51) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree62(features, 103);
    } else if (val <= 387.50000000000006) {
      return calculateTree62(features, 103);
    } else {
      return calculateTree62(features, 104);
    }
  }
  if (node === 211) return -0.3936644494656879;
  if (node === 212) return 0.14641467700959515;
  if (node === 105) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree62(features, 211);
    } else if (val <= 278.50000000000006) {
      return calculateTree62(features, 211);
    } else {
      return calculateTree62(features, 212);
    }
  }
  if (node === 106) return -0.37931728891109207;
  if (node === 52) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree62(features, 105);
    } else if (val <= 387.50000000000006) {
      return calculateTree62(features, 105);
    } else {
      return calculateTree62(features, 106);
    }
  }
  if (node === 25) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree62(features, 51);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree62(features, 51);
    } else {
      return calculateTree62(features, 52);
    }
  }
  if (node === 26) return 0.016133036596346774;
  if (node === 12) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree62(features, 25);
    } else if (val <= 839.5000000000001) {
      return calculateTree62(features, 25);
    } else {
      return calculateTree62(features, 26);
    }
  }
  if (node === 5) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree62(features, 11);
    } else if (val <= 191.50000000000003) {
      return calculateTree62(features, 11);
    } else {
      return calculateTree62(features, 12);
    }
  }
  if (node === 27) return -0.042420396659391915;
  if (node === 28) return -0.3396754650574396;
  if (node === 13) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree62(features, 27);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree62(features, 27);
    } else {
      return calculateTree62(features, 28);
    }
  }
  if (node === 29) return -0.32371745321612594;
  if (node === 495) return 0.29566129835613514;
  if (node === 496) return -0.11719523427995027;
  if (node === 247) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree62(features, 495);
    } else if (val <= 709.5000000000001) {
      return calculateTree62(features, 495);
    } else {
      return calculateTree62(features, 496);
    }
  }
  if (node === 1991) return -0.2744932445392024;
  if (node === 1992) return 0.48539418715582594;
  if (node === 995) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree62(features, 1991);
    } else if (val <= 597.5000000000001) {
      return calculateTree62(features, 1991);
    } else {
      return calculateTree62(features, 1992);
    }
  }
  if (node === 996) return -0.3556916377088956;
  if (node === 497) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree62(features, 995);
    } else if (val <= 861.5000000000001) {
      return calculateTree62(features, 995);
    } else {
      return calculateTree62(features, 996);
    }
  }
  if (node === 498) return 0.0831932546545912;
  if (node === 248) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree62(features, 497);
    } else if (val <= 722.5000000000001) {
      return calculateTree62(features, 497);
    } else {
      return calculateTree62(features, 498);
    }
  }
  if (node === 123) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree62(features, 247);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree62(features, 247);
    } else {
      return calculateTree62(features, 248);
    }
  }
  if (node === 124) return -0.07180597450805919;
  if (node === 61) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree62(features, 123);
    } else if (val <= 34.50000000000001) {
      return calculateTree62(features, 123);
    } else {
      return calculateTree62(features, 124);
    }
  }
  if (node === 125) return -0.26831581905142315;
  if (node === 507) return 0.046219094650152566;
  if (node === 1017) return 0.01072041654964266;
  if (node === 2037) return -0.3261337282339435;
  if (node === 2038) return 0.3010161322516412;
  if (node === 1018) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree62(features, 2037);
    } else if (val <= 973.5000000000001) {
      return calculateTree62(features, 2037);
    } else {
      return calculateTree62(features, 2038);
    }
  }
  if (node === 508) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree62(features, 1017);
    } else if (val <= 693.0000000000001) {
      return calculateTree62(features, 1017);
    } else {
      return calculateTree62(features, 1018);
    }
  }
  if (node === 253) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree62(features, 507);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree62(features, 507);
    } else {
      return calculateTree62(features, 508);
    }
  }
  if (node === 1019) return -0.31336641050506814;
  if (node === 1020) return 0.12194781203152466;
  if (node === 509) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree62(features, 1019);
    } else if (val <= 916.5000000000001) {
      return calculateTree62(features, 1019);
    } else {
      return calculateTree62(features, 1020);
    }
  }
  if (node === 510) return 0.04747502229362871;
  if (node === 254) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree62(features, 509);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree62(features, 509);
    } else {
      return calculateTree62(features, 510);
    }
  }
  if (node === 126) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree62(features, 253);
    } else if (val <= 840.0000000000001) {
      return calculateTree62(features, 253);
    } else {
      return calculateTree62(features, 254);
    }
  }
  if (node === 62) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree62(features, 125);
    } else if (val <= 897.5000000000001) {
      return calculateTree62(features, 125);
    } else {
      return calculateTree62(features, 126);
    }
  }
  if (node === 30) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree62(features, 61);
    } else if (val <= 865.0000000000001) {
      return calculateTree62(features, 61);
    } else {
      return calculateTree62(features, 62);
    }
  }
  if (node === 14) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree62(features, 29);
    } else if (val <= 551.5000000000001) {
      return calculateTree62(features, 29);
    } else {
      return calculateTree62(features, 30);
    }
  }
  if (node === 6) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree62(features, 13);
    } else if (val <= 839.5000000000001) {
      return calculateTree62(features, 13);
    } else {
      return calculateTree62(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree62(features, 5);
    } else if (val <= 525.0000000000001) {
      return calculateTree62(features, 5);
    } else {
      return calculateTree62(features, 6);
    }
  }
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree62(features, 1);
    } else if (val <= 821.0000000000001) {
      return calculateTree62(features, 1);
    } else {
      return calculateTree62(features, 2);
    }
  }
}
function calculateTree63(features, node = 0) {
  if (node === 127) return -0.2882725031627545;
  if (node === 257) return 0.20969288128597868;
  if (node === 517) return -0.39641634342590437;
  if (node === 1037) return 0.023615521568180687;
  if (node === 1038) return -0.07353104028510427;
  if (node === 518) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree63(features, 1037);
    } else if (val <= 75.50000000000001) {
      return calculateTree63(features, 1037);
    } else {
      return calculateTree63(features, 1038);
    }
  }
  if (node === 258) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree63(features, 517);
    } else if (val <= 175.50000000000003) {
      return calculateTree63(features, 517);
    } else {
      return calculateTree63(features, 518);
    }
  }
  if (node === 128) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree63(features, 257);
    } else if (val <= 132.50000000000003) {
      return calculateTree63(features, 257);
    } else {
      return calculateTree63(features, 258);
    }
  }
  if (node === 63) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree63(features, 127);
    } else if (val <= 115.50000000000001) {
      return calculateTree63(features, 127);
    } else {
      return calculateTree63(features, 128);
    }
  }
  if (node === 64) return 0.06062489155019643;
  if (node === 31) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree63(features, 63);
    } else if (val <= 148.00000000000003) {
      return calculateTree63(features, 63);
    } else {
      return calculateTree63(features, 64);
    }
  }
  if (node === 131) return -0.03589439908905889;
  if (node === 132) return -0.3198172699105464;
  if (node === 65) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree63(features, 131);
    } else if (val <= 185.00000000000003) {
      return calculateTree63(features, 131);
    } else {
      return calculateTree63(features, 132);
    }
  }
  if (node === 535) return -0.19919014128897747;
  if (node === 536) return 0.4946704798973667;
  if (node === 267) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree63(features, 535);
    } else if (val <= 203.50000000000003) {
      return calculateTree63(features, 535);
    } else {
      return calculateTree63(features, 536);
    }
  }
  if (node === 537) return -0.32770608021987163;
  if (node === 538) return 0.14135024458466652;
  if (node === 268) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree63(features, 537);
    } else if (val <= 829.0000000000001) {
      return calculateTree63(features, 537);
    } else {
      return calculateTree63(features, 538);
    }
  }
  if (node === 133) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree63(features, 267);
    } else if (val <= 409.50000000000006) {
      return calculateTree63(features, 267);
    } else {
      return calculateTree63(features, 268);
    }
  }
  if (node === 269) return -0.3250487722821273;
  if (node === 270) return -0.03467965868261901;
  if (node === 134) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree63(features, 269);
    } else if (val <= 214.00000000000003) {
      return calculateTree63(features, 269);
    } else {
      return calculateTree63(features, 270);
    }
  }
  if (node === 66) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree63(features, 133);
    } else if (val <= 211.50000000000003) {
      return calculateTree63(features, 133);
    } else {
      return calculateTree63(features, 134);
    }
  }
  if (node === 32) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree63(features, 65);
    } else if (val <= 191.50000000000003) {
      return calculateTree63(features, 65);
    } else {
      return calculateTree63(features, 66);
    }
  }
  if (node === 15) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree63(features, 31);
    } else if (val <= 166.50000000000003) {
      return calculateTree63(features, 31);
    } else {
      return calculateTree63(features, 32);
    }
  }
  if (node === 67) return 0.1929149921412231;
  if (node === 68) return -0.3577906679217044;
  if (node === 33) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree63(features, 67);
    } else if (val <= 763.0000000000001) {
      return calculateTree63(features, 67);
    } else {
      return calculateTree63(features, 68);
    }
  }
  if (node === 139) return 0.3378819584416731;
  if (node === 140) return -0.3980598995220635;
  if (node === 69) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree63(features, 139);
    } else if (val <= 556.0000000000001) {
      return calculateTree63(features, 139);
    } else {
      return calculateTree63(features, 140);
    }
  }
  if (node === 70) return 0.044414316791262794;
  if (node === 34) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree63(features, 69);
    } else if (val <= 282.50000000000006) {
      return calculateTree63(features, 69);
    } else {
      return calculateTree63(features, 70);
    }
  }
  if (node === 16) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree63(features, 33);
    } else if (val <= 278.50000000000006) {
      return calculateTree63(features, 33);
    } else {
      return calculateTree63(features, 34);
    }
  }
  if (node === 7) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree63(features, 15);
    } else if (val <= 271.00000000000006) {
      return calculateTree63(features, 15);
    } else {
      return calculateTree63(features, 16);
    }
  }
  if (node === 35) return -0.31633316314763893;
  if (node === 295) return 0.07071860264370895;
  if (node === 1187) return -0.3562753784977387;
  if (node === 1188) return 0.0015116713395058737;
  if (node === 593) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree63(features, 1187);
    } else if (val <= 178.50000000000003) {
      return calculateTree63(features, 1187);
    } else {
      return calculateTree63(features, 1188);
    }
  }
  if (node === 1189) return 0.0064011481775957686;
  if (node === 1190) return -0.4267751957305313;
  if (node === 594) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree63(features, 1189);
    } else if (val <= 240.00000000000003) {
      return calculateTree63(features, 1189);
    } else {
      return calculateTree63(features, 1190);
    }
  }
  if (node === 296) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree63(features, 593);
    } else if (val <= 166.50000000000003) {
      return calculateTree63(features, 593);
    } else {
      return calculateTree63(features, 594);
    }
  }
  if (node === 147) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree63(features, 295);
    } else if (val <= 66.00000000000001) {
      return calculateTree63(features, 295);
    } else {
      return calculateTree63(features, 296);
    }
  }
  if (node === 148) return -0.32816067948527206;
  if (node === 73) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree63(features, 147);
    } else if (val <= 217.50000000000003) {
      return calculateTree63(features, 147);
    } else {
      return calculateTree63(features, 148);
    }
  }
  if (node === 74) return 0.008799886410555027;
  if (node === 36) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree63(features, 73);
    } else if (val <= 270.00000000000006) {
      return calculateTree63(features, 73);
    } else {
      return calculateTree63(features, 74);
    }
  }
  if (node === 17) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree63(features, 35);
    } else if (val <= 3.5000000000000004) {
      return calculateTree63(features, 35);
    } else {
      return calculateTree63(features, 36);
    }
  }
  if (node === 18) return -0.2944113259607586;
  if (node === 8) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree63(features, 17);
    } else if (val <= 294.50000000000006) {
      return calculateTree63(features, 17);
    } else {
      return calculateTree63(features, 18);
    }
  }
  if (node === 3) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree63(features, 7);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree63(features, 7);
    } else {
      return calculateTree63(features, 8);
    }
  }
  if (node === 9) return -0.3703768250728836;
  if (node === 21) return -0.008705803878190593;
  if (node === 22) return -0.317778121802168;
  if (node === 10) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree63(features, 21);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree63(features, 21);
    } else {
      return calculateTree63(features, 22);
    }
  }
  if (node === 4) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree63(features, 9);
    } else if (val <= 948.0000000000001) {
      return calculateTree63(features, 9);
    } else {
      return calculateTree63(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree63(features, 3);
    } else if (val <= 308.00000000000006) {
      return calculateTree63(features, 3);
    } else {
      return calculateTree63(features, 4);
    }
  }
  if (node === 2) return 0.0009045512945973831;
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree63(features, 1);
    } else if (val <= 311.50000000000006) {
      return calculateTree63(features, 1);
    } else {
      return calculateTree63(features, 2);
    }
  }
}
function calculateTree64(features, node = 0) {
  if (node === 31) return 0.0033951454964134694;
  if (node === 32) return -0.2982623410395201;
  if (node === 15) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree64(features, 31);
    } else if (val <= 377.50000000000006) {
      return calculateTree64(features, 31);
    } else {
      return calculateTree64(features, 32);
    }
  }
  if (node === 16) return 0.1633823884180828;
  if (node === 7) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree64(features, 15);
    } else if (val <= 409.50000000000006) {
      return calculateTree64(features, 15);
    } else {
      return calculateTree64(features, 16);
    }
  }
  if (node === 35) return -0.17871122389263083;
  if (node === 36) return -0.3572483676398313;
  if (node === 17) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree64(features, 35);
    } else if (val <= 127.00000000000001) {
      return calculateTree64(features, 35);
    } else {
      return calculateTree64(features, 36);
    }
  }
  if (node === 18) return -0.009199042740487818;
  if (node === 8) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree64(features, 17);
    } else if (val <= 432.00000000000006) {
      return calculateTree64(features, 17);
    } else {
      return calculateTree64(features, 18);
    }
  }
  if (node === 3) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree64(features, 7);
    } else if (val <= 422.00000000000006) {
      return calculateTree64(features, 7);
    } else {
      return calculateTree64(features, 8);
    }
  }
  if (node === 9) return -0.29487596778800357;
  if (node === 10) return -0.04193314848639218;
  if (node === 4) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree64(features, 9);
    } else if (val <= 705.5000000000001) {
      return calculateTree64(features, 9);
    } else {
      return calculateTree64(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree64(features, 3);
    } else if (val <= 205.50000000000003) {
      return calculateTree64(features, 3);
    } else {
      return calculateTree64(features, 4);
    }
  }
  if (node === 5) return 0.0906735294497774;
  if (node === 223) return -0.07013545891483265;
  if (node === 449) return 0.078739646363642;
  if (node === 901) return -0.14516388237814987;
  if (node === 1805) return -0.13853461370205516;
  if (node === 1806) return 0.0575108271864341;
  if (node === 902) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree64(features, 1805);
    } else if (val <= 571.5000000000001) {
      return calculateTree64(features, 1805);
    } else {
      return calculateTree64(features, 1806);
    }
  }
  if (node === 450) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree64(features, 901);
    } else if (val <= 217.50000000000003) {
      return calculateTree64(features, 901);
    } else {
      return calculateTree64(features, 902);
    }
  }
  if (node === 224) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree64(features, 449);
    } else if (val <= 284.50000000000006) {
      return calculateTree64(features, 449);
    } else {
      return calculateTree64(features, 450);
    }
  }
  if (node === 111) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree64(features, 223);
    } else if (val <= 240.00000000000003) {
      return calculateTree64(features, 223);
    } else {
      return calculateTree64(features, 224);
    }
  }
  if (node === 112) return -0.028627657503853467;
  if (node === 55) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree64(features, 111);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree64(features, 111);
    } else {
      return calculateTree64(features, 112);
    }
  }
  if (node === 113) return -0.01967261520096519;
  if (node === 114) return -0.3242589786826657;
  if (node === 56) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree64(features, 113);
    } else if (val <= 799.5000000000001) {
      return calculateTree64(features, 113);
    } else {
      return calculateTree64(features, 114);
    }
  }
  if (node === 27) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree64(features, 55);
    } else if (val <= 232.50000000000003) {
      return calculateTree64(features, 55);
    } else {
      return calculateTree64(features, 56);
    }
  }
  if (node === 231) return 0.016521584357301767;
  if (node === 232) return -0.42003136473895714;
  if (node === 115) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree64(features, 231);
    } else if (val <= 504.50000000000006) {
      return calculateTree64(features, 231);
    } else {
      return calculateTree64(features, 232);
    }
  }
  if (node === 116) return 0.06674161226353913;
  if (node === 57) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree64(features, 115);
    } else if (val <= 251.00000000000003) {
      return calculateTree64(features, 115);
    } else {
      return calculateTree64(features, 116);
    }
  }
  if (node === 117) return 0.23861597520726616;
  if (node === 475) return -0.12207522111523945;
  if (node === 476) return 0.2426852216174508;
  if (node === 237) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree64(features, 475);
    } else if (val <= 26.500000000000004) {
      return calculateTree64(features, 475);
    } else {
      return calculateTree64(features, 476);
    }
  }
  if (node === 477) return 0.1256786809551032;
  if (node === 478) return 0.031213522081719576;
  if (node === 238) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree64(features, 477);
    } else if (val <= 800.5000000000001) {
      return calculateTree64(features, 477);
    } else {
      return calculateTree64(features, 478);
    }
  }
  if (node === 118) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree64(features, 237);
    } else if (val <= 778.5000000000001) {
      return calculateTree64(features, 237);
    } else {
      return calculateTree64(features, 238);
    }
  }
  if (node === 58) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree64(features, 117);
    } else if (val <= 648.5000000000001) {
      return calculateTree64(features, 117);
    } else {
      return calculateTree64(features, 118);
    }
  }
  if (node === 28) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree64(features, 57);
    } else if (val <= 511.50000000000006) {
      return calculateTree64(features, 57);
    } else {
      return calculateTree64(features, 58);
    }
  }
  if (node === 13) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree64(features, 27);
    } else if (val <= 241.50000000000003) {
      return calculateTree64(features, 27);
    } else {
      return calculateTree64(features, 28);
    }
  }
  if (node === 479) return 0.056915693399744505;
  if (node === 480) return -0.18626146459033208;
  if (node === 239) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree64(features, 479);
    } else if (val <= 3.5000000000000004) {
      return calculateTree64(features, 479);
    } else {
      return calculateTree64(features, 480);
    }
  }
  if (node === 240) return -0.35285473884971;
  if (node === 119) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree64(features, 239);
    } else if (val <= 270.00000000000006) {
      return calculateTree64(features, 239);
    } else {
      return calculateTree64(features, 240);
    }
  }
  if (node === 120) return 0.05160434028298138;
  if (node === 59) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree64(features, 119);
    } else if (val <= 276.00000000000006) {
      return calculateTree64(features, 119);
    } else {
      return calculateTree64(features, 120);
    }
  }
  if (node === 121) return -0.2422052174447608;
  if (node === 122) return 0.04505505592103135;
  if (node === 60) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree64(features, 121);
    } else if (val <= 839.5000000000001) {
      return calculateTree64(features, 121);
    } else {
      return calculateTree64(features, 122);
    }
  }
  if (node === 29) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree64(features, 59);
    } else if (val <= 659.5000000000001) {
      return calculateTree64(features, 59);
    } else {
      return calculateTree64(features, 60);
    }
  }
  if (node === 30) return -0.00024381665976311252;
  if (node === 14) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree64(features, 29);
    } else if (val <= 275.50000000000006) {
      return calculateTree64(features, 29);
    } else {
      return calculateTree64(features, 30);
    }
  }
  if (node === 6) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree64(features, 13);
    } else if (val <= 253.50000000000003) {
      return calculateTree64(features, 13);
    } else {
      return calculateTree64(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree64(features, 5);
    } else if (val <= 208.50000000000003) {
      return calculateTree64(features, 5);
    } else {
      return calculateTree64(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree64(features, 1);
    } else if (val <= 207.00000000000003) {
      return calculateTree64(features, 1);
    } else {
      return calculateTree64(features, 2);
    }
  }
}
function calculateTree65(features, node = 0) {
  if (node === 15) return 0.0011053853844377529;
  if (node === 16) return 0.06050366151459899;
  if (node === 7) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree65(features, 15);
    } else if (val <= 665.0000000000001) {
      return calculateTree65(features, 15);
    } else {
      return calculateTree65(features, 16);
    }
  }
  if (node === 35) return 0.0640235532620279;
  if (node === 147) return 0.012026410170520871;
  if (node === 595) return -0.1169338287437653;
  if (node === 596) return 0.6560373471047933;
  if (node === 297) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree65(features, 595);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree65(features, 595);
    } else {
      return calculateTree65(features, 596);
    }
  }
  if (node === 298) return -0.335272048624262;
  if (node === 148) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree65(features, 297);
    } else if (val <= 577.5000000000001) {
      return calculateTree65(features, 297);
    } else {
      return calculateTree65(features, 298);
    }
  }
  if (node === 73) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree65(features, 147);
    } else if (val <= 672.0000000000001) {
      return calculateTree65(features, 147);
    } else {
      return calculateTree65(features, 148);
    }
  }
  if (node === 74) return -0.33896227105788984;
  if (node === 36) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree65(features, 73);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree65(features, 73);
    } else {
      return calculateTree65(features, 74);
    }
  }
  if (node === 17) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree65(features, 35);
    } else if (val <= 338.50000000000006) {
      return calculateTree65(features, 35);
    } else {
      return calculateTree65(features, 36);
    }
  }
  if (node === 37) return -0.016106092113979256;
  if (node === 38) return 0.1429717141145802;
  if (node === 18) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree65(features, 37);
    } else if (val <= 29.500000000000004) {
      return calculateTree65(features, 37);
    } else {
      return calculateTree65(features, 38);
    }
  }
  if (node === 8) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree65(features, 17);
    } else if (val <= 954.5000000000001) {
      return calculateTree65(features, 17);
    } else {
      return calculateTree65(features, 18);
    }
  }
  if (node === 3) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree65(features, 7);
    } else if (val <= 668.0000000000001) {
      return calculateTree65(features, 7);
    } else {
      return calculateTree65(features, 8);
    }
  }
  if (node === 19) return -0.11955701580228388;
  if (node === 83) return 0.7878385941676266;
  if (node === 84) return -0.041490640210598;
  if (node === 41) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree65(features, 83);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree65(features, 83);
    } else {
      return calculateTree65(features, 84);
    }
  }
  if (node === 42) return -0.13029493636539852;
  if (node === 20) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree65(features, 41);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree65(features, 41);
    } else {
      return calculateTree65(features, 42);
    }
  }
  if (node === 9) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree65(features, 19);
    } else if (val <= 705.5000000000001) {
      return calculateTree65(features, 19);
    } else {
      return calculateTree65(features, 20);
    }
  }
  if (node === 87) return -0.38060823794417337;
  if (node === 177) return 0.4263145607111312;
  if (node === 178) return 0.05849898859336022;
  if (node === 88) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree65(features, 177);
    } else if (val <= 28.500000000000004) {
      return calculateTree65(features, 177);
    } else {
      return calculateTree65(features, 178);
    }
  }
  if (node === 43) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree65(features, 87);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree65(features, 87);
    } else {
      return calculateTree65(features, 88);
    }
  }
  if (node === 179) return 0.24725133449054304;
  if (node === 361) return -0.337982130186861;
  if (node === 725) return 0.8157013686940083;
  if (node === 1453) return -0.34089807521162213;
  if (node === 1454) return 0.2307218677036426;
  if (node === 726) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree65(features, 1453);
    } else if (val <= 705.5000000000001) {
      return calculateTree65(features, 1453);
    } else {
      return calculateTree65(features, 1454);
    }
  }
  if (node === 362) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree65(features, 725);
    } else if (val <= 690.5000000000001) {
      return calculateTree65(features, 725);
    } else {
      return calculateTree65(features, 726);
    }
  }
  if (node === 180) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree65(features, 361);
    } else if (val <= 740.5000000000001) {
      return calculateTree65(features, 361);
    } else {
      return calculateTree65(features, 362);
    }
  }
  if (node === 89) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree65(features, 179);
    } else if (val <= 551.5000000000001) {
      return calculateTree65(features, 179);
    } else {
      return calculateTree65(features, 180);
    }
  }
  if (node === 181) return -0.4375405809055098;
  if (node === 182) return 0.15090025527248976;
  if (node === 90) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree65(features, 181);
    } else if (val <= 551.5000000000001) {
      return calculateTree65(features, 181);
    } else {
      return calculateTree65(features, 182);
    }
  }
  if (node === 44) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree65(features, 89);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree65(features, 89);
    } else {
      return calculateTree65(features, 90);
    }
  }
  if (node === 21) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree65(features, 43);
    } else if (val <= 678.5000000000001) {
      return calculateTree65(features, 43);
    } else {
      return calculateTree65(features, 44);
    }
  }
  if (node === 45) return -0.29024730700795426;
  if (node === 93) return -0.08633669525990155;
  if (node === 189) return 0.2728973219130507;
  if (node === 190) return -0.09182809927445013;
  if (node === 94) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree65(features, 189);
    } else if (val <= 690.5000000000001) {
      return calculateTree65(features, 189);
    } else {
      return calculateTree65(features, 190);
    }
  }
  if (node === 46) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree65(features, 93);
    } else if (val <= 26.500000000000004) {
      return calculateTree65(features, 93);
    } else {
      return calculateTree65(features, 94);
    }
  }
  if (node === 22) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree65(features, 45);
    } else if (val <= 678.5000000000001) {
      return calculateTree65(features, 45);
    } else {
      return calculateTree65(features, 46);
    }
  }
  if (node === 10) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree65(features, 21);
    } else if (val <= 785.5000000000001) {
      return calculateTree65(features, 21);
    } else {
      return calculateTree65(features, 22);
    }
  }
  if (node === 4) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree65(features, 9);
    } else if (val <= 314.50000000000006) {
      return calculateTree65(features, 9);
    } else {
      return calculateTree65(features, 10);
    }
  }
  if (node === 1) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree65(features, 3);
    } else if (val <= 677.5000000000001) {
      return calculateTree65(features, 3);
    } else {
      return calculateTree65(features, 4);
    }
  }
  if (node === 5) return -0.002304750195120267;
  if (node === 13) return -0.2162235504793923;
  if (node === 29) return -0.28639888494365795;
  if (node === 30) return 0.0060352368687145806;
  if (node === 14) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree65(features, 29);
    } else if (val <= 718.5000000000001) {
      return calculateTree65(features, 29);
    } else {
      return calculateTree65(features, 30);
    }
  }
  if (node === 6) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree65(features, 13);
    } else if (val <= 948.5000000000001) {
      return calculateTree65(features, 13);
    } else {
      return calculateTree65(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree65(features, 5);
    } else if (val <= 919.5000000000001) {
      return calculateTree65(features, 5);
    } else {
      return calculateTree65(features, 6);
    }
  }
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree65(features, 1);
    } else if (val <= 708.5000000000001) {
      return calculateTree65(features, 1);
    } else {
      return calculateTree65(features, 2);
    }
  }
}
function calculateTree66(features, node = 0) {
  if (node === 3) return 0.0004382116234445985;
  if (node === 19) return 0.1072016481661813;
  if (node === 20) return 0.28061114704379386;
  if (node === 9) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree66(features, 19);
    } else if (val <= 19.500000000000004) {
      return calculateTree66(features, 19);
    } else {
      return calculateTree66(features, 20);
    }
  }
  if (node === 10) return 0.007557084884798611;
  if (node === 4) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree66(features, 9);
    } else if (val <= 1.5000000000000002) {
      return calculateTree66(features, 9);
    } else {
      return calculateTree66(features, 10);
    }
  }
  if (node === 1) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree66(features, 3);
    } else if (val <= 718.5000000000001) {
      return calculateTree66(features, 3);
    } else {
      return calculateTree66(features, 4);
    }
  }
  if (node === 383) return -0.3181435107631668;
  if (node === 384) return 0.17931958203750273;
  if (node === 191) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree66(features, 383);
    } else if (val <= 275.50000000000006) {
      return calculateTree66(features, 383);
    } else {
      return calculateTree66(features, 384);
    }
  }
  if (node === 192) return -0.3046801627661851;
  if (node === 95) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree66(features, 191);
    } else if (val <= 338.50000000000006) {
      return calculateTree66(features, 191);
    } else {
      return calculateTree66(features, 192);
    }
  }
  if (node === 96) return -0.32947822199441773;
  if (node === 47) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree66(features, 95);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree66(features, 95);
    } else {
      return calculateTree66(features, 96);
    }
  }
  if (node === 1567) return -0.0034839395936993867;
  if (node === 1568) return 0.30113246485700895;
  if (node === 783) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree66(features, 1567);
    } else if (val <= 222.50000000000003) {
      return calculateTree66(features, 1567);
    } else {
      return calculateTree66(features, 1568);
    }
  }
  if (node === 784) return -0.3029520076444587;
  if (node === 391) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree66(features, 783);
    } else if (val <= 232.50000000000003) {
      return calculateTree66(features, 783);
    } else {
      return calculateTree66(features, 784);
    }
  }
  if (node === 1571) return -0.31099365971189014;
  if (node === 1572) return 0.1017988216993435;
  if (node === 785) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree66(features, 1571);
    } else if (val <= 969.5000000000001) {
      return calculateTree66(features, 1571);
    } else {
      return calculateTree66(features, 1572);
    }
  }
  if (node === 1573) return 0.0674280679967598;
  if (node === 1574) return -0.34733954915915666;
  if (node === 786) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree66(features, 1573);
    } else if (val <= 969.5000000000001) {
      return calculateTree66(features, 1573);
    } else {
      return calculateTree66(features, 1574);
    }
  }
  if (node === 392) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree66(features, 785);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree66(features, 785);
    } else {
      return calculateTree66(features, 786);
    }
  }
  if (node === 195) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree66(features, 391);
    } else if (val <= 241.50000000000003) {
      return calculateTree66(features, 391);
    } else {
      return calculateTree66(features, 392);
    }
  }
  if (node === 1575) return 0.2391680194624712;
  if (node === 1576) return -0.20116384627764586;
  if (node === 787) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree66(features, 1575);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree66(features, 1575);
    } else {
      return calculateTree66(features, 1576);
    }
  }
  if (node === 788) return -0.318737417492641;
  if (node === 393) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree66(features, 787);
    } else if (val <= 781.0000000000001) {
      return calculateTree66(features, 787);
    } else {
      return calculateTree66(features, 788);
    }
  }
  if (node === 394) return -0.005206922884937141;
  if (node === 196) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree66(features, 393);
    } else if (val <= 821.0000000000001) {
      return calculateTree66(features, 393);
    } else {
      return calculateTree66(features, 394);
    }
  }
  if (node === 97) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree66(features, 195);
    } else if (val <= 285.00000000000006) {
      return calculateTree66(features, 195);
    } else {
      return calculateTree66(features, 196);
    }
  }
  if (node === 197) return -0.33713398205282863;
  if (node === 397) return 0.28513244825335515;
  if (node === 398) return 0.07825159627979231;
  if (node === 198) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree66(features, 397);
    } else if (val <= 1.5000000000000002) {
      return calculateTree66(features, 397);
    } else {
      return calculateTree66(features, 398);
    }
  }
  if (node === 98) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree66(features, 197);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree66(features, 197);
    } else {
      return calculateTree66(features, 198);
    }
  }
  if (node === 48) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree66(features, 97);
    } else if (val <= 361.50000000000006) {
      return calculateTree66(features, 97);
    } else {
      return calculateTree66(features, 98);
    }
  }
  if (node === 23) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree66(features, 47);
    } else if (val <= 771.0000000000001) {
      return calculateTree66(features, 47);
    } else {
      return calculateTree66(features, 48);
    }
  }
  if (node === 199) return -0.28236533887569876;
  if (node === 200) return 0.4772574591925472;
  if (node === 99) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree66(features, 199);
    } else if (val <= 461.50000000000006) {
      return calculateTree66(features, 199);
    } else {
      return calculateTree66(features, 200);
    }
  }
  if (node === 201) return 0.06997488668680765;
  if (node === 202) return -0.24758804528281894;
  if (node === 100) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree66(features, 201);
    } else if (val <= 925.5000000000001) {
      return calculateTree66(features, 201);
    } else {
      return calculateTree66(features, 202);
    }
  }
  if (node === 49) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree66(features, 99);
    } else if (val <= 835.5000000000001) {
      return calculateTree66(features, 99);
    } else {
      return calculateTree66(features, 100);
    }
  }
  if (node === 50) return -0.2795660588212948;
  if (node === 24) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree66(features, 49);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree66(features, 49);
    } else {
      return calculateTree66(features, 50);
    }
  }
  if (node === 11) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree66(features, 23);
    } else if (val <= 387.50000000000006) {
      return calculateTree66(features, 23);
    } else {
      return calculateTree66(features, 24);
    }
  }
  if (node === 25) return 0.047947735501852086;
  if (node === 53) return 0.01936823180067149;
  if (node === 54) return -0.010339955267802443;
  if (node === 26) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree66(features, 53);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree66(features, 53);
    } else {
      return calculateTree66(features, 54);
    }
  }
  if (node === 12) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree66(features, 25);
    } else if (val <= 496.00000000000006) {
      return calculateTree66(features, 25);
    } else {
      return calculateTree66(features, 26);
    }
  }
  if (node === 5) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree66(features, 11);
    } else if (val <= 470.50000000000006) {
      return calculateTree66(features, 11);
    } else {
      return calculateTree66(features, 12);
    }
  }
  if (node === 13) return -0.3131128536399729;
  if (node === 14) return -0.02191376576137062;
  if (node === 6) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree66(features, 13);
    } else if (val <= 783.5000000000001) {
      return calculateTree66(features, 13);
    } else {
      return calculateTree66(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree66(features, 5);
    } else if (val <= 938.5000000000001) {
      return calculateTree66(features, 5);
    } else {
      return calculateTree66(features, 6);
    }
  }
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree66(features, 1);
    } else if (val <= 755.0000000000001) {
      return calculateTree66(features, 1);
    } else {
      return calculateTree66(features, 2);
    }
  }
}
function calculateTree67(features, node = 0) {
  if (node === 31) return -0.34494960805845426;
  if (node === 32) return 0.13998446513673862;
  if (node === 15) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree67(features, 31);
    } else if (val <= 70.00000000000001) {
      return calculateTree67(features, 31);
    } else {
      return calculateTree67(features, 32);
    }
  }
  if (node === 67) return -0.23034261487536406;
  if (node === 68) return 0.08250216451626506;
  if (node === 33) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree67(features, 67);
    } else if (val <= 99.50000000000001) {
      return calculateTree67(features, 67);
    } else {
      return calculateTree67(features, 68);
    }
  }
  if (node === 34) return -0.35255206508412845;
  if (node === 16) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree67(features, 33);
    } else if (val <= 70.00000000000001) {
      return calculateTree67(features, 33);
    } else {
      return calculateTree67(features, 34);
    }
  }
  if (node === 7) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree67(features, 15);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree67(features, 15);
    } else {
      return calculateTree67(features, 16);
    }
  }
  if (node === 143) return 0.13843639408322028;
  if (node === 144) return 0.01135341144742599;
  if (node === 71) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree67(features, 143);
    } else if (val <= 201.50000000000003) {
      return calculateTree67(features, 143);
    } else {
      return calculateTree67(features, 144);
    }
  }
  if (node === 145) return -0.35725571283286334;
  if (node === 146) return 0.22973311417636844;
  if (node === 72) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree67(features, 145);
    } else if (val <= 953.5000000000001) {
      return calculateTree67(features, 145);
    } else {
      return calculateTree67(features, 146);
    }
  }
  if (node === 35) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree67(features, 71);
    } else if (val <= 935.5000000000001) {
      return calculateTree67(features, 71);
    } else {
      return calculateTree67(features, 72);
    }
  }
  if (node === 36) return -0.34403922583638347;
  if (node === 17) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree67(features, 35);
    } else if (val <= 86.50000000000001) {
      return calculateTree67(features, 35);
    } else {
      return calculateTree67(features, 36);
    }
  }
  if (node === 37) return -0.3335634424234665;
  if (node === 623) return 0.19534287263874525;
  if (node === 1249) return 0.05167052698247606;
  if (node === 1250) return -0.2209397962585211;
  if (node === 624) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree67(features, 1249);
    } else if (val <= 16.500000000000004) {
      return calculateTree67(features, 1249);
    } else {
      return calculateTree67(features, 1250);
    }
  }
  if (node === 311) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree67(features, 623);
    } else if (val <= 310.00000000000006) {
      return calculateTree67(features, 623);
    } else {
      return calculateTree67(features, 624);
    }
  }
  if (node === 312) return -0.2980888125082414;
  if (node === 155) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree67(features, 311);
    } else if (val <= 531.0000000000001) {
      return calculateTree67(features, 311);
    } else {
      return calculateTree67(features, 312);
    }
  }
  if (node === 313) return -0.3118435975482377;
  if (node === 314) return -0.03384303312470275;
  if (node === 156) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree67(features, 313);
    } else if (val <= 70.00000000000001) {
      return calculateTree67(features, 313);
    } else {
      return calculateTree67(features, 314);
    }
  }
  if (node === 77) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree67(features, 155);
    } else if (val <= 41.50000000000001) {
      return calculateTree67(features, 155);
    } else {
      return calculateTree67(features, 156);
    }
  }
  if (node === 157) return 0.13907532021093427;
  if (node === 635) return 0.08125223510938698;
  if (node === 636) return -0.2526835510752278;
  if (node === 317) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree67(features, 635);
    } else if (val <= 17.500000000000004) {
      return calculateTree67(features, 635);
    } else {
      return calculateTree67(features, 636);
    }
  }
  if (node === 1275) return 0.12231061933980919;
  if (node === 1276) return 0.49241415787109666;
  if (node === 637) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree67(features, 1275);
    } else if (val <= 2.5000000000000004) {
      return calculateTree67(features, 1275);
    } else {
      return calculateTree67(features, 1276);
    }
  }
  if (node === 1277) return -0.20203624037743795;
  if (node === 1278) return 0.057498290374228635;
  if (node === 638) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree67(features, 1277);
    } else if (val <= 935.5000000000001) {
      return calculateTree67(features, 1277);
    } else {
      return calculateTree67(features, 1278);
    }
  }
  if (node === 318) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree67(features, 637);
    } else if (val <= 850.5000000000001) {
      return calculateTree67(features, 637);
    } else {
      return calculateTree67(features, 638);
    }
  }
  if (node === 158) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree67(features, 317);
    } else if (val <= 778.5000000000001) {
      return calculateTree67(features, 317);
    } else {
      return calculateTree67(features, 318);
    }
  }
  if (node === 78) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree67(features, 157);
    } else if (val <= 678.5000000000001) {
      return calculateTree67(features, 157);
    } else {
      return calculateTree67(features, 158);
    }
  }
  if (node === 38) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree67(features, 77);
    } else if (val <= 672.0000000000001) {
      return calculateTree67(features, 77);
    } else {
      return calculateTree67(features, 78);
    }
  }
  if (node === 18) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree67(features, 37);
    } else if (val <= 229.50000000000003) {
      return calculateTree67(features, 37);
    } else {
      return calculateTree67(features, 38);
    }
  }
  if (node === 8) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree67(features, 17);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree67(features, 17);
    } else {
      return calculateTree67(features, 18);
    }
  }
  if (node === 3) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree67(features, 7);
    } else if (val <= 175.50000000000003) {
      return calculateTree67(features, 7);
    } else {
      return calculateTree67(features, 8);
    }
  }
  if (node === 4) return 0.00010964342936769126;
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree67(features, 3);
    } else if (val <= 97.50000000000001) {
      return calculateTree67(features, 3);
    } else {
      return calculateTree67(features, 4);
    }
  }
  if (node === 23) return -0.06392585007367836;
  if (node === 24) return 0.5840934848705587;
  if (node === 11) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree67(features, 23);
    } else if (val <= 3.5000000000000004) {
      return calculateTree67(features, 23);
    } else {
      return calculateTree67(features, 24);
    }
  }
  if (node === 12) return -0.2788389438183194;
  if (node === 5) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree67(features, 11);
    } else if (val <= 253.50000000000003) {
      return calculateTree67(features, 11);
    } else {
      return calculateTree67(features, 12);
    }
  }
  if (node === 13) return 0.023106221456899136;
  if (node === 59) return -0.308088832493801;
  if (node === 60) return 1.2774230276418064;
  if (node === 29) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree67(features, 59);
    } else if (val <= 725.5000000000001) {
      return calculateTree67(features, 59);
    } else {
      return calculateTree67(features, 60);
    }
  }
  if (node === 30) return -0.27212402749220366;
  if (node === 14) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree67(features, 29);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree67(features, 29);
    } else {
      return calculateTree67(features, 30);
    }
  }
  if (node === 6) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree67(features, 13);
    } else if (val <= 606.5000000000001) {
      return calculateTree67(features, 13);
    } else {
      return calculateTree67(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree67(features, 5);
    } else if (val <= 551.5000000000001) {
      return calculateTree67(features, 5);
    } else {
      return calculateTree67(features, 6);
    }
  }
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree67(features, 1);
    } else if (val <= 978.0000000000001) {
      return calculateTree67(features, 1);
    } else {
      return calculateTree67(features, 2);
    }
  }
}
function calculateTree68(features, node = 0) {
  if (node === 7) return -0.0005905805885531428;
  if (node === 143) return 0.03502718970160571;
  if (node === 1159) return 0.1475473063855802;
  if (node === 1160) return -0.1324720526145488;
  if (node === 579) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree68(features, 1159);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree68(features, 1159);
    } else {
      return calculateTree68(features, 1160);
    }
  }
  if (node === 1161) return -0.05112643663564758;
  if (node === 1162) return 0.3351721366154168;
  if (node === 580) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree68(features, 1161);
    } else if (val <= 25.500000000000004) {
      return calculateTree68(features, 1161);
    } else {
      return calculateTree68(features, 1162);
    }
  }
  if (node === 289) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree68(features, 579);
    } else if (val <= 1.5000000000000002) {
      return calculateTree68(features, 579);
    } else {
      return calculateTree68(features, 580);
    }
  }
  if (node === 290) return -0.35628924634373305;
  if (node === 144) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree68(features, 289);
    } else if (val <= 27.500000000000004) {
      return calculateTree68(features, 289);
    } else {
      return calculateTree68(features, 290);
    }
  }
  if (node === 71) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree68(features, 143);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree68(features, 143);
    } else {
      return calculateTree68(features, 144);
    }
  }
  if (node === 145) return -0.14528994734160935;
  if (node === 293) return 0.5327749336392903;
  if (node === 294) return 0.13100585502697865;
  if (node === 146) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree68(features, 293);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree68(features, 293);
    } else {
      return calculateTree68(features, 294);
    }
  }
  if (node === 72) {
    const val = features['is_preference_sports'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree68(features, 145);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree68(features, 145);
    } else {
      return calculateTree68(features, 146);
    }
  }
  if (node === 35) {
    const val = features['is_preference_men_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree68(features, 71);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree68(features, 71);
    } else {
      return calculateTree68(features, 72);
    }
  }
  if (node === 295) return -0.16259366361744373;
  if (node === 296) return 0.1587796605904315;
  if (node === 147) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree68(features, 295);
    } else if (val <= 62.50000000000001) {
      return calculateTree68(features, 295);
    } else {
      return calculateTree68(features, 296);
    }
  }
  if (node === 297) return -0.6342381803016643;
  if (node === 298) return -0.26726936685219094;
  if (node === 148) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree68(features, 297);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree68(features, 297);
    } else {
      return calculateTree68(features, 298);
    }
  }
  if (node === 73) {
    const val = features['is_preference_kids'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree68(features, 147);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree68(features, 147);
    } else {
      return calculateTree68(features, 148);
    }
  }
  if (node === 299) return -0.1738552321330728;
  if (node === 300) return 0.12610542661523247;
  if (node === 149) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree68(features, 299);
    } else if (val <= 2.5000000000000004) {
      return calculateTree68(features, 299);
    } else {
      return calculateTree68(features, 300);
    }
  }
  if (node === 150) return -0.2258848816656983;
  if (node === 74) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree68(features, 149);
    } else if (val <= 916.0000000000001) {
      return calculateTree68(features, 149);
    } else {
      return calculateTree68(features, 150);
    }
  }
  if (node === 36) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree68(features, 73);
    } else if (val <= 749.0000000000001) {
      return calculateTree68(features, 73);
    } else {
      return calculateTree68(features, 74);
    }
  }
  if (node === 17) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree68(features, 35);
    } else if (val <= 29.500000000000004) {
      return calculateTree68(features, 35);
    } else {
      return calculateTree68(features, 36);
    }
  }
  if (node === 18) return 0.14845637891075705;
  if (node === 8) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree68(features, 17);
    } else if (val <= 24.500000000000004) {
      return calculateTree68(features, 17);
    } else {
      return calculateTree68(features, 18);
    }
  }
  if (node === 3) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree68(features, 7);
    } else if (val <= 969.5000000000001) {
      return calculateTree68(features, 7);
    } else {
      return calculateTree68(features, 8);
    }
  }
  if (node === 19) return 0.1625681762716127;
  if (node === 20) return -0.3106758415691231;
  if (node === 9) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree68(features, 19);
    } else if (val <= 975.0000000000001) {
      return calculateTree68(features, 19);
    } else {
      return calculateTree68(features, 20);
    }
  }
  if (node === 175) return 0.022585893516959728;
  if (node === 176) return -0.19094592915171588;
  if (node === 87) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree68(features, 175);
    } else if (val <= 40.50000000000001) {
      return calculateTree68(features, 175);
    } else {
      return calculateTree68(features, 176);
    }
  }
  if (node === 177) return 0.3892264352786727;
  if (node === 357) return -0.10002967166260525;
  if (node === 358) return 0.11556059616840235;
  if (node === 178) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree68(features, 357);
    } else if (val <= 1.5000000000000002) {
      return calculateTree68(features, 357);
    } else {
      return calculateTree68(features, 358);
    }
  }
  if (node === 88) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree68(features, 177);
    } else if (val <= 14.500000000000002) {
      return calculateTree68(features, 177);
    } else {
      return calculateTree68(features, 178);
    }
  }
  if (node === 43) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree68(features, 87);
    } else if (val <= 176.50000000000003) {
      return calculateTree68(features, 87);
    } else {
      return calculateTree68(features, 88);
    }
  }
  if (node === 179) return -0.39710212237962267;
  if (node === 180) return -0.009506279883454385;
  if (node === 89) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree68(features, 179);
    } else if (val <= 26.500000000000004) {
      return calculateTree68(features, 179);
    } else {
      return calculateTree68(features, 180);
    }
  }
  if (node === 181) return 0.17551441803775686;
  if (node === 182) return -0.17271516968325043;
  if (node === 90) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree68(features, 181);
    } else if (val <= 576.0000000000001) {
      return calculateTree68(features, 181);
    } else {
      return calculateTree68(features, 182);
    }
  }
  if (node === 44) {
    const val = features['is_preference_men_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree68(features, 89);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree68(features, 89);
    } else {
      return calculateTree68(features, 90);
    }
  }
  if (node === 21) {
    const val = features['is_preference_pets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree68(features, 43);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree68(features, 43);
    } else {
      return calculateTree68(features, 44);
    }
  }
  if (node === 45) return 0.047108540605829044;
  if (node === 46) return 0.18370300684394272;
  if (node === 22) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree68(features, 45);
    } else if (val <= 4.500000000000001) {
      return calculateTree68(features, 45);
    } else {
      return calculateTree68(features, 46);
    }
  }
  if (node === 10) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree68(features, 21);
    } else if (val <= 44.50000000000001) {
      return calculateTree68(features, 21);
    } else {
      return calculateTree68(features, 22);
    }
  }
  if (node === 4) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree68(features, 9);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree68(features, 9);
    } else {
      return calculateTree68(features, 10);
    }
  }
  if (node === 1) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree68(features, 3);
    } else if (val <= 973.5000000000001) {
      return calculateTree68(features, 3);
    } else {
      return calculateTree68(features, 4);
    }
  }
  if (node === 2) return -0.05187626423728274;
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree68(features, 1);
    } else if (val <= 978.0000000000001) {
      return calculateTree68(features, 1);
    } else {
      return calculateTree68(features, 2);
    }
  }
}
function calculateTree69(features, node = 0) {
  if (node === 7) return 0.011994052310272745;
  if (node === 35) return 0.07666522581247795;
  if (node === 73) return -0.29142275510832966;
  if (node === 74) return 0.007213395113604787;
  if (node === 36) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree69(features, 73);
    } else if (val <= 181.50000000000003) {
      return calculateTree69(features, 73);
    } else {
      return calculateTree69(features, 74);
    }
  }
  if (node === 17) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree69(features, 35);
    } else if (val <= 46.50000000000001) {
      return calculateTree69(features, 35);
    } else {
      return calculateTree69(features, 36);
    }
  }
  if (node === 18) return 0.3935518043947987;
  if (node === 8) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree69(features, 17);
    } else if (val <= 62.50000000000001) {
      return calculateTree69(features, 17);
    } else {
      return calculateTree69(features, 18);
    }
  }
  if (node === 3) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree69(features, 7);
    } else if (val <= 307.00000000000006) {
      return calculateTree69(features, 7);
    } else {
      return calculateTree69(features, 8);
    }
  }
  if (node === 79) return -0.11917114729571274;
  if (node === 80) return 0.17887679396366557;
  if (node === 39) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree69(features, 79);
    } else if (val <= 34.50000000000001) {
      return calculateTree69(features, 79);
    } else {
      return calculateTree69(features, 80);
    }
  }
  if (node === 40) return -0.019696143845153922;
  if (node === 19) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree69(features, 39);
    } else if (val <= 3.5000000000000004) {
      return calculateTree69(features, 39);
    } else {
      return calculateTree69(features, 40);
    }
  }
  if (node === 20) return 0.025725956501242483;
  if (node === 9) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree69(features, 19);
    } else if (val <= 148.00000000000003) {
      return calculateTree69(features, 19);
    } else {
      return calculateTree69(features, 20);
    }
  }
  if (node === 87) return 0.011219785377070099;
  if (node === 88) return -0.27025418211357266;
  if (node === 43) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree69(features, 87);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree69(features, 87);
    } else {
      return calculateTree69(features, 88);
    }
  }
  if (node === 359) return -0.12260963420934691;
  if (node === 360) return 0.16492724627358116;
  if (node === 179) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree69(features, 359);
    } else if (val <= 40.50000000000001) {
      return calculateTree69(features, 359);
    } else {
      return calculateTree69(features, 360);
    }
  }
  if (node === 180) return 0.1951964036665481;
  if (node === 89) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree69(features, 179);
    } else if (val <= 361.50000000000006) {
      return calculateTree69(features, 179);
    } else {
      return calculateTree69(features, 180);
    }
  }
  if (node === 363) return 0.03219440164138608;
  if (node === 364) return -0.35242970715415395;
  if (node === 181) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree69(features, 363);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree69(features, 363);
    } else {
      return calculateTree69(features, 364);
    }
  }
  if (node === 182) return -0.0012772409427158685;
  if (node === 90) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree69(features, 181);
    } else if (val <= 386.50000000000006) {
      return calculateTree69(features, 181);
    } else {
      return calculateTree69(features, 182);
    }
  }
  if (node === 44) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree69(features, 89);
    } else if (val <= 383.50000000000006) {
      return calculateTree69(features, 89);
    } else {
      return calculateTree69(features, 90);
    }
  }
  if (node === 21) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree69(features, 43);
    } else if (val <= 350.50000000000006) {
      return calculateTree69(features, 43);
    } else {
      return calculateTree69(features, 44);
    }
  }
  if (node === 45) return 0.1894416724982951;
  if (node === 93) return -0.17279036566104197;
  if (node === 759) return 0.09213914451587332;
  if (node === 1521) return -0.009636203581500585;
  if (node === 1522) return -0.16562565454676093;
  if (node === 760) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree69(features, 1521);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree69(features, 1521);
    } else {
      return calculateTree69(features, 1522);
    }
  }
  if (node === 379) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree69(features, 759);
    } else if (val <= 24.500000000000004) {
      return calculateTree69(features, 759);
    } else {
      return calculateTree69(features, 760);
    }
  }
  if (node === 1523) return -0.013695581894134567;
  if (node === 1524) return 0.10186303044620464;
  if (node === 761) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree69(features, 1523);
    } else if (val <= 647.0000000000001) {
      return calculateTree69(features, 1523);
    } else {
      return calculateTree69(features, 1524);
    }
  }
  if (node === 762) return -0.22687621585844145;
  if (node === 380) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree69(features, 761);
    } else if (val <= 944.5000000000001) {
      return calculateTree69(features, 761);
    } else {
      return calculateTree69(features, 762);
    }
  }
  if (node === 189) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree69(features, 379);
    } else if (val <= 141.50000000000003) {
      return calculateTree69(features, 379);
    } else {
      return calculateTree69(features, 380);
    }
  }
  if (node === 763) return -0.16690573735413086;
  if (node === 1529) return -0.014107608852435114;
  if (node === 1530) return 0.15308257849929188;
  if (node === 764) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree69(features, 1529);
    } else if (val <= 417.00000000000006) {
      return calculateTree69(features, 1529);
    } else {
      return calculateTree69(features, 1530);
    }
  }
  if (node === 381) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree69(features, 763);
    } else if (val <= 511.00000000000006) {
      return calculateTree69(features, 763);
    } else {
      return calculateTree69(features, 764);
    }
  }
  if (node === 1531) return -0.01235751166894363;
  if (node === 1532) return -0.12147369771183983;
  if (node === 765) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree69(features, 1531);
    } else if (val <= 447.00000000000006) {
      return calculateTree69(features, 1531);
    } else {
      return calculateTree69(features, 1532);
    }
  }
  if (node === 766) return -0.026131031969928338;
  if (node === 382) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree69(features, 765);
    } else if (val <= 745.5000000000001) {
      return calculateTree69(features, 765);
    } else {
      return calculateTree69(features, 766);
    }
  }
  if (node === 190) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree69(features, 381);
    } else if (val <= 427.50000000000006) {
      return calculateTree69(features, 381);
    } else {
      return calculateTree69(features, 382);
    }
  }
  if (node === 94) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree69(features, 189);
    } else if (val <= 284.50000000000006) {
      return calculateTree69(features, 189);
    } else {
      return calculateTree69(features, 190);
    }
  }
  if (node === 46) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree69(features, 93);
    } else if (val <= 34.00000000000001) {
      return calculateTree69(features, 93);
    } else {
      return calculateTree69(features, 94);
    }
  }
  if (node === 22) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree69(features, 45);
    } else if (val <= 6.000000000000001) {
      return calculateTree69(features, 45);
    } else {
      return calculateTree69(features, 46);
    }
  }
  if (node === 10) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree69(features, 21);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree69(features, 21);
    } else {
      return calculateTree69(features, 22);
    }
  }
  if (node === 4) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree69(features, 9);
    } else if (val <= 322.00000000000006) {
      return calculateTree69(features, 9);
    } else {
      return calculateTree69(features, 10);
    }
  }
  if (node === 1) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree69(features, 3);
    } else if (val <= 378.50000000000006) {
      return calculateTree69(features, 3);
    } else {
      return calculateTree69(features, 4);
    }
  }
  if (node === 2) return -0.001673898921603088;
  if (node === 0) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree69(features, 1);
    } else if (val <= 1.5000000000000002) {
      return calculateTree69(features, 1);
    } else {
      return calculateTree69(features, 2);
    }
  }
}
function calculateTree70(features, node = 0) {
  if (node === 63) return -0.2892824044131463;
  if (node === 519) return 0.1129540515876585;
  if (node === 1041) return -0.3470895473373297;
  if (node === 1042) return 0.005634333705587527;
  if (node === 520) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree70(features, 1041);
    } else if (val <= 175.50000000000003) {
      return calculateTree70(features, 1041);
    } else {
      return calculateTree70(features, 1042);
    }
  }
  if (node === 259) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree70(features, 519);
    } else if (val <= 132.50000000000003) {
      return calculateTree70(features, 519);
    } else {
      return calculateTree70(features, 520);
    }
  }
  if (node === 260) return 0.2989576958957792;
  if (node === 129) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree70(features, 259);
    } else if (val <= 925.5000000000001) {
      return calculateTree70(features, 259);
    } else {
      return calculateTree70(features, 260);
    }
  }
  if (node === 261) return -0.31194277837529905;
  if (node === 262) return 0.11452579045212098;
  if (node === 130) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree70(features, 261);
    } else if (val <= 973.5000000000001) {
      return calculateTree70(features, 261);
    } else {
      return calculateTree70(features, 262);
    }
  }
  if (node === 64) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree70(features, 129);
    } else if (val <= 935.5000000000001) {
      return calculateTree70(features, 129);
    } else {
      return calculateTree70(features, 130);
    }
  }
  if (node === 31) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree70(features, 63);
    } else if (val <= 115.50000000000001) {
      return calculateTree70(features, 63);
    } else {
      return calculateTree70(features, 64);
    }
  }
  if (node === 32) return -0.3217939209104336;
  if (node === 15) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree70(features, 31);
    } else if (val <= 123.50000000000001) {
      return calculateTree70(features, 31);
    } else {
      return calculateTree70(features, 32);
    }
  }
  if (node === 1087) return 0.06919954023476838;
  if (node === 1088) return -0.6607723283768177;
  if (node === 543) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree70(features, 1087);
    } else if (val <= 150.50000000000003) {
      return calculateTree70(features, 1087);
    } else {
      return calculateTree70(features, 1088);
    }
  }
  if (node === 544) return 0.09324983281926638;
  if (node === 271) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree70(features, 543);
    } else if (val <= 158.50000000000003) {
      return calculateTree70(features, 543);
    } else {
      return calculateTree70(features, 544);
    }
  }
  if (node === 272) return 0.4436322669957428;
  if (node === 135) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree70(features, 271);
    } else if (val <= 709.5000000000001) {
      return calculateTree70(features, 271);
    } else {
      return calculateTree70(features, 272);
    }
  }
  if (node === 136) return -0.4618931121614782;
  if (node === 67) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree70(features, 135);
    } else if (val <= 712.5000000000001) {
      return calculateTree70(features, 135);
    } else {
      return calculateTree70(features, 136);
    }
  }
  if (node === 68) return 0.1409949157246918;
  if (node === 33) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree70(features, 67);
    } else if (val <= 781.0000000000001) {
      return calculateTree70(features, 67);
    } else {
      return calculateTree70(features, 68);
    }
  }
  if (node === 34) return -0.3272548120484867;
  if (node === 16) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree70(features, 33);
    } else if (val <= 969.5000000000001) {
      return calculateTree70(features, 33);
    } else {
      return calculateTree70(features, 34);
    }
  }
  if (node === 7) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree70(features, 15);
    } else if (val <= 135.50000000000003) {
      return calculateTree70(features, 15);
    } else {
      return calculateTree70(features, 16);
    }
  }
  if (node === 35) return -0.30855793314493174;
  if (node === 295) return 0.0015216826523350786;
  if (node === 296) return -0.3484115240626266;
  if (node === 147) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree70(features, 295);
    } else if (val <= 70.00000000000001) {
      return calculateTree70(features, 295);
    } else {
      return calculateTree70(features, 296);
    }
  }
  if (node === 148) return 0.06833467001993888;
  if (node === 73) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree70(features, 147);
    } else if (val <= 141.50000000000003) {
      return calculateTree70(features, 147);
    } else {
      return calculateTree70(features, 148);
    }
  }
  if (node === 299) return -0.21141920051316676;
  if (node === 601) return 0.16113963072003712;
  if (node === 602) return -0.0013223673338682012;
  if (node === 300) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree70(features, 601);
    } else if (val <= 422.00000000000006) {
      return calculateTree70(features, 601);
    } else {
      return calculateTree70(features, 602);
    }
  }
  if (node === 149) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree70(features, 299);
    } else if (val <= 409.50000000000006) {
      return calculateTree70(features, 299);
    } else {
      return calculateTree70(features, 300);
    }
  }
  if (node === 150) return -0.3355691840019308;
  if (node === 74) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree70(features, 149);
    } else if (val <= 143.50000000000003) {
      return calculateTree70(features, 149);
    } else {
      return calculateTree70(features, 150);
    }
  }
  if (node === 36) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree70(features, 73);
    } else if (val <= 288.00000000000006) {
      return calculateTree70(features, 73);
    } else {
      return calculateTree70(features, 74);
    }
  }
  if (node === 17) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree70(features, 35);
    } else if (val <= 3.5000000000000004) {
      return calculateTree70(features, 35);
    } else {
      return calculateTree70(features, 36);
    }
  }
  if (node === 18) return -0.3357561439169458;
  if (node === 8) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree70(features, 17);
    } else if (val <= 158.50000000000003) {
      return calculateTree70(features, 17);
    } else {
      return calculateTree70(features, 18);
    }
  }
  if (node === 3) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree70(features, 7);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree70(features, 7);
    } else {
      return calculateTree70(features, 8);
    }
  }
  if (node === 39) return -0.032775545509874604;
  if (node === 40) return -0.3316241370258736;
  if (node === 19) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree70(features, 39);
    } else if (val <= 679.5000000000001) {
      return calculateTree70(features, 39);
    } else {
      return calculateTree70(features, 40);
    }
  }
  if (node === 83) return 0.10486493907913713;
  if (node === 84) return -0.20306908579549732;
  if (node === 41) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree70(features, 83);
    } else if (val <= 36.50000000000001) {
      return calculateTree70(features, 83);
    } else {
      return calculateTree70(features, 84);
    }
  }
  if (node === 85) return 0.7651013173920437;
  if (node === 86) return 0.01363621939074535;
  if (node === 42) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree70(features, 85);
    } else if (val <= 4.500000000000001) {
      return calculateTree70(features, 85);
    } else {
      return calculateTree70(features, 86);
    }
  }
  if (node === 20) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree70(features, 41);
    } else if (val <= 45.50000000000001) {
      return calculateTree70(features, 41);
    } else {
      return calculateTree70(features, 42);
    }
  }
  if (node === 9) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree70(features, 19);
    } else if (val <= 771.0000000000001) {
      return calculateTree70(features, 19);
    } else {
      return calculateTree70(features, 20);
    }
  }
  if (node === 10) return -0.27987124806509256;
  if (node === 4) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree70(features, 9);
    } else if (val <= 835.5000000000001) {
      return calculateTree70(features, 9);
    } else {
      return calculateTree70(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree70(features, 3);
    } else if (val <= 164.50000000000003) {
      return calculateTree70(features, 3);
    } else {
      return calculateTree70(features, 4);
    }
  }
  if (node === 2) return 0.0016092197486807533;
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree70(features, 1);
    } else if (val <= 188.00000000000003) {
      return calculateTree70(features, 1);
    } else {
      return calculateTree70(features, 2);
    }
  }
}
function calculateTree71(features, node = 0) {
  if (node === 31) return 0.003877617210489773;
  if (node === 65) return 0.08211935816045542;
  if (node === 66) return 0.2885974786201889;
  if (node === 32) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree71(features, 65);
    } else if (val <= 222.50000000000003) {
      return calculateTree71(features, 65);
    } else {
      return calculateTree71(features, 66);
    }
  }
  if (node === 15) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree71(features, 31);
    } else if (val <= 387.50000000000006) {
      return calculateTree71(features, 31);
    } else {
      return calculateTree71(features, 32);
    }
  }
  if (node === 33) return -0.24863082728343794;
  if (node === 34) return -0.0022533462550442325;
  if (node === 16) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree71(features, 33);
    } else if (val <= 409.50000000000006) {
      return calculateTree71(features, 33);
    } else {
      return calculateTree71(features, 34);
    }
  }
  if (node === 7) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree71(features, 15);
    } else if (val <= 398.00000000000006) {
      return calculateTree71(features, 15);
    } else {
      return calculateTree71(features, 16);
    }
  }
  if (node === 71) return -0.022570188126347812;
  if (node === 72) return -0.35176619670210874;
  if (node === 35) {
    const val = features['is_preference_household'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree71(features, 71);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree71(features, 71);
    } else {
      return calculateTree71(features, 72);
    }
  }
  if (node === 36) return 0.17726838330313574;
  if (node === 17) {
    const val = features['is_preference_sports'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree71(features, 35);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree71(features, 35);
    } else {
      return calculateTree71(features, 36);
    }
  }
  if (node === 18) return -0.32373385358290924;
  if (node === 8) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree71(features, 17);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree71(features, 17);
    } else {
      return calculateTree71(features, 18);
    }
  }
  if (node === 3) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree71(features, 7);
    } else if (val <= 558.0000000000001) {
      return calculateTree71(features, 7);
    } else {
      return calculateTree71(features, 8);
    }
  }
  if (node === 9) return -0.2966759020257346;
  if (node === 175) return 0.023788288808606527;
  if (node === 176) return 0.49953852976450475;
  if (node === 87) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree71(features, 175);
    } else if (val <= 57.50000000000001) {
      return calculateTree71(features, 175);
    } else {
      return calculateTree71(features, 176);
    }
  }
  if (node === 177) return -0.03951001248786564;
  if (node === 178) return -0.2823474308634009;
  if (node === 88) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree71(features, 177);
    } else if (val <= 135.50000000000003) {
      return calculateTree71(features, 177);
    } else {
      return calculateTree71(features, 178);
    }
  }
  if (node === 43) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree71(features, 87);
    } else if (val <= 587.5000000000001) {
      return calculateTree71(features, 87);
    } else {
      return calculateTree71(features, 88);
    }
  }
  if (node === 1439) return -0.33688929123690425;
  if (node === 1440) return 0.4933602462462759;
  if (node === 719) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree71(features, 1439);
    } else if (val <= 539.5000000000001) {
      return calculateTree71(features, 1439);
    } else {
      return calculateTree71(features, 1440);
    }
  }
  if (node === 720) return -0.32622752739948374;
  if (node === 359) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree71(features, 719);
    } else if (val <= 628.5000000000001) {
      return calculateTree71(features, 719);
    } else {
      return calculateTree71(features, 720);
    }
  }
  if (node === 360) return -0.3754515578643833;
  if (node === 179) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree71(features, 359);
    } else if (val <= 588.5000000000001) {
      return calculateTree71(features, 359);
    } else {
      return calculateTree71(features, 360);
    }
  }
  if (node === 180) return 0.2508601011509976;
  if (node === 89) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree71(features, 179);
    } else if (val <= 762.5000000000001) {
      return calculateTree71(features, 179);
    } else {
      return calculateTree71(features, 180);
    }
  }
  if (node === 727) return 0.4817141971485734;
  if (node === 1457) return 0.11309967665549299;
  if (node === 1458) return 0.28320466795594057;
  if (node === 728) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree71(features, 1457);
    } else if (val <= 33.50000000000001) {
      return calculateTree71(features, 1457);
    } else {
      return calculateTree71(features, 1458);
    }
  }
  if (node === 363) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree71(features, 727);
    } else if (val <= 599.0000000000001) {
      return calculateTree71(features, 727);
    } else {
      return calculateTree71(features, 728);
    }
  }
  if (node === 729) return -0.3420286496192607;
  if (node === 730) return 0.10085160951639284;
  if (node === 364) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree71(features, 729);
    } else if (val <= 628.5000000000001) {
      return calculateTree71(features, 729);
    } else {
      return calculateTree71(features, 730);
    }
  }
  if (node === 181) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree71(features, 363);
    } else if (val <= 531.5000000000001) {
      return calculateTree71(features, 363);
    } else {
      return calculateTree71(features, 364);
    }
  }
  if (node === 182) return -0.3498182364039532;
  if (node === 90) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree71(features, 181);
    } else if (val <= 760.0000000000001) {
      return calculateTree71(features, 181);
    } else {
      return calculateTree71(features, 182);
    }
  }
  if (node === 44) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree71(features, 89);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree71(features, 89);
    } else {
      return calculateTree71(features, 90);
    }
  }
  if (node === 21) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree71(features, 43);
    } else if (val <= 417.00000000000006) {
      return calculateTree71(features, 43);
    } else {
      return calculateTree71(features, 44);
    }
  }
  if (node === 91) return -0.21949043944704089;
  if (node === 185) return 0.11913123133796043;
  if (node === 373) return -0.07739706944545392;
  if (node === 749) return 0.19670798867654982;
  if (node === 750) return -0.12786122375644973;
  if (node === 374) {
    const val = features['is_preference_home'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree71(features, 749);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree71(features, 749);
    } else {
      return calculateTree71(features, 750);
    }
  }
  if (node === 186) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree71(features, 373);
    } else if (val <= 38.50000000000001) {
      return calculateTree71(features, 373);
    } else {
      return calculateTree71(features, 374);
    }
  }
  if (node === 92) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree71(features, 185);
    } else if (val <= 607.5000000000001) {
      return calculateTree71(features, 185);
    } else {
      return calculateTree71(features, 186);
    }
  }
  if (node === 45) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree71(features, 91);
    } else if (val <= 923.5000000000001) {
      return calculateTree71(features, 91);
    } else {
      return calculateTree71(features, 92);
    }
  }
  if (node === 46) return -0.32933946926164676;
  if (node === 22) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree71(features, 45);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree71(features, 45);
    } else {
      return calculateTree71(features, 46);
    }
  }
  if (node === 10) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree71(features, 21);
    } else if (val <= 789.5000000000001) {
      return calculateTree71(features, 21);
    } else {
      return calculateTree71(features, 22);
    }
  }
  if (node === 4) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree71(features, 9);
    } else if (val <= 51.50000000000001) {
      return calculateTree71(features, 9);
    } else {
      return calculateTree71(features, 10);
    }
  }
  if (node === 1) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree71(features, 3);
    } else if (val <= 563.5000000000001) {
      return calculateTree71(features, 3);
    } else {
      return calculateTree71(features, 4);
    }
  }
  if (node === 2) return -0.002650956139467401;
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree71(features, 1);
    } else if (val <= 611.0000000000001) {
      return calculateTree71(features, 1);
    } else {
      return calculateTree71(features, 2);
    }
  }
}
function calculateTree72(features, node = 0) {
  if (node === 15) return -0.0008579624015381596;
  if (node === 16) return -0.2426982456774366;
  if (node === 7) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree72(features, 15);
    } else if (val <= 558.5000000000001) {
      return calculateTree72(features, 15);
    } else {
      return calculateTree72(features, 16);
    }
  }
  if (node === 35) return 0.31851361128350536;
  if (node === 36) return -0.20353838262775403;
  if (node === 17) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree72(features, 35);
    } else if (val <= 45.50000000000001) {
      return calculateTree72(features, 35);
    } else {
      return calculateTree72(features, 36);
    }
  }
  if (node === 18) return -0.03276815063062279;
  if (node === 8) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree72(features, 17);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree72(features, 17);
    } else {
      return calculateTree72(features, 18);
    }
  }
  if (node === 3) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree72(features, 7);
    } else if (val <= 560.5000000000001) {
      return calculateTree72(features, 7);
    } else {
      return calculateTree72(features, 8);
    }
  }
  if (node === 4) return -0.21933672333874119;
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree72(features, 3);
    } else if (val <= 566.0000000000001) {
      return calculateTree72(features, 3);
    } else {
      return calculateTree72(features, 4);
    }
  }
  if (node === 11) return 0.19227770716481743;
  if (node === 12) return -0.3041824562843619;
  if (node === 5) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree72(features, 11);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree72(features, 11);
    } else {
      return calculateTree72(features, 12);
    }
  }
  if (node === 111) return 0.18878192111499248;
  if (node === 112) return -0.050588889273285545;
  if (node === 55) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree72(features, 111);
    } else if (val <= 577.5000000000001) {
      return calculateTree72(features, 111);
    } else {
      return calculateTree72(features, 112);
    }
  }
  if (node === 113) return -0.3585015936160504;
  if (node === 114) return -0.04920371555995075;
  if (node === 56) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree72(features, 113);
    } else if (val <= 582.5000000000001) {
      return calculateTree72(features, 113);
    } else {
      return calculateTree72(features, 114);
    }
  }
  if (node === 27) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree72(features, 55);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree72(features, 55);
    } else {
      return calculateTree72(features, 56);
    }
  }
  if (node === 57) return -0.34794499545225877;
  if (node === 58) return 0.1656263756115251;
  if (node === 28) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree72(features, 57);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree72(features, 57);
    } else {
      return calculateTree72(features, 58);
    }
  }
  if (node === 13) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree72(features, 27);
    } else if (val <= 597.5000000000001) {
      return calculateTree72(features, 27);
    } else {
      return calculateTree72(features, 28);
    }
  }
  if (node === 59) return -0.057422133128034646;
  if (node === 60) return -0.34976584964664925;
  if (node === 29) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree72(features, 59);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree72(features, 59);
    } else {
      return calculateTree72(features, 60);
    }
  }
  if (node === 495) return -0.30053286359988346;
  if (node === 496) return 0.2427233153789696;
  if (node === 247) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree72(features, 495);
    } else if (val <= 611.5000000000001) {
      return calculateTree72(features, 495);
    } else {
      return calculateTree72(features, 496);
    }
  }
  if (node === 497) return -0.2863416133684402;
  if (node === 997) return 0.03623006684094339;
  if (node === 998) return -0.3111831844022831;
  if (node === 498) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree72(features, 997);
    } else if (val <= 657.5000000000001) {
      return calculateTree72(features, 997);
    } else {
      return calculateTree72(features, 998);
    }
  }
  if (node === 248) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree72(features, 497);
    } else if (val <= 649.5000000000001) {
      return calculateTree72(features, 497);
    } else {
      return calculateTree72(features, 498);
    }
  }
  if (node === 123) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree72(features, 247);
    } else if (val <= 625.0000000000001) {
      return calculateTree72(features, 247);
    } else {
      return calculateTree72(features, 248);
    }
  }
  if (node === 249) return 0.06802590518171284;
  if (node === 1003) return -0.005571012344098257;
  if (node === 2009) return -0.17129626774700432;
  if (node === 2010) return -0.46685464346461103;
  if (node === 1004) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree72(features, 2009);
    } else if (val <= 780.0000000000001) {
      return calculateTree72(features, 2009);
    } else {
      return calculateTree72(features, 2010);
    }
  }
  if (node === 501) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree72(features, 1003);
    } else if (val <= 752.5000000000001) {
      return calculateTree72(features, 1003);
    } else {
      return calculateTree72(features, 1004);
    }
  }
  if (node === 1005) return 0.1418074574118316;
  if (node === 2013) return -0.2717064065390234;
  if (node === 2014) return 0.002676056169914566;
  if (node === 1006) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree72(features, 2013);
    } else if (val <= 799.5000000000001) {
      return calculateTree72(features, 2013);
    } else {
      return calculateTree72(features, 2014);
    }
  }
  if (node === 502) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree72(features, 1005);
    } else if (val <= 789.5000000000001) {
      return calculateTree72(features, 1005);
    } else {
      return calculateTree72(features, 1006);
    }
  }
  if (node === 250) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree72(features, 501);
    } else if (val <= 782.0000000000001) {
      return calculateTree72(features, 501);
    } else {
      return calculateTree72(features, 502);
    }
  }
  if (node === 124) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree72(features, 249);
    } else if (val <= 675.5000000000001) {
      return calculateTree72(features, 249);
    } else {
      return calculateTree72(features, 250);
    }
  }
  if (node === 61) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree72(features, 123);
    } else if (val <= 662.5000000000001) {
      return calculateTree72(features, 123);
    } else {
      return calculateTree72(features, 124);
    }
  }
  if (node === 125) return 0.04692591083170298;
  if (node === 253) return -0.2912182099080505;
  if (node === 509) return 0.0051926142298828375;
  if (node === 510) return -0.18332613393448036;
  if (node === 254) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree72(features, 509);
    } else if (val <= 992.5000000000001) {
      return calculateTree72(features, 509);
    } else {
      return calculateTree72(features, 510);
    }
  }
  if (node === 126) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree72(features, 253);
    } else if (val <= 677.5000000000001) {
      return calculateTree72(features, 253);
    } else {
      return calculateTree72(features, 254);
    }
  }
  if (node === 62) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree72(features, 125);
    } else if (val <= 662.5000000000001) {
      return calculateTree72(features, 125);
    } else {
      return calculateTree72(features, 126);
    }
  }
  if (node === 30) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree72(features, 61);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree72(features, 61);
    } else {
      return calculateTree72(features, 62);
    }
  }
  if (node === 14) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree72(features, 29);
    } else if (val <= 606.5000000000001) {
      return calculateTree72(features, 29);
    } else {
      return calculateTree72(features, 30);
    }
  }
  if (node === 6) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree72(features, 13);
    } else if (val <= 602.5000000000001) {
      return calculateTree72(features, 13);
    } else {
      return calculateTree72(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree72(features, 5);
    } else if (val <= 576.5000000000001) {
      return calculateTree72(features, 5);
    } else {
      return calculateTree72(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree72(features, 1);
    } else if (val <= 572.0000000000001) {
      return calculateTree72(features, 1);
    } else {
      return calculateTree72(features, 2);
    }
  }
}
function calculateTree73(features, node = 0) {
  if (node === 3) return 0.0021849172681076582;
  if (node === 39) return -0.30435814844593573;
  if (node === 81) return 0.0907407202114517;
  if (node === 82) return -0.2215080649491445;
  if (node === 40) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree73(features, 81);
    } else if (val <= 677.5000000000001) {
      return calculateTree73(features, 81);
    } else {
      return calculateTree73(features, 82);
    }
  }
  if (node === 19) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree73(features, 39);
    } else if (val <= 485.50000000000006) {
      return calculateTree73(features, 39);
    } else {
      return calculateTree73(features, 40);
    }
  }
  if (node === 167) return -0.268297116008257;
  if (node === 168) return 0.2318012462306648;
  if (node === 83) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree73(features, 167);
    } else if (val <= 181.50000000000003) {
      return calculateTree73(features, 167);
    } else {
      return calculateTree73(features, 168);
    }
  }
  if (node === 84) return -0.36170100730560223;
  if (node === 41) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree73(features, 83);
    } else if (val <= 485.50000000000006) {
      return calculateTree73(features, 83);
    } else {
      return calculateTree73(features, 84);
    }
  }
  if (node === 85) return 0.25197517707438644;
  if (node === 86) return -0.3498301746574814;
  if (node === 42) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree73(features, 85);
    } else if (val <= 778.5000000000001) {
      return calculateTree73(features, 85);
    } else {
      return calculateTree73(features, 86);
    }
  }
  if (node === 20) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree73(features, 41);
    } else if (val <= 677.5000000000001) {
      return calculateTree73(features, 41);
    } else {
      return calculateTree73(features, 42);
    }
  }
  if (node === 9) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree73(features, 19);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree73(features, 19);
    } else {
      return calculateTree73(features, 20);
    }
  }
  if (node === 43) return -0.39324782383765616;
  if (node === 89) return -0.38313194884452517;
  if (node === 90) return 0.1337330854648102;
  if (node === 44) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree73(features, 89);
    } else if (val <= 377.50000000000006) {
      return calculateTree73(features, 89);
    } else {
      return calculateTree73(features, 90);
    }
  }
  if (node === 21) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree73(features, 43);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree73(features, 43);
    } else {
      return calculateTree73(features, 44);
    }
  }
  if (node === 367) return -0.2263163099449247;
  if (node === 368) return 0.0779377356414451;
  if (node === 183) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree73(features, 367);
    } else if (val <= 474.50000000000006) {
      return calculateTree73(features, 367);
    } else {
      return calculateTree73(features, 368);
    }
  }
  if (node === 369) return 0.07306103459642883;
  if (node === 370) return -0.29079766863836165;
  if (node === 184) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree73(features, 369);
    } else if (val <= 377.50000000000006) {
      return calculateTree73(features, 369);
    } else {
      return calculateTree73(features, 370);
    }
  }
  if (node === 91) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree73(features, 183);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree73(features, 183);
    } else {
      return calculateTree73(features, 184);
    }
  }
  if (node === 92) return -0.26536806991577333;
  if (node === 45) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree73(features, 91);
    } else if (val <= 464.50000000000006) {
      return calculateTree73(features, 91);
    } else {
      return calculateTree73(features, 92);
    }
  }
  if (node === 46) return 0.0007926625141962565;
  if (node === 22) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree73(features, 45);
    } else if (val <= 470.50000000000006) {
      return calculateTree73(features, 45);
    } else {
      return calculateTree73(features, 46);
    }
  }
  if (node === 10) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree73(features, 21);
    } else if (val <= 424.00000000000006) {
      return calculateTree73(features, 21);
    } else {
      return calculateTree73(features, 22);
    }
  }
  if (node === 4) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree73(features, 9);
    } else if (val <= 417.00000000000006) {
      return calculateTree73(features, 9);
    } else {
      return calculateTree73(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree73(features, 3);
    } else if (val <= 398.00000000000006) {
      return calculateTree73(features, 3);
    } else {
      return calculateTree73(features, 4);
    }
  }
  if (node === 5) return 0.2215641559267822;
  if (node === 111) return -0.3510582180354065;
  if (node === 225) return 0.09193989983553556;
  if (node === 226) return -0.29704655039260014;
  if (node === 112) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree73(features, 225);
    } else if (val <= 658.5000000000001) {
      return calculateTree73(features, 225);
    } else {
      return calculateTree73(features, 226);
    }
  }
  if (node === 55) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree73(features, 111);
    } else if (val <= 275.50000000000006) {
      return calculateTree73(features, 111);
    } else {
      return calculateTree73(features, 112);
    }
  }
  if (node === 56) return 0.13299586140409375;
  if (node === 27) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree73(features, 55);
    } else if (val <= 81.00000000000001) {
      return calculateTree73(features, 55);
    } else {
      return calculateTree73(features, 56);
    }
  }
  if (node === 28) return -0.28283198244991004;
  if (node === 13) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree73(features, 27);
    } else if (val <= 6.500000000000001) {
      return calculateTree73(features, 27);
    } else {
      return calculateTree73(features, 28);
    }
  }
  if (node === 239) return 0.4538882269265146;
  if (node === 240) return 0.11044900704059417;
  if (node === 119) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree73(features, 239);
    } else if (val <= 709.5000000000001) {
      return calculateTree73(features, 239);
    } else {
      return calculateTree73(features, 240);
    }
  }
  if (node === 120) return 0.0016500562228213911;
  if (node === 59) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree73(features, 119);
    } else if (val <= 479.50000000000006) {
      return calculateTree73(features, 119);
    } else {
      return calculateTree73(features, 120);
    }
  }
  if (node === 60) return -0.1528692077518442;
  if (node === 29) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree73(features, 59);
    } else if (val <= 95.50000000000001) {
      return calculateTree73(features, 59);
    } else {
      return calculateTree73(features, 60);
    }
  }
  if (node === 247) return -0.11130268021890476;
  if (node === 248) return -0.40346940144097126;
  if (node === 123) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree73(features, 247);
    } else if (val <= 15.500000000000002) {
      return calculateTree73(features, 247);
    } else {
      return calculateTree73(features, 248);
    }
  }
  if (node === 124) return 0.007780943361789561;
  if (node === 61) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree73(features, 123);
    } else if (val <= 626.0000000000001) {
      return calculateTree73(features, 123);
    } else {
      return calculateTree73(features, 124);
    }
  }
  if (node === 62) return 0.06207986354777943;
  if (node === 30) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree73(features, 61);
    } else if (val <= 102.50000000000001) {
      return calculateTree73(features, 61);
    } else {
      return calculateTree73(features, 62);
    }
  }
  if (node === 14) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree73(features, 29);
    } else if (val <= 288.50000000000006) {
      return calculateTree73(features, 29);
    } else {
      return calculateTree73(features, 30);
    }
  }
  if (node === 6) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree73(features, 13);
    } else if (val <= 316.50000000000006) {
      return calculateTree73(features, 13);
    } else {
      return calculateTree73(features, 14);
    }
  }
  if (node === 2) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree73(features, 5);
    } else if (val <= 34.00000000000001) {
      return calculateTree73(features, 5);
    } else {
      return calculateTree73(features, 6);
    }
  }
  if (node === 0) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree73(features, 1);
    } else if (val <= 69.50000000000001) {
      return calculateTree73(features, 1);
    } else {
      return calculateTree73(features, 2);
    }
  }
}
function calculateTree74(features, node = 0) {
  if (node === 1) return 0.001138424662192593;
  if (node === 5) return 0.07574125614193507;
  if (node === 1791) return -0.016580568116246103;
  if (node === 1792) return 0.0040789217710378;
  if (node === 895) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree74(features, 1791);
    } else if (val <= 305.50000000000006) {
      return calculateTree74(features, 1791);
    } else {
      return calculateTree74(features, 1792);
    }
  }
  if (node === 1793) return -0.31014458185169264;
  if (node === 1794) return -0.06097503332958688;
  if (node === 896) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree74(features, 1793);
    } else if (val <= 158.50000000000003) {
      return calculateTree74(features, 1793);
    } else {
      return calculateTree74(features, 1794);
    }
  }
  if (node === 447) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree74(features, 895);
    } else if (val <= 760.0000000000001) {
      return calculateTree74(features, 895);
    } else {
      return calculateTree74(features, 896);
    }
  }
  if (node === 1795) return 0.41150979621327266;
  if (node === 1796) return 0.07671540178630755;
  if (node === 897) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree74(features, 1795);
    } else if (val <= 799.5000000000001) {
      return calculateTree74(features, 1795);
    } else {
      return calculateTree74(features, 1796);
    }
  }
  if (node === 898) return -0.035484691836047355;
  if (node === 448) {
    const val = features['is_preference_household'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree74(features, 897);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree74(features, 897);
    } else {
      return calculateTree74(features, 898);
    }
  }
  if (node === 223) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree74(features, 447);
    } else if (val <= 794.0000000000001) {
      return calculateTree74(features, 447);
    } else {
      return calculateTree74(features, 448);
    }
  }
  if (node === 1799) return -0.09179481313144329;
  if (node === 1800) return 0.20264482343365264;
  if (node === 899) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree74(features, 1799);
    } else if (val <= 840.0000000000001) {
      return calculateTree74(features, 1799);
    } else {
      return calculateTree74(features, 1800);
    }
  }
  if (node === 900) return -0.35246172341620563;
  if (node === 449) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree74(features, 899);
    } else if (val <= 844.5000000000001) {
      return calculateTree74(features, 899);
    } else {
      return calculateTree74(features, 900);
    }
  }
  if (node === 901) return -0.20839162090325122;
  if (node === 1805) return 0.41857833217830137;
  if (node === 1806) return 0.03949737236272983;
  if (node === 902) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree74(features, 1805);
    } else if (val <= 338.50000000000006) {
      return calculateTree74(features, 1805);
    } else {
      return calculateTree74(features, 1806);
    }
  }
  if (node === 450) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree74(features, 901);
    } else if (val <= 305.00000000000006) {
      return calculateTree74(features, 901);
    } else {
      return calculateTree74(features, 902);
    }
  }
  if (node === 224) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree74(features, 449);
    } else if (val <= 846.5000000000001) {
      return calculateTree74(features, 449);
    } else {
      return calculateTree74(features, 450);
    }
  }
  if (node === 111) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree74(features, 223);
    } else if (val <= 818.0000000000001) {
      return calculateTree74(features, 223);
    } else {
      return calculateTree74(features, 224);
    }
  }
  if (node === 451) return -0.3156500569388235;
  if (node === 905) return 0.12149774485348343;
  if (node === 906) return -0.06799474520021222;
  if (node === 452) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree74(features, 905);
    } else if (val <= 38.50000000000001) {
      return calculateTree74(features, 905);
    } else {
      return calculateTree74(features, 906);
    }
  }
  if (node === 225) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree74(features, 451);
    } else if (val <= 191.50000000000003) {
      return calculateTree74(features, 451);
    } else {
      return calculateTree74(features, 452);
    }
  }
  if (node === 226) return -0.29265918677452823;
  if (node === 112) {
    const val = features['is_preference_beauty'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree74(features, 225);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree74(features, 225);
    } else {
      return calculateTree74(features, 226);
    }
  }
  if (node === 55) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree74(features, 111);
    } else if (val <= 893.0000000000001) {
      return calculateTree74(features, 111);
    } else {
      return calculateTree74(features, 112);
    }
  }
  if (node === 113) return 0.14950800325184754;
  if (node === 459) return -0.0019278364616654529;
  if (node === 460) return -0.3288046452936443;
  if (node === 229) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree74(features, 459);
    } else if (val <= 804.5000000000001) {
      return calculateTree74(features, 459);
    } else {
      return calculateTree74(features, 460);
    }
  }
  if (node === 230) return 0.08699494073329034;
  if (node === 114) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree74(features, 229);
    } else if (val <= 840.0000000000001) {
      return calculateTree74(features, 229);
    } else {
      return calculateTree74(features, 230);
    }
  }
  if (node === 56) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree74(features, 113);
    } else if (val <= 51.50000000000001) {
      return calculateTree74(features, 113);
    } else {
      return calculateTree74(features, 114);
    }
  }
  if (node === 27) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree74(features, 55);
    } else if (val <= 897.5000000000001) {
      return calculateTree74(features, 55);
    } else {
      return calculateTree74(features, 56);
    }
  }
  if (node === 28) return -0.27591806280281955;
  if (node === 13) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree74(features, 27);
    } else if (val <= 888.0000000000001) {
      return calculateTree74(features, 27);
    } else {
      return calculateTree74(features, 28);
    }
  }
  if (node === 239) return -0.10189606107631125;
  if (node === 481) return 0.273070352817283;
  if (node === 482) return -0.0791229099999696;
  if (node === 240) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree74(features, 481);
    } else if (val <= 12.500000000000002) {
      return calculateTree74(features, 481);
    } else {
      return calculateTree74(features, 482);
    }
  }
  if (node === 119) {
    const val = features['is_preference_beauty'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree74(features, 239);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree74(features, 239);
    } else {
      return calculateTree74(features, 240);
    }
  }
  if (node === 120) return 0.23911488583341522;
  if (node === 59) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree74(features, 119);
    } else if (val <= 56.50000000000001) {
      return calculateTree74(features, 119);
    } else {
      return calculateTree74(features, 120);
    }
  }
  if (node === 121) return 0.20829935896074986;
  if (node === 122) return 0.05672106250722226;
  if (node === 60) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree74(features, 121);
    } else if (val <= 155.50000000000003) {
      return calculateTree74(features, 121);
    } else {
      return calculateTree74(features, 122);
    }
  }
  if (node === 29) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree74(features, 59);
    } else if (val <= 900.5000000000001) {
      return calculateTree74(features, 59);
    } else {
      return calculateTree74(features, 60);
    }
  }
  if (node === 61) return -0.029237025495634446;
  if (node === 62) return 0.020689118949336383;
  if (node === 30) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree74(features, 61);
    } else if (val <= 40.50000000000001) {
      return calculateTree74(features, 61);
    } else {
      return calculateTree74(features, 62);
    }
  }
  if (node === 14) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree74(features, 29);
    } else if (val <= 905.5000000000001) {
      return calculateTree74(features, 29);
    } else {
      return calculateTree74(features, 30);
    }
  }
  if (node === 6) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree74(features, 13);
    } else if (val <= 899.5000000000001) {
      return calculateTree74(features, 13);
    } else {
      return calculateTree74(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree74(features, 5);
    } else if (val <= 3.5000000000000004) {
      return calculateTree74(features, 5);
    } else {
      return calculateTree74(features, 6);
    }
  }
  if (node === 0) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree74(features, 1);
    } else if (val <= 34.50000000000001) {
      return calculateTree74(features, 1);
    } else {
      return calculateTree74(features, 2);
    }
  }
}
function calculateTree75(features, node = 0) {
  if (node === 3) return 0.0004759116331180948;
  if (node === 9) return -0.3442056978737872;
  if (node === 43) return 0.06590213527997456;
  if (node === 89) return -0.059478302014482225;
  if (node === 181) return 0.03505038511769263;
  if (node === 365) return -0.23928673890017238;
  if (node === 733) return 0.20925062167950884;
  if (node === 1469) return -0.06527820444200506;
  if (node === 1470) return 0.113334190373967;
  if (node === 734) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree75(features, 1469);
    } else if (val <= 11.500000000000002) {
      return calculateTree75(features, 1469);
    } else {
      return calculateTree75(features, 1470);
    }
  }
  if (node === 366) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree75(features, 733);
    } else if (val <= 835.5000000000001) {
      return calculateTree75(features, 733);
    } else {
      return calculateTree75(features, 734);
    }
  }
  if (node === 182) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree75(features, 365);
    } else if (val <= 829.0000000000001) {
      return calculateTree75(features, 365);
    } else {
      return calculateTree75(features, 366);
    }
  }
  if (node === 90) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree75(features, 181);
    } else if (val <= 819.0000000000001) {
      return calculateTree75(features, 181);
    } else {
      return calculateTree75(features, 182);
    }
  }
  if (node === 44) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree75(features, 89);
    } else if (val <= 783.5000000000001) {
      return calculateTree75(features, 89);
    } else {
      return calculateTree75(features, 90);
    }
  }
  if (node === 21) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree75(features, 43);
    } else if (val <= 781.0000000000001) {
      return calculateTree75(features, 43);
    } else {
      return calculateTree75(features, 44);
    }
  }
  if (node === 367) return -0.018482080172637055;
  if (node === 368) return -0.2782721741450016;
  if (node === 183) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree75(features, 367);
    } else if (val <= 783.5000000000001) {
      return calculateTree75(features, 367);
    } else {
      return calculateTree75(features, 368);
    }
  }
  if (node === 369) return 0.08934343323045846;
  if (node === 741) return 0.08145813581672678;
  if (node === 742) return -0.09337607340545252;
  if (node === 370) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree75(features, 741);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree75(features, 741);
    } else {
      return calculateTree75(features, 742);
    }
  }
  if (node === 184) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree75(features, 369);
    } else if (val <= 800.5000000000001) {
      return calculateTree75(features, 369);
    } else {
      return calculateTree75(features, 370);
    }
  }
  if (node === 91) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree75(features, 183);
    } else if (val <= 799.5000000000001) {
      return calculateTree75(features, 183);
    } else {
      return calculateTree75(features, 184);
    }
  }
  if (node === 92) return -0.2527827374336302;
  if (node === 45) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree75(features, 91);
    } else if (val <= 811.5000000000001) {
      return calculateTree75(features, 91);
    } else {
      return calculateTree75(features, 92);
    }
  }
  if (node === 46) return 0.011915512774320068;
  if (node === 22) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree75(features, 45);
    } else if (val <= 819.0000000000001) {
      return calculateTree75(features, 45);
    } else {
      return calculateTree75(features, 46);
    }
  }
  if (node === 10) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree75(features, 21);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree75(features, 21);
    } else {
      return calculateTree75(features, 22);
    }
  }
  if (node === 4) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree75(features, 9);
    } else if (val <= 778.5000000000001) {
      return calculateTree75(features, 9);
    } else {
      return calculateTree75(features, 10);
    }
  }
  if (node === 1) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree75(features, 3);
    } else if (val <= 777.5000000000001) {
      return calculateTree75(features, 3);
    } else {
      return calculateTree75(features, 4);
    }
  }
  if (node === 95) return 0.03982973147102025;
  if (node === 96) return -0.15575840231026786;
  if (node === 47) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree75(features, 95);
    } else if (val <= 320.00000000000006) {
      return calculateTree75(features, 95);
    } else {
      return calculateTree75(features, 96);
    }
  }
  if (node === 195) return 0.20548536794911962;
  if (node === 393) return -0.261832774078733;
  if (node === 394) return 0.009722988781912448;
  if (node === 196) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree75(features, 393);
    } else if (val <= 859.5000000000001) {
      return calculateTree75(features, 393);
    } else {
      return calculateTree75(features, 394);
    }
  }
  if (node === 97) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree75(features, 195);
    } else if (val <= 851.5000000000001) {
      return calculateTree75(features, 195);
    } else {
      return calculateTree75(features, 196);
    }
  }
  if (node === 395) return 0.22165356693615207;
  if (node === 396) return 0.009457274631755346;
  if (node === 197) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree75(features, 395);
    } else if (val <= 749.0000000000001) {
      return calculateTree75(features, 395);
    } else {
      return calculateTree75(features, 396);
    }
  }
  if (node === 198) return 0.06911546300381034;
  if (node === 98) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree75(features, 197);
    } else if (val <= 859.5000000000001) {
      return calculateTree75(features, 197);
    } else {
      return calculateTree75(features, 198);
    }
  }
  if (node === 48) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree75(features, 97);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree75(features, 97);
    } else {
      return calculateTree75(features, 98);
    }
  }
  if (node === 23) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree75(features, 47);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree75(features, 47);
    } else {
      return calculateTree75(features, 48);
    }
  }
  if (node === 24) return -0.08258650711936442;
  if (node === 11) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree75(features, 23);
    } else if (val <= 15.500000000000002) {
      return calculateTree75(features, 23);
    } else {
      return calculateTree75(features, 24);
    }
  }
  if (node === 51) return -0.05680010910462084;
  if (node === 105) return -0.36537132588018095;
  if (node === 106) return 0.4100303516052337;
  if (node === 52) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree75(features, 105);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree75(features, 105);
    } else {
      return calculateTree75(features, 106);
    }
  }
  if (node === 25) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree75(features, 51);
    } else if (val <= 857.5000000000001) {
      return calculateTree75(features, 51);
    } else {
      return calculateTree75(features, 52);
    }
  }
  if (node === 26) return -0.1370194227212995;
  if (node === 12) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree75(features, 25);
    } else if (val <= 859.5000000000001) {
      return calculateTree75(features, 25);
    } else {
      return calculateTree75(features, 26);
    }
  }
  if (node === 5) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree75(features, 11);
    } else if (val <= 35.50000000000001) {
      return calculateTree75(features, 11);
    } else {
      return calculateTree75(features, 12);
    }
  }
  if (node === 27) return 0.02291751690501402;
  if (node === 28) return -0.3438066791185596;
  if (node === 13) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree75(features, 27);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree75(features, 27);
    } else {
      return calculateTree75(features, 28);
    }
  }
  if (node === 14) return 0.0016827286975928243;
  if (node === 6) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree75(features, 13);
    } else if (val <= 877.5000000000001) {
      return calculateTree75(features, 13);
    } else {
      return calculateTree75(features, 14);
    }
  }
  if (node === 2) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree75(features, 5);
    } else if (val <= 865.0000000000001) {
      return calculateTree75(features, 5);
    } else {
      return calculateTree75(features, 6);
    }
  }
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree75(features, 1);
    } else if (val <= 850.5000000000001) {
      return calculateTree75(features, 1);
    } else {
      return calculateTree75(features, 2);
    }
  }
}
function calculateTree76(features, node = 0) {
  if (node === 3) return -0.00019890570613894946;
  if (node === 79) return 0.17999996785217465;
  if (node === 80) return 0.5347486873827392;
  if (node === 39) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree76(features, 79);
    } else if (val <= 314.50000000000006) {
      return calculateTree76(features, 79);
    } else {
      return calculateTree76(features, 80);
    }
  }
  if (node === 81) return -0.13251049557971428;
  if (node === 331) return 0.19712708334732093;
  if (node === 332) return -0.11550625059644624;
  if (node === 165) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree76(features, 331);
    } else if (val <= 677.5000000000001) {
      return calculateTree76(features, 331);
    } else {
      return calculateTree76(features, 332);
    }
  }
  if (node === 166) return 0.2750801221226387;
  if (node === 82) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree76(features, 165);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree76(features, 165);
    } else {
      return calculateTree76(features, 166);
    }
  }
  if (node === 40) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree76(features, 81);
    } else if (val <= 23.500000000000004) {
      return calculateTree76(features, 81);
    } else {
      return calculateTree76(features, 82);
    }
  }
  if (node === 19) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree76(features, 39);
    } else if (val <= 14.500000000000002) {
      return calculateTree76(features, 39);
    } else {
      return calculateTree76(features, 40);
    }
  }
  if (node === 41) return 0.22856725556022744;
  if (node === 85) return -0.278651929886599;
  if (node === 86) return 0.08378671339206671;
  if (node === 42) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree76(features, 85);
    } else if (val <= 496.00000000000006) {
      return calculateTree76(features, 85);
    } else {
      return calculateTree76(features, 86);
    }
  }
  if (node === 20) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree76(features, 41);
    } else if (val <= 185.00000000000003) {
      return calculateTree76(features, 41);
    } else {
      return calculateTree76(features, 42);
    }
  }
  if (node === 9) {
    const val = features['is_preference_home'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree76(features, 19);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree76(features, 19);
    } else {
      return calculateTree76(features, 20);
    }
  }
  if (node === 43) return -0.03347273928387846;
  if (node === 44) return -0.23852799783924014;
  if (node === 21) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree76(features, 43);
    } else if (val <= 953.5000000000001) {
      return calculateTree76(features, 43);
    } else {
      return calculateTree76(features, 44);
    }
  }
  if (node === 45) return 0.3889121755139992;
  if (node === 187) return 0.42430753433594603;
  if (node === 188) return -0.05548653317002943;
  if (node === 93) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree76(features, 187);
    } else if (val <= 522.5000000000001) {
      return calculateTree76(features, 187);
    } else {
      return calculateTree76(features, 188);
    }
  }
  if (node === 189) return -0.3480004759907359;
  if (node === 763) return -0.24096074135036402;
  if (node === 764) return 0.08883798814249447;
  if (node === 381) {
    const val = features['is_preference_sports'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree76(features, 763);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree76(features, 763);
    } else {
      return calculateTree76(features, 764);
    }
  }
  if (node === 765) return 0.35548310098627106;
  if (node === 766) return -0.05241451862943911;
  if (node === 382) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree76(features, 765);
    } else if (val <= 776.0000000000001) {
      return calculateTree76(features, 765);
    } else {
      return calculateTree76(features, 766);
    }
  }
  if (node === 190) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree76(features, 381);
    } else if (val <= 417.00000000000006) {
      return calculateTree76(features, 381);
    } else {
      return calculateTree76(features, 382);
    }
  }
  if (node === 94) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree76(features, 189);
    } else if (val <= 171.00000000000003) {
      return calculateTree76(features, 189);
    } else {
      return calculateTree76(features, 190);
    }
  }
  if (node === 46) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree76(features, 93);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree76(features, 93);
    } else {
      return calculateTree76(features, 94);
    }
  }
  if (node === 22) {
    const val = features['is_preference_kitchen'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree76(features, 45);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree76(features, 45);
    } else {
      return calculateTree76(features, 46);
    }
  }
  if (node === 10) {
    const val = features['is_preference_grocery'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree76(features, 21);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree76(features, 21);
    } else {
      return calculateTree76(features, 22);
    }
  }
  if (node === 4) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree76(features, 9);
    } else if (val <= 916.0000000000001) {
      return calculateTree76(features, 9);
    } else {
      return calculateTree76(features, 10);
    }
  }
  if (node === 1) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree76(features, 3);
    } else if (val <= 908.0000000000001) {
      return calculateTree76(features, 3);
    } else {
      return calculateTree76(features, 4);
    }
  }
  if (node === 47) return -0.13395422764009782;
  if (node === 97) return 0.030081549937133547;
  if (node === 98) return 0.2805888658017184;
  if (node === 48) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree76(features, 97);
    } else if (val <= 470.00000000000006) {
      return calculateTree76(features, 97);
    } else {
      return calculateTree76(features, 98);
    }
  }
  if (node === 23) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree76(features, 47);
    } else if (val <= 21.500000000000004) {
      return calculateTree76(features, 47);
    } else {
      return calculateTree76(features, 48);
    }
  }
  if (node === 49) return 0.04709110958595605;
  if (node === 203) return -0.3593132602793521;
  if (node === 204) return -0.03621857300490128;
  if (node === 101) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree76(features, 203);
    } else if (val <= 789.5000000000001) {
      return calculateTree76(features, 203);
    } else {
      return calculateTree76(features, 204);
    }
  }
  if (node === 102) return -0.04859107595664124;
  if (node === 50) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree76(features, 101);
    } else if (val <= 70.00000000000001) {
      return calculateTree76(features, 101);
    } else {
      return calculateTree76(features, 102);
    }
  }
  if (node === 24) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree76(features, 49);
    } else if (val <= 305.00000000000006) {
      return calculateTree76(features, 49);
    } else {
      return calculateTree76(features, 50);
    }
  }
  if (node === 11) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree76(features, 23);
    } else if (val <= 18.500000000000004) {
      return calculateTree76(features, 23);
    } else {
      return calculateTree76(features, 24);
    }
  }
  if (node === 12) return -0.15345155986662484;
  if (node === 5) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree76(features, 11);
    } else if (val <= 42.50000000000001) {
      return calculateTree76(features, 11);
    } else {
      return calculateTree76(features, 12);
    }
  }
  if (node === 55) return -0.029490477044355787;
  if (node === 56) return 0.11277609441826543;
  if (node === 27) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree76(features, 55);
    } else if (val <= 41.50000000000001) {
      return calculateTree76(features, 55);
    } else {
      return calculateTree76(features, 56);
    }
  }
  if (node === 28) return 0.1012882747558349;
  if (node === 13) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree76(features, 27);
    } else if (val <= 607.5000000000001) {
      return calculateTree76(features, 27);
    } else {
      return calculateTree76(features, 28);
    }
  }
  if (node === 14) return -0.011349116186944935;
  if (node === 6) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree76(features, 13);
    } else if (val <= 208.50000000000003) {
      return calculateTree76(features, 13);
    } else {
      return calculateTree76(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree76(features, 5);
    } else if (val <= 131.50000000000003) {
      return calculateTree76(features, 5);
    } else {
      return calculateTree76(features, 6);
    }
  }
  if (node === 0) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree76(features, 1);
    } else if (val <= 926.0000000000001) {
      return calculateTree76(features, 1);
    } else {
      return calculateTree76(features, 2);
    }
  }
}
function calculateTree77(features, node = 0) {
  if (node === 7) return 0.0011577887179993608;
  if (node === 8) return -0.3115442613229967;
  if (node === 3) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree77(features, 7);
    } else if (val <= 994.5000000000001) {
      return calculateTree77(features, 7);
    } else {
      return calculateTree77(features, 8);
    }
  }
  if (node === 19) return 0.030605173299525005;
  if (node === 41) return 0.3978356194135155;
  if (node === 42) return 0.08653254188202396;
  if (node === 20) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree77(features, 41);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree77(features, 41);
    } else {
      return calculateTree77(features, 42);
    }
  }
  if (node === 9) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree77(features, 19);
    } else if (val <= 25.500000000000004) {
      return calculateTree77(features, 19);
    } else {
      return calculateTree77(features, 20);
    }
  }
  if (node === 10) return -0.0953446016458893;
  if (node === 4) {
    const val = features['is_preference_kitchen'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree77(features, 9);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree77(features, 9);
    } else {
      return calculateTree77(features, 10);
    }
  }
  if (node === 1) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree77(features, 3);
    } else if (val <= 474.50000000000006) {
      return calculateTree77(features, 3);
    } else {
      return calculateTree77(features, 4);
    }
  }
  if (node === 23) return -0.3396758378096271;
  if (node === 399) return 0.2574126082669562;
  if (node === 400) return -0.09265442703192352;
  if (node === 199) {
    const val = features['is_preference_household'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree77(features, 399);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree77(features, 399);
    } else {
      return calculateTree77(features, 400);
    }
  }
  if (node === 1607) return -0.36148213297692083;
  if (node === 1608) return 0.08001931162599303;
  if (node === 803) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree77(features, 1607);
    } else if (val <= 251.00000000000003) {
      return calculateTree77(features, 1607);
    } else {
      return calculateTree77(features, 1608);
    }
  }
  if (node === 1609) return 0.06656833835381694;
  if (node === 1610) return -0.3183468542664634;
  if (node === 804) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree77(features, 1609);
    } else if (val <= 245.00000000000003) {
      return calculateTree77(features, 1609);
    } else {
      return calculateTree77(features, 1610);
    }
  }
  if (node === 401) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree77(features, 803);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree77(features, 803);
    } else {
      return calculateTree77(features, 804);
    }
  }
  if (node === 1611) return -0.34694761689592996;
  if (node === 1612) return 0.31021709521692015;
  if (node === 805) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree77(features, 1611);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree77(features, 1611);
    } else {
      return calculateTree77(features, 1612);
    }
  }
  if (node === 1613) return -0.15603552073318386;
  if (node === 1614) return 0.04694657152014565;
  if (node === 806) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree77(features, 1613);
    } else if (val <= 745.5000000000001) {
      return calculateTree77(features, 1613);
    } else {
      return calculateTree77(features, 1614);
    }
  }
  if (node === 402) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree77(features, 805);
    } else if (val <= 635.0000000000001) {
      return calculateTree77(features, 805);
    } else {
      return calculateTree77(features, 806);
    }
  }
  if (node === 200) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree77(features, 401);
    } else if (val <= 597.5000000000001) {
      return calculateTree77(features, 401);
    } else {
      return calculateTree77(features, 402);
    }
  }
  if (node === 99) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree77(features, 199);
    } else if (val <= 189.50000000000003) {
      return calculateTree77(features, 199);
    } else {
      return calculateTree77(features, 200);
    }
  }
  if (node === 100) return -0.08364233633617718;
  if (node === 49) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree77(features, 99);
    } else if (val <= 38.50000000000001) {
      return calculateTree77(features, 99);
    } else {
      return calculateTree77(features, 100);
    }
  }
  if (node === 50) return -0.33548610860787403;
  if (node === 24) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree77(features, 49);
    } else if (val <= 900.5000000000001) {
      return calculateTree77(features, 49);
    } else {
      return calculateTree77(features, 50);
    }
  }
  if (node === 11) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree77(features, 23);
    } else if (val <= 175.50000000000003) {
      return calculateTree77(features, 23);
    } else {
      return calculateTree77(features, 24);
    }
  }
  if (node === 25) return -0.007022059418464414;
  if (node === 431) return -0.16020555150911728;
  if (node === 432) return 0.39690864396898823;
  if (node === 215) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree77(features, 431);
    } else if (val <= 10.000000000000002) {
      return calculateTree77(features, 431);
    } else {
      return calculateTree77(features, 432);
    }
  }
  if (node === 433) return 0.012591326564467918;
  if (node === 434) return -0.22467761246872048;
  if (node === 216) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree77(features, 433);
    } else if (val <= 649.5000000000001) {
      return calculateTree77(features, 433);
    } else {
      return calculateTree77(features, 434);
    }
  }
  if (node === 107) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree77(features, 215);
    } else if (val <= 18.500000000000004) {
      return calculateTree77(features, 215);
    } else {
      return calculateTree77(features, 216);
    }
  }
  if (node === 108) return 0.08306173800723195;
  if (node === 53) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree77(features, 107);
    } else if (val <= 675.5000000000001) {
      return calculateTree77(features, 107);
    } else {
      return calculateTree77(features, 108);
    }
  }
  if (node === 109) return -0.002444403542836812;
  if (node === 110) return 0.03483937226585996;
  if (node === 54) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree77(features, 109);
    } else if (val <= 890.5000000000001) {
      return calculateTree77(features, 109);
    } else {
      return calculateTree77(features, 110);
    }
  }
  if (node === 26) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree77(features, 53);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree77(features, 53);
    } else {
      return calculateTree77(features, 54);
    }
  }
  if (node === 12) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree77(features, 25);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree77(features, 25);
    } else {
      return calculateTree77(features, 26);
    }
  }
  if (node === 5) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree77(features, 11);
    } else if (val <= 507.00000000000006) {
      return calculateTree77(features, 11);
    } else {
      return calculateTree77(features, 12);
    }
  }
  if (node === 27) return -0.35458060290293575;
  if (node === 28) return -0.11651602378640637;
  if (node === 13) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree77(features, 27);
    } else if (val <= 27.500000000000004) {
      return calculateTree77(features, 27);
    } else {
      return calculateTree77(features, 28);
    }
  }
  if (node === 59) return -0.04565563795982574;
  if (node === 60) return -0.2784445094696134;
  if (node === 29) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree77(features, 59);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree77(features, 59);
    } else {
      return calculateTree77(features, 60);
    }
  }
  if (node === 30) return 0.03637435774828728;
  if (node === 14) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree77(features, 29);
    } else if (val <= 18.500000000000004) {
      return calculateTree77(features, 29);
    } else {
      return calculateTree77(features, 30);
    }
  }
  if (node === 6) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree77(features, 13);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree77(features, 13);
    } else {
      return calculateTree77(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree77(features, 5);
    } else if (val <= 975.5000000000001) {
      return calculateTree77(features, 5);
    } else {
      return calculateTree77(features, 6);
    }
  }
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree77(features, 1);
    } else if (val <= 478.50000000000006) {
      return calculateTree77(features, 1);
    } else {
      return calculateTree77(features, 2);
    }
  }
}
function calculateTree78(features, node = 0) {
  if (node === 127) return 0.00025241828958255875;
  if (node === 128) return -0.2270907591244756;
  if (node === 63) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree78(features, 127);
    } else if (val <= 258.50000000000006) {
      return calculateTree78(features, 127);
    } else {
      return calculateTree78(features, 128);
    }
  }
  if (node === 129) return 0.12831494217752507;
  if (node === 130) return 0.026836897865269934;
  if (node === 64) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree78(features, 129);
    } else if (val <= 278.50000000000006) {
      return calculateTree78(features, 129);
    } else {
      return calculateTree78(features, 130);
    }
  }
  if (node === 31) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree78(features, 63);
    } else if (val <= 271.00000000000006) {
      return calculateTree78(features, 63);
    } else {
      return calculateTree78(features, 64);
    }
  }
  if (node === 65) return -0.2747735168812864;
  if (node === 66) return -0.009103967038694621;
  if (node === 32) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree78(features, 65);
    } else if (val <= 322.00000000000006) {
      return calculateTree78(features, 65);
    } else {
      return calculateTree78(features, 66);
    }
  }
  if (node === 15) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree78(features, 31);
    } else if (val <= 312.50000000000006) {
      return calculateTree78(features, 31);
    } else {
      return calculateTree78(features, 32);
    }
  }
  if (node === 16) return 0.08374869115469305;
  if (node === 7) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree78(features, 15);
    } else if (val <= 325.00000000000006) {
      return calculateTree78(features, 15);
    } else {
      return calculateTree78(features, 16);
    }
  }
  if (node === 8) return -0.353829355464111;
  if (node === 3) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree78(features, 7);
    } else if (val <= 391.50000000000006) {
      return calculateTree78(features, 7);
    } else {
      return calculateTree78(features, 8);
    }
  }
  if (node === 639) return -0.000294298173423945;
  if (node === 640) return -0.2637960708939879;
  if (node === 319) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree78(features, 639);
    } else if (val <= 230.50000000000003) {
      return calculateTree78(features, 639);
    } else {
      return calculateTree78(features, 640);
    }
  }
  if (node === 320) return 0.047429384211032634;
  if (node === 159) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree78(features, 319);
    } else if (val <= 241.50000000000003) {
      return calculateTree78(features, 319);
    } else {
      return calculateTree78(features, 320);
    }
  }
  if (node === 643) return -0.1511136920250458;
  if (node === 644) return -0.4295900102913165;
  if (node === 321) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree78(features, 643);
    } else if (val <= 275.50000000000006) {
      return calculateTree78(features, 643);
    } else {
      return calculateTree78(features, 644);
    }
  }
  if (node === 645) return 0.04748204930840661;
  if (node === 1293) return -0.12091265369398585;
  if (node === 1294) return 0.0076044484435557735;
  if (node === 646) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree78(features, 1293);
    } else if (val <= 311.50000000000006) {
      return calculateTree78(features, 1293);
    } else {
      return calculateTree78(features, 1294);
    }
  }
  if (node === 322) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree78(features, 645);
    } else if (val <= 282.50000000000006) {
      return calculateTree78(features, 645);
    } else {
      return calculateTree78(features, 646);
    }
  }
  if (node === 160) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree78(features, 321);
    } else if (val <= 278.50000000000006) {
      return calculateTree78(features, 321);
    } else {
      return calculateTree78(features, 322);
    }
  }
  if (node === 79) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree78(features, 159);
    } else if (val <= 261.50000000000006) {
      return calculateTree78(features, 159);
    } else {
      return calculateTree78(features, 160);
    }
  }
  if (node === 80) return 0.07658415507960638;
  if (node === 39) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree78(features, 79);
    } else if (val <= 312.50000000000006) {
      return calculateTree78(features, 79);
    } else {
      return calculateTree78(features, 80);
    }
  }
  if (node === 81) return -0.3422815980420972;
  if (node === 331) return 0.06523826577240661;
  if (node === 332) return -0.3485580165314004;
  if (node === 165) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree78(features, 331);
    } else if (val <= 338.50000000000006) {
      return calculateTree78(features, 331);
    } else {
      return calculateTree78(features, 332);
    }
  }
  if (node === 333) return 0.10566605542305083;
  if (node === 669) return -0.18772823939327846;
  if (node === 670) return 0.08244433629224902;
  if (node === 334) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree78(features, 669);
    } else if (val <= 386.50000000000006) {
      return calculateTree78(features, 669);
    } else {
      return calculateTree78(features, 670);
    }
  }
  if (node === 166) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree78(features, 333);
    } else if (val <= 365.50000000000006) {
      return calculateTree78(features, 333);
    } else {
      return calculateTree78(features, 334);
    }
  }
  if (node === 82) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree78(features, 165);
    } else if (val <= 358.50000000000006) {
      return calculateTree78(features, 165);
    } else {
      return calculateTree78(features, 166);
    }
  }
  if (node === 40) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree78(features, 81);
    } else if (val <= 331.50000000000006) {
      return calculateTree78(features, 81);
    } else {
      return calculateTree78(features, 82);
    }
  }
  if (node === 19) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree78(features, 39);
    } else if (val <= 322.00000000000006) {
      return calculateTree78(features, 39);
    } else {
      return calculateTree78(features, 40);
    }
  }
  if (node === 20) return -0.32313804289590464;
  if (node === 9) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree78(features, 19);
    } else if (val <= 387.50000000000006) {
      return calculateTree78(features, 19);
    } else {
      return calculateTree78(features, 20);
    }
  }
  if (node === 21) return 0.2282301624152672;
  if (node === 22) return -0.03589006354217462;
  if (node === 10) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree78(features, 21);
    } else if (val <= 176.50000000000003) {
      return calculateTree78(features, 21);
    } else {
      return calculateTree78(features, 22);
    }
  }
  if (node === 4) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree78(features, 9);
    } else if (val <= 391.50000000000006) {
      return calculateTree78(features, 9);
    } else {
      return calculateTree78(features, 10);
    }
  }
  if (node === 1) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree78(features, 3);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree78(features, 3);
    } else {
      return calculateTree78(features, 4);
    }
  }
  if (node === 23) return -0.15999074245522574;
  if (node === 24) return 0.03289075670929272;
  if (node === 11) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree78(features, 23);
    } else if (val <= 409.50000000000006) {
      return calculateTree78(features, 23);
    } else {
      return calculateTree78(features, 24);
    }
  }
  if (node === 51) return 0.04411123478082405;
  if (node === 52) return -0.3075550238282612;
  if (node === 25) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree78(features, 51);
    } else if (val <= 43.50000000000001) {
      return calculateTree78(features, 51);
    } else {
      return calculateTree78(features, 52);
    }
  }
  if (node === 26) return -0.2510987126580668;
  if (node === 12) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree78(features, 25);
    } else if (val <= 405.00000000000006) {
      return calculateTree78(features, 25);
    } else {
      return calculateTree78(features, 26);
    }
  }
  if (node === 5) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree78(features, 11);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree78(features, 11);
    } else {
      return calculateTree78(features, 12);
    }
  }
  if (node === 6) return -0.001870390666968366;
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree78(features, 5);
    } else if (val <= 417.00000000000006) {
      return calculateTree78(features, 5);
    } else {
      return calculateTree78(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree78(features, 1);
    } else if (val <= 398.00000000000006) {
      return calculateTree78(features, 1);
    } else {
      return calculateTree78(features, 2);
    }
  }
}
function calculateTree79(features, node = 0) {
  if (node === 31) return -0.008248006664965805;
  if (node === 65) return -0.19998990204215614;
  if (node === 66) return -0.019888049780306487;
  if (node === 32) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree79(features, 65);
    } else if (val <= 21.500000000000004) {
      return calculateTree79(features, 65);
    } else {
      return calculateTree79(features, 66);
    }
  }
  if (node === 15) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree79(features, 31);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree79(features, 31);
    } else {
      return calculateTree79(features, 32);
    }
  }
  if (node === 67) return 0.06955792484009765;
  if (node === 68) return 0.3286395530677032;
  if (node === 33) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree79(features, 67);
    } else if (val <= 56.50000000000001) {
      return calculateTree79(features, 67);
    } else {
      return calculateTree79(features, 68);
    }
  }
  if (node === 69) return 0.051501153440041504;
  if (node === 70) return -0.10838912332501263;
  if (node === 34) {
    const val = features['is_preference_grocery'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree79(features, 69);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree79(features, 69);
    } else {
      return calculateTree79(features, 70);
    }
  }
  if (node === 16) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree79(features, 33);
    } else if (val <= 1.5000000000000002) {
      return calculateTree79(features, 33);
    } else {
      return calculateTree79(features, 34);
    }
  }
  if (node === 7) {
    const val = features['is_preference_personal_care'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree79(features, 15);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree79(features, 15);
    } else {
      return calculateTree79(features, 16);
    }
  }
  if (node === 35) return -0.33362012489461973;
  if (node === 147) return 0.3223870786108897;
  if (node === 148) return -0.15521763645832962;
  if (node === 73) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree79(features, 147);
    } else if (val <= 30.500000000000004) {
      return calculateTree79(features, 147);
    } else {
      return calculateTree79(features, 148);
    }
  }
  if (node === 74) return -0.3527135764202792;
  if (node === 36) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree79(features, 73);
    } else if (val <= 893.0000000000001) {
      return calculateTree79(features, 73);
    } else {
      return calculateTree79(features, 74);
    }
  }
  if (node === 17) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree79(features, 35);
    } else if (val <= 705.5000000000001) {
      return calculateTree79(features, 35);
    } else {
      return calculateTree79(features, 36);
    }
  }
  if (node === 37) return -0.11416238462771999;
  if (node === 155) return 0.0303820008480239;
  if (node === 156) return 0.27062716913942325;
  if (node === 77) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree79(features, 155);
    } else if (val <= 893.0000000000001) {
      return calculateTree79(features, 155);
    } else {
      return calculateTree79(features, 156);
    }
  }
  if (node === 315) return 0.47903354481685767;
  if (node === 633) return 0.3152276699034922;
  if (node === 634) return 0.07600932422439333;
  if (node === 316) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree79(features, 633);
    } else if (val <= 56.00000000000001) {
      return calculateTree79(features, 633);
    } else {
      return calculateTree79(features, 634);
    }
  }
  if (node === 157) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree79(features, 315);
    } else if (val <= 1.5000000000000002) {
      return calculateTree79(features, 315);
    } else {
      return calculateTree79(features, 316);
    }
  }
  if (node === 317) return -0.022130414860060114;
  if (node === 318) return 0.22868960321616696;
  if (node === 158) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree79(features, 317);
    } else if (val <= 176.50000000000003) {
      return calculateTree79(features, 317);
    } else {
      return calculateTree79(features, 318);
    }
  }
  if (node === 78) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree79(features, 157);
    } else if (val <= 34.50000000000001) {
      return calculateTree79(features, 157);
    } else {
      return calculateTree79(features, 158);
    }
  }
  if (node === 38) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree79(features, 77);
    } else if (val <= 19.500000000000004) {
      return calculateTree79(features, 77);
    } else {
      return calculateTree79(features, 78);
    }
  }
  if (node === 18) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree79(features, 37);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree79(features, 37);
    } else {
      return calculateTree79(features, 38);
    }
  }
  if (node === 8) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree79(features, 17);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree79(features, 17);
    } else {
      return calculateTree79(features, 18);
    }
  }
  if (node === 3) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree79(features, 7);
    } else if (val <= 17.500000000000004) {
      return calculateTree79(features, 7);
    } else {
      return calculateTree79(features, 8);
    }
  }
  if (node === 79) return -0.0012160118846923239;
  if (node === 161) return 0.3168555098989059;
  if (node === 162) return 0.0388697216377431;
  if (node === 80) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree79(features, 161);
    } else if (val <= 56.00000000000001) {
      return calculateTree79(features, 161);
    } else {
      return calculateTree79(features, 162);
    }
  }
  if (node === 39) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree79(features, 79);
    } else if (val <= 21.500000000000004) {
      return calculateTree79(features, 79);
    } else {
      return calculateTree79(features, 80);
    }
  }
  if (node === 81) return -0.24853474771222772;
  if (node === 165) return 0.2136357019157852;
  if (node === 166) return -0.10787573730263833;
  if (node === 82) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree79(features, 165);
    } else if (val <= 45.50000000000001) {
      return calculateTree79(features, 165);
    } else {
      return calculateTree79(features, 166);
    }
  }
  if (node === 40) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree79(features, 81);
    } else if (val <= 40.50000000000001) {
      return calculateTree79(features, 81);
    } else {
      return calculateTree79(features, 82);
    }
  }
  if (node === 19) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree79(features, 39);
    } else if (val <= 31.500000000000004) {
      return calculateTree79(features, 39);
    } else {
      return calculateTree79(features, 40);
    }
  }
  if (node === 41) return -0.27722124509699725;
  if (node === 42) return -0.13520379017187506;
  if (node === 20) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree79(features, 41);
    } else if (val <= 28.500000000000004) {
      return calculateTree79(features, 41);
    } else {
      return calculateTree79(features, 42);
    }
  }
  if (node === 9) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree79(features, 19);
    } else if (val <= 310.00000000000006) {
      return calculateTree79(features, 19);
    } else {
      return calculateTree79(features, 20);
    }
  }
  if (node === 43) return -0.06351421070287613;
  if (node === 89) return 0.09402105315381835;
  if (node === 90) return 0.2998475900179889;
  if (node === 44) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree79(features, 89);
    } else if (val <= 55.50000000000001) {
      return calculateTree79(features, 89);
    } else {
      return calculateTree79(features, 90);
    }
  }
  if (node === 21) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree79(features, 43);
    } else if (val <= 15.500000000000002) {
      return calculateTree79(features, 43);
    } else {
      return calculateTree79(features, 44);
    }
  }
  if (node === 22) return -0.33287379425410846;
  if (node === 10) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree79(features, 21);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree79(features, 21);
    } else {
      return calculateTree79(features, 22);
    }
  }
  if (node === 4) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree79(features, 9);
    } else if (val <= 898.5000000000001) {
      return calculateTree79(features, 9);
    } else {
      return calculateTree79(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree79(features, 3);
    } else if (val <= 18.500000000000004) {
      return calculateTree79(features, 3);
    } else {
      return calculateTree79(features, 4);
    }
  }
  if (node === 2) return 4.5704799076203726e-5;
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree79(features, 1);
    } else if (val <= 36.50000000000001) {
      return calculateTree79(features, 1);
    } else {
      return calculateTree79(features, 2);
    }
  }
}
function calculateTree80(features, node = 0) {
  if (node === 3) return 0.0012879487481426163;
  if (node === 9) return 0.08388668368689704;
  if (node === 21) return -0.09489883839735491;
  if (node === 22) return 0.0962965917188383;
  if (node === 10) {
    const val = features['is_preference_household'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree80(features, 21);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree80(features, 21);
    } else {
      return calculateTree80(features, 22);
    }
  }
  if (node === 4) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree80(features, 9);
    } else if (val <= 30.500000000000004) {
      return calculateTree80(features, 9);
    } else {
      return calculateTree80(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree80(features, 3);
    } else if (val <= 422.50000000000006) {
      return calculateTree80(features, 3);
    } else {
      return calculateTree80(features, 4);
    }
  }
  if (node === 23) return -0.06485738886853426;
  if (node === 49) return 0.5378826247289937;
  if (node === 50) return 0.012826718287099822;
  if (node === 24) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree80(features, 49);
    } else if (val <= 55.50000000000001) {
      return calculateTree80(features, 49);
    } else {
      return calculateTree80(features, 50);
    }
  }
  if (node === 11) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree80(features, 23);
    } else if (val <= 35.50000000000001) {
      return calculateTree80(features, 23);
    } else {
      return calculateTree80(features, 24);
    }
  }
  if (node === 25) return -0.30893994692739857;
  if (node === 26) return 0.3435600432724756;
  if (node === 12) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree80(features, 25);
    } else if (val <= 491.50000000000006) {
      return calculateTree80(features, 25);
    } else {
      return calculateTree80(features, 26);
    }
  }
  if (node === 5) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree80(features, 11);
    } else if (val <= 464.50000000000006) {
      return calculateTree80(features, 11);
    } else {
      return calculateTree80(features, 12);
    }
  }
  if (node === 55) return -0.024437052235108062;
  if (node === 113) return 0.21952942020300878;
  if (node === 114) return 0.03043347067805224;
  if (node === 56) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree80(features, 113);
    } else if (val <= 27.500000000000004) {
      return calculateTree80(features, 113);
    } else {
      return calculateTree80(features, 114);
    }
  }
  if (node === 27) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree80(features, 55);
    } else if (val <= 117.50000000000001) {
      return calculateTree80(features, 55);
    } else {
      return calculateTree80(features, 56);
    }
  }
  if (node === 115) return -0.1992830068608976;
  if (node === 233) return 0.04133494689122755;
  if (node === 469) return -0.2291793891726855;
  if (node === 941) return 0.23958274252981648;
  if (node === 942) return -0.025084700311673303;
  if (node === 470) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree80(features, 941);
    } else if (val <= 619.5000000000001) {
      return calculateTree80(features, 941);
    } else {
      return calculateTree80(features, 942);
    }
  }
  if (node === 234) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree80(features, 469);
    } else if (val <= 507.00000000000006) {
      return calculateTree80(features, 469);
    } else {
      return calculateTree80(features, 470);
    }
  }
  if (node === 116) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree80(features, 233);
    } else if (val <= 346.00000000000006) {
      return calculateTree80(features, 233);
    } else {
      return calculateTree80(features, 234);
    }
  }
  if (node === 57) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree80(features, 115);
    } else if (val <= 475.50000000000006) {
      return calculateTree80(features, 115);
    } else {
      return calculateTree80(features, 116);
    }
  }
  if (node === 58) return 0.030105015310123544;
  if (node === 28) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree80(features, 57);
    } else if (val <= 729.0000000000001) {
      return calculateTree80(features, 57);
    } else {
      return calculateTree80(features, 58);
    }
  }
  if (node === 13) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree80(features, 27);
    } else if (val <= 118.50000000000001) {
      return calculateTree80(features, 27);
    } else {
      return calculateTree80(features, 28);
    }
  }
  if (node === 479) return 0.09881538714041992;
  if (node === 480) return -0.19257384489758625;
  if (node === 239) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree80(features, 479);
    } else if (val <= 43.50000000000001) {
      return calculateTree80(features, 479);
    } else {
      return calculateTree80(features, 480);
    }
  }
  if (node === 240) return -0.23613790641234073;
  if (node === 119) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree80(features, 239);
    } else if (val <= 19.000000000000004) {
      return calculateTree80(features, 239);
    } else {
      return calculateTree80(features, 240);
    }
  }
  if (node === 1935) return -0.02475755098301491;
  if (node === 1936) return 0.04293112661641355;
  if (node === 967) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree80(features, 1935);
    } else if (val <= 800.5000000000001) {
      return calculateTree80(features, 1935);
    } else {
      return calculateTree80(features, 1936);
    }
  }
  if (node === 968) return -0.3082085596327015;
  if (node === 483) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree80(features, 967);
    } else if (val <= 877.5000000000001) {
      return calculateTree80(features, 967);
    } else {
      return calculateTree80(features, 968);
    }
  }
  if (node === 484) return -0.3233217507002209;
  if (node === 241) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree80(features, 483);
    } else if (val <= 548.5000000000001) {
      return calculateTree80(features, 483);
    } else {
      return calculateTree80(features, 484);
    }
  }
  if (node === 485) return 0.1181001002064456;
  if (node === 973) return -0.0305961991016135;
  if (node === 974) return 0.08111979458845286;
  if (node === 486) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree80(features, 973);
    } else if (val <= 560.5000000000001) {
      return calculateTree80(features, 973);
    } else {
      return calculateTree80(features, 974);
    }
  }
  if (node === 242) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree80(features, 485);
    } else if (val <= 555.5000000000001) {
      return calculateTree80(features, 485);
    } else {
      return calculateTree80(features, 486);
    }
  }
  if (node === 120) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree80(features, 241);
    } else if (val <= 551.5000000000001) {
      return calculateTree80(features, 241);
    } else {
      return calculateTree80(features, 242);
    }
  }
  if (node === 59) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree80(features, 119);
    } else if (val <= 117.50000000000001) {
      return calculateTree80(features, 119);
    } else {
      return calculateTree80(features, 120);
    }
  }
  if (node === 60) return -0.18852116944474578;
  if (node === 29) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree80(features, 59);
    } else if (val <= 566.0000000000001) {
      return calculateTree80(features, 59);
    } else {
      return calculateTree80(features, 60);
    }
  }
  if (node === 123) return 0.11848006180442222;
  if (node === 124) return -0.17303505515361656;
  if (node === 61) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree80(features, 123);
    } else if (val <= 511.50000000000006) {
      return calculateTree80(features, 123);
    } else {
      return calculateTree80(features, 124);
    }
  }
  if (node === 62) return -0.0012767050959182921;
  if (node === 30) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree80(features, 61);
    } else if (val <= 576.5000000000001) {
      return calculateTree80(features, 61);
    } else {
      return calculateTree80(features, 62);
    }
  }
  if (node === 14) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree80(features, 29);
    } else if (val <= 572.0000000000001) {
      return calculateTree80(features, 29);
    } else {
      return calculateTree80(features, 30);
    }
  }
  if (node === 6) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree80(features, 13);
    } else if (val <= 507.50000000000006) {
      return calculateTree80(features, 13);
    } else {
      return calculateTree80(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree80(features, 5);
    } else if (val <= 470.50000000000006) {
      return calculateTree80(features, 5);
    } else {
      return calculateTree80(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree80(features, 1);
    } else if (val <= 424.00000000000006) {
      return calculateTree80(features, 1);
    } else {
      return calculateTree80(features, 2);
    }
  }
}
function calculateTree81(features, node = 0) {
  if (node === 3) return 0.0006231753711777579;
  if (node === 159) return 0.1687881087076151;
  if (node === 160) return -0.061362923803502334;
  if (node === 79) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree81(features, 159);
    } else if (val <= 39.50000000000001) {
      return calculateTree81(features, 159);
    } else {
      return calculateTree81(features, 160);
    }
  }
  if (node === 80) return -0.33404202850436954;
  if (node === 39) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree81(features, 79);
    } else if (val <= 75.50000000000001) {
      return calculateTree81(features, 79);
    } else {
      return calculateTree81(features, 80);
    }
  }
  if (node === 1311) return 0.04818249914216761;
  if (node === 1312) return 0.3252108053742592;
  if (node === 655) {
    const val = features['is_preference_women_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree81(features, 1311);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree81(features, 1311);
    } else {
      return calculateTree81(features, 1312);
    }
  }
  if (node === 1313) return 0.16613150495736914;
  if (node === 1314) return -0.28551697113522767;
  if (node === 656) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree81(features, 1313);
    } else if (val <= 729.0000000000001) {
      return calculateTree81(features, 1313);
    } else {
      return calculateTree81(features, 1314);
    }
  }
  if (node === 327) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree81(features, 655);
    } else if (val <= 19.500000000000004) {
      return calculateTree81(features, 655);
    } else {
      return calculateTree81(features, 656);
    }
  }
  if (node === 328) return -0.38221287071014953;
  if (node === 163) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree81(features, 327);
    } else if (val <= 734.0000000000001) {
      return calculateTree81(features, 327);
    } else {
      return calculateTree81(features, 328);
    }
  }
  if (node === 329) return -0.3126812091431583;
  if (node === 330) return 0.47451127834872986;
  if (node === 164) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree81(features, 329);
    } else if (val <= 300.50000000000006) {
      return calculateTree81(features, 329);
    } else {
      return calculateTree81(features, 330);
    }
  }
  if (node === 81) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree81(features, 163);
    } else if (val <= 750.0000000000001) {
      return calculateTree81(features, 163);
    } else {
      return calculateTree81(features, 164);
    }
  }
  if (node === 82) return 0.20410028920556447;
  if (node === 40) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree81(features, 81);
    } else if (val <= 28.500000000000004) {
      return calculateTree81(features, 81);
    } else {
      return calculateTree81(features, 82);
    }
  }
  if (node === 19) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree81(features, 39);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree81(features, 39);
    } else {
      return calculateTree81(features, 40);
    }
  }
  if (node === 41) return -0.24163515202263083;
  if (node === 42) return 0.008996478354257558;
  if (node === 20) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree81(features, 41);
    } else if (val <= 723.5000000000001) {
      return calculateTree81(features, 41);
    } else {
      return calculateTree81(features, 42);
    }
  }
  if (node === 9) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree81(features, 19);
    } else if (val <= 542.5000000000001) {
      return calculateTree81(features, 19);
    } else {
      return calculateTree81(features, 20);
    }
  }
  if (node === 10) return 0.17139755976387375;
  if (node === 4) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree81(features, 9);
    } else if (val <= 14.500000000000002) {
      return calculateTree81(features, 9);
    } else {
      return calculateTree81(features, 10);
    }
  }
  if (node === 1) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree81(features, 3);
    } else if (val <= 718.5000000000001) {
      return calculateTree81(features, 3);
    } else {
      return calculateTree81(features, 4);
    }
  }
  if (node === 95) return -0.10493759778278121;
  if (node === 96) return -0.0044323312419525295;
  if (node === 47) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree81(features, 95);
    } else if (val <= 771.0000000000001) {
      return calculateTree81(features, 95);
    } else {
      return calculateTree81(features, 96);
    }
  }
  if (node === 97) return -0.37007555110349893;
  if (node === 98) return 0.1273336841585567;
  if (node === 48) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree81(features, 97);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree81(features, 97);
    } else {
      return calculateTree81(features, 98);
    }
  }
  if (node === 23) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree81(features, 47);
    } else if (val <= 745.5000000000001) {
      return calculateTree81(features, 47);
    } else {
      return calculateTree81(features, 48);
    }
  }
  if (node === 99) return 0.13261377418557932;
  if (node === 201) return -0.027886538824079165;
  if (node === 202) return 0.4785303872219995;
  if (node === 100) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree81(features, 201);
    } else if (val <= 969.5000000000001) {
      return calculateTree81(features, 201);
    } else {
      return calculateTree81(features, 202);
    }
  }
  if (node === 49) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree81(features, 99);
    } else if (val <= 781.0000000000001) {
      return calculateTree81(features, 99);
    } else {
      return calculateTree81(features, 100);
    }
  }
  if (node === 101) return -0.3618972343021069;
  if (node === 205) return 0.1916140352172035;
  if (node === 206) return -0.3046289916897187;
  if (node === 102) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree81(features, 205);
    } else if (val <= 877.5000000000001) {
      return calculateTree81(features, 205);
    } else {
      return calculateTree81(features, 206);
    }
  }
  if (node === 50) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree81(features, 101);
    } else if (val <= 859.5000000000001) {
      return calculateTree81(features, 101);
    } else {
      return calculateTree81(features, 102);
    }
  }
  if (node === 24) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree81(features, 49);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree81(features, 49);
    } else {
      return calculateTree81(features, 50);
    }
  }
  if (node === 11) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree81(features, 23);
    } else if (val <= 763.5000000000001) {
      return calculateTree81(features, 23);
    } else {
      return calculateTree81(features, 24);
    }
  }
  if (node === 51) return -0.20773647562539463;
  if (node === 52) return 0.07549957808079577;
  if (node === 25) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree81(features, 51);
    } else if (val <= 944.5000000000001) {
      return calculateTree81(features, 51);
    } else {
      return calculateTree81(features, 52);
    }
  }
  if (node === 107) return 0.17782410723824987;
  if (node === 108) return -0.42706118377268726;
  if (node === 53) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree81(features, 107);
    } else if (val <= 859.5000000000001) {
      return calculateTree81(features, 107);
    } else {
      return calculateTree81(features, 108);
    }
  }
  if (node === 54) return 0.01566086317163247;
  if (node === 26) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree81(features, 53);
    } else if (val <= 835.5000000000001) {
      return calculateTree81(features, 53);
    } else {
      return calculateTree81(features, 54);
    }
  }
  if (node === 12) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree81(features, 25);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree81(features, 25);
    } else {
      return calculateTree81(features, 26);
    }
  }
  if (node === 5) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree81(features, 11);
    } else if (val <= 834.5000000000001) {
      return calculateTree81(features, 11);
    } else {
      return calculateTree81(features, 12);
    }
  }
  if (node === 13) return -0.3019177768864716;
  if (node === 14) return -0.024528467417071275;
  if (node === 6) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree81(features, 13);
    } else if (val <= 783.5000000000001) {
      return calculateTree81(features, 13);
    } else {
      return calculateTree81(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree81(features, 5);
    } else if (val <= 938.5000000000001) {
      return calculateTree81(features, 5);
    } else {
      return calculateTree81(features, 6);
    }
  }
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree81(features, 1);
    } else if (val <= 755.0000000000001) {
      return calculateTree81(features, 1);
    } else {
      return calculateTree81(features, 2);
    }
  }
}
function calculateTree82(features, node = 0) {
  if (node === 3) return -0.0013507088160915218;
  if (node === 39) return 0.021689500284820817;
  if (node === 81) return -0.393329042724054;
  if (node === 82) return -0.04655129402814226;
  if (node === 40) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree82(features, 81);
    } else if (val <= 1.5000000000000002) {
      return calculateTree82(features, 81);
    } else {
      return calculateTree82(features, 82);
    }
  }
  if (node === 19) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree82(features, 39);
    } else if (val <= 24.500000000000004) {
      return calculateTree82(features, 39);
    } else {
      return calculateTree82(features, 40);
    }
  }
  if (node === 20) return 0.09647940918382736;
  if (node === 9) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree82(features, 19);
    } else if (val <= 26.500000000000004) {
      return calculateTree82(features, 19);
    } else {
      return calculateTree82(features, 20);
    }
  }
  if (node === 10) return -0.26865260475043373;
  if (node === 4) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree82(features, 9);
    } else if (val <= 708.5000000000001) {
      return calculateTree82(features, 9);
    } else {
      return calculateTree82(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree82(features, 3);
    } else if (val <= 951.5000000000001) {
      return calculateTree82(features, 3);
    } else {
      return calculateTree82(features, 4);
    }
  }
  if (node === 47) return 0.060282264363656184;
  if (node === 97) return -0.3216216711012525;
  if (node === 197) return 0.0037614245705703256;
  if (node === 198) return -0.22275119271345736;
  if (node === 98) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree82(features, 197);
    } else if (val <= 829.0000000000001) {
      return calculateTree82(features, 197);
    } else {
      return calculateTree82(features, 198);
    }
  }
  if (node === 48) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree82(features, 97);
    } else if (val <= 330.00000000000006) {
      return calculateTree82(features, 97);
    } else {
      return calculateTree82(features, 98);
    }
  }
  if (node === 23) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree82(features, 47);
    } else if (val <= 127.50000000000001) {
      return calculateTree82(features, 47);
    } else {
      return calculateTree82(features, 48);
    }
  }
  if (node === 199) return -0.32410011851703574;
  if (node === 803) return -0.08997052910850925;
  if (node === 804) return 0.2105153421683353;
  if (node === 401) {
    const val = features['is_preference_beauty'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree82(features, 803);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree82(features, 803);
    } else {
      return calculateTree82(features, 804);
    }
  }
  if (node === 402) return 0.25449637194935515;
  if (node === 200) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree82(features, 401);
    } else if (val <= 271.00000000000006) {
      return calculateTree82(features, 401);
    } else {
      return calculateTree82(features, 402);
    }
  }
  if (node === 99) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree82(features, 199);
    } else if (val <= 127.50000000000001) {
      return calculateTree82(features, 199);
    } else {
      return calculateTree82(features, 200);
    }
  }
  if (node === 100) return -0.3131779238764113;
  if (node === 49) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree82(features, 99);
    } else if (val <= 181.50000000000003) {
      return calculateTree82(features, 99);
    } else {
      return calculateTree82(features, 100);
    }
  }
  if (node === 101) return 0.17104345708792523;
  if (node === 205) return -0.30603068429030855;
  if (node === 413) return 0.22753404232332963;
  if (node === 414) return -0.13222932191773312;
  if (node === 206) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree82(features, 413);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree82(features, 413);
    } else {
      return calculateTree82(features, 414);
    }
  }
  if (node === 102) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree82(features, 205);
    } else if (val <= 953.5000000000001) {
      return calculateTree82(features, 205);
    } else {
      return calculateTree82(features, 206);
    }
  }
  if (node === 50) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree82(features, 101);
    } else if (val <= 808.5000000000001) {
      return calculateTree82(features, 101);
    } else {
      return calculateTree82(features, 102);
    }
  }
  if (node === 24) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree82(features, 49);
    } else if (val <= 661.0000000000001) {
      return calculateTree82(features, 49);
    } else {
      return calculateTree82(features, 50);
    }
  }
  if (node === 11) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree82(features, 23);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree82(features, 23);
    } else {
      return calculateTree82(features, 24);
    }
  }
  if (node === 25) return -0.4589496583327154;
  if (node === 26) return 0.04404846261922954;
  if (node === 12) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree82(features, 25);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree82(features, 25);
    } else {
      return calculateTree82(features, 26);
    }
  }
  if (node === 5) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree82(features, 11);
    } else if (val <= 981.5000000000001) {
      return calculateTree82(features, 11);
    } else {
      return calculateTree82(features, 12);
    }
  }
  if (node === 111) return -0.007658983512832199;
  if (node === 112) return -0.4075424877144946;
  if (node === 55) {
    const val = features['is_preference_kids'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree82(features, 111);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree82(features, 111);
    } else {
      return calculateTree82(features, 112);
    }
  }
  if (node === 56) return -0.4155437766814312;
  if (node === 27) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree82(features, 55);
    } else if (val <= 139.50000000000003) {
      return calculateTree82(features, 55);
    } else {
      return calculateTree82(features, 56);
    }
  }
  if (node === 57) return 0.12116678828643294;
  if (node === 117) return 0.23968908642190292;
  if (node === 237) return 0.0011163763480299565;
  if (node === 238) return 0.3222523825194064;
  if (node === 118) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree82(features, 237);
    } else if (val <= 25.500000000000004) {
      return calculateTree82(features, 237);
    } else {
      return calculateTree82(features, 238);
    }
  }
  if (node === 58) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree82(features, 117);
    } else if (val <= 983.5000000000001) {
      return calculateTree82(features, 117);
    } else {
      return calculateTree82(features, 118);
    }
  }
  if (node === 28) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree82(features, 57);
    } else if (val <= 181.50000000000003) {
      return calculateTree82(features, 57);
    } else {
      return calculateTree82(features, 58);
    }
  }
  if (node === 13) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree82(features, 27);
    } else if (val <= 148.50000000000003) {
      return calculateTree82(features, 27);
    } else {
      return calculateTree82(features, 28);
    }
  }
  if (node === 119) return -0.04139122946264925;
  if (node === 120) return -0.3378843820073844;
  if (node === 59) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree82(features, 119);
    } else if (val <= 986.5000000000001) {
      return calculateTree82(features, 119);
    } else {
      return calculateTree82(features, 120);
    }
  }
  if (node === 60) return 0.038909039936957476;
  if (node === 29) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree82(features, 59);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree82(features, 59);
    } else {
      return calculateTree82(features, 60);
    }
  }
  if (node === 30) return -0.01430693964477196;
  if (node === 14) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree82(features, 29);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree82(features, 29);
    } else {
      return calculateTree82(features, 30);
    }
  }
  if (node === 6) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree82(features, 13);
    } else if (val <= 26.500000000000004) {
      return calculateTree82(features, 13);
    } else {
      return calculateTree82(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree82(features, 5);
    } else if (val <= 982.5000000000001) {
      return calculateTree82(features, 5);
    } else {
      return calculateTree82(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree82(features, 1);
    } else if (val <= 960.0000000000001) {
      return calculateTree82(features, 1);
    } else {
      return calculateTree82(features, 2);
    }
  }
}
function calculateTree83(features, node = 0) {
  if (node === 1) return 0.0012175547117379679;
  if (node === 95) return 0.06673819425749108;
  if (node === 96) return -0.21519446791071142;
  if (node === 47) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree83(features, 95);
    } else if (val <= 143.50000000000003) {
      return calculateTree83(features, 95);
    } else {
      return calculateTree83(features, 96);
    }
  }
  if (node === 97) return -0.12832098547243473;
  if (node === 98) return -0.5782216091778766;
  if (node === 48) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree83(features, 97);
    } else if (val <= 1.5000000000000002) {
      return calculateTree83(features, 97);
    } else {
      return calculateTree83(features, 98);
    }
  }
  if (node === 23) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree83(features, 47);
    } else if (val <= 4.500000000000001) {
      return calculateTree83(features, 47);
    } else {
      return calculateTree83(features, 48);
    }
  }
  if (node === 24) return 0.1043990637233382;
  if (node === 11) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree83(features, 23);
    } else if (val <= 8.500000000000002) {
      return calculateTree83(features, 23);
    } else {
      return calculateTree83(features, 24);
    }
  }
  if (node === 25) return 0.038746004402167585;
  if (node === 26) return 0.14339320070889622;
  if (node === 12) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree83(features, 25);
    } else if (val <= 531.0000000000001) {
      return calculateTree83(features, 25);
    } else {
      return calculateTree83(features, 26);
    }
  }
  if (node === 5) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree83(features, 11);
    } else if (val <= 10.000000000000002) {
      return calculateTree83(features, 11);
    } else {
      return calculateTree83(features, 12);
    }
  }
  if (node === 1791) return -0.008304962662293023;
  if (node === 1792) return -0.04228797879822578;
  if (node === 895) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree83(features, 1791);
    } else if (val <= 668.0000000000001) {
      return calculateTree83(features, 1791);
    } else {
      return calculateTree83(features, 1792);
    }
  }
  if (node === 1793) return -0.10989160004172094;
  if (node === 1794) return 0.014532230214011508;
  if (node === 896) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree83(features, 1793);
    } else if (val <= 729.0000000000001) {
      return calculateTree83(features, 1793);
    } else {
      return calculateTree83(features, 1794);
    }
  }
  if (node === 447) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree83(features, 895);
    } else if (val <= 21.500000000000004) {
      return calculateTree83(features, 895);
    } else {
      return calculateTree83(features, 896);
    }
  }
  if (node === 448) return -0.1638711565343554;
  if (node === 223) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree83(features, 447);
    } else if (val <= 801.5000000000001) {
      return calculateTree83(features, 447);
    } else {
      return calculateTree83(features, 448);
    }
  }
  if (node === 1799) return 0.24625995615957033;
  if (node === 1800) return 0.044585378014179874;
  if (node === 899) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree83(features, 1799);
    } else if (val <= 426.00000000000006) {
      return calculateTree83(features, 1799);
    } else {
      return calculateTree83(features, 1800);
    }
  }
  if (node === 1801) return -0.11138473045461601;
  if (node === 1802) return 0.06928161661759827;
  if (node === 900) {
    const val = features['is_preference_home'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree83(features, 1801);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree83(features, 1801);
    } else {
      return calculateTree83(features, 1802);
    }
  }
  if (node === 449) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree83(features, 899);
    } else if (val <= 877.5000000000001) {
      return calculateTree83(features, 899);
    } else {
      return calculateTree83(features, 900);
    }
  }
  if (node === 1803) return -0.028855377580070833;
  if (node === 1804) return -0.2088262568240775;
  if (node === 901) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree83(features, 1803);
    } else if (val <= 540.0000000000001) {
      return calculateTree83(features, 1803);
    } else {
      return calculateTree83(features, 1804);
    }
  }
  if (node === 1805) return 0.22188762893898137;
  if (node === 1806) return -0.04682830059546971;
  if (node === 902) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree83(features, 1805);
    } else if (val <= 1.5000000000000002) {
      return calculateTree83(features, 1805);
    } else {
      return calculateTree83(features, 1806);
    }
  }
  if (node === 450) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree83(features, 901);
    } else if (val <= 948.5000000000001) {
      return calculateTree83(features, 901);
    } else {
      return calculateTree83(features, 902);
    }
  }
  if (node === 224) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree83(features, 449);
    } else if (val <= 2.5000000000000004) {
      return calculateTree83(features, 449);
    } else {
      return calculateTree83(features, 450);
    }
  }
  if (node === 111) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree83(features, 223);
    } else if (val <= 821.0000000000001) {
      return calculateTree83(features, 223);
    } else {
      return calculateTree83(features, 224);
    }
  }
  if (node === 112) return 0.0006248044962969721;
  if (node === 55) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree83(features, 111);
    } else if (val <= 626.0000000000001) {
      return calculateTree83(features, 111);
    } else {
      return calculateTree83(features, 112);
    }
  }
  if (node === 113) return -0.3090736703881125;
  if (node === 229) return -0.08683146943640586;
  if (node === 230) return 0.22463666882249825;
  if (node === 114) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree83(features, 229);
    } else if (val <= 659.5000000000001) {
      return calculateTree83(features, 229);
    } else {
      return calculateTree83(features, 230);
    }
  }
  if (node === 56) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree83(features, 113);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree83(features, 113);
    } else {
      return calculateTree83(features, 114);
    }
  }
  if (node === 27) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree83(features, 55);
    } else if (val <= 973.5000000000001) {
      return calculateTree83(features, 55);
    } else {
      return calculateTree83(features, 56);
    }
  }
  if (node === 57) return 0.12236343947577731;
  if (node === 117) return -0.4041730816262759;
  if (node === 118) return 0.07711970275919824;
  if (node === 58) {
    const val = features['is_preference_pets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree83(features, 117);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree83(features, 117);
    } else {
      return calculateTree83(features, 118);
    }
  }
  if (node === 28) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree83(features, 57);
    } else if (val <= 10.500000000000002) {
      return calculateTree83(features, 57);
    } else {
      return calculateTree83(features, 58);
    }
  }
  if (node === 13) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree83(features, 27);
    } else if (val <= 986.5000000000001) {
      return calculateTree83(features, 27);
    } else {
      return calculateTree83(features, 28);
    }
  }
  if (node === 29) return 0.09693584191238276;
  if (node === 61) return -0.14152945895896554;
  if (node === 125) return 0.25205703345504477;
  if (node === 126) return -0.005594479637268841;
  if (node === 62) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree83(features, 125);
    } else if (val <= 211.50000000000003) {
      return calculateTree83(features, 125);
    } else {
      return calculateTree83(features, 126);
    }
  }
  if (node === 30) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree83(features, 61);
    } else if (val <= 189.50000000000003) {
      return calculateTree83(features, 61);
    } else {
      return calculateTree83(features, 62);
    }
  }
  if (node === 14) {
    const val = features['is_preference_men_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree83(features, 29);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree83(features, 29);
    } else {
      return calculateTree83(features, 30);
    }
  }
  if (node === 6) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree83(features, 13);
    } else if (val <= 50.50000000000001) {
      return calculateTree83(features, 13);
    } else {
      return calculateTree83(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree83(features, 5);
    } else if (val <= 57.50000000000001) {
      return calculateTree83(features, 5);
    } else {
      return calculateTree83(features, 6);
    }
  }
  if (node === 0) {
    const val = features['is_preference_sports'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree83(features, 1);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree83(features, 1);
    } else {
      return calculateTree83(features, 2);
    }
  }
}
function calculateTree84(features, node = 0) {
  if (node === 31) return 0.023073560061917655;
  if (node === 32) return -0.05548317158390328;
  if (node === 15) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree84(features, 31);
    } else if (val <= 33.50000000000001) {
      return calculateTree84(features, 31);
    } else {
      return calculateTree84(features, 32);
    }
  }
  if (node === 33) return -0.0024267032250758808;
  if (node === 34) return 0.1783822520716357;
  if (node === 16) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree84(features, 33);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree84(features, 33);
    } else {
      return calculateTree84(features, 34);
    }
  }
  if (node === 7) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree84(features, 15);
    } else if (val <= 338.50000000000006) {
      return calculateTree84(features, 15);
    } else {
      return calculateTree84(features, 16);
    }
  }
  if (node === 17) return -0.32097863172952196;
  if (node === 18) return -0.004208886750667465;
  if (node === 8) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree84(features, 17);
    } else if (val <= 427.50000000000006) {
      return calculateTree84(features, 17);
    } else {
      return calculateTree84(features, 18);
    }
  }
  if (node === 3) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree84(features, 7);
    } else if (val <= 405.00000000000006) {
      return calculateTree84(features, 7);
    } else {
      return calculateTree84(features, 8);
    }
  }
  if (node === 39) return -0.038072777245433335;
  if (node === 163) return 0.3451411672891261;
  if (node === 329) return -0.35505163824923475;
  if (node === 330) return 0.15000450787641983;
  if (node === 164) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree84(features, 329);
    } else if (val <= 427.50000000000006) {
      return calculateTree84(features, 329);
    } else {
      return calculateTree84(features, 330);
    }
  }
  if (node === 81) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree84(features, 163);
    } else if (val <= 338.50000000000006) {
      return calculateTree84(features, 163);
    } else {
      return calculateTree84(features, 164);
    }
  }
  if (node === 82) return -0.30201488524832515;
  if (node === 40) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree84(features, 81);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree84(features, 81);
    } else {
      return calculateTree84(features, 82);
    }
  }
  if (node === 19) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree84(features, 39);
    } else if (val <= 117.50000000000001) {
      return calculateTree84(features, 39);
    } else {
      return calculateTree84(features, 40);
    }
  }
  if (node === 167) return -0.3611876554997128;
  if (node === 168) return 0.39482081586105605;
  if (node === 83) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree84(features, 167);
    } else if (val <= 539.5000000000001) {
      return calculateTree84(features, 167);
    } else {
      return calculateTree84(features, 168);
    }
  }
  if (node === 169) return -0.3451209328811101;
  if (node === 170) return -0.11130038907678702;
  if (node === 84) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree84(features, 169);
    } else if (val <= 127.50000000000001) {
      return calculateTree84(features, 169);
    } else {
      return calculateTree84(features, 170);
    }
  }
  if (node === 41) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree84(features, 83);
    } else if (val <= 671.5000000000001) {
      return calculateTree84(features, 83);
    } else {
      return calculateTree84(features, 84);
    }
  }
  if (node === 85) return 0.19942293951065887;
  if (node === 173) return -0.36592569219961674;
  if (node === 349) return 0.015368479546903057;
  if (node === 701) return 0.23592850640205595;
  if (node === 702) return 0.012082729648137296;
  if (node === 350) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree84(features, 701);
    } else if (val <= 10.500000000000002) {
      return calculateTree84(features, 701);
    } else {
      return calculateTree84(features, 702);
    }
  }
  if (node === 174) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree84(features, 349);
    } else if (val <= 35.50000000000001) {
      return calculateTree84(features, 349);
    } else {
      return calculateTree84(features, 350);
    }
  }
  if (node === 86) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree84(features, 173);
    } else if (val <= 125.50000000000001) {
      return calculateTree84(features, 173);
    } else {
      return calculateTree84(features, 174);
    }
  }
  if (node === 42) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree84(features, 85);
    } else if (val <= 539.5000000000001) {
      return calculateTree84(features, 85);
    } else {
      return calculateTree84(features, 86);
    }
  }
  if (node === 20) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree84(features, 41);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree84(features, 41);
    } else {
      return calculateTree84(features, 42);
    }
  }
  if (node === 9) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree84(features, 19);
    } else if (val <= 481.50000000000006) {
      return calculateTree84(features, 19);
    } else {
      return calculateTree84(features, 20);
    }
  }
  if (node === 87) return -0.3489217888076052;
  if (node === 88) return 0.4672035871407874;
  if (node === 43) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree84(features, 87);
    } else if (val <= 127.50000000000001) {
      return calculateTree84(features, 87);
    } else {
      return calculateTree84(features, 88);
    }
  }
  if (node === 44) return -0.418839603316266;
  if (node === 21) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree84(features, 43);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree84(features, 43);
    } else {
      return calculateTree84(features, 44);
    }
  }
  if (node === 22) return 0.01848341967125657;
  if (node === 10) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree84(features, 21);
    } else if (val <= 960.0000000000001) {
      return calculateTree84(features, 21);
    } else {
      return calculateTree84(features, 22);
    }
  }
  if (node === 4) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree84(features, 9);
    } else if (val <= 882.5000000000001) {
      return calculateTree84(features, 9);
    } else {
      return calculateTree84(features, 10);
    }
  }
  if (node === 1) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree84(features, 3);
    } else if (val <= 113.50000000000001) {
      return calculateTree84(features, 3);
    } else {
      return calculateTree84(features, 4);
    }
  }
  if (node === 47) return -0.17059202838893384;
  if (node === 97) return 0.12427921908198822;
  if (node === 98) return 0.45205355467610947;
  if (node === 48) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree84(features, 97);
    } else if (val <= 175.50000000000003) {
      return calculateTree84(features, 97);
    } else {
      return calculateTree84(features, 98);
    }
  }
  if (node === 23) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree84(features, 47);
    } else if (val <= 86.50000000000001) {
      return calculateTree84(features, 47);
    } else {
      return calculateTree84(features, 48);
    }
  }
  if (node === 24) return -0.3662939933949462;
  if (node === 11) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree84(features, 23);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree84(features, 23);
    } else {
      return calculateTree84(features, 24);
    }
  }
  if (node === 25) return -0.34891738128775;
  if (node === 53) return 0.2650032108253241;
  if (node === 54) return -0.0070708985416067;
  if (node === 26) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree84(features, 53);
    } else if (val <= 36.50000000000001) {
      return calculateTree84(features, 53);
    } else {
      return calculateTree84(features, 54);
    }
  }
  if (node === 12) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree84(features, 25);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree84(features, 25);
    } else {
      return calculateTree84(features, 26);
    }
  }
  if (node === 5) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree84(features, 11);
    } else if (val <= 628.5000000000001) {
      return calculateTree84(features, 11);
    } else {
      return calculateTree84(features, 12);
    }
  }
  if (node === 6) return 0.00024235410582252752;
  if (node === 2) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree84(features, 5);
    } else if (val <= 132.50000000000003) {
      return calculateTree84(features, 5);
    } else {
      return calculateTree84(features, 6);
    }
  }
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree84(features, 1);
    } else if (val <= 130.00000000000003) {
      return calculateTree84(features, 1);
    } else {
      return calculateTree84(features, 2);
    }
  }
}
function calculateTree85(features, node = 0) {
  if (node === 1) return 0.0010827485979133678;
  if (node === 5) return -0.12691259726028226;
  if (node === 27) return -0.30294869330356333;
  if (node === 28) return 0.003112580490972247;
  if (node === 13) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree85(features, 27);
    } else if (val <= 607.5000000000001) {
      return calculateTree85(features, 27);
    } else {
      return calculateTree85(features, 28);
    }
  }
  if (node === 29) return -0.3816202617418593;
  if (node === 123) return -0.3780535741824192;
  if (node === 999) return -0.1746519613925299;
  if (node === 1000) return 0.049127303421587755;
  if (node === 499) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree85(features, 999);
    } else if (val <= 771.0000000000001) {
      return calculateTree85(features, 999);
    } else {
      return calculateTree85(features, 1000);
    }
  }
  if (node === 500) return -0.030826109945667098;
  if (node === 249) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree85(features, 499);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree85(features, 499);
    } else {
      return calculateTree85(features, 500);
    }
  }
  if (node === 501) return -0.32773536871390746;
  if (node === 1005) return 0.3912705099286906;
  if (node === 1006) return -0.2505452942766237;
  if (node === 502) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree85(features, 1005);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree85(features, 1005);
    } else {
      return calculateTree85(features, 1006);
    }
  }
  if (node === 250) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree85(features, 501);
    } else if (val <= 934.0000000000001) {
      return calculateTree85(features, 501);
    } else {
      return calculateTree85(features, 502);
    }
  }
  if (node === 124) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree85(features, 249);
    } else if (val <= 693.0000000000001) {
      return calculateTree85(features, 249);
    } else {
      return calculateTree85(features, 250);
    }
  }
  if (node === 61) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree85(features, 123);
    } else if (val <= 643.5000000000001) {
      return calculateTree85(features, 123);
    } else {
      return calculateTree85(features, 124);
    }
  }
  if (node === 2015) return 0.2635689782421919;
  if (node === 2016) return -0.34513016542038094;
  if (node === 1007) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree85(features, 2015);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree85(features, 2015);
    } else {
      return calculateTree85(features, 2016);
    }
  }
  if (node === 1008) return -0.24258551873863748;
  if (node === 503) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree85(features, 1007);
    } else if (val <= 740.5000000000001) {
      return calculateTree85(features, 1007);
    } else {
      return calculateTree85(features, 1008);
    }
  }
  if (node === 2019) return 0.06263928753724443;
  if (node === 2020) return -0.18435599780195724;
  if (node === 1009) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree85(features, 2019);
    } else if (val <= 763.5000000000001) {
      return calculateTree85(features, 2019);
    } else {
      return calculateTree85(features, 2020);
    }
  }
  if (node === 1010) return 0.1890432661041452;
  if (node === 504) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree85(features, 1009);
    } else if (val <= 774.0000000000001) {
      return calculateTree85(features, 1009);
    } else {
      return calculateTree85(features, 1010);
    }
  }
  if (node === 251) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree85(features, 503);
    } else if (val <= 685.0000000000001) {
      return calculateTree85(features, 503);
    } else {
      return calculateTree85(features, 504);
    }
  }
  if (node === 1011) return -0.27813335364296066;
  if (node === 2025) return 0.16920730705311895;
  if (node === 2026) return 0.004556517222677402;
  if (node === 1012) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree85(features, 2025);
    } else if (val <= 911.0000000000001) {
      return calculateTree85(features, 2025);
    } else {
      return calculateTree85(features, 2026);
    }
  }
  if (node === 505) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree85(features, 1011);
    } else if (val <= 900.5000000000001) {
      return calculateTree85(features, 1011);
    } else {
      return calculateTree85(features, 1012);
    }
  }
  if (node === 1013) return 0.028108597541652656;
  if (node === 2029) return -0.3225470944323386;
  if (node === 2030) return 0.23707038293437455;
  if (node === 1014) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree85(features, 2029);
    } else if (val <= 973.5000000000001) {
      return calculateTree85(features, 2029);
    } else {
      return calculateTree85(features, 2030);
    }
  }
  if (node === 506) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree85(features, 1013);
    } else if (val <= 877.5000000000001) {
      return calculateTree85(features, 1013);
    } else {
      return calculateTree85(features, 1014);
    }
  }
  if (node === 252) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree85(features, 505);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree85(features, 505);
    } else {
      return calculateTree85(features, 506);
    }
  }
  if (node === 125) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree85(features, 251);
    } else if (val <= 800.5000000000001) {
      return calculateTree85(features, 251);
    } else {
      return calculateTree85(features, 252);
    }
  }
  if (node === 1015) return 0.15471582917576882;
  if (node === 2033) return -0.1980130988195506;
  if (node === 2034) return 0.1878100643627668;
  if (node === 1016) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree85(features, 2033);
    } else if (val <= 969.5000000000001) {
      return calculateTree85(features, 2033);
    } else {
      return calculateTree85(features, 2034);
    }
  }
  if (node === 507) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree85(features, 1015);
    } else if (val <= 656.5000000000001) {
      return calculateTree85(features, 1015);
    } else {
      return calculateTree85(features, 1016);
    }
  }
  if (node === 508) return -0.32420913666190004;
  if (node === 253) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree85(features, 507);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree85(features, 507);
    } else {
      return calculateTree85(features, 508);
    }
  }
  if (node === 509) return -0.27247551827252847;
  if (node === 2043) return 0.21046297954550444;
  if (node === 2044) return -0.2831858599887998;
  if (node === 1021) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree85(features, 2043);
    } else if (val <= 884.0000000000001) {
      return calculateTree85(features, 2043);
    } else {
      return calculateTree85(features, 2044);
    }
  }
  if (node === 1022) return -0.005590586061464518;
  if (node === 510) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree85(features, 1021);
    } else if (val <= 799.5000000000001) {
      return calculateTree85(features, 1021);
    } else {
      return calculateTree85(features, 1022);
    }
  }
  if (node === 254) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree85(features, 509);
    } else if (val <= 607.5000000000001) {
      return calculateTree85(features, 509);
    } else {
      return calculateTree85(features, 510);
    }
  }
  if (node === 126) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree85(features, 253);
    } else if (val <= 789.5000000000001) {
      return calculateTree85(features, 253);
    } else {
      return calculateTree85(features, 254);
    }
  }
  if (node === 62) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree85(features, 125);
    } else if (val <= 777.5000000000001) {
      return calculateTree85(features, 125);
    } else {
      return calculateTree85(features, 126);
    }
  }
  if (node === 30) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree85(features, 61);
    } else if (val <= 709.5000000000001) {
      return calculateTree85(features, 61);
    } else {
      return calculateTree85(features, 62);
    }
  }
  if (node === 14) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree85(features, 29);
    } else if (val <= 517.5000000000001) {
      return calculateTree85(features, 29);
    } else {
      return calculateTree85(features, 30);
    }
  }
  if (node === 6) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree85(features, 13);
    } else if (val <= 507.50000000000006) {
      return calculateTree85(features, 13);
    } else {
      return calculateTree85(features, 14);
    }
  }
  if (node === 2) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree85(features, 5);
    } else if (val <= 606.5000000000001) {
      return calculateTree85(features, 5);
    } else {
      return calculateTree85(features, 6);
    }
  }
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree85(features, 1);
    } else if (val <= 588.5000000000001) {
      return calculateTree85(features, 1);
    } else {
      return calculateTree85(features, 2);
    }
  }
}
function calculateTree86(features, node = 0) {
  if (node === 63) return 0.15133075072958252;
  if (node === 64) return -0.03859179164528769;
  if (node === 31) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree86(features, 63);
    } else if (val <= 132.50000000000003) {
      return calculateTree86(features, 63);
    } else {
      return calculateTree86(features, 64);
    }
  }
  if (node === 131) return -0.5645175223695917;
  if (node === 132) return -0.08683577261399955;
  if (node === 65) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree86(features, 131);
    } else if (val <= 348.50000000000006) {
      return calculateTree86(features, 131);
    } else {
      return calculateTree86(features, 132);
    }
  }
  if (node === 66) return -0.0946053306183914;
  if (node === 32) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree86(features, 65);
    } else if (val <= 576.0000000000001) {
      return calculateTree86(features, 65);
    } else {
      return calculateTree86(features, 66);
    }
  }
  if (node === 15) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree86(features, 31);
    } else if (val <= 479.50000000000006) {
      return calculateTree86(features, 31);
    } else {
      return calculateTree86(features, 32);
    }
  }
  if (node === 33) return -0.042124615096112275;
  if (node === 34) return 0.20099561387860368;
  if (node === 16) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree86(features, 33);
    } else if (val <= 507.00000000000006) {
      return calculateTree86(features, 33);
    } else {
      return calculateTree86(features, 34);
    }
  }
  if (node === 7) {
    const val = features['is_preference_men_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree86(features, 15);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree86(features, 15);
    } else {
      return calculateTree86(features, 16);
    }
  }
  if (node === 17) return 0.055003953127026786;
  if (node === 18) return 0.014454172539369066;
  if (node === 8) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree86(features, 17);
    } else if (val <= 210.00000000000003) {
      return calculateTree86(features, 17);
    } else {
      return calculateTree86(features, 18);
    }
  }
  if (node === 3) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree86(features, 7);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree86(features, 7);
    } else {
      return calculateTree86(features, 8);
    }
  }
  if (node === 19) return -0.1255914156889823;
  if (node === 83) return 0.035745541819390395;
  if (node === 169) return 0.20557140174513408;
  if (node === 341) return -0.30386909395984485;
  if (node === 342) return 0.09521615765463166;
  if (node === 170) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree86(features, 341);
    } else if (val <= 705.5000000000001) {
      return calculateTree86(features, 341);
    } else {
      return calculateTree86(features, 342);
    }
  }
  if (node === 84) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree86(features, 169);
    } else if (val <= 172.50000000000003) {
      return calculateTree86(features, 169);
    } else {
      return calculateTree86(features, 170);
    }
  }
  if (node === 41) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree86(features, 83);
    } else if (val <= 57.50000000000001) {
      return calculateTree86(features, 83);
    } else {
      return calculateTree86(features, 84);
    }
  }
  if (node === 85) return 0.015425755119827418;
  if (node === 86) return -0.15150927853121773;
  if (node === 42) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree86(features, 85);
    } else if (val <= 485.50000000000006) {
      return calculateTree86(features, 85);
    } else {
      return calculateTree86(features, 86);
    }
  }
  if (node === 20) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree86(features, 41);
    } else if (val <= 16.500000000000004) {
      return calculateTree86(features, 41);
    } else {
      return calculateTree86(features, 42);
    }
  }
  if (node === 9) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree86(features, 19);
    } else if (val <= 222.50000000000003) {
      return calculateTree86(features, 19);
    } else {
      return calculateTree86(features, 20);
    }
  }
  if (node === 43) return -0.04061784380930272;
  if (node === 89) return -0.03579937914103887;
  if (node === 90) return 0.40959619890727406;
  if (node === 44) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree86(features, 89);
    } else if (val <= 804.0000000000001) {
      return calculateTree86(features, 89);
    } else {
      return calculateTree86(features, 90);
    }
  }
  if (node === 21) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree86(features, 43);
    } else if (val <= 993.5000000000001) {
      return calculateTree86(features, 43);
    } else {
      return calculateTree86(features, 44);
    }
  }
  if (node === 367) return -0.09485431455406548;
  if (node === 1475) return 0.16466532851109644;
  if (node === 1476) return -0.10416861750198604;
  if (node === 737) {
    const val = features['is_preference_pets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree86(features, 1475);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree86(features, 1475);
    } else {
      return calculateTree86(features, 1476);
    }
  }
  if (node === 738) return -0.22995342133964333;
  if (node === 368) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree86(features, 737);
    } else if (val <= 20.500000000000004) {
      return calculateTree86(features, 737);
    } else {
      return calculateTree86(features, 738);
    }
  }
  if (node === 183) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree86(features, 367);
    } else if (val <= 733.5000000000001) {
      return calculateTree86(features, 367);
    } else {
      return calculateTree86(features, 368);
    }
  }
  if (node === 369) return 0.1352113577117078;
  if (node === 370) return 0.026384076987296733;
  if (node === 184) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree86(features, 369);
    } else if (val <= 459.50000000000006) {
      return calculateTree86(features, 369);
    } else {
      return calculateTree86(features, 370);
    }
  }
  if (node === 91) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree86(features, 183);
    } else if (val <= 358.50000000000006) {
      return calculateTree86(features, 183);
    } else {
      return calculateTree86(features, 184);
    }
  }
  if (node === 185) return 0.2796219182169241;
  if (node === 1495) return 0.07035630496348459;
  if (node === 1496) return 0.35555635555437887;
  if (node === 747) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree86(features, 1495);
    } else if (val <= 834.5000000000001) {
      return calculateTree86(features, 1495);
    } else {
      return calculateTree86(features, 1496);
    }
  }
  if (node === 748) return -0.14941842873907607;
  if (node === 373) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree86(features, 747);
    } else if (val <= 903.5000000000001) {
      return calculateTree86(features, 747);
    } else {
      return calculateTree86(features, 748);
    }
  }
  if (node === 374) return -0.16197400523724514;
  if (node === 186) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree86(features, 373);
    } else if (val <= 877.5000000000001) {
      return calculateTree86(features, 373);
    } else {
      return calculateTree86(features, 374);
    }
  }
  if (node === 92) {
    const val = features['is_preference_kitchen'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree86(features, 185);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree86(features, 185);
    } else {
      return calculateTree86(features, 186);
    }
  }
  if (node === 45) {
    const val = features['is_preference_household'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree86(features, 91);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree86(features, 91);
    } else {
      return calculateTree86(features, 92);
    }
  }
  if (node === 46) return -0.002582939339683663;
  if (node === 22) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree86(features, 45);
    } else if (val <= 22.500000000000004) {
      return calculateTree86(features, 45);
    } else {
      return calculateTree86(features, 46);
    }
  }
  if (node === 10) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree86(features, 21);
    } else if (val <= 18.500000000000004) {
      return calculateTree86(features, 21);
    } else {
      return calculateTree86(features, 22);
    }
  }
  if (node === 4) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree86(features, 9);
    } else if (val <= 41.00000000000001) {
      return calculateTree86(features, 9);
    } else {
      return calculateTree86(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree86(features, 3);
    } else if (val <= 212.50000000000003) {
      return calculateTree86(features, 3);
    } else {
      return calculateTree86(features, 4);
    }
  }
  if (node === 2) return -0.000798788760021957;
  if (node === 0) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree86(features, 1);
    } else if (val <= 1.5000000000000002) {
      return calculateTree86(features, 1);
    } else {
      return calculateTree86(features, 2);
    }
  }
}
function calculateTree87(features, node = 0) {
  if (node === 255) return -0.35874760771839104;
  if (node === 256) return 0.08638377532507649;
  if (node === 127) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree87(features, 255);
    } else if (val <= 649.5000000000001) {
      return calculateTree87(features, 255);
    } else {
      return calculateTree87(features, 256);
    }
  }
  if (node === 128) return -0.38003150540914044;
  if (node === 63) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree87(features, 127);
    } else if (val <= 6.000000000000001) {
      return calculateTree87(features, 127);
    } else {
      return calculateTree87(features, 128);
    }
  }
  if (node === 259) return -0.2063967768365838;
  if (node === 260) return 0.025406821396809713;
  if (node === 129) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree87(features, 259);
    } else if (val <= 70.00000000000001) {
      return calculateTree87(features, 259);
    } else {
      return calculateTree87(features, 260);
    }
  }
  if (node === 1047) return 0.19755403654509363;
  if (node === 1048) return -0.2825311521080227;
  if (node === 523) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree87(features, 1047);
    } else if (val <= 405.00000000000006) {
      return calculateTree87(features, 1047);
    } else {
      return calculateTree87(features, 1048);
    }
  }
  if (node === 524) return 0.4188416514573547;
  if (node === 261) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree87(features, 523);
    } else if (val <= 785.5000000000001) {
      return calculateTree87(features, 523);
    } else {
      return calculateTree87(features, 524);
    }
  }
  if (node === 262) return -0.31040254541728224;
  if (node === 130) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree87(features, 261);
    } else if (val <= 896.5000000000001) {
      return calculateTree87(features, 261);
    } else {
      return calculateTree87(features, 262);
    }
  }
  if (node === 64) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree87(features, 129);
    } else if (val <= 181.50000000000003) {
      return calculateTree87(features, 129);
    } else {
      return calculateTree87(features, 130);
    }
  }
  if (node === 31) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree87(features, 63);
    } else if (val <= 12.000000000000002) {
      return calculateTree87(features, 63);
    } else {
      return calculateTree87(features, 64);
    }
  }
  if (node === 263) return 0.01108954340618307;
  if (node === 1059) return -0.36541881705875323;
  if (node === 1060) return 0.01189260802150014;
  if (node === 529) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree87(features, 1059);
    } else if (val <= 141.50000000000003) {
      return calculateTree87(features, 1059);
    } else {
      return calculateTree87(features, 1060);
    }
  }
  if (node === 1061) return -0.8273040570998295;
  if (node === 1062) return -0.29303670397498194;
  if (node === 530) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree87(features, 1061);
    } else if (val <= 675.5000000000001) {
      return calculateTree87(features, 1061);
    } else {
      return calculateTree87(features, 1062);
    }
  }
  if (node === 264) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree87(features, 529);
    } else if (val <= 658.5000000000001) {
      return calculateTree87(features, 529);
    } else {
      return calculateTree87(features, 530);
    }
  }
  if (node === 131) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree87(features, 263);
    } else if (val <= 132.50000000000003) {
      return calculateTree87(features, 263);
    } else {
      return calculateTree87(features, 264);
    }
  }
  if (node === 265) return -0.40744512623824336;
  if (node === 533) return 0.08199523702148631;
  if (node === 534) return -0.41686396826932437;
  if (node === 266) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree87(features, 533);
    } else if (val <= 172.50000000000003) {
      return calculateTree87(features, 533);
    } else {
      return calculateTree87(features, 534);
    }
  }
  if (node === 132) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree87(features, 265);
    } else if (val <= 158.50000000000003) {
      return calculateTree87(features, 265);
    } else {
      return calculateTree87(features, 266);
    }
  }
  if (node === 65) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree87(features, 131);
    } else if (val <= 903.5000000000001) {
      return calculateTree87(features, 131);
    } else {
      return calculateTree87(features, 132);
    }
  }
  if (node === 66) return 0.05011720341161511;
  if (node === 32) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree87(features, 65);
    } else if (val <= 942.0000000000001) {
      return calculateTree87(features, 65);
    } else {
      return calculateTree87(features, 66);
    }
  }
  if (node === 15) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree87(features, 31);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree87(features, 31);
    } else {
      return calculateTree87(features, 32);
    }
  }
  if (node === 16) return -0.04281606514930494;
  if (node === 7) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree87(features, 15);
    } else if (val <= 194.00000000000003) {
      return calculateTree87(features, 15);
    } else {
      return calculateTree87(features, 16);
    }
  }
  if (node === 8) return 0.00121523441380133;
  if (node === 3) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree87(features, 7);
    } else if (val <= 206.00000000000003) {
      return calculateTree87(features, 7);
    } else {
      return calculateTree87(features, 8);
    }
  }
  if (node === 9) return -0.004820705305073297;
  if (node === 10) return 0.35098232408903257;
  if (node === 4) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree87(features, 9);
    } else if (val <= 1.5000000000000002) {
      return calculateTree87(features, 9);
    } else {
      return calculateTree87(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree87(features, 3);
    } else if (val <= 996.0000000000001) {
      return calculateTree87(features, 3);
    } else {
      return calculateTree87(features, 4);
    }
  }
  if (node === 95) return -0.007586937858817785;
  if (node === 96) return 0.9688643005506594;
  if (node === 47) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree87(features, 95);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree87(features, 95);
    } else {
      return calculateTree87(features, 96);
    }
  }
  if (node === 48) return -0.1789192719302138;
  if (node === 23) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree87(features, 47);
    } else if (val <= 13.500000000000002) {
      return calculateTree87(features, 47);
    } else {
      return calculateTree87(features, 48);
    }
  }
  if (node === 49) return -0.2997202344786119;
  if (node === 50) return 0.11033532141585416;
  if (node === 24) {
    const val = features['is_preference_household'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree87(features, 49);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree87(features, 49);
    } else {
      return calculateTree87(features, 50);
    }
  }
  if (node === 11) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree87(features, 23);
    } else if (val <= 253.50000000000003) {
      return calculateTree87(features, 23);
    } else {
      return calculateTree87(features, 24);
    }
  }
  if (node === 103) return -0.001150546971939259;
  if (node === 104) return 0.22104420512199566;
  if (node === 51) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree87(features, 103);
    } else if (val <= 3.5000000000000004) {
      return calculateTree87(features, 103);
    } else {
      return calculateTree87(features, 104);
    }
  }
  if (node === 52) return -0.28530148645938186;
  if (node === 25) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree87(features, 51);
    } else if (val <= 39.50000000000001) {
      return calculateTree87(features, 51);
    } else {
      return calculateTree87(features, 52);
    }
  }
  if (node === 26) return 0.16160593136343615;
  if (node === 12) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree87(features, 25);
    } else if (val <= 591.0000000000001) {
      return calculateTree87(features, 25);
    } else {
      return calculateTree87(features, 26);
    }
  }
  if (node === 5) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree87(features, 11);
    } else if (val <= 551.5000000000001) {
      return calculateTree87(features, 11);
    } else {
      return calculateTree87(features, 12);
    }
  }
  if (node === 6) return -0.2612673275225637;
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree87(features, 5);
    } else if (val <= 606.5000000000001) {
      return calculateTree87(features, 5);
    } else {
      return calculateTree87(features, 6);
    }
  }
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree87(features, 1);
    } else if (val <= 978.0000000000001) {
      return calculateTree87(features, 1);
    } else {
      return calculateTree87(features, 2);
    }
  }
}
function calculateTree88(features, node = 0) {
  if (node === 7) return -0.005843827488481586;
  if (node === 8) return 0.016212684494775195;
  if (node === 3) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree88(features, 7);
    } else if (val <= 130.00000000000003) {
      return calculateTree88(features, 7);
    } else {
      return calculateTree88(features, 8);
    }
  }
  if (node === 19) return -0.026776722292215945;
  if (node === 41) return -0.3318960059129207;
  if (node === 42) return -0.1035434825940798;
  if (node === 20) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree88(features, 41);
    } else if (val <= 955.5000000000001) {
      return calculateTree88(features, 41);
    } else {
      return calculateTree88(features, 42);
    }
  }
  if (node === 9) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree88(features, 19);
    } else if (val <= 643.5000000000001) {
      return calculateTree88(features, 19);
    } else {
      return calculateTree88(features, 20);
    }
  }
  if (node === 175) return 0.021598843851380303;
  if (node === 707) return 0.5234019625512455;
  if (node === 708) return -0.33180673880540906;
  if (node === 353) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree88(features, 707);
    } else if (val <= 774.0000000000001) {
      return calculateTree88(features, 707);
    } else {
      return calculateTree88(features, 708);
    }
  }
  if (node === 354) return -0.2512239723410593;
  if (node === 176) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree88(features, 353);
    } else if (val <= 166.00000000000003) {
      return calculateTree88(features, 353);
    } else {
      return calculateTree88(features, 354);
    }
  }
  if (node === 87) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree88(features, 175);
    } else if (val <= 282.50000000000006) {
      return calculateTree88(features, 175);
    } else {
      return calculateTree88(features, 176);
    }
  }
  if (node === 177) return -0.1317345768863408;
  if (node === 178) return -0.308934825693259;
  if (node === 88) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree88(features, 177);
    } else if (val <= 171.00000000000003) {
      return calculateTree88(features, 177);
    } else {
      return calculateTree88(features, 178);
    }
  }
  if (node === 43) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree88(features, 87);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree88(features, 87);
    } else {
      return calculateTree88(features, 88);
    }
  }
  if (node === 89) return -0.3308144281115109;
  if (node === 90) return 0.0021113667355225577;
  if (node === 44) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree88(features, 89);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree88(features, 89);
    } else {
      return calculateTree88(features, 90);
    }
  }
  if (node === 21) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree88(features, 43);
    } else if (val <= 905.5000000000001) {
      return calculateTree88(features, 43);
    } else {
      return calculateTree88(features, 44);
    }
  }
  if (node === 45) return 0.0664475307980352;
  if (node === 46) return -0.0169766400270077;
  if (node === 22) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree88(features, 45);
    } else if (val <= 38.50000000000001) {
      return calculateTree88(features, 45);
    } else {
      return calculateTree88(features, 46);
    }
  }
  if (node === 10) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree88(features, 21);
    } else if (val <= 16.500000000000004) {
      return calculateTree88(features, 21);
    } else {
      return calculateTree88(features, 22);
    }
  }
  if (node === 4) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree88(features, 9);
    } else if (val <= 158.50000000000003) {
      return calculateTree88(features, 9);
    } else {
      return calculateTree88(features, 10);
    }
  }
  if (node === 1) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree88(features, 3);
    } else if (val <= 151.50000000000003) {
      return calculateTree88(features, 3);
    } else {
      return calculateTree88(features, 4);
    }
  }
  if (node === 47) return 0.007032081685613058;
  if (node === 97) return -0.5267521442668379;
  if (node === 197) return 0.10041351206755247;
  if (node === 198) return -0.3012440427196711;
  if (node === 98) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree88(features, 197);
    } else if (val <= 39.50000000000001) {
      return calculateTree88(features, 197);
    } else {
      return calculateTree88(features, 198);
    }
  }
  if (node === 48) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree88(features, 97);
    } else if (val <= 230.50000000000003) {
      return calculateTree88(features, 97);
    } else {
      return calculateTree88(features, 98);
    }
  }
  if (node === 23) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree88(features, 47);
    } else if (val <= 150.50000000000003) {
      return calculateTree88(features, 47);
    } else {
      return calculateTree88(features, 48);
    }
  }
  if (node === 49) return -0.3573747520000254;
  if (node === 101) return 0.2362394058674668;
  if (node === 102) return -0.08771716122270488;
  if (node === 50) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree88(features, 101);
    } else if (val <= 47.50000000000001) {
      return calculateTree88(features, 101);
    } else {
      return calculateTree88(features, 102);
    }
  }
  if (node === 24) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree88(features, 49);
    } else if (val <= 150.50000000000003) {
      return calculateTree88(features, 49);
    } else {
      return calculateTree88(features, 50);
    }
  }
  if (node === 11) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree88(features, 23);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree88(features, 23);
    } else {
      return calculateTree88(features, 24);
    }
  }
  if (node === 51) return -0.33862870827394953;
  if (node === 211) return 0.22650934625560237;
  if (node === 212) return 0.46929533904707504;
  if (node === 105) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree88(features, 211);
    } else if (val <= 26.500000000000004) {
      return calculateTree88(features, 211);
    } else {
      return calculateTree88(features, 212);
    }
  }
  if (node === 106) return -0.34414816402653775;
  if (node === 52) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree88(features, 105);
    } else if (val <= 977.5000000000001) {
      return calculateTree88(features, 105);
    } else {
      return calculateTree88(features, 106);
    }
  }
  if (node === 25) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree88(features, 51);
    } else if (val <= 658.5000000000001) {
      return calculateTree88(features, 51);
    } else {
      return calculateTree88(features, 52);
    }
  }
  if (node === 215) return -0.16666289211676327;
  if (node === 216) return 0.23213196345554463;
  if (node === 107) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree88(features, 215);
    } else if (val <= 30.500000000000004) {
      return calculateTree88(features, 215);
    } else {
      return calculateTree88(features, 216);
    }
  }
  if (node === 108) return -0.4353751852268305;
  if (node === 53) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree88(features, 107);
    } else if (val <= 658.5000000000001) {
      return calculateTree88(features, 107);
    } else {
      return calculateTree88(features, 108);
    }
  }
  if (node === 109) return -0.3475815091608914;
  if (node === 110) return 0.5777309213337202;
  if (node === 54) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree88(features, 109);
    } else if (val <= 178.50000000000003) {
      return calculateTree88(features, 109);
    } else {
      return calculateTree88(features, 110);
    }
  }
  if (node === 26) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree88(features, 53);
    } else if (val <= 977.5000000000001) {
      return calculateTree88(features, 53);
    } else {
      return calculateTree88(features, 54);
    }
  }
  if (node === 12) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree88(features, 25);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree88(features, 25);
    } else {
      return calculateTree88(features, 26);
    }
  }
  if (node === 5) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree88(features, 11);
    } else if (val <= 635.0000000000001) {
      return calculateTree88(features, 11);
    } else {
      return calculateTree88(features, 12);
    }
  }
  if (node === 6) return 4.9045923918200636e-5;
  if (node === 2) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree88(features, 5);
    } else if (val <= 181.50000000000003) {
      return calculateTree88(features, 5);
    } else {
      return calculateTree88(features, 6);
    }
  }
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree88(features, 1);
    } else if (val <= 172.50000000000003) {
      return calculateTree88(features, 1);
    } else {
      return calculateTree88(features, 2);
    }
  }
}
function calculateTree89(features, node = 0) {
  if (node === 31) return -0.011579864199890487;
  if (node === 131) return 0.024095985063354555;
  if (node === 265) return -0.08824606949578152;
  if (node === 266) return 0.011263502617315827;
  if (node === 132) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree89(features, 265);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree89(features, 265);
    } else {
      return calculateTree89(features, 266);
    }
  }
  if (node === 65) {
    const val = features['is_preference_women_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree89(features, 131);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree89(features, 131);
    } else {
      return calculateTree89(features, 132);
    }
  }
  if (node === 133) return 0.09980804495411986;
  if (node === 134) return -0.07747324339486687;
  if (node === 66) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree89(features, 133);
    } else if (val <= 1.5000000000000002) {
      return calculateTree89(features, 133);
    } else {
      return calculateTree89(features, 134);
    }
  }
  if (node === 32) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree89(features, 65);
    } else if (val <= 54.50000000000001) {
      return calculateTree89(features, 65);
    } else {
      return calculateTree89(features, 66);
    }
  }
  if (node === 15) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree89(features, 31);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree89(features, 31);
    } else {
      return calculateTree89(features, 32);
    }
  }
  if (node === 67) return -0.06353099221635716;
  if (node === 68) return 0.0023349856920360356;
  if (node === 33) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree89(features, 67);
    } else if (val <= 175.50000000000003) {
      return calculateTree89(features, 67);
    } else {
      return calculateTree89(features, 68);
    }
  }
  if (node === 69) return -0.04673493990211817;
  if (node === 141) return 0.12110472490044842;
  if (node === 142) return -0.29112742660415314;
  if (node === 70) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree89(features, 141);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree89(features, 141);
    } else {
      return calculateTree89(features, 142);
    }
  }
  if (node === 34) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree89(features, 69);
    } else if (val <= 893.0000000000001) {
      return calculateTree89(features, 69);
    } else {
      return calculateTree89(features, 70);
    }
  }
  if (node === 16) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree89(features, 33);
    } else if (val <= 1.5000000000000002) {
      return calculateTree89(features, 33);
    } else {
      return calculateTree89(features, 34);
    }
  }
  if (node === 7) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree89(features, 15);
    } else if (val <= 164.50000000000003) {
      return calculateTree89(features, 15);
    } else {
      return calculateTree89(features, 16);
    }
  }
  if (node === 35) return 0.035033676571035975;
  if (node === 36) return -0.08444474881923422;
  if (node === 17) {
    const val = features['is_preference_personal_care'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree89(features, 35);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree89(features, 35);
    } else {
      return calculateTree89(features, 36);
    }
  }
  if (node === 151) return 0.20726230876265933;
  if (node === 305) return 0.10377137076963282;
  if (node === 306) return -0.1967943162583404;
  if (node === 152) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree89(features, 305);
    } else if (val <= 34.50000000000001) {
      return calculateTree89(features, 305);
    } else {
      return calculateTree89(features, 306);
    }
  }
  if (node === 75) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree89(features, 151);
    } else if (val <= 188.00000000000003) {
      return calculateTree89(features, 151);
    } else {
      return calculateTree89(features, 152);
    }
  }
  if (node === 76) return -0.035574205817191784;
  if (node === 37) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree89(features, 75);
    } else if (val <= 181.50000000000003) {
      return calculateTree89(features, 75);
    } else {
      return calculateTree89(features, 76);
    }
  }
  if (node === 311) return 0.01347838904571223;
  if (node === 312) return 0.2450439460449068;
  if (node === 155) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree89(features, 311);
    } else if (val <= 643.5000000000001) {
      return calculateTree89(features, 311);
    } else {
      return calculateTree89(features, 312);
    }
  }
  if (node === 156) return 0.3154050792571271;
  if (node === 77) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree89(features, 155);
    } else if (val <= 186.50000000000003) {
      return calculateTree89(features, 155);
    } else {
      return calculateTree89(features, 156);
    }
  }
  if (node === 157) return -0.21293985872766796;
  if (node === 158) return 0.0961503448355761;
  if (node === 78) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree89(features, 157);
    } else if (val <= 27.500000000000004) {
      return calculateTree89(features, 157);
    } else {
      return calculateTree89(features, 158);
    }
  }
  if (node === 38) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree89(features, 77);
    } else if (val <= 804.0000000000001) {
      return calculateTree89(features, 77);
    } else {
      return calculateTree89(features, 78);
    }
  }
  if (node === 18) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree89(features, 37);
    } else if (val <= 42.50000000000001) {
      return calculateTree89(features, 37);
    } else {
      return calculateTree89(features, 38);
    }
  }
  if (node === 8) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree89(features, 17);
    } else if (val <= 141.50000000000003) {
      return calculateTree89(features, 17);
    } else {
      return calculateTree89(features, 18);
    }
  }
  if (node === 3) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree89(features, 7);
    } else if (val <= 24.500000000000004) {
      return calculateTree89(features, 7);
    } else {
      return calculateTree89(features, 8);
    }
  }
  if (node === 39) return 0.20812828833092942;
  if (node === 40) return -0.17834776198098654;
  if (node === 19) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree89(features, 39);
    } else if (val <= 181.50000000000003) {
      return calculateTree89(features, 39);
    } else {
      return calculateTree89(features, 40);
    }
  }
  if (node === 20) return -0.3424840264720437;
  if (node === 9) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree89(features, 19);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree89(features, 19);
    } else {
      return calculateTree89(features, 20);
    }
  }
  if (node === 10) return 0.0007190743701623504;
  if (node === 4) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree89(features, 9);
    } else if (val <= 705.5000000000001) {
      return calculateTree89(features, 9);
    } else {
      return calculateTree89(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree89(features, 3);
    } else if (val <= 205.50000000000003) {
      return calculateTree89(features, 3);
    } else {
      return calculateTree89(features, 4);
    }
  }
  if (node === 11) return -0.33109078424008165;
  if (node === 103) return 0.036963210285920915;
  if (node === 104) return 0.26769403451461327;
  if (node === 51) {
    const val = features['is_preference_beauty'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree89(features, 103);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree89(features, 103);
    } else {
      return calculateTree89(features, 104);
    }
  }
  if (node === 52) return -0.19030694962780645;
  if (node === 25) {
    const val = features['is_preference_men_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree89(features, 51);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree89(features, 51);
    } else {
      return calculateTree89(features, 52);
    }
  }
  if (node === 26) return 0.17656708249461586;
  if (node === 12) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree89(features, 25);
    } else if (val <= 27.500000000000004) {
      return calculateTree89(features, 25);
    } else {
      return calculateTree89(features, 26);
    }
  }
  if (node === 5) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree89(features, 11);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree89(features, 11);
    } else {
      return calculateTree89(features, 12);
    }
  }
  if (node === 6) return 0.000863242769169978;
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree89(features, 5);
    } else if (val <= 208.50000000000003) {
      return calculateTree89(features, 5);
    } else {
      return calculateTree89(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree89(features, 1);
    } else if (val <= 207.00000000000003) {
      return calculateTree89(features, 1);
    } else {
      return calculateTree89(features, 2);
    }
  }
}
function calculateTree90(features, node = 0) {
  if (node === 7) return 0.0003468242599642635;
  if (node === 17) return -0.0577522608461969;
  if (node === 18) return 0.10579779728742429;
  if (node === 8) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree90(features, 17);
    } else if (val <= 49.50000000000001) {
      return calculateTree90(features, 17);
    } else {
      return calculateTree90(features, 18);
    }
  }
  if (node === 3) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree90(features, 7);
    } else if (val <= 619.5000000000001) {
      return calculateTree90(features, 7);
    } else {
      return calculateTree90(features, 8);
    }
  }
  if (node === 79) return 0.1467874022769095;
  if (node === 80) return 0.5294939443157644;
  if (node === 39) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree90(features, 79);
    } else if (val <= 214.00000000000003) {
      return calculateTree90(features, 79);
    } else {
      return calculateTree90(features, 80);
    }
  }
  if (node === 163) return -0.46237696058137523;
  if (node === 329) return 0.6614531527397468;
  if (node === 661) return -0.2726852670921503;
  if (node === 1325) return 0.0848479465334996;
  if (node === 1326) return -0.3494890059562286;
  if (node === 662) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree90(features, 1325);
    } else if (val <= 908.5000000000001) {
      return calculateTree90(features, 1325);
    } else {
      return calculateTree90(features, 1326);
    }
  }
  if (node === 330) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree90(features, 661);
    } else if (val <= 784.5000000000001) {
      return calculateTree90(features, 661);
    } else {
      return calculateTree90(features, 662);
    }
  }
  if (node === 164) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree90(features, 329);
    } else if (val <= 740.5000000000001) {
      return calculateTree90(features, 329);
    } else {
      return calculateTree90(features, 330);
    }
  }
  if (node === 81) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree90(features, 163);
    } else if (val <= 398.00000000000006) {
      return calculateTree90(features, 163);
    } else {
      return calculateTree90(features, 164);
    }
  }
  if (node === 82) return 0.23656705708331696;
  if (node === 40) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree90(features, 81);
    } else if (val <= 659.5000000000001) {
      return calculateTree90(features, 81);
    } else {
      return calculateTree90(features, 82);
    }
  }
  if (node === 19) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree90(features, 39);
    } else if (val <= 253.50000000000003) {
      return calculateTree90(features, 39);
    } else {
      return calculateTree90(features, 40);
    }
  }
  if (node === 41) return -0.36275582784682386;
  if (node === 85) return 0.32661927922213635;
  if (node === 695) return -0.36617785945236053;
  if (node === 1393) return 0.045338132452407344;
  if (node === 1394) return 0.37050304710034704;
  if (node === 696) {
    const val = features['is_preference_women_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree90(features, 1393);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree90(features, 1393);
    } else {
      return calculateTree90(features, 1394);
    }
  }
  if (node === 347) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree90(features, 695);
    } else if (val <= 606.5000000000001) {
      return calculateTree90(features, 695);
    } else {
      return calculateTree90(features, 696);
    }
  }
  if (node === 348) return -0.3656675765654485;
  if (node === 173) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree90(features, 347);
    } else if (val <= 784.5000000000001) {
      return calculateTree90(features, 347);
    } else {
      return calculateTree90(features, 348);
    }
  }
  if (node === 349) return 0.7412922657017421;
  if (node === 350) return -0.17445740409402685;
  if (node === 174) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree90(features, 349);
    } else if (val <= 943.5000000000001) {
      return calculateTree90(features, 349);
    } else {
      return calculateTree90(features, 350);
    }
  }
  if (node === 86) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree90(features, 173);
    } else if (val <= 908.5000000000001) {
      return calculateTree90(features, 173);
    } else {
      return calculateTree90(features, 174);
    }
  }
  if (node === 42) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree90(features, 85);
    } else if (val <= 398.00000000000006) {
      return calculateTree90(features, 85);
    } else {
      return calculateTree90(features, 86);
    }
  }
  if (node === 20) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree90(features, 41);
    } else if (val <= 291.50000000000006) {
      return calculateTree90(features, 41);
    } else {
      return calculateTree90(features, 42);
    }
  }
  if (node === 9) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree90(features, 19);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree90(features, 19);
    } else {
      return calculateTree90(features, 20);
    }
  }
  if (node === 87) return -0.019879233201532554;
  if (node === 88) return -0.21327056425918448;
  if (node === 43) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree90(features, 87);
    } else if (val <= 740.5000000000001) {
      return calculateTree90(features, 87);
    } else {
      return calculateTree90(features, 88);
    }
  }
  if (node === 44) return 0.23812265394000398;
  if (node === 21) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree90(features, 43);
    } else if (val <= 822.5000000000001) {
      return calculateTree90(features, 43);
    } else {
      return calculateTree90(features, 44);
    }
  }
  if (node === 22) return -0.3833581417438771;
  if (node === 10) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree90(features, 21);
    } else if (val <= 908.5000000000001) {
      return calculateTree90(features, 21);
    } else {
      return calculateTree90(features, 22);
    }
  }
  if (node === 4) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree90(features, 9);
    } else if (val <= 32.50000000000001) {
      return calculateTree90(features, 9);
    } else {
      return calculateTree90(features, 10);
    }
  }
  if (node === 1) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree90(features, 3);
    } else if (val <= 656.5000000000001) {
      return calculateTree90(features, 3);
    } else {
      return calculateTree90(features, 4);
    }
  }
  if (node === 11) return -0.3558409362259137;
  if (node === 25) return -0.364029662359544;
  if (node === 26) return 0.12416700943047103;
  if (node === 12) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree90(features, 25);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree90(features, 25);
    } else {
      return calculateTree90(features, 26);
    }
  }
  if (node === 5) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree90(features, 11);
    } else if (val <= 973.5000000000001) {
      return calculateTree90(features, 11);
    } else {
      return calculateTree90(features, 12);
    }
  }
  if (node === 111) return -0.3612386923069907;
  if (node === 112) return 0.11755930731799895;
  if (node === 55) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree90(features, 111);
    } else if (val <= 705.5000000000001) {
      return calculateTree90(features, 111);
    } else {
      return calculateTree90(features, 112);
    }
  }
  if (node === 56) return -0.3256674969515031;
  if (node === 27) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree90(features, 55);
    } else if (val <= 708.5000000000001) {
      return calculateTree90(features, 55);
    } else {
      return calculateTree90(features, 56);
    }
  }
  if (node === 57) return 0.13093747247353163;
  if (node === 58) return -0.10096560412780327;
  if (node === 28) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree90(features, 57);
    } else if (val <= 17.500000000000004) {
      return calculateTree90(features, 57);
    } else {
      return calculateTree90(features, 58);
    }
  }
  if (node === 13) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree90(features, 27);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree90(features, 27);
    } else {
      return calculateTree90(features, 28);
    }
  }
  if (node === 14) return -0.002977467608965857;
  if (node === 6) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree90(features, 13);
    } else if (val <= 28.500000000000004) {
      return calculateTree90(features, 13);
    } else {
      return calculateTree90(features, 14);
    }
  }
  if (node === 2) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree90(features, 5);
    } else if (val <= 665.0000000000001) {
      return calculateTree90(features, 5);
    } else {
      return calculateTree90(features, 6);
    }
  }
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree90(features, 1);
    } else if (val <= 661.0000000000001) {
      return calculateTree90(features, 1);
    } else {
      return calculateTree90(features, 2);
    }
  }
}
function calculateTree91(features, node = 0) {
  if (node === 1) return 0.000581632594331266;
  if (node === 23) return 0.1742121512277934;
  if (node === 49) return -0.12836441482995237;
  if (node === 203) return 0.011716451848261191;
  if (node === 204) return 0.3492907894202267;
  if (node === 101) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree91(features, 203);
    } else if (val <= 295.00000000000006) {
      return calculateTree91(features, 203);
    } else {
      return calculateTree91(features, 204);
    }
  }
  if (node === 102) return -0.012816337307283036;
  if (node === 50) {
    const val = features['is_preference_kids'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree91(features, 101);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree91(features, 101);
    } else {
      return calculateTree91(features, 102);
    }
  }
  if (node === 24) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree91(features, 49);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree91(features, 49);
    } else {
      return calculateTree91(features, 50);
    }
  }
  if (node === 11) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree91(features, 23);
    } else if (val <= 25.500000000000004) {
      return calculateTree91(features, 23);
    } else {
      return calculateTree91(features, 24);
    }
  }
  if (node === 51) return -0.04641256036335147;
  if (node === 52) return 0.18932984655492355;
  if (node === 25) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree91(features, 51);
    } else if (val <= 34.50000000000001) {
      return calculateTree91(features, 51);
    } else {
      return calculateTree91(features, 52);
    }
  }
  if (node === 53) return -0.20019761331760993;
  if (node === 109) return 0.2676756981491348;
  if (node === 110) return -0.13259772893813399;
  if (node === 54) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree91(features, 109);
    } else if (val <= 44.50000000000001) {
      return calculateTree91(features, 109);
    } else {
      return calculateTree91(features, 110);
    }
  }
  if (node === 26) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree91(features, 53);
    } else if (val <= 39.50000000000001) {
      return calculateTree91(features, 53);
    } else {
      return calculateTree91(features, 54);
    }
  }
  if (node === 12) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree91(features, 25);
    } else if (val <= 4.500000000000001) {
      return calculateTree91(features, 25);
    } else {
      return calculateTree91(features, 26);
    }
  }
  if (node === 5) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree91(features, 11);
    } else if (val <= 551.5000000000001) {
      return calculateTree91(features, 11);
    } else {
      return calculateTree91(features, 12);
    }
  }
  if (node === 27) return 0.005451959862478508;
  if (node === 115) return -0.31457755381423297;
  if (node === 233) return 0.1557444819773766;
  if (node === 234) return -0.18419080807636037;
  if (node === 116) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree91(features, 233);
    } else if (val <= 10.500000000000002) {
      return calculateTree91(features, 233);
    } else {
      return calculateTree91(features, 234);
    }
  }
  if (node === 57) {
    const val = features['is_preference_pets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree91(features, 115);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree91(features, 115);
    } else {
      return calculateTree91(features, 116);
    }
  }
  if (node === 235) return 0.15533883353483174;
  if (node === 236) return -0.07585734602090238;
  if (node === 117) {
    const val = features['is_preference_kitchen'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree91(features, 235);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree91(features, 235);
    } else {
      return calculateTree91(features, 236);
    }
  }
  if (node === 237) return -0.02280870923123131;
  if (node === 955) return -0.46080438153645276;
  if (node === 956) return -0.1123734042624683;
  if (node === 477) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree91(features, 955);
    } else if (val <= 503.50000000000006) {
      return calculateTree91(features, 955);
    } else {
      return calculateTree91(features, 956);
    }
  }
  if (node === 957) return -0.06689704188615596;
  if (node === 958) return 0.042079678577049404;
  if (node === 478) {
    const val = features['is_preference_women_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree91(features, 957);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree91(features, 957);
    } else {
      return calculateTree91(features, 958);
    }
  }
  if (node === 238) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree91(features, 477);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree91(features, 477);
    } else {
      return calculateTree91(features, 478);
    }
  }
  if (node === 118) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree91(features, 237);
    } else if (val <= 13.500000000000002) {
      return calculateTree91(features, 237);
    } else {
      return calculateTree91(features, 238);
    }
  }
  if (node === 58) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree91(features, 117);
    } else if (val <= 424.00000000000006) {
      return calculateTree91(features, 117);
    } else {
      return calculateTree91(features, 118);
    }
  }
  if (node === 28) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree91(features, 57);
    } else if (val <= 417.00000000000006) {
      return calculateTree91(features, 57);
    } else {
      return calculateTree91(features, 58);
    }
  }
  if (node === 13) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree91(features, 27);
    } else if (val <= 391.50000000000006) {
      return calculateTree91(features, 27);
    } else {
      return calculateTree91(features, 28);
    }
  }
  if (node === 29) return -0.025018075617637726;
  if (node === 991) return -0.09760832398255763;
  if (node === 992) return 0.17833179183521528;
  if (node === 495) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree91(features, 991);
    } else if (val <= 28.500000000000004) {
      return calculateTree91(features, 991);
    } else {
      return calculateTree91(features, 992);
    }
  }
  if (node === 496) return -0.09143312628699657;
  if (node === 247) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree91(features, 495);
    } else if (val <= 507.50000000000006) {
      return calculateTree91(features, 495);
    } else {
      return calculateTree91(features, 496);
    }
  }
  if (node === 248) return 0.021811336795810002;
  if (node === 123) {
    const val = features['is_preference_home'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree91(features, 247);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree91(features, 247);
    } else {
      return calculateTree91(features, 248);
    }
  }
  if (node === 124) return -0.03408001127777103;
  if (node === 61) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree91(features, 123);
    } else if (val <= 783.5000000000001) {
      return calculateTree91(features, 123);
    } else {
      return calculateTree91(features, 124);
    }
  }
  if (node === 503) return -0.1810249453420994;
  if (node === 504) return 0.29557455174378156;
  if (node === 251) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree91(features, 503);
    } else if (val <= 576.0000000000001) {
      return calculateTree91(features, 503);
    } else {
      return calculateTree91(features, 504);
    }
  }
  if (node === 252) return 0.4554719495755779;
  if (node === 125) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree91(features, 251);
    } else if (val <= 777.5000000000001) {
      return calculateTree91(features, 251);
    } else {
      return calculateTree91(features, 252);
    }
  }
  if (node === 126) return 0.040498972551357905;
  if (node === 62) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree91(features, 125);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree91(features, 125);
    } else {
      return calculateTree91(features, 126);
    }
  }
  if (node === 30) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree91(features, 61);
    } else if (val <= 963.0000000000001) {
      return calculateTree91(features, 61);
    } else {
      return calculateTree91(features, 62);
    }
  }
  if (node === 14) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree91(features, 29);
    } else if (val <= 253.50000000000003) {
      return calculateTree91(features, 29);
    } else {
      return calculateTree91(features, 30);
    }
  }
  if (node === 6) {
    const val = features['is_preference_kids'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree91(features, 13);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree91(features, 13);
    } else {
      return calculateTree91(features, 14);
    }
  }
  if (node === 2) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree91(features, 5);
    } else if (val <= 34.00000000000001) {
      return calculateTree91(features, 5);
    } else {
      return calculateTree91(features, 6);
    }
  }
  if (node === 0) {
    const val = features['is_preference_men_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree91(features, 1);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree91(features, 1);
    } else {
      return calculateTree91(features, 2);
    }
  }
}
function calculateTree92(features, node = 0) {
  if (node === 3) return 0.03995687651930486;
  if (node === 4) return -0.3062957994853991;
  if (node === 1) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree92(features, 3);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree92(features, 3);
    } else {
      return calculateTree92(features, 4);
    }
  }
  if (node === 383) return 0.18157013565255473;
  if (node === 384) return -0.3540074653134108;
  if (node === 191) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree92(features, 383);
    } else if (val <= 8.500000000000002) {
      return calculateTree92(features, 383);
    } else {
      return calculateTree92(features, 384);
    }
  }
  if (node === 385) return -0.32028778227096116;
  if (node === 773) return 0.09844179256280451;
  if (node === 1549) return -0.3982203216522314;
  if (node === 1550) return 0.10033257868443957;
  if (node === 774) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree92(features, 1549);
    } else if (val <= 470.00000000000006) {
      return calculateTree92(features, 1549);
    } else {
      return calculateTree92(features, 1550);
    }
  }
  if (node === 386) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree92(features, 773);
    } else if (val <= 229.50000000000003) {
      return calculateTree92(features, 773);
    } else {
      return calculateTree92(features, 774);
    }
  }
  if (node === 192) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree92(features, 385);
    } else if (val <= 181.50000000000003) {
      return calculateTree92(features, 385);
    } else {
      return calculateTree92(features, 386);
    }
  }
  if (node === 95) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree92(features, 191);
    } else if (val <= 16.500000000000004) {
      return calculateTree92(features, 191);
    } else {
      return calculateTree92(features, 192);
    }
  }
  if (node === 96) return -0.36811271683483937;
  if (node === 47) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree92(features, 95);
    } else if (val <= 26.500000000000004) {
      return calculateTree92(features, 95);
    } else {
      return calculateTree92(features, 96);
    }
  }
  if (node === 1567) return -0.012253990142435335;
  if (node === 1568) return 0.0747772649333768;
  if (node === 783) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree92(features, 1567);
    } else if (val <= 30.500000000000004) {
      return calculateTree92(features, 1567);
    } else {
      return calculateTree92(features, 1568);
    }
  }
  if (node === 784) return -0.3927459056566015;
  if (node === 391) {
    const val = features['is_preference_home'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree92(features, 783);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree92(features, 783);
    } else {
      return calculateTree92(features, 784);
    }
  }
  if (node === 785) return -0.28494533057878774;
  if (node === 1573) return 0.09337056198321726;
  if (node === 1574) return -0.20603877879553367;
  if (node === 786) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree92(features, 1573);
    } else if (val <= 888.5000000000001) {
      return calculateTree92(features, 1573);
    } else {
      return calculateTree92(features, 1574);
    }
  }
  if (node === 392) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree92(features, 785);
    } else if (val <= 437.50000000000006) {
      return calculateTree92(features, 785);
    } else {
      return calculateTree92(features, 786);
    }
  }
  if (node === 195) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree92(features, 391);
    } else if (val <= 176.50000000000003) {
      return calculateTree92(features, 391);
    } else {
      return calculateTree92(features, 392);
    }
  }
  if (node === 196) return 0.11041543915154102;
  if (node === 97) {
    const val = features['is_preference_personal_care'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree92(features, 195);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree92(features, 195);
    } else {
      return calculateTree92(features, 196);
    }
  }
  if (node === 98) return 0.2497190747755824;
  if (node === 48) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree92(features, 97);
    } else if (val <= 24.500000000000004) {
      return calculateTree92(features, 97);
    } else {
      return calculateTree92(features, 98);
    }
  }
  if (node === 23) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree92(features, 47);
    } else if (val <= 531.0000000000001) {
      return calculateTree92(features, 47);
    } else {
      return calculateTree92(features, 48);
    }
  }
  if (node === 399) return -0.20848486672192604;
  if (node === 400) return -0.01634075604470231;
  if (node === 199) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree92(features, 399);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree92(features, 399);
    } else {
      return calculateTree92(features, 400);
    }
  }
  if (node === 200) return 0.03733057189953876;
  if (node === 99) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree92(features, 199);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree92(features, 199);
    } else {
      return calculateTree92(features, 200);
    }
  }
  if (node === 807) return 0.9150491374650537;
  if (node === 808) return 0.3435426631219385;
  if (node === 403) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree92(features, 807);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree92(features, 807);
    } else {
      return calculateTree92(features, 808);
    }
  }
  if (node === 404) return -0.17984785021511257;
  if (node === 201) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree92(features, 403);
    } else if (val <= 17.500000000000004) {
      return calculateTree92(features, 403);
    } else {
      return calculateTree92(features, 404);
    }
  }
  if (node === 202) return -0.21665425129403917;
  if (node === 100) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree92(features, 201);
    } else if (val <= 36.50000000000001) {
      return calculateTree92(features, 201);
    } else {
      return calculateTree92(features, 202);
    }
  }
  if (node === 49) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree92(features, 99);
    } else if (val <= 28.500000000000004) {
      return calculateTree92(features, 99);
    } else {
      return calculateTree92(features, 100);
    }
  }
  if (node === 203) return -0.3162772519214984;
  if (node === 409) return 0.1441807273927482;
  if (node === 410) return -0.08514923187571186;
  if (node === 204) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree92(features, 409);
    } else if (val <= 576.0000000000001) {
      return calculateTree92(features, 409);
    } else {
      return calculateTree92(features, 410);
    }
  }
  if (node === 101) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree92(features, 203);
    } else if (val <= 695.5000000000001) {
      return calculateTree92(features, 203);
    } else {
      return calculateTree92(features, 204);
    }
  }
  if (node === 205) return -0.2720479705126183;
  if (node === 413) return 0.10022566003028961;
  if (node === 414) return -0.32398778206052725;
  if (node === 206) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree92(features, 413);
    } else if (val <= 898.5000000000001) {
      return calculateTree92(features, 413);
    } else {
      return calculateTree92(features, 414);
    }
  }
  if (node === 102) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree92(features, 205);
    } else if (val <= 893.0000000000001) {
      return calculateTree92(features, 205);
    } else {
      return calculateTree92(features, 206);
    }
  }
  if (node === 50) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree92(features, 101);
    } else if (val <= 14.500000000000002) {
      return calculateTree92(features, 101);
    } else {
      return calculateTree92(features, 102);
    }
  }
  if (node === 24) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree92(features, 49);
    } else if (val <= 531.0000000000001) {
      return calculateTree92(features, 49);
    } else {
      return calculateTree92(features, 50);
    }
  }
  if (node === 11) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree92(features, 23);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree92(features, 23);
    } else {
      return calculateTree92(features, 24);
    }
  }
  if (node === 12) return 0.08756985641831763;
  if (node === 5) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree92(features, 11);
    } else if (val <= 56.50000000000001) {
      return calculateTree92(features, 11);
    } else {
      return calculateTree92(features, 12);
    }
  }
  if (node === 6) return -6.132519925364407e-5;
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree92(features, 5);
    } else if (val <= 41.50000000000001) {
      return calculateTree92(features, 5);
    } else {
      return calculateTree92(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree92(features, 1);
    } else if (val <= 3.5000000000000004) {
      return calculateTree92(features, 1);
    } else {
      return calculateTree92(features, 2);
    }
  }
}
function calculateTree93(features, node = 0) {
  if (node === 3) return 3.5662607593360605e-5;
  if (node === 9) return -0.011110043296137184;
  if (node === 87) return -0.08805986023970472;
  if (node === 88) return 0.21815211680926413;
  if (node === 43) {
    const val = features['is_preference_beauty'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree93(features, 87);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree93(features, 87);
    } else {
      return calculateTree93(features, 88);
    }
  }
  if (node === 44) return -0.06614738839644548;
  if (node === 21) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree93(features, 43);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree93(features, 43);
    } else {
      return calculateTree93(features, 44);
    }
  }
  if (node === 22) return -0.19677209652654237;
  if (node === 10) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree93(features, 21);
    } else if (val <= 29.500000000000004) {
      return calculateTree93(features, 21);
    } else {
      return calculateTree93(features, 22);
    }
  }
  if (node === 4) {
    const val = features['is_preference_men_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree93(features, 9);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree93(features, 9);
    } else {
      return calculateTree93(features, 10);
    }
  }
  if (node === 1) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree93(features, 3);
    } else if (val <= 540.0000000000001) {
      return calculateTree93(features, 3);
    } else {
      return calculateTree93(features, 4);
    }
  }
  if (node === 11) return 0.006482747994816951;
  if (node === 51) return 0.037216822836393534;
  if (node === 423) return 0.2551972814998187;
  if (node === 424) return 0.04310929161420139;
  if (node === 211) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree93(features, 423);
    } else if (val <= 31.500000000000004) {
      return calculateTree93(features, 423);
    } else {
      return calculateTree93(features, 424);
    }
  }
  if (node === 425) return 0.47437123842639567;
  if (node === 426) return 0.09221779330633857;
  if (node === 212) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree93(features, 425);
    } else if (val <= 52.50000000000001) {
      return calculateTree93(features, 425);
    } else {
      return calculateTree93(features, 426);
    }
  }
  if (node === 105) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree93(features, 211);
    } else if (val <= 48.50000000000001) {
      return calculateTree93(features, 211);
    } else {
      return calculateTree93(features, 212);
    }
  }
  if (node === 213) return 0.1201511371165345;
  if (node === 214) return -0.04297839448249324;
  if (node === 106) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree93(features, 213);
    } else if (val <= 673.0000000000001) {
      return calculateTree93(features, 213);
    } else {
      return calculateTree93(features, 214);
    }
  }
  if (node === 52) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree93(features, 105);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree93(features, 105);
    } else {
      return calculateTree93(features, 106);
    }
  }
  if (node === 25) {
    const val = features['is_preference_men_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree93(features, 51);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree93(features, 51);
    } else {
      return calculateTree93(features, 52);
    }
  }
  if (node === 107) return 0.32030888458912604;
  if (node === 1743) return -0.11267951666442548;
  if (node === 1744) return 0.25799635398902343;
  if (node === 871) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree93(features, 1743);
    } else if (val <= 31.500000000000004) {
      return calculateTree93(features, 1743);
    } else {
      return calculateTree93(features, 1744);
    }
  }
  if (node === 872) return -0.04101012023949622;
  if (node === 435) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree93(features, 871);
    } else if (val <= 37.50000000000001) {
      return calculateTree93(features, 871);
    } else {
      return calculateTree93(features, 872);
    }
  }
  if (node === 436) return 0.2608807909217613;
  if (node === 217) {
    const val = features['is_preference_household'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree93(features, 435);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree93(features, 435);
    } else {
      return calculateTree93(features, 436);
    }
  }
  if (node === 875) return 0.14418706807802498;
  if (node === 876) return -0.16899290369992678;
  if (node === 437) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree93(features, 875);
    } else if (val <= 32.50000000000001) {
      return calculateTree93(features, 875);
    } else {
      return calculateTree93(features, 876);
    }
  }
  if (node === 438) return 0.12347641486373544;
  if (node === 218) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree93(features, 437);
    } else if (val <= 56.50000000000001) {
      return calculateTree93(features, 437);
    } else {
      return calculateTree93(features, 438);
    }
  }
  if (node === 108) {
    const val = features['is_preference_pets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree93(features, 217);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree93(features, 217);
    } else {
      return calculateTree93(features, 218);
    }
  }
  if (node === 53) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree93(features, 107);
    } else if (val <= 660.0000000000001) {
      return calculateTree93(features, 107);
    } else {
      return calculateTree93(features, 108);
    }
  }
  if (node === 439) return -0.1870534559368872;
  if (node === 881) return -0.14204221572709458;
  if (node === 882) return 0.13915152508193213;
  if (node === 440) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree93(features, 881);
    } else if (val <= 673.0000000000001) {
      return calculateTree93(features, 881);
    } else {
      return calculateTree93(features, 882);
    }
  }
  if (node === 219) {
    const val = features['is_preference_kids'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree93(features, 439);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree93(features, 439);
    } else {
      return calculateTree93(features, 440);
    }
  }
  if (node === 220) return -0.42679329507159;
  if (node === 109) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree93(features, 219);
    } else if (val <= 758.5000000000001) {
      return calculateTree93(features, 219);
    } else {
      return calculateTree93(features, 220);
    }
  }
  if (node === 443) return -0.05967885760569651;
  if (node === 889) return 0.4817186206242961;
  if (node === 890) return 0.16411751714204345;
  if (node === 444) {
    const val = features['is_preference_grocery'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree93(features, 889);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree93(features, 889);
    } else {
      return calculateTree93(features, 890);
    }
  }
  if (node === 221) {
    const val = features['is_preference_kitchen'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree93(features, 443);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree93(features, 443);
    } else {
      return calculateTree93(features, 444);
    }
  }
  if (node === 445) return -0.256311601526588;
  if (node === 446) return 0.011892744423608674;
  if (node === 222) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree93(features, 445);
    } else if (val <= 804.0000000000001) {
      return calculateTree93(features, 445);
    } else {
      return calculateTree93(features, 446);
    }
  }
  if (node === 110) {
    const val = features['is_preference_kids'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree93(features, 221);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree93(features, 221);
    } else {
      return calculateTree93(features, 222);
    }
  }
  if (node === 54) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree93(features, 109);
    } else if (val <= 763.5000000000001) {
      return calculateTree93(features, 109);
    } else {
      return calculateTree93(features, 110);
    }
  }
  if (node === 26) {
    const val = features['is_preference_women_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree93(features, 53);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree93(features, 53);
    } else {
      return calculateTree93(features, 54);
    }
  }
  if (node === 12) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree93(features, 25);
    } else if (val <= 29.500000000000004) {
      return calculateTree93(features, 25);
    } else {
      return calculateTree93(features, 26);
    }
  }
  if (node === 5) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree93(features, 11);
    } else if (val <= 18.500000000000004) {
      return calculateTree93(features, 11);
    } else {
      return calculateTree93(features, 12);
    }
  }
  if (node === 6) return -0.0012744692818081559;
  if (node === 2) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree93(features, 5);
    } else if (val <= 880.0000000000001) {
      return calculateTree93(features, 5);
    } else {
      return calculateTree93(features, 6);
    }
  }
  if (node === 0) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree93(features, 1);
    } else if (val <= 626.0000000000001) {
      return calculateTree93(features, 1);
    } else {
      return calculateTree93(features, 2);
    }
  }
}
function calculateTree94(features, node = 0) {
  if (node === 63) return -5.695433900660558e-5;
  if (node === 1039) return -0.1499929250247863;
  if (node === 1040) return 0.014747068611677612;
  if (node === 519) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree94(features, 1039);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree94(features, 1039);
    } else {
      return calculateTree94(features, 1040);
    }
  }
  if (node === 520) return -0.3109117075644457;
  if (node === 259) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree94(features, 519);
    } else if (val <= 643.5000000000001) {
      return calculateTree94(features, 519);
    } else {
      return calculateTree94(features, 520);
    }
  }
  if (node === 521) return -0.3276304330490015;
  if (node === 522) return 0.07161900405750536;
  if (node === 260) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree94(features, 521);
    } else if (val <= 643.5000000000001) {
      return calculateTree94(features, 521);
    } else {
      return calculateTree94(features, 522);
    }
  }
  if (node === 129) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree94(features, 259);
    } else if (val <= 276.00000000000006) {
      return calculateTree94(features, 259);
    } else {
      return calculateTree94(features, 260);
    }
  }
  if (node === 261) return -0.30961255021666023;
  if (node === 262) return 0.04603015465272612;
  if (node === 130) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree94(features, 261);
    } else if (val <= 606.5000000000001) {
      return calculateTree94(features, 261);
    } else {
      return calculateTree94(features, 262);
    }
  }
  if (node === 64) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree94(features, 129);
    } else if (val <= 435.00000000000006) {
      return calculateTree94(features, 129);
    } else {
      return calculateTree94(features, 130);
    }
  }
  if (node === 31) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree94(features, 63);
    } else if (val <= 602.5000000000001) {
      return calculateTree94(features, 63);
    } else {
      return calculateTree94(features, 64);
    }
  }
  if (node === 65) return -0.3278441885069517;
  if (node === 133) return 0.15629493494888066;
  if (node === 134) return 0.01550025879064532;
  if (node === 66) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree94(features, 133);
    } else if (val <= 715.5000000000001) {
      return calculateTree94(features, 133);
    } else {
      return calculateTree94(features, 134);
    }
  }
  if (node === 32) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree94(features, 65);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree94(features, 65);
    } else {
      return calculateTree94(features, 66);
    }
  }
  if (node === 15) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree94(features, 31);
    } else if (val <= 647.0000000000001) {
      return calculateTree94(features, 31);
    } else {
      return calculateTree94(features, 32);
    }
  }
  if (node === 33) return 0.011318535946806272;
  if (node === 69) return -0.3980791496289271;
  if (node === 141) return -0.24079029157007126;
  if (node === 285) return 0.2316738978833058;
  if (node === 286) return -0.139177820227593;
  if (node === 142) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree94(features, 285);
    } else if (val <= 668.0000000000001) {
      return calculateTree94(features, 285);
    } else {
      return calculateTree94(features, 286);
    }
  }
  if (node === 70) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree94(features, 141);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree94(features, 141);
    } else {
      return calculateTree94(features, 142);
    }
  }
  if (node === 34) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree94(features, 69);
    } else if (val <= 281.50000000000006) {
      return calculateTree94(features, 69);
    } else {
      return calculateTree94(features, 70);
    }
  }
  if (node === 16) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree94(features, 33);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree94(features, 33);
    } else {
      return calculateTree94(features, 34);
    }
  }
  if (node === 7) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree94(features, 15);
    } else if (val <= 649.5000000000001) {
      return calculateTree94(features, 15);
    } else {
      return calculateTree94(features, 16);
    }
  }
  if (node === 35) return -0.2915649424559936;
  if (node === 36) return 0.11447616621063195;
  if (node === 17) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree94(features, 35);
    } else if (val <= 861.5000000000001) {
      return calculateTree94(features, 35);
    } else {
      return calculateTree94(features, 36);
    }
  }
  if (node === 151) return 0.12918618754139946;
  if (node === 152) return -0.5212379599612561;
  if (node === 75) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree94(features, 151);
    } else if (val <= 69.50000000000001) {
      return calculateTree94(features, 151);
    } else {
      return calculateTree94(features, 152);
    }
  }
  if (node === 153) return 0.14260615069139917;
  if (node === 154) return 0.6084230800772269;
  if (node === 76) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree94(features, 153);
    } else if (val <= 542.0000000000001) {
      return calculateTree94(features, 153);
    } else {
      return calculateTree94(features, 154);
    }
  }
  if (node === 37) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree94(features, 75);
    } else if (val <= 422.00000000000006) {
      return calculateTree94(features, 75);
    } else {
      return calculateTree94(features, 76);
    }
  }
  if (node === 38) return -0.32919597942739853;
  if (node === 18) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree94(features, 37);
    } else if (val <= 861.5000000000001) {
      return calculateTree94(features, 37);
    } else {
      return calculateTree94(features, 38);
    }
  }
  if (node === 8) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree94(features, 17);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree94(features, 17);
    } else {
      return calculateTree94(features, 18);
    }
  }
  if (node === 3) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree94(features, 7);
    } else if (val <= 657.5000000000001) {
      return calculateTree94(features, 7);
    } else {
      return calculateTree94(features, 8);
    }
  }
  if (node === 19) return -0.07527358077399118;
  if (node === 20) return -0.34768873058612293;
  if (node === 9) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree94(features, 19);
    } else if (val <= 107.00000000000001) {
      return calculateTree94(features, 19);
    } else {
      return calculateTree94(features, 20);
    }
  }
  if (node === 21) return 0.025428731914636127;
  if (node === 367) return -0.09088538626945432;
  if (node === 368) return -0.29596637366646733;
  if (node === 183) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree94(features, 367);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree94(features, 367);
    } else {
      return calculateTree94(features, 368);
    }
  }
  if (node === 184) return -0.022535051001155045;
  if (node === 91) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree94(features, 183);
    } else if (val <= 693.0000000000001) {
      return calculateTree94(features, 183);
    } else {
      return calculateTree94(features, 184);
    }
  }
  if (node === 92) return -0.31608656595680046;
  if (node === 45) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree94(features, 91);
    } else if (val <= 498.50000000000006) {
      return calculateTree94(features, 91);
    } else {
      return calculateTree94(features, 92);
    }
  }
  if (node === 46) return -0.0007165282274154706;
  if (node === 22) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree94(features, 45);
    } else if (val <= 584.5000000000001) {
      return calculateTree94(features, 45);
    } else {
      return calculateTree94(features, 46);
    }
  }
  if (node === 10) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree94(features, 21);
    } else if (val <= 414.50000000000006) {
      return calculateTree94(features, 21);
    } else {
      return calculateTree94(features, 22);
    }
  }
  if (node === 4) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree94(features, 9);
    } else if (val <= 125.50000000000001) {
      return calculateTree94(features, 9);
    } else {
      return calculateTree94(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree94(features, 3);
    } else if (val <= 667.5000000000001) {
      return calculateTree94(features, 3);
    } else {
      return calculateTree94(features, 4);
    }
  }
  if (node === 2) return 0.003001194551212022;
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree94(features, 1);
    } else if (val <= 745.5000000000001) {
      return calculateTree94(features, 1);
    } else {
      return calculateTree94(features, 2);
    }
  }
}
function calculateTree95(features, node = 0) {
  if (node === 1023) return 0.007923605568479455;
  if (node === 1024) return 0.15093650486570379;
  if (node === 511) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree95(features, 1023);
    } else if (val <= 210.00000000000003) {
      return calculateTree95(features, 1023);
    } else {
      return calculateTree95(features, 1024);
    }
  }
  if (node === 1025) return -0.35659897264807694;
  if (node === 1026) return 0.11230921858989738;
  if (node === 512) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree95(features, 1025);
    } else if (val <= 240.00000000000003) {
      return calculateTree95(features, 1025);
    } else {
      return calculateTree95(features, 1026);
    }
  }
  if (node === 255) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree95(features, 511);
    } else if (val <= 225.50000000000003) {
      return calculateTree95(features, 511);
    } else {
      return calculateTree95(features, 512);
    }
  }
  if (node === 256) return 0.1625485591648956;
  if (node === 127) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree95(features, 255);
    } else if (val <= 350.50000000000006) {
      return calculateTree95(features, 255);
    } else {
      return calculateTree95(features, 256);
    }
  }
  if (node === 257) return -0.30711419194395495;
  if (node === 258) return 0.18685980786333858;
  if (node === 128) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree95(features, 257);
    } else if (val <= 427.50000000000006) {
      return calculateTree95(features, 257);
    } else {
      return calculateTree95(features, 258);
    }
  }
  if (node === 63) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree95(features, 127);
    } else if (val <= 391.50000000000006) {
      return calculateTree95(features, 127);
    } else {
      return calculateTree95(features, 128);
    }
  }
  if (node === 64) return -0.3200220995766907;
  if (node === 31) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree95(features, 63);
    } else if (val <= 253.00000000000003) {
      return calculateTree95(features, 63);
    } else {
      return calculateTree95(features, 64);
    }
  }
  if (node === 1055) return -0.3414593850059598;
  if (node === 1056) return 0.038039894101973684;
  if (node === 527) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree95(features, 1055);
    } else if (val <= 186.50000000000003) {
      return calculateTree95(features, 1055);
    } else {
      return calculateTree95(features, 1056);
    }
  }
  if (node === 528) return -0.036971365474222105;
  if (node === 263) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree95(features, 527);
    } else if (val <= 34.00000000000001) {
      return calculateTree95(features, 527);
    } else {
      return calculateTree95(features, 528);
    }
  }
  if (node === 264) return -0.37506023938402194;
  if (node === 131) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree95(features, 263);
    } else if (val <= 240.00000000000003) {
      return calculateTree95(features, 263);
    } else {
      return calculateTree95(features, 264);
    }
  }
  if (node === 132) return 0.0927933462718112;
  if (node === 65) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree95(features, 131);
    } else if (val <= 270.00000000000006) {
      return calculateTree95(features, 131);
    } else {
      return calculateTree95(features, 132);
    }
  }
  if (node === 133) return -0.30274206388241015;
  if (node === 269) return -0.3231306342815252;
  if (node === 541) return 0.1446922589278495;
  if (node === 1085) return 0.056396242841576;
  if (node === 1086) return -0.14257459804336006;
  if (node === 542) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree95(features, 1085);
    } else if (val <= 331.50000000000006) {
      return calculateTree95(features, 1085);
    } else {
      return calculateTree95(features, 1086);
    }
  }
  if (node === 270) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree95(features, 541);
    } else if (val <= 107.00000000000001) {
      return calculateTree95(features, 541);
    } else {
      return calculateTree95(features, 542);
    }
  }
  if (node === 134) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree95(features, 269);
    } else if (val <= 89.00000000000001) {
      return calculateTree95(features, 269);
    } else {
      return calculateTree95(features, 270);
    }
  }
  if (node === 66) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree95(features, 133);
    } else if (val <= 312.50000000000006) {
      return calculateTree95(features, 133);
    } else {
      return calculateTree95(features, 134);
    }
  }
  if (node === 32) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree95(features, 65);
    } else if (val <= 217.50000000000003) {
      return calculateTree95(features, 65);
    } else {
      return calculateTree95(features, 66);
    }
  }
  if (node === 15) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree95(features, 31);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree95(features, 31);
    } else {
      return calculateTree95(features, 32);
    }
  }
  if (node === 16) return 0.001915668967881561;
  if (node === 7) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree95(features, 15);
    } else if (val <= 276.00000000000006) {
      return calculateTree95(features, 15);
    } else {
      return calculateTree95(features, 16);
    }
  }
  if (node === 35) return -0.33086285140378713;
  if (node === 147) return 0.12958113691745077;
  if (node === 148) return 0.3065660976707223;
  if (node === 73) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree95(features, 147);
    } else if (val <= 25.500000000000004) {
      return calculateTree95(features, 147);
    } else {
      return calculateTree95(features, 148);
    }
  }
  if (node === 149) return -0.25703871995579647;
  if (node === 150) return 0.132833051433093;
  if (node === 74) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree95(features, 149);
    } else if (val <= 236.00000000000003) {
      return calculateTree95(features, 149);
    } else {
      return calculateTree95(features, 150);
    }
  }
  if (node === 36) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree95(features, 73);
    } else if (val <= 12.500000000000002) {
      return calculateTree95(features, 73);
    } else {
      return calculateTree95(features, 74);
    }
  }
  if (node === 17) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree95(features, 35);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree95(features, 35);
    } else {
      return calculateTree95(features, 36);
    }
  }
  if (node === 37) return -0.22028417194781422;
  if (node === 155) return 0.07116367870458508;
  if (node === 156) return -0.3567340702443506;
  if (node === 77) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree95(features, 155);
    } else if (val <= 957.5000000000001) {
      return calculateTree95(features, 155);
    } else {
      return calculateTree95(features, 156);
    }
  }
  if (node === 78) return -0.2608070207708688;
  if (node === 38) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree95(features, 77);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree95(features, 77);
    } else {
      return calculateTree95(features, 78);
    }
  }
  if (node === 18) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree95(features, 37);
    } else if (val <= 461.50000000000006) {
      return calculateTree95(features, 37);
    } else {
      return calculateTree95(features, 38);
    }
  }
  if (node === 8) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree95(features, 17);
    } else if (val <= 377.50000000000006) {
      return calculateTree95(features, 17);
    } else {
      return calculateTree95(features, 18);
    }
  }
  if (node === 3) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree95(features, 7);
    } else if (val <= 439.50000000000006) {
      return calculateTree95(features, 7);
    } else {
      return calculateTree95(features, 8);
    }
  }
  if (node === 9) return -0.29741966224978056;
  if (node === 21) return -0.3335258534926732;
  if (node === 22) return 0.799614325178255;
  if (node === 10) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree95(features, 21);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree95(features, 21);
    } else {
      return calculateTree95(features, 22);
    }
  }
  if (node === 4) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree95(features, 9);
    } else if (val <= 491.50000000000006) {
      return calculateTree95(features, 9);
    } else {
      return calculateTree95(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree95(features, 3);
    } else if (val <= 464.50000000000006) {
      return calculateTree95(features, 3);
    } else {
      return calculateTree95(features, 4);
    }
  }
  if (node === 2) return 0.0020918817386301673;
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree95(features, 1);
    } else if (val <= 470.50000000000006) {
      return calculateTree95(features, 1);
    } else {
      return calculateTree95(features, 2);
    }
  }
}
function calculateTree96(features, node = 0) {
  if (node === 3) return -0.31108930624493664;
  if (node === 4) return -0.027724180506517267;
  if (node === 1) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree96(features, 3);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree96(features, 3);
    } else {
      return calculateTree96(features, 4);
    }
  }
  if (node === 767) return -0.06420843073488139;
  if (node === 768) return 0.06407621967508931;
  if (node === 383) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree96(features, 767);
    } else if (val <= 649.5000000000001) {
      return calculateTree96(features, 767);
    } else {
      return calculateTree96(features, 768);
    }
  }
  if (node === 384) return -0.370133560353753;
  if (node === 191) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree96(features, 383);
    } else if (val <= 658.5000000000001) {
      return calculateTree96(features, 383);
    } else {
      return calculateTree96(features, 384);
    }
  }
  if (node === 192) return 0.017951492897154565;
  if (node === 95) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree96(features, 191);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree96(features, 191);
    } else {
      return calculateTree96(features, 192);
    }
  }
  if (node === 96) return -0.0919530822175859;
  if (node === 47) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree96(features, 95);
    } else if (val <= 715.5000000000001) {
      return calculateTree96(features, 95);
    } else {
      return calculateTree96(features, 96);
    }
  }
  if (node === 48) return 0.0013594717839781664;
  if (node === 23) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree96(features, 47);
    } else if (val <= 39.50000000000001) {
      return calculateTree96(features, 47);
    } else {
      return calculateTree96(features, 48);
    }
  }
  if (node === 199) return -0.3155796600512318;
  if (node === 401) return 0.4795431036640742;
  if (node === 402) return 0.1279423437945998;
  if (node === 200) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree96(features, 401);
    } else if (val <= 43.50000000000001) {
      return calculateTree96(features, 401);
    } else {
      return calculateTree96(features, 402);
    }
  }
  if (node === 99) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree96(features, 199);
    } else if (val <= 305.00000000000006) {
      return calculateTree96(features, 199);
    } else {
      return calculateTree96(features, 200);
    }
  }
  if (node === 201) return -0.38294405770866063;
  if (node === 405) return 0.6488143535727166;
  if (node === 406) return -0.3143403571513761;
  if (node === 202) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree96(features, 405);
    } else if (val <= 781.0000000000001) {
      return calculateTree96(features, 405);
    } else {
      return calculateTree96(features, 406);
    }
  }
  if (node === 100) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree96(features, 201);
    } else if (val <= 705.5000000000001) {
      return calculateTree96(features, 201);
    } else {
      return calculateTree96(features, 202);
    }
  }
  if (node === 49) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree96(features, 99);
    } else if (val <= 389.00000000000006) {
      return calculateTree96(features, 99);
    } else {
      return calculateTree96(features, 100);
    }
  }
  if (node === 101) return -0.41544500559967557;
  if (node === 205) return 0.03668137783446391;
  if (node === 206) return -0.2913298328929673;
  if (node === 102) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree96(features, 205);
    } else if (val <= 26.500000000000004) {
      return calculateTree96(features, 205);
    } else {
      return calculateTree96(features, 206);
    }
  }
  if (node === 50) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree96(features, 101);
    } else if (val <= 781.0000000000001) {
      return calculateTree96(features, 101);
    } else {
      return calculateTree96(features, 102);
    }
  }
  if (node === 24) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree96(features, 49);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree96(features, 49);
    } else {
      return calculateTree96(features, 50);
    }
  }
  if (node === 11) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree96(features, 23);
    } else if (val <= 882.5000000000001) {
      return calculateTree96(features, 23);
    } else {
      return calculateTree96(features, 24);
    }
  }
  if (node === 51) return -0.32173518017894803;
  if (node === 211) return 0.13722252960404852;
  if (node === 212) return 0.4416095556179602;
  if (node === 105) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree96(features, 211);
    } else if (val <= 734.0000000000001) {
      return calculateTree96(features, 211);
    } else {
      return calculateTree96(features, 212);
    }
  }
  if (node === 106) return 0.08507373238630515;
  if (node === 52) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree96(features, 105);
    } else if (val <= 863.0000000000001) {
      return calculateTree96(features, 105);
    } else {
      return calculateTree96(features, 106);
    }
  }
  if (node === 25) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree96(features, 51);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree96(features, 51);
    } else {
      return calculateTree96(features, 52);
    }
  }
  if (node === 53) return -0.2718931985961658;
  if (node === 109) return 0.0029610554580388926;
  if (node === 887) return 0.23147617489750277;
  if (node === 888) return -0.07252023353897263;
  if (node === 443) {
    const val = features['is_preference_beauty'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree96(features, 887);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree96(features, 887);
    } else {
      return calculateTree96(features, 888);
    }
  }
  if (node === 444) return 0.007150158236239589;
  if (node === 221) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree96(features, 443);
    } else if (val <= 9.500000000000002) {
      return calculateTree96(features, 443);
    } else {
      return calculateTree96(features, 444);
    }
  }
  if (node === 1783) return -0.09888476938738416;
  if (node === 1784) return 0.09728317608350813;
  if (node === 891) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree96(features, 1783);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree96(features, 1783);
    } else {
      return calculateTree96(features, 1784);
    }
  }
  if (node === 1785) return 0.10496341151048823;
  if (node === 1786) return -0.10274740112060801;
  if (node === 892) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree96(features, 1785);
    } else if (val <= 835.5000000000001) {
      return calculateTree96(features, 1785);
    } else {
      return calculateTree96(features, 1786);
    }
  }
  if (node === 445) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree96(features, 891);
    } else if (val <= 467.50000000000006) {
      return calculateTree96(features, 891);
    } else {
      return calculateTree96(features, 892);
    }
  }
  if (node === 446) return -0.029929784880271232;
  if (node === 222) {
    const val = features['is_preference_kitchen'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree96(features, 445);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree96(features, 445);
    } else {
      return calculateTree96(features, 446);
    }
  }
  if (node === 110) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree96(features, 221);
    } else if (val <= 39.50000000000001) {
      return calculateTree96(features, 221);
    } else {
      return calculateTree96(features, 222);
    }
  }
  if (node === 54) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree96(features, 109);
    } else if (val <= 41.50000000000001) {
      return calculateTree96(features, 109);
    } else {
      return calculateTree96(features, 110);
    }
  }
  if (node === 26) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree96(features, 53);
    } else if (val <= 899.5000000000001) {
      return calculateTree96(features, 53);
    } else {
      return calculateTree96(features, 54);
    }
  }
  if (node === 12) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree96(features, 25);
    } else if (val <= 896.5000000000001) {
      return calculateTree96(features, 25);
    } else {
      return calculateTree96(features, 26);
    }
  }
  if (node === 5) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree96(features, 11);
    } else if (val <= 890.5000000000001) {
      return calculateTree96(features, 11);
    } else {
      return calculateTree96(features, 12);
    }
  }
  if (node === 6) return -0.01194536051161395;
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree96(features, 5);
    } else if (val <= 966.0000000000001) {
      return calculateTree96(features, 5);
    } else {
      return calculateTree96(features, 6);
    }
  }
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree96(features, 1);
    } else if (val <= 3.0000000000000004) {
      return calculateTree96(features, 1);
    } else {
      return calculateTree96(features, 2);
    }
  }
}
function calculateTree97(features, node = 0) {
  if (node === 3) return -0.00018034072434180883;
  if (node === 39) return -0.044846596153101444;
  if (node === 81) return 0.14442317764624102;
  if (node === 82) return -0.3529556716002465;
  if (node === 40) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree97(features, 81);
    } else if (val <= 15.500000000000002) {
      return calculateTree97(features, 81);
    } else {
      return calculateTree97(features, 82);
    }
  }
  if (node === 19) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree97(features, 39);
    } else if (val <= 222.50000000000003) {
      return calculateTree97(features, 39);
    } else {
      return calculateTree97(features, 40);
    }
  }
  if (node === 41) return 0.14991522974496777;
  if (node === 42) return -0.35009826232830216;
  if (node === 20) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree97(features, 41);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree97(features, 41);
    } else {
      return calculateTree97(features, 42);
    }
  }
  if (node === 9) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree97(features, 19);
    } else if (val <= 285.00000000000006) {
      return calculateTree97(features, 19);
    } else {
      return calculateTree97(features, 20);
    }
  }
  if (node === 175) return 0.5310399707454546;
  if (node === 176) return 0.15498428294128613;
  if (node === 87) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree97(features, 175);
    } else if (val <= 682.5000000000001) {
      return calculateTree97(features, 175);
    } else {
      return calculateTree97(features, 176);
    }
  }
  if (node === 177) return -0.33056825652506977;
  if (node === 357) return 0.31760801477002515;
  if (node === 358) return -0.12256230052296223;
  if (node === 178) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree97(features, 357);
    } else if (val <= 690.5000000000001) {
      return calculateTree97(features, 357);
    } else {
      return calculateTree97(features, 358);
    }
  }
  if (node === 88) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree97(features, 177);
    } else if (val <= 740.5000000000001) {
      return calculateTree97(features, 177);
    } else {
      return calculateTree97(features, 178);
    }
  }
  if (node === 43) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree97(features, 87);
    } else if (val <= 551.5000000000001) {
      return calculateTree97(features, 87);
    } else {
      return calculateTree97(features, 88);
    }
  }
  if (node === 89) return -0.37451812108971355;
  if (node === 363) return 0.09857571447468504;
  if (node === 364) return -0.3475703258271715;
  if (node === 181) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree97(features, 363);
    } else if (val <= 740.5000000000001) {
      return calculateTree97(features, 363);
    } else {
      return calculateTree97(features, 364);
    }
  }
  if (node === 182) return 0.17514401919683065;
  if (node === 90) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree97(features, 181);
    } else if (val <= 774.0000000000001) {
      return calculateTree97(features, 181);
    } else {
      return calculateTree97(features, 182);
    }
  }
  if (node === 44) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree97(features, 89);
    } else if (val <= 551.5000000000001) {
      return calculateTree97(features, 89);
    } else {
      return calculateTree97(features, 90);
    }
  }
  if (node === 21) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree97(features, 43);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree97(features, 43);
    } else {
      return calculateTree97(features, 44);
    }
  }
  if (node === 91) return -0.31333944780782474;
  if (node === 92) return 0.21531897770664202;
  if (node === 45) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree97(features, 91);
    } else if (val <= 882.5000000000001) {
      return calculateTree97(features, 91);
    } else {
      return calculateTree97(features, 92);
    }
  }
  if (node === 187) return -0.008557987475242551;
  if (node === 188) return 0.2016605359278843;
  if (node === 93) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree97(features, 187);
    } else if (val <= 25.500000000000004) {
      return calculateTree97(features, 187);
    } else {
      return calculateTree97(features, 188);
    }
  }
  if (node === 94) return -0.38965795131648445;
  if (node === 46) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree97(features, 93);
    } else if (val <= 882.5000000000001) {
      return calculateTree97(features, 93);
    } else {
      return calculateTree97(features, 94);
    }
  }
  if (node === 22) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree97(features, 45);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree97(features, 45);
    } else {
      return calculateTree97(features, 46);
    }
  }
  if (node === 10) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree97(features, 21);
    } else if (val <= 852.5000000000001) {
      return calculateTree97(features, 21);
    } else {
      return calculateTree97(features, 22);
    }
  }
  if (node === 4) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree97(features, 9);
    } else if (val <= 314.50000000000006) {
      return calculateTree97(features, 9);
    } else {
      return calculateTree97(features, 10);
    }
  }
  if (node === 1) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree97(features, 3);
    } else if (val <= 678.5000000000001) {
      return calculateTree97(features, 3);
    } else {
      return calculateTree97(features, 4);
    }
  }
  if (node === 95) return -0.26613236230201737;
  if (node === 96) return 0.18671659856667075;
  if (node === 47) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree97(features, 95);
    } else if (val <= 141.50000000000003) {
      return calculateTree97(features, 95);
    } else {
      return calculateTree97(features, 96);
    }
  }
  if (node === 48) return -0.34442046257778103;
  if (node === 23) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree97(features, 47);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree97(features, 47);
    } else {
      return calculateTree97(features, 48);
    }
  }
  if (node === 49) return -0.41354316177974865;
  if (node === 203) return 0.04125142329125029;
  if (node === 204) return 0.20281176809553958;
  if (node === 101) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree97(features, 203);
    } else if (val <= 41.50000000000001) {
      return calculateTree97(features, 203);
    } else {
      return calculateTree97(features, 204);
    }
  }
  if (node === 205) return 0.22094670485014453;
  if (node === 206) return -0.08935513475661938;
  if (node === 102) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree97(features, 205);
    } else if (val <= 9.500000000000002) {
      return calculateTree97(features, 205);
    } else {
      return calculateTree97(features, 206);
    }
  }
  if (node === 50) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree97(features, 101);
    } else if (val <= 709.5000000000001) {
      return calculateTree97(features, 101);
    } else {
      return calculateTree97(features, 102);
    }
  }
  if (node === 24) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree97(features, 49);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree97(features, 49);
    } else {
      return calculateTree97(features, 50);
    }
  }
  if (node === 11) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree97(features, 23);
    } else if (val <= 207.00000000000003) {
      return calculateTree97(features, 23);
    } else {
      return calculateTree97(features, 24);
    }
  }
  if (node === 12) return -0.2463884247076606;
  if (node === 5) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree97(features, 11);
    } else if (val <= 461.50000000000006) {
      return calculateTree97(features, 11);
    } else {
      return calculateTree97(features, 12);
    }
  }
  if (node === 27) return -0.005008686191688463;
  if (node === 28) return -0.03535262470608405;
  if (node === 13) {
    const val = features['is_preference_pets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree97(features, 27);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree97(features, 27);
    } else {
      return calculateTree97(features, 28);
    }
  }
  if (node === 14) return 0.009423443679687093;
  if (node === 6) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree97(features, 13);
    } else if (val <= 3.5000000000000004) {
      return calculateTree97(features, 13);
    } else {
      return calculateTree97(features, 14);
    }
  }
  if (node === 2) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree97(features, 5);
    } else if (val <= 712.5000000000001) {
      return calculateTree97(features, 5);
    } else {
      return calculateTree97(features, 6);
    }
  }
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree97(features, 1);
    } else if (val <= 708.5000000000001) {
      return calculateTree97(features, 1);
    } else {
      return calculateTree97(features, 2);
    }
  }
}
function calculateTree98(features, node = 0) {
  if (node === 15) return -0.308237495949971;
  if (node === 33) return -0.039436039192190186;
  if (node === 69) return 0.27470663325350486;
  if (node === 70) return -0.0005418448991024715;
  if (node === 34) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree98(features, 69);
    } else if (val <= 22.500000000000004) {
      return calculateTree98(features, 69);
    } else {
      return calculateTree98(features, 70);
    }
  }
  if (node === 16) {
    const val = features['is_preference_women_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree98(features, 33);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree98(features, 33);
    } else {
      return calculateTree98(features, 34);
    }
  }
  if (node === 7) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree98(features, 15);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree98(features, 15);
    } else {
      return calculateTree98(features, 16);
    }
  }
  if (node === 8) return -0.0008222451566998957;
  if (node === 3) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree98(features, 7);
    } else if (val <= 3.0000000000000004) {
      return calculateTree98(features, 7);
    } else {
      return calculateTree98(features, 8);
    }
  }
  if (node === 4) return -0.31851248997188963;
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree98(features, 3);
    } else if (val <= 992.5000000000001) {
      return calculateTree98(features, 3);
    } else {
      return calculateTree98(features, 4);
    }
  }
  if (node === 11) return -0.3314990203793576;
  if (node === 51) return 0.3325960387541875;
  if (node === 423) return -0.2669139373823268;
  if (node === 424) return -0.048655458005907204;
  if (node === 211) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree98(features, 423);
    } else if (val <= 39.50000000000001) {
      return calculateTree98(features, 423);
    } else {
      return calculateTree98(features, 424);
    }
  }
  if (node === 212) return 0.12923074264067547;
  if (node === 105) {
    const val = features['is_preference_beauty'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree98(features, 211);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree98(features, 211);
    } else {
      return calculateTree98(features, 212);
    }
  }
  if (node === 213) return 0.21462325448114744;
  if (node === 214) return 0.03459597729813177;
  if (node === 106) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree98(features, 213);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree98(features, 213);
    } else {
      return calculateTree98(features, 214);
    }
  }
  if (node === 52) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree98(features, 105);
    } else if (val <= 4.500000000000001) {
      return calculateTree98(features, 105);
    } else {
      return calculateTree98(features, 106);
    }
  }
  if (node === 25) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree98(features, 51);
    } else if (val <= 28.500000000000004) {
      return calculateTree98(features, 51);
    } else {
      return calculateTree98(features, 52);
    }
  }
  if (node === 215) return 0.04652749363099325;
  if (node === 433) return -0.15055460580544633;
  if (node === 434) return 0.02623776678960779;
  if (node === 216) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree98(features, 433);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree98(features, 433);
    } else {
      return calculateTree98(features, 434);
    }
  }
  if (node === 107) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree98(features, 215);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree98(features, 215);
    } else {
      return calculateTree98(features, 216);
    }
  }
  if (node === 435) return 0.11839123145243657;
  if (node === 436) return -0.19135443899158402;
  if (node === 217) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree98(features, 435);
    } else if (val <= 19.500000000000004) {
      return calculateTree98(features, 435);
    } else {
      return calculateTree98(features, 436);
    }
  }
  if (node === 218) return 0.2556793438351279;
  if (node === 108) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree98(features, 217);
    } else if (val <= 1.5000000000000002) {
      return calculateTree98(features, 217);
    } else {
      return calculateTree98(features, 218);
    }
  }
  if (node === 53) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree98(features, 107);
    } else if (val <= 13.500000000000002) {
      return calculateTree98(features, 107);
    } else {
      return calculateTree98(features, 108);
    }
  }
  if (node === 439) return -0.2295400102827288;
  if (node === 1763) return -0.031155864817624904;
  if (node === 1764) return -0.17176101093799803;
  if (node === 881) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree98(features, 1763);
    } else if (val <= 46.50000000000001) {
      return calculateTree98(features, 1763);
    } else {
      return calculateTree98(features, 1764);
    }
  }
  if (node === 882) return 0.15274954899583448;
  if (node === 440) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree98(features, 881);
    } else if (val <= 56.50000000000001) {
      return calculateTree98(features, 881);
    } else {
      return calculateTree98(features, 882);
    }
  }
  if (node === 219) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree98(features, 439);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree98(features, 439);
    } else {
      return calculateTree98(features, 440);
    }
  }
  if (node === 441) return 0.35380849087720406;
  if (node === 442) return -0.043261714912695155;
  if (node === 220) {
    const val = features['is_preference_beauty'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree98(features, 441);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree98(features, 441);
    } else {
      return calculateTree98(features, 442);
    }
  }
  if (node === 109) {
    const val = features['is_preference_household'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree98(features, 219);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree98(features, 219);
    } else {
      return calculateTree98(features, 220);
    }
  }
  if (node === 221) return -0.09240871180497758;
  if (node === 445) return 0.06891508933732644;
  if (node === 446) return 0.33889665106766637;
  if (node === 222) {
    const val = features['is_preference_women_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree98(features, 445);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree98(features, 445);
    } else {
      return calculateTree98(features, 446);
    }
  }
  if (node === 110) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree98(features, 221);
    } else if (val <= 34.50000000000001) {
      return calculateTree98(features, 221);
    } else {
      return calculateTree98(features, 222);
    }
  }
  if (node === 54) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree98(features, 109);
    } else if (val <= 5.500000000000001) {
      return calculateTree98(features, 109);
    } else {
      return calculateTree98(features, 110);
    }
  }
  if (node === 26) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree98(features, 53);
    } else if (val <= 26.500000000000004) {
      return calculateTree98(features, 53);
    } else {
      return calculateTree98(features, 54);
    }
  }
  if (node === 12) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree98(features, 25);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree98(features, 25);
    } else {
      return calculateTree98(features, 26);
    }
  }
  if (node === 5) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree98(features, 11);
    } else if (val <= 283.50000000000006) {
      return calculateTree98(features, 11);
    } else {
      return calculateTree98(features, 12);
    }
  }
  if (node === 55) return 0.18829517781583166;
  if (node === 56) return 0.61411236334109;
  if (node === 27) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree98(features, 55);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree98(features, 55);
    } else {
      return calculateTree98(features, 56);
    }
  }
  if (node === 28) return -0.060138124166882134;
  if (node === 13) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree98(features, 27);
    } else if (val <= 26.500000000000004) {
      return calculateTree98(features, 27);
    } else {
      return calculateTree98(features, 28);
    }
  }
  if (node === 14) return -0.31794235219600264;
  if (node === 6) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree98(features, 13);
    } else if (val <= 181.50000000000003) {
      return calculateTree98(features, 13);
    } else {
      return calculateTree98(features, 14);
    }
  }
  if (node === 2) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree98(features, 5);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree98(features, 5);
    } else {
      return calculateTree98(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree98(features, 1);
    } else if (val <= 993.5000000000001) {
      return calculateTree98(features, 1);
    } else {
      return calculateTree98(features, 2);
    }
  }
}
function calculateTree99(features, node = 0) {
  if (node === 3) return -0.0006084483934775251;
  if (node === 79) return -0.018221537794883558;
  if (node === 80) return -0.24897557258502737;
  if (node === 39) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree99(features, 79);
    } else if (val <= 132.50000000000003) {
      return calculateTree99(features, 79);
    } else {
      return calculateTree99(features, 80);
    }
  }
  if (node === 163) return -0.0783073168648728;
  if (node === 164) return 0.2157487217802399;
  if (node === 81) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree99(features, 163);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree99(features, 163);
    } else {
      return calculateTree99(features, 164);
    }
  }
  if (node === 165) return -0.29718444692859763;
  if (node === 1335) return 0.03772358844152848;
  if (node === 1336) return -0.25813905220525074;
  if (node === 667) {
    const val = features['is_preference_household'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree99(features, 1335);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree99(features, 1335);
    } else {
      return calculateTree99(features, 1336);
    }
  }
  if (node === 668) return 0.1418968374487805;
  if (node === 333) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree99(features, 667);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree99(features, 667);
    } else {
      return calculateTree99(features, 668);
    }
  }
  if (node === 334) return 0.008076236109327853;
  if (node === 166) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree99(features, 333);
    } else if (val <= 19.500000000000004) {
      return calculateTree99(features, 333);
    } else {
      return calculateTree99(features, 334);
    }
  }
  if (node === 82) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree99(features, 165);
    } else if (val <= 166.00000000000003) {
      return calculateTree99(features, 165);
    } else {
      return calculateTree99(features, 166);
    }
  }
  if (node === 40) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree99(features, 81);
    } else if (val <= 151.50000000000003) {
      return calculateTree99(features, 81);
    } else {
      return calculateTree99(features, 82);
    }
  }
  if (node === 19) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree99(features, 39);
    } else if (val <= 141.50000000000003) {
      return calculateTree99(features, 39);
    } else {
      return calculateTree99(features, 40);
    }
  }
  if (node === 671) return -0.3252468385024684;
  if (node === 672) return 0.14702437405819763;
  if (node === 335) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree99(features, 671);
    } else if (val <= 903.5000000000001) {
      return calculateTree99(features, 671);
    } else {
      return calculateTree99(features, 672);
    }
  }
  if (node === 673) return 0.22933595713298358;
  if (node === 674) return -0.027103776409741806;
  if (node === 336) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree99(features, 673);
    } else if (val <= 24.500000000000004) {
      return calculateTree99(features, 673);
    } else {
      return calculateTree99(features, 674);
    }
  }
  if (node === 167) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree99(features, 335);
    } else if (val <= 127.50000000000001) {
      return calculateTree99(features, 335);
    } else {
      return calculateTree99(features, 336);
    }
  }
  if (node === 168) return -0.26001531525640004;
  if (node === 83) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree99(features, 167);
    } else if (val <= 412.00000000000006) {
      return calculateTree99(features, 167);
    } else {
      return calculateTree99(features, 168);
    }
  }
  if (node === 169) return 0.05716796316026448;
  if (node === 170) return -0.27262367745065474;
  if (node === 84) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree99(features, 169);
    } else if (val <= 840.0000000000001) {
      return calculateTree99(features, 169);
    } else {
      return calculateTree99(features, 170);
    }
  }
  if (node === 41) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree99(features, 83);
    } else if (val <= 504.50000000000006) {
      return calculateTree99(features, 83);
    } else {
      return calculateTree99(features, 84);
    }
  }
  if (node === 171) return 0.3679700348535906;
  if (node === 345) return -0.4098615951693089;
  if (node === 1387) return 0.35333010461453146;
  if (node === 1388) return 0.08663737270658482;
  if (node === 693) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree99(features, 1387);
    } else if (val <= 899.5000000000001) {
      return calculateTree99(features, 1387);
    } else {
      return calculateTree99(features, 1388);
    }
  }
  if (node === 1389) return 0.013644752988381095;
  if (node === 1390) return 0.07913854719227333;
  if (node === 694) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree99(features, 1389);
    } else if (val <= 441.00000000000006) {
      return calculateTree99(features, 1389);
    } else {
      return calculateTree99(features, 1390);
    }
  }
  if (node === 346) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree99(features, 693);
    } else if (val <= 117.50000000000001) {
      return calculateTree99(features, 693);
    } else {
      return calculateTree99(features, 694);
    }
  }
  if (node === 172) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree99(features, 345);
    } else if (val <= 39.50000000000001) {
      return calculateTree99(features, 345);
    } else {
      return calculateTree99(features, 346);
    }
  }
  if (node === 85) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree99(features, 171);
    } else if (val <= 34.00000000000001) {
      return calculateTree99(features, 171);
    } else {
      return calculateTree99(features, 172);
    }
  }
  if (node === 173) return -0.09236676477219817;
  if (node === 174) return 0.1337619385282807;
  if (node === 86) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree99(features, 173);
    } else if (val <= 905.5000000000001) {
      return calculateTree99(features, 173);
    } else {
      return calculateTree99(features, 174);
    }
  }
  if (node === 42) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree99(features, 85);
    } else if (val <= 877.5000000000001) {
      return calculateTree99(features, 85);
    } else {
      return calculateTree99(features, 86);
    }
  }
  if (node === 20) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree99(features, 41);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree99(features, 41);
    } else {
      return calculateTree99(features, 42);
    }
  }
  if (node === 9) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree99(features, 19);
    } else if (val <= 742.5000000000001) {
      return calculateTree99(features, 19);
    } else {
      return calculateTree99(features, 20);
    }
  }
  if (node === 21) return -0.17440670979148712;
  if (node === 91) return 0.09046878295559091;
  if (node === 92) return 0.5457381181189395;
  if (node === 45) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree99(features, 91);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree99(features, 91);
    } else {
      return calculateTree99(features, 92);
    }
  }
  if (node === 187) return 0.037086049627201426;
  if (node === 188) return -0.3748229701790384;
  if (node === 93) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree99(features, 187);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree99(features, 187);
    } else {
      return calculateTree99(features, 188);
    }
  }
  if (node === 94) return -0.01188019167877922;
  if (node === 46) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree99(features, 93);
    } else if (val <= 938.5000000000001) {
      return calculateTree99(features, 93);
    } else {
      return calculateTree99(features, 94);
    }
  }
  if (node === 22) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree99(features, 45);
    } else if (val <= 923.5000000000001) {
      return calculateTree99(features, 45);
    } else {
      return calculateTree99(features, 46);
    }
  }
  if (node === 10) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree99(features, 21);
    } else if (val <= 922.5000000000001) {
      return calculateTree99(features, 21);
    } else {
      return calculateTree99(features, 22);
    }
  }
  if (node === 4) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree99(features, 9);
    } else if (val <= 908.5000000000001) {
      return calculateTree99(features, 9);
    } else {
      return calculateTree99(features, 10);
    }
  }
  if (node === 1) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree99(features, 3);
    } else if (val <= 18.500000000000004) {
      return calculateTree99(features, 3);
    } else {
      return calculateTree99(features, 4);
    }
  }
  if (node === 2) return -0.003197881348841936;
  if (node === 0) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree99(features, 1);
    } else if (val <= 29.500000000000004) {
      return calculateTree99(features, 1);
    } else {
      return calculateTree99(features, 2);
    }
  }
}
function calculateTree100(features, node = 0) {
  if (node === 7) return -0.1814320981667514;
  if (node === 8) return -0.00013018783798573238;
  if (node === 3) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree100(features, 7);
    } else if (val <= 60.00000000000001) {
      return calculateTree100(features, 7);
    } else {
      return calculateTree100(features, 8);
    }
  }
  if (node === 19) return 0.012130760744784854;
  if (node === 167) return -0.37791211246358125;
  if (node === 337) return 0.14540974263313577;
  if (node === 338) return -0.3424369742143388;
  if (node === 168) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree100(features, 337);
    } else if (val <= 127.50000000000001) {
      return calculateTree100(features, 337);
    } else {
      return calculateTree100(features, 338);
    }
  }
  if (node === 83) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree100(features, 167);
    } else if (val <= 118.50000000000001) {
      return calculateTree100(features, 167);
    } else {
      return calculateTree100(features, 168);
    }
  }
  if (node === 169) return 0.032959653717013836;
  if (node === 170) return 0.3281247351298286;
  if (node === 84) {
    const val = features['is_preference_women_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree100(features, 169);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree100(features, 169);
    } else {
      return calculateTree100(features, 170);
    }
  }
  if (node === 41) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree100(features, 83);
    } else if (val <= 365.50000000000006) {
      return calculateTree100(features, 83);
    } else {
      return calculateTree100(features, 84);
    }
  }
  if (node === 85) return -0.31377251631572867;
  if (node === 86) return 0.06252011715820309;
  if (node === 42) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree100(features, 85);
    } else if (val <= 127.50000000000001) {
      return calculateTree100(features, 85);
    } else {
      return calculateTree100(features, 86);
    }
  }
  if (node === 20) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree100(features, 41);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree100(features, 41);
    } else {
      return calculateTree100(features, 42);
    }
  }
  if (node === 9) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree100(features, 19);
    } else if (val <= 150.50000000000003) {
      return calculateTree100(features, 19);
    } else {
      return calculateTree100(features, 20);
    }
  }
  if (node === 43) return 0.11274632964574009;
  if (node === 179) return -0.023354000913907293;
  if (node === 180) return -0.3269318852340025;
  if (node === 89) {
    const val = features['is_preference_personal_care'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree100(features, 179);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree100(features, 179);
    } else {
      return calculateTree100(features, 180);
    }
  }
  if (node === 90) return 0.11106361584103004;
  if (node === 44) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree100(features, 89);
    } else if (val <= 5.500000000000001) {
      return calculateTree100(features, 89);
    } else {
      return calculateTree100(features, 90);
    }
  }
  if (node === 21) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree100(features, 43);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree100(features, 43);
    } else {
      return calculateTree100(features, 44);
    }
  }
  if (node === 45) return -0.3296174700809859;
  if (node === 751) return -0.32245029896678884;
  if (node === 1505) return -0.008897702289414618;
  if (node === 1506) return -0.32442825066907033;
  if (node === 752) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree100(features, 1505);
    } else if (val <= 757.5000000000001) {
      return calculateTree100(features, 1505);
    } else {
      return calculateTree100(features, 1506);
    }
  }
  if (node === 375) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree100(features, 751);
    } else if (val <= 127.50000000000001) {
      return calculateTree100(features, 751);
    } else {
      return calculateTree100(features, 752);
    }
  }
  if (node === 753) return 0.020421788113728582;
  if (node === 1509) return -0.26979120080540675;
  if (node === 1510) return 0.13493259124804663;
  if (node === 754) {
    const val = features['is_preference_kids'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree100(features, 1509);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree100(features, 1509);
    } else {
      return calculateTree100(features, 1510);
    }
  }
  if (node === 376) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree100(features, 753);
    } else if (val <= 10.500000000000002) {
      return calculateTree100(features, 753);
    } else {
      return calculateTree100(features, 754);
    }
  }
  if (node === 187) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree100(features, 375);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree100(features, 375);
    } else {
      return calculateTree100(features, 376);
    }
  }
  if (node === 1511) return -0.30866028038447096;
  if (node === 1512) return 0.11246545627161249;
  if (node === 755) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree100(features, 1511);
    } else if (val <= 127.50000000000001) {
      return calculateTree100(features, 1511);
    } else {
      return calculateTree100(features, 1512);
    }
  }
  if (node === 756) return -0.3669996357829432;
  if (node === 377) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree100(features, 755);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree100(features, 755);
    } else {
      return calculateTree100(features, 756);
    }
  }
  if (node === 378) return -0.004541130291325939;
  if (node === 188) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree100(features, 377);
    } else if (val <= 960.0000000000001) {
      return calculateTree100(features, 377);
    } else {
      return calculateTree100(features, 378);
    }
  }
  if (node === 93) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree100(features, 187);
    } else if (val <= 882.5000000000001) {
      return calculateTree100(features, 187);
    } else {
      return calculateTree100(features, 188);
    }
  }
  if (node === 189) return 0.046030647106328156;
  if (node === 190) return 0.2350451294106981;
  if (node === 94) {
    const val = features['is_preference_sports'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree100(features, 189);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree100(features, 189);
    } else {
      return calculateTree100(features, 190);
    }
  }
  if (node === 46) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree100(features, 93);
    } else if (val <= 40.50000000000001) {
      return calculateTree100(features, 93);
    } else {
      return calculateTree100(features, 94);
    }
  }
  if (node === 22) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree100(features, 45);
    } else if (val <= 685.0000000000001) {
      return calculateTree100(features, 45);
    } else {
      return calculateTree100(features, 46);
    }
  }
  if (node === 10) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree100(features, 21);
    } else if (val <= 671.5000000000001) {
      return calculateTree100(features, 21);
    } else {
      return calculateTree100(features, 22);
    }
  }
  if (node === 4) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree100(features, 9);
    } else if (val <= 481.50000000000006) {
      return calculateTree100(features, 9);
    } else {
      return calculateTree100(features, 10);
    }
  }
  if (node === 1) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree100(features, 3);
    } else if (val <= 113.50000000000001) {
      return calculateTree100(features, 3);
    } else {
      return calculateTree100(features, 4);
    }
  }
  if (node === 23) return -0.06107280284707948;
  if (node === 49) return 0.1864148653907548;
  if (node === 50) return -0.3069590858397415;
  if (node === 24) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree100(features, 49);
    } else if (val <= 835.5000000000001) {
      return calculateTree100(features, 49);
    } else {
      return calculateTree100(features, 50);
    }
  }
  if (node === 11) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree100(features, 23);
    } else if (val <= 175.50000000000003) {
      return calculateTree100(features, 23);
    } else {
      return calculateTree100(features, 24);
    }
  }
  if (node === 12) return -0.03721582317812707;
  if (node === 5) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree100(features, 11);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree100(features, 11);
    } else {
      return calculateTree100(features, 12);
    }
  }
  if (node === 6) return -0.00015850332285983573;
  if (node === 2) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree100(features, 5);
    } else if (val <= 144.50000000000003) {
      return calculateTree100(features, 5);
    } else {
      return calculateTree100(features, 6);
    }
  }
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree100(features, 1);
    } else if (val <= 130.00000000000003) {
      return calculateTree100(features, 1);
    } else {
      return calculateTree100(features, 2);
    }
  }
}
function calculateTree101(features, node = 0) {
  if (node === 1) return 0.0008663833619679449;
  if (node === 47) return 0.16255406665320393;
  if (node === 48) return 0.02879331564254766;
  if (node === 23) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree101(features, 47);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree101(features, 47);
    } else {
      return calculateTree101(features, 48);
    }
  }
  if (node === 49) return -0.30540827321234393;
  if (node === 203) return -0.004116023399900883;
  if (node === 409) return -0.3218103489533742;
  if (node === 821) return -0.08809968862799471;
  if (node === 1645) return 0.2801439141677578;
  if (node === 1646) return -0.09225306434585043;
  if (node === 822) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree101(features, 1645);
    } else if (val <= 4.500000000000001) {
      return calculateTree101(features, 1645);
    } else {
      return calculateTree101(features, 1646);
    }
  }
  if (node === 410) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree101(features, 821);
    } else if (val <= 39.50000000000001) {
      return calculateTree101(features, 821);
    } else {
      return calculateTree101(features, 822);
    }
  }
  if (node === 204) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree101(features, 409);
    } else if (val <= 808.5000000000001) {
      return calculateTree101(features, 409);
    } else {
      return calculateTree101(features, 410);
    }
  }
  if (node === 101) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree101(features, 203);
    } else if (val <= 661.0000000000001) {
      return calculateTree101(features, 203);
    } else {
      return calculateTree101(features, 204);
    }
  }
  if (node === 205) return 0.06730401185139905;
  if (node === 413) return -0.20614496879530314;
  if (node === 829) return -0.05753781130742604;
  if (node === 830) return 0.2392137175523865;
  if (node === 414) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree101(features, 829);
    } else if (val <= 607.5000000000001) {
      return calculateTree101(features, 829);
    } else {
      return calculateTree101(features, 830);
    }
  }
  if (node === 206) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree101(features, 413);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree101(features, 413);
    } else {
      return calculateTree101(features, 414);
    }
  }
  if (node === 102) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree101(features, 205);
    } else if (val <= 50.50000000000001) {
      return calculateTree101(features, 205);
    } else {
      return calculateTree101(features, 206);
    }
  }
  if (node === 50) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree101(features, 101);
    } else if (val <= 6.500000000000001) {
      return calculateTree101(features, 101);
    } else {
      return calculateTree101(features, 102);
    }
  }
  if (node === 24) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree101(features, 49);
    } else if (val <= 283.50000000000006) {
      return calculateTree101(features, 49);
    } else {
      return calculateTree101(features, 50);
    }
  }
  if (node === 11) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree101(features, 23);
    } else if (val <= 127.50000000000001) {
      return calculateTree101(features, 23);
    } else {
      return calculateTree101(features, 24);
    }
  }
  if (node === 25) return -0.3127813087183022;
  if (node === 1727) return -0.04911522207429585;
  if (node === 1728) return -0.4082035710883613;
  if (node === 863) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree101(features, 1727);
    } else if (val <= 983.5000000000001) {
      return calculateTree101(features, 1727);
    } else {
      return calculateTree101(features, 1728);
    }
  }
  if (node === 1729) return 0.08096363130444056;
  if (node === 1730) return -0.17827225409688935;
  if (node === 864) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree101(features, 1729);
    } else if (val <= 33.50000000000001) {
      return calculateTree101(features, 1729);
    } else {
      return calculateTree101(features, 1730);
    }
  }
  if (node === 431) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree101(features, 863);
    } else if (val <= 986.5000000000001) {
      return calculateTree101(features, 863);
    } else {
      return calculateTree101(features, 864);
    }
  }
  if (node === 432) return -0.2835803581361951;
  if (node === 215) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree101(features, 431);
    } else if (val <= 181.50000000000003) {
      return calculateTree101(features, 431);
    } else {
      return calculateTree101(features, 432);
    }
  }
  if (node === 867) return -0.001328090471100885;
  if (node === 1737) return 0.3514675465782351;
  if (node === 1738) return 0.002684402779155963;
  if (node === 868) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree101(features, 1737);
    } else if (val <= 540.0000000000001) {
      return calculateTree101(features, 1737);
    } else {
      return calculateTree101(features, 1738);
    }
  }
  if (node === 433) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree101(features, 867);
    } else if (val <= 17.500000000000004) {
      return calculateTree101(features, 867);
    } else {
      return calculateTree101(features, 868);
    }
  }
  if (node === 434) return -0.30562758752591174;
  if (node === 216) {
    const val = features['is_preference_sports'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree101(features, 433);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree101(features, 433);
    } else {
      return calculateTree101(features, 434);
    }
  }
  if (node === 107) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree101(features, 215);
    } else if (val <= 389.00000000000006) {
      return calculateTree101(features, 215);
    } else {
      return calculateTree101(features, 216);
    }
  }
  if (node === 108) return -0.3076888109006578;
  if (node === 53) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree101(features, 107);
    } else if (val <= 449.50000000000006) {
      return calculateTree101(features, 107);
    } else {
      return calculateTree101(features, 108);
    }
  }
  if (node === 439) return -0.004987004910014889;
  if (node === 440) return 0.30352795725082393;
  if (node === 219) {
    const val = features['is_preference_men_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree101(features, 439);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree101(features, 439);
    } else {
      return calculateTree101(features, 440);
    }
  }
  if (node === 883) return 0.15429432961021847;
  if (node === 884) return 0.4835146618729543;
  if (node === 441) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree101(features, 883);
    } else if (val <= 197.50000000000003) {
      return calculateTree101(features, 883);
    } else {
      return calculateTree101(features, 884);
    }
  }
  if (node === 442) return -0.022594286033105267;
  if (node === 220) {
    const val = features['is_preference_men_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree101(features, 441);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree101(features, 441);
    } else {
      return calculateTree101(features, 442);
    }
  }
  if (node === 109) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree101(features, 219);
    } else if (val <= 33.50000000000001) {
      return calculateTree101(features, 219);
    } else {
      return calculateTree101(features, 220);
    }
  }
  if (node === 110) return -0.12943868581766194;
  if (node === 54) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree101(features, 109);
    } else if (val <= 880.0000000000001) {
      return calculateTree101(features, 109);
    } else {
      return calculateTree101(features, 110);
    }
  }
  if (node === 26) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree101(features, 53);
    } else if (val <= 661.0000000000001) {
      return calculateTree101(features, 53);
    } else {
      return calculateTree101(features, 54);
    }
  }
  if (node === 12) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree101(features, 25);
    } else if (val <= 69.50000000000001) {
      return calculateTree101(features, 25);
    } else {
      return calculateTree101(features, 26);
    }
  }
  if (node === 5) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree101(features, 11);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree101(features, 11);
    } else {
      return calculateTree101(features, 12);
    }
  }
  if (node === 13) return -0.2908697873580333;
  if (node === 14) return 0.024129225403453636;
  if (node === 6) {
    const val = features['is_preference_women_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree101(features, 13);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree101(features, 13);
    } else {
      return calculateTree101(features, 14);
    }
  }
  if (node === 2) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree101(features, 5);
    } else if (val <= 29.500000000000004) {
      return calculateTree101(features, 5);
    } else {
      return calculateTree101(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree101(features, 1);
    } else if (val <= 960.0000000000001) {
      return calculateTree101(features, 1);
    } else {
      return calculateTree101(features, 2);
    }
  }
}
function calculateTree102(features, node = 0) {
  if (node === 31) return -0.0036629640229718288;
  if (node === 65) return -0.08036812744882624;
  if (node === 133) return -0.10648612929033162;
  if (node === 269) return 0.04663619082356618;
  if (node === 270) return 0.20765261570109672;
  if (node === 134) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree102(features, 269);
    } else if (val <= 2.5000000000000004) {
      return calculateTree102(features, 269);
    } else {
      return calculateTree102(features, 270);
    }
  }
  if (node === 66) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree102(features, 133);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree102(features, 133);
    } else {
      return calculateTree102(features, 134);
    }
  }
  if (node === 32) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree102(features, 65);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree102(features, 65);
    } else {
      return calculateTree102(features, 66);
    }
  }
  if (node === 15) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree102(features, 31);
    } else if (val <= 17.500000000000004) {
      return calculateTree102(features, 31);
    } else {
      return calculateTree102(features, 32);
    }
  }
  if (node === 135) return 0.05285069780261472;
  if (node === 136) return -0.17833082166967;
  if (node === 67) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree102(features, 135);
    } else if (val <= 20.500000000000004) {
      return calculateTree102(features, 135);
    } else {
      return calculateTree102(features, 136);
    }
  }
  if (node === 137) return 0.36049122755380797;
  if (node === 138) return 0.10431439248825994;
  if (node === 68) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree102(features, 137);
    } else if (val <= 8.500000000000002) {
      return calculateTree102(features, 137);
    } else {
      return calculateTree102(features, 138);
    }
  }
  if (node === 33) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree102(features, 67);
    } else if (val <= 697.0000000000001) {
      return calculateTree102(features, 67);
    } else {
      return calculateTree102(features, 68);
    }
  }
  if (node === 69) return -0.4295839626838343;
  if (node === 70) return -0.002728167848338696;
  if (node === 34) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree102(features, 69);
    } else if (val <= 51.50000000000001) {
      return calculateTree102(features, 69);
    } else {
      return calculateTree102(features, 70);
    }
  }
  if (node === 16) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree102(features, 33);
    } else if (val <= 926.0000000000001) {
      return calculateTree102(features, 33);
    } else {
      return calculateTree102(features, 34);
    }
  }
  if (node === 7) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree102(features, 15);
    } else if (val <= 43.50000000000001) {
      return calculateTree102(features, 15);
    } else {
      return calculateTree102(features, 16);
    }
  }
  if (node === 71) return 0.0688847402089129;
  if (node === 291) return 0.1151437231916434;
  if (node === 292) return -0.17238608017049284;
  if (node === 145) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree102(features, 291);
    } else if (val <= 540.0000000000001) {
      return calculateTree102(features, 291);
    } else {
      return calculateTree102(features, 292);
    }
  }
  if (node === 293) return -0.5087965779606594;
  if (node === 294) return -0.06091104537495523;
  if (node === 146) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree102(features, 293);
    } else if (val <= 2.5000000000000004) {
      return calculateTree102(features, 293);
    } else {
      return calculateTree102(features, 294);
    }
  }
  if (node === 72) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree102(features, 145);
    } else if (val <= 1.5000000000000002) {
      return calculateTree102(features, 145);
    } else {
      return calculateTree102(features, 146);
    }
  }
  if (node === 35) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree102(features, 71);
    } else if (val <= 13.500000000000002) {
      return calculateTree102(features, 71);
    } else {
      return calculateTree102(features, 72);
    }
  }
  if (node === 147) return 0.15862378079938777;
  if (node === 297) return -0.16560278384704266;
  if (node === 597) return 0.34475661009306163;
  if (node === 598) return -0.044634947296832206;
  if (node === 298) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree102(features, 597);
    } else if (val <= 34.50000000000001) {
      return calculateTree102(features, 597);
    } else {
      return calculateTree102(features, 598);
    }
  }
  if (node === 148) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree102(features, 297);
    } else if (val <= 197.50000000000003) {
      return calculateTree102(features, 297);
    } else {
      return calculateTree102(features, 298);
    }
  }
  if (node === 73) {
    const val = features['is_preference_grocery'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree102(features, 147);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree102(features, 147);
    } else {
      return calculateTree102(features, 148);
    }
  }
  if (node === 74) return -0.25540869693710205;
  if (node === 36) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree102(features, 73);
    } else if (val <= 29.500000000000004) {
      return calculateTree102(features, 73);
    } else {
      return calculateTree102(features, 74);
    }
  }
  if (node === 17) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree102(features, 35);
    } else if (val <= 28.500000000000004) {
      return calculateTree102(features, 35);
    } else {
      return calculateTree102(features, 36);
    }
  }
  if (node === 18) return -0.2777307174922358;
  if (node === 8) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree102(features, 17);
    } else if (val <= 51.50000000000001) {
      return calculateTree102(features, 17);
    } else {
      return calculateTree102(features, 18);
    }
  }
  if (node === 3) {
    const val = features['is_preference_kids'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree102(features, 7);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree102(features, 7);
    } else {
      return calculateTree102(features, 8);
    }
  }
  if (node === 4) return 0.22352486333704744;
  if (node === 1) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree102(features, 3);
    } else if (val <= 960.0000000000001) {
      return calculateTree102(features, 3);
    } else {
      return calculateTree102(features, 4);
    }
  }
  if (node === 47) return -0.003547585450559528;
  if (node === 48) return 0.25405943415576976;
  if (node === 23) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree102(features, 47);
    } else if (val <= 26.500000000000004) {
      return calculateTree102(features, 47);
    } else {
      return calculateTree102(features, 48);
    }
  }
  if (node === 99) return -0.3974915006451432;
  if (node === 201) return -0.05179094659543694;
  if (node === 202) return -0.30955253855495785;
  if (node === 100) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree102(features, 201);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree102(features, 201);
    } else {
      return calculateTree102(features, 202);
    }
  }
  if (node === 49) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree102(features, 99);
    } else if (val <= 35.50000000000001) {
      return calculateTree102(features, 99);
    } else {
      return calculateTree102(features, 100);
    }
  }
  if (node === 101) return 0.12505013859362288;
  if (node === 102) return -0.1801689139125713;
  if (node === 50) {
    const val = features['is_preference_personal_care'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree102(features, 101);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree102(features, 101);
    } else {
      return calculateTree102(features, 102);
    }
  }
  if (node === 24) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree102(features, 49);
    } else if (val <= 320.00000000000006) {
      return calculateTree102(features, 49);
    } else {
      return calculateTree102(features, 50);
    }
  }
  if (node === 11) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree102(features, 23);
    } else if (val <= 31.500000000000004) {
      return calculateTree102(features, 23);
    } else {
      return calculateTree102(features, 24);
    }
  }
  if (node === 12) return -0.25605173621448063;
  if (node === 5) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree102(features, 11);
    } else if (val <= 26.500000000000004) {
      return calculateTree102(features, 11);
    } else {
      return calculateTree102(features, 12);
    }
  }
  if (node === 6) return 8.900466677821136e-5;
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree102(features, 5);
    } else if (val <= 28.500000000000004) {
      return calculateTree102(features, 5);
    } else {
      return calculateTree102(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree102(features, 1);
    } else if (val <= 18.500000000000004) {
      return calculateTree102(features, 1);
    } else {
      return calculateTree102(features, 2);
    }
  }
}
function calculateTree103(features, node = 0) {
  if (node === 3) return 0.0006432684854281821;
  if (node === 9) return -0.33153893643088744;
  if (node === 351) return 0.014205267922438744;
  if (node === 1411) return 0.034889408765434456;
  if (node === 1412) return -0.23801821780849278;
  if (node === 705) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree103(features, 1411);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree103(features, 1411);
    } else {
      return calculateTree103(features, 1412);
    }
  }
  if (node === 1413) return 0.1141811142427767;
  if (node === 1414) return -0.1990012691879007;
  if (node === 706) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree103(features, 1413);
    } else if (val <= 626.0000000000001) {
      return calculateTree103(features, 1413);
    } else {
      return calculateTree103(features, 1414);
    }
  }
  if (node === 352) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree103(features, 705);
    } else if (val <= 39.50000000000001) {
      return calculateTree103(features, 705);
    } else {
      return calculateTree103(features, 706);
    }
  }
  if (node === 175) {
    const val = features['is_preference_sports'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree103(features, 351);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree103(features, 351);
    } else {
      return calculateTree103(features, 352);
    }
  }
  if (node === 176) return -0.17343486967413466;
  if (node === 87) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree103(features, 175);
    } else if (val <= 57.50000000000001) {
      return calculateTree103(features, 175);
    } else {
      return calculateTree103(features, 176);
    }
  }
  if (node === 177) return -0.18350136564282543;
  if (node === 178) return 0.0869557140258565;
  if (node === 88) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree103(features, 177);
    } else if (val <= 14.500000000000002) {
      return calculateTree103(features, 177);
    } else {
      return calculateTree103(features, 178);
    }
  }
  if (node === 43) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree103(features, 87);
    } else if (val <= 9.500000000000002) {
      return calculateTree103(features, 87);
    } else {
      return calculateTree103(features, 88);
    }
  }
  if (node === 89) return -0.19594599049692243;
  if (node === 363) return 0.2168330156673737;
  if (node === 364) return -0.07508932138046928;
  if (node === 181) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree103(features, 363);
    } else if (val <= 56.00000000000001) {
      return calculateTree103(features, 363);
    } else {
      return calculateTree103(features, 364);
    }
  }
  if (node === 182) return -0.03996921131777464;
  if (node === 90) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree103(features, 181);
    } else if (val <= 835.5000000000001) {
      return calculateTree103(features, 181);
    } else {
      return calculateTree103(features, 182);
    }
  }
  if (node === 44) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree103(features, 89);
    } else if (val <= 829.0000000000001) {
      return calculateTree103(features, 89);
    } else {
      return calculateTree103(features, 90);
    }
  }
  if (node === 21) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree103(features, 43);
    } else if (val <= 819.0000000000001) {
      return calculateTree103(features, 43);
    } else {
      return calculateTree103(features, 44);
    }
  }
  if (node === 91) return -0.0318210959179315;
  if (node === 92) return -0.36779502126131614;
  if (node === 45) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree103(features, 91);
    } else if (val <= 829.0000000000001) {
      return calculateTree103(features, 91);
    } else {
      return calculateTree103(features, 92);
    }
  }
  if (node === 46) return 0.008922017536117934;
  if (node === 22) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree103(features, 45);
    } else if (val <= 835.5000000000001) {
      return calculateTree103(features, 45);
    } else {
      return calculateTree103(features, 46);
    }
  }
  if (node === 10) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree103(features, 21);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree103(features, 21);
    } else {
      return calculateTree103(features, 22);
    }
  }
  if (node === 4) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree103(features, 9);
    } else if (val <= 778.5000000000001) {
      return calculateTree103(features, 9);
    } else {
      return calculateTree103(features, 10);
    }
  }
  if (node === 1) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree103(features, 3);
    } else if (val <= 777.5000000000001) {
      return calculateTree103(features, 3);
    } else {
      return calculateTree103(features, 4);
    }
  }
  if (node === 5) return 0.010163029039287761;
  if (node === 13) return -0.047101823256593296;
  if (node === 119) return 0.008081065666259902;
  if (node === 120) return 0.3241005172052483;
  if (node === 59) {
    const val = features['is_preference_men_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree103(features, 119);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree103(features, 119);
    } else {
      return calculateTree103(features, 120);
    }
  }
  if (node === 121) return -0.06313169415262417;
  if (node === 1967) return 0.01638428732209482;
  if (node === 1968) return -0.27993935823535604;
  if (node === 983) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree103(features, 1967);
    } else if (val <= 893.0000000000001) {
      return calculateTree103(features, 1967);
    } else {
      return calculateTree103(features, 1968);
    }
  }
  if (node === 984) return 0.12679733884007027;
  if (node === 491) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree103(features, 983);
    } else if (val <= 900.5000000000001) {
      return calculateTree103(features, 983);
    } else {
      return calculateTree103(features, 984);
    }
  }
  if (node === 492) return -0.16492178721503556;
  if (node === 245) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree103(features, 491);
    } else if (val <= 969.5000000000001) {
      return calculateTree103(features, 491);
    } else {
      return calculateTree103(features, 492);
    }
  }
  if (node === 987) return -0.04181662660738161;
  if (node === 988) return 0.24853189191112918;
  if (node === 493) {
    const val = features['is_preference_home'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree103(features, 987);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree103(features, 987);
    } else {
      return calculateTree103(features, 988);
    }
  }
  if (node === 989) return -0.20755358700159335;
  if (node === 1981) return 0.34225354031118993;
  if (node === 1982) return -0.09620214119076745;
  if (node === 990) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree103(features, 1981);
    } else if (val <= 42.50000000000001) {
      return calculateTree103(features, 1981);
    } else {
      return calculateTree103(features, 1982);
    }
  }
  if (node === 494) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree103(features, 989);
    } else if (val <= 926.0000000000001) {
      return calculateTree103(features, 989);
    } else {
      return calculateTree103(features, 990);
    }
  }
  if (node === 246) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree103(features, 493);
    } else if (val <= 897.0000000000001) {
      return calculateTree103(features, 493);
    } else {
      return calculateTree103(features, 494);
    }
  }
  if (node === 122) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree103(features, 245);
    } else if (val <= 479.50000000000006) {
      return calculateTree103(features, 245);
    } else {
      return calculateTree103(features, 246);
    }
  }
  if (node === 60) {
    const val = features['is_preference_women_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree103(features, 121);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree103(features, 121);
    } else {
      return calculateTree103(features, 122);
    }
  }
  if (node === 29) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree103(features, 59);
    } else if (val <= 29.500000000000004) {
      return calculateTree103(features, 59);
    } else {
      return calculateTree103(features, 60);
    }
  }
  if (node === 30) return -0.023117408509713645;
  if (node === 14) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree103(features, 29);
    } else if (val <= 6.500000000000001) {
      return calculateTree103(features, 29);
    } else {
      return calculateTree103(features, 30);
    }
  }
  if (node === 6) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree103(features, 13);
    } else if (val <= 26.500000000000004) {
      return calculateTree103(features, 13);
    } else {
      return calculateTree103(features, 14);
    }
  }
  if (node === 2) {
    const val = features['is_preference_personal_care'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree103(features, 5);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree103(features, 5);
    } else {
      return calculateTree103(features, 6);
    }
  }
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree103(features, 1);
    } else if (val <= 850.5000000000001) {
      return calculateTree103(features, 1);
    } else {
      return calculateTree103(features, 2);
    }
  }
}
function calculateTree104(features, node = 0) {
  if (node === 15) return 3.373997579318716e-5;
  if (node === 33) return -0.1728099853747306;
  if (node === 69) return 0.2507199508806649;
  if (node === 70) return -0.12395214353984448;
  if (node === 34) {
    const val = features['is_preference_sports'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree104(features, 69);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree104(features, 69);
    } else {
      return calculateTree104(features, 70);
    }
  }
  if (node === 16) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree104(features, 33);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree104(features, 33);
    } else {
      return calculateTree104(features, 34);
    }
  }
  if (node === 7) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree104(features, 15);
    } else if (val <= 358.50000000000006) {
      return calculateTree104(features, 15);
    } else {
      return calculateTree104(features, 16);
    }
  }
  if (node === 71) return -0.12153946474037108;
  if (node === 72) return 0.012148275982460365;
  if (node === 35) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree104(features, 71);
    } else if (val <= 377.50000000000006) {
      return calculateTree104(features, 71);
    } else {
      return calculateTree104(features, 72);
    }
  }
  if (node === 36) return -0.14246732368724974;
  if (node === 17) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree104(features, 35);
    } else if (val <= 386.00000000000006) {
      return calculateTree104(features, 35);
    } else {
      return calculateTree104(features, 36);
    }
  }
  if (node === 75) return 0.2084262808362701;
  if (node === 76) return -0.12001476916375248;
  if (node === 37) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree104(features, 75);
    } else if (val <= 19.500000000000004) {
      return calculateTree104(features, 75);
    } else {
      return calculateTree104(features, 76);
    }
  }
  if (node === 155) return 0.28619790754733504;
  if (node === 156) return 0.02752846531020729;
  if (node === 77) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree104(features, 155);
    } else if (val <= 21.500000000000004) {
      return calculateTree104(features, 155);
    } else {
      return calculateTree104(features, 156);
    }
  }
  if (node === 157) return 0.36038810756568307;
  if (node === 158) return -0.046973353437148486;
  if (node === 78) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree104(features, 157);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree104(features, 157);
    } else {
      return calculateTree104(features, 158);
    }
  }
  if (node === 38) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree104(features, 77);
    } else if (val <= 23.500000000000004) {
      return calculateTree104(features, 77);
    } else {
      return calculateTree104(features, 78);
    }
  }
  if (node === 18) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree104(features, 37);
    } else if (val <= 389.00000000000006) {
      return calculateTree104(features, 37);
    } else {
      return calculateTree104(features, 38);
    }
  }
  if (node === 8) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree104(features, 17);
    } else if (val <= 16.500000000000004) {
      return calculateTree104(features, 17);
    } else {
      return calculateTree104(features, 18);
    }
  }
  if (node === 3) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree104(features, 7);
    } else if (val <= 361.50000000000006) {
      return calculateTree104(features, 7);
    } else {
      return calculateTree104(features, 8);
    }
  }
  if (node === 39) return -0.03783217245091008;
  if (node === 40) return 0.05859291796035274;
  if (node === 19) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree104(features, 39);
    } else if (val <= 414.50000000000006) {
      return calculateTree104(features, 39);
    } else {
      return calculateTree104(features, 40);
    }
  }
  if (node === 41) return 0.014983662742234088;
  if (node === 42) return -0.05870699149963625;
  if (node === 20) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree104(features, 41);
    } else if (val <= 9.500000000000002) {
      return calculateTree104(features, 41);
    } else {
      return calculateTree104(features, 42);
    }
  }
  if (node === 9) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree104(features, 19);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree104(features, 19);
    } else {
      return calculateTree104(features, 20);
    }
  }
  if (node === 21) return 0.11124035283594133;
  if (node === 183) return -0.07876492381570073;
  if (node === 184) return 0.02630389470259267;
  if (node === 91) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree104(features, 183);
    } else if (val <= 414.50000000000006) {
      return calculateTree104(features, 183);
    } else {
      return calculateTree104(features, 184);
    }
  }
  if (node === 185) return -0.2345156714995713;
  if (node === 186) return -0.08707444359534464;
  if (node === 92) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree104(features, 185);
    } else if (val <= 432.00000000000006) {
      return calculateTree104(features, 185);
    } else {
      return calculateTree104(features, 186);
    }
  }
  if (node === 45) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree104(features, 91);
    } else if (val <= 429.00000000000006) {
      return calculateTree104(features, 91);
    } else {
      return calculateTree104(features, 92);
    }
  }
  if (node === 375) return 0.09023546049956453;
  if (node === 376) return 0.4124265039164184;
  if (node === 187) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree104(features, 375);
    } else if (val <= 295.00000000000006) {
      return calculateTree104(features, 375);
    } else {
      return calculateTree104(features, 376);
    }
  }
  if (node === 188) return 0.04243279703275688;
  if (node === 93) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree104(features, 187);
    } else if (val <= 449.50000000000006) {
      return calculateTree104(features, 187);
    } else {
      return calculateTree104(features, 188);
    }
  }
  if (node === 759) return -0.11082060688039479;
  if (node === 760) return 0.05529656886992873;
  if (node === 379) {
    const val = features['is_preference_personal_care'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree104(features, 759);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree104(features, 759);
    } else {
      return calculateTree104(features, 760);
    }
  }
  if (node === 380) return 0.02535588069365199;
  if (node === 189) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree104(features, 379);
    } else if (val <= 504.50000000000006) {
      return calculateTree104(features, 379);
    } else {
      return calculateTree104(features, 380);
    }
  }
  if (node === 190) return -0.0756311930168815;
  if (node === 94) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree104(features, 189);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree104(features, 189);
    } else {
      return calculateTree104(features, 190);
    }
  }
  if (node === 46) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree104(features, 93);
    } else if (val <= 478.50000000000006) {
      return calculateTree104(features, 93);
    } else {
      return calculateTree104(features, 94);
    }
  }
  if (node === 22) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree104(features, 45);
    } else if (val <= 447.00000000000006) {
      return calculateTree104(features, 45);
    } else {
      return calculateTree104(features, 46);
    }
  }
  if (node === 10) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree104(features, 21);
    } else if (val <= 409.50000000000006) {
      return calculateTree104(features, 21);
    } else {
      return calculateTree104(features, 22);
    }
  }
  if (node === 4) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree104(features, 9);
    } else if (val <= 38.50000000000001) {
      return calculateTree104(features, 9);
    } else {
      return calculateTree104(features, 10);
    }
  }
  if (node === 1) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree104(features, 3);
    } else if (val <= 391.00000000000006) {
      return calculateTree104(features, 3);
    } else {
      return calculateTree104(features, 4);
    }
  }
  if (node === 11) return -0.15861315259166356;
  if (node === 12) return 0.08268365696503466;
  if (node === 5) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree104(features, 11);
    } else if (val <= 6.500000000000001) {
      return calculateTree104(features, 11);
    } else {
      return calculateTree104(features, 12);
    }
  }
  if (node === 6) return -0.0019903624774709875;
  if (node === 2) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree104(features, 5);
    } else if (val <= 606.5000000000001) {
      return calculateTree104(features, 5);
    } else {
      return calculateTree104(features, 6);
    }
  }
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree104(features, 1);
    } else if (val <= 588.5000000000001) {
      return calculateTree104(features, 1);
    } else {
      return calculateTree104(features, 2);
    }
  }
}
function calculateTree105(features, node = 0) {
  if (node === 15) return -0.001197736218455457;
  if (node === 33) return 0.06884593847190779;
  if (node === 34) return -0.37694659421882276;
  if (node === 16) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree105(features, 33);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree105(features, 33);
    } else {
      return calculateTree105(features, 34);
    }
  }
  if (node === 7) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree105(features, 15);
    } else if (val <= 923.5000000000001) {
      return calculateTree105(features, 15);
    } else {
      return calculateTree105(features, 16);
    }
  }
  if (node === 71) return 0.13831788505065287;
  if (node === 72) return -0.34379307818936417;
  if (node === 35) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree105(features, 71);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree105(features, 71);
    } else {
      return calculateTree105(features, 72);
    }
  }
  if (node === 73) return -0.3756954839306511;
  if (node === 74) return 0.3707713758357343;
  if (node === 36) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree105(features, 73);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree105(features, 73);
    } else {
      return calculateTree105(features, 74);
    }
  }
  if (node === 17) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree105(features, 35);
    } else if (val <= 358.50000000000006) {
      return calculateTree105(features, 35);
    } else {
      return calculateTree105(features, 36);
    }
  }
  if (node === 18) return 0.007696696798095148;
  if (node === 8) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree105(features, 17);
    } else if (val <= 942.0000000000001) {
      return calculateTree105(features, 17);
    } else {
      return calculateTree105(features, 18);
    }
  }
  if (node === 3) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree105(features, 7);
    } else if (val <= 934.0000000000001) {
      return calculateTree105(features, 7);
    } else {
      return calculateTree105(features, 8);
    }
  }
  if (node === 319) return -0.3434134512110169;
  if (node === 641) return 0.3966211235140915;
  if (node === 642) return -0.045579126656391473;
  if (node === 320) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree105(features, 641);
    } else if (val <= 677.5000000000001) {
      return calculateTree105(features, 641);
    } else {
      return calculateTree105(features, 642);
    }
  }
  if (node === 159) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree105(features, 319);
    } else if (val <= 480.00000000000006) {
      return calculateTree105(features, 319);
    } else {
      return calculateTree105(features, 320);
    }
  }
  if (node === 321) return -0.32854391699540614;
  if (node === 1291) return -0.044427156150069126;
  if (node === 1292) return 0.48239402269130494;
  if (node === 645) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree105(features, 1291);
    } else if (val <= 449.50000000000006) {
      return calculateTree105(features, 1291);
    } else {
      return calculateTree105(features, 1292);
    }
  }
  if (node === 646) return -0.3251363948377433;
  if (node === 322) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree105(features, 645);
    } else if (val <= 497.50000000000006) {
      return calculateTree105(features, 645);
    } else {
      return calculateTree105(features, 646);
    }
  }
  if (node === 160) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree105(features, 321);
    } else if (val <= 432.00000000000006) {
      return calculateTree105(features, 321);
    } else {
      return calculateTree105(features, 322);
    }
  }
  if (node === 79) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree105(features, 159);
    } else if (val <= 422.00000000000006) {
      return calculateTree105(features, 159);
    } else {
      return calculateTree105(features, 160);
    }
  }
  if (node === 80) return 0.09579589457296803;
  if (node === 39) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree105(features, 79);
    } else if (val <= 693.0000000000001) {
      return calculateTree105(features, 79);
    } else {
      return calculateTree105(features, 80);
    }
  }
  if (node === 40) return -0.40344058166892743;
  if (node === 19) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree105(features, 39);
    } else if (val <= 780.0000000000001) {
      return calculateTree105(features, 39);
    } else {
      return calculateTree105(features, 40);
    }
  }
  if (node === 41) return 0.11359553777808204;
  if (node === 42) return -0.3045008752201772;
  if (node === 20) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree105(features, 41);
    } else if (val <= 441.00000000000006) {
      return calculateTree105(features, 41);
    } else {
      return calculateTree105(features, 42);
    }
  }
  if (node === 9) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree105(features, 19);
    } else if (val <= 859.5000000000001) {
      return calculateTree105(features, 19);
    } else {
      return calculateTree105(features, 20);
    }
  }
  if (node === 87) return 0.22538673549152374;
  if (node === 88) return -0.3200745006746579;
  if (node === 43) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree105(features, 87);
    } else if (val <= 36.50000000000001) {
      return calculateTree105(features, 87);
    } else {
      return calculateTree105(features, 88);
    }
  }
  if (node === 1439) return 0.15308598193012332;
  if (node === 1440) return 0.37524229704114453;
  if (node === 719) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree105(features, 1439);
    } else if (val <= 295.00000000000006) {
      return calculateTree105(features, 1439);
    } else {
      return calculateTree105(features, 1440);
    }
  }
  if (node === 1441) return -0.36864916741459314;
  if (node === 1442) return 0.08307649726982774;
  if (node === 720) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree105(features, 1441);
    } else if (val <= 422.00000000000006) {
      return calculateTree105(features, 1441);
    } else {
      return calculateTree105(features, 1442);
    }
  }
  if (node === 359) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree105(features, 719);
    } else if (val <= 314.50000000000006) {
      return calculateTree105(features, 719);
    } else {
      return calculateTree105(features, 720);
    }
  }
  if (node === 360) return -0.35901388897472536;
  if (node === 179) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree105(features, 359);
    } else if (val <= 449.50000000000006) {
      return calculateTree105(features, 359);
    } else {
      return calculateTree105(features, 360);
    }
  }
  if (node === 180) return -0.3161957278723518;
  if (node === 89) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree105(features, 179);
    } else if (val <= 675.5000000000001) {
      return calculateTree105(features, 179);
    } else {
      return calculateTree105(features, 180);
    }
  }
  if (node === 90) return 0.13252572054533918;
  if (node === 44) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree105(features, 89);
    } else if (val <= 780.0000000000001) {
      return calculateTree105(features, 89);
    } else {
      return calculateTree105(features, 90);
    }
  }
  if (node === 21) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree105(features, 43);
    } else if (val <= 232.50000000000003) {
      return calculateTree105(features, 43);
    } else {
      return calculateTree105(features, 44);
    }
  }
  if (node === 45) return -0.37129878485370116;
  if (node === 93) return 0.1645532934547901;
  if (node === 94) return 0.029134276008929993;
  if (node === 46) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree105(features, 93);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree105(features, 93);
    } else {
      return calculateTree105(features, 94);
    }
  }
  if (node === 22) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree105(features, 45);
    } else if (val <= 441.00000000000006) {
      return calculateTree105(features, 45);
    } else {
      return calculateTree105(features, 46);
    }
  }
  if (node === 10) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree105(features, 21);
    } else if (val <= 859.5000000000001) {
      return calculateTree105(features, 21);
    } else {
      return calculateTree105(features, 22);
    }
  }
  if (node === 4) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree105(features, 9);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree105(features, 9);
    } else {
      return calculateTree105(features, 10);
    }
  }
  if (node === 1) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree105(features, 3);
    } else if (val <= 414.50000000000006) {
      return calculateTree105(features, 3);
    } else {
      return calculateTree105(features, 4);
    }
  }
  if (node === 2) return -0.002104466378224114;
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree105(features, 1);
    } else if (val <= 467.50000000000006) {
      return calculateTree105(features, 1);
    } else {
      return calculateTree105(features, 2);
    }
  }
}
function calculateTree106(features, node = 0) {
  if (node === 15) return 0.0009496752567882068;
  if (node === 33) return -0.34122422676788267;
  if (node === 69) return 0.02430205853415713;
  if (node === 70) return 0.09777401452856546;
  if (node === 34) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree106(features, 69);
    } else if (val <= 197.50000000000003) {
      return calculateTree106(features, 69);
    } else {
      return calculateTree106(features, 70);
    }
  }
  if (node === 16) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree106(features, 33);
    } else if (val <= 253.00000000000003) {
      return calculateTree106(features, 33);
    } else {
      return calculateTree106(features, 34);
    }
  }
  if (node === 7) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree106(features, 15);
    } else if (val <= 422.50000000000006) {
      return calculateTree106(features, 15);
    } else {
      return calculateTree106(features, 16);
    }
  }
  if (node === 143) return 0.012099609482203212;
  if (node === 144) return -0.22906041083133782;
  if (node === 71) {
    const val = features['is_preference_kids'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree106(features, 143);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree106(features, 143);
    } else {
      return calculateTree106(features, 144);
    }
  }
  if (node === 72) return -0.22539049662807695;
  if (node === 35) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree106(features, 71);
    } else if (val <= 804.0000000000001) {
      return calculateTree106(features, 71);
    } else {
      return calculateTree106(features, 72);
    }
  }
  if (node === 73) return -0.29078379947868127;
  if (node === 74) return -0.13172157977715226;
  if (node === 36) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree106(features, 73);
    } else if (val <= 443.50000000000006) {
      return calculateTree106(features, 73);
    } else {
      return calculateTree106(features, 74);
    }
  }
  if (node === 17) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree106(features, 35);
    } else if (val <= 439.50000000000006) {
      return calculateTree106(features, 35);
    } else {
      return calculateTree106(features, 36);
    }
  }
  if (node === 37) return -0.021045414195405097;
  if (node === 77) return -0.06845957869751775;
  if (node === 157) return 0.26666819079853316;
  if (node === 158) return 0.01021272848906569;
  if (node === 78) {
    const val = features['is_preference_kids'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree106(features, 157);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree106(features, 157);
    } else {
      return calculateTree106(features, 158);
    }
  }
  if (node === 38) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree106(features, 77);
    } else if (val <= 31.500000000000004) {
      return calculateTree106(features, 77);
    } else {
      return calculateTree106(features, 78);
    }
  }
  if (node === 18) {
    const val = features['is_preference_personal_care'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree106(features, 37);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree106(features, 37);
    } else {
      return calculateTree106(features, 38);
    }
  }
  if (node === 8) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree106(features, 17);
    } else if (val <= 455.50000000000006) {
      return calculateTree106(features, 17);
    } else {
      return calculateTree106(features, 18);
    }
  }
  if (node === 3) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree106(features, 7);
    } else if (val <= 427.50000000000006) {
      return calculateTree106(features, 7);
    } else {
      return calculateTree106(features, 8);
    }
  }
  if (node === 9) return -0.10119573744916263;
  if (node === 21) return 0.028249703740130697;
  if (node === 91) return -0.04892539896309287;
  if (node === 92) return 0.1334567447422811;
  if (node === 45) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree106(features, 91);
    } else if (val <= 25.500000000000004) {
      return calculateTree106(features, 91);
    } else {
      return calculateTree106(features, 92);
    }
  }
  if (node === 93) return -0.03877100683612327;
  if (node === 759) return -0.03057634360736807;
  if (node === 760) return -0.3592543484436948;
  if (node === 379) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree106(features, 759);
    } else if (val <= 799.5000000000001) {
      return calculateTree106(features, 759);
    } else {
      return calculateTree106(features, 760);
    }
  }
  if (node === 761) return 0.13158172339213012;
  if (node === 762) return -0.040044172049506235;
  if (node === 380) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree106(features, 761);
    } else if (val <= 29.500000000000004) {
      return calculateTree106(features, 761);
    } else {
      return calculateTree106(features, 762);
    }
  }
  if (node === 189) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree106(features, 379);
    } else if (val <= 800.5000000000001) {
      return calculateTree106(features, 379);
    } else {
      return calculateTree106(features, 380);
    }
  }
  if (node === 190) return 0.09184420850258089;
  if (node === 94) {
    const val = features['is_preference_beauty'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree106(features, 189);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree106(features, 189);
    } else {
      return calculateTree106(features, 190);
    }
  }
  if (node === 46) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree106(features, 93);
    } else if (val <= 503.50000000000006) {
      return calculateTree106(features, 93);
    } else {
      return calculateTree106(features, 94);
    }
  }
  if (node === 22) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree106(features, 45);
    } else if (val <= 475.50000000000006) {
      return calculateTree106(features, 45);
    } else {
      return calculateTree106(features, 46);
    }
  }
  if (node === 10) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree106(features, 21);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree106(features, 21);
    } else {
      return calculateTree106(features, 22);
    }
  }
  if (node === 4) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree106(features, 9);
    } else if (val <= 82.50000000000001) {
      return calculateTree106(features, 9);
    } else {
      return calculateTree106(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree106(features, 3);
    } else if (val <= 472.50000000000006) {
      return calculateTree106(features, 3);
    } else {
      return calculateTree106(features, 4);
    }
  }
  if (node === 47) return -0.13990902562264068;
  if (node === 48) return 0.2388736505795701;
  if (node === 23) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree106(features, 47);
    } else if (val <= 28.500000000000004) {
      return calculateTree106(features, 47);
    } else {
      return calculateTree106(features, 48);
    }
  }
  if (node === 24) return -0.3359218256339226;
  if (node === 11) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree106(features, 23);
    } else if (val <= 201.50000000000003) {
      return calculateTree106(features, 23);
    } else {
      return calculateTree106(features, 24);
    }
  }
  if (node === 51) return 0.15758165785325254;
  if (node === 52) return -0.16411897609128828;
  if (node === 25) {
    const val = features['is_preference_pets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree106(features, 51);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree106(features, 51);
    } else {
      return calculateTree106(features, 52);
    }
  }
  if (node === 53) return -0.2657989679863448;
  if (node === 109) return -0.29452625672610033;
  if (node === 110) return 0.06877041680538114;
  if (node === 54) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree106(features, 109);
    } else if (val <= 531.5000000000001) {
      return calculateTree106(features, 109);
    } else {
      return calculateTree106(features, 110);
    }
  }
  if (node === 26) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree106(features, 53);
    } else if (val <= 816.5000000000001) {
      return calculateTree106(features, 53);
    } else {
      return calculateTree106(features, 54);
    }
  }
  if (node === 12) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree106(features, 25);
    } else if (val <= 470.00000000000006) {
      return calculateTree106(features, 25);
    } else {
      return calculateTree106(features, 26);
    }
  }
  if (node === 5) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree106(features, 11);
    } else if (val <= 310.00000000000006) {
      return calculateTree106(features, 11);
    } else {
      return calculateTree106(features, 12);
    }
  }
  if (node === 6) return -0.0023845108834589803;
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree106(features, 5);
    } else if (val <= 539.5000000000001) {
      return calculateTree106(features, 5);
    } else {
      return calculateTree106(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree106(features, 1);
    } else if (val <= 525.0000000000001) {
      return calculateTree106(features, 1);
    } else {
      return calculateTree106(features, 2);
    }
  }
}
function calculateTree107(features, node = 0) {
  if (node === 1) return 0.00048586808901683153;
  if (node === 95) return 0.014627484904179576;
  if (node === 193) return -0.08386149631942201;
  if (node === 194) return -0.5906132575622561;
  if (node === 96) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree107(features, 193);
    } else if (val <= 19.500000000000004) {
      return calculateTree107(features, 193);
    } else {
      return calculateTree107(features, 194);
    }
  }
  if (node === 47) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree107(features, 95);
    } else if (val <= 25.000000000000004) {
      return calculateTree107(features, 95);
    } else {
      return calculateTree107(features, 96);
    }
  }
  if (node === 48) return -0.007313089515972489;
  if (node === 23) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree107(features, 47);
    } else if (val <= 39.50000000000001) {
      return calculateTree107(features, 47);
    } else {
      return calculateTree107(features, 48);
    }
  }
  if (node === 199) return -0.0286135242308431;
  if (node === 200) return -0.328844898163032;
  if (node === 99) {
    const val = features['is_preference_kitchen'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree107(features, 199);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree107(features, 199);
    } else {
      return calculateTree107(features, 200);
    }
  }
  if (node === 100) return 0.19860613269247399;
  if (node === 49) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree107(features, 99);
    } else if (val <= 821.0000000000001) {
      return calculateTree107(features, 99);
    } else {
      return calculateTree107(features, 100);
    }
  }
  if (node === 50) return 0.09253732544357073;
  if (node === 24) {
    const val = features['is_preference_beauty'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree107(features, 49);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree107(features, 49);
    } else {
      return calculateTree107(features, 50);
    }
  }
  if (node === 11) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree107(features, 23);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree107(features, 23);
    } else {
      return calculateTree107(features, 24);
    }
  }
  if (node === 103) return -0.06130283174072115;
  if (node === 839) return 0.14041913039367687;
  if (node === 1681) return -0.33981941519372477;
  if (node === 1682) return 0.058713422330742164;
  if (node === 840) {
    const val = features['is_preference_personal_care'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree107(features, 1681);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree107(features, 1681);
    } else {
      return calculateTree107(features, 1682);
    }
  }
  if (node === 419) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree107(features, 839);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree107(features, 839);
    } else {
      return calculateTree107(features, 840);
    }
  }
  if (node === 841) return -0.3546213517462641;
  if (node === 842) return -0.018108338602870463;
  if (node === 420) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree107(features, 841);
    } else if (val <= 23.500000000000004) {
      return calculateTree107(features, 841);
    } else {
      return calculateTree107(features, 842);
    }
  }
  if (node === 209) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree107(features, 419);
    } else if (val <= 316.50000000000006) {
      return calculateTree107(features, 419);
    } else {
      return calculateTree107(features, 420);
    }
  }
  if (node === 1687) return 0.005792831312878004;
  if (node === 1688) return 0.42734067470057124;
  if (node === 843) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree107(features, 1687);
    } else if (val <= 155.50000000000003) {
      return calculateTree107(features, 1687);
    } else {
      return calculateTree107(features, 1688);
    }
  }
  if (node === 844) return -0.15337944961130426;
  if (node === 421) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree107(features, 843);
    } else if (val <= 540.0000000000001) {
      return calculateTree107(features, 843);
    } else {
      return calculateTree107(features, 844);
    }
  }
  if (node === 845) return 0.1498021825961145;
  if (node === 846) return 0.4821373976283218;
  if (node === 422) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree107(features, 845);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree107(features, 845);
    } else {
      return calculateTree107(features, 846);
    }
  }
  if (node === 210) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree107(features, 421);
    } else if (val <= 286.50000000000006) {
      return calculateTree107(features, 421);
    } else {
      return calculateTree107(features, 422);
    }
  }
  if (node === 104) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree107(features, 209);
    } else if (val <= 38.50000000000001) {
      return calculateTree107(features, 209);
    } else {
      return calculateTree107(features, 210);
    }
  }
  if (node === 51) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree107(features, 103);
    } else if (val <= 203.50000000000003) {
      return calculateTree107(features, 103);
    } else {
      return calculateTree107(features, 104);
    }
  }
  if (node === 105) return -0.05366044059125893;
  if (node === 213) return -0.37679323849440466;
  if (node === 214) return -0.12963881818575565;
  if (node === 106) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree107(features, 213);
    } else if (val <= 498.50000000000006) {
      return calculateTree107(features, 213);
    } else {
      return calculateTree107(features, 214);
    }
  }
  if (node === 52) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree107(features, 105);
    } else if (val <= 22.500000000000004) {
      return calculateTree107(features, 105);
    } else {
      return calculateTree107(features, 106);
    }
  }
  if (node === 25) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree107(features, 51);
    } else if (val <= 338.50000000000006) {
      return calculateTree107(features, 51);
    } else {
      return calculateTree107(features, 52);
    }
  }
  if (node === 215) return -0.10025250909239783;
  if (node === 216) return 0.16739897124233932;
  if (node === 107) {
    const val = features['is_preference_kitchen'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree107(features, 215);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree107(features, 215);
    } else {
      return calculateTree107(features, 216);
    }
  }
  if (node === 108) return 0.23046164764313176;
  if (node === 53) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree107(features, 107);
    } else if (val <= 29.500000000000004) {
      return calculateTree107(features, 107);
    } else {
      return calculateTree107(features, 108);
    }
  }
  if (node === 219) return -0.0354792736940625;
  if (node === 220) return 0.38498641709811815;
  if (node === 109) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree107(features, 219);
    } else if (val <= 155.50000000000003) {
      return calculateTree107(features, 219);
    } else {
      return calculateTree107(features, 220);
    }
  }
  if (node === 443) return -0.19115453452490233;
  if (node === 444) return -0.027563521384500373;
  if (node === 221) {
    const val = features['is_preference_beauty'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree107(features, 443);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree107(features, 443);
    } else {
      return calculateTree107(features, 444);
    }
  }
  if (node === 222) return 0.2297724499649279;
  if (node === 110) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree107(features, 221);
    } else if (val <= 59.50000000000001) {
      return calculateTree107(features, 221);
    } else {
      return calculateTree107(features, 222);
    }
  }
  if (node === 54) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree107(features, 109);
    } else if (val <= 234.50000000000003) {
      return calculateTree107(features, 109);
    } else {
      return calculateTree107(features, 110);
    }
  }
  if (node === 26) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree107(features, 53);
    } else if (val <= 34.50000000000001) {
      return calculateTree107(features, 53);
    } else {
      return calculateTree107(features, 54);
    }
  }
  if (node === 12) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree107(features, 25);
    } else if (val <= 27.500000000000004) {
      return calculateTree107(features, 25);
    } else {
      return calculateTree107(features, 26);
    }
  }
  if (node === 5) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree107(features, 11);
    } else if (val <= 1.5000000000000002) {
      return calculateTree107(features, 11);
    } else {
      return calculateTree107(features, 12);
    }
  }
  if (node === 6) return 0.0009011157741608991;
  if (node === 2) {
    const val = features['is_preference_home'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree107(features, 5);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree107(features, 5);
    } else {
      return calculateTree107(features, 6);
    }
  }
  if (node === 0) {
    const val = features['is_preference_women_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree107(features, 1);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree107(features, 1);
    } else {
      return calculateTree107(features, 2);
    }
  }
}
function calculateTree108(features, node = 0) {
  if (node === 7) return 0.002469085499645514;
  if (node === 8) return -0.17887237508220125;
  if (node === 3) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree108(features, 7);
    } else if (val <= 143.50000000000003) {
      return calculateTree108(features, 7);
    } else {
      return calculateTree108(features, 8);
    }
  }
  if (node === 19) return 0.14494449797406259;
  if (node === 41) return 0.08415881455349379;
  if (node === 85) return -0.32397069199955353;
  if (node === 86) return 0.045904028259630546;
  if (node === 42) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree108(features, 85);
    } else if (val <= 158.50000000000003) {
      return calculateTree108(features, 85);
    } else {
      return calculateTree108(features, 86);
    }
  }
  if (node === 20) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree108(features, 41);
    } else if (val <= 150.50000000000003) {
      return calculateTree108(features, 41);
    } else {
      return calculateTree108(features, 42);
    }
  }
  if (node === 9) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree108(features, 19);
    } else if (val <= 1.5000000000000002) {
      return calculateTree108(features, 19);
    } else {
      return calculateTree108(features, 20);
    }
  }
  if (node === 21) return -0.3257105740868059;
  if (node === 45) return 0.2309677870314487;
  if (node === 46) return -0.3042129391479255;
  if (node === 22) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree108(features, 45);
    } else if (val <= 288.00000000000006) {
      return calculateTree108(features, 45);
    } else {
      return calculateTree108(features, 46);
    }
  }
  if (node === 10) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree108(features, 21);
    } else if (val <= 178.50000000000003) {
      return calculateTree108(features, 21);
    } else {
      return calculateTree108(features, 22);
    }
  }
  if (node === 4) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree108(features, 9);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree108(features, 9);
    } else {
      return calculateTree108(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree108(features, 3);
    } else if (val <= 148.00000000000003) {
      return calculateTree108(features, 3);
    } else {
      return calculateTree108(features, 4);
    }
  }
  if (node === 191) return 0.013893697011984108;
  if (node === 192) return -0.3182831512153129;
  if (node === 95) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree108(features, 191);
    } else if (val <= 175.50000000000003) {
      return calculateTree108(features, 191);
    } else {
      return calculateTree108(features, 192);
    }
  }
  if (node === 96) return -0.3212739149346637;
  if (node === 47) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree108(features, 95);
    } else if (val <= 180.50000000000003) {
      return calculateTree108(features, 95);
    } else {
      return calculateTree108(features, 96);
    }
  }
  if (node === 1567) return -0.024961125197600725;
  if (node === 1568) return 0.2193368457307119;
  if (node === 783) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree108(features, 1567);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree108(features, 1567);
    } else {
      return calculateTree108(features, 1568);
    }
  }
  if (node === 784) return -0.3044539478781325;
  if (node === 391) {
    const val = features['is_preference_kids'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree108(features, 783);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree108(features, 783);
    } else {
      return calculateTree108(features, 784);
    }
  }
  if (node === 785) return -0.1482893076247422;
  if (node === 1573) return 0.3087704030287735;
  if (node === 1574) return 0.07946265655784501;
  if (node === 786) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree108(features, 1573);
    } else if (val <= 1.5000000000000002) {
      return calculateTree108(features, 1573);
    } else {
      return calculateTree108(features, 1574);
    }
  }
  if (node === 392) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree108(features, 785);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree108(features, 785);
    } else {
      return calculateTree108(features, 786);
    }
  }
  if (node === 195) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree108(features, 391);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree108(features, 391);
    } else {
      return calculateTree108(features, 392);
    }
  }
  if (node === 196) return -0.30533882174774923;
  if (node === 97) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree108(features, 195);
    } else if (val <= 643.5000000000001) {
      return calculateTree108(features, 195);
    } else {
      return calculateTree108(features, 196);
    }
  }
  if (node === 197) return 0.550831694320525;
  if (node === 198) return -0.04385074347736335;
  if (node === 98) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree108(features, 197);
    } else if (val <= 2.5000000000000004) {
      return calculateTree108(features, 197);
    } else {
      return calculateTree108(features, 198);
    }
  }
  if (node === 48) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree108(features, 97);
    } else if (val <= 181.50000000000003) {
      return calculateTree108(features, 97);
    } else {
      return calculateTree108(features, 98);
    }
  }
  if (node === 23) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree108(features, 47);
    } else if (val <= 542.0000000000001) {
      return calculateTree108(features, 47);
    } else {
      return calculateTree108(features, 48);
    }
  }
  if (node === 49) return -0.3176037366554254;
  if (node === 1631) return -0.33714150565449547;
  if (node === 1632) return 0.02314715956081689;
  if (node === 815) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree108(features, 1631);
    } else if (val <= 186.50000000000003) {
      return calculateTree108(features, 1631);
    } else {
      return calculateTree108(features, 1632);
    }
  }
  if (node === 1633) return 0.02033790894088868;
  if (node === 1634) return -0.1815694231724951;
  if (node === 816) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree108(features, 1633);
    } else if (val <= 186.50000000000003) {
      return calculateTree108(features, 1633);
    } else {
      return calculateTree108(features, 1634);
    }
  }
  if (node === 407) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree108(features, 815);
    } else if (val <= 34.00000000000001) {
      return calculateTree108(features, 815);
    } else {
      return calculateTree108(features, 816);
    }
  }
  if (node === 408) return 0.10760885991345415;
  if (node === 203) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree108(features, 407);
    } else if (val <= 358.50000000000006) {
      return calculateTree108(features, 407);
    } else {
      return calculateTree108(features, 408);
    }
  }
  if (node === 204) return -0.3323403394023047;
  if (node === 101) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree108(features, 203);
    } else if (val <= 679.5000000000001) {
      return calculateTree108(features, 203);
    } else {
      return calculateTree108(features, 204);
    }
  }
  if (node === 205) return 0.17698694972290055;
  if (node === 206) return -0.10262846452652592;
  if (node === 102) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree108(features, 205);
    } else if (val <= 10.500000000000002) {
      return calculateTree108(features, 205);
    } else {
      return calculateTree108(features, 206);
    }
  }
  if (node === 50) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree108(features, 101);
    } else if (val <= 808.5000000000001) {
      return calculateTree108(features, 101);
    } else {
      return calculateTree108(features, 102);
    }
  }
  if (node === 24) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree108(features, 49);
    } else if (val <= 166.50000000000003) {
      return calculateTree108(features, 49);
    } else {
      return calculateTree108(features, 50);
    }
  }
  if (node === 11) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree108(features, 23);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree108(features, 23);
    } else {
      return calculateTree108(features, 24);
    }
  }
  if (node === 12) return -0.24252603982498164;
  if (node === 5) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree108(features, 11);
    } else if (val <= 835.5000000000001) {
      return calculateTree108(features, 11);
    } else {
      return calculateTree108(features, 12);
    }
  }
  if (node === 6) return -0.00021192922223070882;
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree108(features, 5);
    } else if (val <= 191.50000000000003) {
      return calculateTree108(features, 5);
    } else {
      return calculateTree108(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree108(features, 1);
    } else if (val <= 164.50000000000003) {
      return calculateTree108(features, 1);
    } else {
      return calculateTree108(features, 2);
    }
  }
}
function calculateTree109(features, node = 0) {
  if (node === 3) return 0.007951316227003541;
  if (node === 19) return 0.012838899039778945;
  if (node === 20) return -0.2383805909484793;
  if (node === 9) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree109(features, 19);
    } else if (val <= 112.00000000000001) {
      return calculateTree109(features, 19);
    } else {
      return calculateTree109(features, 20);
    }
  }
  if (node === 10) return -0.1368565646471008;
  if (node === 4) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree109(features, 9);
    } else if (val <= 784.5000000000001) {
      return calculateTree109(features, 9);
    } else {
      return calculateTree109(features, 10);
    }
  }
  if (node === 1) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree109(features, 3);
    } else if (val <= 27.500000000000004) {
      return calculateTree109(features, 3);
    } else {
      return calculateTree109(features, 4);
    }
  }
  if (node === 383) return -0.004845645145746265;
  if (node === 1539) return 0.2693112058747953;
  if (node === 1540) return -0.07830471515718851;
  if (node === 769) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree109(features, 1539);
    } else if (val <= 1.5000000000000002) {
      return calculateTree109(features, 1539);
    } else {
      return calculateTree109(features, 1540);
    }
  }
  if (node === 770) return -0.2810135218713125;
  if (node === 384) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree109(features, 769);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree109(features, 769);
    } else {
      return calculateTree109(features, 770);
    }
  }
  if (node === 191) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree109(features, 383);
    } else if (val <= 75.50000000000001) {
      return calculateTree109(features, 383);
    } else {
      return calculateTree109(features, 384);
    }
  }
  if (node === 1543) return 0.1351678643577733;
  if (node === 1544) return 0.011411230873857816;
  if (node === 771) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree109(features, 1543);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree109(features, 1543);
    } else {
      return calculateTree109(features, 1544);
    }
  }
  if (node === 1545) return -0.3177568869586354;
  if (node === 1546) return 0.00523419346447731;
  if (node === 772) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree109(features, 1545);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree109(features, 1545);
    } else {
      return calculateTree109(features, 1546);
    }
  }
  if (node === 385) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree109(features, 771);
    } else if (val <= 671.5000000000001) {
      return calculateTree109(features, 771);
    } else {
      return calculateTree109(features, 772);
    }
  }
  if (node === 386) return 0.2736001689956971;
  if (node === 192) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree109(features, 385);
    } else if (val <= 139.50000000000003) {
      return calculateTree109(features, 385);
    } else {
      return calculateTree109(features, 386);
    }
  }
  if (node === 95) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree109(features, 191);
    } else if (val <= 481.50000000000006) {
      return calculateTree109(features, 191);
    } else {
      return calculateTree109(features, 192);
    }
  }
  if (node === 193) return -0.2522986017488148;
  if (node === 1559) return -0.30680981615836145;
  if (node === 1560) return -0.05814345122477737;
  if (node === 779) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree109(features, 1559);
    } else if (val <= 972.5000000000001) {
      return calculateTree109(features, 1559);
    } else {
      return calculateTree109(features, 1560);
    }
  }
  if (node === 1561) return -0.032246503399789815;
  if (node === 1562) return 0.15834486101713893;
  if (node === 780) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree109(features, 1561);
    } else if (val <= 234.50000000000003) {
      return calculateTree109(features, 1561);
    } else {
      return calculateTree109(features, 1562);
    }
  }
  if (node === 389) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree109(features, 779);
    } else if (val <= 127.50000000000001) {
      return calculateTree109(features, 779);
    } else {
      return calculateTree109(features, 780);
    }
  }
  if (node === 390) return -0.3545680916041545;
  if (node === 194) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree109(features, 389);
    } else if (val <= 983.5000000000001) {
      return calculateTree109(features, 389);
    } else {
      return calculateTree109(features, 390);
    }
  }
  if (node === 96) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree109(features, 193);
    } else if (val <= 757.5000000000001) {
      return calculateTree109(features, 193);
    } else {
      return calculateTree109(features, 194);
    }
  }
  if (node === 47) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree109(features, 95);
    } else if (val <= 729.5000000000001) {
      return calculateTree109(features, 95);
    } else {
      return calculateTree109(features, 96);
    }
  }
  if (node === 48) return 0.041389787865672;
  if (node === 23) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree109(features, 47);
    } else if (val <= 148.50000000000003) {
      return calculateTree109(features, 47);
    } else {
      return calculateTree109(features, 48);
    }
  }
  if (node === 49) return -0.19345468155670906;
  if (node === 50) return -0.03755608846056049;
  if (node === 24) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree109(features, 49);
    } else if (val <= 158.50000000000003) {
      return calculateTree109(features, 49);
    } else {
      return calculateTree109(features, 50);
    }
  }
  if (node === 11) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree109(features, 23);
    } else if (val <= 151.50000000000003) {
      return calculateTree109(features, 23);
    } else {
      return calculateTree109(features, 24);
    }
  }
  if (node === 25) return 0.027429005598437195;
  if (node === 863) return -0.02788476966196876;
  if (node === 864) return 0.15667091519125434;
  if (node === 431) {
    const val = features['is_preference_home'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree109(features, 863);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree109(features, 863);
    } else {
      return calculateTree109(features, 864);
    }
  }
  if (node === 432) return -0.2585862704594787;
  if (node === 215) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree109(features, 431);
    } else if (val <= 908.0000000000001) {
      return calculateTree109(features, 431);
    } else {
      return calculateTree109(features, 432);
    }
  }
  if (node === 216) return -0.2966962169137808;
  if (node === 107) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree109(features, 215);
    } else if (val <= 171.00000000000003) {
      return calculateTree109(features, 215);
    } else {
      return calculateTree109(features, 216);
    }
  }
  if (node === 217) return -0.3625574160428885;
  if (node === 437) return -0.004236578991808254;
  if (node === 438) return -0.3204345562557319;
  if (node === 218) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree109(features, 437);
    } else if (val <= 365.50000000000006) {
      return calculateTree109(features, 437);
    } else {
      return calculateTree109(features, 438);
    }
  }
  if (node === 108) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree109(features, 217);
    } else if (val <= 282.50000000000006) {
      return calculateTree109(features, 217);
    } else {
      return calculateTree109(features, 218);
    }
  }
  if (node === 53) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree109(features, 107);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree109(features, 107);
    } else {
      return calculateTree109(features, 108);
    }
  }
  if (node === 54) return 0.006503011680125741;
  if (node === 26) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree109(features, 53);
    } else if (val <= 427.50000000000006) {
      return calculateTree109(features, 53);
    } else {
      return calculateTree109(features, 54);
    }
  }
  if (node === 12) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree109(features, 25);
    } else if (val <= 175.50000000000003) {
      return calculateTree109(features, 25);
    } else {
      return calculateTree109(features, 26);
    }
  }
  if (node === 5) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree109(features, 11);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree109(features, 11);
    } else {
      return calculateTree109(features, 12);
    }
  }
  if (node === 6) return 3.864426019837455e-5;
  if (node === 2) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree109(features, 5);
    } else if (val <= 172.50000000000003) {
      return calculateTree109(features, 5);
    } else {
      return calculateTree109(features, 6);
    }
  }
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree109(features, 1);
    } else if (val <= 113.50000000000001) {
      return calculateTree109(features, 1);
    } else {
      return calculateTree109(features, 2);
    }
  }
}
function calculateTree110(features, node = 0) {
  if (node === 63) return 0.10566951109454346;
  if (node === 259) return -0.030707985973635006;
  if (node === 260) return 0.05236079739303019;
  if (node === 129) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree110(features, 259);
    } else if (val <= 25.000000000000004) {
      return calculateTree110(features, 259);
    } else {
      return calculateTree110(features, 260);
    }
  }
  if (node === 261) return 0.020029280767499057;
  if (node === 262) return -0.32252289302795606;
  if (node === 130) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree110(features, 261);
    } else if (val <= 188.00000000000003) {
      return calculateTree110(features, 261);
    } else {
      return calculateTree110(features, 262);
    }
  }
  if (node === 64) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree110(features, 129);
    } else if (val <= 34.00000000000001) {
      return calculateTree110(features, 129);
    } else {
      return calculateTree110(features, 130);
    }
  }
  if (node === 31) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree110(features, 63);
    } else if (val <= 166.50000000000003) {
      return calculateTree110(features, 63);
    } else {
      return calculateTree110(features, 64);
    }
  }
  if (node === 263) return -0.05431518112760078;
  if (node === 264) return 0.10114172225790652;
  if (node === 131) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree110(features, 263);
    } else if (val <= 897.0000000000001) {
      return calculateTree110(features, 263);
    } else {
      return calculateTree110(features, 264);
    }
  }
  if (node === 265) return -0.06977141722409604;
  if (node === 266) return 0.17255040218480347;
  if (node === 132) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree110(features, 265);
    } else if (val <= 155.50000000000003) {
      return calculateTree110(features, 265);
    } else {
      return calculateTree110(features, 266);
    }
  }
  if (node === 65) {
    const val = features['is_preference_grocery'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree110(features, 131);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree110(features, 131);
    } else {
      return calculateTree110(features, 132);
    }
  }
  if (node === 267) return 0.17274364875911832;
  if (node === 268) return -0.13064102082993628;
  if (node === 133) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree110(features, 267);
    } else if (val <= 57.50000000000001) {
      return calculateTree110(features, 267);
    } else {
      return calculateTree110(features, 268);
    }
  }
  if (node === 134) return -0.3183788033911988;
  if (node === 66) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree110(features, 133);
    } else if (val <= 188.00000000000003) {
      return calculateTree110(features, 133);
    } else {
      return calculateTree110(features, 134);
    }
  }
  if (node === 32) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree110(features, 65);
    } else if (val <= 34.00000000000001) {
      return calculateTree110(features, 65);
    } else {
      return calculateTree110(features, 66);
    }
  }
  if (node === 15) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree110(features, 31);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree110(features, 31);
    } else {
      return calculateTree110(features, 32);
    }
  }
  if (node === 33) return -0.2797251843923032;
  if (node === 34) return 0.05910629293540848;
  if (node === 16) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree110(features, 33);
    } else if (val <= 12.000000000000002) {
      return calculateTree110(features, 33);
    } else {
      return calculateTree110(features, 34);
    }
  }
  if (node === 7) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree110(features, 15);
    } else if (val <= 191.50000000000003) {
      return calculateTree110(features, 15);
    } else {
      return calculateTree110(features, 16);
    }
  }
  if (node === 17) return -0.2768408229510937;
  if (node === 1215) return 0.011386568727574728;
  if (node === 1216) return -0.07217265069153349;
  if (node === 607) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree110(features, 1215);
    } else if (val <= 589.5000000000001) {
      return calculateTree110(features, 1215);
    } else {
      return calculateTree110(features, 1216);
    }
  }
  if (node === 608) return 0.04465921573384617;
  if (node === 303) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree110(features, 607);
    } else if (val <= 658.5000000000001) {
      return calculateTree110(features, 607);
    } else {
      return calculateTree110(features, 608);
    }
  }
  if (node === 609) return -0.23180166129386542;
  if (node === 1221) return -0.046024878361430224;
  if (node === 1222) return 0.1650545488449499;
  if (node === 610) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree110(features, 1221);
    } else if (val <= 176.50000000000003) {
      return calculateTree110(features, 1221);
    } else {
      return calculateTree110(features, 1222);
    }
  }
  if (node === 304) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree110(features, 609);
    } else if (val <= 731.5000000000001) {
      return calculateTree110(features, 609);
    } else {
      return calculateTree110(features, 610);
    }
  }
  if (node === 151) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree110(features, 303);
    } else if (val <= 685.0000000000001) {
      return calculateTree110(features, 303);
    } else {
      return calculateTree110(features, 304);
    }
  }
  if (node === 1223) return 0.209952297805597;
  if (node === 1224) return 0.014969949951287472;
  if (node === 611) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree110(features, 1223);
    } else if (val <= 760.0000000000001) {
      return calculateTree110(features, 1223);
    } else {
      return calculateTree110(features, 1224);
    }
  }
  if (node === 612) return 0.42619396216305955;
  if (node === 305) {
    const val = features['is_preference_men_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree110(features, 611);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree110(features, 611);
    } else {
      return calculateTree110(features, 612);
    }
  }
  if (node === 306) return -0.254905032950015;
  if (node === 152) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree110(features, 305);
    } else if (val <= 41.50000000000001) {
      return calculateTree110(features, 305);
    } else {
      return calculateTree110(features, 306);
    }
  }
  if (node === 75) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree110(features, 151);
    } else if (val <= 752.5000000000001) {
      return calculateTree110(features, 151);
    } else {
      return calculateTree110(features, 152);
    }
  }
  if (node === 76) return -0.09158444716615886;
  if (node === 37) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree110(features, 75);
    } else if (val <= 784.5000000000001) {
      return calculateTree110(features, 75);
    } else {
      return calculateTree110(features, 76);
    }
  }
  if (node === 155) return 0.02963723060290713;
  if (node === 156) return -0.12004181876132267;
  if (node === 77) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree110(features, 155);
    } else if (val <= 28.500000000000004) {
      return calculateTree110(features, 155);
    } else {
      return calculateTree110(features, 156);
    }
  }
  if (node === 157) return -0.23201167260283478;
  if (node === 158) return -0.015544011292116477;
  if (node === 78) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree110(features, 157);
    } else if (val <= 931.5000000000001) {
      return calculateTree110(features, 157);
    } else {
      return calculateTree110(features, 158);
    }
  }
  if (node === 38) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree110(features, 77);
    } else if (val <= 55.50000000000001) {
      return calculateTree110(features, 77);
    } else {
      return calculateTree110(features, 78);
    }
  }
  if (node === 18) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree110(features, 37);
    } else if (val <= 864.5000000000001) {
      return calculateTree110(features, 37);
    } else {
      return calculateTree110(features, 38);
    }
  }
  if (node === 8) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree110(features, 17);
    } else if (val <= 427.50000000000006) {
      return calculateTree110(features, 17);
    } else {
      return calculateTree110(features, 18);
    }
  }
  if (node === 3) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree110(features, 7);
    } else if (val <= 405.00000000000006) {
      return calculateTree110(features, 7);
    } else {
      return calculateTree110(features, 8);
    }
  }
  if (node === 4) return 0.14528224990116698;
  if (node === 1) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree110(features, 3);
    } else if (val <= 68.50000000000001) {
      return calculateTree110(features, 3);
    } else {
      return calculateTree110(features, 4);
    }
  }
  if (node === 2) return -0.0013377435682668034;
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree110(features, 1);
    } else if (val <= 113.50000000000001) {
      return calculateTree110(features, 1);
    } else {
      return calculateTree110(features, 2);
    }
  }
}
function calculateTree111(features, node = 0) {
  if (node === 1) return -0.0009194746321437565;
  if (node === 767) return -0.08014583629329212;
  if (node === 768) return 0.1985891818399493;
  if (node === 383) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree111(features, 767);
    } else if (val <= 143.50000000000003) {
      return calculateTree111(features, 767);
    } else {
      return calculateTree111(features, 768);
    }
  }
  if (node === 384) return -0.2577203290491674;
  if (node === 191) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree111(features, 383);
    } else if (val <= 908.0000000000001) {
      return calculateTree111(features, 383);
    } else {
      return calculateTree111(features, 384);
    }
  }
  if (node === 192) return -0.13627039850123976;
  if (node === 95) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree111(features, 191);
    } else if (val <= 8.500000000000002) {
      return calculateTree111(features, 191);
    } else {
      return calculateTree111(features, 192);
    }
  }
  if (node === 96) return 0.10974787782088154;
  if (node === 47) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree111(features, 95);
    } else if (val <= 28.500000000000004) {
      return calculateTree111(features, 95);
    } else {
      return calculateTree111(features, 96);
    }
  }
  if (node === 195) return -0.07790938527125578;
  if (node === 196) return 0.10856841698490423;
  if (node === 97) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree111(features, 195);
    } else if (val <= 19.500000000000004) {
      return calculateTree111(features, 195);
    } else {
      return calculateTree111(features, 196);
    }
  }
  if (node === 98) return -0.1988844468494726;
  if (node === 48) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree111(features, 97);
    } else if (val <= 28.500000000000004) {
      return calculateTree111(features, 97);
    } else {
      return calculateTree111(features, 98);
    }
  }
  if (node === 23) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree111(features, 47);
    } else if (val <= 75.50000000000001) {
      return calculateTree111(features, 47);
    } else {
      return calculateTree111(features, 48);
    }
  }
  if (node === 24) return -0.0450950718740652;
  if (node === 11) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree111(features, 23);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree111(features, 23);
    } else {
      return calculateTree111(features, 24);
    }
  }
  if (node === 12) return 0.024740765431298557;
  if (node === 5) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree111(features, 11);
    } else if (val <= 141.50000000000003) {
      return calculateTree111(features, 11);
    } else {
      return calculateTree111(features, 12);
    }
  }
  if (node === 111) return -0.0800509536480711;
  if (node === 225) return 0.20637906867041542;
  if (node === 226) return -0.06704134371954922;
  if (node === 112) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree111(features, 225);
    } else if (val <= 758.5000000000001) {
      return calculateTree111(features, 225);
    } else {
      return calculateTree111(features, 226);
    }
  }
  if (node === 55) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree111(features, 111);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree111(features, 111);
    } else {
      return calculateTree111(features, 112);
    }
  }
  if (node === 56) return -0.0247906004106054;
  if (node === 27) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree111(features, 55);
    } else if (val <= 12.500000000000002) {
      return calculateTree111(features, 55);
    } else {
      return calculateTree111(features, 56);
    }
  }
  if (node === 115) return -0.09501058494702126;
  if (node === 116) return 0.2045789466815938;
  if (node === 57) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree111(features, 115);
    } else if (val <= 271.00000000000006) {
      return calculateTree111(features, 115);
    } else {
      return calculateTree111(features, 116);
    }
  }
  if (node === 58) return -0.23336969293618345;
  if (node === 28) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree111(features, 57);
    } else if (val <= 511.00000000000006) {
      return calculateTree111(features, 57);
    } else {
      return calculateTree111(features, 58);
    }
  }
  if (node === 13) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree111(features, 27);
    } else if (val <= 496.00000000000006) {
      return calculateTree111(features, 27);
    } else {
      return calculateTree111(features, 28);
    }
  }
  if (node === 59) return 0.19203734375432793;
  if (node === 121) return 0.03671767463419368;
  if (node === 983) return -0.09698904754858842;
  if (node === 984) return 0.1389412875858208;
  if (node === 491) {
    const val = features['is_preference_grocery'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree111(features, 983);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree111(features, 983);
    } else {
      return calculateTree111(features, 984);
    }
  }
  if (node === 985) return 0.07506044860309909;
  if (node === 986) return -0.17544807888541092;
  if (node === 492) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree111(features, 985);
    } else if (val <= 234.50000000000003) {
      return calculateTree111(features, 985);
    } else {
      return calculateTree111(features, 986);
    }
  }
  if (node === 245) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree111(features, 491);
    } else if (val <= 752.5000000000001) {
      return calculateTree111(features, 491);
    } else {
      return calculateTree111(features, 492);
    }
  }
  if (node === 246) return 0.014674352123318569;
  if (node === 122) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree111(features, 245);
    } else if (val <= 15.500000000000002) {
      return calculateTree111(features, 245);
    } else {
      return calculateTree111(features, 246);
    }
  }
  if (node === 60) {
    const val = features['is_preference_women_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree111(features, 121);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree111(features, 121);
    } else {
      return calculateTree111(features, 122);
    }
  }
  if (node === 29) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree111(features, 59);
    } else if (val <= 507.50000000000006) {
      return calculateTree111(features, 59);
    } else {
      return calculateTree111(features, 60);
    }
  }
  if (node === 61) return 0.011702157674502356;
  if (node === 1007) return 0.3141029741333638;
  if (node === 2017) return -0.2419215654182987;
  if (node === 2018) return 0.06874831462494298;
  if (node === 1008) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree111(features, 2017);
    } else if (val <= 782.0000000000001) {
      return calculateTree111(features, 2017);
    } else {
      return calculateTree111(features, 2018);
    }
  }
  if (node === 503) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree111(features, 1007);
    } else if (val <= 602.5000000000001) {
      return calculateTree111(features, 1007);
    } else {
      return calculateTree111(features, 1008);
    }
  }
  if (node === 504) return -0.13535394131753423;
  if (node === 251) {
    const val = features['is_preference_sports'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree111(features, 503);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree111(features, 503);
    } else {
      return calculateTree111(features, 504);
    }
  }
  if (node === 252) return 0.12063865748422473;
  if (node === 125) {
    const val = features['is_preference_grocery'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree111(features, 251);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree111(features, 251);
    } else {
      return calculateTree111(features, 252);
    }
  }
  if (node === 126) return -0.04675766847954321;
  if (node === 62) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree111(features, 125);
    } else if (val <= 13.500000000000002) {
      return calculateTree111(features, 125);
    } else {
      return calculateTree111(features, 126);
    }
  }
  if (node === 30) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree111(features, 61);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree111(features, 61);
    } else {
      return calculateTree111(features, 62);
    }
  }
  if (node === 14) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree111(features, 29);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree111(features, 29);
    } else {
      return calculateTree111(features, 30);
    }
  }
  if (node === 6) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree111(features, 13);
    } else if (val <= 503.50000000000006) {
      return calculateTree111(features, 13);
    } else {
      return calculateTree111(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree111(features, 5);
    } else if (val <= 322.00000000000006) {
      return calculateTree111(features, 5);
    } else {
      return calculateTree111(features, 6);
    }
  }
  if (node === 0) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree111(features, 1);
    } else if (val <= 11.500000000000002) {
      return calculateTree111(features, 1);
    } else {
      return calculateTree111(features, 2);
    }
  }
}
function calculateTree112(features, node = 0) {
  if (node === 1) return -0.0011174342836233432;
  if (node === 47) return 0.05744129298806882;
  if (node === 97) return -0.20914420992094576;
  if (node === 791) return 0.013965607350730538;
  if (node === 792) return -0.09299142089833619;
  if (node === 395) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree112(features, 791);
    } else if (val <= 555.5000000000001) {
      return calculateTree112(features, 791);
    } else {
      return calculateTree112(features, 792);
    }
  }
  if (node === 396) return 0.053027192465221216;
  if (node === 197) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree112(features, 395);
    } else if (val <= 859.5000000000001) {
      return calculateTree112(features, 395);
    } else {
      return calculateTree112(features, 396);
    }
  }
  if (node === 1591) return 0.0306923636927709;
  if (node === 1592) return -0.2878528908908633;
  if (node === 795) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree112(features, 1591);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree112(features, 1591);
    } else {
      return calculateTree112(features, 1592);
    }
  }
  if (node === 796) return 0.3894270338885881;
  if (node === 397) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree112(features, 795);
    } else if (val <= 611.5000000000001) {
      return calculateTree112(features, 795);
    } else {
      return calculateTree112(features, 796);
    }
  }
  if (node === 398) return -0.3298565107720394;
  if (node === 198) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree112(features, 397);
    } else if (val <= 625.0000000000001) {
      return calculateTree112(features, 397);
    } else {
      return calculateTree112(features, 398);
    }
  }
  if (node === 98) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree112(features, 197);
    } else if (val <= 865.0000000000001) {
      return calculateTree112(features, 197);
    } else {
      return calculateTree112(features, 198);
    }
  }
  if (node === 48) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree112(features, 97);
    } else if (val <= 253.50000000000003) {
      return calculateTree112(features, 97);
    } else {
      return calculateTree112(features, 98);
    }
  }
  if (node === 23) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree112(features, 47);
    } else if (val <= 123.50000000000001) {
      return calculateTree112(features, 47);
    } else {
      return calculateTree112(features, 48);
    }
  }
  if (node === 199) return 0.09066165554047999;
  if (node === 200) return -0.022173104326143753;
  if (node === 99) {
    const val = features['is_preference_women_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree112(features, 199);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree112(features, 199);
    } else {
      return calculateTree112(features, 200);
    }
  }
  if (node === 100) return 0.4316857441172452;
  if (node === 49) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree112(features, 99);
    } else if (val <= 582.5000000000001) {
      return calculateTree112(features, 99);
    } else {
      return calculateTree112(features, 100);
    }
  }
  if (node === 203) return -0.03419702443026328;
  if (node === 204) return -0.3676541872650308;
  if (node === 101) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree112(features, 203);
    } else if (val <= 17.500000000000004) {
      return calculateTree112(features, 203);
    } else {
      return calculateTree112(features, 204);
    }
  }
  if (node === 102) return 0.0733527810951119;
  if (node === 50) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree112(features, 101);
    } else if (val <= 789.5000000000001) {
      return calculateTree112(features, 101);
    } else {
      return calculateTree112(features, 102);
    }
  }
  if (node === 24) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree112(features, 49);
    } else if (val <= 625.0000000000001) {
      return calculateTree112(features, 49);
    } else {
      return calculateTree112(features, 50);
    }
  }
  if (node === 11) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree112(features, 23);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree112(features, 23);
    } else {
      return calculateTree112(features, 24);
    }
  }
  if (node === 51) return 0.007067447846306564;
  if (node === 423) return 0.05249042753977752;
  if (node === 1699) return -0.1249368203484755;
  if (node === 1700) return 0.26917857763155756;
  if (node === 849) {
    const val = features['is_preference_men_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree112(features, 1699);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree112(features, 1699);
    } else {
      return calculateTree112(features, 1700);
    }
  }
  if (node === 850) return -0.35220062405926916;
  if (node === 424) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree112(features, 849);
    } else if (val <= 135.50000000000003) {
      return calculateTree112(features, 849);
    } else {
      return calculateTree112(features, 850);
    }
  }
  if (node === 211) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree112(features, 423);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree112(features, 423);
    } else {
      return calculateTree112(features, 424);
    }
  }
  if (node === 212) return 0.09654295587483581;
  if (node === 105) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree112(features, 211);
    } else if (val <= 217.50000000000003) {
      return calculateTree112(features, 211);
    } else {
      return calculateTree112(features, 212);
    }
  }
  if (node === 213) return -0.26313065636609195;
  if (node === 214) return -0.031453152873718776;
  if (node === 106) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree112(features, 213);
    } else if (val <= 308.00000000000006) {
      return calculateTree112(features, 213);
    } else {
      return calculateTree112(features, 214);
    }
  }
  if (node === 52) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree112(features, 105);
    } else if (val <= 294.50000000000006) {
      return calculateTree112(features, 105);
    } else {
      return calculateTree112(features, 106);
    }
  }
  if (node === 25) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree112(features, 51);
    } else if (val <= 13.500000000000002) {
      return calculateTree112(features, 51);
    } else {
      return calculateTree112(features, 52);
    }
  }
  if (node === 26) return 0.11944426851624029;
  if (node === 12) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree112(features, 25);
    } else if (val <= 28.500000000000004) {
      return calculateTree112(features, 25);
    } else {
      return calculateTree112(features, 26);
    }
  }
  if (node === 5) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree112(features, 11);
    } else if (val <= 887.0000000000001) {
      return calculateTree112(features, 11);
    } else {
      return calculateTree112(features, 12);
    }
  }
  if (node === 111) return 0.012408124382886696;
  if (node === 112) return -0.09781068019640603;
  if (node === 55) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree112(features, 111);
    } else if (val <= 398.00000000000006) {
      return calculateTree112(features, 111);
    } else {
      return calculateTree112(features, 112);
    }
  }
  if (node === 113) return -0.33606645143100516;
  if (node === 229) return 0.028255162031361337;
  if (node === 230) return 0.34355645804444834;
  if (node === 114) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree112(features, 229);
    } else if (val <= 1.5000000000000002) {
      return calculateTree112(features, 229);
    } else {
      return calculateTree112(features, 230);
    }
  }
  if (node === 56) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree112(features, 113);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree112(features, 113);
    } else {
      return calculateTree112(features, 114);
    }
  }
  if (node === 27) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree112(features, 55);
    } else if (val <= 857.5000000000001) {
      return calculateTree112(features, 55);
    } else {
      return calculateTree112(features, 56);
    }
  }
  if (node === 28) return -0.114670705695392;
  if (node === 13) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree112(features, 27);
    } else if (val <= 859.5000000000001) {
      return calculateTree112(features, 27);
    } else {
      return calculateTree112(features, 28);
    }
  }
  if (node === 14) return 0.0032959297648039;
  if (node === 6) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree112(features, 13);
    } else if (val <= 877.5000000000001) {
      return calculateTree112(features, 13);
    } else {
      return calculateTree112(features, 14);
    }
  }
  if (node === 2) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree112(features, 5);
    } else if (val <= 29.500000000000004) {
      return calculateTree112(features, 5);
    } else {
      return calculateTree112(features, 6);
    }
  }
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree112(features, 1);
    } else if (val <= 835.5000000000001) {
      return calculateTree112(features, 1);
    } else {
      return calculateTree112(features, 2);
    }
  }
}
function calculateTree113(features, node = 0) {
  if (node === 63) return 0.0536261737538465;
  if (node === 129) return -0.6291913606113315;
  if (node === 261) return -0.13842752841684577;
  if (node === 1051) return 0.033837551604558334;
  if (node === 1052) return -0.06129711355826797;
  if (node === 525) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree113(features, 1051);
    } else if (val <= 4.500000000000001) {
      return calculateTree113(features, 1051);
    } else {
      return calculateTree113(features, 1052);
    }
  }
  if (node === 1053) return 0.23387791657192197;
  if (node === 1054) return -0.028683840832342402;
  if (node === 526) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree113(features, 1053);
    } else if (val <= 9.500000000000002) {
      return calculateTree113(features, 1053);
    } else {
      return calculateTree113(features, 1054);
    }
  }
  if (node === 262) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree113(features, 525);
    } else if (val <= 176.50000000000003) {
      return calculateTree113(features, 525);
    } else {
      return calculateTree113(features, 526);
    }
  }
  if (node === 130) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree113(features, 261);
    } else if (val <= 32.50000000000001) {
      return calculateTree113(features, 261);
    } else {
      return calculateTree113(features, 262);
    }
  }
  if (node === 64) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree113(features, 129);
    } else if (val <= 21.500000000000004) {
      return calculateTree113(features, 129);
    } else {
      return calculateTree113(features, 130);
    }
  }
  if (node === 31) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree113(features, 63);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree113(features, 63);
    } else {
      return calculateTree113(features, 64);
    }
  }
  if (node === 32) return -0.35548584789431714;
  if (node === 15) {
    const val = features['is_preference_grocery'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree113(features, 31);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree113(features, 31);
    } else {
      return calculateTree113(features, 32);
    }
  }
  if (node === 67) return 0.2343058181040923;
  if (node === 68) return -0.035498313492733316;
  if (node === 33) {
    const val = features['is_preference_kids'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree113(features, 67);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree113(features, 67);
    } else {
      return calculateTree113(features, 68);
    }
  }
  if (node === 139) return 0.038021916000143215;
  if (node === 140) return 0.29844730475598674;
  if (node === 69) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree113(features, 139);
    } else if (val <= 40.50000000000001) {
      return calculateTree113(features, 139);
    } else {
      return calculateTree113(features, 140);
    }
  }
  if (node === 70) return 0.3729098149165099;
  if (node === 34) {
    const val = features['is_preference_grocery'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree113(features, 69);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree113(features, 69);
    } else {
      return calculateTree113(features, 70);
    }
  }
  if (node === 16) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree113(features, 33);
    } else if (val <= 197.50000000000003) {
      return calculateTree113(features, 33);
    } else {
      return calculateTree113(features, 34);
    }
  }
  if (node === 7) {
    const val = features['is_preference_beauty'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree113(features, 15);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree113(features, 15);
    } else {
      return calculateTree113(features, 16);
    }
  }
  if (node === 8) return -0.3044001046508284;
  if (node === 3) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree113(features, 7);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree113(features, 7);
    } else {
      return calculateTree113(features, 8);
    }
  }
  if (node === 9) return 0.0003111279890353843;
  if (node === 175) return 0.2838633807486631;
  if (node === 176) return 0.017723666803884323;
  if (node === 87) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree113(features, 175);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree113(features, 175);
    } else {
      return calculateTree113(features, 176);
    }
  }
  if (node === 88) return -0.08001855349073729;
  if (node === 43) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree113(features, 87);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree113(features, 87);
    } else {
      return calculateTree113(features, 88);
    }
  }
  if (node === 44) return 0.33768259904218506;
  if (node === 21) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree113(features, 43);
    } else if (val <= 15.500000000000002) {
      return calculateTree113(features, 43);
    } else {
      return calculateTree113(features, 44);
    }
  }
  if (node === 22) return 0.0036261294462603686;
  if (node === 10) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree113(features, 21);
    } else if (val <= 916.0000000000001) {
      return calculateTree113(features, 21);
    } else {
      return calculateTree113(features, 22);
    }
  }
  if (node === 4) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree113(features, 9);
    } else if (val <= 908.0000000000001) {
      return calculateTree113(features, 9);
    } else {
      return calculateTree113(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree113(features, 3);
    } else if (val <= 3.5000000000000004) {
      return calculateTree113(features, 3);
    } else {
      return calculateTree113(features, 4);
    }
  }
  if (node === 23) return -0.1971903471290694;
  if (node === 99) return -0.03146468811260741;
  if (node === 100) return -0.33086197912801485;
  if (node === 49) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree113(features, 99);
    } else if (val <= 15.500000000000002) {
      return calculateTree113(features, 99);
    } else {
      return calculateTree113(features, 100);
    }
  }
  if (node === 50) return 0.22100803662808904;
  if (node === 24) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree113(features, 49);
    } else if (val <= 41.00000000000001) {
      return calculateTree113(features, 49);
    } else {
      return calculateTree113(features, 50);
    }
  }
  if (node === 11) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree113(features, 23);
    } else if (val <= 551.5000000000001) {
      return calculateTree113(features, 23);
    } else {
      return calculateTree113(features, 24);
    }
  }
  if (node === 12) return -0.3141927474592635;
  if (node === 5) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree113(features, 11);
    } else if (val <= 693.0000000000001) {
      return calculateTree113(features, 11);
    } else {
      return calculateTree113(features, 12);
    }
  }
  if (node === 55) return 0.5764065210445483;
  if (node === 113) return 0.04807417715443656;
  if (node === 114) return 0.33179763453378697;
  if (node === 56) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree113(features, 113);
    } else if (val <= 320.00000000000006) {
      return calculateTree113(features, 113);
    } else {
      return calculateTree113(features, 114);
    }
  }
  if (node === 27) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree113(features, 55);
    } else if (val <= 15.500000000000002) {
      return calculateTree113(features, 55);
    } else {
      return calculateTree113(features, 56);
    }
  }
  if (node === 28) return -0.2725380337453296;
  if (node === 13) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree113(features, 27);
    } else if (val <= 40.50000000000001) {
      return calculateTree113(features, 27);
    } else {
      return calculateTree113(features, 28);
    }
  }
  if (node === 29) return 0.13490000608540748;
  if (node === 61) return -0.43957152102015545;
  if (node === 62) return -0.07345467355617441;
  if (node === 30) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree113(features, 61);
    } else if (val <= 37.50000000000001) {
      return calculateTree113(features, 61);
    } else {
      return calculateTree113(features, 62);
    }
  }
  if (node === 14) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree113(features, 29);
    } else if (val <= 479.50000000000006) {
      return calculateTree113(features, 29);
    } else {
      return calculateTree113(features, 30);
    }
  }
  if (node === 6) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree113(features, 13);
    } else if (val <= 7.500000000000001) {
      return calculateTree113(features, 13);
    } else {
      return calculateTree113(features, 14);
    }
  }
  if (node === 2) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree113(features, 5);
    } else if (val <= 2.5000000000000004) {
      return calculateTree113(features, 5);
    } else {
      return calculateTree113(features, 6);
    }
  }
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree113(features, 1);
    } else if (val <= 978.0000000000001) {
      return calculateTree113(features, 1);
    } else {
      return calculateTree113(features, 2);
    }
  }
}
function calculateTree114(features, node = 0) {
  if (node === 3) return 0.00044684541348912144;
  if (node === 19) return 0.05890862518463209;
  if (node === 20) return 0.29003144932357877;
  if (node === 9) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree114(features, 19);
    } else if (val <= 540.0000000000001) {
      return calculateTree114(features, 19);
    } else {
      return calculateTree114(features, 20);
    }
  }
  if (node === 10) return -0.31569698736197016;
  if (node === 4) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree114(features, 9);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree114(features, 9);
    } else {
      return calculateTree114(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree114(features, 3);
    } else if (val <= 954.5000000000001) {
      return calculateTree114(features, 3);
    } else {
      return calculateTree114(features, 4);
    }
  }
  if (node === 383) return -0.14390645018902792;
  if (node === 384) return 0.2030483837809963;
  if (node === 191) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree114(features, 383);
    } else if (val <= 993.5000000000001) {
      return calculateTree114(features, 383);
    } else {
      return calculateTree114(features, 384);
    }
  }
  if (node === 385) return 0.15696678537475808;
  if (node === 386) return 0.046530232026021155;
  if (node === 192) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree114(features, 385);
    } else if (val <= 29.500000000000004) {
      return calculateTree114(features, 385);
    } else {
      return calculateTree114(features, 386);
    }
  }
  if (node === 95) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree114(features, 191);
    } else if (val <= 20.500000000000004) {
      return calculateTree114(features, 191);
    } else {
      return calculateTree114(features, 192);
    }
  }
  if (node === 96) return 0.21014806265709957;
  if (node === 47) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree114(features, 95);
    } else if (val <= 500.50000000000006) {
      return calculateTree114(features, 95);
    } else {
      return calculateTree114(features, 96);
    }
  }
  if (node === 195) return -0.1583282473710077;
  if (node === 196) return 0.2317423245382815;
  if (node === 97) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree114(features, 195);
    } else if (val <= 32.50000000000001) {
      return calculateTree114(features, 195);
    } else {
      return calculateTree114(features, 196);
    }
  }
  if (node === 197) return -0.36838257700530014;
  if (node === 795) return 0.3270913504142706;
  if (node === 796) return -0.11246778201554401;
  if (node === 397) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree114(features, 795);
    } else if (val <= 34.50000000000001) {
      return calculateTree114(features, 795);
    } else {
      return calculateTree114(features, 796);
    }
  }
  if (node === 398) return -0.26872798580665164;
  if (node === 198) {
    const val = features['is_preference_kitchen'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree114(features, 397);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree114(features, 397);
    } else {
      return calculateTree114(features, 398);
    }
  }
  if (node === 98) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree114(features, 197);
    } else if (val <= 25.500000000000004) {
      return calculateTree114(features, 197);
    } else {
      return calculateTree114(features, 198);
    }
  }
  if (node === 48) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree114(features, 97);
    } else if (val <= 980.5000000000001) {
      return calculateTree114(features, 97);
    } else {
      return calculateTree114(features, 98);
    }
  }
  if (node === 23) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree114(features, 47);
    } else if (val <= 607.5000000000001) {
      return calculateTree114(features, 47);
    } else {
      return calculateTree114(features, 48);
    }
  }
  if (node === 99) return -0.29537387964905637;
  if (node === 201) return -0.07404947968984649;
  if (node === 202) return 0.23361948304723085;
  if (node === 100) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree114(features, 201);
    } else if (val <= 982.5000000000001) {
      return calculateTree114(features, 201);
    } else {
      return calculateTree114(features, 202);
    }
  }
  if (node === 49) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree114(features, 99);
    } else if (val <= 52.50000000000001) {
      return calculateTree114(features, 99);
    } else {
      return calculateTree114(features, 100);
    }
  }
  if (node === 101) return -0.4406664634048799;
  if (node === 205) return 0.06807649888816161;
  if (node === 206) return -0.2912960500883787;
  if (node === 102) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree114(features, 205);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree114(features, 205);
    } else {
      return calculateTree114(features, 206);
    }
  }
  if (node === 50) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree114(features, 101);
    } else if (val <= 3.5000000000000004) {
      return calculateTree114(features, 101);
    } else {
      return calculateTree114(features, 102);
    }
  }
  if (node === 24) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree114(features, 49);
    } else if (val <= 697.0000000000001) {
      return calculateTree114(features, 49);
    } else {
      return calculateTree114(features, 50);
    }
  }
  if (node === 11) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree114(features, 23);
    } else if (val <= 50.50000000000001) {
      return calculateTree114(features, 23);
    } else {
      return calculateTree114(features, 24);
    }
  }
  if (node === 51) return -0.014197249029503681;
  if (node === 52) return -0.21919580066602184;
  if (node === 25) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree114(features, 51);
    } else if (val <= 944.5000000000001) {
      return calculateTree114(features, 51);
    } else {
      return calculateTree114(features, 52);
    }
  }
  if (node === 215) return 0.2160412065028961;
  if (node === 216) return -0.08658957041095713;
  if (node === 107) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree114(features, 215);
    } else if (val <= 1.5000000000000002) {
      return calculateTree114(features, 215);
    } else {
      return calculateTree114(features, 216);
    }
  }
  if (node === 871) return -0.37950468916150293;
  if (node === 872) return 0.019019539250476156;
  if (node === 435) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree114(features, 871);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree114(features, 871);
    } else {
      return calculateTree114(features, 872);
    }
  }
  if (node === 436) return -0.3507145777715985;
  if (node === 217) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree114(features, 435);
    } else if (val <= 986.5000000000001) {
      return calculateTree114(features, 435);
    } else {
      return calculateTree114(features, 436);
    }
  }
  if (node === 218) return 0.04085750106113451;
  if (node === 108) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree114(features, 217);
    } else if (val <= 57.50000000000001) {
      return calculateTree114(features, 217);
    } else {
      return calculateTree114(features, 218);
    }
  }
  if (node === 53) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree114(features, 107);
    } else if (val <= 50.50000000000001) {
      return calculateTree114(features, 107);
    } else {
      return calculateTree114(features, 108);
    }
  }
  if (node === 54) return 0.09597902630649881;
  if (node === 26) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree114(features, 53);
    } else if (val <= 2.5000000000000004) {
      return calculateTree114(features, 53);
    } else {
      return calculateTree114(features, 54);
    }
  }
  if (node === 12) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree114(features, 25);
    } else if (val <= 48.50000000000001) {
      return calculateTree114(features, 25);
    } else {
      return calculateTree114(features, 26);
    }
  }
  if (node === 5) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree114(features, 11);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree114(features, 11);
    } else {
      return calculateTree114(features, 12);
    }
  }
  if (node === 13) return -0.061034545671657364;
  if (node === 14) return -0.26551498670343326;
  if (node === 6) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree114(features, 13);
    } else if (val <= 675.0000000000001) {
      return calculateTree114(features, 13);
    } else {
      return calculateTree114(features, 14);
    }
  }
  if (node === 2) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree114(features, 5);
    } else if (val <= 29.500000000000004) {
      return calculateTree114(features, 5);
    } else {
      return calculateTree114(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree114(features, 1);
    } else if (val <= 955.5000000000001) {
      return calculateTree114(features, 1);
    } else {
      return calculateTree114(features, 2);
    }
  }
}
function calculateTree115(features, node = 0) {
  if (node === 15) return -0.0002633416321964773;
  if (node === 271) return 0.24859529468156472;
  if (node === 545) return -0.3316505195402456;
  if (node === 546) return 0.04331660616893871;
  if (node === 272) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree115(features, 545);
    } else if (val <= 175.50000000000003) {
      return calculateTree115(features, 545);
    } else {
      return calculateTree115(features, 546);
    }
  }
  if (node === 135) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree115(features, 271);
    } else if (val <= 66.00000000000001) {
      return calculateTree115(features, 271);
    } else {
      return calculateTree115(features, 272);
    }
  }
  if (node === 547) return -0.09397122922522821;
  if (node === 548) return -0.4140775249064821;
  if (node === 273) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree115(features, 547);
    } else if (val <= 8.500000000000002) {
      return calculateTree115(features, 547);
    } else {
      return calculateTree115(features, 548);
    }
  }
  if (node === 274) return -0.5146656965256848;
  if (node === 136) {
    const val = features['is_preference_women_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree115(features, 273);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree115(features, 273);
    } else {
      return calculateTree115(features, 274);
    }
  }
  if (node === 67) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree115(features, 135);
    } else if (val <= 1.5000000000000002) {
      return calculateTree115(features, 135);
    } else {
      return calculateTree115(features, 136);
    }
  }
  if (node === 551) return 0.13827670466005534;
  if (node === 552) return 0.005357835240703901;
  if (node === 275) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree115(features, 551);
    } else if (val <= 144.50000000000003) {
      return calculateTree115(features, 551);
    } else {
      return calculateTree115(features, 552);
    }
  }
  if (node === 553) return 0.125864845864343;
  if (node === 1109) return -0.021409486959982686;
  if (node === 1110) return -0.424813432605398;
  if (node === 554) {
    const val = features['is_preference_women_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree115(features, 1109);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree115(features, 1109);
    } else {
      return calculateTree115(features, 1110);
    }
  }
  if (node === 276) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree115(features, 553);
    } else if (val <= 61.50000000000001) {
      return calculateTree115(features, 553);
    } else {
      return calculateTree115(features, 554);
    }
  }
  if (node === 137) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree115(features, 275);
    } else if (val <= 23.500000000000004) {
      return calculateTree115(features, 275);
    } else {
      return calculateTree115(features, 276);
    }
  }
  if (node === 138) return -0.3016824137155132;
  if (node === 68) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree115(features, 137);
    } else if (val <= 286.50000000000006) {
      return calculateTree115(features, 137);
    } else {
      return calculateTree115(features, 138);
    }
  }
  if (node === 33) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree115(features, 67);
    } else if (val <= 311.50000000000006) {
      return calculateTree115(features, 67);
    } else {
      return calculateTree115(features, 68);
    }
  }
  if (node === 139) return 0.2776035600073555;
  if (node === 140) return -0.04443882171015203;
  if (node === 69) {
    const val = features['is_preference_home'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree115(features, 139);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree115(features, 139);
    } else {
      return calculateTree115(features, 140);
    }
  }
  if (node === 70) return 0.023871003477157312;
  if (node === 34) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree115(features, 69);
    } else if (val <= 338.50000000000006) {
      return calculateTree115(features, 69);
    } else {
      return calculateTree115(features, 70);
    }
  }
  if (node === 16) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree115(features, 33);
    } else if (val <= 316.50000000000006) {
      return calculateTree115(features, 33);
    } else {
      return calculateTree115(features, 34);
    }
  }
  if (node === 7) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree115(features, 15);
    } else if (val <= 60.50000000000001) {
      return calculateTree115(features, 15);
    } else {
      return calculateTree115(features, 16);
    }
  }
  if (node === 143) return -0.09682374687989126;
  if (node === 144) return 0.13978187125271244;
  if (node === 71) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree115(features, 143);
    } else if (val <= 4.500000000000001) {
      return calculateTree115(features, 143);
    } else {
      return calculateTree115(features, 144);
    }
  }
  if (node === 72) return -0.27849881038208757;
  if (node === 35) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree115(features, 71);
    } else if (val <= 378.50000000000006) {
      return calculateTree115(features, 71);
    } else {
      return calculateTree115(features, 72);
    }
  }
  if (node === 36) return 0.1747834057693006;
  if (node === 17) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree115(features, 35);
    } else if (val <= 685.0000000000001) {
      return calculateTree115(features, 35);
    } else {
      return calculateTree115(features, 36);
    }
  }
  if (node === 18) return -0.39789738433996685;
  if (node === 8) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree115(features, 17);
    } else if (val <= 750.0000000000001) {
      return calculateTree115(features, 17);
    } else {
      return calculateTree115(features, 18);
    }
  }
  if (node === 3) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree115(features, 7);
    } else if (val <= 69.50000000000001) {
      return calculateTree115(features, 7);
    } else {
      return calculateTree115(features, 8);
    }
  }
  if (node === 39) return 0.002183579980163851;
  if (node === 81) return 0.0767078699465346;
  if (node === 82) return 0.28603734578310525;
  if (node === 40) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree115(features, 81);
    } else if (val <= 2.5000000000000004) {
      return calculateTree115(features, 81);
    } else {
      return calculateTree115(features, 82);
    }
  }
  if (node === 19) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree115(features, 39);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree115(features, 39);
    } else {
      return calculateTree115(features, 40);
    }
  }
  if (node === 20) return -0.10709639543150919;
  if (node === 9) {
    const val = features['is_preference_men_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree115(features, 19);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree115(features, 19);
    } else {
      return calculateTree115(features, 20);
    }
  }
  if (node === 21) return -0.27411958658870933;
  if (node === 22) return 0.02404540027528203;
  if (node === 10) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree115(features, 21);
    } else if (val <= 902.0000000000001) {
      return calculateTree115(features, 21);
    } else {
      return calculateTree115(features, 22);
    }
  }
  if (node === 4) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree115(features, 9);
    } else if (val <= 818.0000000000001) {
      return calculateTree115(features, 9);
    } else {
      return calculateTree115(features, 10);
    }
  }
  if (node === 1) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree115(features, 3);
    } else if (val <= 71.50000000000001) {
      return calculateTree115(features, 3);
    } else {
      return calculateTree115(features, 4);
    }
  }
  if (node === 23) return -0.12665339549519153;
  if (node === 24) return 0.1859454312105943;
  if (node === 11) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree115(features, 23);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree115(features, 23);
    } else {
      return calculateTree115(features, 24);
    }
  }
  if (node === 25) return -0.31078640884118863;
  if (node === 26) return -0.12232024457348321;
  if (node === 12) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree115(features, 25);
    } else if (val <= 1.5000000000000002) {
      return calculateTree115(features, 25);
    } else {
      return calculateTree115(features, 26);
    }
  }
  if (node === 5) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree115(features, 11);
    } else if (val <= 210.00000000000003) {
      return calculateTree115(features, 11);
    } else {
      return calculateTree115(features, 12);
    }
  }
  if (node === 6) return 0.1171285926391245;
  if (node === 2) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree115(features, 5);
    } else if (val <= 781.0000000000001) {
      return calculateTree115(features, 5);
    } else {
      return calculateTree115(features, 6);
    }
  }
  if (node === 0) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree115(features, 1);
    } else if (val <= 78.50000000000001) {
      return calculateTree115(features, 1);
    } else {
      return calculateTree115(features, 2);
    }
  }
}
function calculateTree116(features, node = 0) {
  if (node === 1) return -0.0009284019219031561;
  if (node === 95) return -0.13703844303819507;
  if (node === 193) return -0.2764030927717644;
  if (node === 194) return 0.06366317595723976;
  if (node === 96) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree116(features, 193);
    } else if (val <= 25.000000000000004) {
      return calculateTree116(features, 193);
    } else {
      return calculateTree116(features, 194);
    }
  }
  if (node === 47) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree116(features, 95);
    } else if (val <= 73.00000000000001) {
      return calculateTree116(features, 95);
    } else {
      return calculateTree116(features, 96);
    }
  }
  if (node === 391) return 0.02556015343016688;
  if (node === 392) return 0.25429515424680843;
  if (node === 195) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree116(features, 391);
    } else if (val <= 56.00000000000001) {
      return calculateTree116(features, 391);
    } else {
      return calculateTree116(features, 392);
    }
  }
  if (node === 1575) return -0.012610289723243967;
  if (node === 1576) return -0.20206525748415718;
  if (node === 787) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree116(features, 1575);
    } else if (val <= 353.00000000000006) {
      return calculateTree116(features, 1575);
    } else {
      return calculateTree116(features, 1576);
    }
  }
  if (node === 788) return 0.020432085501679943;
  if (node === 393) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree116(features, 787);
    } else if (val <= 422.00000000000006) {
      return calculateTree116(features, 787);
    } else {
      return calculateTree116(features, 788);
    }
  }
  if (node === 789) return -0.21544050897332842;
  if (node === 1581) return 0.03530787347475978;
  if (node === 1582) return -0.161412835786768;
  if (node === 790) {
    const val = features['is_preference_women_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree116(features, 1581);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree116(features, 1581);
    } else {
      return calculateTree116(features, 1582);
    }
  }
  if (node === 394) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree116(features, 789);
    } else if (val <= 863.0000000000001) {
      return calculateTree116(features, 789);
    } else {
      return calculateTree116(features, 790);
    }
  }
  if (node === 196) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree116(features, 393);
    } else if (val <= 811.5000000000001) {
      return calculateTree116(features, 393);
    } else {
      return calculateTree116(features, 394);
    }
  }
  if (node === 97) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree116(features, 195);
    } else if (val <= 143.50000000000003) {
      return calculateTree116(features, 195);
    } else {
      return calculateTree116(features, 196);
    }
  }
  if (node === 395) return -0.24964196716916348;
  if (node === 396) return -0.02074040204033446;
  if (node === 197) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree116(features, 395);
    } else if (val <= 660.0000000000001) {
      return calculateTree116(features, 395);
    } else {
      return calculateTree116(features, 396);
    }
  }
  if (node === 198) return -0.39100504144568043;
  if (node === 98) {
    const val = features['is_preference_men_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree116(features, 197);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree116(features, 197);
    } else {
      return calculateTree116(features, 198);
    }
  }
  if (node === 48) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree116(features, 97);
    } else if (val <= 903.5000000000001) {
      return calculateTree116(features, 97);
    } else {
      return calculateTree116(features, 98);
    }
  }
  if (node === 23) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree116(features, 47);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree116(features, 47);
    } else {
      return calculateTree116(features, 48);
    }
  }
  if (node === 799) return 0.33427865850585997;
  if (node === 1601) return -0.06547346375470015;
  if (node === 1602) return 0.08715319962302638;
  if (node === 800) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree116(features, 1601);
    } else if (val <= 141.50000000000003) {
      return calculateTree116(features, 1601);
    } else {
      return calculateTree116(features, 1602);
    }
  }
  if (node === 399) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree116(features, 799);
    } else if (val <= 56.00000000000001) {
      return calculateTree116(features, 799);
    } else {
      return calculateTree116(features, 800);
    }
  }
  if (node === 400) return -0.21058044753482458;
  if (node === 199) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree116(features, 399);
    } else if (val <= 897.0000000000001) {
      return calculateTree116(features, 399);
    } else {
      return calculateTree116(features, 400);
    }
  }
  if (node === 200) return 0.1819764158888616;
  if (node === 99) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree116(features, 199);
    } else if (val <= 908.0000000000001) {
      return calculateTree116(features, 199);
    } else {
      return calculateTree116(features, 200);
    }
  }
  if (node === 100) return -0.028677331999469327;
  if (node === 49) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree116(features, 99);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree116(features, 99);
    } else {
      return calculateTree116(features, 100);
    }
  }
  if (node === 815) return -0.22066380368940375;
  if (node === 1633) return 0.008524888803667132;
  if (node === 1634) return -0.17149358774211135;
  if (node === 816) {
    const val = features['is_preference_men_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree116(features, 1633);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree116(features, 1633);
    } else {
      return calculateTree116(features, 1634);
    }
  }
  if (node === 407) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree116(features, 815);
    } else if (val <= 234.50000000000003) {
      return calculateTree116(features, 815);
    } else {
      return calculateTree116(features, 816);
    }
  }
  if (node === 408) return -0.3630322178228847;
  if (node === 203) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree116(features, 407);
    } else if (val <= 957.0000000000001) {
      return calculateTree116(features, 407);
    } else {
      return calculateTree116(features, 408);
    }
  }
  if (node === 204) return 0.21417202247273764;
  if (node === 101) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree116(features, 203);
    } else if (val <= 944.5000000000001) {
      return calculateTree116(features, 203);
    } else {
      return calculateTree116(features, 204);
    }
  }
  if (node === 205) return -0.213835933171546;
  if (node === 206) return 0.04769029708544484;
  if (node === 102) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree116(features, 205);
    } else if (val <= 34.00000000000001) {
      return calculateTree116(features, 205);
    } else {
      return calculateTree116(features, 206);
    }
  }
  if (node === 50) {
    const val = features['is_preference_sports'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree116(features, 101);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree116(features, 101);
    } else {
      return calculateTree116(features, 102);
    }
  }
  if (node === 24) {
    const val = features['is_preference_kids'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree116(features, 49);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree116(features, 49);
    } else {
      return calculateTree116(features, 50);
    }
  }
  if (node === 11) {
    const val = features['is_preference_pets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree116(features, 23);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree116(features, 23);
    } else {
      return calculateTree116(features, 24);
    }
  }
  if (node === 25) return 0.010589951454584193;
  if (node === 26) return 0.24382088501919458;
  if (node === 12) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree116(features, 25);
    } else if (val <= 777.5000000000001) {
      return calculateTree116(features, 25);
    } else {
      return calculateTree116(features, 26);
    }
  }
  if (node === 5) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree116(features, 11);
    } else if (val <= 957.5000000000001) {
      return calculateTree116(features, 11);
    } else {
      return calculateTree116(features, 12);
    }
  }
  if (node === 13) return -0.014868386659589878;
  if (node === 14) return -0.3782867096541478;
  if (node === 6) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree116(features, 13);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree116(features, 13);
    } else {
      return calculateTree116(features, 14);
    }
  }
  if (node === 2) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree116(features, 5);
    } else if (val <= 975.0000000000001) {
      return calculateTree116(features, 5);
    } else {
      return calculateTree116(features, 6);
    }
  }
  if (node === 0) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree116(features, 1);
    } else if (val <= 28.500000000000004) {
      return calculateTree116(features, 1);
    } else {
      return calculateTree116(features, 2);
    }
  }
}
function calculateTree117(features, node = 0) {
  if (node === 15) return 0.0011361902733615452;
  if (node === 543) return 0.11633982411860178;
  if (node === 544) return -0.34067230337474586;
  if (node === 271) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree117(features, 543);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree117(features, 543);
    } else {
      return calculateTree117(features, 544);
    }
  }
  if (node === 272) return 0.17327917233422366;
  if (node === 135) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree117(features, 271);
    } else if (val <= 291.50000000000006) {
      return calculateTree117(features, 271);
    } else {
      return calculateTree117(features, 272);
    }
  }
  if (node === 547) return -0.08325100671736925;
  if (node === 548) return 0.24995753525201903;
  if (node === 273) {
    const val = features['is_preference_beauty'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree117(features, 547);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree117(features, 547);
    } else {
      return calculateTree117(features, 548);
    }
  }
  if (node === 274) return 0.1035771799640065;
  if (node === 136) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree117(features, 273);
    } else if (val <= 822.5000000000001) {
      return calculateTree117(features, 273);
    } else {
      return calculateTree117(features, 274);
    }
  }
  if (node === 67) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree117(features, 135);
    } else if (val <= 606.5000000000001) {
      return calculateTree117(features, 135);
    } else {
      return calculateTree117(features, 136);
    }
  }
  if (node === 137) return 0.3020533743918903;
  if (node === 277) return 0.11013276068941241;
  if (node === 278) return -0.17784247264947906;
  if (node === 138) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree117(features, 277);
    } else if (val <= 926.0000000000001) {
      return calculateTree117(features, 277);
    } else {
      return calculateTree117(features, 278);
    }
  }
  if (node === 68) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree117(features, 137);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree117(features, 137);
    } else {
      return calculateTree117(features, 138);
    }
  }
  if (node === 33) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree117(features, 67);
    } else if (val <= 2.5000000000000004) {
      return calculateTree117(features, 67);
    } else {
      return calculateTree117(features, 68);
    }
  }
  if (node === 279) return 0.1519874369957285;
  if (node === 280) return -0.1426060494657858;
  if (node === 139) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree117(features, 279);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree117(features, 279);
    } else {
      return calculateTree117(features, 280);
    }
  }
  if (node === 281) return -0.06363544358719375;
  if (node === 282) return 0.18588848777857941;
  if (node === 140) {
    const val = features['is_preference_pets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree117(features, 281);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree117(features, 281);
    } else {
      return calculateTree117(features, 282);
    }
  }
  if (node === 69) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree117(features, 139);
    } else if (val <= 15.500000000000002) {
      return calculateTree117(features, 139);
    } else {
      return calculateTree117(features, 140);
    }
  }
  if (node === 70) return -0.3461412784195645;
  if (node === 34) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree117(features, 69);
    } else if (val <= 908.5000000000001) {
      return calculateTree117(features, 69);
    } else {
      return calculateTree117(features, 70);
    }
  }
  if (node === 16) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree117(features, 33);
    } else if (val <= 37.50000000000001) {
      return calculateTree117(features, 33);
    } else {
      return calculateTree117(features, 34);
    }
  }
  if (node === 7) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree117(features, 15);
    } else if (val <= 656.5000000000001) {
      return calculateTree117(features, 15);
    } else {
      return calculateTree117(features, 16);
    }
  }
  if (node === 17) return -0.3359994475386258;
  if (node === 37) return -0.33162938821761073;
  if (node === 38) return 0.060428174311107946;
  if (node === 18) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree117(features, 37);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree117(features, 37);
    } else {
      return calculateTree117(features, 38);
    }
  }
  if (node === 8) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree117(features, 17);
    } else if (val <= 973.5000000000001) {
      return calculateTree117(features, 17);
    } else {
      return calculateTree117(features, 18);
    }
  }
  if (node === 3) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree117(features, 7);
    } else if (val <= 661.0000000000001) {
      return calculateTree117(features, 7);
    } else {
      return calculateTree117(features, 8);
    }
  }
  if (node === 79) return 0.01928012703431953;
  if (node === 161) return 0.014118322338241526;
  if (node === 162) return 0.3156550110813919;
  if (node === 80) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree117(features, 161);
    } else if (val <= 75.50000000000001) {
      return calculateTree117(features, 161);
    } else {
      return calculateTree117(features, 162);
    }
  }
  if (node === 39) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree117(features, 79);
    } else if (val <= 4.500000000000001) {
      return calculateTree117(features, 79);
    } else {
      return calculateTree117(features, 80);
    }
  }
  if (node === 40) return 0.25113692338082527;
  if (node === 19) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree117(features, 39);
    } else if (val <= 794.0000000000001) {
      return calculateTree117(features, 39);
    } else {
      return calculateTree117(features, 40);
    }
  }
  if (node === 20) return -0.2510262265294909;
  if (node === 9) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree117(features, 19);
    } else if (val <= 916.5000000000001) {
      return calculateTree117(features, 19);
    } else {
      return calculateTree117(features, 20);
    }
  }
  if (node === 43) return -0.15103157801974568;
  if (node === 89) return 0.17941944733433768;
  if (node === 90) return -0.20763242886040612;
  if (node === 44) {
    const val = features['is_preference_household'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree117(features, 89);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree117(features, 89);
    } else {
      return calculateTree117(features, 90);
    }
  }
  if (node === 21) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree117(features, 43);
    } else if (val <= 794.0000000000001) {
      return calculateTree117(features, 43);
    } else {
      return calculateTree117(features, 44);
    }
  }
  if (node === 22) return 0.17880411559533324;
  if (node === 10) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree117(features, 21);
    } else if (val <= 51.50000000000001) {
      return calculateTree117(features, 21);
    } else {
      return calculateTree117(features, 22);
    }
  }
  if (node === 4) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree117(features, 9);
    } else if (val <= 41.00000000000001) {
      return calculateTree117(features, 9);
    } else {
      return calculateTree117(features, 10);
    }
  }
  if (node === 1) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree117(features, 3);
    } else if (val <= 665.0000000000001) {
      return calculateTree117(features, 3);
    } else {
      return calculateTree117(features, 4);
    }
  }
  if (node === 47) return -0.19682643207136113;
  if (node === 48) return 0.730723880270595;
  if (node === 23) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree117(features, 47);
    } else if (val <= 654.0000000000001) {
      return calculateTree117(features, 47);
    } else {
      return calculateTree117(features, 48);
    }
  }
  if (node === 24) return -0.07071042370266671;
  if (node === 11) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree117(features, 23);
    } else if (val <= 782.0000000000001) {
      return calculateTree117(features, 23);
    } else {
      return calculateTree117(features, 24);
    }
  }
  if (node === 12) return -0.3578434062742984;
  if (node === 5) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree117(features, 11);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree117(features, 11);
    } else {
      return calculateTree117(features, 12);
    }
  }
  if (node === 6) return -0.0023117336746760614;
  if (node === 2) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree117(features, 5);
    } else if (val <= 672.0000000000001) {
      return calculateTree117(features, 5);
    } else {
      return calculateTree117(features, 6);
    }
  }
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree117(features, 1);
    } else if (val <= 668.0000000000001) {
      return calculateTree117(features, 1);
    } else {
      return calculateTree117(features, 2);
    }
  }
}
function calculateTree118(features, node = 0) {
  if (node === 31) return -0.23257506909534864;
  if (node === 32) return -0.03016805812284643;
  if (node === 15) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree118(features, 31);
    } else if (val <= 1.5000000000000002) {
      return calculateTree118(features, 31);
    } else {
      return calculateTree118(features, 32);
    }
  }
  if (node === 16) return 0.16020205104209867;
  if (node === 7) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree118(features, 15);
    } else if (val <= 25.500000000000004) {
      return calculateTree118(features, 15);
    } else {
      return calculateTree118(features, 16);
    }
  }
  if (node === 17) return 0.011911284893678223;
  if (node === 18) return -0.260479986787595;
  if (node === 8) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree118(features, 17);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree118(features, 17);
    } else {
      return calculateTree118(features, 18);
    }
  }
  if (node === 3) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree118(features, 7);
    } else if (val <= 33.50000000000001) {
      return calculateTree118(features, 7);
    } else {
      return calculateTree118(features, 8);
    }
  }
  if (node === 9) return 0.16255548379309084;
  if (node === 43) return -0.14283097081006796;
  if (node === 44) return 0.1751975198219702;
  if (node === 21) {
    const val = features['is_preference_kitchen'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree118(features, 43);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree118(features, 43);
    } else {
      return calculateTree118(features, 44);
    }
  }
  if (node === 22) return -0.17481661104101145;
  if (node === 10) {
    const val = features['is_preference_sports'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree118(features, 21);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree118(features, 21);
    } else {
      return calculateTree118(features, 22);
    }
  }
  if (node === 4) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree118(features, 9);
    } else if (val <= 22.500000000000004) {
      return calculateTree118(features, 9);
    } else {
      return calculateTree118(features, 10);
    }
  }
  if (node === 1) {
    const val = features['is_preference_women_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree118(features, 3);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree118(features, 3);
    } else {
      return calculateTree118(features, 4);
    }
  }
  if (node === 5) return -5.799730017289022e-5;
  if (node === 223) return -0.02832796433851639;
  if (node === 224) return -0.1097861317973174;
  if (node === 111) {
    const val = features['is_preference_grocery'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree118(features, 223);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree118(features, 223);
    } else {
      return calculateTree118(features, 224);
    }
  }
  if (node === 225) return 0.08537485227909873;
  if (node === 453) return -0.045842813107626605;
  if (node === 454) return 0.06734716338350286;
  if (node === 226) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree118(features, 453);
    } else if (val <= 414.50000000000006) {
      return calculateTree118(features, 453);
    } else {
      return calculateTree118(features, 454);
    }
  }
  if (node === 112) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree118(features, 225);
    } else if (val <= 39.50000000000001) {
      return calculateTree118(features, 225);
    } else {
      return calculateTree118(features, 226);
    }
  }
  if (node === 55) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree118(features, 111);
    } else if (val <= 628.5000000000001) {
      return calculateTree118(features, 111);
    } else {
      return calculateTree118(features, 112);
    }
  }
  if (node === 227) return -0.1013076027221955;
  if (node === 457) return 0.35309660336775095;
  if (node === 917) return 0.034882819844735936;
  if (node === 1837) return 0.15369770830789345;
  if (node === 1838) return -0.1695877714676138;
  if (node === 918) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree118(features, 1837);
    } else if (val <= 74.50000000000001) {
      return calculateTree118(features, 1837);
    } else {
      return calculateTree118(features, 1838);
    }
  }
  if (node === 458) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree118(features, 917);
    } else if (val <= 60.50000000000001) {
      return calculateTree118(features, 917);
    } else {
      return calculateTree118(features, 918);
    }
  }
  if (node === 228) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree118(features, 457);
    } else if (val <= 127.00000000000001) {
      return calculateTree118(features, 457);
    } else {
      return calculateTree118(features, 458);
    }
  }
  if (node === 113) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree118(features, 227);
    } else if (val <= 86.50000000000001) {
      return calculateTree118(features, 227);
    } else {
      return calculateTree118(features, 228);
    }
  }
  if (node === 229) return -0.20393807554733104;
  if (node === 230) return -0.00818793827689944;
  if (node === 114) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree118(features, 229);
    } else if (val <= 868.0000000000001) {
      return calculateTree118(features, 229);
    } else {
      return calculateTree118(features, 230);
    }
  }
  if (node === 56) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree118(features, 113);
    } else if (val <= 762.5000000000001) {
      return calculateTree118(features, 113);
    } else {
      return calculateTree118(features, 114);
    }
  }
  if (node === 27) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree118(features, 55);
    } else if (val <= 507.00000000000006) {
      return calculateTree118(features, 55);
    } else {
      return calculateTree118(features, 56);
    }
  }
  if (node === 463) return -0.13989711969232177;
  if (node === 464) return 0.057019765190404345;
  if (node === 231) {
    const val = features['is_preference_sports'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree118(features, 463);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree118(features, 463);
    } else {
      return calculateTree118(features, 464);
    }
  }
  if (node === 232) return 0.07104529585977348;
  if (node === 115) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree118(features, 231);
    } else if (val <= 902.0000000000001) {
      return calculateTree118(features, 231);
    } else {
      return calculateTree118(features, 232);
    }
  }
  if (node === 116) return 0.08190338868807608;
  if (node === 57) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree118(features, 115);
    } else if (val <= 71.50000000000001) {
      return calculateTree118(features, 115);
    } else {
      return calculateTree118(features, 116);
    }
  }
  if (node === 117) return -0.0852187833951295;
  if (node === 118) return 0.1469359119464925;
  if (node === 58) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree118(features, 117);
    } else if (val <= 1.5000000000000002) {
      return calculateTree118(features, 117);
    } else {
      return calculateTree118(features, 118);
    }
  }
  if (node === 28) {
    const val = features['is_preference_men_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree118(features, 57);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree118(features, 57);
    } else {
      return calculateTree118(features, 58);
    }
  }
  if (node === 13) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree118(features, 27);
    } else if (val <= 734.0000000000001) {
      return calculateTree118(features, 27);
    } else {
      return calculateTree118(features, 28);
    }
  }
  if (node === 29) return 0.11129894644466892;
  if (node === 61) return 0.03025385455879291;
  if (node === 125) return -0.43385472623088256;
  if (node === 126) return -0.05158240240144294;
  if (node === 62) {
    const val = features['is_preference_kitchen'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree118(features, 125);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree118(features, 125);
    } else {
      return calculateTree118(features, 126);
    }
  }
  if (node === 30) {
    const val = features['is_preference_pets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree118(features, 61);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree118(features, 61);
    } else {
      return calculateTree118(features, 62);
    }
  }
  if (node === 14) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree118(features, 29);
    } else if (val <= 948.0000000000001) {
      return calculateTree118(features, 29);
    } else {
      return calculateTree118(features, 30);
    }
  }
  if (node === 6) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree118(features, 13);
    } else if (val <= 898.5000000000001) {
      return calculateTree118(features, 13);
    } else {
      return calculateTree118(features, 14);
    }
  }
  if (node === 2) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree118(features, 5);
    } else if (val <= 55.50000000000001) {
      return calculateTree118(features, 5);
    } else {
      return calculateTree118(features, 6);
    }
  }
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree118(features, 1);
    } else if (val <= 3.0000000000000004) {
      return calculateTree118(features, 1);
    } else {
      return calculateTree118(features, 2);
    }
  }
}
function calculateTree119(features, node = 0) {
  if (node === 7) return -0.0173471038261666;
  if (node === 8) return -0.3583226885452257;
  if (node === 3) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree119(features, 7);
    } else if (val <= 6.000000000000001) {
      return calculateTree119(features, 7);
    } else {
      return calculateTree119(features, 8);
    }
  }
  if (node === 19) return 0.010867026988728946;
  if (node === 20) return 0.08829087620127968;
  if (node === 9) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree119(features, 19);
    } else if (val <= 181.50000000000003) {
      return calculateTree119(features, 19);
    } else {
      return calculateTree119(features, 20);
    }
  }
  if (node === 21) return -0.3526466137312194;
  if (node === 367) return -0.026113272636664155;
  if (node === 368) return -0.20771340167102317;
  if (node === 183) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree119(features, 367);
    } else if (val <= 225.50000000000003) {
      return calculateTree119(features, 367);
    } else {
      return calculateTree119(features, 368);
    }
  }
  if (node === 184) return 0.08343944916968364;
  if (node === 91) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree119(features, 183);
    } else if (val <= 236.00000000000003) {
      return calculateTree119(features, 183);
    } else {
      return calculateTree119(features, 184);
    }
  }
  if (node === 92) return -0.349240511057932;
  if (node === 45) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree119(features, 91);
    } else if (val <= 265.00000000000006) {
      return calculateTree119(features, 91);
    } else {
      return calculateTree119(features, 92);
    }
  }
  if (node === 751) return -0.0020641802419914365;
  if (node === 752) return 0.23920716704570583;
  if (node === 375) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree119(features, 751);
    } else if (val <= 286.50000000000006) {
      return calculateTree119(features, 751);
    } else {
      return calculateTree119(features, 752);
    }
  }
  if (node === 376) return -0.33135009700846385;
  if (node === 187) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree119(features, 375);
    } else if (val <= 288.00000000000006) {
      return calculateTree119(features, 375);
    } else {
      return calculateTree119(features, 376);
    }
  }
  if (node === 755) return 0.27202400045338077;
  if (node === 756) return 0.10478665808636385;
  if (node === 377) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree119(features, 755);
    } else if (val <= 27.500000000000004) {
      return calculateTree119(features, 755);
    } else {
      return calculateTree119(features, 756);
    }
  }
  if (node === 757) return 0.02327839001507725;
  if (node === 758) return 0.20769728340985996;
  if (node === 378) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree119(features, 757);
    } else if (val <= 310.00000000000006) {
      return calculateTree119(features, 757);
    } else {
      return calculateTree119(features, 758);
    }
  }
  if (node === 188) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree119(features, 377);
    } else if (val <= 300.50000000000006) {
      return calculateTree119(features, 377);
    } else {
      return calculateTree119(features, 378);
    }
  }
  if (node === 93) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree119(features, 187);
    } else if (val <= 290.50000000000006) {
      return calculateTree119(features, 187);
    } else {
      return calculateTree119(features, 188);
    }
  }
  if (node === 759) return -0.3412266269172479;
  if (node === 760) return -0.013383778750619437;
  if (node === 379) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree119(features, 759);
    } else if (val <= 316.50000000000006) {
      return calculateTree119(features, 759);
    } else {
      return calculateTree119(features, 760);
    }
  }
  if (node === 761) return -0.23987878339675386;
  if (node === 762) return -0.034177668129750455;
  if (node === 380) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree119(features, 761);
    } else if (val <= 363.00000000000006) {
      return calculateTree119(features, 761);
    } else {
      return calculateTree119(features, 762);
    }
  }
  if (node === 189) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree119(features, 379);
    } else if (val <= 353.00000000000006) {
      return calculateTree119(features, 379);
    } else {
      return calculateTree119(features, 380);
    }
  }
  if (node === 763) return 0.04100046691329498;
  if (node === 1529) return -0.03553155619194003;
  if (node === 1530) return 0.0022972145963719324;
  if (node === 764) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree119(features, 1529);
    } else if (val <= 467.50000000000006) {
      return calculateTree119(features, 1529);
    } else {
      return calculateTree119(features, 1530);
    }
  }
  if (node === 381) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree119(features, 763);
    } else if (val <= 409.50000000000006) {
      return calculateTree119(features, 763);
    } else {
      return calculateTree119(features, 764);
    }
  }
  if (node === 382) return -0.2066529438664365;
  if (node === 190) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree119(features, 381);
    } else if (val <= 975.0000000000001) {
      return calculateTree119(features, 381);
    } else {
      return calculateTree119(features, 382);
    }
  }
  if (node === 94) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree119(features, 189);
    } else if (val <= 381.50000000000006) {
      return calculateTree119(features, 189);
    } else {
      return calculateTree119(features, 190);
    }
  }
  if (node === 46) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree119(features, 93);
    } else if (val <= 312.50000000000006) {
      return calculateTree119(features, 93);
    } else {
      return calculateTree119(features, 94);
    }
  }
  if (node === 22) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree119(features, 45);
    } else if (val <= 283.50000000000006) {
      return calculateTree119(features, 45);
    } else {
      return calculateTree119(features, 46);
    }
  }
  if (node === 10) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree119(features, 21);
    } else if (val <= 201.50000000000003) {
      return calculateTree119(features, 21);
    } else {
      return calculateTree119(features, 22);
    }
  }
  if (node === 4) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree119(features, 9);
    } else if (val <= 194.00000000000003) {
      return calculateTree119(features, 9);
    } else {
      return calculateTree119(features, 10);
    }
  }
  if (node === 1) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree119(features, 3);
    } else if (val <= 12.000000000000002) {
      return calculateTree119(features, 3);
    } else {
      return calculateTree119(features, 4);
    }
  }
  if (node === 11) return 0.01216123871079244;
  if (node === 12) return 0.0916274082955409;
  if (node === 5) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree119(features, 11);
    } else if (val <= 6.000000000000001) {
      return calculateTree119(features, 11);
    } else {
      return calculateTree119(features, 12);
    }
  }
  if (node === 27) return 0.0017762939595129322;
  if (node === 57) return -0.07596070806348588;
  if (node === 58) return 0.05445878642191447;
  if (node === 28) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree119(features, 57);
    } else if (val <= 43.50000000000001) {
      return calculateTree119(features, 57);
    } else {
      return calculateTree119(features, 58);
    }
  }
  if (node === 13) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree119(features, 27);
    } else if (val <= 34.00000000000001) {
      return calculateTree119(features, 27);
    } else {
      return calculateTree119(features, 28);
    }
  }
  if (node === 29) return 0.09579748998861122;
  if (node === 61) return -0.10371719910767173;
  if (node === 62) return 0.0008095059154602819;
  if (node === 30) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree119(features, 61);
    } else if (val <= 69.50000000000001) {
      return calculateTree119(features, 61);
    } else {
      return calculateTree119(features, 62);
    }
  }
  if (node === 14) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree119(features, 29);
    } else if (val <= 44.50000000000001) {
      return calculateTree119(features, 29);
    } else {
      return calculateTree119(features, 30);
    }
  }
  if (node === 6) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree119(features, 13);
    } else if (val <= 39.50000000000001) {
      return calculateTree119(features, 13);
    } else {
      return calculateTree119(features, 14);
    }
  }
  if (node === 2) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree119(features, 5);
    } else if (val <= 12.000000000000002) {
      return calculateTree119(features, 5);
    } else {
      return calculateTree119(features, 6);
    }
  }
  if (node === 0) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree119(features, 1);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree119(features, 1);
    } else {
      return calculateTree119(features, 2);
    }
  }
}
function calculateTree120(features, node = 0) {
  if (node === 7) return 0.0006187326871488036;
  if (node === 35) return 0.1269530337506531;
  if (node === 36) return -0.1454022606191828;
  if (node === 17) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree120(features, 35);
    } else if (val <= 43.50000000000001) {
      return calculateTree120(features, 35);
    } else {
      return calculateTree120(features, 36);
    }
  }
  if (node === 18) return -0.26929880074051016;
  if (node === 8) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree120(features, 17);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree120(features, 17);
    } else {
      return calculateTree120(features, 18);
    }
  }
  if (node === 3) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree120(features, 7);
    } else if (val <= 822.5000000000001) {
      return calculateTree120(features, 7);
    } else {
      return calculateTree120(features, 8);
    }
  }
  if (node === 9) return 0.11850723919783404;
  if (node === 10) return -0.15846647197309785;
  if (node === 4) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree120(features, 9);
    } else if (val <= 46.50000000000001) {
      return calculateTree120(features, 9);
    } else {
      return calculateTree120(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree120(features, 3);
    } else if (val <= 834.5000000000001) {
      return calculateTree120(features, 3);
    } else {
      return calculateTree120(features, 4);
    }
  }
  if (node === 95) return -0.03123976430532631;
  if (node === 96) return -0.28108484230343955;
  if (node === 47) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree120(features, 95);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree120(features, 95);
    } else {
      return calculateTree120(features, 96);
    }
  }
  if (node === 195) return -0.11177138223490189;
  if (node === 393) return 0.37355822208789835;
  if (node === 1579) return 0.20233799073254902;
  if (node === 1580) return -0.1498372763312632;
  if (node === 789) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree120(features, 1579);
    } else if (val <= 40.50000000000001) {
      return calculateTree120(features, 1579);
    } else {
      return calculateTree120(features, 1580);
    }
  }
  if (node === 790) return 0.3460429542424;
  if (node === 394) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree120(features, 789);
    } else if (val <= 15.500000000000002) {
      return calculateTree120(features, 789);
    } else {
      return calculateTree120(features, 790);
    }
  }
  if (node === 196) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree120(features, 393);
    } else if (val <= 1.5000000000000002) {
      return calculateTree120(features, 393);
    } else {
      return calculateTree120(features, 394);
    }
  }
  if (node === 97) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree120(features, 195);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree120(features, 195);
    } else {
      return calculateTree120(features, 196);
    }
  }
  if (node === 1583) return -0.022497585226765333;
  if (node === 1584) return -0.3581581848369414;
  if (node === 791) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree120(features, 1583);
    } else if (val <= 864.5000000000001) {
      return calculateTree120(features, 1583);
    } else {
      return calculateTree120(features, 1584);
    }
  }
  if (node === 1585) return 0.06377935566207367;
  if (node === 1586) return 0.34603945683506254;
  if (node === 792) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree120(features, 1585);
    } else if (val <= 15.500000000000002) {
      return calculateTree120(features, 1585);
    } else {
      return calculateTree120(features, 1586);
    }
  }
  if (node === 395) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree120(features, 791);
    } else if (val <= 866.5000000000001) {
      return calculateTree120(features, 791);
    } else {
      return calculateTree120(features, 792);
    }
  }
  if (node === 396) return -0.33796342004485347;
  if (node === 197) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree120(features, 395);
    } else if (val <= 868.0000000000001) {
      return calculateTree120(features, 395);
    } else {
      return calculateTree120(features, 396);
    }
  }
  if (node === 198) return 0.12686733904729244;
  if (node === 98) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree120(features, 197);
    } else if (val <= 882.5000000000001) {
      return calculateTree120(features, 197);
    } else {
      return calculateTree120(features, 198);
    }
  }
  if (node === 48) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree120(features, 97);
    } else if (val <= 852.5000000000001) {
      return calculateTree120(features, 97);
    } else {
      return calculateTree120(features, 98);
    }
  }
  if (node === 23) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree120(features, 47);
    } else if (val <= 846.5000000000001) {
      return calculateTree120(features, 47);
    } else {
      return calculateTree120(features, 48);
    }
  }
  if (node === 24) return -0.25717955293090605;
  if (node === 11) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree120(features, 23);
    } else if (val <= 888.0000000000001) {
      return calculateTree120(features, 23);
    } else {
      return calculateTree120(features, 24);
    }
  }
  if (node === 12) return -0.0015119777903932622;
  if (node === 5) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree120(features, 11);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree120(features, 11);
    } else {
      return calculateTree120(features, 12);
    }
  }
  if (node === 55) return -0.32521170430128216;
  if (node === 113) return 0.20843271252034695;
  if (node === 229) return -0.38760092771342064;
  if (node === 230) return -0.004442850675942916;
  if (node === 114) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree120(features, 229);
    } else if (val <= 908.5000000000001) {
      return calculateTree120(features, 229);
    } else {
      return calculateTree120(features, 230);
    }
  }
  if (node === 56) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree120(features, 113);
    } else if (val <= 905.5000000000001) {
      return calculateTree120(features, 113);
    } else {
      return calculateTree120(features, 114);
    }
  }
  if (node === 27) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree120(features, 55);
    } else if (val <= 903.5000000000001) {
      return calculateTree120(features, 55);
    } else {
      return calculateTree120(features, 56);
    }
  }
  if (node === 231) return 0.016588553168274844;
  if (node === 232) return 0.20442408372293194;
  if (node === 115) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree120(features, 231);
    } else if (val <= 35.50000000000001) {
      return calculateTree120(features, 231);
    } else {
      return calculateTree120(features, 232);
    }
  }
  if (node === 116) return -0.4022088303918635;
  if (node === 57) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree120(features, 115);
    } else if (val <= 903.5000000000001) {
      return calculateTree120(features, 115);
    } else {
      return calculateTree120(features, 116);
    }
  }
  if (node === 235) return 0.006631368288038247;
  if (node === 236) return 0.1424380829381581;
  if (node === 117) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree120(features, 235);
    } else if (val <= 908.5000000000001) {
      return calculateTree120(features, 235);
    } else {
      return calculateTree120(features, 236);
    }
  }
  if (node === 118) return 0.20155325948729272;
  if (node === 58) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree120(features, 117);
    } else if (val <= 697.0000000000001) {
      return calculateTree120(features, 117);
    } else {
      return calculateTree120(features, 118);
    }
  }
  if (node === 28) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree120(features, 57);
    } else if (val <= 905.5000000000001) {
      return calculateTree120(features, 57);
    } else {
      return calculateTree120(features, 58);
    }
  }
  if (node === 13) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree120(features, 27);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree120(features, 27);
    } else {
      return calculateTree120(features, 28);
    }
  }
  if (node === 14) return -0.0021081192514252017;
  if (node === 6) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree120(features, 13);
    } else if (val <= 916.5000000000001) {
      return calculateTree120(features, 13);
    } else {
      return calculateTree120(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree120(features, 5);
    } else if (val <= 902.0000000000001) {
      return calculateTree120(features, 5);
    } else {
      return calculateTree120(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree120(features, 1);
    } else if (val <= 835.5000000000001) {
      return calculateTree120(features, 1);
    } else {
      return calculateTree120(features, 2);
    }
  }
}
function calculateTree121(features, node = 0) {
  if (node === 3) return -0.0028670264832738623;
  if (node === 39) return -0.3681516980838308;
  if (node === 81) return 0.1126555814680179;
  if (node === 82) return -0.33974856054040387;
  if (node === 40) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree121(features, 81);
    } else if (val <= 734.0000000000001) {
      return calculateTree121(features, 81);
    } else {
      return calculateTree121(features, 82);
    }
  }
  if (node === 19) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree121(features, 39);
    } else if (val <= 682.5000000000001) {
      return calculateTree121(features, 39);
    } else {
      return calculateTree121(features, 40);
    }
  }
  if (node === 83) return -0.3436546175237782;
  if (node === 84) return 0.09213914034508247;
  if (node === 41) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree121(features, 83);
    } else if (val <= 253.00000000000003) {
      return calculateTree121(features, 83);
    } else {
      return calculateTree121(features, 84);
    }
  }
  if (node === 85) return -0.3625250671123122;
  if (node === 173) return 0.43729305296625604;
  if (node === 349) return 0.10796369099373733;
  if (node === 350) return -0.18695921368654925;
  if (node === 174) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree121(features, 349);
    } else if (val <= 803.0000000000001) {
      return calculateTree121(features, 349);
    } else {
      return calculateTree121(features, 350);
    }
  }
  if (node === 86) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree121(features, 173);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree121(features, 173);
    } else {
      return calculateTree121(features, 174);
    }
  }
  if (node === 42) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree121(features, 85);
    } else if (val <= 800.5000000000001) {
      return calculateTree121(features, 85);
    } else {
      return calculateTree121(features, 86);
    }
  }
  if (node === 20) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree121(features, 41);
    } else if (val <= 682.5000000000001) {
      return calculateTree121(features, 41);
    } else {
      return calculateTree121(features, 42);
    }
  }
  if (node === 9) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree121(features, 19);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree121(features, 19);
    } else {
      return calculateTree121(features, 20);
    }
  }
  if (node === 175) return 0.07413971290292513;
  if (node === 176) return -0.23971495115577318;
  if (node === 87) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree121(features, 175);
    } else if (val <= 409.50000000000006) {
      return calculateTree121(features, 175);
    } else {
      return calculateTree121(features, 176);
    }
  }
  if (node === 355) return 0.19367238892120944;
  if (node === 356) return 0.7931590452830114;
  if (node === 177) {
    const val = features['is_preference_personal_care'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree121(features, 355);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree121(features, 355);
    } else {
      return calculateTree121(features, 356);
    }
  }
  if (node === 357) return 0.06961854457586096;
  if (node === 358) return -0.13766618873154884;
  if (node === 178) {
    const val = features['is_preference_home'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree121(features, 357);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree121(features, 357);
    } else {
      return calculateTree121(features, 358);
    }
  }
  if (node === 88) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree121(features, 177);
    } else if (val <= 808.5000000000001) {
      return calculateTree121(features, 177);
    } else {
      return calculateTree121(features, 178);
    }
  }
  if (node === 43) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree121(features, 87);
    } else if (val <= 672.0000000000001) {
      return calculateTree121(features, 87);
    } else {
      return calculateTree121(features, 88);
    }
  }
  if (node === 359) return -0.29890264608900524;
  if (node === 360) return -0.010350330578234245;
  if (node === 179) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree121(features, 359);
    } else if (val <= 443.50000000000006) {
      return calculateTree121(features, 359);
    } else {
      return calculateTree121(features, 360);
    }
  }
  if (node === 180) return 0.19736783188219192;
  if (node === 89) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree121(features, 179);
    } else if (val <= 643.5000000000001) {
      return calculateTree121(features, 179);
    } else {
      return calculateTree121(features, 180);
    }
  }
  if (node === 90) return -0.3299956394559874;
  if (node === 44) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree121(features, 89);
    } else if (val <= 672.0000000000001) {
      return calculateTree121(features, 89);
    } else {
      return calculateTree121(features, 90);
    }
  }
  if (node === 21) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree121(features, 43);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree121(features, 43);
    } else {
      return calculateTree121(features, 44);
    }
  }
  if (node === 22) return 0.0007771492787905865;
  if (node === 10) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree121(features, 21);
    } else if (val <= 455.50000000000006) {
      return calculateTree121(features, 21);
    } else {
      return calculateTree121(features, 22);
    }
  }
  if (node === 4) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree121(features, 9);
    } else if (val <= 427.50000000000006) {
      return calculateTree121(features, 9);
    } else {
      return calculateTree121(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree121(features, 3);
    } else if (val <= 417.00000000000006) {
      return calculateTree121(features, 3);
    } else {
      return calculateTree121(features, 4);
    }
  }
  if (node === 11) return 1.0082957055428783;
  if (node === 25) return -0.28725133485811155;
  if (node === 53) return -0.30847194120811816;
  if (node === 54) return 0.7538399150172458;
  if (node === 26) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree121(features, 53);
    } else if (val <= 978.0000000000001) {
      return calculateTree121(features, 53);
    } else {
      return calculateTree121(features, 54);
    }
  }
  if (node === 12) {
    const val = features['is_preference_kids'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree121(features, 25);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree121(features, 25);
    } else {
      return calculateTree121(features, 26);
    }
  }
  if (node === 5) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree121(features, 11);
    } else if (val <= 150.50000000000003) {
      return calculateTree121(features, 11);
    } else {
      return calculateTree121(features, 12);
    }
  }
  if (node === 27) return 0.02258618133861918;
  if (node === 57) return -0.2521557236542945;
  if (node === 58) return 0.01744377249062724;
  if (node === 28) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree121(features, 57);
    } else if (val <= 295.00000000000006) {
      return calculateTree121(features, 57);
    } else {
      return calculateTree121(features, 58);
    }
  }
  if (node === 13) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree121(features, 27);
    } else if (val <= 234.50000000000003) {
      return calculateTree121(features, 27);
    } else {
      return calculateTree121(features, 28);
    }
  }
  if (node === 29) return 0.2315281490898874;
  if (node === 123) return 0.06738162740141872;
  if (node === 124) return 0.2606873092626636;
  if (node === 61) {
    const val = features['is_preference_grocery'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree121(features, 123);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree121(features, 123);
    } else {
      return calculateTree121(features, 124);
    }
  }
  if (node === 62) return -0.02418711916662684;
  if (node === 30) {
    const val = features['is_preference_pets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree121(features, 61);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree121(features, 61);
    } else {
      return calculateTree121(features, 62);
    }
  }
  if (node === 14) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree121(features, 29);
    } else if (val <= 511.00000000000006) {
      return calculateTree121(features, 29);
    } else {
      return calculateTree121(features, 30);
    }
  }
  if (node === 6) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree121(features, 13);
    } else if (val <= 437.50000000000006) {
      return calculateTree121(features, 13);
    } else {
      return calculateTree121(features, 14);
    }
  }
  if (node === 2) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree121(features, 5);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree121(features, 5);
    } else {
      return calculateTree121(features, 6);
    }
  }
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree121(features, 1);
    } else if (val <= 975.0000000000001) {
      return calculateTree121(features, 1);
    } else {
      return calculateTree121(features, 2);
    }
  }
}
function calculateTree122(features, node = 0) {
  if (node === 127) return -0.007139699019761914;
  if (node === 128) return -0.3406877430468324;
  if (node === 63) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree122(features, 127);
    } else if (val <= 6.000000000000001) {
      return calculateTree122(features, 127);
    } else {
      return calculateTree122(features, 128);
    }
  }
  if (node === 64) return 0.006141930655927939;
  if (node === 31) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree122(features, 63);
    } else if (val <= 12.000000000000002) {
      return calculateTree122(features, 63);
    } else {
      return calculateTree122(features, 64);
    }
  }
  if (node === 32) return -0.13819081826432372;
  if (node === 15) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree122(features, 31);
    } else if (val <= 39.50000000000001) {
      return calculateTree122(features, 31);
    } else {
      return calculateTree122(features, 32);
    }
  }
  if (node === 33) return 0.020219691798933185;
  if (node === 69) return 0.06310581330693228;
  if (node === 70) return 0.28188632974951466;
  if (node === 34) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree122(features, 69);
    } else if (val <= 172.50000000000003) {
      return calculateTree122(features, 69);
    } else {
      return calculateTree122(features, 70);
    }
  }
  if (node === 16) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree122(features, 33);
    } else if (val <= 28.500000000000004) {
      return calculateTree122(features, 33);
    } else {
      return calculateTree122(features, 34);
    }
  }
  if (node === 7) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree122(features, 15);
    } else if (val <= 44.50000000000001) {
      return calculateTree122(features, 15);
    } else {
      return calculateTree122(features, 16);
    }
  }
  if (node === 287) return 0.02990878319492947;
  if (node === 577) return -0.020234490919132125;
  if (node === 1157) return 0.1205089797083213;
  if (node === 1158) return -0.06485471687409708;
  if (node === 578) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree122(features, 1157);
    } else if (val <= 13.500000000000002) {
      return calculateTree122(features, 1157);
    } else {
      return calculateTree122(features, 1158);
    }
  }
  if (node === 288) {
    const val = features['is_preference_grocery'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree122(features, 577);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree122(features, 577);
    } else {
      return calculateTree122(features, 578);
    }
  }
  if (node === 143) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree122(features, 287);
    } else if (val <= 12.000000000000002) {
      return calculateTree122(features, 287);
    } else {
      return calculateTree122(features, 288);
    }
  }
  if (node === 289) return 0.16249946885926803;
  if (node === 581) return -0.15887049012670132;
  if (node === 582) return 0.058544958960474784;
  if (node === 290) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree122(features, 581);
    } else if (val <= 1.5000000000000002) {
      return calculateTree122(features, 581);
    } else {
      return calculateTree122(features, 582);
    }
  }
  if (node === 144) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree122(features, 289);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree122(features, 289);
    } else {
      return calculateTree122(features, 290);
    }
  }
  if (node === 71) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree122(features, 143);
    } else if (val <= 39.50000000000001) {
      return calculateTree122(features, 143);
    } else {
      return calculateTree122(features, 144);
    }
  }
  if (node === 145) return -0.18274306258319198;
  if (node === 146) return -0.0147203819086796;
  if (node === 72) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree122(features, 145);
    } else if (val <= 55.50000000000001) {
      return calculateTree122(features, 145);
    } else {
      return calculateTree122(features, 146);
    }
  }
  if (node === 35) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree122(features, 71);
    } else if (val <= 44.50000000000001) {
      return calculateTree122(features, 71);
    } else {
      return calculateTree122(features, 72);
    }
  }
  if (node === 36) return 0.0907708341242171;
  if (node === 17) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree122(features, 35);
    } else if (val <= 130.00000000000003) {
      return calculateTree122(features, 35);
    } else {
      return calculateTree122(features, 36);
    }
  }
  if (node === 37) return -0.34510630011729043;
  if (node === 311) return -0.00885306148310718;
  if (node === 312) return -0.15110220061610577;
  if (node === 155) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree122(features, 311);
    } else if (val <= 172.50000000000003) {
      return calculateTree122(features, 311);
    } else {
      return calculateTree122(features, 312);
    }
  }
  if (node === 156) return 0.06443774864886276;
  if (node === 77) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree122(features, 155);
    } else if (val <= 175.50000000000003) {
      return calculateTree122(features, 155);
    } else {
      return calculateTree122(features, 156);
    }
  }
  if (node === 78) return -0.04870514852368174;
  if (node === 38) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree122(features, 77);
    } else if (val <= 181.50000000000003) {
      return calculateTree122(features, 77);
    } else {
      return calculateTree122(features, 78);
    }
  }
  if (node === 18) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree122(features, 37);
    } else if (val <= 141.50000000000003) {
      return calculateTree122(features, 37);
    } else {
      return calculateTree122(features, 38);
    }
  }
  if (node === 8) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree122(features, 17);
    } else if (val <= 132.50000000000003) {
      return calculateTree122(features, 17);
    } else {
      return calculateTree122(features, 18);
    }
  }
  if (node === 3) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree122(features, 7);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree122(features, 7);
    } else {
      return calculateTree122(features, 8);
    }
  }
  if (node === 4) return -0.025764710810469604;
  if (node === 1) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree122(features, 3);
    } else if (val <= 194.00000000000003) {
      return calculateTree122(features, 3);
    } else {
      return calculateTree122(features, 4);
    }
  }
  if (node === 47) return -0.33547994361274;
  if (node === 48) return 0.02347139740259714;
  if (node === 23) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree122(features, 47);
    } else if (val <= 210.00000000000003) {
      return calculateTree122(features, 47);
    } else {
      return calculateTree122(features, 48);
    }
  }
  if (node === 24) return 0.041964868252934946;
  if (node === 11) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree122(features, 23);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree122(features, 23);
    } else {
      return calculateTree122(features, 24);
    }
  }
  if (node === 25) return 0.01871335959791407;
  if (node === 107) return 0.3373785469462944;
  if (node === 108) return -0.02195669814674823;
  if (node === 53) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree122(features, 107);
    } else if (val <= 210.00000000000003) {
      return calculateTree122(features, 107);
    } else {
      return calculateTree122(features, 108);
    }
  }
  if (node === 109) return 0.29328164050096783;
  if (node === 110) return 0.13064795194596243;
  if (node === 54) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree122(features, 109);
    } else if (val <= 880.0000000000001) {
      return calculateTree122(features, 109);
    } else {
      return calculateTree122(features, 110);
    }
  }
  if (node === 26) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree122(features, 53);
    } else if (val <= 176.50000000000003) {
      return calculateTree122(features, 53);
    } else {
      return calculateTree122(features, 54);
    }
  }
  if (node === 12) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree122(features, 25);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree122(features, 25);
    } else {
      return calculateTree122(features, 26);
    }
  }
  if (node === 5) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree122(features, 11);
    } else if (val <= 6.500000000000001) {
      return calculateTree122(features, 11);
    } else {
      return calculateTree122(features, 12);
    }
  }
  if (node === 6) return 0.0006157398991490207;
  if (node === 2) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree122(features, 5);
    } else if (val <= 225.50000000000003) {
      return calculateTree122(features, 5);
    } else {
      return calculateTree122(features, 6);
    }
  }
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree122(features, 1);
    } else if (val <= 206.00000000000003) {
      return calculateTree122(features, 1);
    } else {
      return calculateTree122(features, 2);
    }
  }
}
function calculateTree123(features, node = 0) {
  if (node === 7) return 0.0009443668628442824;
  if (node === 17) return -0.0045028839504964325;
  if (node === 18) return -0.06888470162924007;
  if (node === 8) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree123(features, 17);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree123(features, 17);
    } else {
      return calculateTree123(features, 18);
    }
  }
  if (node === 3) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree123(features, 7);
    } else if (val <= 729.5000000000001) {
      return calculateTree123(features, 7);
    } else {
      return calculateTree123(features, 8);
    }
  }
  if (node === 19) return 0.1898658495125147;
  if (node === 20) return -0.12436435815152436;
  if (node === 9) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree123(features, 19);
    } else if (val <= 35.50000000000001) {
      return calculateTree123(features, 19);
    } else {
      return calculateTree123(features, 20);
    }
  }
  if (node === 87) return -0.30385964632837464;
  if (node === 355) return 0.25446403706497744;
  if (node === 356) return 0.05981188266757191;
  if (node === 177) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree123(features, 355);
    } else if (val <= 31.500000000000004) {
      return calculateTree123(features, 355);
    } else {
      return calculateTree123(features, 356);
    }
  }
  if (node === 357) return -0.32597921763026155;
  if (node === 358) return 0.09707168671326616;
  if (node === 178) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree123(features, 357);
    } else if (val <= 734.0000000000001) {
      return calculateTree123(features, 357);
    } else {
      return calculateTree123(features, 358);
    }
  }
  if (node === 88) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree123(features, 177);
    } else if (val <= 588.5000000000001) {
      return calculateTree123(features, 177);
    } else {
      return calculateTree123(features, 178);
    }
  }
  if (node === 43) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree123(features, 87);
    } else if (val <= 504.50000000000006) {
      return calculateTree123(features, 87);
    } else {
      return calculateTree123(features, 88);
    }
  }
  if (node === 719) return 0.020700986696345458;
  if (node === 720) return 0.2794281619372383;
  if (node === 359) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree123(features, 719);
    } else if (val <= 16.500000000000004) {
      return calculateTree123(features, 719);
    } else {
      return calculateTree123(features, 720);
    }
  }
  if (node === 360) return -0.1433828704053871;
  if (node === 179) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree123(features, 359);
    } else if (val <= 24.500000000000004) {
      return calculateTree123(features, 359);
    } else {
      return calculateTree123(features, 360);
    }
  }
  if (node === 180) return 0.2564896476320652;
  if (node === 89) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree123(features, 179);
    } else if (val <= 40.50000000000001) {
      return calculateTree123(features, 179);
    } else {
      return calculateTree123(features, 180);
    }
  }
  if (node === 181) return -0.3806819211770694;
  if (node === 731) return -0.1224018826040337;
  if (node === 732) return -0.43903550699913163;
  if (node === 365) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree123(features, 731);
    } else if (val <= 675.0000000000001) {
      return calculateTree123(features, 731);
    } else {
      return calculateTree123(features, 732);
    }
  }
  if (node === 366) return 0.3186401808404924;
  if (node === 182) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree123(features, 365);
    } else if (val <= 155.50000000000003) {
      return calculateTree123(features, 365);
    } else {
      return calculateTree123(features, 366);
    }
  }
  if (node === 90) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree123(features, 181);
    } else if (val <= 588.5000000000001) {
      return calculateTree123(features, 181);
    } else {
      return calculateTree123(features, 182);
    }
  }
  if (node === 44) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree123(features, 89);
    } else if (val <= 144.50000000000003) {
      return calculateTree123(features, 89);
    } else {
      return calculateTree123(features, 90);
    }
  }
  if (node === 21) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree123(features, 43);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree123(features, 43);
    } else {
      return calculateTree123(features, 44);
    }
  }
  if (node === 45) return -0.3523737571301879;
  if (node === 187) return 0.07447659038319787;
  if (node === 188) return -0.12655322399444846;
  if (node === 93) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree123(features, 187);
    } else if (val <= 46.50000000000001) {
      return calculateTree123(features, 187);
    } else {
      return calculateTree123(features, 188);
    }
  }
  if (node === 94) return 0.17984164811671263;
  if (node === 46) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree123(features, 93);
    } else if (val <= 8.500000000000002) {
      return calculateTree123(features, 93);
    } else {
      return calculateTree123(features, 94);
    }
  }
  if (node === 22) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree123(features, 45);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree123(features, 45);
    } else {
      return calculateTree123(features, 46);
    }
  }
  if (node === 10) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree123(features, 21);
    } else if (val <= 758.0000000000001) {
      return calculateTree123(features, 21);
    } else {
      return calculateTree123(features, 22);
    }
  }
  if (node === 4) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree123(features, 9);
    } else if (val <= 127.50000000000001) {
      return calculateTree123(features, 9);
    } else {
      return calculateTree123(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree123(features, 3);
    } else if (val <= 745.5000000000001) {
      return calculateTree123(features, 3);
    } else {
      return calculateTree123(features, 4);
    }
  }
  if (node === 11) return 0.1833567390398831;
  if (node === 25) return -0.276983321914276;
  if (node === 107) return 0.094272850809349;
  if (node === 217) return -0.031618679298016615;
  if (node === 218) return -0.22520421947282956;
  if (node === 108) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree123(features, 217);
    } else if (val <= 11.500000000000002) {
      return calculateTree123(features, 217);
    } else {
      return calculateTree123(features, 218);
    }
  }
  if (node === 53) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree123(features, 107);
    } else if (val <= 25.500000000000004) {
      return calculateTree123(features, 107);
    } else {
      return calculateTree123(features, 108);
    }
  }
  if (node === 109) return 0.4192344291181675;
  if (node === 221) return -0.3027630485722533;
  if (node === 445) return -0.06286801642073986;
  if (node === 446) return 0.5374064251130494;
  if (node === 222) {
    const val = features['is_preference_men_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree123(features, 445);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree123(features, 445);
    } else {
      return calculateTree123(features, 446);
    }
  }
  if (node === 110) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree123(features, 221);
    } else if (val <= 969.5000000000001) {
      return calculateTree123(features, 221);
    } else {
      return calculateTree123(features, 222);
    }
  }
  if (node === 54) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree123(features, 109);
    } else if (val <= 763.5000000000001) {
      return calculateTree123(features, 109);
    } else {
      return calculateTree123(features, 110);
    }
  }
  if (node === 26) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree123(features, 53);
    } else if (val <= 695.5000000000001) {
      return calculateTree123(features, 53);
    } else {
      return calculateTree123(features, 54);
    }
  }
  if (node === 12) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree123(features, 25);
    } else if (val <= 556.0000000000001) {
      return calculateTree123(features, 25);
    } else {
      return calculateTree123(features, 26);
    }
  }
  if (node === 5) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree123(features, 11);
    } else if (val <= 89.00000000000001) {
      return calculateTree123(features, 11);
    } else {
      return calculateTree123(features, 12);
    }
  }
  if (node === 6) return -0.003054818505851245;
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree123(features, 5);
    } else if (val <= 780.0000000000001) {
      return calculateTree123(features, 5);
    } else {
      return calculateTree123(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree123(features, 1);
    } else if (val <= 760.0000000000001) {
      return calculateTree123(features, 1);
    } else {
      return calculateTree123(features, 2);
    }
  }
}
function calculateTree124(features, node = 0) {
  if (node === 7) return -0.0010893127507844358;
  if (node === 35) return -0.28102168178735987;
  if (node === 73) return 0.047535915249445164;
  if (node === 74) return 0.9806994852498537;
  if (node === 36) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree124(features, 73);
    } else if (val <= 880.0000000000001) {
      return calculateTree124(features, 73);
    } else {
      return calculateTree124(features, 74);
    }
  }
  if (node === 17) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree124(features, 35);
    } else if (val <= 991.0000000000001) {
      return calculateTree124(features, 35);
    } else {
      return calculateTree124(features, 36);
    }
  }
  if (node === 303) return 0.03153504984962356;
  if (node === 304) return -0.012115890407765053;
  if (node === 151) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree124(features, 303);
    } else if (val <= 14.500000000000002) {
      return calculateTree124(features, 303);
    } else {
      return calculateTree124(features, 304);
    }
  }
  if (node === 152) return -0.27301518375934214;
  if (node === 75) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree124(features, 151);
    } else if (val <= 29.500000000000004) {
      return calculateTree124(features, 151);
    } else {
      return calculateTree124(features, 152);
    }
  }
  if (node === 307) return 0.025239727993202468;
  if (node === 308) return 0.23693446546980096;
  if (node === 153) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree124(features, 307);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree124(features, 307);
    } else {
      return calculateTree124(features, 308);
    }
  }
  if (node === 309) return -0.14957834891348018;
  if (node === 310) return 0.19366112878861144;
  if (node === 154) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree124(features, 309);
    } else if (val <= 58.50000000000001) {
      return calculateTree124(features, 309);
    } else {
      return calculateTree124(features, 310);
    }
  }
  if (node === 76) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree124(features, 153);
    } else if (val <= 53.50000000000001) {
      return calculateTree124(features, 153);
    } else {
      return calculateTree124(features, 154);
    }
  }
  if (node === 37) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree124(features, 75);
    } else if (val <= 44.50000000000001) {
      return calculateTree124(features, 75);
    } else {
      return calculateTree124(features, 76);
    }
  }
  if (node === 38) return -0.2651879998490415;
  if (node === 18) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree124(features, 37);
    } else if (val <= 606.5000000000001) {
      return calculateTree124(features, 37);
    } else {
      return calculateTree124(features, 38);
    }
  }
  if (node === 8) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree124(features, 17);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree124(features, 17);
    } else {
      return calculateTree124(features, 18);
    }
  }
  if (node === 3) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree124(features, 7);
    } else if (val <= 975.0000000000001) {
      return calculateTree124(features, 7);
    } else {
      return calculateTree124(features, 8);
    }
  }
  if (node === 4) return -0.28520732440434343;
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree124(features, 3);
    } else if (val <= 992.5000000000001) {
      return calculateTree124(features, 3);
    } else {
      return calculateTree124(features, 4);
    }
  }
  if (node === 11) return -0.32732541820130595;
  if (node === 103) return 0.2334912366241616;
  if (node === 419) return 0.02709826113468466;
  if (node === 420) return 0.25667229330778446;
  if (node === 209) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree124(features, 419);
    } else if (val <= 7.500000000000001) {
      return calculateTree124(features, 419);
    } else {
      return calculateTree124(features, 420);
    }
  }
  if (node === 210) return -0.14408440634748385;
  if (node === 104) {
    const val = features['is_preference_pets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree124(features, 209);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree124(features, 209);
    } else {
      return calculateTree124(features, 210);
    }
  }
  if (node === 51) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree124(features, 103);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree124(features, 103);
    } else {
      return calculateTree124(features, 104);
    }
  }
  if (node === 105) return -0.19131623271631584;
  if (node === 213) return -0.16128683338836966;
  if (node === 1719) return 0.2357395182993125;
  if (node === 1720) return 0.08889522149203924;
  if (node === 859) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree124(features, 1719);
    } else if (val <= 2.5000000000000004) {
      return calculateTree124(features, 1719);
    } else {
      return calculateTree124(features, 1720);
    }
  }
  if (node === 860) return -0.17302985800571627;
  if (node === 429) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree124(features, 859);
    } else if (val <= 880.0000000000001) {
      return calculateTree124(features, 859);
    } else {
      return calculateTree124(features, 860);
    }
  }
  if (node === 861) return -0.2973861588605228;
  if (node === 1725) return 0.08485368571332688;
  if (node === 1726) return -0.01622916085585965;
  if (node === 862) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree124(features, 1725);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree124(features, 1725);
    } else {
      return calculateTree124(features, 1726);
    }
  }
  if (node === 430) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree124(features, 861);
    } else if (val <= 38.50000000000001) {
      return calculateTree124(features, 861);
    } else {
      return calculateTree124(features, 862);
    }
  }
  if (node === 214) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree124(features, 429);
    } else if (val <= 36.50000000000001) {
      return calculateTree124(features, 429);
    } else {
      return calculateTree124(features, 430);
    }
  }
  if (node === 106) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree124(features, 213);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree124(features, 213);
    } else {
      return calculateTree124(features, 214);
    }
  }
  if (node === 52) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree124(features, 105);
    } else if (val <= 29.500000000000004) {
      return calculateTree124(features, 105);
    } else {
      return calculateTree124(features, 106);
    }
  }
  if (node === 25) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree124(features, 51);
    } else if (val <= 26.500000000000004) {
      return calculateTree124(features, 51);
    } else {
      return calculateTree124(features, 52);
    }
  }
  if (node === 107) return 0.11255686111126292;
  if (node === 108) return -0.1869607420007777;
  if (node === 53) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree124(features, 107);
    } else if (val <= 48.50000000000001) {
      return calculateTree124(features, 107);
    } else {
      return calculateTree124(features, 108);
    }
  }
  if (node === 54) return 0.18694402500331136;
  if (node === 26) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree124(features, 53);
    } else if (val <= 8.500000000000002) {
      return calculateTree124(features, 53);
    } else {
      return calculateTree124(features, 54);
    }
  }
  if (node === 12) {
    const val = features['is_preference_household'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree124(features, 25);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree124(features, 25);
    } else {
      return calculateTree124(features, 26);
    }
  }
  if (node === 5) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree124(features, 11);
    } else if (val <= 283.50000000000006) {
      return calculateTree124(features, 11);
    } else {
      return calculateTree124(features, 12);
    }
  }
  if (node === 27) return 0.06795829436443777;
  if (node === 28) return 0.32299183668265075;
  if (node === 13) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree124(features, 27);
    } else if (val <= 56.00000000000001) {
      return calculateTree124(features, 27);
    } else {
      return calculateTree124(features, 28);
    }
  }
  if (node === 14) return -0.31063173060364485;
  if (node === 6) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree124(features, 13);
    } else if (val <= 181.50000000000003) {
      return calculateTree124(features, 13);
    } else {
      return calculateTree124(features, 14);
    }
  }
  if (node === 2) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree124(features, 5);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree124(features, 5);
    } else {
      return calculateTree124(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree124(features, 1);
    } else if (val <= 993.5000000000001) {
      return calculateTree124(features, 1);
    } else {
      return calculateTree124(features, 2);
    }
  }
}
function calculateTree125(features, node = 0) {
  if (node === 7) return 0.002058798437825154;
  if (node === 35) return -0.004609200470364521;
  if (node === 36) return 0.07079415796951327;
  if (node === 17) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree125(features, 35);
    } else if (val <= 391.00000000000006) {
      return calculateTree125(features, 35);
    } else {
      return calculateTree125(features, 36);
    }
  }
  if (node === 37) return -0.35460015034190967;
  if (node === 77) return 0.15498998456921187;
  if (node === 157) return -0.31784920164486036;
  if (node === 158) return -0.07475536045235334;
  if (node === 78) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree125(features, 157);
    } else if (val <= 389.00000000000006) {
      return calculateTree125(features, 157);
    } else {
      return calculateTree125(features, 158);
    }
  }
  if (node === 38) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree125(features, 77);
    } else if (val <= 381.50000000000006) {
      return calculateTree125(features, 77);
    } else {
      return calculateTree125(features, 78);
    }
  }
  if (node === 18) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree125(features, 37);
    } else if (val <= 377.50000000000006) {
      return calculateTree125(features, 37);
    } else {
      return calculateTree125(features, 38);
    }
  }
  if (node === 8) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree125(features, 17);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree125(features, 17);
    } else {
      return calculateTree125(features, 18);
    }
  }
  if (node === 3) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree125(features, 7);
    } else if (val <= 363.00000000000006) {
      return calculateTree125(features, 7);
    } else {
      return calculateTree125(features, 8);
    }
  }
  if (node === 159) return 0.005128185862286315;
  if (node === 321) return -0.3158580739963875;
  if (node === 322) return -0.0781600003888626;
  if (node === 160) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree125(features, 321);
    } else if (val <= 426.00000000000006) {
      return calculateTree125(features, 321);
    } else {
      return calculateTree125(features, 322);
    }
  }
  if (node === 79) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree125(features, 159);
    } else if (val <= 422.00000000000006) {
      return calculateTree125(features, 159);
    } else {
      return calculateTree125(features, 160);
    }
  }
  if (node === 80) return 0.08997245103553386;
  if (node === 39) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree125(features, 79);
    } else if (val <= 432.00000000000006) {
      return calculateTree125(features, 79);
    } else {
      return calculateTree125(features, 80);
    }
  }
  if (node === 81) return -0.33129818532569894;
  if (node === 331) return 0.45497132177228017;
  if (node === 332) return -0.10315893701587125;
  if (node === 165) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree125(features, 331);
    } else if (val <= 14.500000000000002) {
      return calculateTree125(features, 331);
    } else {
      return calculateTree125(features, 332);
    }
  }
  if (node === 166) return -0.13768867071493063;
  if (node === 82) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree125(features, 165);
    } else if (val <= 459.50000000000006) {
      return calculateTree125(features, 165);
    } else {
      return calculateTree125(features, 166);
    }
  }
  if (node === 40) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree125(features, 81);
    } else if (val <= 449.50000000000006) {
      return calculateTree125(features, 81);
    } else {
      return calculateTree125(features, 82);
    }
  }
  if (node === 19) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree125(features, 39);
    } else if (val <= 441.00000000000006) {
      return calculateTree125(features, 39);
    } else {
      return calculateTree125(features, 40);
    }
  }
  if (node === 41) return 0.13615692641008484;
  if (node === 85) return -0.09009976914768726;
  if (node === 86) return 0.038423676981947666;
  if (node === 42) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree125(features, 85);
    } else if (val <= 474.50000000000006) {
      return calculateTree125(features, 85);
    } else {
      return calculateTree125(features, 86);
    }
  }
  if (node === 20) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree125(features, 41);
    } else if (val <= 470.00000000000006) {
      return calculateTree125(features, 41);
    } else {
      return calculateTree125(features, 42);
    }
  }
  if (node === 9) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree125(features, 19);
    } else if (val <= 467.50000000000006) {
      return calculateTree125(features, 19);
    } else {
      return calculateTree125(features, 20);
    }
  }
  if (node === 703) return 0.027872231896539746;
  if (node === 704) return -0.32874613090317856;
  if (node === 351) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree125(features, 703);
    } else if (val <= 426.00000000000006) {
      return calculateTree125(features, 703);
    } else {
      return calculateTree125(features, 704);
    }
  }
  if (node === 705) return 0.07160501156020314;
  if (node === 706) return 0.30564405831225727;
  if (node === 352) {
    const val = features['is_preference_men_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree125(features, 705);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree125(features, 705);
    } else {
      return calculateTree125(features, 706);
    }
  }
  if (node === 175) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree125(features, 351);
    } else if (val <= 429.00000000000006) {
      return calculateTree125(features, 351);
    } else {
      return calculateTree125(features, 352);
    }
  }
  if (node === 353) return 0.0031856661513586086;
  if (node === 354) return -0.15895206329735162;
  if (node === 176) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree125(features, 353);
    } else if (val <= 426.00000000000006) {
      return calculateTree125(features, 353);
    } else {
      return calculateTree125(features, 354);
    }
  }
  if (node === 87) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree125(features, 175);
    } else if (val <= 30.500000000000004) {
      return calculateTree125(features, 175);
    } else {
      return calculateTree125(features, 176);
    }
  }
  if (node === 177) return -0.3106923580649217;
  if (node === 178) return -0.04921470607662409;
  if (node === 88) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree125(features, 177);
    } else if (val <= 435.00000000000006) {
      return calculateTree125(features, 177);
    } else {
      return calculateTree125(features, 178);
    }
  }
  if (node === 43) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree125(features, 87);
    } else if (val <= 432.00000000000006) {
      return calculateTree125(features, 87);
    } else {
      return calculateTree125(features, 88);
    }
  }
  if (node === 89) return 0.005541583220280658;
  if (node === 90) return 0.10930713739071991;
  if (node === 44) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree125(features, 89);
    } else if (val <= 13.500000000000002) {
      return calculateTree125(features, 89);
    } else {
      return calculateTree125(features, 90);
    }
  }
  if (node === 21) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree125(features, 43);
    } else if (val <= 441.00000000000006) {
      return calculateTree125(features, 43);
    } else {
      return calculateTree125(features, 44);
    }
  }
  if (node === 45) return -0.34978520168527627;
  if (node === 93) return -0.012457376708900288;
  if (node === 94) return 0.102667314673002;
  if (node === 46) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree125(features, 93);
    } else if (val <= 34.50000000000001) {
      return calculateTree125(features, 93);
    } else {
      return calculateTree125(features, 94);
    }
  }
  if (node === 22) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree125(features, 45);
    } else if (val <= 470.00000000000006) {
      return calculateTree125(features, 45);
    } else {
      return calculateTree125(features, 46);
    }
  }
  if (node === 10) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree125(features, 21);
    } else if (val <= 467.50000000000006) {
      return calculateTree125(features, 21);
    } else {
      return calculateTree125(features, 22);
    }
  }
  if (node === 4) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree125(features, 9);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree125(features, 9);
    } else {
      return calculateTree125(features, 10);
    }
  }
  if (node === 1) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree125(features, 3);
    } else if (val <= 409.50000000000006) {
      return calculateTree125(features, 3);
    } else {
      return calculateTree125(features, 4);
    }
  }
  if (node === 2) return -0.0020168281502263127;
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree125(features, 1);
    } else if (val <= 478.50000000000006) {
      return calculateTree125(features, 1);
    } else {
      return calculateTree125(features, 2);
    }
  }
}
function calculateTree126(features, node = 0) {
  if (node === 1) return -0.0002527306379805248;
  if (node === 5) return -0.2067181021059434;
  if (node === 447) return -0.030040757390593344;
  if (node === 448) return 0.12579912766011936;
  if (node === 223) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree126(features, 447);
    } else if (val <= 576.0000000000001) {
      return calculateTree126(features, 447);
    } else {
      return calculateTree126(features, 448);
    }
  }
  if (node === 449) return -0.08681364767126261;
  if (node === 450) return -0.3726904752242605;
  if (node === 224) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree126(features, 449);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree126(features, 449);
    } else {
      return calculateTree126(features, 450);
    }
  }
  if (node === 111) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree126(features, 223);
    } else if (val <= 677.5000000000001) {
      return calculateTree126(features, 223);
    } else {
      return calculateTree126(features, 224);
    }
  }
  if (node === 451) return 0.21121697313876311;
  if (node === 452) return -0.021711881957625564;
  if (node === 225) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree126(features, 451);
    } else if (val <= 18.500000000000004) {
      return calculateTree126(features, 451);
    } else {
      return calculateTree126(features, 452);
    }
  }
  if (node === 453) return -0.3427807100071376;
  if (node === 1819) return -0.23759723263810006;
  if (node === 1820) return 0.02794029073384781;
  if (node === 909) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree126(features, 1819);
    } else if (val <= 799.5000000000001) {
      return calculateTree126(features, 1819);
    } else {
      return calculateTree126(features, 1820);
    }
  }
  if (node === 1821) return 0.16310329028911882;
  if (node === 1822) return -0.01805873648334355;
  if (node === 910) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree126(features, 1821);
    } else if (val <= 143.50000000000003) {
      return calculateTree126(features, 1821);
    } else {
      return calculateTree126(features, 1822);
    }
  }
  if (node === 454) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree126(features, 909);
    } else if (val <= 665.0000000000001) {
      return calculateTree126(features, 909);
    } else {
      return calculateTree126(features, 910);
    }
  }
  if (node === 226) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree126(features, 453);
    } else if (val <= 459.50000000000006) {
      return calculateTree126(features, 453);
    } else {
      return calculateTree126(features, 454);
    }
  }
  if (node === 112) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree126(features, 225);
    } else if (val <= 236.00000000000003) {
      return calculateTree126(features, 225);
    } else {
      return calculateTree126(features, 226);
    }
  }
  if (node === 55) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree126(features, 111);
    } else if (val <= 789.5000000000001) {
      return calculateTree126(features, 111);
    } else {
      return calculateTree126(features, 112);
    }
  }
  if (node === 56) return -0.006193919980150679;
  if (node === 27) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree126(features, 55);
    } else if (val <= 801.5000000000001) {
      return calculateTree126(features, 55);
    } else {
      return calculateTree126(features, 56);
    }
  }
  if (node === 57) return -0.33159675280662393;
  if (node === 117) return -0.3174969212698263;
  if (node === 951) return 0.024646947090186676;
  if (node === 1905) return -0.39879971896515776;
  if (node === 1906) return 0.09251435598306329;
  if (node === 952) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree126(features, 1905);
    } else if (val <= 916.5000000000001) {
      return calculateTree126(features, 1905);
    } else {
      return calculateTree126(features, 1906);
    }
  }
  if (node === 475) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree126(features, 951);
    } else if (val <= 925.5000000000001) {
      return calculateTree126(features, 951);
    } else {
      return calculateTree126(features, 952);
    }
  }
  if (node === 476) return -0.25864294437551283;
  if (node === 237) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree126(features, 475);
    } else if (val <= 46.50000000000001) {
      return calculateTree126(features, 475);
    } else {
      return calculateTree126(features, 476);
    }
  }
  if (node === 238) return 0.25011461594609147;
  if (node === 118) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree126(features, 237);
    } else if (val <= 52.50000000000001) {
      return calculateTree126(features, 237);
    } else {
      return calculateTree126(features, 238);
    }
  }
  if (node === 58) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree126(features, 117);
    } else if (val <= 799.5000000000001) {
      return calculateTree126(features, 117);
    } else {
      return calculateTree126(features, 118);
    }
  }
  if (node === 28) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree126(features, 57);
    } else if (val <= 893.0000000000001) {
      return calculateTree126(features, 57);
    } else {
      return calculateTree126(features, 58);
    }
  }
  if (node === 13) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree126(features, 27);
    } else if (val <= 884.0000000000001) {
      return calculateTree126(features, 27);
    } else {
      return calculateTree126(features, 28);
    }
  }
  if (node === 119) return -0.09202271333057606;
  if (node === 120) return 0.028993852678713086;
  if (node === 59) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree126(features, 119);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree126(features, 119);
    } else {
      return calculateTree126(features, 120);
    }
  }
  if (node === 243) return 0.2693309933695683;
  if (node === 489) return 0.07244233523060689;
  if (node === 490) return 0.27533655067222457;
  if (node === 244) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree126(features, 489);
    } else if (val <= 55.50000000000001) {
      return calculateTree126(features, 489);
    } else {
      return calculateTree126(features, 490);
    }
  }
  if (node === 121) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree126(features, 243);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree126(features, 243);
    } else {
      return calculateTree126(features, 244);
    }
  }
  if (node === 491) return 0.26459002199573745;
  if (node === 492) return -0.1046431465753941;
  if (node === 245) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree126(features, 491);
    } else if (val <= 51.50000000000001) {
      return calculateTree126(features, 491);
    } else {
      return calculateTree126(features, 492);
    }
  }
  if (node === 246) return -0.15924496679381583;
  if (node === 122) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree126(features, 245);
    } else if (val <= 591.0000000000001) {
      return calculateTree126(features, 245);
    } else {
      return calculateTree126(features, 246);
    }
  }
  if (node === 60) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree126(features, 121);
    } else if (val <= 10.500000000000002) {
      return calculateTree126(features, 121);
    } else {
      return calculateTree126(features, 122);
    }
  }
  if (node === 29) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree126(features, 59);
    } else if (val <= 38.50000000000001) {
      return calculateTree126(features, 59);
    } else {
      return calculateTree126(features, 60);
    }
  }
  if (node === 123) return -0.008449385964597194;
  if (node === 124) return -0.09532515980275795;
  if (node === 61) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree126(features, 123);
    } else if (val <= 27.500000000000004) {
      return calculateTree126(features, 123);
    } else {
      return calculateTree126(features, 124);
    }
  }
  if (node === 62) return 0.14875699899844813;
  if (node === 30) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree126(features, 61);
    } else if (val <= 22.500000000000004) {
      return calculateTree126(features, 61);
    } else {
      return calculateTree126(features, 62);
    }
  }
  if (node === 14) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree126(features, 29);
    } else if (val <= 963.0000000000001) {
      return calculateTree126(features, 29);
    } else {
      return calculateTree126(features, 30);
    }
  }
  if (node === 6) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree126(features, 13);
    } else if (val <= 932.5000000000001) {
      return calculateTree126(features, 13);
    } else {
      return calculateTree126(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree126(features, 5);
    } else if (val <= 785.5000000000001) {
      return calculateTree126(features, 5);
    } else {
      return calculateTree126(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree126(features, 1);
    } else if (val <= 784.5000000000001) {
      return calculateTree126(features, 1);
    } else {
      return calculateTree126(features, 2);
    }
  }
}
function calculateTree127(features, node = 0) {
  if (node === 15) return 2.813509698197996e-5;
  if (node === 135) return -0.28103063964728553;
  if (node === 136) return 0.2564324132257625;
  if (node === 67) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree127(features, 135);
    } else if (val <= 305.00000000000006) {
      return calculateTree127(features, 135);
    } else {
      return calculateTree127(features, 136);
    }
  }
  if (node === 68) return -0.26727429180909223;
  if (node === 33) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree127(features, 67);
    } else if (val <= 389.00000000000006) {
      return calculateTree127(features, 67);
    } else {
      return calculateTree127(features, 68);
    }
  }
  if (node === 69) return -0.37040450050404905;
  if (node === 567) return 0.06471663243782662;
  if (node === 1137) return -0.298569347339704;
  if (node === 1138) return 0.018671594327142545;
  if (node === 568) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree127(features, 1137);
    } else if (val <= 19.500000000000004) {
      return calculateTree127(features, 1137);
    } else {
      return calculateTree127(features, 1138);
    }
  }
  if (node === 283) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree127(features, 567);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree127(features, 567);
    } else {
      return calculateTree127(features, 568);
    }
  }
  if (node === 284) return 0.40710917314271466;
  if (node === 141) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree127(features, 283);
    } else if (val <= 24.500000000000004) {
      return calculateTree127(features, 283);
    } else {
      return calculateTree127(features, 284);
    }
  }
  if (node === 142) return -0.2753093308104034;
  if (node === 70) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree127(features, 141);
    } else if (val <= 30.500000000000004) {
      return calculateTree127(features, 141);
    } else {
      return calculateTree127(features, 142);
    }
  }
  if (node === 34) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree127(features, 69);
    } else if (val <= 781.0000000000001) {
      return calculateTree127(features, 69);
    } else {
      return calculateTree127(features, 70);
    }
  }
  if (node === 16) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree127(features, 33);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree127(features, 33);
    } else {
      return calculateTree127(features, 34);
    }
  }
  if (node === 7) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree127(features, 15);
    } else if (val <= 882.5000000000001) {
      return calculateTree127(features, 15);
    } else {
      return calculateTree127(features, 16);
    }
  }
  if (node === 17) return -0.31060641962361446;
  if (node === 37) return 0.14195590374234832;
  if (node === 38) return 0.06259225307940619;
  if (node === 18) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree127(features, 37);
    } else if (val <= 863.0000000000001) {
      return calculateTree127(features, 37);
    } else {
      return calculateTree127(features, 38);
    }
  }
  if (node === 8) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree127(features, 17);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree127(features, 17);
    } else {
      return calculateTree127(features, 18);
    }
  }
  if (node === 3) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree127(features, 7);
    } else if (val <= 890.5000000000001) {
      return calculateTree127(features, 7);
    } else {
      return calculateTree127(features, 8);
    }
  }
  if (node === 9) return -0.25846315903549344;
  if (node === 43) return -0.33726588703405097;
  if (node === 179) return 0.19683105672016768;
  if (node === 180) return 0.6109550669746439;
  if (node === 89) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree127(features, 179);
    } else if (val <= 42.50000000000001) {
      return calculateTree127(features, 179);
    } else {
      return calculateTree127(features, 180);
    }
  }
  if (node === 90) return -0.3360909003537423;
  if (node === 44) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree127(features, 89);
    } else if (val <= 500.50000000000006) {
      return calculateTree127(features, 89);
    } else {
      return calculateTree127(features, 90);
    }
  }
  if (node === 21) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree127(features, 43);
    } else if (val <= 127.50000000000001) {
      return calculateTree127(features, 43);
    } else {
      return calculateTree127(features, 44);
    }
  }
  if (node === 735) return 0.05399664082346494;
  if (node === 736) return 0.3280958864176811;
  if (node === 367) {
    const val = features['is_preference_kitchen'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree127(features, 735);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree127(features, 735);
    } else {
      return calculateTree127(features, 736);
    }
  }
  if (node === 368) return -0.2969655670000079;
  if (node === 183) {
    const val = features['is_preference_men_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree127(features, 367);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree127(features, 367);
    } else {
      return calculateTree127(features, 368);
    }
  }
  if (node === 739) return 0.24194473522421134;
  if (node === 740) return -0.015248315968378497;
  if (node === 369) {
    const val = features['is_preference_household'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree127(features, 739);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree127(features, 739);
    } else {
      return calculateTree127(features, 740);
    }
  }
  if (node === 741) return -0.05277253140002428;
  if (node === 742) return -0.19766466877121655;
  if (node === 370) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree127(features, 741);
    } else if (val <= 25.500000000000004) {
      return calculateTree127(features, 741);
    } else {
      return calculateTree127(features, 742);
    }
  }
  if (node === 184) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree127(features, 369);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree127(features, 369);
    } else {
      return calculateTree127(features, 370);
    }
  }
  if (node === 91) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree127(features, 183);
    } else if (val <= 1.5000000000000002) {
      return calculateTree127(features, 183);
    } else {
      return calculateTree127(features, 184);
    }
  }
  if (node === 92) return 0.2870416644646884;
  if (node === 45) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree127(features, 91);
    } else if (val <= 53.50000000000001) {
      return calculateTree127(features, 91);
    } else {
      return calculateTree127(features, 92);
    }
  }
  if (node === 93) return -0.3800603585683627;
  if (node === 379) return -0.04876284681492981;
  if (node === 761) return 0.2829930237695797;
  if (node === 762) return 0.017327791613523474;
  if (node === 380) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree127(features, 761);
    } else if (val <= 2.5000000000000004) {
      return calculateTree127(features, 761);
    } else {
      return calculateTree127(features, 762);
    }
  }
  if (node === 189) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree127(features, 379);
    } else if (val <= 27.500000000000004) {
      return calculateTree127(features, 379);
    } else {
      return calculateTree127(features, 380);
    }
  }
  if (node === 190) return 0.22009454246434276;
  if (node === 94) {
    const val = features['is_preference_personal_care'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree127(features, 189);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree127(features, 189);
    } else {
      return calculateTree127(features, 190);
    }
  }
  if (node === 46) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree127(features, 93);
    } else if (val <= 723.5000000000001) {
      return calculateTree127(features, 93);
    } else {
      return calculateTree127(features, 94);
    }
  }
  if (node === 22) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree127(features, 45);
    } else if (val <= 127.50000000000001) {
      return calculateTree127(features, 45);
    } else {
      return calculateTree127(features, 46);
    }
  }
  if (node === 10) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree127(features, 21);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree127(features, 21);
    } else {
      return calculateTree127(features, 22);
    }
  }
  if (node === 4) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree127(features, 9);
    } else if (val <= 899.5000000000001) {
      return calculateTree127(features, 9);
    } else {
      return calculateTree127(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree127(features, 3);
    } else if (val <= 896.5000000000001) {
      return calculateTree127(features, 3);
    } else {
      return calculateTree127(features, 4);
    }
  }
  if (node === 2) return 0.00849705192003881;
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree127(features, 1);
    } else if (val <= 902.0000000000001) {
      return calculateTree127(features, 1);
    } else {
      return calculateTree127(features, 2);
    }
  }
}
function calculateTree128(features, node = 0) {
  if (node === 1) return 0.0018051228229077295;
  if (node === 47) return -0.31133031174272247;
  if (node === 391) return -0.34801001050859187;
  if (node === 392) return 0.2375203115865449;
  if (node === 195) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree128(features, 391);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree128(features, 391);
    } else {
      return calculateTree128(features, 392);
    }
  }
  if (node === 393) return 0.24583424433633919;
  if (node === 394) return -0.31912634048974337;
  if (node === 196) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree128(features, 393);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree128(features, 393);
    } else {
      return calculateTree128(features, 394);
    }
  }
  if (node === 97) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree128(features, 195);
    } else if (val <= 580.0000000000001) {
      return calculateTree128(features, 195);
    } else {
      return calculateTree128(features, 196);
    }
  }
  if (node === 197) return -0.15216350118748412;
  if (node === 198) return 0.16108782548405728;
  if (node === 98) {
    const val = features['is_preference_sports'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree128(features, 197);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree128(features, 197);
    } else {
      return calculateTree128(features, 198);
    }
  }
  if (node === 48) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree128(features, 97);
    } else if (val <= 3.5000000000000004) {
      return calculateTree128(features, 97);
    } else {
      return calculateTree128(features, 98);
    }
  }
  if (node === 23) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree128(features, 47);
    } else if (val <= 556.0000000000001) {
      return calculateTree128(features, 47);
    } else {
      return calculateTree128(features, 48);
    }
  }
  if (node === 199) return -0.007223130342131582;
  if (node === 200) return -0.29447298735059024;
  if (node === 99) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree128(features, 199);
    } else if (val <= 338.50000000000006) {
      return calculateTree128(features, 199);
    } else {
      return calculateTree128(features, 200);
    }
  }
  if (node === 100) return 0.01005898337194955;
  if (node === 49) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree128(features, 99);
    } else if (val <= 348.50000000000006) {
      return calculateTree128(features, 99);
    } else {
      return calculateTree128(features, 100);
    }
  }
  if (node === 101) return -0.4118250549660761;
  if (node === 823) return -0.32946458043330745;
  if (node === 1649) return 0.07652170823044549;
  if (node === 1650) return -0.24180137445440603;
  if (node === 824) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree128(features, 1649);
    } else if (val <= 695.5000000000001) {
      return calculateTree128(features, 1649);
    } else {
      return calculateTree128(features, 1650);
    }
  }
  if (node === 411) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree128(features, 823);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree128(features, 823);
    } else {
      return calculateTree128(features, 824);
    }
  }
  if (node === 825) return -0.09506303717017252;
  if (node === 826) return -0.2633523077156057;
  if (node === 412) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree128(features, 825);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree128(features, 825);
    } else {
      return calculateTree128(features, 826);
    }
  }
  if (node === 205) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree128(features, 411);
    } else if (val <= 777.5000000000001) {
      return calculateTree128(features, 411);
    } else {
      return calculateTree128(features, 412);
    }
  }
  if (node === 827) return -0.36157621283203606;
  if (node === 828) return 0.1424568208491246;
  if (node === 413) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree128(features, 827);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree128(features, 827);
    } else {
      return calculateTree128(features, 828);
    }
  }
  if (node === 414) return -0.009727019316154642;
  if (node === 206) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree128(features, 413);
    } else if (val <= 804.5000000000001) {
      return calculateTree128(features, 413);
    } else {
      return calculateTree128(features, 414);
    }
  }
  if (node === 102) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree128(features, 205);
    } else if (val <= 789.5000000000001) {
      return calculateTree128(features, 205);
    } else {
      return calculateTree128(features, 206);
    }
  }
  if (node === 50) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree128(features, 101);
    } else if (val <= 648.5000000000001) {
      return calculateTree128(features, 101);
    } else {
      return calculateTree128(features, 102);
    }
  }
  if (node === 24) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree128(features, 49);
    } else if (val <= 643.5000000000001) {
      return calculateTree128(features, 49);
    } else {
      return calculateTree128(features, 50);
    }
  }
  if (node === 11) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree128(features, 23);
    } else if (val <= 763.5000000000001) {
      return calculateTree128(features, 23);
    } else {
      return calculateTree128(features, 24);
    }
  }
  if (node === 25) return -0.3211132126555785;
  if (node === 53) return -0.31280492983592234;
  if (node === 1759) return -0.0017230668789266872;
  if (node === 1760) return 0.4511980867745347;
  if (node === 879) {
    const val = features['is_preference_kitchen'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree128(features, 1759);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree128(features, 1759);
    } else {
      return calculateTree128(features, 1760);
    }
  }
  if (node === 880) return -0.32311678149092043;
  if (node === 439) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree128(features, 879);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree128(features, 879);
    } else {
      return calculateTree128(features, 880);
    }
  }
  if (node === 1763) return 0.6157896384767384;
  if (node === 1764) return 0.08275278356852958;
  if (node === 881) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree128(features, 1763);
    } else if (val <= 1.5000000000000002) {
      return calculateTree128(features, 1763);
    } else {
      return calculateTree128(features, 1764);
    }
  }
  if (node === 882) return -0.11976633148764147;
  if (node === 440) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree128(features, 881);
    } else if (val <= 13.500000000000002) {
      return calculateTree128(features, 881);
    } else {
      return calculateTree128(features, 882);
    }
  }
  if (node === 219) {
    const val = features['is_preference_women_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree128(features, 439);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree128(features, 439);
    } else {
      return calculateTree128(features, 440);
    }
  }
  if (node === 220) return 0.3523878963783427;
  if (node === 109) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree128(features, 219);
    } else if (val <= 52.50000000000001) {
      return calculateTree128(features, 219);
    } else {
      return calculateTree128(features, 220);
    }
  }
  if (node === 221) return -0.3740184397707022;
  if (node === 222) return 0.031639846941816735;
  if (node === 110) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree128(features, 221);
    } else if (val <= 916.5000000000001) {
      return calculateTree128(features, 221);
    } else {
      return calculateTree128(features, 222);
    }
  }
  if (node === 54) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree128(features, 109);
    } else if (val <= 925.5000000000001) {
      return calculateTree128(features, 109);
    } else {
      return calculateTree128(features, 110);
    }
  }
  if (node === 26) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree128(features, 53);
    } else if (val <= 799.5000000000001) {
      return calculateTree128(features, 53);
    } else {
      return calculateTree128(features, 54);
    }
  }
  if (node === 12) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree128(features, 25);
    } else if (val <= 893.0000000000001) {
      return calculateTree128(features, 25);
    } else {
      return calculateTree128(features, 26);
    }
  }
  if (node === 5) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree128(features, 11);
    } else if (val <= 877.5000000000001) {
      return calculateTree128(features, 11);
    } else {
      return calculateTree128(features, 12);
    }
  }
  if (node === 6) return 0.017476077714241887;
  if (node === 2) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree128(features, 5);
    } else if (val <= 932.5000000000001) {
      return calculateTree128(features, 5);
    } else {
      return calculateTree128(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree128(features, 1);
    } else if (val <= 760.0000000000001) {
      return calculateTree128(features, 1);
    } else {
      return calculateTree128(features, 2);
    }
  }
}
function calculateTree129(features, node = 0) {
  if (node === 1) return -3.051306471702691e-5;
  if (node === 191) return -0.002174932978843697;
  if (node === 192) return 0.2110146481689453;
  if (node === 95) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree129(features, 191);
    } else if (val <= 916.0000000000001) {
      return calculateTree129(features, 191);
    } else {
      return calculateTree129(features, 192);
    }
  }
  if (node === 387) return -0.1397284750634033;
  if (node === 388) return -0.4281694484477639;
  if (node === 193) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree129(features, 387);
    } else if (val <= 14.500000000000002) {
      return calculateTree129(features, 387);
    } else {
      return calculateTree129(features, 388);
    }
  }
  if (node === 194) return 0.013192165679350226;
  if (node === 96) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree129(features, 193);
    } else if (val <= 9.500000000000002) {
      return calculateTree129(features, 193);
    } else {
      return calculateTree129(features, 194);
    }
  }
  if (node === 47) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree129(features, 95);
    } else if (val <= 5.500000000000001) {
      return calculateTree129(features, 95);
    } else {
      return calculateTree129(features, 96);
    }
  }
  if (node === 97) return 0.1279277958620277;
  if (node === 395) return -0.01697868939682766;
  if (node === 396) return -0.34211729400508556;
  if (node === 197) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree129(features, 395);
    } else if (val <= 479.50000000000006) {
      return calculateTree129(features, 395);
    } else {
      return calculateTree129(features, 396);
    }
  }
  if (node === 397) return -0.014070225175349586;
  if (node === 398) return 0.36427123607157014;
  if (node === 198) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree129(features, 397);
    } else if (val <= 8.500000000000002) {
      return calculateTree129(features, 397);
    } else {
      return calculateTree129(features, 398);
    }
  }
  if (node === 98) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree129(features, 197);
    } else if (val <= 787.0000000000001) {
      return calculateTree129(features, 197);
    } else {
      return calculateTree129(features, 198);
    }
  }
  if (node === 48) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree129(features, 97);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree129(features, 97);
    } else {
      return calculateTree129(features, 98);
    }
  }
  if (node === 23) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree129(features, 47);
    } else if (val <= 19.500000000000004) {
      return calculateTree129(features, 47);
    } else {
      return calculateTree129(features, 48);
    }
  }
  if (node === 24) return -0.15339862665372594;
  if (node === 11) {
    const val = features['is_preference_kitchen'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree129(features, 23);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree129(features, 23);
    } else {
      return calculateTree129(features, 24);
    }
  }
  if (node === 25) return -0.06033057378636805;
  if (node === 53) return -0.028232654075115438;
  if (node === 219) return -0.0068974338627693435;
  if (node === 220) return 0.32886325355430673;
  if (node === 109) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree129(features, 219);
    } else if (val <= 43.50000000000001) {
      return calculateTree129(features, 219);
    } else {
      return calculateTree129(features, 220);
    }
  }
  if (node === 110) return -0.024543498888939815;
  if (node === 54) {
    const val = features['is_preference_personal_care'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree129(features, 109);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree129(features, 109);
    } else {
      return calculateTree129(features, 110);
    }
  }
  if (node === 26) {
    const val = features['is_preference_kitchen'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree129(features, 53);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree129(features, 53);
    } else {
      return calculateTree129(features, 54);
    }
  }
  if (node === 12) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree129(features, 25);
    } else if (val <= 4.500000000000001) {
      return calculateTree129(features, 25);
    } else {
      return calculateTree129(features, 26);
    }
  }
  if (node === 5) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree129(features, 11);
    } else if (val <= 24.500000000000004) {
      return calculateTree129(features, 11);
    } else {
      return calculateTree129(features, 12);
    }
  }
  if (node === 27) return -0.31271244785377356;
  if (node === 28) return 0.1817782848226755;
  if (node === 13) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree129(features, 27);
    } else if (val <= 20.500000000000004) {
      return calculateTree129(features, 27);
    } else {
      return calculateTree129(features, 28);
    }
  }
  if (node === 29) return 0.14855220529216626;
  if (node === 247) return -0.22369624232100088;
  if (node === 248) return 0.12988256063698456;
  if (node === 123) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree129(features, 247);
    } else if (val <= 8.500000000000002) {
      return calculateTree129(features, 247);
    } else {
      return calculateTree129(features, 248);
    }
  }
  if (node === 124) return 0.2692527549347267;
  if (node === 61) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree129(features, 123);
    } else if (val <= 234.50000000000003) {
      return calculateTree129(features, 123);
    } else {
      return calculateTree129(features, 124);
    }
  }
  if (node === 125) return 0.1265120350256257;
  if (node === 507) return -0.33234722984523385;
  if (node === 1017) return -0.34740384796881063;
  if (node === 2037) return 0.3105954331254344;
  if (node === 2038) return -0.013626143326627172;
  if (node === 1018) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree129(features, 2037);
    } else if (val <= 6.500000000000001) {
      return calculateTree129(features, 2037);
    } else {
      return calculateTree129(features, 2038);
    }
  }
  if (node === 508) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree129(features, 1017);
    } else if (val <= 3.5000000000000004) {
      return calculateTree129(features, 1017);
    } else {
      return calculateTree129(features, 1018);
    }
  }
  if (node === 253) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree129(features, 507);
    } else if (val <= 26.500000000000004) {
      return calculateTree129(features, 507);
    } else {
      return calculateTree129(features, 508);
    }
  }
  if (node === 509) return 0.1706136619967006;
  if (node === 2043) return -0.15963354760504797;
  if (node === 2044) return 0.21649241915392511;
  if (node === 1021) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree129(features, 2043);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree129(features, 2043);
    } else {
      return calculateTree129(features, 2044);
    }
  }
  if (node === 1022) return -0.13364712812085172;
  if (node === 510) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree129(features, 1021);
    } else if (val <= 9.500000000000002) {
      return calculateTree129(features, 1021);
    } else {
      return calculateTree129(features, 1022);
    }
  }
  if (node === 254) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree129(features, 509);
    } else if (val <= 162.50000000000003) {
      return calculateTree129(features, 509);
    } else {
      return calculateTree129(features, 510);
    }
  }
  if (node === 126) {
    const val = features['is_preference_sports'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree129(features, 253);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree129(features, 253);
    } else {
      return calculateTree129(features, 254);
    }
  }
  if (node === 62) {
    const val = features['is_preference_personal_care'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree129(features, 125);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree129(features, 125);
    } else {
      return calculateTree129(features, 126);
    }
  }
  if (node === 30) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree129(features, 61);
    } else if (val <= 311.50000000000006) {
      return calculateTree129(features, 61);
    } else {
      return calculateTree129(features, 62);
    }
  }
  if (node === 14) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree129(features, 29);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree129(features, 29);
    } else {
      return calculateTree129(features, 30);
    }
  }
  if (node === 6) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree129(features, 13);
    } else if (val <= 253.50000000000003) {
      return calculateTree129(features, 13);
    } else {
      return calculateTree129(features, 14);
    }
  }
  if (node === 2) {
    const val = features['is_preference_kids'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree129(features, 5);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree129(features, 5);
    } else {
      return calculateTree129(features, 6);
    }
  }
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree129(features, 1);
    } else if (val <= 948.0000000000001) {
      return calculateTree129(features, 1);
    } else {
      return calculateTree129(features, 2);
    }
  }
}
function calculateTree130(features, node = 0) {
  if (node === 3) return 4.232467186144081e-5;
  if (node === 9) return -0.3321666535148007;
  if (node === 175) return -0.024415023076791543;
  if (node === 176) return -0.3309157239521571;
  if (node === 87) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree130(features, 175);
    } else if (val <= 29.500000000000004) {
      return calculateTree130(features, 175);
    } else {
      return calculateTree130(features, 176);
    }
  }
  if (node === 177) return 0.17050186684199206;
  if (node === 715) return -0.1655504553543194;
  if (node === 716) return 0.06616379984481668;
  if (node === 357) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree130(features, 715);
    } else if (val <= 803.0000000000001) {
      return calculateTree130(features, 715);
    } else {
      return calculateTree130(features, 716);
    }
  }
  if (node === 717) return -0.45317024915343723;
  if (node === 1437) return -0.20713456286359688;
  if (node === 1438) return 0.22559321406054944;
  if (node === 718) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree130(features, 1437);
    } else if (val <= 25.500000000000004) {
      return calculateTree130(features, 1437);
    } else {
      return calculateTree130(features, 1438);
    }
  }
  if (node === 358) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree130(features, 717);
    } else if (val <= 162.50000000000003) {
      return calculateTree130(features, 717);
    } else {
      return calculateTree130(features, 718);
    }
  }
  if (node === 178) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree130(features, 357);
    } else if (val <= 12.500000000000002) {
      return calculateTree130(features, 357);
    } else {
      return calculateTree130(features, 358);
    }
  }
  if (node === 88) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree130(features, 177);
    } else if (val <= 800.5000000000001) {
      return calculateTree130(features, 177);
    } else {
      return calculateTree130(features, 178);
    }
  }
  if (node === 43) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree130(features, 87);
    } else if (val <= 2.5000000000000004) {
      return calculateTree130(features, 87);
    } else {
      return calculateTree130(features, 88);
    }
  }
  if (node === 89) return 0.07066873687131116;
  if (node === 363) return -0.014247084902418236;
  if (node === 364) return -0.2821419423725055;
  if (node === 181) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree130(features, 363);
    } else if (val <= 24.500000000000004) {
      return calculateTree130(features, 363);
    } else {
      return calculateTree130(features, 364);
    }
  }
  if (node === 182) return -0.2545020832208198;
  if (node === 90) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree130(features, 181);
    } else if (val <= 811.5000000000001) {
      return calculateTree130(features, 181);
    } else {
      return calculateTree130(features, 182);
    }
  }
  if (node === 44) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree130(features, 89);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree130(features, 89);
    } else {
      return calculateTree130(features, 90);
    }
  }
  if (node === 21) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree130(features, 43);
    } else if (val <= 787.0000000000001) {
      return calculateTree130(features, 43);
    } else {
      return calculateTree130(features, 44);
    }
  }
  if (node === 91) return -0.021671858222931488;
  if (node === 185) return -0.13286342191912054;
  if (node === 186) return -0.4401145435025496;
  if (node === 92) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree130(features, 185);
    } else if (val <= 660.0000000000001) {
      return calculateTree130(features, 185);
    } else {
      return calculateTree130(features, 186);
    }
  }
  if (node === 45) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree130(features, 91);
    } else if (val <= 46.50000000000001) {
      return calculateTree130(features, 91);
    } else {
      return calculateTree130(features, 92);
    }
  }
  if (node === 375) return 0.039643955489976956;
  if (node === 376) return 0.2942061709178137;
  if (node === 187) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree130(features, 375);
    } else if (val <= 749.0000000000001) {
      return calculateTree130(features, 375);
    } else {
      return calculateTree130(features, 376);
    }
  }
  if (node === 377) return 0.4308182275895764;
  if (node === 378) return 0.05371328694270377;
  if (node === 188) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree130(features, 377);
    } else if (val <= 234.50000000000003) {
      return calculateTree130(features, 377);
    } else {
      return calculateTree130(features, 378);
    }
  }
  if (node === 93) {
    const val = features['is_preference_personal_care'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree130(features, 187);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree130(features, 187);
    } else {
      return calculateTree130(features, 188);
    }
  }
  if (node === 1519) return 0.024465921698910015;
  if (node === 1520) return 0.16406528503862228;
  if (node === 759) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree130(features, 1519);
    } else if (val <= 804.0000000000001) {
      return calculateTree130(features, 1519);
    } else {
      return calculateTree130(features, 1520);
    }
  }
  if (node === 760) return 0.2252012469539778;
  if (node === 379) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree130(features, 759);
    } else if (val <= 52.50000000000001) {
      return calculateTree130(features, 759);
    } else {
      return calculateTree130(features, 760);
    }
  }
  if (node === 1523) return -0.08352318355780917;
  if (node === 1524) return 0.09706021830326712;
  if (node === 761) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree130(features, 1523);
    } else if (val <= 14.500000000000002) {
      return calculateTree130(features, 1523);
    } else {
      return calculateTree130(features, 1524);
    }
  }
  if (node === 762) return -0.2817185297938513;
  if (node === 380) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree130(features, 761);
    } else if (val <= 811.5000000000001) {
      return calculateTree130(features, 761);
    } else {
      return calculateTree130(features, 762);
    }
  }
  if (node === 189) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree130(features, 379);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree130(features, 379);
    } else {
      return calculateTree130(features, 380);
    }
  }
  if (node === 1527) return -0.2119045034633447;
  if (node === 1528) return -0.007739576646902794;
  if (node === 763) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree130(features, 1527);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree130(features, 1527);
    } else {
      return calculateTree130(features, 1528);
    }
  }
  if (node === 764) return 0.17992789523705527;
  if (node === 381) {
    const val = features['is_preference_grocery'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree130(features, 763);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree130(features, 763);
    } else {
      return calculateTree130(features, 764);
    }
  }
  if (node === 382) return -0.13459111419003233;
  if (node === 190) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree130(features, 381);
    } else if (val <= 13.500000000000002) {
      return calculateTree130(features, 381);
    } else {
      return calculateTree130(features, 382);
    }
  }
  if (node === 94) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree130(features, 189);
    } else if (val <= 816.5000000000001) {
      return calculateTree130(features, 189);
    } else {
      return calculateTree130(features, 190);
    }
  }
  if (node === 46) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree130(features, 93);
    } else if (val <= 1.5000000000000002) {
      return calculateTree130(features, 93);
    } else {
      return calculateTree130(features, 94);
    }
  }
  if (node === 22) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree130(features, 45);
    } else if (val <= 800.5000000000001) {
      return calculateTree130(features, 45);
    } else {
      return calculateTree130(features, 46);
    }
  }
  if (node === 10) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree130(features, 21);
    } else if (val <= 30.500000000000004) {
      return calculateTree130(features, 21);
    } else {
      return calculateTree130(features, 22);
    }
  }
  if (node === 4) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree130(features, 9);
    } else if (val <= 799.5000000000001) {
      return calculateTree130(features, 9);
    } else {
      return calculateTree130(features, 10);
    }
  }
  if (node === 1) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree130(features, 3);
    } else if (val <= 795.5000000000001) {
      return calculateTree130(features, 3);
    } else {
      return calculateTree130(features, 4);
    }
  }
  if (node === 2) return 0.004272743044462323;
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree130(features, 1);
    } else if (val <= 835.5000000000001) {
      return calculateTree130(features, 1);
    } else {
      return calculateTree130(features, 2);
    }
  }
}
function calculateTree131(features, node = 0) {
  if (node === 63) return 0.04287985402740776;
  if (node === 129) return -0.4477749252060736;
  if (node === 130) return -0.06001557166376375;
  if (node === 64) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree131(features, 129);
    } else if (val <= 19.500000000000004) {
      return calculateTree131(features, 129);
    } else {
      return calculateTree131(features, 130);
    }
  }
  if (node === 31) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree131(features, 63);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree131(features, 63);
    } else {
      return calculateTree131(features, 64);
    }
  }
  if (node === 263) return 0.04236309591275602;
  if (node === 264) return 0.20909765632717406;
  if (node === 131) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree131(features, 263);
    } else if (val <= 155.50000000000003) {
      return calculateTree131(features, 263);
    } else {
      return calculateTree131(features, 264);
    }
  }
  if (node === 132) return 0.3618829856783517;
  if (node === 65) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree131(features, 131);
    } else if (val <= 39.50000000000001) {
      return calculateTree131(features, 131);
    } else {
      return calculateTree131(features, 132);
    }
  }
  if (node === 535) return -0.03966220436978973;
  if (node === 1073) return 0.28671739238446514;
  if (node === 1074) return 0.03637202781429718;
  if (node === 536) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree131(features, 1073);
    } else if (val <= 44.50000000000001) {
      return calculateTree131(features, 1073);
    } else {
      return calculateTree131(features, 1074);
    }
  }
  if (node === 267) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree131(features, 535);
    } else if (val <= 43.50000000000001) {
      return calculateTree131(features, 535);
    } else {
      return calculateTree131(features, 536);
    }
  }
  if (node === 268) return -0.22281109318376408;
  if (node === 133) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree131(features, 267);
    } else if (val <= 46.50000000000001) {
      return calculateTree131(features, 267);
    } else {
      return calculateTree131(features, 268);
    }
  }
  if (node === 134) return 0.09601361265754264;
  if (node === 66) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree131(features, 133);
    } else if (val <= 48.50000000000001) {
      return calculateTree131(features, 133);
    } else {
      return calculateTree131(features, 134);
    }
  }
  if (node === 32) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree131(features, 65);
    } else if (val <= 40.50000000000001) {
      return calculateTree131(features, 65);
    } else {
      return calculateTree131(features, 66);
    }
  }
  if (node === 15) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree131(features, 31);
    } else if (val <= 32.50000000000001) {
      return calculateTree131(features, 31);
    } else {
      return calculateTree131(features, 32);
    }
  }
  if (node === 16) return -0.21930848480641377;
  if (node === 7) {
    const val = features['is_preference_grocery'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree131(features, 15);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree131(features, 15);
    } else {
      return calculateTree131(features, 16);
    }
  }
  if (node === 17) return 0.29631933228980895;
  if (node === 18) return 0.06666931236664751;
  if (node === 8) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree131(features, 17);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree131(features, 17);
    } else {
      return calculateTree131(features, 18);
    }
  }
  if (node === 3) {
    const val = features['is_preference_beauty'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree131(features, 7);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree131(features, 7);
    } else {
      return calculateTree131(features, 8);
    }
  }
  if (node === 4) return -0.30313450219635585;
  if (node === 1) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree131(features, 3);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree131(features, 3);
    } else {
      return calculateTree131(features, 4);
    }
  }
  if (node === 11) return -0.019169201874344642;
  if (node === 51) return -0.4312020371872011;
  if (node === 52) return -0.03315052949260197;
  if (node === 25) {
    const val = features['is_preference_pets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree131(features, 51);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree131(features, 51);
    } else {
      return calculateTree131(features, 52);
    }
  }
  if (node === 26) return 0.14644406067726012;
  if (node === 12) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree131(features, 25);
    } else if (val <= 57.50000000000001) {
      return calculateTree131(features, 25);
    } else {
      return calculateTree131(features, 26);
    }
  }
  if (node === 5) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree131(features, 11);
    } else if (val <= 44.50000000000001) {
      return calculateTree131(features, 11);
    } else {
      return calculateTree131(features, 12);
    }
  }
  if (node === 13) return 0.0022619309343482543;
  if (node === 119) return -0.3769694643978358;
  if (node === 241) return 0.10945844606685726;
  if (node === 1943) return 0.8917732527420028;
  if (node === 1944) return -0.3244862002339715;
  if (node === 971) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree131(features, 1943);
    } else if (val <= 835.5000000000001) {
      return calculateTree131(features, 1943);
    } else {
      return calculateTree131(features, 1944);
    }
  }
  if (node === 972) return -0.3433035053524421;
  if (node === 485) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree131(features, 971);
    } else if (val <= 286.50000000000006) {
      return calculateTree131(features, 971);
    } else {
      return calculateTree131(features, 972);
    }
  }
  if (node === 486) return 0.13008574560694341;
  if (node === 242) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree131(features, 485);
    } else if (val <= 882.5000000000001) {
      return calculateTree131(features, 485);
    } else {
      return calculateTree131(features, 486);
    }
  }
  if (node === 120) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree131(features, 241);
    } else if (val <= 566.0000000000001) {
      return calculateTree131(features, 241);
    } else {
      return calculateTree131(features, 242);
    }
  }
  if (node === 59) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree131(features, 119);
    } else if (val <= 41.50000000000001) {
      return calculateTree131(features, 119);
    } else {
      return calculateTree131(features, 120);
    }
  }
  if (node === 243) return 0.09602012661759354;
  if (node === 489) return -0.355065737273992;
  if (node === 490) return -0.12825596552579832;
  if (node === 244) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree131(features, 489);
    } else if (val <= 531.5000000000001) {
      return calculateTree131(features, 489);
    } else {
      return calculateTree131(features, 490);
    }
  }
  if (node === 121) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree131(features, 243);
    } else if (val <= 41.50000000000001) {
      return calculateTree131(features, 243);
    } else {
      return calculateTree131(features, 244);
    }
  }
  if (node === 245) return 0.30223586866142177;
  if (node === 246) return -0.046220529300522596;
  if (node === 122) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree131(features, 245);
    } else if (val <= 733.5000000000001) {
      return calculateTree131(features, 245);
    } else {
      return calculateTree131(features, 246);
    }
  }
  if (node === 60) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree131(features, 121);
    } else if (val <= 715.5000000000001) {
      return calculateTree131(features, 121);
    } else {
      return calculateTree131(features, 122);
    }
  }
  if (node === 29) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree131(features, 59);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree131(features, 59);
    } else {
      return calculateTree131(features, 60);
    }
  }
  if (node === 30) return -0.0005136955916224895;
  if (node === 14) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree131(features, 29);
    } else if (val <= 316.50000000000006) {
      return calculateTree131(features, 29);
    } else {
      return calculateTree131(features, 30);
    }
  }
  if (node === 6) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree131(features, 13);
    } else if (val <= 284.50000000000006) {
      return calculateTree131(features, 13);
    } else {
      return calculateTree131(features, 14);
    }
  }
  if (node === 2) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree131(features, 5);
    } else if (val <= 3.0000000000000004) {
      return calculateTree131(features, 5);
    } else {
      return calculateTree131(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree131(features, 1);
    } else if (val <= 3.5000000000000004) {
      return calculateTree131(features, 1);
    } else {
      return calculateTree131(features, 2);
    }
  }
}
function calculateTree132(features, node = 0) {
  if (node === 1) return 0.0009263058699835302;
  if (node === 23) return 0.17809163369264516;
  if (node === 24) return 0.013506233392020476;
  if (node === 11) {
    const val = features['is_preference_home'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree132(features, 23);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree132(features, 23);
    } else {
      return calculateTree132(features, 24);
    }
  }
  if (node === 103) return -0.014720945975612337;
  if (node === 104) return -0.08944167458912153;
  if (node === 51) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree132(features, 103);
    } else if (val <= 525.0000000000001) {
      return calculateTree132(features, 103);
    } else {
      return calculateTree132(features, 104);
    }
  }
  if (node === 423) return -0.1133668415726636;
  if (node === 424) return 0.2203792221225322;
  if (node === 211) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree132(features, 423);
    } else if (val <= 3.5000000000000004) {
      return calculateTree132(features, 423);
    } else {
      return calculateTree132(features, 424);
    }
  }
  if (node === 212) return 0.34626809950898974;
  if (node === 105) {
    const val = features['is_preference_beauty'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree132(features, 211);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree132(features, 211);
    } else {
      return calculateTree132(features, 212);
    }
  }
  if (node === 1711) return 0.052469701452796995;
  if (node === 1712) return 0.5395519758582517;
  if (node === 855) {
    const val = features['is_preference_pets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree132(features, 1711);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree132(features, 1711);
    } else {
      return calculateTree132(features, 1712);
    }
  }
  if (node === 1713) return 0.21487535510535732;
  if (node === 1714) return -0.14171592563775667;
  if (node === 856) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree132(features, 1713);
    } else if (val <= 234.50000000000003) {
      return calculateTree132(features, 1713);
    } else {
      return calculateTree132(features, 1714);
    }
  }
  if (node === 427) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree132(features, 855);
    } else if (val <= 24.500000000000004) {
      return calculateTree132(features, 855);
    } else {
      return calculateTree132(features, 856);
    }
  }
  if (node === 1715) return -0.0499126899942042;
  if (node === 1716) return -0.37359291577743475;
  if (node === 857) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree132(features, 1715);
    } else if (val <= 7.500000000000001) {
      return calculateTree132(features, 1715);
    } else {
      return calculateTree132(features, 1716);
    }
  }
  if (node === 858) return 0.12104001752952284;
  if (node === 428) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree132(features, 857);
    } else if (val <= 498.00000000000006) {
      return calculateTree132(features, 857);
    } else {
      return calculateTree132(features, 858);
    }
  }
  if (node === 213) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree132(features, 427);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree132(features, 427);
    } else {
      return calculateTree132(features, 428);
    }
  }
  if (node === 1719) return 0.1251039964592609;
  if (node === 1720) return -0.146732820177111;
  if (node === 859) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree132(features, 1719);
    } else if (val <= 794.0000000000001) {
      return calculateTree132(features, 1719);
    } else {
      return calculateTree132(features, 1720);
    }
  }
  if (node === 860) return -0.12196385049720285;
  if (node === 429) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree132(features, 859);
    } else if (val <= 859.5000000000001) {
      return calculateTree132(features, 859);
    } else {
      return calculateTree132(features, 860);
    }
  }
  if (node === 861) return -0.308186104348584;
  if (node === 862) return 0.015321890430707077;
  if (node === 430) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree132(features, 861);
    } else if (val <= 35.50000000000001) {
      return calculateTree132(features, 861);
    } else {
      return calculateTree132(features, 862);
    }
  }
  if (node === 214) {
    const val = features['is_preference_grocery'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree132(features, 429);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree132(features, 429);
    } else {
      return calculateTree132(features, 430);
    }
  }
  if (node === 106) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree132(features, 213);
    } else if (val <= 500.50000000000006) {
      return calculateTree132(features, 213);
    } else {
      return calculateTree132(features, 214);
    }
  }
  if (node === 52) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree132(features, 105);
    } else if (val <= 804.5000000000001) {
      return calculateTree132(features, 105);
    } else {
      return calculateTree132(features, 106);
    }
  }
  if (node === 25) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree132(features, 51);
    } else if (val <= 780.0000000000001) {
      return calculateTree132(features, 51);
    } else {
      return calculateTree132(features, 52);
    }
  }
  if (node === 215) return 0.12171572209372471;
  if (node === 433) return -0.06468414331073169;
  if (node === 434) return -0.4690505328050318;
  if (node === 216) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree132(features, 433);
    } else if (val <= 1.5000000000000002) {
      return calculateTree132(features, 433);
    } else {
      return calculateTree132(features, 434);
    }
  }
  if (node === 107) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree132(features, 215);
    } else if (val <= 20.500000000000004) {
      return calculateTree132(features, 215);
    } else {
      return calculateTree132(features, 216);
    }
  }
  if (node === 108) return -0.7157185860107554;
  if (node === 53) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree132(features, 107);
    } else if (val <= 12.500000000000002) {
      return calculateTree132(features, 107);
    } else {
      return calculateTree132(features, 108);
    }
  }
  if (node === 219) return -0.1384638439888456;
  if (node === 220) return 0.17971498879801862;
  if (node === 109) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree132(features, 219);
    } else if (val <= 962.5000000000001) {
      return calculateTree132(features, 219);
    } else {
      return calculateTree132(features, 220);
    }
  }
  if (node === 110) return -0.23265480133575062;
  if (node === 54) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree132(features, 109);
    } else if (val <= 41.50000000000001) {
      return calculateTree132(features, 109);
    } else {
      return calculateTree132(features, 110);
    }
  }
  if (node === 26) {
    const val = features['is_preference_household'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree132(features, 53);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree132(features, 53);
    } else {
      return calculateTree132(features, 54);
    }
  }
  if (node === 12) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree132(features, 25);
    } else if (val <= 943.5000000000001) {
      return calculateTree132(features, 25);
    } else {
      return calculateTree132(features, 26);
    }
  }
  if (node === 5) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree132(features, 11);
    } else if (val <= 39.50000000000001) {
      return calculateTree132(features, 11);
    } else {
      return calculateTree132(features, 12);
    }
  }
  if (node === 55) return -0.22146355937197346;
  if (node === 56) return 0.03721531052539957;
  if (node === 27) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree132(features, 55);
    } else if (val <= 511.00000000000006) {
      return calculateTree132(features, 55);
    } else {
      return calculateTree132(features, 56);
    }
  }
  if (node === 28) return -0.002266115581482046;
  if (node === 13) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree132(features, 27);
    } else if (val <= 6.000000000000001) {
      return calculateTree132(features, 27);
    } else {
      return calculateTree132(features, 28);
    }
  }
  if (node === 29) return 0.22068708542191479;
  if (node === 30) return 0.027621582262250706;
  if (node === 14) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree132(features, 29);
    } else if (val <= 953.5000000000001) {
      return calculateTree132(features, 29);
    } else {
      return calculateTree132(features, 30);
    }
  }
  if (node === 6) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree132(features, 13);
    } else if (val <= 944.5000000000001) {
      return calculateTree132(features, 13);
    } else {
      return calculateTree132(features, 14);
    }
  }
  if (node === 2) {
    const val = features['is_preference_women_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree132(features, 5);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree132(features, 5);
    } else {
      return calculateTree132(features, 6);
    }
  }
  if (node === 0) {
    const val = features['is_preference_kids'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree132(features, 1);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree132(features, 1);
    } else {
      return calculateTree132(features, 2);
    }
  }
}
function calculateTree133(features, node = 0) {
  if (node === 127) return 0.018639025082807917;
  if (node === 257) return -0.3434260369082995;
  if (node === 258) return 0.030521514036949908;
  if (node === 128) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree133(features, 257);
    } else if (val <= 29.500000000000004) {
      return calculateTree133(features, 257);
    } else {
      return calculateTree133(features, 258);
    }
  }
  if (node === 63) {
    const val = features['is_preference_kids'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree133(features, 127);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree133(features, 127);
    } else {
      return calculateTree133(features, 128);
    }
  }
  if (node === 519) return -0.21507510133778168;
  if (node === 1041) return -0.03903201427225761;
  if (node === 1042) return 0.20981468551250668;
  if (node === 520) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree133(features, 1041);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree133(features, 1041);
    } else {
      return calculateTree133(features, 1042);
    }
  }
  if (node === 259) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree133(features, 519);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree133(features, 519);
    } else {
      return calculateTree133(features, 520);
    }
  }
  if (node === 521) return 0.32166093087262;
  if (node === 522) return 0.01700318443161697;
  if (node === 260) {
    const val = features['is_preference_kids'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree133(features, 521);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree133(features, 521);
    } else {
      return calculateTree133(features, 522);
    }
  }
  if (node === 129) {
    const val = features['is_preference_kitchen'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree133(features, 259);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree133(features, 259);
    } else {
      return calculateTree133(features, 260);
    }
  }
  if (node === 130) return 0.32684579722280405;
  if (node === 64) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree133(features, 129);
    } else if (val <= 660.0000000000001) {
      return calculateTree133(features, 129);
    } else {
      return calculateTree133(features, 130);
    }
  }
  if (node === 31) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree133(features, 63);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree133(features, 63);
    } else {
      return calculateTree133(features, 64);
    }
  }
  if (node === 65) return 0.27917614347948305;
  if (node === 535) return 0.28639054939254793;
  if (node === 536) return -0.0754458609948274;
  if (node === 267) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree133(features, 535);
    } else if (val <= 4.500000000000001) {
      return calculateTree133(features, 535);
    } else {
      return calculateTree133(features, 536);
    }
  }
  if (node === 268) return -0.1506750159857931;
  if (node === 133) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree133(features, 267);
    } else if (val <= 3.5000000000000004) {
      return calculateTree133(features, 267);
    } else {
      return calculateTree133(features, 268);
    }
  }
  if (node === 134) return -0.30747676481549246;
  if (node === 66) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree133(features, 133);
    } else if (val <= 9.500000000000002) {
      return calculateTree133(features, 133);
    } else {
      return calculateTree133(features, 134);
    }
  }
  if (node === 32) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree133(features, 65);
    } else if (val <= 15.500000000000002) {
      return calculateTree133(features, 65);
    } else {
      return calculateTree133(features, 66);
    }
  }
  if (node === 15) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree133(features, 31);
    } else if (val <= 787.0000000000001) {
      return calculateTree133(features, 31);
    } else {
      return calculateTree133(features, 32);
    }
  }
  if (node === 33) return 0.15165324127902555;
  if (node === 34) return -0.03427496562251708;
  if (node === 16) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree133(features, 33);
    } else if (val <= 49.50000000000001) {
      return calculateTree133(features, 33);
    } else {
      return calculateTree133(features, 34);
    }
  }
  if (node === 7) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree133(features, 15);
    } else if (val <= 13.500000000000002) {
      return calculateTree133(features, 15);
    } else {
      return calculateTree133(features, 16);
    }
  }
  if (node === 8) return -0.20167057412886105;
  if (node === 3) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree133(features, 7);
    } else if (val <= 944.5000000000001) {
      return calculateTree133(features, 7);
    } else {
      return calculateTree133(features, 8);
    }
  }
  if (node === 9) return -0.006499141319704531;
  if (node === 10) return 0.22870062268056518;
  if (node === 4) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree133(features, 9);
    } else if (val <= 63.50000000000001) {
      return calculateTree133(features, 9);
    } else {
      return calculateTree133(features, 10);
    }
  }
  if (node === 1) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree133(features, 3);
    } else if (val <= 470.00000000000006) {
      return calculateTree133(features, 3);
    } else {
      return calculateTree133(features, 4);
    }
  }
  if (node === 95) return 0.06392284670046221;
  if (node === 96) return -0.3530753087378153;
  if (node === 47) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree133(features, 95);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree133(features, 95);
    } else {
      return calculateTree133(features, 96);
    }
  }
  if (node === 48) return 0.11500391094273874;
  if (node === 23) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree133(features, 47);
    } else if (val <= 229.50000000000003) {
      return calculateTree133(features, 47);
    } else {
      return calculateTree133(features, 48);
    }
  }
  if (node === 49) return -0.3304876092595474;
  if (node === 203) return 0.18240583763521478;
  if (node === 204) return -0.08481527965088663;
  if (node === 101) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree133(features, 203);
    } else if (val <= 43.50000000000001) {
      return calculateTree133(features, 203);
    } else {
      return calculateTree133(features, 204);
    }
  }
  if (node === 102) return -0.20085324430344614;
  if (node === 50) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree133(features, 101);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree133(features, 101);
    } else {
      return calculateTree133(features, 102);
    }
  }
  if (node === 24) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree133(features, 49);
    } else if (val <= 35.50000000000001) {
      return calculateTree133(features, 49);
    } else {
      return calculateTree133(features, 50);
    }
  }
  if (node === 11) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree133(features, 23);
    } else if (val <= 31.500000000000004) {
      return calculateTree133(features, 23);
    } else {
      return calculateTree133(features, 24);
    }
  }
  if (node === 25) return -0.31749035343378657;
  if (node === 53) return -0.23466554176996315;
  if (node === 109) return 0.695364576606618;
  if (node === 221) return -0.3246299289287875;
  if (node === 222) return 0.1434187954630605;
  if (node === 110) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree133(features, 221);
    } else if (val <= 10.500000000000002) {
      return calculateTree133(features, 221);
    } else {
      return calculateTree133(features, 222);
    }
  }
  if (node === 54) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree133(features, 109);
    } else if (val <= 2.5000000000000004) {
      return calculateTree133(features, 109);
    } else {
      return calculateTree133(features, 110);
    }
  }
  if (node === 26) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree133(features, 53);
    } else if (val <= 27.500000000000004) {
      return calculateTree133(features, 53);
    } else {
      return calculateTree133(features, 54);
    }
  }
  if (node === 12) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree133(features, 25);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree133(features, 25);
    } else {
      return calculateTree133(features, 26);
    }
  }
  if (node === 5) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree133(features, 11);
    } else if (val <= 310.00000000000006) {
      return calculateTree133(features, 11);
    } else {
      return calculateTree133(features, 12);
    }
  }
  if (node === 6) return -0.0010377634872116325;
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree133(features, 5);
    } else if (val <= 28.500000000000004) {
      return calculateTree133(features, 5);
    } else {
      return calculateTree133(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree133(features, 1);
    } else if (val <= 18.500000000000004) {
      return calculateTree133(features, 1);
    } else {
      return calculateTree133(features, 2);
    }
  }
}
function calculateTree134(features, node = 0) {
  if (node === 3) return -0.001994070619379577;
  if (node === 9) return -0.28544733386094323;
  if (node === 21) return -0.3244586940811834;
  if (node === 22) return 0.22073162844151617;
  if (node === 10) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree134(features, 21);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree134(features, 21);
    } else {
      return calculateTree134(features, 22);
    }
  }
  if (node === 4) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree134(features, 9);
    } else if (val <= 491.50000000000006) {
      return calculateTree134(features, 9);
    } else {
      return calculateTree134(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree134(features, 3);
    } else if (val <= 464.50000000000006) {
      return calculateTree134(features, 3);
    } else {
      return calculateTree134(features, 4);
    }
  }
  if (node === 11) return -9.204626034474014e-5;
  if (node === 103) return -0.31355980078779927;
  if (node === 209) return 1.0823605363859594;
  if (node === 843) return -0.31141291962884793;
  if (node === 1689) return -0.318819671876662;
  if (node === 1690) return 0.20444971788523267;
  if (node === 844) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree134(features, 1689);
    } else if (val <= 172.50000000000003) {
      return calculateTree134(features, 1689);
    } else {
      return calculateTree134(features, 1690);
    }
  }
  if (node === 421) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree134(features, 843);
    } else if (val <= 658.5000000000001) {
      return calculateTree134(features, 843);
    } else {
      return calculateTree134(features, 844);
    }
  }
  if (node === 422) return -0.3130659619935995;
  if (node === 210) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree134(features, 421);
    } else if (val <= 194.00000000000003) {
      return calculateTree134(features, 421);
    } else {
      return calculateTree134(features, 422);
    }
  }
  if (node === 104) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree134(features, 209);
    } else if (val <= 606.5000000000001) {
      return calculateTree134(features, 209);
    } else {
      return calculateTree134(features, 210);
    }
  }
  if (node === 51) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree134(features, 103);
    } else if (val <= 602.5000000000001) {
      return calculateTree134(features, 103);
    } else {
      return calculateTree134(features, 104);
    }
  }
  if (node === 423) return 0.1126325447015797;
  if (node === 424) return -0.32941306165068057;
  if (node === 211) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree134(features, 423);
    } else if (val <= 525.0000000000001) {
      return calculateTree134(features, 423);
    } else {
      return calculateTree134(features, 424);
    }
  }
  if (node === 212) return 0.16538740222233797;
  if (node === 105) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree134(features, 211);
    } else if (val <= 589.5000000000001) {
      return calculateTree134(features, 211);
    } else {
      return calculateTree134(features, 212);
    }
  }
  if (node === 213) return -0.3578663654955729;
  if (node === 429) return 0.12156764202444806;
  if (node === 1723) return -0.3990566858645835;
  if (node === 1724) return 0.058119162605717374;
  if (node === 861) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree134(features, 1723);
    } else if (val <= 194.00000000000003) {
      return calculateTree134(features, 1723);
    } else {
      return calculateTree134(features, 1724);
    }
  }
  if (node === 1725) return 0.017938918839273402;
  if (node === 1726) return 0.10915047741004964;
  if (node === 862) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree134(features, 1725);
    } else if (val <= 201.50000000000003) {
      return calculateTree134(features, 1725);
    } else {
      return calculateTree134(features, 1726);
    }
  }
  if (node === 430) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree134(features, 861);
    } else if (val <= 789.5000000000001) {
      return calculateTree134(features, 861);
    } else {
      return calculateTree134(features, 862);
    }
  }
  if (node === 214) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree134(features, 429);
    } else if (val <= 658.5000000000001) {
      return calculateTree134(features, 429);
    } else {
      return calculateTree134(features, 430);
    }
  }
  if (node === 106) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree134(features, 213);
    } else if (val <= 635.0000000000001) {
      return calculateTree134(features, 213);
    } else {
      return calculateTree134(features, 214);
    }
  }
  if (node === 52) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree134(features, 105);
    } else if (val <= 602.5000000000001) {
      return calculateTree134(features, 105);
    } else {
      return calculateTree134(features, 106);
    }
  }
  if (node === 25) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree134(features, 51);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree134(features, 51);
    } else {
      return calculateTree134(features, 52);
    }
  }
  if (node === 26) return -0.17833338608316593;
  if (node === 12) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree134(features, 25);
    } else if (val <= 963.5000000000001) {
      return calculateTree134(features, 25);
    } else {
      return calculateTree134(features, 26);
    }
  }
  if (node === 5) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree134(features, 11);
    } else if (val <= 171.00000000000003) {
      return calculateTree134(features, 11);
    } else {
      return calculateTree134(features, 12);
    }
  }
  if (node === 111) return -0.33731897045513953;
  if (node === 451) return 0.10971685189457285;
  if (node === 452) return -0.11791526170635429;
  if (node === 225) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree134(features, 451);
    } else if (val <= 2.5000000000000004) {
      return calculateTree134(features, 451);
    } else {
      return calculateTree134(features, 452);
    }
  }
  if (node === 226) return -0.26974245231273036;
  if (node === 112) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree134(features, 225);
    } else if (val <= 236.00000000000003) {
      return calculateTree134(features, 225);
    } else {
      return calculateTree134(features, 226);
    }
  }
  if (node === 55) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree134(features, 111);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree134(features, 111);
    } else {
      return calculateTree134(features, 112);
    }
  }
  if (node === 113) return 0.18393969546567124;
  if (node === 114) return -0.3190108158226785;
  if (node === 56) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree134(features, 113);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree134(features, 113);
    } else {
      return calculateTree134(features, 114);
    }
  }
  if (node === 27) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree134(features, 55);
    } else if (val <= 672.5000000000001) {
      return calculateTree134(features, 55);
    } else {
      return calculateTree134(features, 56);
    }
  }
  if (node === 231) return 0.31029014387590054;
  if (node === 232) return -0.08273766177599125;
  if (node === 115) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree134(features, 231);
    } else if (val <= 23.500000000000004) {
      return calculateTree134(features, 231);
    } else {
      return calculateTree134(features, 232);
    }
  }
  if (node === 116) return -0.4192823543545243;
  if (node === 57) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree134(features, 115);
    } else if (val <= 934.0000000000001) {
      return calculateTree134(features, 115);
    } else {
      return calculateTree134(features, 116);
    }
  }
  if (node === 58) return -0.3504489080829111;
  if (node === 28) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree134(features, 57);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree134(features, 57);
    } else {
      return calculateTree134(features, 58);
    }
  }
  if (node === 13) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree134(features, 27);
    } else if (val <= 789.5000000000001) {
      return calculateTree134(features, 27);
    } else {
      return calculateTree134(features, 28);
    }
  }
  if (node === 14) return -0.0003556890000204879;
  if (node === 6) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree134(features, 13);
    } else if (val <= 265.00000000000006) {
      return calculateTree134(features, 13);
    } else {
      return calculateTree134(features, 14);
    }
  }
  if (node === 2) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree134(features, 5);
    } else if (val <= 231.50000000000003) {
      return calculateTree134(features, 5);
    } else {
      return calculateTree134(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree134(features, 1);
    } else if (val <= 470.50000000000006) {
      return calculateTree134(features, 1);
    } else {
      return calculateTree134(features, 2);
    }
  }
}
function calculateTree135(features, node = 0) {
  if (node === 3) return -0.00034128847661955185;
  if (node === 79) return 0.31614842524495745;
  if (node === 80) return -0.32018982196441587;
  if (node === 39) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree135(features, 79);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree135(features, 79);
    } else {
      return calculateTree135(features, 80);
    }
  }
  if (node === 81) return -0.32309172026408883;
  if (node === 82) return -0.002107844502343548;
  if (node === 40) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree135(features, 81);
    } else if (val <= 611.5000000000001) {
      return calculateTree135(features, 81);
    } else {
      return calculateTree135(features, 82);
    }
  }
  if (node === 19) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree135(features, 39);
    } else if (val <= 606.5000000000001) {
      return calculateTree135(features, 39);
    } else {
      return calculateTree135(features, 40);
    }
  }
  if (node === 20) return -0.3215081375495386;
  if (node === 9) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree135(features, 19);
    } else if (val <= 276.00000000000006) {
      return calculateTree135(features, 19);
    } else {
      return calculateTree135(features, 20);
    }
  }
  if (node === 21) return -0.2991673157934105;
  if (node === 91) return 0.022617231101291258;
  if (node === 92) return -0.320724110987121;
  if (node === 45) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree135(features, 91);
    } else if (val <= 865.0000000000001) {
      return calculateTree135(features, 91);
    } else {
      return calculateTree135(features, 92);
    }
  }
  if (node === 93) return -0.142831799515782;
  if (node === 94) return 0.33647854881836775;
  if (node === 46) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree135(features, 93);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree135(features, 93);
    } else {
      return calculateTree135(features, 94);
    }
  }
  if (node === 22) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree135(features, 45);
    } else if (val <= 884.0000000000001) {
      return calculateTree135(features, 45);
    } else {
      return calculateTree135(features, 46);
    }
  }
  if (node === 10) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree135(features, 21);
    } else if (val <= 606.5000000000001) {
      return calculateTree135(features, 21);
    } else {
      return calculateTree135(features, 22);
    }
  }
  if (node === 4) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree135(features, 9);
    } else if (val <= 435.00000000000006) {
      return calculateTree135(features, 9);
    } else {
      return calculateTree135(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree135(features, 3);
    } else if (val <= 602.5000000000001) {
      return calculateTree135(features, 3);
    } else {
      return calculateTree135(features, 4);
    }
  }
  if (node === 95) return 0.08858730187102347;
  if (node === 96) return -0.30596826389988224;
  if (node === 47) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree135(features, 95);
    } else if (val <= 52.50000000000001) {
      return calculateTree135(features, 95);
    } else {
      return calculateTree135(features, 96);
    }
  }
  if (node === 97) return -0.3108927472406837;
  if (node === 98) return -0.18489367200495563;
  if (node === 48) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree135(features, 97);
    } else if (val <= 654.0000000000001) {
      return calculateTree135(features, 97);
    } else {
      return calculateTree135(features, 98);
    }
  }
  if (node === 23) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree135(features, 47);
    } else if (val <= 12.000000000000002) {
      return calculateTree135(features, 47);
    } else {
      return calculateTree135(features, 48);
    }
  }
  if (node === 24) return 0.1063496025331594;
  if (node === 11) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree135(features, 23);
    } else if (val <= 776.0000000000001) {
      return calculateTree135(features, 23);
    } else {
      return calculateTree135(features, 24);
    }
  }
  if (node === 25) return -0.35653935005197235;
  if (node === 215) return 0.07649819918131613;
  if (node === 433) return -0.1899110266586194;
  if (node === 869) return 0.09966148982002701;
  if (node === 1741) return -0.11536948070591443;
  if (node === 1742) return 0.02953306647645563;
  if (node === 870) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree135(features, 1741);
    } else if (val <= 389.00000000000006) {
      return calculateTree135(features, 1741);
    } else {
      return calculateTree135(features, 1742);
    }
  }
  if (node === 434) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree135(features, 869);
    } else if (val <= 240.00000000000003) {
      return calculateTree135(features, 869);
    } else {
      return calculateTree135(features, 870);
    }
  }
  if (node === 216) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree135(features, 433);
    } else if (val <= 181.50000000000003) {
      return calculateTree135(features, 433);
    } else {
      return calculateTree135(features, 434);
    }
  }
  if (node === 107) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree135(features, 215);
    } else if (val <= 151.50000000000003) {
      return calculateTree135(features, 215);
    } else {
      return calculateTree135(features, 216);
    }
  }
  if (node === 871) return 0.12395407622674892;
  if (node === 1745) return -0.19381343085926603;
  if (node === 1746) return 0.0957900119349733;
  if (node === 872) {
    const val = features['is_preference_kitchen'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree135(features, 1745);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree135(features, 1745);
    } else {
      return calculateTree135(features, 1746);
    }
  }
  if (node === 435) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree135(features, 871);
    } else if (val <= 2.5000000000000004) {
      return calculateTree135(features, 871);
    } else {
      return calculateTree135(features, 872);
    }
  }
  if (node === 436) return 0.09593509670005212;
  if (node === 217) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree135(features, 435);
    } else if (val <= 181.50000000000003) {
      return calculateTree135(features, 435);
    } else {
      return calculateTree135(features, 436);
    }
  }
  if (node === 218) return 0.12481922234874958;
  if (node === 108) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree135(features, 217);
    } else if (val <= 29.500000000000004) {
      return calculateTree135(features, 217);
    } else {
      return calculateTree135(features, 218);
    }
  }
  if (node === 53) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree135(features, 107);
    } else if (val <= 1.5000000000000002) {
      return calculateTree135(features, 107);
    } else {
      return calculateTree135(features, 108);
    }
  }
  if (node === 54) return -0.27221417195634956;
  if (node === 26) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree135(features, 53);
    } else if (val <= 776.0000000000001) {
      return calculateTree135(features, 53);
    } else {
      return calculateTree135(features, 54);
    }
  }
  if (node === 12) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree135(features, 25);
    } else if (val <= 12.000000000000002) {
      return calculateTree135(features, 25);
    } else {
      return calculateTree135(features, 26);
    }
  }
  if (node === 5) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree135(features, 11);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree135(features, 11);
    } else {
      return calculateTree135(features, 12);
    }
  }
  if (node === 55) return 0.0883601068912089;
  if (node === 56) return -0.43490309594574295;
  if (node === 27) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree135(features, 55);
    } else if (val <= 34.50000000000001) {
      return calculateTree135(features, 55);
    } else {
      return calculateTree135(features, 56);
    }
  }
  if (node === 28) return -0.28295695608402777;
  if (node === 13) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree135(features, 27);
    } else if (val <= 511.50000000000006) {
      return calculateTree135(features, 27);
    } else {
      return calculateTree135(features, 28);
    }
  }
  if (node === 14) return -2.452420952674832e-5;
  if (node === 6) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree135(features, 13);
    } else if (val <= 670.5000000000001) {
      return calculateTree135(features, 13);
    } else {
      return calculateTree135(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree135(features, 5);
    } else if (val <= 667.5000000000001) {
      return calculateTree135(features, 5);
    } else {
      return calculateTree135(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree135(features, 1);
    } else if (val <= 628.5000000000001) {
      return calculateTree135(features, 1);
    } else {
      return calculateTree135(features, 2);
    }
  }
}
function calculateTree136(features, node = 0) {
  if (node === 3) return 0.0007065727881173685;
  if (node === 159) return -0.4999663309871512;
  if (node === 160) return -0.020019622130513146;
  if (node === 79) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree136(features, 159);
    } else if (val <= 1.5000000000000002) {
      return calculateTree136(features, 159);
    } else {
      return calculateTree136(features, 160);
    }
  }
  if (node === 161) return 0.35630087077333555;
  if (node === 325) return -0.02733601826790064;
  if (node === 326) return 0.08810747196221042;
  if (node === 162) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree136(features, 325);
    } else if (val <= 944.5000000000001) {
      return calculateTree136(features, 325);
    } else {
      return calculateTree136(features, 326);
    }
  }
  if (node === 80) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree136(features, 161);
    } else if (val <= 14.500000000000002) {
      return calculateTree136(features, 161);
    } else {
      return calculateTree136(features, 162);
    }
  }
  if (node === 39) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree136(features, 79);
    } else if (val <= 10.000000000000002) {
      return calculateTree136(features, 79);
    } else {
      return calculateTree136(features, 80);
    }
  }
  if (node === 40) return 0.25717391658863986;
  if (node === 19) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree136(features, 39);
    } else if (val <= 972.5000000000001) {
      return calculateTree136(features, 39);
    } else {
      return calculateTree136(features, 40);
    }
  }
  if (node === 20) return 0.00025108733395900643;
  if (node === 9) {
    const val = features['is_preference_personal_care'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree136(features, 19);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree136(features, 19);
    } else {
      return calculateTree136(features, 20);
    }
  }
  if (node === 10) return -0.09524218484252366;
  if (node === 4) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree136(features, 9);
    } else if (val <= 980.5000000000001) {
      return calculateTree136(features, 9);
    } else {
      return calculateTree136(features, 10);
    }
  }
  if (node === 1) {
    const val = features['is_preference_women_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree136(features, 3);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree136(features, 3);
    } else {
      return calculateTree136(features, 4);
    }
  }
  if (node === 1535) return -0.04127112520306574;
  if (node === 1536) return 0.15281724315620304;
  if (node === 767) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree136(features, 1535);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree136(features, 1535);
    } else {
      return calculateTree136(features, 1536);
    }
  }
  if (node === 1537) return 0.03430790951783792;
  if (node === 1538) return 0.28887678200574246;
  if (node === 768) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree136(features, 1537);
    } else if (val <= 40.50000000000001) {
      return calculateTree136(features, 1537);
    } else {
      return calculateTree136(features, 1538);
    }
  }
  if (node === 383) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree136(features, 767);
    } else if (val <= 4.500000000000001) {
      return calculateTree136(features, 767);
    } else {
      return calculateTree136(features, 768);
    }
  }
  if (node === 384) return 0.34060619600571607;
  if (node === 191) {
    const val = features['is_preference_personal_care'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree136(features, 383);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree136(features, 383);
    } else {
      return calculateTree136(features, 384);
    }
  }
  if (node === 192) return -0.09221261173164569;
  if (node === 95) {
    const val = features['is_preference_men_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree136(features, 191);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree136(features, 191);
    } else {
      return calculateTree136(features, 192);
    }
  }
  if (node === 1551) return 0.024845222157908246;
  if (node === 1552) return -0.05537171819564827;
  if (node === 775) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree136(features, 1551);
    } else if (val <= 19.500000000000004) {
      return calculateTree136(features, 1551);
    } else {
      return calculateTree136(features, 1552);
    }
  }
  if (node === 776) return 0.28920610892112275;
  if (node === 387) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree136(features, 775);
    } else if (val <= 30.500000000000004) {
      return calculateTree136(features, 775);
    } else {
      return calculateTree136(features, 776);
    }
  }
  if (node === 388) return -0.020879752304013646;
  if (node === 193) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree136(features, 387);
    } else if (val <= 33.50000000000001) {
      return calculateTree136(features, 387);
    } else {
      return calculateTree136(features, 388);
    }
  }
  if (node === 389) return -0.3880256868220031;
  if (node === 781) return 0.22158287394040693;
  if (node === 782) return -0.09535495061083903;
  if (node === 390) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree136(features, 781);
    } else if (val <= 697.0000000000001) {
      return calculateTree136(features, 781);
    } else {
      return calculateTree136(features, 782);
    }
  }
  if (node === 194) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree136(features, 389);
    } else if (val <= 307.00000000000006) {
      return calculateTree136(features, 389);
    } else {
      return calculateTree136(features, 390);
    }
  }
  if (node === 96) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree136(features, 193);
    } else if (val <= 41.00000000000001) {
      return calculateTree136(features, 193);
    } else {
      return calculateTree136(features, 194);
    }
  }
  if (node === 47) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree136(features, 95);
    } else if (val <= 1.5000000000000002) {
      return calculateTree136(features, 95);
    } else {
      return calculateTree136(features, 96);
    }
  }
  if (node === 48) return 0.19443062752659399;
  if (node === 23) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree136(features, 47);
    } else if (val <= 10.500000000000002) {
      return calculateTree136(features, 47);
    } else {
      return calculateTree136(features, 48);
    }
  }
  if (node === 49) return 0.06177642810258298;
  if (node === 101) return -0.2781258590399701;
  if (node === 102) return -0.01653183171282754;
  if (node === 50) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree136(features, 101);
    } else if (val <= 847.0000000000001) {
      return calculateTree136(features, 101);
    } else {
      return calculateTree136(features, 102);
    }
  }
  if (node === 24) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree136(features, 49);
    } else if (val <= 41.00000000000001) {
      return calculateTree136(features, 49);
    } else {
      return calculateTree136(features, 50);
    }
  }
  if (node === 11) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree136(features, 23);
    } else if (val <= 12.500000000000002) {
      return calculateTree136(features, 23);
    } else {
      return calculateTree136(features, 24);
    }
  }
  if (node === 25) return 0.4290900553516988;
  if (node === 107) return 0.13541546416156375;
  if (node === 108) return -0.1748391251847251;
  if (node === 53) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree136(features, 107);
    } else if (val <= 660.0000000000001) {
      return calculateTree136(features, 107);
    } else {
      return calculateTree136(features, 108);
    }
  }
  if (node === 54) return 0.185322131043095;
  if (node === 26) {
    const val = features['is_preference_women_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree136(features, 53);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree136(features, 53);
    } else {
      return calculateTree136(features, 54);
    }
  }
  if (node === 12) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree136(features, 25);
    } else if (val <= 56.00000000000001) {
      return calculateTree136(features, 25);
    } else {
      return calculateTree136(features, 26);
    }
  }
  if (node === 5) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree136(features, 11);
    } else if (val <= 18.500000000000004) {
      return calculateTree136(features, 11);
    } else {
      return calculateTree136(features, 12);
    }
  }
  if (node === 13) return 0.07720636247167005;
  if (node === 14) return -0.30779265714247017;
  if (node === 6) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree136(features, 13);
    } else if (val <= 181.50000000000003) {
      return calculateTree136(features, 13);
    } else {
      return calculateTree136(features, 14);
    }
  }
  if (node === 2) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree136(features, 5);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree136(features, 5);
    } else {
      return calculateTree136(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree136(features, 1);
    } else if (val <= 993.5000000000001) {
      return calculateTree136(features, 1);
    } else {
      return calculateTree136(features, 2);
    }
  }
}
function calculateTree137(features, node = 0) {
  if (node === 7) return -0.0001199021398410836;
  if (node === 17) return -0.2390969367173292;
  if (node === 18) return -0.0034526200626846023;
  if (node === 8) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree137(features, 17);
    } else if (val <= 973.5000000000001) {
      return calculateTree137(features, 17);
    } else {
      return calculateTree137(features, 18);
    }
  }
  if (node === 3) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree137(features, 7);
    } else if (val <= 969.5000000000001) {
      return calculateTree137(features, 7);
    } else {
      return calculateTree137(features, 8);
    }
  }
  if (node === 159) return 0.24311397777851956;
  if (node === 160) return -0.4267132262544339;
  if (node === 79) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree137(features, 159);
    } else if (val <= 251.00000000000003) {
      return calculateTree137(features, 159);
    } else {
      return calculateTree137(features, 160);
    }
  }
  if (node === 80) return -0.36929127927271327;
  if (node === 39) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree137(features, 79);
    } else if (val <= 393.00000000000006) {
      return calculateTree137(features, 79);
    } else {
      return calculateTree137(features, 80);
    }
  }
  if (node === 81) return -0.3592464911475968;
  if (node === 82) return 0.16982868641488533;
  if (node === 40) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree137(features, 81);
    } else if (val <= 251.00000000000003) {
      return calculateTree137(features, 81);
    } else {
      return calculateTree137(features, 82);
    }
  }
  if (node === 19) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree137(features, 39);
    } else if (val <= 504.50000000000006) {
      return calculateTree137(features, 39);
    } else {
      return calculateTree137(features, 40);
    }
  }
  if (node === 41) return -0.35418238168719096;
  if (node === 42) return -0.2055016939696464;
  if (node === 20) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree137(features, 41);
    } else if (val <= 800.5000000000001) {
      return calculateTree137(features, 41);
    } else {
      return calculateTree137(features, 42);
    }
  }
  if (node === 9) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree137(features, 19);
    } else if (val <= 511.50000000000006) {
      return calculateTree137(features, 19);
    } else {
      return calculateTree137(features, 20);
    }
  }
  if (node === 87) return 0.5599002262225213;
  if (node === 177) return -0.31867362219462064;
  if (node === 178) return 0.10059900059285962;
  if (node === 88) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree137(features, 177);
    } else if (val <= 393.00000000000006) {
      return calculateTree137(features, 177);
    } else {
      return calculateTree137(features, 178);
    }
  }
  if (node === 43) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree137(features, 87);
    } else if (val <= 148.50000000000003) {
      return calculateTree137(features, 87);
    } else {
      return calculateTree137(features, 88);
    }
  }
  if (node === 44) return -0.3841655945357873;
  if (node === 21) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree137(features, 43);
    } else if (val <= 504.50000000000006) {
      return calculateTree137(features, 43);
    } else {
      return calculateTree137(features, 44);
    }
  }
  if (node === 45) return 0.23883724037655737;
  if (node === 187) return -0.0043119612033474;
  if (node === 188) return 0.10327498988469996;
  if (node === 93) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree137(features, 187);
    } else if (val <= 778.5000000000001) {
      return calculateTree137(features, 187);
    } else {
      return calculateTree137(features, 188);
    }
  }
  if (node === 189) return -0.42443328687618137;
  if (node === 763) return -0.08224936720187498;
  if (node === 1529) return 0.06100703169088673;
  if (node === 1530) return 0.23536095935114223;
  if (node === 764) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree137(features, 1529);
    } else if (val <= 41.00000000000001) {
      return calculateTree137(features, 1529);
    } else {
      return calculateTree137(features, 1530);
    }
  }
  if (node === 381) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree137(features, 763);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree137(features, 763);
    } else {
      return calculateTree137(features, 764);
    }
  }
  if (node === 1531) return -0.02378957362204652;
  if (node === 1532) return -0.26525883287636587;
  if (node === 765) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree137(features, 1531);
    } else if (val <= 880.0000000000001) {
      return calculateTree137(features, 1531);
    } else {
      return calculateTree137(features, 1532);
    }
  }
  if (node === 1533) return 0.24808175692398615;
  if (node === 1534) return -0.05062077093099695;
  if (node === 766) {
    const val = features['is_preference_kids'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree137(features, 1533);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree137(features, 1533);
    } else {
      return calculateTree137(features, 1534);
    }
  }
  if (node === 382) {
    const val = features['is_preference_personal_care'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree137(features, 765);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree137(features, 765);
    } else {
      return calculateTree137(features, 766);
    }
  }
  if (node === 190) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree137(features, 381);
    } else if (val <= 251.00000000000003) {
      return calculateTree137(features, 381);
    } else {
      return calculateTree137(features, 382);
    }
  }
  if (node === 94) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree137(features, 189);
    } else if (val <= 816.5000000000001) {
      return calculateTree137(features, 189);
    } else {
      return calculateTree137(features, 190);
    }
  }
  if (node === 46) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree137(features, 93);
    } else if (val <= 800.5000000000001) {
      return calculateTree137(features, 93);
    } else {
      return calculateTree137(features, 94);
    }
  }
  if (node === 22) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree137(features, 45);
    } else if (val <= 648.5000000000001) {
      return calculateTree137(features, 45);
    } else {
      return calculateTree137(features, 46);
    }
  }
  if (node === 10) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree137(features, 21);
    } else if (val <= 511.50000000000006) {
      return calculateTree137(features, 21);
    } else {
      return calculateTree137(features, 22);
    }
  }
  if (node === 4) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree137(features, 9);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree137(features, 9);
    } else {
      return calculateTree137(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree137(features, 3);
    } else if (val <= 241.50000000000003) {
      return calculateTree137(features, 3);
    } else {
      return calculateTree137(features, 4);
    }
  }
  if (node === 23) return -0.29992650954695393;
  if (node === 24) return 0.06774276778080136;
  if (node === 11) {
    const val = features['is_preference_grocery'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree137(features, 23);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree137(features, 23);
    } else {
      return calculateTree137(features, 24);
    }
  }
  if (node === 51) return 0.02711637751428115;
  if (node === 52) return 0.2196870520153194;
  if (node === 25) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree137(features, 51);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree137(features, 51);
    } else {
      return calculateTree137(features, 52);
    }
  }
  if (node === 53) return 0.09111169012613632;
  if (node === 54) return -0.34317001487619886;
  if (node === 26) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree137(features, 53);
    } else if (val <= 969.5000000000001) {
      return calculateTree137(features, 53);
    } else {
      return calculateTree137(features, 54);
    }
  }
  if (node === 12) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree137(features, 25);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree137(features, 25);
    } else {
      return calculateTree137(features, 26);
    }
  }
  if (node === 5) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree137(features, 11);
    } else if (val <= 839.5000000000001) {
      return calculateTree137(features, 11);
    } else {
      return calculateTree137(features, 12);
    }
  }
  if (node === 6) return -0.0004676770559793171;
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree137(features, 5);
    } else if (val <= 258.50000000000006) {
      return calculateTree137(features, 5);
    } else {
      return calculateTree137(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree137(features, 1);
    } else if (val <= 253.50000000000003) {
      return calculateTree137(features, 1);
    } else {
      return calculateTree137(features, 2);
    }
  }
}
function calculateTree138(features, node = 0) {
  if (node === 1) return 0.00033535365776723753;
  if (node === 1535) return 0.0097568549940761;
  if (node === 1536) return -0.19502166469545867;
  if (node === 767) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree138(features, 1535);
    } else if (val <= 926.0000000000001) {
      return calculateTree138(features, 1535);
    } else {
      return calculateTree138(features, 1536);
    }
  }
  if (node === 1537) return 0.18171189721151412;
  if (node === 1538) return -0.0003083714271966298;
  if (node === 768) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree138(features, 1537);
    } else if (val <= 2.5000000000000004) {
      return calculateTree138(features, 1537);
    } else {
      return calculateTree138(features, 1538);
    }
  }
  if (node === 383) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree138(features, 767);
    } else if (val <= 253.00000000000003) {
      return calculateTree138(features, 767);
    } else {
      return calculateTree138(features, 768);
    }
  }
  if (node === 769) return -0.21424307481600904;
  if (node === 1541) return -0.19204750988908054;
  if (node === 1542) return 0.05628510030687877;
  if (node === 770) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree138(features, 1541);
    } else if (val <= 203.50000000000003) {
      return calculateTree138(features, 1541);
    } else {
      return calculateTree138(features, 1542);
    }
  }
  if (node === 384) {
    const val = features['is_preference_pets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree138(features, 769);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree138(features, 769);
    } else {
      return calculateTree138(features, 770);
    }
  }
  if (node === 191) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree138(features, 383);
    } else if (val <= 286.50000000000006) {
      return calculateTree138(features, 383);
    } else {
      return calculateTree138(features, 384);
    }
  }
  if (node === 1543) return 0.19170527439839394;
  if (node === 1544) return 0.5542731251836669;
  if (node === 771) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree138(features, 1543);
    } else if (val <= 7.500000000000001) {
      return calculateTree138(features, 1543);
    } else {
      return calculateTree138(features, 1544);
    }
  }
  if (node === 772) return -0.0031086268538777014;
  if (node === 385) {
    const val = features['is_preference_women_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree138(features, 771);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree138(features, 771);
    } else {
      return calculateTree138(features, 772);
    }
  }
  if (node === 1547) return -0.17059463302297473;
  if (node === 1548) return 0.030176760521314704;
  if (node === 773) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree138(features, 1547);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree138(features, 1547);
    } else {
      return calculateTree138(features, 1548);
    }
  }
  if (node === 774) return 0.09705004352020932;
  if (node === 386) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree138(features, 773);
    } else if (val <= 21.500000000000004) {
      return calculateTree138(features, 773);
    } else {
      return calculateTree138(features, 774);
    }
  }
  if (node === 192) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree138(features, 385);
    } else if (val <= 19.500000000000004) {
      return calculateTree138(features, 385);
    } else {
      return calculateTree138(features, 386);
    }
  }
  if (node === 95) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree138(features, 191);
    } else if (val <= 18.500000000000004) {
      return calculateTree138(features, 191);
    } else {
      return calculateTree138(features, 192);
    }
  }
  if (node === 96) return -0.262910930638105;
  if (node === 47) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree138(features, 95);
    } else if (val <= 960.0000000000001) {
      return calculateTree138(features, 95);
    } else {
      return calculateTree138(features, 96);
    }
  }
  if (node === 97) return -0.030018598013649228;
  if (node === 197) return -0.029000729482006507;
  if (node === 1591) return 0.005008637988435964;
  if (node === 1592) return 0.06276289044552916;
  if (node === 795) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree138(features, 1591);
    } else if (val <= 158.50000000000003) {
      return calculateTree138(features, 1591);
    } else {
      return calculateTree138(features, 1592);
    }
  }
  if (node === 796) return 0.21065396924810012;
  if (node === 397) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree138(features, 795);
    } else if (val <= 993.5000000000001) {
      return calculateTree138(features, 795);
    } else {
      return calculateTree138(features, 796);
    }
  }
  if (node === 797) return -0.22442970023324563;
  if (node === 798) return 0.11017791097346881;
  if (node === 398) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree138(features, 797);
    } else if (val <= 240.00000000000003) {
      return calculateTree138(features, 797);
    } else {
      return calculateTree138(features, 798);
    }
  }
  if (node === 198) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree138(features, 397);
    } else if (val <= 59.50000000000001) {
      return calculateTree138(features, 397);
    } else {
      return calculateTree138(features, 398);
    }
  }
  if (node === 98) {
    const val = features['is_preference_pets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree138(features, 197);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree138(features, 197);
    } else {
      return calculateTree138(features, 198);
    }
  }
  if (node === 48) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree138(features, 97);
    } else if (val <= 36.50000000000001) {
      return calculateTree138(features, 97);
    } else {
      return calculateTree138(features, 98);
    }
  }
  if (node === 23) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree138(features, 47);
    } else if (val <= 23.500000000000004) {
      return calculateTree138(features, 47);
    } else {
      return calculateTree138(features, 48);
    }
  }
  if (node === 49) return 0.513172353727603;
  if (node === 50) return 0.05853289599006213;
  if (node === 24) {
    const val = features['is_preference_kitchen'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree138(features, 49);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree138(features, 49);
    } else {
      return calculateTree138(features, 50);
    }
  }
  if (node === 11) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree138(features, 23);
    } else if (val <= 327.50000000000006) {
      return calculateTree138(features, 23);
    } else {
      return calculateTree138(features, 24);
    }
  }
  if (node === 25) return -0.021539725503494787;
  if (node === 53) return 0.22756281016295685;
  if (node === 109) return 0.03876861732224071;
  if (node === 110) return -0.0377669386938685;
  if (node === 54) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree138(features, 109);
    } else if (val <= 414.50000000000006) {
      return calculateTree138(features, 109);
    } else {
      return calculateTree138(features, 110);
    }
  }
  if (node === 26) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree138(features, 53);
    } else if (val <= 23.500000000000004) {
      return calculateTree138(features, 53);
    } else {
      return calculateTree138(features, 54);
    }
  }
  if (node === 12) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree138(features, 25);
    } else if (val <= 26.500000000000004) {
      return calculateTree138(features, 25);
    } else {
      return calculateTree138(features, 26);
    }
  }
  if (node === 5) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree138(features, 11);
    } else if (val <= 330.00000000000006) {
      return calculateTree138(features, 11);
    } else {
      return calculateTree138(features, 12);
    }
  }
  if (node === 55) return -0.1838853819780249;
  if (node === 56) return 0.18136815494952271;
  if (node === 27) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree138(features, 55);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree138(features, 55);
    } else {
      return calculateTree138(features, 56);
    }
  }
  if (node === 28) return 0.3958870295638057;
  if (node === 13) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree138(features, 27);
    } else if (val <= 880.0000000000001) {
      return calculateTree138(features, 27);
    } else {
      return calculateTree138(features, 28);
    }
  }
  if (node === 14) return 0.013938227480091277;
  if (node === 6) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree138(features, 13);
    } else if (val <= 937.5000000000001) {
      return calculateTree138(features, 13);
    } else {
      return calculateTree138(features, 14);
    }
  }
  if (node === 2) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree138(features, 5);
    } else if (val <= 906.0000000000001) {
      return calculateTree138(features, 5);
    } else {
      return calculateTree138(features, 6);
    }
  }
  if (node === 0) {
    const val = features['is_preference_men_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree138(features, 1);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree138(features, 1);
    } else {
      return calculateTree138(features, 2);
    }
  }
}
function calculateTree139(features, node = 0) {
  if (node === 7) return -0.0008596403475145864;
  if (node === 71) return -0.3143247049535937;
  if (node === 145) return -0.03713629606513493;
  if (node === 146) return 0.03926761464584736;
  if (node === 72) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree139(features, 145);
    } else if (val <= 28.500000000000004) {
      return calculateTree139(features, 145);
    } else {
      return calculateTree139(features, 146);
    }
  }
  if (node === 35) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree139(features, 71);
    } else if (val <= 203.50000000000003) {
      return calculateTree139(features, 71);
    } else {
      return calculateTree139(features, 72);
    }
  }
  if (node === 73) return 0.12797445966406729;
  if (node === 74) return -0.3773593524665934;
  if (node === 36) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree139(features, 73);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree139(features, 73);
    } else {
      return calculateTree139(features, 74);
    }
  }
  if (node === 17) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree139(features, 35);
    } else if (val <= 275.50000000000006) {
      return calculateTree139(features, 35);
    } else {
      return calculateTree139(features, 36);
    }
  }
  if (node === 37) return -0.335998460330625;
  if (node === 77) return -0.33050265380426175;
  if (node === 78) return 0.10781735485075365;
  if (node === 38) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree139(features, 77);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree139(features, 77);
    } else {
      return calculateTree139(features, 78);
    }
  }
  if (node === 18) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree139(features, 37);
    } else if (val <= 789.5000000000001) {
      return calculateTree139(features, 37);
    } else {
      return calculateTree139(features, 38);
    }
  }
  if (node === 8) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree139(features, 17);
    } else if (val <= 308.00000000000006) {
      return calculateTree139(features, 17);
    } else {
      return calculateTree139(features, 18);
    }
  }
  if (node === 3) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree139(features, 7);
    } else if (val <= 201.50000000000003) {
      return calculateTree139(features, 7);
    } else {
      return calculateTree139(features, 8);
    }
  }
  if (node === 19) return -0.09715850821789933;
  if (node === 41) return 0.09911038023105496;
  if (node === 42) return -0.16075595219910543;
  if (node === 20) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree139(features, 41);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree139(features, 41);
    } else {
      return calculateTree139(features, 42);
    }
  }
  if (node === 9) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree139(features, 19);
    } else if (val <= 201.50000000000003) {
      return calculateTree139(features, 19);
    } else {
      return calculateTree139(features, 20);
    }
  }
  if (node === 10) return -0.007548509884507406;
  if (node === 4) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree139(features, 9);
    } else if (val <= 338.50000000000006) {
      return calculateTree139(features, 9);
    } else {
      return calculateTree139(features, 10);
    }
  }
  if (node === 1) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree139(features, 3);
    } else if (val <= 50.50000000000001) {
      return calculateTree139(features, 3);
    } else {
      return calculateTree139(features, 4);
    }
  }
  if (node === 47) return -0.36617299182230545;
  if (node === 48) return 0.05524744217124808;
  if (node === 23) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree139(features, 47);
    } else if (val <= 215.00000000000003) {
      return calculateTree139(features, 47);
    } else {
      return calculateTree139(features, 48);
    }
  }
  if (node === 399) return -0.32228937703011507;
  if (node === 801) return 0.09843218656371189;
  if (node === 802) return -0.20031133578354346;
  if (node === 400) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree139(features, 801);
    } else if (val <= 387.50000000000006) {
      return calculateTree139(features, 801);
    } else {
      return calculateTree139(features, 802);
    }
  }
  if (node === 199) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree139(features, 399);
    } else if (val <= 210.00000000000003) {
      return calculateTree139(features, 399);
    } else {
      return calculateTree139(features, 400);
    }
  }
  if (node === 401) return 0.019275500286132886;
  if (node === 402) return -0.286250791012585;
  if (node === 200) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree139(features, 401);
    } else if (val <= 210.00000000000003) {
      return calculateTree139(features, 401);
    } else {
      return calculateTree139(features, 402);
    }
  }
  if (node === 99) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree139(features, 199);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree139(features, 199);
    } else {
      return calculateTree139(features, 200);
    }
  }
  if (node === 201) return -0.30389216468319047;
  if (node === 202) return 0.0639842003089737;
  if (node === 100) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree139(features, 201);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree139(features, 201);
    } else {
      return calculateTree139(features, 202);
    }
  }
  if (node === 49) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree139(features, 99);
    } else if (val <= 599.5000000000001) {
      return calculateTree139(features, 99);
    } else {
      return calculateTree139(features, 100);
    }
  }
  if (node === 50) return 0.16635246348472313;
  if (node === 24) {
    const val = features['is_preference_kitchen'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree139(features, 49);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree139(features, 49);
    } else {
      return calculateTree139(features, 50);
    }
  }
  if (node === 11) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree139(features, 23);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree139(features, 23);
    } else {
      return calculateTree139(features, 24);
    }
  }
  if (node === 51) return 0.10006887849589179;
  if (node === 52) return 0.4012908626690037;
  if (node === 25) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree139(features, 51);
    } else if (val <= 697.0000000000001) {
      return calculateTree139(features, 51);
    } else {
      return calculateTree139(features, 52);
    }
  }
  if (node === 215) return 0.05940255863586967;
  if (node === 216) return 0.3330934015949113;
  if (node === 107) {
    const val = features['is_preference_pets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree139(features, 215);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree139(features, 215);
    } else {
      return calculateTree139(features, 216);
    }
  }
  if (node === 217) return 0.017651461080525045;
  if (node === 218) return -0.3074983733689018;
  if (node === 108) {
    const val = features['is_preference_kids'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree139(features, 217);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree139(features, 217);
    } else {
      return calculateTree139(features, 218);
    }
  }
  if (node === 53) {
    const val = features['is_preference_sports'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree139(features, 107);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree139(features, 107);
    } else {
      return calculateTree139(features, 108);
    }
  }
  if (node === 109) return 0.09050737242831271;
  if (node === 110) return -0.2487840119205977;
  if (node === 54) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree139(features, 109);
    } else if (val <= 210.00000000000003) {
      return calculateTree139(features, 109);
    } else {
      return calculateTree139(features, 110);
    }
  }
  if (node === 26) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree139(features, 53);
    } else if (val <= 44.50000000000001) {
      return calculateTree139(features, 53);
    } else {
      return calculateTree139(features, 54);
    }
  }
  if (node === 12) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree139(features, 25);
    } else if (val <= 725.5000000000001) {
      return calculateTree139(features, 25);
    } else {
      return calculateTree139(features, 26);
    }
  }
  if (node === 5) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree139(features, 11);
    } else if (val <= 155.50000000000003) {
      return calculateTree139(features, 11);
    } else {
      return calculateTree139(features, 12);
    }
  }
  if (node === 6) return 0.000585902213848634;
  if (node === 2) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree139(features, 5);
    } else if (val <= 225.50000000000003) {
      return calculateTree139(features, 5);
    } else {
      return calculateTree139(features, 6);
    }
  }
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree139(features, 1);
    } else if (val <= 206.00000000000003) {
      return calculateTree139(features, 1);
    } else {
      return calculateTree139(features, 2);
    }
  }
}
function calculateTree140(features, node = 0) {
  if (node === 1) return -0.000461444624195726;
  if (node === 23) return 0.046184272410632644;
  if (node === 24) return 0.0034807214884764307;
  if (node === 11) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree140(features, 23);
    } else if (val <= 42.50000000000001) {
      return calculateTree140(features, 23);
    } else {
      return calculateTree140(features, 24);
    }
  }
  if (node === 25) return 0.10068794459964953;
  if (node === 53) return -0.10204174393510808;
  if (node === 54) return 0.192529088318756;
  if (node === 26) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree140(features, 53);
    } else if (val <= 804.0000000000001) {
      return calculateTree140(features, 53);
    } else {
      return calculateTree140(features, 54);
    }
  }
  if (node === 12) {
    const val = features['is_preference_men_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree140(features, 25);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree140(features, 25);
    } else {
      return calculateTree140(features, 26);
    }
  }
  if (node === 5) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree140(features, 11);
    } else if (val <= 712.5000000000001) {
      return calculateTree140(features, 11);
    } else {
      return calculateTree140(features, 12);
    }
  }
  if (node === 27) return -0.08901904355366609;
  if (node === 28) return -0.3456166396445332;
  if (node === 13) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree140(features, 27);
    } else if (val <= 1.5000000000000002) {
      return calculateTree140(features, 27);
    } else {
      return calculateTree140(features, 28);
    }
  }
  if (node === 479) return -0.30390778753622955;
  if (node === 961) return -0.16877204875026852;
  if (node === 962) return 0.03910724162211831;
  if (node === 480) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree140(features, 961);
    } else if (val <= 43.50000000000001) {
      return calculateTree140(features, 961);
    } else {
      return calculateTree140(features, 962);
    }
  }
  if (node === 239) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree140(features, 479);
    } else if (val <= 148.00000000000003) {
      return calculateTree140(features, 479);
    } else {
      return calculateTree140(features, 480);
    }
  }
  if (node === 481) return -0.23729565940772135;
  if (node === 482) return -0.09650163913359464;
  if (node === 240) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree140(features, 481);
    } else if (val <= 350.50000000000006) {
      return calculateTree140(features, 481);
    } else {
      return calculateTree140(features, 482);
    }
  }
  if (node === 119) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree140(features, 239);
    } else if (val <= 253.50000000000003) {
      return calculateTree140(features, 239);
    } else {
      return calculateTree140(features, 240);
    }
  }
  if (node === 483) return 0.11947766789752169;
  if (node === 969) return -0.3281414628359808;
  if (node === 970) return 0.05669669762077751;
  if (node === 484) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree140(features, 969);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree140(features, 969);
    } else {
      return calculateTree140(features, 970);
    }
  }
  if (node === 241) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree140(features, 483);
    } else if (val <= 819.5000000000001) {
      return calculateTree140(features, 483);
    } else {
      return calculateTree140(features, 484);
    }
  }
  if (node === 242) return -0.25886823918345886;
  if (node === 120) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree140(features, 241);
    } else if (val <= 926.0000000000001) {
      return calculateTree140(features, 241);
    } else {
      return calculateTree140(features, 242);
    }
  }
  if (node === 59) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree140(features, 119);
    } else if (val <= 804.5000000000001) {
      return calculateTree140(features, 119);
    } else {
      return calculateTree140(features, 120);
    }
  }
  if (node === 121) return 0.14821861098850198;
  if (node === 983) return -0.13030502903934565;
  if (node === 984) return 0.12895072738010258;
  if (node === 491) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree140(features, 983);
    } else if (val <= 789.5000000000001) {
      return calculateTree140(features, 983);
    } else {
      return calculateTree140(features, 984);
    }
  }
  if (node === 492) return -0.22812447815576387;
  if (node === 245) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree140(features, 491);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree140(features, 491);
    } else {
      return calculateTree140(features, 492);
    }
  }
  if (node === 493) return 0.18897282375585467;
  if (node === 989) return 0.15484063962542366;
  if (node === 990) return -0.11092337783008548;
  if (node === 494) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree140(features, 989);
    } else if (val <= 757.5000000000001) {
      return calculateTree140(features, 989);
    } else {
      return calculateTree140(features, 990);
    }
  }
  if (node === 246) {
    const val = features['is_preference_pets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree140(features, 493);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree140(features, 493);
    } else {
      return calculateTree140(features, 494);
    }
  }
  if (node === 122) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree140(features, 245);
    } else if (val <= 723.5000000000001) {
      return calculateTree140(features, 245);
    } else {
      return calculateTree140(features, 246);
    }
  }
  if (node === 60) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree140(features, 121);
    } else if (val <= 253.50000000000003) {
      return calculateTree140(features, 121);
    } else {
      return calculateTree140(features, 122);
    }
  }
  if (node === 29) {
    const val = features['is_preference_beauty'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree140(features, 59);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree140(features, 59);
    } else {
      return calculateTree140(features, 60);
    }
  }
  if (node === 61) return 0.10813843009454016;
  if (node === 2015) return 0.09809723583702118;
  if (node === 2016) return -0.3326015628192806;
  if (node === 1007) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree140(features, 2015);
    } else if (val <= 861.5000000000001) {
      return calculateTree140(features, 2015);
    } else {
      return calculateTree140(features, 2016);
    }
  }
  if (node === 1008) return -0.2795056885847793;
  if (node === 503) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree140(features, 1007);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree140(features, 1007);
    } else {
      return calculateTree140(features, 1008);
    }
  }
  if (node === 504) return 0.032482551738495784;
  if (node === 251) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree140(features, 503);
    } else if (val <= 865.0000000000001) {
      return calculateTree140(features, 503);
    } else {
      return calculateTree140(features, 504);
    }
  }
  if (node === 252) return -0.09879632229267335;
  if (node === 125) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree140(features, 251);
    } else if (val <= 212.50000000000003) {
      return calculateTree140(features, 251);
    } else {
      return calculateTree140(features, 252);
    }
  }
  if (node === 253) return 0.030963781036083937;
  if (node === 254) return -0.015687405954100037;
  if (node === 126) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree140(features, 253);
    } else if (val <= 475.50000000000006) {
      return calculateTree140(features, 253);
    } else {
      return calculateTree140(features, 254);
    }
  }
  if (node === 62) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree140(features, 125);
    } else if (val <= 278.50000000000006) {
      return calculateTree140(features, 125);
    } else {
      return calculateTree140(features, 126);
    }
  }
  if (node === 30) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree140(features, 61);
    } else if (val <= 811.5000000000001) {
      return calculateTree140(features, 61);
    } else {
      return calculateTree140(features, 62);
    }
  }
  if (node === 14) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree140(features, 29);
    } else if (val <= 800.5000000000001) {
      return calculateTree140(features, 29);
    } else {
      return calculateTree140(features, 30);
    }
  }
  if (node === 6) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree140(features, 13);
    } else if (val <= 758.0000000000001) {
      return calculateTree140(features, 13);
    } else {
      return calculateTree140(features, 14);
    }
  }
  if (node === 2) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree140(features, 5);
    } else if (val <= 734.0000000000001) {
      return calculateTree140(features, 5);
    } else {
      return calculateTree140(features, 6);
    }
  }
  if (node === 0) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree140(features, 1);
    } else if (val <= 41.50000000000001) {
      return calculateTree140(features, 1);
    } else {
      return calculateTree140(features, 2);
    }
  }
}
function calculateTree141(features, node = 0) {
  if (node === 1) return 0.00015413534879785173;
  if (node === 11) return 0.33667842267684417;
  if (node === 25) return 0.09208426221888005;
  if (node === 107) return -0.06431777088855055;
  if (node === 108) return 0.2530764493603978;
  if (node === 53) {
    const val = features['is_preference_household'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree141(features, 107);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree141(features, 107);
    } else {
      return calculateTree141(features, 108);
    }
  }
  if (node === 109) return -0.34720477705487934;
  if (node === 110) return -0.07787176668226699;
  if (node === 54) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree141(features, 109);
    } else if (val <= 41.50000000000001) {
      return calculateTree141(features, 109);
    } else {
      return calculateTree141(features, 110);
    }
  }
  if (node === 26) {
    const val = features['is_preference_women_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree141(features, 53);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree141(features, 53);
    } else {
      return calculateTree141(features, 54);
    }
  }
  if (node === 12) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree141(features, 25);
    } else if (val <= 29.500000000000004) {
      return calculateTree141(features, 25);
    } else {
      return calculateTree141(features, 26);
    }
  }
  if (node === 5) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree141(features, 11);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree141(features, 11);
    } else {
      return calculateTree141(features, 12);
    }
  }
  if (node === 55) return -0.3999095886539139;
  if (node === 56) return -0.02789687471567206;
  if (node === 27) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree141(features, 55);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree141(features, 55);
    } else {
      return calculateTree141(features, 56);
    }
  }
  if (node === 28) return 0.005865599008080072;
  if (node === 13) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree141(features, 27);
    } else if (val <= 34.00000000000001) {
      return calculateTree141(features, 27);
    } else {
      return calculateTree141(features, 28);
    }
  }
  if (node === 119) return -0.0030152541322203707;
  if (node === 241) return -0.026594352611068007;
  if (node === 971) return -0.05111081770589427;
  if (node === 972) return 0.22681851254951385;
  if (node === 485) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree141(features, 971);
    } else if (val <= 236.00000000000003) {
      return calculateTree141(features, 971);
    } else {
      return calculateTree141(features, 972);
    }
  }
  if (node === 486) return -0.2099348744681829;
  if (node === 242) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree141(features, 485);
    } else if (val <= 284.50000000000006) {
      return calculateTree141(features, 485);
    } else {
      return calculateTree141(features, 486);
    }
  }
  if (node === 120) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree141(features, 241);
    } else if (val <= 11.500000000000002) {
      return calculateTree141(features, 241);
    } else {
      return calculateTree141(features, 242);
    }
  }
  if (node === 59) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree141(features, 119);
    } else if (val <= 1.5000000000000002) {
      return calculateTree141(features, 119);
    } else {
      return calculateTree141(features, 120);
    }
  }
  if (node === 121) return -0.18152858569436678;
  if (node === 122) return -0.03923475117001352;
  if (node === 60) {
    const val = features['is_preference_pets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree141(features, 121);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree141(features, 121);
    } else {
      return calculateTree141(features, 122);
    }
  }
  if (node === 29) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree141(features, 59);
    } else if (val <= 504.50000000000006) {
      return calculateTree141(features, 59);
    } else {
      return calculateTree141(features, 60);
    }
  }
  if (node === 495) return -0.010238614189012942;
  if (node === 1987) return 0.026437008379936098;
  if (node === 1988) return 0.42637627791239974;
  if (node === 993) {
    const val = features['is_preference_pets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree141(features, 1987);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree141(features, 1987);
    } else {
      return calculateTree141(features, 1988);
    }
  }
  if (node === 1989) return 0.2711994583767668;
  if (node === 1990) return 0.02017069377789873;
  if (node === 994) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree141(features, 1989);
    } else if (val <= 643.5000000000001) {
      return calculateTree141(features, 1989);
    } else {
      return calculateTree141(features, 1990);
    }
  }
  if (node === 496) {
    const val = features['is_preference_household'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree141(features, 993);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree141(features, 993);
    } else {
      return calculateTree141(features, 994);
    }
  }
  if (node === 247) {
    const val = features['is_preference_kitchen'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree141(features, 495);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree141(features, 495);
    } else {
      return calculateTree141(features, 496);
    }
  }
  if (node === 497) return -0.14213740589128776;
  if (node === 498) return 0.005793824295565941;
  if (node === 248) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree141(features, 497);
    } else if (val <= 591.0000000000001) {
      return calculateTree141(features, 497);
    } else {
      return calculateTree141(features, 498);
    }
  }
  if (node === 123) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree141(features, 247);
    } else if (val <= 378.50000000000006) {
      return calculateTree141(features, 247);
    } else {
      return calculateTree141(features, 248);
    }
  }
  if (node === 124) return 0.14797276596617975;
  if (node === 61) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree141(features, 123);
    } else if (val <= 50.50000000000001) {
      return calculateTree141(features, 123);
    } else {
      return calculateTree141(features, 124);
    }
  }
  if (node === 1007) return -0.08677883224566076;
  if (node === 1008) return -0.3795131231900989;
  if (node === 503) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree141(features, 1007);
    } else if (val <= 7.500000000000001) {
      return calculateTree141(features, 1007);
    } else {
      return calculateTree141(features, 1008);
    }
  }
  if (node === 1009) return 0.06212130526437433;
  if (node === 1010) return -0.08921609889454107;
  if (node === 504) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree141(features, 1009);
    } else if (val <= 307.00000000000006) {
      return calculateTree141(features, 1009);
    } else {
      return calculateTree141(features, 1010);
    }
  }
  if (node === 251) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree141(features, 503);
    } else if (val <= 8.500000000000002) {
      return calculateTree141(features, 503);
    } else {
      return calculateTree141(features, 504);
    }
  }
  if (node === 252) return 0.03741834733116151;
  if (node === 125) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree141(features, 251);
    } else if (val <= 697.0000000000001) {
      return calculateTree141(features, 251);
    } else {
      return calculateTree141(features, 252);
    }
  }
  if (node === 253) return 0.04924560759395133;
  if (node === 254) return -0.012198899328603437;
  if (node === 126) {
    const val = features['is_preference_household'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree141(features, 253);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree141(features, 253);
    } else {
      return calculateTree141(features, 254);
    }
  }
  if (node === 62) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree141(features, 125);
    } else if (val <= 839.5000000000001) {
      return calculateTree141(features, 125);
    } else {
      return calculateTree141(features, 126);
    }
  }
  if (node === 30) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree141(features, 61);
    } else if (val <= 715.5000000000001) {
      return calculateTree141(features, 61);
    } else {
      return calculateTree141(features, 62);
    }
  }
  if (node === 14) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree141(features, 29);
    } else if (val <= 607.5000000000001) {
      return calculateTree141(features, 29);
    } else {
      return calculateTree141(features, 30);
    }
  }
  if (node === 6) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree141(features, 13);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree141(features, 13);
    } else {
      return calculateTree141(features, 14);
    }
  }
  if (node === 2) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree141(features, 5);
    } else if (val <= 6.000000000000001) {
      return calculateTree141(features, 5);
    } else {
      return calculateTree141(features, 6);
    }
  }
  if (node === 0) {
    const val = features['is_preference_sports'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree141(features, 1);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree141(features, 1);
    } else {
      return calculateTree141(features, 2);
    }
  }
}
function calculateTree142(features, node = 0) {
  if (node === 7) return 0.0001846893309672942;
  if (node === 8) return -0.11877540543041942;
  if (node === 3) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree142(features, 7);
    } else if (val <= 969.5000000000001) {
      return calculateTree142(features, 7);
    } else {
      return calculateTree142(features, 8);
    }
  }
  if (node === 9) return -0.0016405351960648252;
  if (node === 43) return -0.2655385040558215;
  if (node === 44) return 0.05310969283682298;
  if (node === 21) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree142(features, 43);
    } else if (val <= 969.5000000000001) {
      return calculateTree142(features, 43);
    } else {
      return calculateTree142(features, 44);
    }
  }
  if (node === 22) return 0.04249680103262179;
  if (node === 10) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree142(features, 21);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree142(features, 21);
    } else {
      return calculateTree142(features, 22);
    }
  }
  if (node === 4) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree142(features, 9);
    } else if (val <= 778.5000000000001) {
      return calculateTree142(features, 9);
    } else {
      return calculateTree142(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree142(features, 3);
    } else if (val <= 241.50000000000003) {
      return calculateTree142(features, 3);
    } else {
      return calculateTree142(features, 4);
    }
  }
  if (node === 95) return 0.11244875654485782;
  if (node === 193) return -0.36401813564781643;
  if (node === 194) return 0.04196695687581257;
  if (node === 96) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree142(features, 193);
    } else if (val <= 144.50000000000003) {
      return calculateTree142(features, 193);
    } else {
      return calculateTree142(features, 194);
    }
  }
  if (node === 47) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree142(features, 95);
    } else if (val <= 89.00000000000001) {
      return calculateTree142(features, 95);
    } else {
      return calculateTree142(features, 96);
    }
  }
  if (node === 48) return -0.33468728602597003;
  if (node === 23) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree142(features, 47);
    } else if (val <= 171.00000000000003) {
      return calculateTree142(features, 47);
    } else {
      return calculateTree142(features, 48);
    }
  }
  if (node === 99) return 0.10788929352905616;
  if (node === 403) return -0.12295920243735285;
  if (node === 404) return 0.17701308927798862;
  if (node === 201) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree142(features, 403);
    } else if (val <= 877.5000000000001) {
      return calculateTree142(features, 403);
    } else {
      return calculateTree142(features, 404);
    }
  }
  if (node === 202) return -0.2868167565515412;
  if (node === 100) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree142(features, 201);
    } else if (val <= 36.50000000000001) {
      return calculateTree142(features, 201);
    } else {
      return calculateTree142(features, 202);
    }
  }
  if (node === 49) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree142(features, 99);
    } else if (val <= 781.0000000000001) {
      return calculateTree142(features, 99);
    } else {
      return calculateTree142(features, 100);
    }
  }
  if (node === 407) return 0.06827947981329568;
  if (node === 408) return 0.393805482101071;
  if (node === 203) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree142(features, 407);
    } else if (val <= 37.50000000000001) {
      return calculateTree142(features, 407);
    } else {
      return calculateTree142(features, 408);
    }
  }
  if (node === 204) return -0.34366804710682675;
  if (node === 101) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree142(features, 203);
    } else if (val <= 781.0000000000001) {
      return calculateTree142(features, 203);
    } else {
      return calculateTree142(features, 204);
    }
  }
  if (node === 102) return 0.6231988987647897;
  if (node === 50) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree142(features, 101);
    } else if (val <= 877.5000000000001) {
      return calculateTree142(features, 101);
    } else {
      return calculateTree142(features, 102);
    }
  }
  if (node === 24) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree142(features, 49);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree142(features, 49);
    } else {
      return calculateTree142(features, 50);
    }
  }
  if (node === 11) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree142(features, 23);
    } else if (val <= 712.5000000000001) {
      return calculateTree142(features, 23);
    } else {
      return calculateTree142(features, 24);
    }
  }
  if (node === 51) return -0.2528527235920195;
  if (node === 423) return 0.04628619000052996;
  if (node === 424) return -0.26213708321089746;
  if (node === 211) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree142(features, 423);
    } else if (val <= 682.5000000000001) {
      return calculateTree142(features, 423);
    } else {
      return calculateTree142(features, 424);
    }
  }
  if (node === 212) return -0.25065451768248115;
  if (node === 105) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree142(features, 211);
    } else if (val <= 13.500000000000002) {
      return calculateTree142(features, 211);
    } else {
      return calculateTree142(features, 212);
    }
  }
  if (node === 213) return 0.2022496210425745;
  if (node === 429) return 0.18404874946249175;
  if (node === 430) return -0.21970437934264464;
  if (node === 214) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree142(features, 429);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree142(features, 429);
    } else {
      return calculateTree142(features, 430);
    }
  }
  if (node === 106) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree142(features, 213);
    } else if (val <= 677.5000000000001) {
      return calculateTree142(features, 213);
    } else {
      return calculateTree142(features, 214);
    }
  }
  if (node === 52) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree142(features, 105);
    } else if (val <= 20.500000000000004) {
      return calculateTree142(features, 105);
    } else {
      return calculateTree142(features, 106);
    }
  }
  if (node === 25) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree142(features, 51);
    } else if (val <= 447.00000000000006) {
      return calculateTree142(features, 51);
    } else {
      return calculateTree142(features, 52);
    }
  }
  if (node === 26) return -0.3413255062805175;
  if (node === 12) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree142(features, 25);
    } else if (val <= 712.5000000000001) {
      return calculateTree142(features, 25);
    } else {
      return calculateTree142(features, 26);
    }
  }
  if (node === 5) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree142(features, 11);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree142(features, 11);
    } else {
      return calculateTree142(features, 12);
    }
  }
  if (node === 55) return -0.40518112577494764;
  if (node === 56) return 0.23354310263429037;
  if (node === 27) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree142(features, 55);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree142(features, 55);
    } else {
      return calculateTree142(features, 56);
    }
  }
  if (node === 115) return 0.2956036575461011;
  if (node === 116) return -0.15365599343463174;
  if (node === 57) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree142(features, 115);
    } else if (val <= 35.50000000000001) {
      return calculateTree142(features, 115);
    } else {
      return calculateTree142(features, 116);
    }
  }
  if (node === 58) return -0.38443091930802203;
  if (node === 28) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree142(features, 57);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree142(features, 57);
    } else {
      return calculateTree142(features, 58);
    }
  }
  if (node === 13) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree142(features, 27);
    } else if (val <= 778.5000000000001) {
      return calculateTree142(features, 27);
    } else {
      return calculateTree142(features, 28);
    }
  }
  if (node === 14) return -0.00251120121218535;
  if (node === 6) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree142(features, 13);
    } else if (val <= 294.50000000000006) {
      return calculateTree142(features, 13);
    } else {
      return calculateTree142(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree142(features, 5);
    } else if (val <= 291.50000000000006) {
      return calculateTree142(features, 5);
    } else {
      return calculateTree142(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree142(features, 1);
    } else if (val <= 282.50000000000006) {
      return calculateTree142(features, 1);
    } else {
      return calculateTree142(features, 2);
    }
  }
}
function calculateTree143(features, node = 0) {
  if (node === 7) return -0.08054391033923891;
  if (node === 35) return -0.26873082122844705;
  if (node === 1183) return 0.03627354724655943;
  if (node === 1184) return 0.27629940744103504;
  if (node === 591) {
    const val = features['is_preference_beauty'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree143(features, 1183);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree143(features, 1183);
    } else {
      return calculateTree143(features, 1184);
    }
  }
  if (node === 1185) return -0.1013933307216069;
  if (node === 1186) return 0.17641577690676224;
  if (node === 592) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree143(features, 1185);
    } else if (val <= 801.5000000000001) {
      return calculateTree143(features, 1185);
    } else {
      return calculateTree143(features, 1186);
    }
  }
  if (node === 295) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree143(features, 591);
    } else if (val <= 582.5000000000001) {
      return calculateTree143(features, 591);
    } else {
      return calculateTree143(features, 592);
    }
  }
  if (node === 593) return -0.3243225339640336;
  if (node === 594) return 0.01547965946170005;
  if (node === 296) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree143(features, 593);
    } else if (val <= 127.50000000000001) {
      return calculateTree143(features, 593);
    } else {
      return calculateTree143(features, 594);
    }
  }
  if (node === 147) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree143(features, 295);
    } else if (val <= 835.5000000000001) {
      return calculateTree143(features, 295);
    } else {
      return calculateTree143(features, 296);
    }
  }
  if (node === 297) return 0.1647654051067663;
  if (node === 298) return 0.0056970844734940814;
  if (node === 148) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree143(features, 297);
    } else if (val <= 69.50000000000001) {
      return calculateTree143(features, 297);
    } else {
      return calculateTree143(features, 298);
    }
  }
  if (node === 73) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree143(features, 147);
    } else if (val <= 903.5000000000001) {
      return calculateTree143(features, 147);
    } else {
      return calculateTree143(features, 148);
    }
  }
  if (node === 149) return -0.17369313491737073;
  if (node === 301) return 0.17450737122888996;
  if (node === 302) return -0.027347192984475344;
  if (node === 150) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree143(features, 301);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree143(features, 301);
    } else {
      return calculateTree143(features, 302);
    }
  }
  if (node === 74) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree143(features, 149);
    } else if (val <= 555.5000000000001) {
      return calculateTree143(features, 149);
    } else {
      return calculateTree143(features, 150);
    }
  }
  if (node === 36) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree143(features, 73);
    } else if (val <= 132.50000000000003) {
      return calculateTree143(features, 73);
    } else {
      return calculateTree143(features, 74);
    }
  }
  if (node === 17) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree143(features, 35);
    } else if (val <= 70.00000000000001) {
      return calculateTree143(features, 35);
    } else {
      return calculateTree143(features, 36);
    }
  }
  if (node === 75) return 0.0027279247246714882;
  if (node === 76) return -0.024100988924052897;
  if (node === 37) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree143(features, 75);
    } else if (val <= 589.5000000000001) {
      return calculateTree143(features, 75);
    } else {
      return calculateTree143(features, 76);
    }
  }
  if (node === 1247) return 0.003831238438040389;
  if (node === 1248) return 0.0879862180462006;
  if (node === 623) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree143(features, 1247);
    } else if (val <= 745.5000000000001) {
      return calculateTree143(features, 1247);
    } else {
      return calculateTree143(features, 1248);
    }
  }
  if (node === 1249) return -0.12479819538057885;
  if (node === 1250) return 0.008102755638559268;
  if (node === 624) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree143(features, 1249);
    } else if (val <= 643.5000000000001) {
      return calculateTree143(features, 1249);
    } else {
      return calculateTree143(features, 1250);
    }
  }
  if (node === 311) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree143(features, 623);
    } else if (val <= 607.5000000000001) {
      return calculateTree143(features, 623);
    } else {
      return calculateTree143(features, 624);
    }
  }
  if (node === 1251) return 0.08106365914168022;
  if (node === 1252) return 0.0056010001811427745;
  if (node === 625) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree143(features, 1251);
    } else if (val <= 777.5000000000001) {
      return calculateTree143(features, 1251);
    } else {
      return calculateTree143(features, 1252);
    }
  }
  if (node === 1253) return 0.07213374699445504;
  if (node === 1254) return 0.306961636685607;
  if (node === 626) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree143(features, 1253);
    } else if (val <= 783.5000000000001) {
      return calculateTree143(features, 1253);
    } else {
      return calculateTree143(features, 1254);
    }
  }
  if (node === 312) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree143(features, 625);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree143(features, 625);
    } else {
      return calculateTree143(features, 626);
    }
  }
  if (node === 155) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree143(features, 311);
    } else if (val <= 715.5000000000001) {
      return calculateTree143(features, 311);
    } else {
      return calculateTree143(features, 312);
    }
  }
  if (node === 313) return -0.3315647067543085;
  if (node === 1259) return -0.0028625172001901025;
  if (node === 1260) return -0.21005157215414796;
  if (node === 629) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree143(features, 1259);
    } else if (val <= 443.50000000000006) {
      return calculateTree143(features, 1259);
    } else {
      return calculateTree143(features, 1260);
    }
  }
  if (node === 1261) return 0.07195198791916836;
  if (node === 1262) return 0.005340442842270268;
  if (node === 630) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree143(features, 1261);
    } else if (val <= 688.5000000000001) {
      return calculateTree143(features, 1261);
    } else {
      return calculateTree143(features, 1262);
    }
  }
  if (node === 314) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree143(features, 629);
    } else if (val <= 531.5000000000001) {
      return calculateTree143(features, 629);
    } else {
      return calculateTree143(features, 630);
    }
  }
  if (node === 156) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree143(features, 313);
    } else if (val <= 28.500000000000004) {
      return calculateTree143(features, 313);
    } else {
      return calculateTree143(features, 314);
    }
  }
  if (node === 77) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree143(features, 155);
    } else if (val <= 795.5000000000001) {
      return calculateTree143(features, 155);
    } else {
      return calculateTree143(features, 156);
    }
  }
  if (node === 78) return -0.32097762235380506;
  if (node === 38) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree143(features, 77);
    } else if (val <= 983.5000000000001) {
      return calculateTree143(features, 77);
    } else {
      return calculateTree143(features, 78);
    }
  }
  if (node === 18) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree143(features, 37);
    } else if (val <= 504.50000000000006) {
      return calculateTree143(features, 37);
    } else {
      return calculateTree143(features, 38);
    }
  }
  if (node === 8) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree143(features, 17);
    } else if (val <= 144.50000000000003) {
      return calculateTree143(features, 17);
    } else {
      return calculateTree143(features, 18);
    }
  }
  if (node === 3) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree143(features, 7);
    } else if (val <= 19.000000000000004) {
      return calculateTree143(features, 7);
    } else {
      return calculateTree143(features, 8);
    }
  }
  if (node === 4) return -0.004710505798906536;
  if (node === 1) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree143(features, 3);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree143(features, 3);
    } else {
      return calculateTree143(features, 4);
    }
  }
  if (node === 5) return 0.04091285039513635;
  if (node === 6) return 0.0013630964599310384;
  if (node === 2) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree143(features, 5);
    } else if (val <= 26.500000000000004) {
      return calculateTree143(features, 5);
    } else {
      return calculateTree143(features, 6);
    }
  }
  if (node === 0) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree143(features, 1);
    } else if (val <= 25.500000000000004) {
      return calculateTree143(features, 1);
    } else {
      return calculateTree143(features, 2);
    }
  }
}
function calculateTree144(features, node = 0) {
  if (node === 15) return -0.0006980251291062768;
  if (node === 33) return -0.32650938358328907;
  if (node === 1119) return -0.13696272475842475;
  if (node === 1120) return 0.26682916859657796;
  if (node === 559) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree144(features, 1119);
    } else if (val <= 9.500000000000002) {
      return calculateTree144(features, 1119);
    } else {
      return calculateTree144(features, 1120);
    }
  }
  if (node === 560) return -0.24202720416896267;
  if (node === 279) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree144(features, 559);
    } else if (val <= 35.50000000000001) {
      return calculateTree144(features, 559);
    } else {
      return calculateTree144(features, 560);
    }
  }
  if (node === 561) return 0.03515077493045998;
  if (node === 562) return 0.2735810945496453;
  if (node === 280) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree144(features, 561);
    } else if (val <= 44.50000000000001) {
      return calculateTree144(features, 561);
    } else {
      return calculateTree144(features, 562);
    }
  }
  if (node === 139) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree144(features, 279);
    } else if (val <= 1.5000000000000002) {
      return calculateTree144(features, 279);
    } else {
      return calculateTree144(features, 280);
    }
  }
  if (node === 140) return -0.08646340570631478;
  if (node === 69) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree144(features, 139);
    } else if (val <= 566.0000000000001) {
      return calculateTree144(features, 139);
    } else {
      return calculateTree144(features, 140);
    }
  }
  if (node === 70) return -0.2976179773808337;
  if (node === 34) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree144(features, 69);
    } else if (val <= 52.50000000000001) {
      return calculateTree144(features, 69);
    } else {
      return calculateTree144(features, 70);
    }
  }
  if (node === 16) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree144(features, 33);
    } else if (val <= 286.50000000000006) {
      return calculateTree144(features, 33);
    } else {
      return calculateTree144(features, 34);
    }
  }
  if (node === 7) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree144(features, 15);
    } else if (val <= 558.5000000000001) {
      return calculateTree144(features, 15);
    } else {
      return calculateTree144(features, 16);
    }
  }
  if (node === 17) return 0.055695557259078605;
  if (node === 18) return -0.3024465710061928;
  if (node === 8) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree144(features, 17);
    } else if (val <= 778.5000000000001) {
      return calculateTree144(features, 17);
    } else {
      return calculateTree144(features, 18);
    }
  }
  if (node === 3) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree144(features, 7);
    } else if (val <= 572.0000000000001) {
      return calculateTree144(features, 7);
    } else {
      return calculateTree144(features, 8);
    }
  }
  if (node === 39) return 0.018300868669762865;
  if (node === 40) return -0.333641246945856;
  if (node === 19) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree144(features, 39);
    } else if (val <= 293.00000000000006) {
      return calculateTree144(features, 39);
    } else {
      return calculateTree144(features, 40);
    }
  }
  if (node === 41) return -0.32984338052644496;
  if (node === 343) return 0.17716392429996505;
  if (node === 689) return 0.0036339637949560624;
  if (node === 690) return -0.24201227517842847;
  if (node === 344) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree144(features, 689);
    } else if (val <= 236.00000000000003) {
      return calculateTree144(features, 689);
    } else {
      return calculateTree144(features, 690);
    }
  }
  if (node === 171) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree144(features, 343);
    } else if (val <= 589.5000000000001) {
      return calculateTree144(features, 343);
    } else {
      return calculateTree144(features, 344);
    }
  }
  if (node === 172) return -0.3045834336163034;
  if (node === 85) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree144(features, 171);
    } else if (val <= 305.00000000000006) {
      return calculateTree144(features, 171);
    } else {
      return calculateTree144(features, 172);
    }
  }
  if (node === 173) return -0.25668693442235907;
  if (node === 1399) return 0.1963140770054116;
  if (node === 1400) return 0.027336586918948705;
  if (node === 699) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree144(features, 1399);
    } else if (val <= 507.00000000000006) {
      return calculateTree144(features, 1399);
    } else {
      return calculateTree144(features, 1400);
    }
  }
  if (node === 1401) return -0.1172026874659719;
  if (node === 1402) return 0.04379660501866388;
  if (node === 700) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree144(features, 1401);
    } else if (val <= 659.5000000000001) {
      return calculateTree144(features, 1401);
    } else {
      return calculateTree144(features, 1402);
    }
  }
  if (node === 349) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree144(features, 699);
    } else if (val <= 23.500000000000004) {
      return calculateTree144(features, 699);
    } else {
      return calculateTree144(features, 700);
    }
  }
  if (node === 701) return -0.3191438101773231;
  if (node === 702) return 0.2848434580333724;
  if (node === 350) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree144(features, 701);
    } else if (val <= 5.500000000000001) {
      return calculateTree144(features, 701);
    } else {
      return calculateTree144(features, 702);
    }
  }
  if (node === 174) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree144(features, 349);
    } else if (val <= 625.0000000000001) {
      return calculateTree144(features, 349);
    } else {
      return calculateTree144(features, 350);
    }
  }
  if (node === 86) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree144(features, 173);
    } else if (val <= 597.5000000000001) {
      return calculateTree144(features, 173);
    } else {
      return calculateTree144(features, 174);
    }
  }
  if (node === 42) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree144(features, 85);
    } else if (val <= 435.00000000000006) {
      return calculateTree144(features, 85);
    } else {
      return calculateTree144(features, 86);
    }
  }
  if (node === 20) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree144(features, 41);
    } else if (val <= 82.50000000000001) {
      return calculateTree144(features, 41);
    } else {
      return calculateTree144(features, 42);
    }
  }
  if (node === 9) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree144(features, 19);
    } else if (val <= 582.5000000000001) {
      return calculateTree144(features, 19);
    } else {
      return calculateTree144(features, 20);
    }
  }
  if (node === 10) return -0.23033163908976256;
  if (node === 4) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree144(features, 9);
    } else if (val <= 893.0000000000001) {
      return calculateTree144(features, 9);
    } else {
      return calculateTree144(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree144(features, 3);
    } else if (val <= 577.5000000000001) {
      return calculateTree144(features, 3);
    } else {
      return calculateTree144(features, 4);
    }
  }
  if (node === 11) return -0.3523360303720844;
  if (node === 51) return 0.04235539008059625;
  if (node === 52) return -0.07444352032526032;
  if (node === 25) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree144(features, 51);
    } else if (val <= 32.50000000000001) {
      return calculateTree144(features, 51);
    } else {
      return calculateTree144(features, 52);
    }
  }
  if (node === 53) return 0.24329236722430758;
  if (node === 54) return 0.03482852020172902;
  if (node === 26) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree144(features, 53);
    } else if (val <= 38.50000000000001) {
      return calculateTree144(features, 53);
    } else {
      return calculateTree144(features, 54);
    }
  }
  if (node === 12) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree144(features, 25);
    } else if (val <= 7.500000000000001) {
      return calculateTree144(features, 25);
    } else {
      return calculateTree144(features, 26);
    }
  }
  if (node === 5) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree144(features, 11);
    } else if (val <= 130.00000000000003) {
      return calculateTree144(features, 11);
    } else {
      return calculateTree144(features, 12);
    }
  }
  if (node === 6) return 0.0015472730393203261;
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree144(features, 5);
    } else if (val <= 639.5000000000001) {
      return calculateTree144(features, 5);
    } else {
      return calculateTree144(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree144(features, 1);
    } else if (val <= 628.5000000000001) {
      return calculateTree144(features, 1);
    } else {
      return calculateTree144(features, 2);
    }
  }
}
function calculateTree145(features, node = 0) {
  if (node === 1) return 0.0010361777277010918;
  if (node === 47) return -0.1316118625319732;
  if (node === 48) return 0.11400202598388214;
  if (node === 23) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree145(features, 47);
    } else if (val <= 7.500000000000001) {
      return calculateTree145(features, 47);
    } else {
      return calculateTree145(features, 48);
    }
  }
  if (node === 24) return -0.37979123717051777;
  if (node === 11) {
    const val = features['is_preference_men_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree145(features, 23);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree145(features, 23);
    } else {
      return calculateTree145(features, 24);
    }
  }
  if (node === 25) return 0.22374216888145024;
  if (node === 26) return -0.16829296322950482;
  if (node === 12) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree145(features, 25);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree145(features, 25);
    } else {
      return calculateTree145(features, 26);
    }
  }
  if (node === 5) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree145(features, 11);
    } else if (val <= 44.50000000000001) {
      return calculateTree145(features, 11);
    } else {
      return calculateTree145(features, 12);
    }
  }
  if (node === 111) return -0.003692607489598744;
  if (node === 112) return -0.1251205060100034;
  if (node === 55) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree145(features, 111);
    } else if (val <= 819.5000000000001) {
      return calculateTree145(features, 111);
    } else {
      return calculateTree145(features, 112);
    }
  }
  if (node === 227) return -0.2747308002924007;
  if (node === 228) return 0.052456961798632566;
  if (node === 113) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree145(features, 227);
    } else if (val <= 412.00000000000006) {
      return calculateTree145(features, 227);
    } else {
      return calculateTree145(features, 228);
    }
  }
  if (node === 919) return -0.035070046061855015;
  if (node === 920) return -0.20926962974256258;
  if (node === 459) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree145(features, 919);
    } else if (val <= 338.50000000000006) {
      return calculateTree145(features, 919);
    } else {
      return calculateTree145(features, 920);
    }
  }
  if (node === 460) return 0.027491185925983954;
  if (node === 229) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree145(features, 459);
    } else if (val <= 898.5000000000001) {
      return calculateTree145(features, 459);
    } else {
      return calculateTree145(features, 460);
    }
  }
  if (node === 230) return 0.006063110640313697;
  if (node === 114) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree145(features, 229);
    } else if (val <= 864.5000000000001) {
      return calculateTree145(features, 229);
    } else {
      return calculateTree145(features, 230);
    }
  }
  if (node === 56) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree145(features, 113);
    } else if (val <= 835.5000000000001) {
      return calculateTree145(features, 113);
    } else {
      return calculateTree145(features, 114);
    }
  }
  if (node === 27) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree145(features, 55);
    } else if (val <= 821.0000000000001) {
      return calculateTree145(features, 55);
    } else {
      return calculateTree145(features, 56);
    }
  }
  if (node === 28) return 0.05837893414923648;
  if (node === 13) {
    const val = features['is_preference_pets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree145(features, 27);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree145(features, 27);
    } else {
      return calculateTree145(features, 28);
    }
  }
  if (node === 959) return -0.1073968352484253;
  if (node === 1921) return 0.21375214298500275;
  if (node === 1922) return -0.0014088300331149487;
  if (node === 960) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree145(features, 1921);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree145(features, 1921);
    } else {
      return calculateTree145(features, 1922);
    }
  }
  if (node === 479) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree145(features, 959);
    } else if (val <= 819.5000000000001) {
      return calculateTree145(features, 959);
    } else {
      return calculateTree145(features, 960);
    }
  }
  if (node === 1923) return -0.1364458604193034;
  if (node === 1924) return 0.12101985825340793;
  if (node === 961) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree145(features, 1923);
    } else if (val <= 944.5000000000001) {
      return calculateTree145(features, 1923);
    } else {
      return calculateTree145(features, 1924);
    }
  }
  if (node === 962) return 0.023289663192873695;
  if (node === 480) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree145(features, 961);
    } else if (val <= 986.5000000000001) {
      return calculateTree145(features, 961);
    } else {
      return calculateTree145(features, 962);
    }
  }
  if (node === 239) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree145(features, 479);
    } else if (val <= 840.0000000000001) {
      return calculateTree145(features, 479);
    } else {
      return calculateTree145(features, 480);
    }
  }
  if (node === 963) return 0.010611561691843152;
  if (node === 964) return 0.31597752630488;
  if (node === 481) {
    const val = features['is_preference_pets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree145(features, 963);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree145(features, 963);
    } else {
      return calculateTree145(features, 964);
    }
  }
  if (node === 965) return 0.1484239742474058;
  if (node === 966) return -0.24599574733889382;
  if (node === 482) {
    const val = features['is_preference_household'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree145(features, 965);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree145(features, 965);
    } else {
      return calculateTree145(features, 966);
    }
  }
  if (node === 240) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree145(features, 481);
    } else if (val <= 14.500000000000002) {
      return calculateTree145(features, 481);
    } else {
      return calculateTree145(features, 482);
    }
  }
  if (node === 119) {
    const val = features['is_preference_men_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree145(features, 239);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree145(features, 239);
    } else {
      return calculateTree145(features, 240);
    }
  }
  if (node === 483) return 0.32391837444567323;
  if (node === 969) return 0.2905469588861943;
  if (node === 970) return -0.029740589021468807;
  if (node === 484) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree145(features, 969);
    } else if (val <= 897.0000000000001) {
      return calculateTree145(features, 969);
    } else {
      return calculateTree145(features, 970);
    }
  }
  if (node === 241) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree145(features, 483);
    } else if (val <= 348.50000000000006) {
      return calculateTree145(features, 483);
    } else {
      return calculateTree145(features, 484);
    }
  }
  if (node === 242) return -0.23409739073632782;
  if (node === 120) {
    const val = features['is_preference_kids'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree145(features, 241);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree145(features, 241);
    } else {
      return calculateTree145(features, 242);
    }
  }
  if (node === 59) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree145(features, 119);
    } else if (val <= 888.5000000000001) {
      return calculateTree145(features, 119);
    } else {
      return calculateTree145(features, 120);
    }
  }
  if (node === 121) return 0.3369557583829287;
  if (node === 122) return 0.0074246270656900355;
  if (node === 60) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree145(features, 121);
    } else if (val <= 467.50000000000006) {
      return calculateTree145(features, 121);
    } else {
      return calculateTree145(features, 122);
    }
  }
  if (node === 29) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree145(features, 59);
    } else if (val <= 15.500000000000002) {
      return calculateTree145(features, 59);
    } else {
      return calculateTree145(features, 60);
    }
  }
  if (node === 30) return -0.006606854467801756;
  if (node === 14) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree145(features, 29);
    } else if (val <= 16.500000000000004) {
      return calculateTree145(features, 29);
    } else {
      return calculateTree145(features, 30);
    }
  }
  if (node === 6) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree145(features, 13);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree145(features, 13);
    } else {
      return calculateTree145(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree145(features, 5);
    } else if (val <= 804.5000000000001) {
      return calculateTree145(features, 5);
    } else {
      return calculateTree145(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree145(features, 1);
    } else if (val <= 801.5000000000001) {
      return calculateTree145(features, 1);
    } else {
      return calculateTree145(features, 2);
    }
  }
}
function calculateTree146(features, node = 0) {
  if (node === 1) return -0.0008662827312456593;
  if (node === 23) return 0.02809173918467425;
  if (node === 49) return -0.3438524043518179;
  if (node === 203) return -0.44414675409737503;
  if (node === 204) return 0.025152935610373513;
  if (node === 101) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree146(features, 203);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree146(features, 203);
    } else {
      return calculateTree146(features, 204);
    }
  }
  if (node === 1647) return -0.1444893989972684;
  if (node === 1648) return 0.07934235285898628;
  if (node === 823) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree146(features, 1647);
    } else if (val <= 954.5000000000001) {
      return calculateTree146(features, 1647);
    } else {
      return calculateTree146(features, 1648);
    }
  }
  if (node === 824) return 0.26612370610929137;
  if (node === 411) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree146(features, 823);
    } else if (val <= 4.500000000000001) {
      return calculateTree146(features, 823);
    } else {
      return calculateTree146(features, 824);
    }
  }
  if (node === 412) return -0.18253372420409383;
  if (node === 205) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree146(features, 411);
    } else if (val <= 52.50000000000001) {
      return calculateTree146(features, 411);
    } else {
      return calculateTree146(features, 412);
    }
  }
  if (node === 206) return 0.1718376899401655;
  if (node === 102) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree146(features, 205);
    } else if (val <= 59.50000000000001) {
      return calculateTree146(features, 205);
    } else {
      return calculateTree146(features, 206);
    }
  }
  if (node === 50) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree146(features, 101);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree146(features, 101);
    } else {
      return calculateTree146(features, 102);
    }
  }
  if (node === 24) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree146(features, 49);
    } else if (val <= 43.50000000000001) {
      return calculateTree146(features, 49);
    } else {
      return calculateTree146(features, 50);
    }
  }
  if (node === 11) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree146(features, 23);
    } else if (val <= 42.50000000000001) {
      return calculateTree146(features, 23);
    } else {
      return calculateTree146(features, 24);
    }
  }
  if (node === 207) return -0.1449850129161032;
  if (node === 417) return 0.2471783372658099;
  if (node === 837) return -0.1040413808459704;
  if (node === 838) return 0.2523262832649573;
  if (node === 418) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree146(features, 837);
    } else if (val <= 197.50000000000003) {
      return calculateTree146(features, 837);
    } else {
      return calculateTree146(features, 838);
    }
  }
  if (node === 208) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree146(features, 417);
    } else if (val <= 38.50000000000001) {
      return calculateTree146(features, 417);
    } else {
      return calculateTree146(features, 418);
    }
  }
  if (node === 103) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree146(features, 207);
    } else if (val <= 951.5000000000001) {
      return calculateTree146(features, 207);
    } else {
      return calculateTree146(features, 208);
    }
  }
  if (node === 209) return 0.5075094430217415;
  if (node === 210) return 0.20171748954301694;
  if (node === 104) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree146(features, 209);
    } else if (val <= 951.5000000000001) {
      return calculateTree146(features, 209);
    } else {
      return calculateTree146(features, 210);
    }
  }
  if (node === 51) {
    const val = features['is_preference_beauty'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree146(features, 103);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree146(features, 103);
    } else {
      return calculateTree146(features, 104);
    }
  }
  if (node === 52) return -0.32898335946236434;
  if (node === 25) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree146(features, 51);
    } else if (val <= 955.5000000000001) {
      return calculateTree146(features, 51);
    } else {
      return calculateTree146(features, 52);
    }
  }
  if (node === 53) return -0.33442053221746104;
  if (node === 219) return 0.06555884852294275;
  if (node === 220) return -0.17003063140247807;
  if (node === 109) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree146(features, 219);
    } else if (val <= 19.500000000000004) {
      return calculateTree146(features, 219);
    } else {
      return calculateTree146(features, 220);
    }
  }
  if (node === 221) return 0.23131557251116042;
  if (node === 445) return 0.09079461718489394;
  if (node === 446) return -0.12792458489993536;
  if (node === 222) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree146(features, 445);
    } else if (val <= 40.50000000000001) {
      return calculateTree146(features, 445);
    } else {
      return calculateTree146(features, 446);
    }
  }
  if (node === 110) {
    const val = features['is_preference_kitchen'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree146(features, 221);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree146(features, 221);
    } else {
      return calculateTree146(features, 222);
    }
  }
  if (node === 54) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree146(features, 109);
    } else if (val <= 26.500000000000004) {
      return calculateTree146(features, 109);
    } else {
      return calculateTree146(features, 110);
    }
  }
  if (node === 26) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree146(features, 53);
    } else if (val <= 955.5000000000001) {
      return calculateTree146(features, 53);
    } else {
      return calculateTree146(features, 54);
    }
  }
  if (node === 12) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree146(features, 25);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree146(features, 25);
    } else {
      return calculateTree146(features, 26);
    }
  }
  if (node === 5) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree146(features, 11);
    } else if (val <= 6.500000000000001) {
      return calculateTree146(features, 11);
    } else {
      return calculateTree146(features, 12);
    }
  }
  if (node === 223) return 0.011560426411731774;
  if (node === 224) return 0.26916746871303465;
  if (node === 111) {
    const val = features['is_preference_kids'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree146(features, 223);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree146(features, 223);
    } else {
      return calculateTree146(features, 224);
    }
  }
  if (node === 112) return -0.06878494079571533;
  if (node === 55) {
    const val = features['is_preference_home'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree146(features, 111);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree146(features, 111);
    } else {
      return calculateTree146(features, 112);
    }
  }
  if (node === 113) return -0.03824401173516745;
  if (node === 114) return -0.30584945780771217;
  if (node === 56) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree146(features, 113);
    } else if (val <= 980.5000000000001) {
      return calculateTree146(features, 113);
    } else {
      return calculateTree146(features, 114);
    }
  }
  if (node === 27) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree146(features, 55);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree146(features, 55);
    } else {
      return calculateTree146(features, 56);
    }
  }
  if (node === 57) return -0.37127085728575776;
  if (node === 117) return 0.4604166184128118;
  if (node === 118) return -0.3293853036077099;
  if (node === 58) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree146(features, 117);
    } else if (val <= 22.500000000000004) {
      return calculateTree146(features, 117);
    } else {
      return calculateTree146(features, 118);
    }
  }
  if (node === 28) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree146(features, 57);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree146(features, 57);
    } else {
      return calculateTree146(features, 58);
    }
  }
  if (node === 13) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree146(features, 27);
    } else if (val <= 981.5000000000001) {
      return calculateTree146(features, 27);
    } else {
      return calculateTree146(features, 28);
    }
  }
  if (node === 14) return 0.006506239536088805;
  if (node === 6) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree146(features, 13);
    } else if (val <= 982.5000000000001) {
      return calculateTree146(features, 13);
    } else {
      return calculateTree146(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree146(features, 5);
    } else if (val <= 960.0000000000001) {
      return calculateTree146(features, 5);
    } else {
      return calculateTree146(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree146(features, 1);
    } else if (val <= 948.5000000000001) {
      return calculateTree146(features, 1);
    } else {
      return calculateTree146(features, 2);
    }
  }
}
function calculateTree147(features, node = 0) {
  if (node === 7) return -0.011375966530496388;
  if (node === 17) return -0.3432115085230847;
  if (node === 37) return 0.08556906962242565;
  if (node === 38) return -0.14476554487907303;
  if (node === 18) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree147(features, 37);
    } else if (val <= 26.500000000000004) {
      return calculateTree147(features, 37);
    } else {
      return calculateTree147(features, 38);
    }
  }
  if (node === 8) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree147(features, 17);
    } else if (val <= 6.500000000000001) {
      return calculateTree147(features, 17);
    } else {
      return calculateTree147(features, 18);
    }
  }
  if (node === 3) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree147(features, 7);
    } else if (val <= 3.5000000000000004) {
      return calculateTree147(features, 7);
    } else {
      return calculateTree147(features, 8);
    }
  }
  if (node === 4) return 0.16819189673390308;
  if (node === 1) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree147(features, 3);
    } else if (val <= 29.500000000000004) {
      return calculateTree147(features, 3);
    } else {
      return calculateTree147(features, 4);
    }
  }
  if (node === 5) return 0.0029603175241951665;
  if (node === 27) return 0.0004009992869299807;
  if (node === 115) return -0.3145991793621002;
  if (node === 467) return -0.2981586114134044;
  if (node === 468) return 0.07416413030000149;
  if (node === 233) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree147(features, 467);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree147(features, 467);
    } else {
      return calculateTree147(features, 468);
    }
  }
  if (node === 939) return 0.15438124655926624;
  if (node === 1881) return -0.03802939197078613;
  if (node === 1882) return -0.2564270724591749;
  if (node === 940) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree147(features, 1881);
    } else if (val <= 675.5000000000001) {
      return calculateTree147(features, 1881);
    } else {
      return calculateTree147(features, 1882);
    }
  }
  if (node === 469) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree147(features, 939);
    } else if (val <= 447.00000000000006) {
      return calculateTree147(features, 939);
    } else {
      return calculateTree147(features, 940);
    }
  }
  if (node === 470) return -0.2801401360023035;
  if (node === 234) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree147(features, 469);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree147(features, 469);
    } else {
      return calculateTree147(features, 470);
    }
  }
  if (node === 116) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree147(features, 233);
    } else if (val <= 667.5000000000001) {
      return calculateTree147(features, 233);
    } else {
      return calculateTree147(features, 234);
    }
  }
  if (node === 57) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree147(features, 115);
    } else if (val <= 597.5000000000001) {
      return calculateTree147(features, 115);
    } else {
      return calculateTree147(features, 116);
    }
  }
  if (node === 1887) return 0.4043776889549115;
  if (node === 1888) return 0.07420625947849674;
  if (node === 943) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree147(features, 1887);
    } else if (val <= 22.500000000000004) {
      return calculateTree147(features, 1887);
    } else {
      return calculateTree147(features, 1888);
    }
  }
  if (node === 1889) return -0.3205841096863908;
  if (node === 1890) return 0.11249000206256002;
  if (node === 944) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree147(features, 1889);
    } else if (val <= 718.5000000000001) {
      return calculateTree147(features, 1889);
    } else {
      return calculateTree147(features, 1890);
    }
  }
  if (node === 471) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree147(features, 943);
    } else if (val <= 705.5000000000001) {
      return calculateTree147(features, 943);
    } else {
      return calculateTree147(features, 944);
    }
  }
  if (node === 472) return -0.24354120223852876;
  if (node === 235) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree147(features, 471);
    } else if (val <= 577.5000000000001) {
      return calculateTree147(features, 471);
    } else {
      return calculateTree147(features, 472);
    }
  }
  if (node === 473) return 0.1595233137736908;
  if (node === 949) return -0.3287014695866096;
  if (node === 1901) return 0.051271400774281384;
  if (node === 1902) return -0.15078719653533793;
  if (node === 950) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree147(features, 1901);
    } else if (val <= 948.0000000000001) {
      return calculateTree147(features, 1901);
    } else {
      return calculateTree147(features, 1902);
    }
  }
  if (node === 474) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree147(features, 949);
    } else if (val <= 783.5000000000001) {
      return calculateTree147(features, 949);
    } else {
      return calculateTree147(features, 950);
    }
  }
  if (node === 236) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree147(features, 473);
    } else if (val <= 777.5000000000001) {
      return calculateTree147(features, 473);
    } else {
      return calculateTree147(features, 474);
    }
  }
  if (node === 117) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree147(features, 235);
    } else if (val <= 771.0000000000001) {
      return calculateTree147(features, 235);
    } else {
      return calculateTree147(features, 236);
    }
  }
  if (node === 475) return -0.31012317151576735;
  if (node === 476) return 0.05103675995598999;
  if (node === 237) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree147(features, 475);
    } else if (val <= 27.500000000000004) {
      return calculateTree147(features, 475);
    } else {
      return calculateTree147(features, 476);
    }
  }
  if (node === 955) return 0.09555853836504591;
  if (node === 1913) return -0.2994335139829959;
  if (node === 1914) return 0.011019122614232284;
  if (node === 956) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree147(features, 1913);
    } else if (val <= 606.5000000000001) {
      return calculateTree147(features, 1913);
    } else {
      return calculateTree147(features, 1914);
    }
  }
  if (node === 477) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree147(features, 955);
    } else if (val <= 566.0000000000001) {
      return calculateTree147(features, 955);
    } else {
      return calculateTree147(features, 956);
    }
  }
  if (node === 957) return -0.30640545643532086;
  if (node === 1917) return 0.02907999139776128;
  if (node === 1918) return -0.3290669935754289;
  if (node === 958) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree147(features, 1917);
    } else if (val <= 702.5000000000001) {
      return calculateTree147(features, 1917);
    } else {
      return calculateTree147(features, 1918);
    }
  }
  if (node === 478) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree147(features, 957);
    } else if (val <= 839.5000000000001) {
      return calculateTree147(features, 957);
    } else {
      return calculateTree147(features, 958);
    }
  }
  if (node === 238) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree147(features, 477);
    } else if (val <= 718.5000000000001) {
      return calculateTree147(features, 477);
    } else {
      return calculateTree147(features, 478);
    }
  }
  if (node === 118) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree147(features, 237);
    } else if (val <= 551.5000000000001) {
      return calculateTree147(features, 237);
    } else {
      return calculateTree147(features, 238);
    }
  }
  if (node === 58) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree147(features, 117);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree147(features, 117);
    } else {
      return calculateTree147(features, 118);
    }
  }
  if (node === 28) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree147(features, 57);
    } else if (val <= 542.0000000000001) {
      return calculateTree147(features, 57);
    } else {
      return calculateTree147(features, 58);
    }
  }
  if (node === 13) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree147(features, 27);
    } else if (val <= 518.5000000000001) {
      return calculateTree147(features, 27);
    } else {
      return calculateTree147(features, 28);
    }
  }
  if (node === 14) return 0.004967631653734977;
  if (node === 6) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree147(features, 13);
    } else if (val <= 715.5000000000001) {
      return calculateTree147(features, 13);
    } else {
      return calculateTree147(features, 14);
    }
  }
  if (node === 2) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree147(features, 5);
    } else if (val <= 432.00000000000006) {
      return calculateTree147(features, 5);
    } else {
      return calculateTree147(features, 6);
    }
  }
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree147(features, 1);
    } else if (val <= 3.0000000000000004) {
      return calculateTree147(features, 1);
    } else {
      return calculateTree147(features, 2);
    }
  }
}
function calculateTree148(features, node = 0) {
  if (node === 1) return -0.0013970051633696307;
  if (node === 47) return 0.0299671211225909;
  if (node === 48) return -0.0670029622103474;
  if (node === 23) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree148(features, 47);
    } else if (val <= 7.500000000000001) {
      return calculateTree148(features, 47);
    } else {
      return calculateTree148(features, 48);
    }
  }
  if (node === 99) return 0.3890788481186224;
  if (node === 201) return 0.19877974838278298;
  if (node === 202) return -0.10891825919526238;
  if (node === 100) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree148(features, 201);
    } else if (val <= 48.50000000000001) {
      return calculateTree148(features, 201);
    } else {
      return calculateTree148(features, 202);
    }
  }
  if (node === 49) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree148(features, 99);
    } else if (val <= 17.500000000000004) {
      return calculateTree148(features, 99);
    } else {
      return calculateTree148(features, 100);
    }
  }
  if (node === 50) return 0.02312432519912667;
  if (node === 24) {
    const val = features['is_preference_home'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree148(features, 49);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree148(features, 49);
    } else {
      return calculateTree148(features, 50);
    }
  }
  if (node === 11) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree148(features, 23);
    } else if (val <= 14.500000000000002) {
      return calculateTree148(features, 23);
    } else {
      return calculateTree148(features, 24);
    }
  }
  if (node === 12) return 0.247293445966144;
  if (node === 5) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree148(features, 11);
    } else if (val <= 62.50000000000001) {
      return calculateTree148(features, 11);
    } else {
      return calculateTree148(features, 12);
    }
  }
  if (node === 447) return 0.2841130033485701;
  if (node === 448) return -0.03248229348117882;
  if (node === 223) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree148(features, 447);
    } else if (val <= 181.50000000000003) {
      return calculateTree148(features, 447);
    } else {
      return calculateTree148(features, 448);
    }
  }
  if (node === 449) return -0.24892020520167707;
  if (node === 450) return 0.14516529758223004;
  if (node === 224) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree148(features, 449);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree148(features, 449);
    } else {
      return calculateTree148(features, 450);
    }
  }
  if (node === 111) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree148(features, 223);
    } else if (val <= 775.5000000000001) {
      return calculateTree148(features, 223);
    } else {
      return calculateTree148(features, 224);
    }
  }
  if (node === 451) return -0.06837354254593835;
  if (node === 452) return 0.09116718941953497;
  if (node === 225) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree148(features, 451);
    } else if (val <= 903.5000000000001) {
      return calculateTree148(features, 451);
    } else {
      return calculateTree148(features, 452);
    }
  }
  if (node === 1815) return -0.05644459959306294;
  if (node === 1816) return -0.35813065799590743;
  if (node === 907) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree148(features, 1815);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree148(features, 1815);
    } else {
      return calculateTree148(features, 1816);
    }
  }
  if (node === 908) return -0.429732530164817;
  if (node === 453) {
    const val = features['is_preference_kitchen'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree148(features, 907);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree148(features, 907);
    } else {
      return calculateTree148(features, 908);
    }
  }
  if (node === 454) return -0.0064912907595672436;
  if (node === 226) {
    const val = features['is_preference_grocery'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree148(features, 453);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree148(features, 453);
    } else {
      return calculateTree148(features, 454);
    }
  }
  if (node === 112) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree148(features, 225);
    } else if (val <= 12.500000000000002) {
      return calculateTree148(features, 225);
    } else {
      return calculateTree148(features, 226);
    }
  }
  if (node === 55) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree148(features, 111);
    } else if (val <= 34.50000000000001) {
      return calculateTree148(features, 111);
    } else {
      return calculateTree148(features, 112);
    }
  }
  if (node === 455) return 0.16776693582731997;
  if (node === 913) return -0.04659746528117418;
  if (node === 914) return 0.21333899328508454;
  if (node === 456) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree148(features, 913);
    } else if (val <= 7.500000000000001) {
      return calculateTree148(features, 913);
    } else {
      return calculateTree148(features, 914);
    }
  }
  if (node === 227) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree148(features, 455);
    } else if (val <= 693.0000000000001) {
      return calculateTree148(features, 455);
    } else {
      return calculateTree148(features, 456);
    }
  }
  if (node === 915) return 0.25240376277796955;
  if (node === 916) return -0.19706430918020532;
  if (node === 457) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree148(features, 915);
    } else if (val <= 6.500000000000001) {
      return calculateTree148(features, 915);
    } else {
      return calculateTree148(features, 916);
    }
  }
  if (node === 458) return -0.3415652539627561;
  if (node === 228) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree148(features, 457);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree148(features, 457);
    } else {
      return calculateTree148(features, 458);
    }
  }
  if (node === 113) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree148(features, 227);
    } else if (val <= 804.0000000000001) {
      return calculateTree148(features, 227);
    } else {
      return calculateTree148(features, 228);
    }
  }
  if (node === 114) return -0.008107130357066573;
  if (node === 56) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree148(features, 113);
    } else if (val <= 47.50000000000001) {
      return calculateTree148(features, 113);
    } else {
      return calculateTree148(features, 114);
    }
  }
  if (node === 27) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree148(features, 55);
    } else if (val <= 42.50000000000001) {
      return calculateTree148(features, 55);
    } else {
      return calculateTree148(features, 56);
    }
  }
  if (node === 231) return 0.5190228094842345;
  if (node === 232) return -0.11780574319833022;
  if (node === 115) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree148(features, 231);
    } else if (val <= 143.50000000000003) {
      return calculateTree148(features, 231);
    } else {
      return calculateTree148(features, 232);
    }
  }
  if (node === 116) return -0.20150780646260635;
  if (node === 57) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree148(features, 115);
    } else if (val <= 1.5000000000000002) {
      return calculateTree148(features, 115);
    } else {
      return calculateTree148(features, 116);
    }
  }
  if (node === 235) return -0.2717037598207847;
  if (node === 236) return 0.07345838756172349;
  if (node === 117) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree148(features, 235);
    } else if (val <= 481.50000000000006) {
      return calculateTree148(features, 235);
    } else {
      return calculateTree148(features, 236);
    }
  }
  if (node === 118) return -0.2753902424938101;
  if (node === 58) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree148(features, 117);
    } else if (val <= 507.50000000000006) {
      return calculateTree148(features, 117);
    } else {
      return calculateTree148(features, 118);
    }
  }
  if (node === 28) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree148(features, 57);
    } else if (val <= 33.50000000000001) {
      return calculateTree148(features, 57);
    } else {
      return calculateTree148(features, 58);
    }
  }
  if (node === 13) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree148(features, 27);
    } else if (val <= 55.50000000000001) {
      return calculateTree148(features, 27);
    } else {
      return calculateTree148(features, 28);
    }
  }
  if (node === 14) return 0.0027739479405234917;
  if (node === 6) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree148(features, 13);
    } else if (val <= 82.50000000000001) {
      return calculateTree148(features, 13);
    } else {
      return calculateTree148(features, 14);
    }
  }
  if (node === 2) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree148(features, 5);
    } else if (val <= 19.000000000000004) {
      return calculateTree148(features, 5);
    } else {
      return calculateTree148(features, 6);
    }
  }
  if (node === 0) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree148(features, 1);
    } else if (val <= 25.500000000000004) {
      return calculateTree148(features, 1);
    } else {
      return calculateTree148(features, 2);
    }
  }
}
function calculateTree149(features, node = 0) {
  if (node === 3) return -0.0024222404745982947;
  if (node === 39) return 0.1743253880793643;
  if (node === 40) return 0.47016671307726354;
  if (node === 19) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree149(features, 39);
    } else if (val <= 197.50000000000003) {
      return calculateTree149(features, 39);
    } else {
      return calculateTree149(features, 40);
    }
  }
  if (node === 83) return -0.3334867732821562;
  if (node === 679) return 0.019572200353683917;
  if (node === 1361) return -0.3582250341474816;
  if (node === 1362) return -0.04030726071870371;
  if (node === 680) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree149(features, 1361);
    } else if (val <= 38.50000000000001) {
      return calculateTree149(features, 1361);
    } else {
      return calculateTree149(features, 1362);
    }
  }
  if (node === 339) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree149(features, 679);
    } else if (val <= 13.500000000000002) {
      return calculateTree149(features, 679);
    } else {
      return calculateTree149(features, 680);
    }
  }
  if (node === 340) return 0.26337963129772435;
  if (node === 169) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree149(features, 339);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree149(features, 339);
    } else {
      return calculateTree149(features, 340);
    }
  }
  if (node === 341) return 0.2728867948474113;
  if (node === 342) return -0.03521570607950932;
  if (node === 170) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree149(features, 341);
    } else if (val <= 24.500000000000004) {
      return calculateTree149(features, 341);
    } else {
      return calculateTree149(features, 342);
    }
  }
  if (node === 84) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree149(features, 169);
    } else if (val <= 449.50000000000006) {
      return calculateTree149(features, 169);
    } else {
      return calculateTree149(features, 170);
    }
  }
  if (node === 41) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree149(features, 83);
    } else if (val <= 391.00000000000006) {
      return calculateTree149(features, 83);
    } else {
      return calculateTree149(features, 84);
    }
  }
  if (node === 42) return -0.32222183402622284;
  if (node === 20) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree149(features, 41);
    } else if (val <= 507.00000000000006) {
      return calculateTree149(features, 41);
    } else {
      return calculateTree149(features, 42);
    }
  }
  if (node === 9) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree149(features, 19);
    } else if (val <= 127.50000000000001) {
      return calculateTree149(features, 19);
    } else {
      return calculateTree149(features, 20);
    }
  }
  if (node === 43) return -0.3185254789353729;
  if (node === 44) return 0.0669993160196181;
  if (node === 21) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree149(features, 43);
    } else if (val <= 127.50000000000001) {
      return calculateTree149(features, 43);
    } else {
      return calculateTree149(features, 44);
    }
  }
  if (node === 22) return -0.3335338066237408;
  if (node === 10) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree149(features, 21);
    } else if (val <= 240.00000000000003) {
      return calculateTree149(features, 21);
    } else {
      return calculateTree149(features, 22);
    }
  }
  if (node === 4) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree149(features, 9);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree149(features, 9);
    } else {
      return calculateTree149(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree149(features, 3);
    } else if (val <= 338.50000000000006) {
      return calculateTree149(features, 3);
    } else {
      return calculateTree149(features, 4);
    }
  }
  if (node === 191) return 0.032539631482280444;
  if (node === 192) return -0.22177464276858627;
  if (node === 95) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree149(features, 191);
    } else if (val <= 11.500000000000002) {
      return calculateTree149(features, 191);
    } else {
      return calculateTree149(features, 192);
    }
  }
  if (node === 96) return -0.23308466626564242;
  if (node === 47) {
    const val = features['is_preference_pets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree149(features, 95);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree149(features, 95);
    } else {
      return calculateTree149(features, 96);
    }
  }
  if (node === 48) return 0.1924694961350467;
  if (node === 23) {
    const val = features['is_preference_men_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree149(features, 47);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree149(features, 47);
    } else {
      return calculateTree149(features, 48);
    }
  }
  if (node === 49) return -0.23381237025606758;
  if (node === 101) return 0.19689674185076322;
  if (node === 823) return 0.12518011457168055;
  if (node === 1649) return -0.01402446325202847;
  if (node === 1650) return -0.281047918057973;
  if (node === 824) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree149(features, 1649);
    } else if (val <= 888.5000000000001) {
      return calculateTree149(features, 1649);
    } else {
      return calculateTree149(features, 1650);
    }
  }
  if (node === 411) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree149(features, 823);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree149(features, 823);
    } else {
      return calculateTree149(features, 824);
    }
  }
  if (node === 825) return -0.4694139093427044;
  if (node === 826) return -0.11727558645249848;
  if (node === 412) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree149(features, 825);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree149(features, 825);
    } else {
      return calculateTree149(features, 826);
    }
  }
  if (node === 205) {
    const val = features['is_preference_beauty'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree149(features, 411);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree149(features, 411);
    } else {
      return calculateTree149(features, 412);
    }
  }
  if (node === 206) return 0.06587756890418968;
  if (node === 102) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree149(features, 205);
    } else if (val <= 43.50000000000001) {
      return calculateTree149(features, 205);
    } else {
      return calculateTree149(features, 206);
    }
  }
  if (node === 50) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree149(features, 101);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree149(features, 101);
    } else {
      return calculateTree149(features, 102);
    }
  }
  if (node === 24) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree149(features, 49);
    } else if (val <= 576.0000000000001) {
      return calculateTree149(features, 49);
    } else {
      return calculateTree149(features, 50);
    }
  }
  if (node === 11) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree149(features, 23);
    } else if (val <= 307.00000000000006) {
      return calculateTree149(features, 23);
    } else {
      return calculateTree149(features, 24);
    }
  }
  if (node === 25) return -0.3869502383734484;
  if (node === 107) return 0.12049363894460904;
  if (node === 108) return -0.10667436593725427;
  if (node === 53) {
    const val = features['is_preference_pets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree149(features, 107);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree149(features, 107);
    } else {
      return calculateTree149(features, 108);
    }
  }
  if (node === 109) return 0.32496180537684477;
  if (node === 110) return 0.10158130423318033;
  if (node === 54) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree149(features, 109);
    } else if (val <= 33.50000000000001) {
      return calculateTree149(features, 109);
    } else {
      return calculateTree149(features, 110);
    }
  }
  if (node === 26) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree149(features, 53);
    } else if (val <= 162.50000000000003) {
      return calculateTree149(features, 53);
    } else {
      return calculateTree149(features, 54);
    }
  }
  if (node === 12) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree149(features, 25);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree149(features, 25);
    } else {
      return calculateTree149(features, 26);
    }
  }
  if (node === 5) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree149(features, 11);
    } else if (val <= 361.50000000000006) {
      return calculateTree149(features, 11);
    } else {
      return calculateTree149(features, 12);
    }
  }
  if (node === 6) return -0.00011436187543400724;
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree149(features, 5);
    } else if (val <= 365.50000000000006) {
      return calculateTree149(features, 5);
    } else {
      return calculateTree149(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree149(features, 1);
    } else if (val <= 350.50000000000006) {
      return calculateTree149(features, 1);
    } else {
      return calculateTree149(features, 2);
    }
  }
}
function calculateTree150(features, node = 0) {
  if (node === 7) return 0.0009365411486636293;
  if (node === 35) return 0.009498439828406627;
  if (node === 295) return 0.031799030256915654;
  if (node === 296) return -0.23824052940241242;
  if (node === 147) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree150(features, 295);
    } else if (val <= 57.50000000000001) {
      return calculateTree150(features, 295);
    } else {
      return calculateTree150(features, 296);
    }
  }
  if (node === 595) return 0.03292440071410482;
  if (node === 596) return 0.20755061704955857;
  if (node === 297) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree150(features, 595);
    } else if (val <= 15.500000000000002) {
      return calculateTree150(features, 595);
    } else {
      return calculateTree150(features, 596);
    }
  }
  if (node === 597) return -0.3131632635272861;
  if (node === 598) return 0.01051286768768934;
  if (node === 298) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree150(features, 597);
    } else if (val <= 311.50000000000006) {
      return calculateTree150(features, 597);
    } else {
      return calculateTree150(features, 598);
    }
  }
  if (node === 148) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree150(features, 297);
    } else if (val <= 253.50000000000003) {
      return calculateTree150(features, 297);
    } else {
      return calculateTree150(features, 298);
    }
  }
  if (node === 73) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree150(features, 147);
    } else if (val <= 217.50000000000003) {
      return calculateTree150(features, 147);
    } else {
      return calculateTree150(features, 148);
    }
  }
  if (node === 149) return -0.2930929480626163;
  if (node === 150) return 0.12465923318422104;
  if (node === 74) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree150(features, 149);
    } else if (val <= 973.5000000000001) {
      return calculateTree150(features, 149);
    } else {
      return calculateTree150(features, 150);
    }
  }
  if (node === 36) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree150(features, 73);
    } else if (val <= 702.5000000000001) {
      return calculateTree150(features, 73);
    } else {
      return calculateTree150(features, 74);
    }
  }
  if (node === 17) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree150(features, 35);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree150(features, 35);
    } else {
      return calculateTree150(features, 36);
    }
  }
  if (node === 18) return -0.3508290175995114;
  if (node === 8) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree150(features, 17);
    } else if (val <= 804.5000000000001) {
      return calculateTree150(features, 17);
    } else {
      return calculateTree150(features, 18);
    }
  }
  if (node === 3) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree150(features, 7);
    } else if (val <= 893.0000000000001) {
      return calculateTree150(features, 7);
    } else {
      return calculateTree150(features, 8);
    }
  }
  if (node === 9) return -0.33472761615118;
  if (node === 43) return 0.07366347554512098;
  if (node === 179) return 0.00295957194449563;
  if (node === 180) return -0.13707703110803754;
  if (node === 89) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree150(features, 179);
    } else if (val <= 56.00000000000001) {
      return calculateTree150(features, 179);
    } else {
      return calculateTree150(features, 180);
    }
  }
  if (node === 181) return -0.03668955874309827;
  if (node === 365) return 0.347897301016611;
  if (node === 366) return 0.08716672390117466;
  if (node === 182) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree150(features, 365);
    } else if (val <= 15.500000000000002) {
      return calculateTree150(features, 365);
    } else {
      return calculateTree150(features, 366);
    }
  }
  if (node === 90) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree150(features, 181);
    } else if (val <= 7.500000000000001) {
      return calculateTree150(features, 181);
    } else {
      return calculateTree150(features, 182);
    }
  }
  if (node === 44) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree150(features, 89);
    } else if (val <= 511.00000000000006) {
      return calculateTree150(features, 89);
    } else {
      return calculateTree150(features, 90);
    }
  }
  if (node === 21) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree150(features, 43);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree150(features, 43);
    } else {
      return calculateTree150(features, 44);
    }
  }
  if (node === 183) return 0.25915932500002337;
  if (node === 184) return -0.05394265600343893;
  if (node === 91) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree150(features, 183);
    } else if (val <= 295.00000000000006) {
      return calculateTree150(features, 183);
    } else {
      return calculateTree150(features, 184);
    }
  }
  if (node === 92) return 0.38278398161073207;
  if (node === 45) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree150(features, 91);
    } else if (val <= 804.0000000000001) {
      return calculateTree150(features, 91);
    } else {
      return calculateTree150(features, 92);
    }
  }
  if (node === 46) return 0.05029076156777195;
  if (node === 22) {
    const val = features['is_preference_sports'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree150(features, 45);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree150(features, 45);
    } else {
      return calculateTree150(features, 46);
    }
  }
  if (node === 10) {
    const val = features['is_preference_home'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree150(features, 21);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree150(features, 21);
    } else {
      return calculateTree150(features, 22);
    }
  }
  if (node === 4) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree150(features, 9);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree150(features, 9);
    } else {
      return calculateTree150(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree150(features, 3);
    } else if (val <= 818.0000000000001) {
      return calculateTree150(features, 3);
    } else {
      return calculateTree150(features, 4);
    }
  }
  if (node === 23) return -0.12600387739703545;
  if (node === 199) return 0.026577807113419453;
  if (node === 200) return 0.16823661199669251;
  if (node === 99) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree150(features, 199);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree150(features, 199);
    } else {
      return calculateTree150(features, 200);
    }
  }
  if (node === 100) return -0.2584856467349618;
  if (node === 49) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree150(features, 99);
    } else if (val <= 6.500000000000001) {
      return calculateTree150(features, 99);
    } else {
      return calculateTree150(features, 100);
    }
  }
  if (node === 101) return 0.4695161432049144;
  if (node === 102) return 0.2076180766642974;
  if (node === 50) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree150(features, 101);
    } else if (val <= 1.5000000000000002) {
      return calculateTree150(features, 101);
    } else {
      return calculateTree150(features, 102);
    }
  }
  if (node === 24) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree150(features, 49);
    } else if (val <= 197.50000000000003) {
      return calculateTree150(features, 49);
    } else {
      return calculateTree150(features, 50);
    }
  }
  if (node === 11) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree150(features, 23);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree150(features, 23);
    } else {
      return calculateTree150(features, 24);
    }
  }
  if (node === 12) return -0.32508547266901994;
  if (node === 5) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree150(features, 11);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree150(features, 11);
    } else {
      return calculateTree150(features, 12);
    }
  }
  if (node === 27) return -0.0042482005447157445;
  if (node === 28) return -0.2785394065139471;
  if (node === 13) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree150(features, 27);
    } else if (val <= 884.0000000000001) {
      return calculateTree150(features, 27);
    } else {
      return calculateTree150(features, 28);
    }
  }
  if (node === 14) return 0.01286638670848375;
  if (node === 6) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree150(features, 13);
    } else if (val <= 898.5000000000001) {
      return calculateTree150(features, 13);
    } else {
      return calculateTree150(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree150(features, 5);
    } else if (val <= 821.0000000000001) {
      return calculateTree150(features, 5);
    } else {
      return calculateTree150(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree150(features, 1);
    } else if (val <= 819.5000000000001) {
      return calculateTree150(features, 1);
    } else {
      return calculateTree150(features, 2);
    }
  }
}
function calculateTree151(features, node = 0) {
  if (node === 3) return -0.0006790567485284033;
  if (node === 39) return -0.33618685013769867;
  if (node === 40) return 0.06564556004334236;
  if (node === 19) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree151(features, 39);
    } else if (val <= 480.00000000000006) {
      return calculateTree151(features, 39);
    } else {
      return calculateTree151(features, 40);
    }
  }
  if (node === 20) return -0.3324951474949233;
  if (node === 9) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree151(features, 19);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree151(features, 19);
    } else {
      return calculateTree151(features, 20);
    }
  }
  if (node === 21) return -0.3204371743765907;
  if (node === 45) return 0.07302085008902479;
  if (node === 46) return 0.32216644366337005;
  if (node === 22) {
    const val = features['is_preference_personal_care'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree151(features, 45);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree151(features, 45);
    } else {
      return calculateTree151(features, 46);
    }
  }
  if (node === 10) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree151(features, 21);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree151(features, 21);
    } else {
      return calculateTree151(features, 22);
    }
  }
  if (node === 4) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree151(features, 9);
    } else if (val <= 507.00000000000006) {
      return calculateTree151(features, 9);
    } else {
      return calculateTree151(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree151(features, 3);
    } else if (val <= 475.50000000000006) {
      return calculateTree151(features, 3);
    } else {
      return calculateTree151(features, 4);
    }
  }
  if (node === 23) return 0.14611326873801816;
  if (node === 24) return -0.2898425443142303;
  if (node === 11) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree151(features, 23);
    } else if (val <= 117.50000000000001) {
      return calculateTree151(features, 23);
    } else {
      return calculateTree151(features, 24);
    }
  }
  if (node === 25) return -0.3642444381804927;
  if (node === 431) return 0.19207797717532776;
  if (node === 432) return -0.07660288773939292;
  if (node === 215) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree151(features, 431);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree151(features, 431);
    } else {
      return calculateTree151(features, 432);
    }
  }
  if (node === 433) return -0.29337186767645446;
  if (node === 434) return -0.12698474039244226;
  if (node === 216) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree151(features, 433);
    } else if (val <= 729.0000000000001) {
      return calculateTree151(features, 433);
    } else {
      return calculateTree151(features, 434);
    }
  }
  if (node === 107) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree151(features, 215);
    } else if (val <= 56.00000000000001) {
      return calculateTree151(features, 215);
    } else {
      return calculateTree151(features, 216);
    }
  }
  if (node === 435) return -0.11593388675537897;
  if (node === 1747) return 0.03253820768824506;
  if (node === 1748) return 0.2906372720893996;
  if (node === 873) {
    const val = features['is_preference_grocery'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree151(features, 1747);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree151(features, 1747);
    } else {
      return calculateTree151(features, 1748);
    }
  }
  if (node === 1749) return 0.21967089230943446;
  if (node === 1750) return -0.28392674652693545;
  if (node === 874) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree151(features, 1749);
    } else if (val <= 479.50000000000006) {
      return calculateTree151(features, 1749);
    } else {
      return calculateTree151(features, 1750);
    }
  }
  if (node === 436) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree151(features, 873);
    } else if (val <= 51.50000000000001) {
      return calculateTree151(features, 873);
    } else {
      return calculateTree151(features, 874);
    }
  }
  if (node === 217) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree151(features, 435);
    } else if (val <= 2.5000000000000004) {
      return calculateTree151(features, 435);
    } else {
      return calculateTree151(features, 436);
    }
  }
  if (node === 218) return 0.2295160477549429;
  if (node === 108) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree151(features, 217);
    } else if (val <= 503.50000000000006) {
      return calculateTree151(features, 217);
    } else {
      return calculateTree151(features, 218);
    }
  }
  if (node === 53) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree151(features, 107);
    } else if (val <= 33.50000000000001) {
      return calculateTree151(features, 107);
    } else {
      return calculateTree151(features, 108);
    }
  }
  if (node === 219) return 0.15131618880468276;
  if (node === 220) return -0.055736602960934724;
  if (node === 109) {
    const val = features['is_preference_beauty'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree151(features, 219);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree151(features, 219);
    } else {
      return calculateTree151(features, 220);
    }
  }
  if (node === 221) return -0.3238460257888095;
  if (node === 445) return 0.07235215139591243;
  if (node === 893) return -0.03311439235769759;
  if (node === 894) return 0.11610349155033499;
  if (node === 446) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree151(features, 893);
    } else if (val <= 234.50000000000003) {
      return calculateTree151(features, 893);
    } else {
      return calculateTree151(features, 894);
    }
  }
  if (node === 222) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree151(features, 445);
    } else if (val <= 2.5000000000000004) {
      return calculateTree151(features, 445);
    } else {
      return calculateTree151(features, 446);
    }
  }
  if (node === 110) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree151(features, 221);
    } else if (val <= 723.5000000000001) {
      return calculateTree151(features, 221);
    } else {
      return calculateTree151(features, 222);
    }
  }
  if (node === 54) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree151(features, 109);
    } else if (val <= 435.00000000000006) {
      return calculateTree151(features, 109);
    } else {
      return calculateTree151(features, 110);
    }
  }
  if (node === 26) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree151(features, 53);
    } else if (val <= 1.5000000000000002) {
      return calculateTree151(features, 53);
    } else {
      return calculateTree151(features, 54);
    }
  }
  if (node === 12) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree151(features, 25);
    } else if (val <= 117.50000000000001) {
      return calculateTree151(features, 25);
    } else {
      return calculateTree151(features, 26);
    }
  }
  if (node === 5) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree151(features, 11);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree151(features, 11);
    } else {
      return calculateTree151(features, 12);
    }
  }
  if (node === 27) return -0.14105881015764946;
  if (node === 57) return 0.002658013989375754;
  if (node === 117) return -0.17882848546143942;
  if (node === 237) return -0.007328779003580966;
  if (node === 238) return -0.08374991580210729;
  if (node === 118) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree151(features, 237);
    } else if (val <= 295.00000000000006) {
      return calculateTree151(features, 237);
    } else {
      return calculateTree151(features, 238);
    }
  }
  if (node === 58) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree151(features, 117);
    } else if (val <= 556.0000000000001) {
      return calculateTree151(features, 117);
    } else {
      return calculateTree151(features, 118);
    }
  }
  if (node === 28) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree151(features, 57);
    } else if (val <= 507.00000000000006) {
      return calculateTree151(features, 57);
    } else {
      return calculateTree151(features, 58);
    }
  }
  if (node === 13) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree151(features, 27);
    } else if (val <= 517.5000000000001) {
      return calculateTree151(features, 27);
    } else {
      return calculateTree151(features, 28);
    }
  }
  if (node === 14) return 0.005017061108998331;
  if (node === 6) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree151(features, 13);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree151(features, 13);
    } else {
      return calculateTree151(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree151(features, 5);
    } else if (val <= 507.50000000000006) {
      return calculateTree151(features, 5);
    } else {
      return calculateTree151(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree151(features, 1);
    } else if (val <= 481.50000000000006) {
      return calculateTree151(features, 1);
    } else {
      return calculateTree151(features, 2);
    }
  }
}
function calculateTree152(features, node = 0) {
  if (node === 3) return -0.0002972406926819737;
  if (node === 39) return -0.2563604908244944;
  if (node === 40) return 0.07103882612832106;
  if (node === 19) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree152(features, 39);
    } else if (val <= 37.50000000000001) {
      return calculateTree152(features, 39);
    } else {
      return calculateTree152(features, 40);
    }
  }
  if (node === 20) return -0.3345629418621184;
  if (node === 9) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree152(features, 19);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree152(features, 19);
    } else {
      return calculateTree152(features, 20);
    }
  }
  if (node === 21) return -0.05123208266331574;
  if (node === 22) return 0.5422979053079916;
  if (node === 10) {
    const val = features['is_preference_home'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree152(features, 21);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree152(features, 21);
    } else {
      return calculateTree152(features, 22);
    }
  }
  if (node === 4) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree152(features, 9);
    } else if (val <= 1.5000000000000002) {
      return calculateTree152(features, 9);
    } else {
      return calculateTree152(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree152(features, 3);
    } else if (val <= 975.5000000000001) {
      return calculateTree152(features, 3);
    } else {
      return calculateTree152(features, 4);
    }
  }
  if (node === 95) return 0.040040847032279996;
  if (node === 1551) return -0.0854828769469085;
  if (node === 1552) return -0.3069240335343831;
  if (node === 775) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree152(features, 1551);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree152(features, 1551);
    } else {
      return calculateTree152(features, 1552);
    }
  }
  if (node === 776) return 0.017822849855958;
  if (node === 387) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree152(features, 775);
    } else if (val <= 34.50000000000001) {
      return calculateTree152(features, 775);
    } else {
      return calculateTree152(features, 776);
    }
  }
  if (node === 388) return -0.2916641229492882;
  if (node === 193) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree152(features, 387);
    } else if (val <= 986.5000000000001) {
      return calculateTree152(features, 387);
    } else {
      return calculateTree152(features, 388);
    }
  }
  if (node === 194) return 0.010983520555409562;
  if (node === 96) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree152(features, 193);
    } else if (val <= 993.5000000000001) {
      return calculateTree152(features, 193);
    } else {
      return calculateTree152(features, 194);
    }
  }
  if (node === 47) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree152(features, 95);
    } else if (val <= 983.5000000000001) {
      return calculateTree152(features, 95);
    } else {
      return calculateTree152(features, 96);
    }
  }
  if (node === 97) return -0.24581475612007916;
  if (node === 395) return 0.05403428055783783;
  if (node === 396) return -0.2341612790431578;
  if (node === 197) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree152(features, 395);
    } else if (val <= 27.500000000000004) {
      return calculateTree152(features, 395);
    } else {
      return calculateTree152(features, 396);
    }
  }
  if (node === 795) return 0.15186982610754318;
  if (node === 1593) return 0.008184879503111323;
  if (node === 1594) return -0.36695589501584763;
  if (node === 796) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree152(features, 1593);
    } else if (val <= 162.50000000000003) {
      return calculateTree152(features, 1593);
    } else {
      return calculateTree152(features, 1594);
    }
  }
  if (node === 397) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree152(features, 795);
    } else if (val <= 989.5000000000001) {
      return calculateTree152(features, 795);
    } else {
      return calculateTree152(features, 796);
    }
  }
  if (node === 797) return 0.46201617604037504;
  if (node === 1597) return 0.038693544385834014;
  if (node === 1598) return 0.27950473265616294;
  if (node === 798) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree152(features, 1597);
    } else if (val <= 989.5000000000001) {
      return calculateTree152(features, 1597);
    } else {
      return calculateTree152(features, 1598);
    }
  }
  if (node === 398) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree152(features, 797);
    } else if (val <= 27.500000000000004) {
      return calculateTree152(features, 797);
    } else {
      return calculateTree152(features, 798);
    }
  }
  if (node === 198) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree152(features, 397);
    } else if (val <= 25.500000000000004) {
      return calculateTree152(features, 397);
    } else {
      return calculateTree152(features, 398);
    }
  }
  if (node === 98) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree152(features, 197);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree152(features, 197);
    } else {
      return calculateTree152(features, 198);
    }
  }
  if (node === 48) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree152(features, 97);
    } else if (val <= 983.5000000000001) {
      return calculateTree152(features, 97);
    } else {
      return calculateTree152(features, 98);
    }
  }
  if (node === 23) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree152(features, 47);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree152(features, 47);
    } else {
      return calculateTree152(features, 48);
    }
  }
  if (node === 49) return -0.24544883851702454;
  if (node === 50) return 0.06556161545972115;
  if (node === 24) {
    const val = features['is_preference_kids'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree152(features, 49);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree152(features, 49);
    } else {
      return calculateTree152(features, 50);
    }
  }
  if (node === 11) {
    const val = features['is_preference_women_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree152(features, 23);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree152(features, 23);
    } else {
      return calculateTree152(features, 24);
    }
  }
  if (node === 207) return -0.27460175237281603;
  if (node === 208) return 0.12928548584101293;
  if (node === 103) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree152(features, 207);
    } else if (val <= 697.0000000000001) {
      return calculateTree152(features, 207);
    } else {
      return calculateTree152(features, 208);
    }
  }
  if (node === 209) return -0.0038924066685072007;
  if (node === 421) return 0.09151196941370913;
  if (node === 422) return 0.4579640366482541;
  if (node === 210) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree152(features, 421);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree152(features, 421);
    } else {
      return calculateTree152(features, 422);
    }
  }
  if (node === 104) {
    const val = features['is_preference_personal_care'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree152(features, 209);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree152(features, 209);
    } else {
      return calculateTree152(features, 210);
    }
  }
  if (node === 51) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree152(features, 103);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree152(features, 103);
    } else {
      return calculateTree152(features, 104);
    }
  }
  if (node === 52) return -0.15031401035640635;
  if (node === 25) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree152(features, 51);
    } else if (val <= 46.50000000000001) {
      return calculateTree152(features, 51);
    } else {
      return calculateTree152(features, 52);
    }
  }
  if (node === 53) return -0.08624610065950448;
  if (node === 54) return 0.2673508887470775;
  if (node === 26) {
    const val = features['is_preference_kitchen'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree152(features, 53);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree152(features, 53);
    } else {
      return calculateTree152(features, 54);
    }
  }
  if (node === 12) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree152(features, 25);
    } else if (val <= 1.5000000000000002) {
      return calculateTree152(features, 25);
    } else {
      return calculateTree152(features, 26);
    }
  }
  if (node === 5) {
    const val = features['is_preference_home'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree152(features, 11);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree152(features, 11);
    } else {
      return calculateTree152(features, 12);
    }
  }
  if (node === 6) return -0.015065490836665217;
  if (node === 2) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree152(features, 5);
    } else if (val <= 3.5000000000000004) {
      return calculateTree152(features, 5);
    } else {
      return calculateTree152(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree152(features, 1);
    } else if (val <= 980.5000000000001) {
      return calculateTree152(features, 1);
    } else {
      return calculateTree152(features, 2);
    }
  }
}
function calculateTree153(features, node = 0) {
  if (node === 15) return -0.003838724618036575;
  if (node === 67) return -0.022349170254604128;
  if (node === 137) return -0.10184815991745817;
  if (node === 138) return -0.46580042406683275;
  if (node === 68) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree153(features, 137);
    } else if (val <= 148.50000000000003) {
      return calculateTree153(features, 137);
    } else {
      return calculateTree153(features, 138);
    }
  }
  if (node === 33) {
    const val = features['is_preference_sports'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree153(features, 67);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree153(features, 67);
    } else {
      return calculateTree153(features, 68);
    }
  }
  if (node === 139) return 0.02414428188123159;
  if (node === 281) return 0.04390554007902619;
  if (node === 282) return 0.3046958982730513;
  if (node === 140) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree153(features, 281);
    } else if (val <= 1.5000000000000002) {
      return calculateTree153(features, 281);
    } else {
      return calculateTree153(features, 282);
    }
  }
  if (node === 69) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree153(features, 139);
    } else if (val <= 222.50000000000003) {
      return calculateTree153(features, 139);
    } else {
      return calculateTree153(features, 140);
    }
  }
  if (node === 70) return -0.0040578864052828074;
  if (node === 34) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree153(features, 69);
    } else if (val <= 34.50000000000001) {
      return calculateTree153(features, 69);
    } else {
      return calculateTree153(features, 70);
    }
  }
  if (node === 16) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree153(features, 33);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree153(features, 33);
    } else {
      return calculateTree153(features, 34);
    }
  }
  if (node === 7) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree153(features, 15);
    } else if (val <= 660.0000000000001) {
      return calculateTree153(features, 15);
    } else {
      return calculateTree153(features, 16);
    }
  }
  if (node === 17) return -0.03579158481561448;
  if (node === 75) return 0.20498216833495586;
  if (node === 153) return -0.06416062484435524;
  if (node === 154) return 0.2670365996810562;
  if (node === 76) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree153(features, 153);
    } else if (val <= 16.500000000000004) {
      return calculateTree153(features, 153);
    } else {
      return calculateTree153(features, 154);
    }
  }
  if (node === 37) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree153(features, 75);
    } else if (val <= 5.500000000000001) {
      return calculateTree153(features, 75);
    } else {
      return calculateTree153(features, 76);
    }
  }
  if (node === 38) return 0.30495424668018967;
  if (node === 18) {
    const val = features['is_preference_personal_care'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree153(features, 37);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree153(features, 37);
    } else {
      return calculateTree153(features, 38);
    }
  }
  if (node === 8) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree153(features, 17);
    } else if (val <= 32.50000000000001) {
      return calculateTree153(features, 17);
    } else {
      return calculateTree153(features, 18);
    }
  }
  if (node === 3) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree153(features, 7);
    } else if (val <= 236.00000000000003) {
      return calculateTree153(features, 7);
    } else {
      return calculateTree153(features, 8);
    }
  }
  if (node === 79) return 0.46686544807345587;
  if (node === 161) return 0.05673651074461061;
  if (node === 325) return 0.3251690616798199;
  if (node === 326) return 0.10796804785944993;
  if (node === 162) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree153(features, 325);
    } else if (val <= 33.50000000000001) {
      return calculateTree153(features, 325);
    } else {
      return calculateTree153(features, 326);
    }
  }
  if (node === 80) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree153(features, 161);
    } else if (val <= 14.500000000000002) {
      return calculateTree153(features, 161);
    } else {
      return calculateTree153(features, 162);
    }
  }
  if (node === 39) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree153(features, 79);
    } else if (val <= 1.5000000000000002) {
      return calculateTree153(features, 79);
    } else {
      return calculateTree153(features, 80);
    }
  }
  if (node === 40) return -0.027381339475623472;
  if (node === 19) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree153(features, 39);
    } else if (val <= 3.5000000000000004) {
      return calculateTree153(features, 39);
    } else {
      return calculateTree153(features, 40);
    }
  }
  if (node === 83) return -0.054058020743109496;
  if (node === 84) return -0.3009808271636279;
  if (node === 41) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree153(features, 83);
    } else if (val <= 295.00000000000006) {
      return calculateTree153(features, 83);
    } else {
      return calculateTree153(features, 84);
    }
  }
  if (node === 85) return 0.357100172238332;
  if (node === 173) return -0.08792366669884041;
  if (node === 174) return 0.1565235292616193;
  if (node === 86) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree153(features, 173);
    } else if (val <= 15.500000000000002) {
      return calculateTree153(features, 173);
    } else {
      return calculateTree153(features, 174);
    }
  }
  if (node === 42) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree153(features, 85);
    } else if (val <= 804.0000000000001) {
      return calculateTree153(features, 85);
    } else {
      return calculateTree153(features, 86);
    }
  }
  if (node === 20) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree153(features, 41);
    } else if (val <= 626.0000000000001) {
      return calculateTree153(features, 41);
    } else {
      return calculateTree153(features, 42);
    }
  }
  if (node === 9) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree153(features, 19);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree153(features, 19);
    } else {
      return calculateTree153(features, 20);
    }
  }
  if (node === 21) return -0.3676732860434161;
  if (node === 45) return -0.31849753054359026;
  if (node === 93) return -0.019173452612931574;
  if (node === 94) return 0.21305450614438431;
  if (node === 46) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree153(features, 93);
    } else if (val <= 2.5000000000000004) {
      return calculateTree153(features, 93);
    } else {
      return calculateTree153(features, 94);
    }
  }
  if (node === 22) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree153(features, 45);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree153(features, 45);
    } else {
      return calculateTree153(features, 46);
    }
  }
  if (node === 10) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree153(features, 21);
    } else if (val <= 253.00000000000003) {
      return calculateTree153(features, 21);
    } else {
      return calculateTree153(features, 22);
    }
  }
  if (node === 4) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree153(features, 9);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree153(features, 9);
    } else {
      return calculateTree153(features, 10);
    }
  }
  if (node === 1) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree153(features, 3);
    } else if (val <= 240.00000000000003) {
      return calculateTree153(features, 3);
    } else {
      return calculateTree153(features, 4);
    }
  }
  if (node === 11) return -0.3622651396003674;
  if (node === 51) return 0.098426984375477;
  if (node === 52) return 0.5193153460471223;
  if (node === 25) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree153(features, 51);
    } else if (val <= 1.5000000000000002) {
      return calculateTree153(features, 51);
    } else {
      return calculateTree153(features, 52);
    }
  }
  if (node === 26) return 0.051391992934071554;
  if (node === 12) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree153(features, 25);
    } else if (val <= 22.500000000000004) {
      return calculateTree153(features, 25);
    } else {
      return calculateTree153(features, 26);
    }
  }
  if (node === 5) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree153(features, 11);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree153(features, 11);
    } else {
      return calculateTree153(features, 12);
    }
  }
  if (node === 6) return 0.0005500576037881793;
  if (node === 2) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree153(features, 5);
    } else if (val <= 270.00000000000006) {
      return calculateTree153(features, 5);
    } else {
      return calculateTree153(features, 6);
    }
  }
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree153(features, 1);
    } else if (val <= 265.00000000000006) {
      return calculateTree153(features, 1);
    } else {
      return calculateTree153(features, 2);
    }
  }
}
function calculateTree154(features, node = 0) {
  if (node === 1) return 0.0027491788266808463;
  if (node === 383) return 0.08293193810039007;
  if (node === 769) return -0.3620199664629647;
  if (node === 770) return 0.016353527509910013;
  if (node === 384) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree154(features, 769);
    } else if (val <= 24.500000000000004) {
      return calculateTree154(features, 769);
    } else {
      return calculateTree154(features, 770);
    }
  }
  if (node === 191) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree154(features, 383);
    } else if (val <= 15.500000000000002) {
      return calculateTree154(features, 383);
    } else {
      return calculateTree154(features, 384);
    }
  }
  if (node === 385) return 0.26278993707162207;
  if (node === 386) return 0.0762803630443576;
  if (node === 192) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree154(features, 385);
    } else if (val <= 34.50000000000001) {
      return calculateTree154(features, 385);
    } else {
      return calculateTree154(features, 386);
    }
  }
  if (node === 95) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree154(features, 191);
    } else if (val <= 107.00000000000001) {
      return calculateTree154(features, 191);
    } else {
      return calculateTree154(features, 192);
    }
  }
  if (node === 193) return 0.0716867205455748;
  if (node === 194) return -0.3405679464518166;
  if (node === 96) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree154(features, 193);
    } else if (val <= 44.50000000000001) {
      return calculateTree154(features, 193);
    } else {
      return calculateTree154(features, 194);
    }
  }
  if (node === 47) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree154(features, 95);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree154(features, 95);
    } else {
      return calculateTree154(features, 96);
    }
  }
  if (node === 97) return -0.3187778778939911;
  if (node === 197) return -0.22232068808976985;
  if (node === 397) return 0.014489456901922573;
  if (node === 398) return 0.19836576767928413;
  if (node === 198) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree154(features, 397);
    } else if (val <= 40.50000000000001) {
      return calculateTree154(features, 397);
    } else {
      return calculateTree154(features, 398);
    }
  }
  if (node === 98) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree154(features, 197);
    } else if (val <= 447.00000000000006) {
      return calculateTree154(features, 197);
    } else {
      return calculateTree154(features, 198);
    }
  }
  if (node === 48) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree154(features, 97);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree154(features, 97);
    } else {
      return calculateTree154(features, 98);
    }
  }
  if (node === 23) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree154(features, 47);
    } else if (val <= 171.00000000000003) {
      return calculateTree154(features, 47);
    } else {
      return calculateTree154(features, 48);
    }
  }
  if (node === 49) return -0.3427445063140651;
  if (node === 203) return 0.07655754916673606;
  if (node === 819) return 0.2094662073164256;
  if (node === 820) return -0.3793833335244146;
  if (node === 409) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree154(features, 819);
    } else if (val <= 34.50000000000001) {
      return calculateTree154(features, 819);
    } else {
      return calculateTree154(features, 820);
    }
  }
  if (node === 410) return -0.34115061731909935;
  if (node === 204) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree154(features, 409);
    } else if (val <= 712.5000000000001) {
      return calculateTree154(features, 409);
    } else {
      return calculateTree154(features, 410);
    }
  }
  if (node === 101) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree154(features, 203);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree154(features, 203);
    } else {
      return calculateTree154(features, 204);
    }
  }
  if (node === 205) return -0.2602308976846992;
  if (node === 206) return 0.32127619273956864;
  if (node === 102) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree154(features, 205);
    } else if (val <= 29.500000000000004) {
      return calculateTree154(features, 205);
    } else {
      return calculateTree154(features, 206);
    }
  }
  if (node === 50) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree154(features, 101);
    } else if (val <= 285.00000000000006) {
      return calculateTree154(features, 101);
    } else {
      return calculateTree154(features, 102);
    }
  }
  if (node === 24) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree154(features, 49);
    } else if (val <= 677.5000000000001) {
      return calculateTree154(features, 49);
    } else {
      return calculateTree154(features, 50);
    }
  }
  if (node === 11) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree154(features, 23);
    } else if (val <= 556.0000000000001) {
      return calculateTree154(features, 23);
    } else {
      return calculateTree154(features, 24);
    }
  }
  if (node === 51) return -0.340034284520699;
  if (node === 105) return 0.36619217687704225;
  if (node === 106) return 0.014023983333409562;
  if (node === 52) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree154(features, 105);
    } else if (val <= 1.5000000000000002) {
      return calculateTree154(features, 105);
    } else {
      return calculateTree154(features, 106);
    }
  }
  if (node === 25) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree154(features, 51);
    } else if (val <= 877.5000000000001) {
      return calculateTree154(features, 51);
    } else {
      return calculateTree154(features, 52);
    }
  }
  if (node === 53) return 0.05489699409092365;
  if (node === 54) return -0.32939591263491025;
  if (node === 26) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree154(features, 53);
    } else if (val <= 877.5000000000001) {
      return calculateTree154(features, 53);
    } else {
      return calculateTree154(features, 54);
    }
  }
  if (node === 12) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree154(features, 25);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree154(features, 25);
    } else {
      return calculateTree154(features, 26);
    }
  }
  if (node === 5) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree154(features, 11);
    } else if (val <= 835.5000000000001) {
      return calculateTree154(features, 11);
    } else {
      return calculateTree154(features, 12);
    }
  }
  if (node === 55) return -0.3732851963488179;
  if (node === 113) return 0.2621050854339503;
  if (node === 229) return -0.059367710191552514;
  if (node === 230) return 0.16843962005734728;
  if (node === 114) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree154(features, 229);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree154(features, 229);
    } else {
      return calculateTree154(features, 230);
    }
  }
  if (node === 56) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree154(features, 113);
    } else if (val <= 24.500000000000004) {
      return calculateTree154(features, 113);
    } else {
      return calculateTree154(features, 114);
    }
  }
  if (node === 27) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree154(features, 55);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree154(features, 55);
    } else {
      return calculateTree154(features, 56);
    }
  }
  if (node === 115) return 0.19958197733922822;
  if (node === 116) return -0.11724794794742925;
  if (node === 57) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree154(features, 115);
    } else if (val <= 3.5000000000000004) {
      return calculateTree154(features, 115);
    } else {
      return calculateTree154(features, 116);
    }
  }
  if (node === 58) return -0.3575295990753104;
  if (node === 28) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree154(features, 57);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree154(features, 57);
    } else {
      return calculateTree154(features, 58);
    }
  }
  if (node === 13) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree154(features, 27);
    } else if (val <= 778.5000000000001) {
      return calculateTree154(features, 27);
    } else {
      return calculateTree154(features, 28);
    }
  }
  if (node === 14) return -0.0007347791515559515;
  if (node === 6) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree154(features, 13);
    } else if (val <= 294.50000000000006) {
      return calculateTree154(features, 13);
    } else {
      return calculateTree154(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree154(features, 5);
    } else if (val <= 291.50000000000006) {
      return calculateTree154(features, 5);
    } else {
      return calculateTree154(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree154(features, 1);
    } else if (val <= 278.50000000000006) {
      return calculateTree154(features, 1);
    } else {
      return calculateTree154(features, 2);
    }
  }
}
function calculateTree155(features, node = 0) {
  if (node === 3) return -0.2785099268287592;
  if (node === 4) return 0.005588922497273863;
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree155(features, 3);
    } else if (val <= 51.50000000000001) {
      return calculateTree155(features, 3);
    } else {
      return calculateTree155(features, 4);
    }
  }
  if (node === 191) return -0.0027678301353493773;
  if (node === 771) return 0.15497082698479261;
  if (node === 772) return -0.21671040035058253;
  if (node === 385) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree155(features, 771);
    } else if (val <= 880.0000000000001) {
      return calculateTree155(features, 771);
    } else {
      return calculateTree155(features, 772);
    }
  }
  if (node === 386) return 0.39901343085051205;
  if (node === 192) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree155(features, 385);
    } else if (val <= 1.5000000000000002) {
      return calculateTree155(features, 385);
    } else {
      return calculateTree155(features, 386);
    }
  }
  if (node === 95) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree155(features, 191);
    } else if (val <= 7.500000000000001) {
      return calculateTree155(features, 191);
    } else {
      return calculateTree155(features, 192);
    }
  }
  if (node === 387) return -0.3077034664410569;
  if (node === 1555) return -0.1211734651789594;
  if (node === 1556) return 0.32887679401746045;
  if (node === 777) {
    const val = features['is_preference_personal_care'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree155(features, 1555);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree155(features, 1555);
    } else {
      return calculateTree155(features, 1556);
    }
  }
  if (node === 778) return -0.2755276061516517;
  if (node === 388) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree155(features, 777);
    } else if (val <= 28.500000000000004) {
      return calculateTree155(features, 777);
    } else {
      return calculateTree155(features, 778);
    }
  }
  if (node === 193) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree155(features, 387);
    } else if (val <= 117.50000000000001) {
      return calculateTree155(features, 387);
    } else {
      return calculateTree155(features, 388);
    }
  }
  if (node === 1559) return 0.02795784347342061;
  if (node === 1560) return -0.10551500426649171;
  if (node === 779) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree155(features, 1559);
    } else if (val <= 32.50000000000001) {
      return calculateTree155(features, 1559);
    } else {
      return calculateTree155(features, 1560);
    }
  }
  if (node === 1561) return 0.034673576336533306;
  if (node === 1562) return 0.3593029639222654;
  if (node === 780) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree155(features, 1561);
    } else if (val <= 42.50000000000001) {
      return calculateTree155(features, 1561);
    } else {
      return calculateTree155(features, 1562);
    }
  }
  if (node === 389) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree155(features, 779);
    } else if (val <= 11.500000000000002) {
      return calculateTree155(features, 779);
    } else {
      return calculateTree155(features, 780);
    }
  }
  if (node === 781) return -0.2759967000697685;
  if (node === 1565) return -0.020934735860945235;
  if (node === 1566) return -0.20371777958107468;
  if (node === 782) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree155(features, 1565);
    } else if (val <= 52.50000000000001) {
      return calculateTree155(features, 1565);
    } else {
      return calculateTree155(features, 1566);
    }
  }
  if (node === 390) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree155(features, 781);
    } else if (val <= 117.50000000000001) {
      return calculateTree155(features, 781);
    } else {
      return calculateTree155(features, 782);
    }
  }
  if (node === 194) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree155(features, 389);
    } else if (val <= 539.5000000000001) {
      return calculateTree155(features, 389);
    } else {
      return calculateTree155(features, 390);
    }
  }
  if (node === 96) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree155(features, 193);
    } else if (val <= 365.50000000000006) {
      return calculateTree155(features, 193);
    } else {
      return calculateTree155(features, 194);
    }
  }
  if (node === 47) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree155(features, 95);
    } else if (val <= 150.50000000000003) {
      return calculateTree155(features, 95);
    } else {
      return calculateTree155(features, 96);
    }
  }
  if (node === 48) return 0.10824700907868773;
  if (node === 23) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree155(features, 47);
    } else if (val <= 54.50000000000001) {
      return calculateTree155(features, 47);
    } else {
      return calculateTree155(features, 48);
    }
  }
  if (node === 49) return 0.09315613997230586;
  if (node === 50) return -0.30665750768079075;
  if (node === 24) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree155(features, 49);
    } else if (val <= 1.5000000000000002) {
      return calculateTree155(features, 49);
    } else {
      return calculateTree155(features, 50);
    }
  }
  if (node === 11) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree155(features, 23);
    } else if (val <= 57.50000000000001) {
      return calculateTree155(features, 23);
    } else {
      return calculateTree155(features, 24);
    }
  }
  if (node === 25) return 0.24197525257383456;
  if (node === 26) return -0.12663421533396527;
  if (node === 12) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree155(features, 25);
    } else if (val <= 67.50000000000001) {
      return calculateTree155(features, 25);
    } else {
      return calculateTree155(features, 26);
    }
  }
  if (node === 5) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree155(features, 11);
    } else if (val <= 60.50000000000001) {
      return calculateTree155(features, 11);
    } else {
      return calculateTree155(features, 12);
    }
  }
  if (node === 13) return 0.01286139974370054;
  if (node === 29) return -0.016230151890259285;
  if (node === 247) return -0.23649164137464973;
  if (node === 497) return 0.17071374488478516;
  if (node === 498) return -0.1718017795067559;
  if (node === 248) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree155(features, 497);
    } else if (val <= 804.0000000000001) {
      return calculateTree155(features, 497);
    } else {
      return calculateTree155(features, 498);
    }
  }
  if (node === 123) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree155(features, 247);
    } else if (val <= 348.50000000000006) {
      return calculateTree155(features, 247);
    } else {
      return calculateTree155(features, 248);
    }
  }
  if (node === 124) return -0.32995527012760323;
  if (node === 61) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree155(features, 123);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree155(features, 123);
    } else {
      return calculateTree155(features, 124);
    }
  }
  if (node === 251) return -0.1576957452865819;
  if (node === 505) return 0.05177588508879321;
  if (node === 506) return -0.24993088273873285;
  if (node === 252) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree155(features, 505);
    } else if (val <= 893.0000000000001) {
      return calculateTree155(features, 505);
    } else {
      return calculateTree155(features, 506);
    }
  }
  if (node === 125) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree155(features, 251);
    } else if (val <= 648.5000000000001) {
      return calculateTree155(features, 251);
    } else {
      return calculateTree155(features, 252);
    }
  }
  if (node === 126) return -0.00021890376900971374;
  if (node === 62) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree155(features, 125);
    } else if (val <= 83.50000000000001) {
      return calculateTree155(features, 125);
    } else {
      return calculateTree155(features, 126);
    }
  }
  if (node === 30) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree155(features, 61);
    } else if (val <= 70.00000000000001) {
      return calculateTree155(features, 61);
    } else {
      return calculateTree155(features, 62);
    }
  }
  if (node === 14) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree155(features, 29);
    } else if (val <= 66.00000000000001) {
      return calculateTree155(features, 29);
    } else {
      return calculateTree155(features, 30);
    }
  }
  if (node === 6) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree155(features, 13);
    } else if (val <= 18.500000000000004) {
      return calculateTree155(features, 13);
    } else {
      return calculateTree155(features, 14);
    }
  }
  if (node === 2) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree155(features, 5);
    } else if (val <= 130.00000000000003) {
      return calculateTree155(features, 5);
    } else {
      return calculateTree155(features, 6);
    }
  }
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree155(features, 1);
    } else if (val <= 113.50000000000001) {
      return calculateTree155(features, 1);
    } else {
      return calculateTree155(features, 2);
    }
  }
}
function calculateTree156(features, node = 0) {
  if (node === 7) return 0.0006035993741665108;
  if (node === 71) return -0.3207696485440694;
  if (node === 72) return 0.007935514035042092;
  if (node === 35) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree156(features, 71);
    } else if (val <= 28.500000000000004) {
      return calculateTree156(features, 71);
    } else {
      return calculateTree156(features, 72);
    }
  }
  if (node === 295) return -0.19999583595502154;
  if (node === 296) return -0.003014114031297196;
  if (node === 147) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree156(features, 295);
    } else if (val <= 241.50000000000003) {
      return calculateTree156(features, 295);
    } else {
      return calculateTree156(features, 296);
    }
  }
  if (node === 148) return -0.0029080708343931084;
  if (node === 73) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree156(features, 147);
    } else if (val <= 900.5000000000001) {
      return calculateTree156(features, 147);
    } else {
      return calculateTree156(features, 148);
    }
  }
  if (node === 149) return -0.2836902700935277;
  if (node === 150) return 0.171971270124684;
  if (node === 74) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree156(features, 149);
    } else if (val <= 973.5000000000001) {
      return calculateTree156(features, 149);
    } else {
      return calculateTree156(features, 150);
    }
  }
  if (node === 36) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree156(features, 73);
    } else if (val <= 702.5000000000001) {
      return calculateTree156(features, 73);
    } else {
      return calculateTree156(features, 74);
    }
  }
  if (node === 17) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree156(features, 35);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree156(features, 35);
    } else {
      return calculateTree156(features, 36);
    }
  }
  if (node === 18) return -0.33638113334418845;
  if (node === 8) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree156(features, 17);
    } else if (val <= 804.5000000000001) {
      return calculateTree156(features, 17);
    } else {
      return calculateTree156(features, 18);
    }
  }
  if (node === 3) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree156(features, 7);
    } else if (val <= 893.0000000000001) {
      return calculateTree156(features, 7);
    } else {
      return calculateTree156(features, 8);
    }
  }
  if (node === 19) return -0.3294233476943002;
  if (node === 83) return 0.3971650014941745;
  if (node === 84) return 0.10385323635721151;
  if (node === 41) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree156(features, 83);
    } else if (val <= 898.5000000000001) {
      return calculateTree156(features, 83);
    } else {
      return calculateTree156(features, 84);
    }
  }
  if (node === 42) return -0.20439269573448096;
  if (node === 20) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree156(features, 41);
    } else if (val <= 34.50000000000001) {
      return calculateTree156(features, 41);
    } else {
      return calculateTree156(features, 42);
    }
  }
  if (node === 9) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree156(features, 19);
    } else if (val <= 859.5000000000001) {
      return calculateTree156(features, 19);
    } else {
      return calculateTree156(features, 20);
    }
  }
  if (node === 703) return 0.0006762580492559751;
  if (node === 704) return -0.16407608130267395;
  if (node === 351) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree156(features, 703);
    } else if (val <= 2.5000000000000004) {
      return calculateTree156(features, 703);
    } else {
      return calculateTree156(features, 704);
    }
  }
  if (node === 1411) return 0.36649078264824214;
  if (node === 1412) return 0.0758367015992843;
  if (node === 705) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree156(features, 1411);
    } else if (val <= 284.50000000000006) {
      return calculateTree156(features, 1411);
    } else {
      return calculateTree156(features, 1412);
    }
  }
  if (node === 1413) return 0.080666834574261;
  if (node === 1414) return -0.13823156304788833;
  if (node === 706) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree156(features, 1413);
    } else if (val <= 3.5000000000000004) {
      return calculateTree156(features, 1413);
    } else {
      return calculateTree156(features, 1414);
    }
  }
  if (node === 352) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree156(features, 705);
    } else if (val <= 33.50000000000001) {
      return calculateTree156(features, 705);
    } else {
      return calculateTree156(features, 706);
    }
  }
  if (node === 175) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree156(features, 351);
    } else if (val <= 21.500000000000004) {
      return calculateTree156(features, 351);
    } else {
      return calculateTree156(features, 352);
    }
  }
  if (node === 353) return 0.38424621847420615;
  if (node === 709) return -0.051734079091265886;
  if (node === 710) return 0.20643502606630723;
  if (node === 354) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree156(features, 709);
    } else if (val <= 8.500000000000002) {
      return calculateTree156(features, 709);
    } else {
      return calculateTree156(features, 710);
    }
  }
  if (node === 176) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree156(features, 353);
    } else if (val <= 23.500000000000004) {
      return calculateTree156(features, 353);
    } else {
      return calculateTree156(features, 354);
    }
  }
  if (node === 87) {
    const val = features['is_preference_pets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree156(features, 175);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree156(features, 175);
    } else {
      return calculateTree156(features, 176);
    }
  }
  if (node === 355) return 0.11521146672950122;
  if (node === 356) return -0.1339526281619803;
  if (node === 177) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree156(features, 355);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree156(features, 355);
    } else {
      return calculateTree156(features, 356);
    }
  }
  if (node === 178) return 0.21191062008598635;
  if (node === 88) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree156(features, 177);
    } else if (val <= 2.5000000000000004) {
      return calculateTree156(features, 177);
    } else {
      return calculateTree156(features, 178);
    }
  }
  if (node === 43) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree156(features, 87);
    } else if (val <= 799.5000000000001) {
      return calculateTree156(features, 87);
    } else {
      return calculateTree156(features, 88);
    }
  }
  if (node === 89) return -0.3999484924957623;
  if (node === 363) return 0.05329719120528275;
  if (node === 364) return -0.16438115989110527;
  if (node === 181) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree156(features, 363);
    } else if (val <= 34.50000000000001) {
      return calculateTree156(features, 363);
    } else {
      return calculateTree156(features, 364);
    }
  }
  if (node === 182) return 0.25188448840191197;
  if (node === 90) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree156(features, 181);
    } else if (val <= 50.50000000000001) {
      return calculateTree156(features, 181);
    } else {
      return calculateTree156(features, 182);
    }
  }
  if (node === 44) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree156(features, 89);
    } else if (val <= 898.5000000000001) {
      return calculateTree156(features, 89);
    } else {
      return calculateTree156(features, 90);
    }
  }
  if (node === 21) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree156(features, 43);
    } else if (val <= 859.5000000000001) {
      return calculateTree156(features, 43);
    } else {
      return calculateTree156(features, 44);
    }
  }
  if (node === 22) return -0.32595391011976305;
  if (node === 10) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree156(features, 21);
    } else if (val <= 969.5000000000001) {
      return calculateTree156(features, 21);
    } else {
      return calculateTree156(features, 22);
    }
  }
  if (node === 4) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree156(features, 9);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree156(features, 9);
    } else {
      return calculateTree156(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree156(features, 3);
    } else if (val <= 834.5000000000001) {
      return calculateTree156(features, 3);
    } else {
      return calculateTree156(features, 4);
    }
  }
  if (node === 5) return -0.3313536484286983;
  if (node === 6) return -0.0032261089359686065;
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree156(features, 5);
    } else if (val <= 845.5000000000001) {
      return calculateTree156(features, 5);
    } else {
      return calculateTree156(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree156(features, 1);
    } else if (val <= 844.5000000000001) {
      return calculateTree156(features, 1);
    } else {
      return calculateTree156(features, 2);
    }
  }
}
function calculateTree157(features, node = 0) {
  if (node === 7) return -0.0001692872284282093;
  if (node === 143) return 0.3815177784552952;
  if (node === 144) return -0.11897530837952051;
  if (node === 71) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree157(features, 143);
    } else if (val <= 27.500000000000004) {
      return calculateTree157(features, 143);
    } else {
      return calculateTree157(features, 144);
    }
  }
  if (node === 145) return -0.27735846976017275;
  if (node === 293) return 0.1185229786406549;
  if (node === 589) return -0.38374596539125144;
  if (node === 590) return 0.010007762829970323;
  if (node === 294) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree157(features, 589);
    } else if (val <= 316.50000000000006) {
      return calculateTree157(features, 589);
    } else {
      return calculateTree157(features, 590);
    }
  }
  if (node === 146) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree157(features, 293);
    } else if (val <= 305.00000000000006) {
      return calculateTree157(features, 293);
    } else {
      return calculateTree157(features, 294);
    }
  }
  if (node === 72) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree157(features, 145);
    } else if (val <= 139.50000000000003) {
      return calculateTree157(features, 145);
    } else {
      return calculateTree157(features, 146);
    }
  }
  if (node === 35) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree157(features, 71);
    } else if (val <= 69.50000000000001) {
      return calculateTree157(features, 71);
    } else {
      return calculateTree157(features, 72);
    }
  }
  if (node === 36) return -0.29475869302792446;
  if (node === 17) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree157(features, 35);
    } else if (val <= 685.0000000000001) {
      return calculateTree157(features, 35);
    } else {
      return calculateTree157(features, 36);
    }
  }
  if (node === 75) return -0.3834602075599481;
  if (node === 153) return 0.05159477898419528;
  if (node === 154) return -0.14565460510579167;
  if (node === 76) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree157(features, 153);
    } else if (val <= 10.500000000000002) {
      return calculateTree157(features, 153);
    } else {
      return calculateTree157(features, 154);
    }
  }
  if (node === 37) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree157(features, 75);
    } else if (val <= 69.50000000000001) {
      return calculateTree157(features, 75);
    } else {
      return calculateTree157(features, 76);
    }
  }
  if (node === 38) return -0.2533405520690988;
  if (node === 18) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree157(features, 37);
    } else if (val <= 236.00000000000003) {
      return calculateTree157(features, 37);
    } else {
      return calculateTree157(features, 38);
    }
  }
  if (node === 8) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree157(features, 17);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree157(features, 17);
    } else {
      return calculateTree157(features, 18);
    }
  }
  if (node === 3) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree157(features, 7);
    } else if (val <= 662.5000000000001) {
      return calculateTree157(features, 7);
    } else {
      return calculateTree157(features, 8);
    }
  }
  if (node === 39) return -0.2249698079994438;
  if (node === 81) return 0.40480340811277576;
  if (node === 82) return -0.3296820472419959;
  if (node === 40) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree157(features, 81);
    } else if (val <= 34.50000000000001) {
      return calculateTree157(features, 81);
    } else {
      return calculateTree157(features, 82);
    }
  }
  if (node === 19) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree157(features, 39);
    } else if (val <= 2.5000000000000004) {
      return calculateTree157(features, 39);
    } else {
      return calculateTree157(features, 40);
    }
  }
  if (node === 20) return -0.03169215495716953;
  if (node === 9) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree157(features, 19);
    } else if (val <= 731.5000000000001) {
      return calculateTree157(features, 19);
    } else {
      return calculateTree157(features, 20);
    }
  }
  if (node === 87) return 0.1908846508142959;
  if (node === 88) return -0.2819577078816605;
  if (node === 43) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree157(features, 87);
    } else if (val <= 1.5000000000000002) {
      return calculateTree157(features, 87);
    } else {
      return calculateTree157(features, 88);
    }
  }
  if (node === 179) return 0.1376239028479964;
  if (node === 180) return -0.1668426855687947;
  if (node === 89) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree157(features, 179);
    } else if (val <= 52.50000000000001) {
      return calculateTree157(features, 179);
    } else {
      return calculateTree157(features, 180);
    }
  }
  if (node === 1455) return 0.19579508128408926;
  if (node === 1456) return -0.3041404790616375;
  if (node === 727) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree157(features, 1455);
    } else if (val <= 709.5000000000001) {
      return calculateTree157(features, 1455);
    } else {
      return calculateTree157(features, 1456);
    }
  }
  if (node === 728) return 0.08408085751177269;
  if (node === 363) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree157(features, 727);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree157(features, 727);
    } else {
      return calculateTree157(features, 728);
    }
  }
  if (node === 729) return -0.2937450499525838;
  if (node === 730) return 0.11202082407273183;
  if (node === 364) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree157(features, 729);
    } else if (val <= 715.5000000000001) {
      return calculateTree157(features, 729);
    } else {
      return calculateTree157(features, 730);
    }
  }
  if (node === 181) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree157(features, 363);
    } else if (val <= 718.5000000000001) {
      return calculateTree157(features, 363);
    } else {
      return calculateTree157(features, 364);
    }
  }
  if (node === 182) return 0.0537231511384551;
  if (node === 90) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree157(features, 181);
    } else if (val <= 900.5000000000001) {
      return calculateTree157(features, 181);
    } else {
      return calculateTree157(features, 182);
    }
  }
  if (node === 44) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree157(features, 89);
    } else if (val <= 435.00000000000006) {
      return calculateTree157(features, 89);
    } else {
      return calculateTree157(features, 90);
    }
  }
  if (node === 21) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree157(features, 43);
    } else if (val <= 305.00000000000006) {
      return calculateTree157(features, 43);
    } else {
      return calculateTree157(features, 44);
    }
  }
  if (node === 91) return 0.12862505453630696;
  if (node === 185) return -0.27428261484218236;
  if (node === 186) return 0.05528911609517594;
  if (node === 92) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree157(features, 185);
    } else if (val <= 1.5000000000000002) {
      return calculateTree157(features, 185);
    } else {
      return calculateTree157(features, 186);
    }
  }
  if (node === 45) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree157(features, 91);
    } else if (val <= 23.500000000000004) {
      return calculateTree157(features, 91);
    } else {
      return calculateTree157(features, 92);
    }
  }
  if (node === 46) return 0.0040257131066899635;
  if (node === 22) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree157(features, 45);
    } else if (val <= 127.50000000000001) {
      return calculateTree157(features, 45);
    } else {
      return calculateTree157(features, 46);
    }
  }
  if (node === 10) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree157(features, 21);
    } else if (val <= 722.5000000000001) {
      return calculateTree157(features, 21);
    } else {
      return calculateTree157(features, 22);
    }
  }
  if (node === 4) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree157(features, 9);
    } else if (val <= 55.50000000000001) {
      return calculateTree157(features, 9);
    } else {
      return calculateTree157(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree157(features, 3);
    } else if (val <= 688.5000000000001) {
      return calculateTree157(features, 3);
    } else {
      return calculateTree157(features, 4);
    }
  }
  if (node === 5) return -0.3217365711319579;
  if (node === 6) return -0.003276509823575787;
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree157(features, 5);
    } else if (val <= 845.5000000000001) {
      return calculateTree157(features, 5);
    } else {
      return calculateTree157(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree157(features, 1);
    } else if (val <= 844.5000000000001) {
      return calculateTree157(features, 1);
    } else {
      return calculateTree157(features, 2);
    }
  }
}
function calculateTree158(features, node = 0) {
  if (node === 7) return -0.2842830746982363;
  if (node === 8) return 0.02790938364221371;
  if (node === 3) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree158(features, 7);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree158(features, 7);
    } else {
      return calculateTree158(features, 8);
    }
  }
  if (node === 19) return 0.0011318422642580305;
  if (node === 20) return -0.3332797423458108;
  if (node === 9) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree158(features, 19);
    } else if (val <= 935.5000000000001) {
      return calculateTree158(features, 19);
    } else {
      return calculateTree158(features, 20);
    }
  }
  if (node === 43) return -0.3264605627125276;
  if (node === 179) return 0.025125205802168458;
  if (node === 361) return -0.3285695041222147;
  if (node === 362) return -0.02046300285484191;
  if (node === 180) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree158(features, 361);
    } else if (val <= 18.500000000000004) {
      return calculateTree158(features, 361);
    } else {
      return calculateTree158(features, 362);
    }
  }
  if (node === 89) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree158(features, 179);
    } else if (val <= 14.500000000000002) {
      return calculateTree158(features, 179);
    } else {
      return calculateTree158(features, 180);
    }
  }
  if (node === 90) return 0.03714385789590426;
  if (node === 44) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree158(features, 89);
    } else if (val <= 708.5000000000001) {
      return calculateTree158(features, 89);
    } else {
      return calculateTree158(features, 90);
    }
  }
  if (node === 21) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree158(features, 43);
    } else if (val <= 229.50000000000003) {
      return calculateTree158(features, 43);
    } else {
      return calculateTree158(features, 44);
    }
  }
  if (node === 22) return -0.32443598342879965;
  if (node === 10) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree158(features, 21);
    } else if (val <= 57.50000000000001) {
      return calculateTree158(features, 21);
    } else {
      return calculateTree158(features, 22);
    }
  }
  if (node === 4) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree158(features, 9);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree158(features, 9);
    } else {
      return calculateTree158(features, 10);
    }
  }
  if (node === 1) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree158(features, 3);
    } else if (val <= 175.50000000000003) {
      return calculateTree158(features, 3);
    } else {
      return calculateTree158(features, 4);
    }
  }
  if (node === 191) return 0.058584234044216824;
  if (node === 192) return -0.11783131755044199;
  if (node === 95) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree158(features, 191);
    } else if (val <= 229.50000000000003) {
      return calculateTree158(features, 191);
    } else {
      return calculateTree158(features, 192);
    }
  }
  if (node === 96) return -0.28325590960263647;
  if (node === 47) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree158(features, 95);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree158(features, 95);
    } else {
      return calculateTree158(features, 96);
    }
  }
  if (node === 1567) return 0.08897239350814518;
  if (node === 1568) return 0.27352939559758993;
  if (node === 783) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree158(features, 1567);
    } else if (val <= 20.500000000000004) {
      return calculateTree158(features, 1567);
    } else {
      return calculateTree158(features, 1568);
    }
  }
  if (node === 784) return -0.11136167806630462;
  if (node === 391) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree158(features, 783);
    } else if (val <= 893.0000000000001) {
      return calculateTree158(features, 783);
    } else {
      return calculateTree158(features, 784);
    }
  }
  if (node === 392) return -0.012605180844206592;
  if (node === 195) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree158(features, 391);
    } else if (val <= 26.500000000000004) {
      return calculateTree158(features, 391);
    } else {
      return calculateTree158(features, 392);
    }
  }
  if (node === 196) return 0.30222687069671383;
  if (node === 97) {
    const val = features['is_preference_kitchen'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree158(features, 195);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree158(features, 195);
    } else {
      return calculateTree158(features, 196);
    }
  }
  if (node === 98) return -0.17614954682783976;
  if (node === 48) {
    const val = features['is_preference_men_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree158(features, 97);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree158(features, 97);
    } else {
      return calculateTree158(features, 98);
    }
  }
  if (node === 23) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree158(features, 47);
    } else if (val <= 348.50000000000006) {
      return calculateTree158(features, 47);
    } else {
      return calculateTree158(features, 48);
    }
  }
  if (node === 49) return 0.2521610149680823;
  if (node === 101) return -0.020382855001785508;
  if (node === 102) return 0.27610581228286335;
  if (node === 50) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree158(features, 101);
    } else if (val <= 870.5000000000001) {
      return calculateTree158(features, 101);
    } else {
      return calculateTree158(features, 102);
    }
  }
  if (node === 24) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree158(features, 49);
    } else if (val <= 35.50000000000001) {
      return calculateTree158(features, 49);
    } else {
      return calculateTree158(features, 50);
    }
  }
  if (node === 11) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree158(features, 23);
    } else if (val <= 8.500000000000002) {
      return calculateTree158(features, 23);
    } else {
      return calculateTree158(features, 24);
    }
  }
  if (node === 12) return -0.2688753713978764;
  if (node === 5) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree158(features, 11);
    } else if (val <= 56.50000000000001) {
      return calculateTree158(features, 11);
    } else {
      return calculateTree158(features, 12);
    }
  }
  if (node === 111) return -0.0674928059485481;
  if (node === 112) return -0.3178720417568566;
  if (node === 55) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree158(features, 111);
    } else if (val <= 201.50000000000003) {
      return calculateTree158(features, 111);
    } else {
      return calculateTree158(features, 112);
    }
  }
  if (node === 455) return 0.09599449559607044;
  if (node === 456) return -0.3467392147310953;
  if (node === 227) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree158(features, 455);
    } else if (val <= 41.00000000000001) {
      return calculateTree158(features, 455);
    } else {
      return calculateTree158(features, 456);
    }
  }
  if (node === 228) return 0.40315642744384605;
  if (node === 113) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree158(features, 227);
    } else if (val <= 9.500000000000002) {
      return calculateTree158(features, 227);
    } else {
      return calculateTree158(features, 228);
    }
  }
  if (node === 229) return -0.28533842105698165;
  if (node === 230) return 0.3780260441829038;
  if (node === 114) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree158(features, 229);
    } else if (val <= 953.5000000000001) {
      return calculateTree158(features, 229);
    } else {
      return calculateTree158(features, 230);
    }
  }
  if (node === 56) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree158(features, 113);
    } else if (val <= 718.5000000000001) {
      return calculateTree158(features, 113);
    } else {
      return calculateTree158(features, 114);
    }
  }
  if (node === 27) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree158(features, 55);
    } else if (val <= 409.50000000000006) {
      return calculateTree158(features, 55);
    } else {
      return calculateTree158(features, 56);
    }
  }
  if (node === 28) return -0.37654587911450094;
  if (node === 13) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree158(features, 27);
    } else if (val <= 45.50000000000001) {
      return calculateTree158(features, 27);
    } else {
      return calculateTree158(features, 28);
    }
  }
  if (node === 14) return 0.00023526208608119076;
  if (node === 6) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree158(features, 13);
    } else if (val <= 83.50000000000001) {
      return calculateTree158(features, 13);
    } else {
      return calculateTree158(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree158(features, 5);
    } else if (val <= 75.50000000000001) {
      return calculateTree158(features, 5);
    } else {
      return calculateTree158(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree158(features, 1);
    } else if (val <= 66.00000000000001) {
      return calculateTree158(features, 1);
    } else {
      return calculateTree158(features, 2);
    }
  }
}
function calculateTree159(features, node = 0) {
  if (node === 7) return -0.0014421596171808283;
  if (node === 287) return 0.15129284279033298;
  if (node === 288) return -0.17012217941860383;
  if (node === 143) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree159(features, 287);
    } else if (val <= 8.500000000000002) {
      return calculateTree159(features, 287);
    } else {
      return calculateTree159(features, 288);
    }
  }
  if (node === 289) return 0.08422713081941556;
  if (node === 290) return -0.1869171557585481;
  if (node === 144) {
    const val = features['is_preference_pets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree159(features, 289);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree159(features, 289);
    } else {
      return calculateTree159(features, 290);
    }
  }
  if (node === 71) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree159(features, 143);
    } else if (val <= 21.500000000000004) {
      return calculateTree159(features, 143);
    } else {
      return calculateTree159(features, 144);
    }
  }
  if (node === 145) return 0.1722687531891846;
  if (node === 293) return -0.19074790097552294;
  if (node === 294) return 0.18248348841089174;
  if (node === 146) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree159(features, 293);
    } else if (val <= 891.0000000000001) {
      return calculateTree159(features, 293);
    } else {
      return calculateTree159(features, 294);
    }
  }
  if (node === 72) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree159(features, 145);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree159(features, 145);
    } else {
      return calculateTree159(features, 146);
    }
  }
  if (node === 35) {
    const val = features['is_preference_sports'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree159(features, 71);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree159(features, 71);
    } else {
      return calculateTree159(features, 72);
    }
  }
  if (node === 147) return -0.0056161590820516276;
  if (node === 297) return 0.1993524944501851;
  if (node === 298) return -0.04712866911042858;
  if (node === 148) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree159(features, 297);
    } else if (val <= 271.00000000000006) {
      return calculateTree159(features, 297);
    } else {
      return calculateTree159(features, 298);
    }
  }
  if (node === 73) {
    const val = features['is_preference_men_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree159(features, 147);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree159(features, 147);
    } else {
      return calculateTree159(features, 148);
    }
  }
  if (node === 74) return -0.04710783170811947;
  if (node === 36) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree159(features, 73);
    } else if (val <= 5.500000000000001) {
      return calculateTree159(features, 73);
    } else {
      return calculateTree159(features, 74);
    }
  }
  if (node === 17) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree159(features, 35);
    } else if (val <= 180.50000000000003) {
      return calculateTree159(features, 35);
    } else {
      return calculateTree159(features, 36);
    }
  }
  if (node === 18) return -0.0009898720684195842;
  if (node === 8) {
    const val = features['is_preference_household'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree159(features, 17);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree159(features, 17);
    } else {
      return calculateTree159(features, 18);
    }
  }
  if (node === 3) {
    const val = features['is_preference_beauty'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree159(features, 7);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree159(features, 7);
    } else {
      return calculateTree159(features, 8);
    }
  }
  if (node === 9) return -0.19175002132323846;
  if (node === 10) return 0.48688533309614307;
  if (node === 4) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree159(features, 9);
    } else if (val <= 29.500000000000004) {
      return calculateTree159(features, 9);
    } else {
      return calculateTree159(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree159(features, 3);
    } else if (val <= 464.50000000000006) {
      return calculateTree159(features, 3);
    } else {
      return calculateTree159(features, 4);
    }
  }
  if (node === 23) return -0.007895777742751688;
  if (node === 24) return 0.2467803172592661;
  if (node === 11) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree159(features, 23);
    } else if (val <= 908.0000000000001) {
      return calculateTree159(features, 23);
    } else {
      return calculateTree159(features, 24);
    }
  }
  if (node === 831) return -0.13088789153640168;
  if (node === 832) return 0.09019994176383918;
  if (node === 415) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree159(features, 831);
    } else if (val <= 1.5000000000000002) {
      return calculateTree159(features, 831);
    } else {
      return calculateTree159(features, 832);
    }
  }
  if (node === 1667) return -0.20246746360240878;
  if (node === 1668) return 0.2455671740006441;
  if (node === 833) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree159(features, 1667);
    } else if (val <= 5.500000000000001) {
      return calculateTree159(features, 1667);
    } else {
      return calculateTree159(features, 1668);
    }
  }
  if (node === 1669) return 0.25835689929237227;
  if (node === 1670) return 0.08073319066828931;
  if (node === 834) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree159(features, 1669);
    } else if (val <= 4.500000000000001) {
      return calculateTree159(features, 1669);
    } else {
      return calculateTree159(features, 1670);
    }
  }
  if (node === 416) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree159(features, 833);
    } else if (val <= 472.50000000000006) {
      return calculateTree159(features, 833);
    } else {
      return calculateTree159(features, 834);
    }
  }
  if (node === 207) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree159(features, 415);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree159(features, 415);
    } else {
      return calculateTree159(features, 416);
    }
  }
  if (node === 417) return 0.39392245613683746;
  if (node === 418) return 0.08567407066733501;
  if (node === 208) {
    const val = features['is_preference_kids'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree159(features, 417);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree159(features, 417);
    } else {
      return calculateTree159(features, 418);
    }
  }
  if (node === 103) {
    const val = features['is_preference_beauty'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree159(features, 207);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree159(features, 207);
    } else {
      return calculateTree159(features, 208);
    }
  }
  if (node === 419) return -0.05610658546491778;
  if (node === 420) return 0.14831086866062781;
  if (node === 209) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree159(features, 419);
    } else if (val <= 339.50000000000006) {
      return calculateTree159(features, 419);
    } else {
      return calculateTree159(features, 420);
    }
  }
  if (node === 210) return -0.18825768967099796;
  if (node === 104) {
    const val = features['is_preference_men_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree159(features, 209);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree159(features, 209);
    } else {
      return calculateTree159(features, 210);
    }
  }
  if (node === 51) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree159(features, 103);
    } else if (val <= 12.500000000000002) {
      return calculateTree159(features, 103);
    } else {
      return calculateTree159(features, 104);
    }
  }
  if (node === 52) return 0.3853621375889848;
  if (node === 25) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree159(features, 51);
    } else if (val <= 749.0000000000001) {
      return calculateTree159(features, 51);
    } else {
      return calculateTree159(features, 52);
    }
  }
  if (node === 53) return 0.13251853355582027;
  if (node === 54) return -0.1270103208019094;
  if (node === 26) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree159(features, 53);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree159(features, 53);
    } else {
      return calculateTree159(features, 54);
    }
  }
  if (node === 12) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree159(features, 25);
    } else if (val <= 870.5000000000001) {
      return calculateTree159(features, 25);
    } else {
      return calculateTree159(features, 26);
    }
  }
  if (node === 5) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree159(features, 11);
    } else if (val <= 1.5000000000000002) {
      return calculateTree159(features, 11);
    } else {
      return calculateTree159(features, 12);
    }
  }
  if (node === 6) return -1.767277868656894e-5;
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree159(features, 5);
    } else if (val <= 475.50000000000006) {
      return calculateTree159(features, 5);
    } else {
      return calculateTree159(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree159(features, 1);
    } else if (val <= 470.50000000000006) {
      return calculateTree159(features, 1);
    } else {
      return calculateTree159(features, 2);
    }
  }
}
function calculateTree160(features, node = 0) {
  if (node === 15) return -0.016377483449135408;
  if (node === 135) return 0.04726545161317031;
  if (node === 136) return -0.34510784557415625;
  if (node === 67) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree160(features, 135);
    } else if (val <= 695.5000000000001) {
      return calculateTree160(features, 135);
    } else {
      return calculateTree160(features, 136);
    }
  }
  if (node === 68) return -0.321829231362377;
  if (node === 33) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree160(features, 67);
    } else if (val <= 8.500000000000002) {
      return calculateTree160(features, 67);
    } else {
      return calculateTree160(features, 68);
    }
  }
  if (node === 69) return 0.1379054537729504;
  if (node === 283) return 0.04065903395229608;
  if (node === 284) return -0.3297390058739184;
  if (node === 141) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree160(features, 283);
    } else if (val <= 83.50000000000001) {
      return calculateTree160(features, 283);
    } else {
      return calculateTree160(features, 284);
    }
  }
  if (node === 285) return -0.31156358991211797;
  if (node === 286) return 0.2889539004713873;
  if (node === 142) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree160(features, 285);
    } else if (val <= 507.00000000000006) {
      return calculateTree160(features, 285);
    } else {
      return calculateTree160(features, 286);
    }
  }
  if (node === 70) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree160(features, 141);
    } else if (val <= 97.50000000000001) {
      return calculateTree160(features, 141);
    } else {
      return calculateTree160(features, 142);
    }
  }
  if (node === 34) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree160(features, 69);
    } else if (val <= 338.50000000000006) {
      return calculateTree160(features, 69);
    } else {
      return calculateTree160(features, 70);
    }
  }
  if (node === 16) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree160(features, 33);
    } else if (val <= 14.500000000000002) {
      return calculateTree160(features, 33);
    } else {
      return calculateTree160(features, 34);
    }
  }
  if (node === 7) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree160(features, 15);
    } else if (val <= 327.50000000000006) {
      return calculateTree160(features, 15);
    } else {
      return calculateTree160(features, 16);
    }
  }
  if (node === 35) return -0.29304737763558936;
  if (node === 36) return 0.04409466106967636;
  if (node === 17) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree160(features, 35);
    } else if (val <= 28.500000000000004) {
      return calculateTree160(features, 35);
    } else {
      return calculateTree160(features, 36);
    }
  }
  if (node === 37) return -0.35396912612463355;
  if (node === 77) return -0.19979458059906863;
  if (node === 78) return 0.2153633596517501;
  if (node === 38) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree160(features, 77);
    } else if (val <= 97.50000000000001) {
      return calculateTree160(features, 77);
    } else {
      return calculateTree160(features, 78);
    }
  }
  if (node === 18) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree160(features, 37);
    } else if (val <= 884.0000000000001) {
      return calculateTree160(features, 37);
    } else {
      return calculateTree160(features, 38);
    }
  }
  if (node === 8) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree160(features, 17);
    } else if (val <= 41.50000000000001) {
      return calculateTree160(features, 17);
    } else {
      return calculateTree160(features, 18);
    }
  }
  if (node === 3) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree160(features, 7);
    } else if (val <= 777.5000000000001) {
      return calculateTree160(features, 7);
    } else {
      return calculateTree160(features, 8);
    }
  }
  if (node === 159) return 0.02683050618112437;
  if (node === 643) return 0.010465046847176912;
  if (node === 644) return -0.304581179418291;
  if (node === 321) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree160(features, 643);
    } else if (val <= 41.50000000000001) {
      return calculateTree160(features, 643);
    } else {
      return calculateTree160(features, 644);
    }
  }
  if (node === 322) return 0.006883100724203718;
  if (node === 160) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree160(features, 321);
    } else if (val <= 409.50000000000006) {
      return calculateTree160(features, 321);
    } else {
      return calculateTree160(features, 322);
    }
  }
  if (node === 79) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree160(features, 159);
    } else if (val <= 300.50000000000006) {
      return calculateTree160(features, 159);
    } else {
      return calculateTree160(features, 160);
    }
  }
  if (node === 80) return -0.3167609876282395;
  if (node === 39) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree160(features, 79);
    } else if (val <= 659.5000000000001) {
      return calculateTree160(features, 79);
    } else {
      return calculateTree160(features, 80);
    }
  }
  if (node === 40) return 0.089135185371693;
  if (node === 19) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree160(features, 39);
    } else if (val <= 672.0000000000001) {
      return calculateTree160(features, 39);
    } else {
      return calculateTree160(features, 40);
    }
  }
  if (node === 20) return -0.28817756178350723;
  if (node === 9) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree160(features, 19);
    } else if (val <= 705.5000000000001) {
      return calculateTree160(features, 19);
    } else {
      return calculateTree160(features, 20);
    }
  }
  if (node === 21) return 0.2069370631411837;
  if (node === 45) return -0.33894739270159063;
  if (node === 375) return 0.08600647082215944;
  if (node === 376) return -0.06816774189048717;
  if (node === 187) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree160(features, 375);
    } else if (val <= 19.500000000000004) {
      return calculateTree160(features, 375);
    } else {
      return calculateTree160(features, 376);
    }
  }
  if (node === 188) return 0.15542343191752805;
  if (node === 93) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree160(features, 187);
    } else if (val <= 877.5000000000001) {
      return calculateTree160(features, 187);
    } else {
      return calculateTree160(features, 188);
    }
  }
  if (node === 189) return 0.03384279310078152;
  if (node === 190) return -0.4460540726347333;
  if (node === 94) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree160(features, 189);
    } else if (val <= 97.50000000000001) {
      return calculateTree160(features, 189);
    } else {
      return calculateTree160(features, 190);
    }
  }
  if (node === 46) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree160(features, 93);
    } else if (val <= 884.0000000000001) {
      return calculateTree160(features, 93);
    } else {
      return calculateTree160(features, 94);
    }
  }
  if (node === 22) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree160(features, 45);
    } else if (val <= 861.5000000000001) {
      return calculateTree160(features, 45);
    } else {
      return calculateTree160(features, 46);
    }
  }
  if (node === 10) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree160(features, 21);
    } else if (val <= 839.5000000000001) {
      return calculateTree160(features, 21);
    } else {
      return calculateTree160(features, 22);
    }
  }
  if (node === 4) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree160(features, 9);
    } else if (val <= 778.5000000000001) {
      return calculateTree160(features, 9);
    } else {
      return calculateTree160(features, 10);
    }
  }
  if (node === 1) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree160(features, 3);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree160(features, 3);
    } else {
      return calculateTree160(features, 4);
    }
  }
  if (node === 23) return -0.33305887617434043;
  if (node === 24) return 0.09823815388464939;
  if (node === 11) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree160(features, 23);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree160(features, 23);
    } else {
      return calculateTree160(features, 24);
    }
  }
  if (node === 12) return -0.32716481300668826;
  if (node === 5) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree160(features, 11);
    } else if (val <= 422.00000000000006) {
      return calculateTree160(features, 11);
    } else {
      return calculateTree160(features, 12);
    }
  }
  if (node === 6) return 0.0007705504835578287;
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree160(features, 5);
    } else if (val <= 131.50000000000003) {
      return calculateTree160(features, 5);
    } else {
      return calculateTree160(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree160(features, 1);
    } else if (val <= 127.00000000000001) {
      return calculateTree160(features, 1);
    } else {
      return calculateTree160(features, 2);
    }
  }
}
function calculateTree161(features, node = 0) {
  if (node === 1023) return -0.08849317088755321;
  if (node === 1024) return 0.04305069308204747;
  if (node === 511) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree161(features, 1023);
    } else if (val <= 322.00000000000006) {
      return calculateTree161(features, 1023);
    } else {
      return calculateTree161(features, 1024);
    }
  }
  if (node === 1025) return -0.3395839272857339;
  if (node === 1026) return -0.08072308669461854;
  if (node === 512) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree161(features, 1025);
    } else if (val <= 19.000000000000004) {
      return calculateTree161(features, 1025);
    } else {
      return calculateTree161(features, 1026);
    }
  }
  if (node === 255) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree161(features, 511);
    } else if (val <= 542.5000000000001) {
      return calculateTree161(features, 511);
    } else {
      return calculateTree161(features, 512);
    }
  }
  if (node === 513) return -0.14769290265183246;
  if (node === 1029) return 0.05423717490777792;
  if (node === 1030) return 0.24068025410521607;
  if (node === 514) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree161(features, 1029);
    } else if (val <= 162.50000000000003) {
      return calculateTree161(features, 1029);
    } else {
      return calculateTree161(features, 1030);
    }
  }
  if (node === 256) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree161(features, 513);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree161(features, 513);
    } else {
      return calculateTree161(features, 514);
    }
  }
  if (node === 127) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree161(features, 255);
    } else if (val <= 649.5000000000001) {
      return calculateTree161(features, 255);
    } else {
      return calculateTree161(features, 256);
    }
  }
  if (node === 128) return -0.3215444675532998;
  if (node === 63) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree161(features, 127);
    } else if (val <= 658.5000000000001) {
      return calculateTree161(features, 127);
    } else {
      return calculateTree161(features, 128);
    }
  }
  if (node === 64) return 0.12034000776239477;
  if (node === 31) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree161(features, 63);
    } else if (val <= 801.5000000000001) {
      return calculateTree161(features, 63);
    } else {
      return calculateTree161(features, 64);
    }
  }
  if (node === 65) return 0.19202696764507976;
  if (node === 66) return -0.006811537207821424;
  if (node === 32) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree161(features, 65);
    } else if (val <= 271.00000000000006) {
      return calculateTree161(features, 65);
    } else {
      return calculateTree161(features, 66);
    }
  }
  if (node === 15) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree161(features, 31);
    } else if (val <= 39.50000000000001) {
      return calculateTree161(features, 31);
    } else {
      return calculateTree161(features, 32);
    }
  }
  if (node === 16) return -0.32543235082307953;
  if (node === 7) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree161(features, 15);
    } else if (val <= 864.5000000000001) {
      return calculateTree161(features, 15);
    } else {
      return calculateTree161(features, 16);
    }
  }
  if (node === 17) return -0.32770087918600027;
  if (node === 37) return 0.011845931062457902;
  if (node === 38) return 0.11139974242767335;
  if (node === 18) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree161(features, 37);
    } else if (val <= 34.00000000000001) {
      return calculateTree161(features, 37);
    } else {
      return calculateTree161(features, 38);
    }
  }
  if (node === 8) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree161(features, 17);
    } else if (val <= 12.000000000000002) {
      return calculateTree161(features, 17);
    } else {
      return calculateTree161(features, 18);
    }
  }
  if (node === 3) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree161(features, 7);
    } else if (val <= 903.5000000000001) {
      return calculateTree161(features, 7);
    } else {
      return calculateTree161(features, 8);
    }
  }
  if (node === 19) return -0.3213462280785112;
  if (node === 83) return 0.10255885221306228;
  if (node === 84) return 0.43187701399854134;
  if (node === 41) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree161(features, 83);
    } else if (val <= 479.50000000000006) {
      return calculateTree161(features, 83);
    } else {
      return calculateTree161(features, 84);
    }
  }
  if (node === 1375) return 0.03379879991580634;
  if (node === 1376) return -0.03358711747028639;
  if (node === 687) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree161(features, 1375);
    } else if (val <= 551.5000000000001) {
      return calculateTree161(features, 1375);
    } else {
      return calculateTree161(features, 1376);
    }
  }
  if (node === 688) return -0.3375096485141202;
  if (node === 343) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree161(features, 687);
    } else if (val <= 649.5000000000001) {
      return calculateTree161(features, 687);
    } else {
      return calculateTree161(features, 688);
    }
  }
  if (node === 344) return 0.05346933222949731;
  if (node === 171) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree161(features, 343);
    } else if (val <= 785.5000000000001) {
      return calculateTree161(features, 343);
    } else {
      return calculateTree161(features, 344);
    }
  }
  if (node === 691) return -0.3053693356342195;
  if (node === 1385) return 0.059084247932691555;
  if (node === 1386) return 0.20202799574448363;
  if (node === 692) {
    const val = features['is_preference_kids'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree161(features, 1385);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree161(features, 1385);
    } else {
      return calculateTree161(features, 1386);
    }
  }
  if (node === 345) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree161(features, 691);
    } else if (val <= 658.5000000000001) {
      return calculateTree161(features, 691);
    } else {
      return calculateTree161(features, 692);
    }
  }
  if (node === 346) return -0.36182467127595613;
  if (node === 172) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree161(features, 345);
    } else if (val <= 801.5000000000001) {
      return calculateTree161(features, 345);
    } else {
      return calculateTree161(features, 346);
    }
  }
  if (node === 85) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree161(features, 171);
    } else if (val <= 34.00000000000001) {
      return calculateTree161(features, 171);
    } else {
      return calculateTree161(features, 172);
    }
  }
  if (node === 173) return -0.31282354229883785;
  if (node === 349) return 0.12777277979523533;
  if (node === 701) return 0.0983345576610556;
  if (node === 702) return 0.024516257655623767;
  if (node === 350) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree161(features, 701);
    } else if (val <= 899.5000000000001) {
      return calculateTree161(features, 701);
    } else {
      return calculateTree161(features, 702);
    }
  }
  if (node === 174) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree161(features, 349);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree161(features, 349);
    } else {
      return calculateTree161(features, 350);
    }
  }
  if (node === 86) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree161(features, 173);
    } else if (val <= 864.5000000000001) {
      return calculateTree161(features, 173);
    } else {
      return calculateTree161(features, 174);
    }
  }
  if (node === 42) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree161(features, 85);
    } else if (val <= 39.50000000000001) {
      return calculateTree161(features, 85);
    } else {
      return calculateTree161(features, 86);
    }
  }
  if (node === 20) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree161(features, 41);
    } else if (val <= 186.50000000000003) {
      return calculateTree161(features, 41);
    } else {
      return calculateTree161(features, 42);
    }
  }
  if (node === 9) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree161(features, 19);
    } else if (val <= 181.50000000000003) {
      return calculateTree161(features, 19);
    } else {
      return calculateTree161(features, 20);
    }
  }
  if (node === 10) return -0.30929920063433486;
  if (node === 4) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree161(features, 9);
    } else if (val <= 982.5000000000001) {
      return calculateTree161(features, 9);
    } else {
      return calculateTree161(features, 10);
    }
  }
  if (node === 1) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree161(features, 3);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree161(features, 3);
    } else {
      return calculateTree161(features, 4);
    }
  }
  if (node === 2) return -0.0014386678735990612;
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree161(features, 1);
    } else if (val <= 44.50000000000001) {
      return calculateTree161(features, 1);
    } else {
      return calculateTree161(features, 2);
    }
  }
}
function calculateTree162(features, node = 0) {
  if (node === 3) return -0.0005766091537623972;
  if (node === 9) return -0.32232710465115727;
  if (node === 351) return -0.24945656344950765;
  if (node === 352) return 0.05925729034824894;
  if (node === 175) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree162(features, 351);
    } else if (val <= 560.5000000000001) {
      return calculateTree162(features, 351);
    } else {
      return calculateTree162(features, 352);
    }
  }
  if (node === 176) return -0.3821301010136176;
  if (node === 87) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree162(features, 175);
    } else if (val <= 498.00000000000006) {
      return calculateTree162(features, 175);
    } else {
      return calculateTree162(features, 176);
    }
  }
  if (node === 88) return -0.32682239774550037;
  if (node === 43) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree162(features, 87);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree162(features, 87);
    } else {
      return calculateTree162(features, 88);
    }
  }
  if (node === 719) return 0.33291283919195436;
  if (node === 720) return -0.327770865899223;
  if (node === 359) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree162(features, 719);
    } else if (val <= 422.50000000000006) {
      return calculateTree162(features, 719);
    } else {
      return calculateTree162(features, 720);
    }
  }
  if (node === 360) return -0.29716882283268226;
  if (node === 179) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree162(features, 359);
    } else if (val <= 803.0000000000001) {
      return calculateTree162(features, 359);
    } else {
      return calculateTree162(features, 360);
    }
  }
  if (node === 361) return 0.17650133488585293;
  if (node === 725) return -0.34892066537784816;
  if (node === 1453) return 0.14618886176653928;
  if (node === 1454) return -0.000953205134963372;
  if (node === 726) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree162(features, 1453);
    } else if (val <= 733.5000000000001) {
      return calculateTree162(features, 1453);
    } else {
      return calculateTree162(features, 1454);
    }
  }
  if (node === 362) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree162(features, 725);
    } else if (val <= 611.5000000000001) {
      return calculateTree162(features, 725);
    } else {
      return calculateTree162(features, 726);
    }
  }
  if (node === 180) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree162(features, 361);
    } else if (val <= 582.5000000000001) {
      return calculateTree162(features, 361);
    } else {
      return calculateTree162(features, 362);
    }
  }
  if (node === 89) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree162(features, 179);
    } else if (val <= 518.5000000000001) {
      return calculateTree162(features, 179);
    } else {
      return calculateTree162(features, 180);
    }
  }
  if (node === 1455) return 0.06887007222555153;
  if (node === 1456) return -0.017200735947588586;
  if (node === 727) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree162(features, 1455);
    } else if (val <= 251.00000000000003) {
      return calculateTree162(features, 1455);
    } else {
      return calculateTree162(features, 1456);
    }
  }
  if (node === 728) return 0.3811263132242245;
  if (node === 363) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree162(features, 727);
    } else if (val <= 4.500000000000001) {
      return calculateTree162(features, 727);
    } else {
      return calculateTree162(features, 728);
    }
  }
  if (node === 1459) return 0.09399258851135692;
  if (node === 1460) return -0.10746108560458341;
  if (node === 729) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree162(features, 1459);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree162(features, 1459);
    } else {
      return calculateTree162(features, 1460);
    }
  }
  if (node === 730) return -0.1990492301476677;
  if (node === 364) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree162(features, 729);
    } else if (val <= 880.0000000000001) {
      return calculateTree162(features, 729);
    } else {
      return calculateTree162(features, 730);
    }
  }
  if (node === 181) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree162(features, 363);
    } else if (val <= 5.500000000000001) {
      return calculateTree162(features, 363);
    } else {
      return calculateTree162(features, 364);
    }
  }
  if (node === 182) return -0.3160383698046873;
  if (node === 90) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree162(features, 181);
    } else if (val <= 443.50000000000006) {
      return calculateTree162(features, 181);
    } else {
      return calculateTree162(features, 182);
    }
  }
  if (node === 44) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree162(features, 89);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree162(features, 89);
    } else {
      return calculateTree162(features, 90);
    }
  }
  if (node === 21) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree162(features, 43);
    } else if (val <= 800.5000000000001) {
      return calculateTree162(features, 43);
    } else {
      return calculateTree162(features, 44);
    }
  }
  if (node === 91) return -0.3448001174109967;
  if (node === 185) return -0.037740251631172485;
  if (node === 186) return 0.14281174838772656;
  if (node === 92) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree162(features, 185);
    } else if (val <= 43.50000000000001) {
      return calculateTree162(features, 185);
    } else {
      return calculateTree162(features, 186);
    }
  }
  if (node === 45) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree162(features, 91);
    } else if (val <= 943.5000000000001) {
      return calculateTree162(features, 91);
    } else {
      return calculateTree162(features, 92);
    }
  }
  if (node === 751) return 0.08793451652689792;
  if (node === 752) return -0.16721207361910276;
  if (node === 375) {
    const val = features['is_preference_beauty'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree162(features, 751);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree162(features, 751);
    } else {
      return calculateTree162(features, 752);
    }
  }
  if (node === 376) return 0.2907234399521639;
  if (node === 187) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree162(features, 375);
    } else if (val <= 5.500000000000001) {
      return calculateTree162(features, 375);
    } else {
      return calculateTree162(features, 376);
    }
  }
  if (node === 188) return -0.11071000403785577;
  if (node === 93) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree162(features, 187);
    } else if (val <= 897.0000000000001) {
      return calculateTree162(features, 187);
    } else {
      return calculateTree162(features, 188);
    }
  }
  if (node === 94) return -0.32262806239990544;
  if (node === 46) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree162(features, 93);
    } else if (val <= 800.5000000000001) {
      return calculateTree162(features, 93);
    } else {
      return calculateTree162(features, 94);
    }
  }
  if (node === 22) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree162(features, 45);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree162(features, 45);
    } else {
      return calculateTree162(features, 46);
    }
  }
  if (node === 10) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree162(features, 21);
    } else if (val <= 834.5000000000001) {
      return calculateTree162(features, 21);
    } else {
      return calculateTree162(features, 22);
    }
  }
  if (node === 4) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree162(features, 9);
    } else if (val <= 799.5000000000001) {
      return calculateTree162(features, 9);
    } else {
      return calculateTree162(features, 10);
    }
  }
  if (node === 1) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree162(features, 3);
    } else if (val <= 795.5000000000001) {
      return calculateTree162(features, 3);
    } else {
      return calculateTree162(features, 4);
    }
  }
  if (node === 11) return 0.004018956445297739;
  if (node === 12) return -0.017418724895059537;
  if (node === 5) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree162(features, 11);
    } else if (val <= 30.500000000000004) {
      return calculateTree162(features, 11);
    } else {
      return calculateTree162(features, 12);
    }
  }
  if (node === 13) return 0.08269049244142403;
  if (node === 14) return 0.01122939605214932;
  if (node === 6) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree162(features, 13);
    } else if (val <= 13.500000000000002) {
      return calculateTree162(features, 13);
    } else {
      return calculateTree162(features, 14);
    }
  }
  if (node === 2) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree162(features, 5);
    } else if (val <= 197.50000000000003) {
      return calculateTree162(features, 5);
    } else {
      return calculateTree162(features, 6);
    }
  }
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree162(features, 1);
    } else if (val <= 821.0000000000001) {
      return calculateTree162(features, 1);
    } else {
      return calculateTree162(features, 2);
    }
  }
}
function calculateTree163(features, node = 0) {
  if (node === 15) return -0.024133237902206797;
  if (node === 16) return 0.25543182474300985;
  if (node === 7) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree163(features, 15);
    } else if (val <= 749.0000000000001) {
      return calculateTree163(features, 15);
    } else {
      return calculateTree163(features, 16);
    }
  }
  if (node === 35) return -0.1268534461711252;
  if (node === 73) return 0.17518822638865522;
  if (node === 299) return -0.07079019211722057;
  if (node === 300) return 0.11911835234582253;
  if (node === 149) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree163(features, 299);
    } else if (val <= 40.50000000000001) {
      return calculateTree163(features, 299);
    } else {
      return calculateTree163(features, 300);
    }
  }
  if (node === 150) return -0.2087237918742324;
  if (node === 74) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree163(features, 149);
    } else if (val <= 44.50000000000001) {
      return calculateTree163(features, 149);
    } else {
      return calculateTree163(features, 150);
    }
  }
  if (node === 36) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree163(features, 73);
    } else if (val <= 28.500000000000004) {
      return calculateTree163(features, 73);
    } else {
      return calculateTree163(features, 74);
    }
  }
  if (node === 17) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree163(features, 35);
    } else if (val <= 24.500000000000004) {
      return calculateTree163(features, 35);
    } else {
      return calculateTree163(features, 36);
    }
  }
  if (node === 37) return 0.035223528226430864;
  if (node === 77) return -0.18764096080218046;
  if (node === 78) return -0.4530829431074814;
  if (node === 38) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree163(features, 77);
    } else if (val <= 26.500000000000004) {
      return calculateTree163(features, 77);
    } else {
      return calculateTree163(features, 78);
    }
  }
  if (node === 18) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree163(features, 37);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree163(features, 37);
    } else {
      return calculateTree163(features, 38);
    }
  }
  if (node === 8) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree163(features, 17);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree163(features, 17);
    } else {
      return calculateTree163(features, 18);
    }
  }
  if (node === 3) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree163(features, 7);
    } else if (val <= 18.500000000000004) {
      return calculateTree163(features, 7);
    } else {
      return calculateTree163(features, 8);
    }
  }
  if (node === 4) return 0.11774933195797317;
  if (node === 1) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree163(features, 3);
    } else if (val <= 54.50000000000001) {
      return calculateTree163(features, 3);
    } else {
      return calculateTree163(features, 4);
    }
  }
  if (node === 191) return 0.05906730275889476;
  if (node === 192) return 0.30933792755395606;
  if (node === 95) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree163(features, 191);
    } else if (val <= 39.50000000000001) {
      return calculateTree163(features, 191);
    } else {
      return calculateTree163(features, 192);
    }
  }
  if (node === 387) return -0.3531014733732956;
  if (node === 388) return 0.05586979826469011;
  if (node === 193) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree163(features, 387);
    } else if (val <= 677.5000000000001) {
      return calculateTree163(features, 387);
    } else {
      return calculateTree163(features, 388);
    }
  }
  if (node === 779) return 0.36198430364312634;
  if (node === 780) return 0.0981032996449958;
  if (node === 389) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree163(features, 779);
    } else if (val <= 572.0000000000001) {
      return calculateTree163(features, 779);
    } else {
      return calculateTree163(features, 780);
    }
  }
  if (node === 781) return -0.1963484434884261;
  if (node === 782) return 0.1249256781937591;
  if (node === 390) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree163(features, 781);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree163(features, 781);
    } else {
      return calculateTree163(features, 782);
    }
  }
  if (node === 194) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree163(features, 389);
    } else if (val <= 234.50000000000003) {
      return calculateTree163(features, 389);
    } else {
      return calculateTree163(features, 390);
    }
  }
  if (node === 96) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree163(features, 193);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree163(features, 193);
    } else {
      return calculateTree163(features, 194);
    }
  }
  if (node === 47) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree163(features, 95);
    } else if (val <= 391.50000000000006) {
      return calculateTree163(features, 95);
    } else {
      return calculateTree163(features, 96);
    }
  }
  if (node === 48) return 5.1894972860249196e-5;
  if (node === 23) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree163(features, 47);
    } else if (val <= 1.5000000000000002) {
      return calculateTree163(features, 47);
    } else {
      return calculateTree163(features, 48);
    }
  }
  if (node === 49) return -0.05755655343003047;
  if (node === 815) return -0.33791385559633175;
  if (node === 1633) return 0.11001155666909364;
  if (node === 1634) return -0.1785561021594517;
  if (node === 816) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree163(features, 1633);
    } else if (val <= 53.50000000000001) {
      return calculateTree163(features, 1633);
    } else {
      return calculateTree163(features, 1634);
    }
  }
  if (node === 407) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree163(features, 815);
    } else if (val <= 649.5000000000001) {
      return calculateTree163(features, 815);
    } else {
      return calculateTree163(features, 816);
    }
  }
  if (node === 408) return -0.3211750159583644;
  if (node === 203) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree163(features, 407);
    } else if (val <= 6.000000000000001) {
      return calculateTree163(features, 407);
    } else {
      return calculateTree163(features, 408);
    }
  }
  if (node === 409) return -0.006095958294924833;
  if (node === 410) return 0.09882843001960498;
  if (node === 204) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree163(features, 409);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree163(features, 409);
    } else {
      return calculateTree163(features, 410);
    }
  }
  if (node === 101) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree163(features, 203);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree163(features, 203);
    } else {
      return calculateTree163(features, 204);
    }
  }
  if (node === 102) return -0.00893617226987218;
  if (node === 50) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree163(features, 101);
    } else if (val <= 12.000000000000002) {
      return calculateTree163(features, 101);
    } else {
      return calculateTree163(features, 102);
    }
  }
  if (node === 24) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree163(features, 49);
    } else if (val <= 20.500000000000004) {
      return calculateTree163(features, 49);
    } else {
      return calculateTree163(features, 50);
    }
  }
  if (node === 11) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree163(features, 23);
    } else if (val <= 15.500000000000002) {
      return calculateTree163(features, 23);
    } else {
      return calculateTree163(features, 24);
    }
  }
  if (node === 51) return 0.15790014047155254;
  if (node === 52) return -0.17855415946681671;
  if (node === 25) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree163(features, 51);
    } else if (val <= 28.500000000000004) {
      return calculateTree163(features, 51);
    } else {
      return calculateTree163(features, 52);
    }
  }
  if (node === 26) return -0.3482468752983489;
  if (node === 12) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree163(features, 25);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree163(features, 25);
    } else {
      return calculateTree163(features, 26);
    }
  }
  if (node === 5) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree163(features, 11);
    } else if (val <= 118.50000000000001) {
      return calculateTree163(features, 11);
    } else {
      return calculateTree163(features, 12);
    }
  }
  if (node === 6) return 0.0010165441878893292;
  if (node === 2) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree163(features, 5);
    } else if (val <= 125.50000000000001) {
      return calculateTree163(features, 5);
    } else {
      return calculateTree163(features, 6);
    }
  }
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree163(features, 1);
    } else if (val <= 3.0000000000000004) {
      return calculateTree163(features, 1);
    } else {
      return calculateTree163(features, 2);
    }
  }
}
function calculateTree164(features, node = 0) {
  if (node === 3) return 0.0009917081864537958;
  if (node === 9) return -0.299458910496193;
  if (node === 351) return -0.0015906931469412994;
  if (node === 705) return 0.17533018066511413;
  if (node === 706) return 0.0220807339996186;
  if (node === 352) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree164(features, 705);
    } else if (val <= 22.500000000000004) {
      return calculateTree164(features, 705);
    } else {
      return calculateTree164(features, 706);
    }
  }
  if (node === 175) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree164(features, 351);
    } else if (val <= 19.500000000000004) {
      return calculateTree164(features, 351);
    } else {
      return calculateTree164(features, 352);
    }
  }
  if (node === 176) return -0.26363593125809637;
  if (node === 87) {
    const val = features['is_preference_sports'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree164(features, 175);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree164(features, 175);
    } else {
      return calculateTree164(features, 176);
    }
  }
  if (node === 355) return 0.3335692091561023;
  if (node === 356) return 0.07618301786590898;
  if (node === 177) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree164(features, 355);
    } else if (val <= 18.500000000000004) {
      return calculateTree164(features, 355);
    } else {
      return calculateTree164(features, 356);
    }
  }
  if (node === 357) return -0.1378514524008153;
  if (node === 358) return 0.21363114507376083;
  if (node === 178) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree164(features, 357);
    } else if (val <= 17.500000000000004) {
      return calculateTree164(features, 357);
    } else {
      return calculateTree164(features, 358);
    }
  }
  if (node === 88) {
    const val = features['is_preference_household'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree164(features, 177);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree164(features, 177);
    } else {
      return calculateTree164(features, 178);
    }
  }
  if (node === 43) {
    const val = features['is_preference_beauty'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree164(features, 87);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree164(features, 87);
    } else {
      return calculateTree164(features, 88);
    }
  }
  if (node === 179) return 0.0007060885180231142;
  if (node === 180) return 0.2283041711254445;
  if (node === 89) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree164(features, 179);
    } else if (val <= 38.50000000000001) {
      return calculateTree164(features, 179);
    } else {
      return calculateTree164(features, 180);
    }
  }
  if (node === 363) return -0.03661014425907038;
  if (node === 729) return -0.3541436350027802;
  if (node === 730) return -0.14183978581147194;
  if (node === 364) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree164(features, 729);
    } else if (val <= 33.50000000000001) {
      return calculateTree164(features, 729);
    } else {
      return calculateTree164(features, 730);
    }
  }
  if (node === 181) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree164(features, 363);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree164(features, 363);
    } else {
      return calculateTree164(features, 364);
    }
  }
  if (node === 731) return 0.07414578681470138;
  if (node === 732) return -0.10365491663338232;
  if (node === 365) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree164(features, 731);
    } else if (val <= 19.500000000000004) {
      return calculateTree164(features, 731);
    } else {
      return calculateTree164(features, 732);
    }
  }
  if (node === 366) return -0.1332122952501231;
  if (node === 182) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree164(features, 365);
    } else if (val <= 882.5000000000001) {
      return calculateTree164(features, 365);
    } else {
      return calculateTree164(features, 366);
    }
  }
  if (node === 90) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree164(features, 181);
    } else if (val <= 2.5000000000000004) {
      return calculateTree164(features, 181);
    } else {
      return calculateTree164(features, 182);
    }
  }
  if (node === 44) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree164(features, 89);
    } else if (val <= 389.00000000000006) {
      return calculateTree164(features, 89);
    } else {
      return calculateTree164(features, 90);
    }
  }
  if (node === 21) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree164(features, 43);
    } else if (val <= 26.500000000000004) {
      return calculateTree164(features, 43);
    } else {
      return calculateTree164(features, 44);
    }
  }
  if (node === 183) return 0.0066315883316362305;
  if (node === 369) return -0.35400961219759064;
  if (node === 741) return -0.0014046501725178955;
  if (node === 1485) return -0.08946723495032956;
  if (node === 1486) return -0.32758235552811965;
  if (node === 742) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree164(features, 1485);
    } else if (val <= 7.500000000000001) {
      return calculateTree164(features, 1485);
    } else {
      return calculateTree164(features, 1486);
    }
  }
  if (node === 370) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree164(features, 741);
    } else if (val <= 389.00000000000006) {
      return calculateTree164(features, 741);
    } else {
      return calculateTree164(features, 742);
    }
  }
  if (node === 184) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree164(features, 369);
    } else if (val <= 2.5000000000000004) {
      return calculateTree164(features, 369);
    } else {
      return calculateTree164(features, 370);
    }
  }
  if (node === 91) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree164(features, 183);
    } else if (val <= 1.5000000000000002) {
      return calculateTree164(features, 183);
    } else {
      return calculateTree164(features, 184);
    }
  }
  if (node === 92) return 0.12666327390995813;
  if (node === 45) {
    const val = features['is_preference_pets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree164(features, 91);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree164(features, 91);
    } else {
      return calculateTree164(features, 92);
    }
  }
  if (node === 46) return -0.4060266125771306;
  if (node === 22) {
    const val = features['is_preference_sports'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree164(features, 45);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree164(features, 45);
    } else {
      return calculateTree164(features, 46);
    }
  }
  if (node === 10) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree164(features, 21);
    } else if (val <= 43.50000000000001) {
      return calculateTree164(features, 21);
    } else {
      return calculateTree164(features, 22);
    }
  }
  if (node === 4) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree164(features, 9);
    } else if (val <= 305.00000000000006) {
      return calculateTree164(features, 9);
    } else {
      return calculateTree164(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree164(features, 3);
    } else if (val <= 877.5000000000001) {
      return calculateTree164(features, 3);
    } else {
      return calculateTree164(features, 4);
    }
  }
  if (node === 23) return 0.10683700194651163;
  if (node === 24) return 0.03669887939483412;
  if (node === 11) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree164(features, 23);
    } else if (val <= 1.5000000000000002) {
      return calculateTree164(features, 23);
    } else {
      return calculateTree164(features, 24);
    }
  }
  if (node === 12) return -0.22289283499023135;
  if (node === 5) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree164(features, 11);
    } else if (val <= 52.50000000000001) {
      return calculateTree164(features, 11);
    } else {
      return calculateTree164(features, 12);
    }
  }
  if (node === 55) return -0.16710223946616948;
  if (node === 56) return 0.10998026441451707;
  if (node === 27) {
    const val = features['is_preference_kids'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree164(features, 55);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree164(features, 55);
    } else {
      return calculateTree164(features, 56);
    }
  }
  if (node === 28) return -0.3185382904234426;
  if (node === 13) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree164(features, 27);
    } else if (val <= 27.500000000000004) {
      return calculateTree164(features, 27);
    } else {
      return calculateTree164(features, 28);
    }
  }
  if (node === 14) return 0.005656042837538048;
  if (node === 6) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree164(features, 13);
    } else if (val <= 899.5000000000001) {
      return calculateTree164(features, 13);
    } else {
      return calculateTree164(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree164(features, 5);
    } else if (val <= 896.5000000000001) {
      return calculateTree164(features, 5);
    } else {
      return calculateTree164(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree164(features, 1);
    } else if (val <= 890.5000000000001) {
      return calculateTree164(features, 1);
    } else {
      return calculateTree164(features, 2);
    }
  }
}
function calculateTree165(features, node = 0) {
  if (node === 31) return -0.0009420342846499171;
  if (node === 65) return -0.2852098294961579;
  if (node === 1071) return 0.008315785181970313;
  if (node === 1072) return -0.04578715376874134;
  if (node === 535) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree165(features, 1071);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree165(features, 1071);
    } else {
      return calculateTree165(features, 1072);
    }
  }
  if (node === 1073) return 0.0791530417563965;
  if (node === 1074) return -0.007177259665655774;
  if (node === 536) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree165(features, 1073);
    } else if (val <= 251.00000000000003) {
      return calculateTree165(features, 1073);
    } else {
      return calculateTree165(features, 1074);
    }
  }
  if (node === 267) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree165(features, 535);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree165(features, 535);
    } else {
      return calculateTree165(features, 536);
    }
  }
  if (node === 1075) return 0.0808963104436514;
  if (node === 1076) return 0.3055994835828914;
  if (node === 537) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree165(features, 1075);
    } else if (val <= 654.0000000000001) {
      return calculateTree165(features, 1075);
    } else {
      return calculateTree165(features, 1076);
    }
  }
  if (node === 1077) return -0.02819448842940153;
  if (node === 1078) return 0.04080266058861738;
  if (node === 538) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree165(features, 1077);
    } else if (val <= 745.5000000000001) {
      return calculateTree165(features, 1077);
    } else {
      return calculateTree165(features, 1078);
    }
  }
  if (node === 268) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree165(features, 537);
    } else if (val <= 657.5000000000001) {
      return calculateTree165(features, 537);
    } else {
      return calculateTree165(features, 538);
    }
  }
  if (node === 133) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree165(features, 267);
    } else if (val <= 599.5000000000001) {
      return calculateTree165(features, 267);
    } else {
      return calculateTree165(features, 268);
    }
  }
  if (node === 539) return -0.20551458200294254;
  if (node === 540) return -0.05476779629051259;
  if (node === 269) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree165(features, 539);
    } else if (val <= 846.5000000000001) {
      return calculateTree165(features, 539);
    } else {
      return calculateTree165(features, 540);
    }
  }
  if (node === 1083) return -0.14297569925395032;
  if (node === 1084) return 0.03193897068802015;
  if (node === 541) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree165(features, 1083);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree165(features, 1083);
    } else {
      return calculateTree165(features, 1084);
    }
  }
  if (node === 542) return 0.19513679835534944;
  if (node === 270) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree165(features, 541);
    } else if (val <= 993.5000000000001) {
      return calculateTree165(features, 541);
    } else {
      return calculateTree165(features, 542);
    }
  }
  if (node === 134) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree165(features, 269);
    } else if (val <= 2.5000000000000004) {
      return calculateTree165(features, 269);
    } else {
      return calculateTree165(features, 270);
    }
  }
  if (node === 66) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree165(features, 133);
    } else if (val <= 840.0000000000001) {
      return calculateTree165(features, 133);
    } else {
      return calculateTree165(features, 134);
    }
  }
  if (node === 32) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree165(features, 65);
    } else if (val <= 3.5000000000000004) {
      return calculateTree165(features, 65);
    } else {
      return calculateTree165(features, 66);
    }
  }
  if (node === 15) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree165(features, 31);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree165(features, 31);
    } else {
      return calculateTree165(features, 32);
    }
  }
  if (node === 67) return -0.14817195748842238;
  if (node === 68) return 0.22706357679020506;
  if (node === 33) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree165(features, 67);
    } else if (val <= 657.5000000000001) {
      return calculateTree165(features, 67);
    } else {
      return calculateTree165(features, 68);
    }
  }
  if (node === 34) return -0.43633555174536914;
  if (node === 16) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree165(features, 33);
    } else if (val <= 852.5000000000001) {
      return calculateTree165(features, 33);
    } else {
      return calculateTree165(features, 34);
    }
  }
  if (node === 7) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree165(features, 15);
    } else if (val <= 591.0000000000001) {
      return calculateTree165(features, 15);
    } else {
      return calculateTree165(features, 16);
    }
  }
  if (node === 35) return -0.06487586873855436;
  if (node === 36) return 0.21610706309907116;
  if (node === 17) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree165(features, 35);
    } else if (val <= 2.5000000000000004) {
      return calculateTree165(features, 35);
    } else {
      return calculateTree165(features, 36);
    }
  }
  if (node === 18) return 0.013038039301073674;
  if (node === 8) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree165(features, 17);
    } else if (val <= 660.0000000000001) {
      return calculateTree165(features, 17);
    } else {
      return calculateTree165(features, 18);
    }
  }
  if (node === 3) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree165(features, 7);
    } else if (val <= 626.0000000000001) {
      return calculateTree165(features, 7);
    } else {
      return calculateTree165(features, 8);
    }
  }
  if (node === 19) return -0.09764419141341854;
  if (node === 41) return 0.3150680243379515;
  if (node === 42) return -0.0020678867522785395;
  if (node === 20) {
    const val = features['is_preference_personal_care'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree165(features, 41);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree165(features, 41);
    } else {
      return calculateTree165(features, 42);
    }
  }
  if (node === 9) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree165(features, 19);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree165(features, 19);
    } else {
      return calculateTree165(features, 20);
    }
  }
  if (node === 87) return -0.5007997943940522;
  if (node === 88) return -0.184385431498263;
  if (node === 43) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree165(features, 87);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree165(features, 87);
    } else {
      return calculateTree165(features, 88);
    }
  }
  if (node === 44) return -0.10083879114256425;
  if (node === 21) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree165(features, 43);
    } else if (val <= 551.5000000000001) {
      return calculateTree165(features, 43);
    } else {
      return calculateTree165(features, 44);
    }
  }
  if (node === 91) return -0.38830943353173447;
  if (node === 185) return -0.03337621434314986;
  if (node === 186) return -0.2955652392455389;
  if (node === 92) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree165(features, 185);
    } else if (val <= 916.0000000000001) {
      return calculateTree165(features, 185);
    } else {
      return calculateTree165(features, 186);
    }
  }
  if (node === 45) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree165(features, 91);
    } else if (val <= 212.50000000000003) {
      return calculateTree165(features, 91);
    } else {
      return calculateTree165(features, 92);
    }
  }
  if (node === 46) return 0.1361298699618146;
  if (node === 22) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree165(features, 45);
    } else if (val <= 859.5000000000001) {
      return calculateTree165(features, 45);
    } else {
      return calculateTree165(features, 46);
    }
  }
  if (node === 10) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree165(features, 21);
    } else if (val <= 23.500000000000004) {
      return calculateTree165(features, 21);
    } else {
      return calculateTree165(features, 22);
    }
  }
  if (node === 4) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree165(features, 9);
    } else if (val <= 195.00000000000003) {
      return calculateTree165(features, 9);
    } else {
      return calculateTree165(features, 10);
    }
  }
  if (node === 1) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree165(features, 3);
    } else if (val <= 21.500000000000004) {
      return calculateTree165(features, 3);
    } else {
      return calculateTree165(features, 4);
    }
  }
  if (node === 2) return 0.0030741850052459015;
  if (node === 0) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree165(features, 1);
    } else if (val <= 20.500000000000004) {
      return calculateTree165(features, 1);
    } else {
      return calculateTree165(features, 2);
    }
  }
}
function calculateTree166(features, node = 0) {
  if (node === 7) return -0.0004083778479887013;
  if (node === 17) return -0.21727869843351139;
  if (node === 18) return -0.028945505647792583;
  if (node === 8) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree166(features, 17);
    } else if (val <= 705.5000000000001) {
      return calculateTree166(features, 17);
    } else {
      return calculateTree166(features, 18);
    }
  }
  if (node === 3) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree166(features, 7);
    } else if (val <= 205.50000000000003) {
      return calculateTree166(features, 7);
    } else {
      return calculateTree166(features, 8);
    }
  }
  if (node === 19) return 0.1550252110561767;
  if (node === 41) return -0.03891944663554848;
  if (node === 85) return 0.3362253370459939;
  if (node === 347) return 0.05043387093924474;
  if (node === 348) return -0.13352047674788672;
  if (node === 173) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree166(features, 347);
    } else if (val <= 27.500000000000004) {
      return calculateTree166(features, 347);
    } else {
      return calculateTree166(features, 348);
    }
  }
  if (node === 349) return -0.024985710063818573;
  if (node === 350) return 0.02154738985867265;
  if (node === 174) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree166(features, 349);
    } else if (val <= 679.5000000000001) {
      return calculateTree166(features, 349);
    } else {
      return calculateTree166(features, 350);
    }
  }
  if (node === 86) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree166(features, 173);
    } else if (val <= 284.50000000000006) {
      return calculateTree166(features, 173);
    } else {
      return calculateTree166(features, 174);
    }
  }
  if (node === 42) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree166(features, 85);
    } else if (val <= 283.50000000000006) {
      return calculateTree166(features, 85);
    } else {
      return calculateTree166(features, 86);
    }
  }
  if (node === 20) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree166(features, 41);
    } else if (val <= 240.00000000000003) {
      return calculateTree166(features, 41);
    } else {
      return calculateTree166(features, 42);
    }
  }
  if (node === 9) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree166(features, 19);
    } else if (val <= 55.50000000000001) {
      return calculateTree166(features, 19);
    } else {
      return calculateTree166(features, 20);
    }
  }
  if (node === 43) return 0.14948946570171892;
  if (node === 179) return 0.17812038352162082;
  if (node === 180) return -0.1714889842432677;
  if (node === 89) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree166(features, 179);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree166(features, 179);
    } else {
      return calculateTree166(features, 180);
    }
  }
  if (node === 90) return 0.09371526799352309;
  if (node === 44) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree166(features, 89);
    } else if (val <= 10.500000000000002) {
      return calculateTree166(features, 89);
    } else {
      return calculateTree166(features, 90);
    }
  }
  if (node === 21) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree166(features, 43);
    } else if (val <= 2.5000000000000004) {
      return calculateTree166(features, 43);
    } else {
      return calculateTree166(features, 44);
    }
  }
  if (node === 1471) return 0.23378329099765074;
  if (node === 1472) return -0.198832501488746;
  if (node === 735) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree166(features, 1471);
    } else if (val <= 2.5000000000000004) {
      return calculateTree166(features, 1471);
    } else {
      return calculateTree166(features, 1472);
    }
  }
  if (node === 736) return -0.1778368135854093;
  if (node === 367) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree166(features, 735);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree166(features, 735);
    } else {
      return calculateTree166(features, 736);
    }
  }
  if (node === 368) return 0.023456519523502497;
  if (node === 183) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree166(features, 367);
    } else if (val <= 5.500000000000001) {
      return calculateTree166(features, 367);
    } else {
      return calculateTree166(features, 368);
    }
  }
  if (node === 739) return -0.34228553639841036;
  if (node === 740) return -0.009762283466064958;
  if (node === 369) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree166(features, 739);
    } else if (val <= 10.500000000000002) {
      return calculateTree166(features, 739);
    } else {
      return calculateTree166(features, 740);
    }
  }
  if (node === 1483) return -0.1112830312440367;
  if (node === 1484) return 0.06512157829340098;
  if (node === 741) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree166(features, 1483);
    } else if (val <= 531.0000000000001) {
      return calculateTree166(features, 1483);
    } else {
      return calculateTree166(features, 1484);
    }
  }
  if (node === 742) return 0.2787771879495658;
  if (node === 370) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree166(features, 741);
    } else if (val <= 891.0000000000001) {
      return calculateTree166(features, 741);
    } else {
      return calculateTree166(features, 742);
    }
  }
  if (node === 184) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree166(features, 369);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree166(features, 369);
    } else {
      return calculateTree166(features, 370);
    }
  }
  if (node === 91) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree166(features, 183);
    } else if (val <= 222.50000000000003) {
      return calculateTree166(features, 183);
    } else {
      return calculateTree166(features, 184);
    }
  }
  if (node === 185) return -0.17624905519158499;
  if (node === 186) return 0.03645255985736614;
  if (node === 92) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree166(features, 185);
    } else if (val <= 900.5000000000001) {
      return calculateTree166(features, 185);
    } else {
      return calculateTree166(features, 186);
    }
  }
  if (node === 45) {
    const val = features['is_preference_beauty'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree166(features, 91);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree166(features, 91);
    } else {
      return calculateTree166(features, 92);
    }
  }
  if (node === 46) return 0.02421067251939947;
  if (node === 22) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree166(features, 45);
    } else if (val <= 245.00000000000003) {
      return calculateTree166(features, 45);
    } else {
      return calculateTree166(features, 46);
    }
  }
  if (node === 10) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree166(features, 21);
    } else if (val <= 212.50000000000003) {
      return calculateTree166(features, 21);
    } else {
      return calculateTree166(features, 22);
    }
  }
  if (node === 4) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree166(features, 9);
    } else if (val <= 34.50000000000001) {
      return calculateTree166(features, 9);
    } else {
      return calculateTree166(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree166(features, 3);
    } else if (val <= 207.00000000000003) {
      return calculateTree166(features, 3);
    } else {
      return calculateTree166(features, 4);
    }
  }
  if (node === 23) return -0.28347410827233;
  if (node === 24) return 0.1884364173030874;
  if (node === 11) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree166(features, 23);
    } else if (val <= 26.500000000000004) {
      return calculateTree166(features, 23);
    } else {
      return calculateTree166(features, 24);
    }
  }
  if (node === 51) return 0.024063744801639985;
  if (node === 52) return -0.15221994788153456;
  if (node === 25) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree166(features, 51);
    } else if (val <= 41.50000000000001) {
      return calculateTree166(features, 51);
    } else {
      return calculateTree166(features, 52);
    }
  }
  if (node === 26) return 0.31206458228614564;
  if (node === 12) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree166(features, 25);
    } else if (val <= 50.50000000000001) {
      return calculateTree166(features, 25);
    } else {
      return calculateTree166(features, 26);
    }
  }
  if (node === 5) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree166(features, 11);
    } else if (val <= 839.5000000000001) {
      return calculateTree166(features, 11);
    } else {
      return calculateTree166(features, 12);
    }
  }
  if (node === 6) return -0.0015523354501078854;
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree166(features, 5);
    } else if (val <= 258.50000000000006) {
      return calculateTree166(features, 5);
    } else {
      return calculateTree166(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree166(features, 1);
    } else if (val <= 253.50000000000003) {
      return calculateTree166(features, 1);
    } else {
      return calculateTree166(features, 2);
    }
  }
}
function calculateTree167(features, node = 0) {
  if (node === 31) return -0.0038083204534725204;
  if (node === 131) return -0.2999353490502364;
  if (node === 531) return 0.08286606566535602;
  if (node === 1065) return 0.0745633992496494;
  if (node === 1066) return -0.33475017958138453;
  if (node === 532) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree167(features, 1065);
    } else if (val <= 752.5000000000001) {
      return calculateTree167(features, 1065);
    } else {
      return calculateTree167(features, 1066);
    }
  }
  if (node === 265) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree167(features, 531);
    } else if (val <= 144.50000000000003) {
      return calculateTree167(features, 531);
    } else {
      return calculateTree167(features, 532);
    }
  }
  if (node === 533) return -0.3323015699415922;
  if (node === 534) return 0.03242486744870679;
  if (node === 266) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree167(features, 533);
    } else if (val <= 33.50000000000001) {
      return calculateTree167(features, 533);
    } else {
      return calculateTree167(features, 534);
    }
  }
  if (node === 132) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree167(features, 265);
    } else if (val <= 15.500000000000002) {
      return calculateTree167(features, 265);
    } else {
      return calculateTree167(features, 266);
    }
  }
  if (node === 65) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree167(features, 131);
    } else if (val <= 288.50000000000006) {
      return calculateTree167(features, 131);
    } else {
      return calculateTree167(features, 132);
    }
  }
  if (node === 133) return -0.32762023911263555;
  if (node === 539) return -0.015849002518266906;
  if (node === 540) return -0.3245832445088014;
  if (node === 269) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree167(features, 539);
    } else if (val <= 288.50000000000006) {
      return calculateTree167(features, 539);
    } else {
      return calculateTree167(features, 540);
    }
  }
  if (node === 270) return 0.1709541339458436;
  if (node === 134) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree167(features, 269);
    } else if (val <= 859.5000000000001) {
      return calculateTree167(features, 269);
    } else {
      return calculateTree167(features, 270);
    }
  }
  if (node === 66) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree167(features, 133);
    } else if (val <= 141.50000000000003) {
      return calculateTree167(features, 133);
    } else {
      return calculateTree167(features, 134);
    }
  }
  if (node === 32) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree167(features, 65);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree167(features, 65);
    } else {
      return calculateTree167(features, 66);
    }
  }
  if (node === 15) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree167(features, 31);
    } else if (val <= 132.50000000000003) {
      return calculateTree167(features, 31);
    } else {
      return calculateTree167(features, 32);
    }
  }
  if (node === 67) return 0.035264259980067166;
  if (node === 68) return 0.27938403606062195;
  if (node === 33) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree167(features, 67);
    } else if (val <= 13.500000000000002) {
      return calculateTree167(features, 67);
    } else {
      return calculateTree167(features, 68);
    }
  }
  if (node === 34) return -0.2538026320935889;
  if (node === 16) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree167(features, 33);
    } else if (val <= 33.50000000000001) {
      return calculateTree167(features, 33);
    } else {
      return calculateTree167(features, 34);
    }
  }
  if (node === 7) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree167(features, 15);
    } else if (val <= 148.50000000000003) {
      return calculateTree167(features, 15);
    } else {
      return calculateTree167(features, 16);
    }
  }
  if (node === 71) return -0.04738861314878761;
  if (node === 145) return -0.19738070746951106;
  if (node === 293) return -0.20230637505725368;
  if (node === 294) return 0.2275145241305701;
  if (node === 146) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree167(features, 293);
    } else if (val <= 599.5000000000001) {
      return calculateTree167(features, 293);
    } else {
      return calculateTree167(features, 294);
    }
  }
  if (node === 72) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree167(features, 145);
    } else if (val <= 13.500000000000002) {
      return calculateTree167(features, 145);
    } else {
      return calculateTree167(features, 146);
    }
  }
  if (node === 35) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree167(features, 71);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree167(features, 71);
    } else {
      return calculateTree167(features, 72);
    }
  }
  if (node === 73) return -0.31524051082274707;
  if (node === 149) return 0.010051132687388232;
  if (node === 150) return -0.20511929610724852;
  if (node === 74) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree167(features, 149);
    } else if (val <= 22.500000000000004) {
      return calculateTree167(features, 149);
    } else {
      return calculateTree167(features, 150);
    }
  }
  if (node === 36) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree167(features, 73);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree167(features, 73);
    } else {
      return calculateTree167(features, 74);
    }
  }
  if (node === 17) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree167(features, 35);
    } else if (val <= 905.5000000000001) {
      return calculateTree167(features, 35);
    } else {
      return calculateTree167(features, 36);
    }
  }
  if (node === 75) return 0.2153605274369918;
  if (node === 76) return -0.04252233102149011;
  if (node === 37) {
    const val = features['is_preference_kids'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree167(features, 75);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree167(features, 75);
    } else {
      return calculateTree167(features, 76);
    }
  }
  if (node === 38) return -0.082562864598036;
  if (node === 18) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree167(features, 37);
    } else if (val <= 41.50000000000001) {
      return calculateTree167(features, 37);
    } else {
      return calculateTree167(features, 38);
    }
  }
  if (node === 8) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree167(features, 17);
    } else if (val <= 27.500000000000004) {
      return calculateTree167(features, 17);
    } else {
      return calculateTree167(features, 18);
    }
  }
  if (node === 3) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree167(features, 7);
    } else if (val <= 151.50000000000003) {
      return calculateTree167(features, 7);
    } else {
      return calculateTree167(features, 8);
    }
  }
  if (node === 39) return 0.006713390933220357;
  if (node === 327) return -0.042683757212471383;
  if (node === 328) return 0.32947674306615554;
  if (node === 163) {
    const val = features['is_preference_kitchen'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree167(features, 327);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree167(features, 327);
    } else {
      return calculateTree167(features, 328);
    }
  }
  if (node === 164) return -0.20392819153028227;
  if (node === 81) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree167(features, 163);
    } else if (val <= 125.50000000000001) {
      return calculateTree167(features, 163);
    } else {
      return calculateTree167(features, 164);
    }
  }
  if (node === 82) return -0.3375813356648235;
  if (node === 40) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree167(features, 81);
    } else if (val <= 774.0000000000001) {
      return calculateTree167(features, 81);
    } else {
      return calculateTree167(features, 82);
    }
  }
  if (node === 19) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree167(features, 39);
    } else if (val <= 960.0000000000001) {
      return calculateTree167(features, 39);
    } else {
      return calculateTree167(features, 40);
    }
  }
  if (node === 20) return -0.034739239812591305;
  if (node === 9) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree167(features, 19);
    } else if (val <= 977.5000000000001) {
      return calculateTree167(features, 19);
    } else {
      return calculateTree167(features, 20);
    }
  }
  if (node === 10) return -0.289661635486684;
  if (node === 4) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree167(features, 9);
    } else if (val <= 82.50000000000001) {
      return calculateTree167(features, 9);
    } else {
      return calculateTree167(features, 10);
    }
  }
  if (node === 1) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree167(features, 3);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree167(features, 3);
    } else {
      return calculateTree167(features, 4);
    }
  }
  if (node === 2) return 0.001668696466106505;
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree167(features, 1);
    } else if (val <= 172.50000000000003) {
      return calculateTree167(features, 1);
    } else {
      return calculateTree167(features, 2);
    }
  }
}
function calculateTree168(features, node = 0) {
  if (node === 3) return -0.0021384691972430696;
  if (node === 39) return -0.002297004853835806;
  if (node === 40) return -0.3421010598097984;
  if (node === 19) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree168(features, 39);
    } else if (val <= 661.0000000000001) {
      return calculateTree168(features, 39);
    } else {
      return calculateTree168(features, 40);
    }
  }
  if (node === 20) return 0.06417459497826931;
  if (node === 9) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree168(features, 19);
    } else if (val <= 325.00000000000006) {
      return calculateTree168(features, 19);
    } else {
      return calculateTree168(features, 20);
    }
  }
  if (node === 10) return -0.32387688392861425;
  if (node === 4) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree168(features, 9);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree168(features, 9);
    } else {
      return calculateTree168(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree168(features, 3);
    } else if (val <= 322.00000000000006) {
      return calculateTree168(features, 3);
    } else {
      return calculateTree168(features, 4);
    }
  }
  if (node === 47) return -0.14992896201346378;
  if (node === 48) return 0.28459197611251347;
  if (node === 23) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree168(features, 47);
    } else if (val <= 155.50000000000003) {
      return calculateTree168(features, 47);
    } else {
      return calculateTree168(features, 48);
    }
  }
  if (node === 24) return -0.40915714289417043;
  if (node === 11) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree168(features, 23);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree168(features, 23);
    } else {
      return calculateTree168(features, 24);
    }
  }
  if (node === 103) return -0.4086915423448842;
  if (node === 104) return 0.29818617438395995;
  if (node === 51) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree168(features, 103);
    } else if (val <= 675.0000000000001) {
      return calculateTree168(features, 103);
    } else {
      return calculateTree168(features, 104);
    }
  }
  if (node === 105) return 0.15365474240567256;
  if (node === 106) return -0.5387847631416862;
  if (node === 52) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree168(features, 105);
    } else if (val <= 675.0000000000001) {
      return calculateTree168(features, 105);
    } else {
      return calculateTree168(features, 106);
    }
  }
  if (node === 25) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree168(features, 51);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree168(features, 51);
    } else {
      return calculateTree168(features, 52);
    }
  }
  if (node === 26) return -0.13816366942197392;
  if (node === 12) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree168(features, 25);
    } else if (val <= 8.500000000000002) {
      return calculateTree168(features, 25);
    } else {
      return calculateTree168(features, 26);
    }
  }
  if (node === 5) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree168(features, 11);
    } else if (val <= 348.50000000000006) {
      return calculateTree168(features, 11);
    } else {
      return calculateTree168(features, 12);
    }
  }
  if (node === 223) return 0.01936515736542853;
  if (node === 224) return 0.22353305666877013;
  if (node === 111) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree168(features, 223);
    } else if (val <= 6.500000000000001) {
      return calculateTree168(features, 223);
    } else {
      return calculateTree168(features, 224);
    }
  }
  if (node === 112) return -0.20030082000752505;
  if (node === 55) {
    const val = features['is_preference_sports'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree168(features, 111);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree168(features, 111);
    } else {
      return calculateTree168(features, 112);
    }
  }
  if (node === 56) return -0.2927707158663334;
  if (node === 27) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree168(features, 55);
    } else if (val <= 507.00000000000006) {
      return calculateTree168(features, 55);
    } else {
      return calculateTree168(features, 56);
    }
  }
  if (node === 57) return -0.11136175874573158;
  if (node === 58) return -0.32416521614617827;
  if (node === 28) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree168(features, 57);
    } else if (val <= 240.00000000000003) {
      return calculateTree168(features, 57);
    } else {
      return calculateTree168(features, 58);
    }
  }
  if (node === 13) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree168(features, 27);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree168(features, 27);
    } else {
      return calculateTree168(features, 28);
    }
  }
  if (node === 59) return -0.3820861698988521;
  if (node === 1951) return 0.002179455490976563;
  if (node === 1952) return -0.2191961161267046;
  if (node === 975) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree168(features, 1951);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree168(features, 1951);
    } else {
      return calculateTree168(features, 1952);
    }
  }
  if (node === 976) return 0.02914646657914687;
  if (node === 487) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree168(features, 975);
    } else if (val <= 181.50000000000003) {
      return calculateTree168(features, 975);
    } else {
      return calculateTree168(features, 976);
    }
  }
  if (node === 977) return -0.27834509941968627;
  if (node === 978) return -0.04343589323500041;
  if (node === 488) {
    const val = features['is_preference_home'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree168(features, 977);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree168(features, 977);
    } else {
      return calculateTree168(features, 978);
    }
  }
  if (node === 243) {
    const val = features['is_preference_beauty'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree168(features, 487);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree168(features, 487);
    } else {
      return calculateTree168(features, 488);
    }
  }
  if (node === 1959) return 0.014812865309818776;
  if (node === 1960) return 0.3364056304529448;
  if (node === 979) {
    const val = features['is_preference_kids'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree168(features, 1959);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree168(features, 1959);
    } else {
      return calculateTree168(features, 1960);
    }
  }
  if (node === 1961) return -0.24990102242014567;
  if (node === 1962) return 0.046513632487082714;
  if (node === 980) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree168(features, 1961);
    } else if (val <= 197.50000000000003) {
      return calculateTree168(features, 1961);
    } else {
      return calculateTree168(features, 1962);
    }
  }
  if (node === 489) {
    const val = features['is_preference_pets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree168(features, 979);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree168(features, 979);
    } else {
      return calculateTree168(features, 980);
    }
  }
  if (node === 490) return 0.14387798707576602;
  if (node === 244) {
    const val = features['is_preference_men_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree168(features, 489);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree168(features, 489);
    } else {
      return calculateTree168(features, 490);
    }
  }
  if (node === 121) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree168(features, 243);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree168(features, 243);
    } else {
      return calculateTree168(features, 244);
    }
  }
  if (node === 122) return 0.24792852139360375;
  if (node === 60) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree168(features, 121);
    } else if (val <= 941.5000000000001) {
      return calculateTree168(features, 121);
    } else {
      return calculateTree168(features, 122);
    }
  }
  if (node === 29) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree168(features, 59);
    } else if (val <= 113.50000000000001) {
      return calculateTree168(features, 59);
    } else {
      return calculateTree168(features, 60);
    }
  }
  if (node === 30) return -0.0005608741934555098;
  if (node === 14) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree168(features, 29);
    } else if (val <= 383.50000000000006) {
      return calculateTree168(features, 29);
    } else {
      return calculateTree168(features, 30);
    }
  }
  if (node === 6) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree168(features, 13);
    } else if (val <= 350.50000000000006) {
      return calculateTree168(features, 13);
    } else {
      return calculateTree168(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree168(features, 5);
    } else if (val <= 338.50000000000006) {
      return calculateTree168(features, 5);
    } else {
      return calculateTree168(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree168(features, 1);
    } else if (val <= 331.50000000000006) {
      return calculateTree168(features, 1);
    } else {
      return calculateTree168(features, 2);
    }
  }
}
function calculateTree169(features, node = 0) {
  if (node === 31) return 0.012179164751788143;
  if (node === 32) return 0.15775949363259598;
  if (node === 15) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree169(features, 31);
    } else if (val <= 27.500000000000004) {
      return calculateTree169(features, 31);
    } else {
      return calculateTree169(features, 32);
    }
  }
  if (node === 67) return -0.14799830484849114;
  if (node === 137) return 0.301156073641489;
  if (node === 138) return -0.10856184762248852;
  if (node === 68) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree169(features, 137);
    } else if (val <= 4.500000000000001) {
      return calculateTree169(features, 137);
    } else {
      return calculateTree169(features, 138);
    }
  }
  if (node === 33) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree169(features, 67);
    } else if (val <= 17.500000000000004) {
      return calculateTree169(features, 67);
    } else {
      return calculateTree169(features, 68);
    }
  }
  if (node === 34) return -0.2292694068184902;
  if (node === 16) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree169(features, 33);
    } else if (val <= 33.50000000000001) {
      return calculateTree169(features, 33);
    } else {
      return calculateTree169(features, 34);
    }
  }
  if (node === 7) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree169(features, 15);
    } else if (val <= 695.5000000000001) {
      return calculateTree169(features, 15);
    } else {
      return calculateTree169(features, 16);
    }
  }
  if (node === 143) return 0.37255652892856483;
  if (node === 579) return 0.12748615205813324;
  if (node === 580) return -0.018153000751001562;
  if (node === 289) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree169(features, 579);
    } else if (val <= 803.0000000000001) {
      return calculateTree169(features, 579);
    } else {
      return calculateTree169(features, 580);
    }
  }
  if (node === 290) return 0.25174600206423625;
  if (node === 144) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree169(features, 289);
    } else if (val <= 1.5000000000000002) {
      return calculateTree169(features, 289);
    } else {
      return calculateTree169(features, 290);
    }
  }
  if (node === 71) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree169(features, 143);
    } else if (val <= 1.5000000000000002) {
      return calculateTree169(features, 143);
    } else {
      return calculateTree169(features, 144);
    }
  }
  if (node === 72) return -0.14214037990659273;
  if (node === 35) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree169(features, 71);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree169(features, 71);
    } else {
      return calculateTree169(features, 72);
    }
  }
  if (node === 36) return 0.5080178373750746;
  if (node === 17) {
    const val = features['is_preference_grocery'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree169(features, 35);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree169(features, 35);
    } else {
      return calculateTree169(features, 36);
    }
  }
  if (node === 18) return -0.16295370520527494;
  if (node === 8) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree169(features, 17);
    } else if (val <= 34.50000000000001) {
      return calculateTree169(features, 17);
    } else {
      return calculateTree169(features, 18);
    }
  }
  if (node === 3) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree169(features, 7);
    } else if (val <= 705.5000000000001) {
      return calculateTree169(features, 7);
    } else {
      return calculateTree169(features, 8);
    }
  }
  if (node === 19) return -0.06423833657769296;
  if (node === 20) return -0.31523384501628515;
  if (node === 9) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree169(features, 19);
    } else if (val <= 470.00000000000006) {
      return calculateTree169(features, 19);
    } else {
      return calculateTree169(features, 20);
    }
  }
  if (node === 21) return -0.16055421608711096;
  if (node === 22) return 0.13239957784020182;
  if (node === 10) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree169(features, 21);
    } else if (val <= 660.0000000000001) {
      return calculateTree169(features, 21);
    } else {
      return calculateTree169(features, 22);
    }
  }
  if (node === 4) {
    const val = features['is_preference_household'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree169(features, 9);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree169(features, 9);
    } else {
      return calculateTree169(features, 10);
    }
  }
  if (node === 1) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree169(features, 3);
    } else if (val <= 29.500000000000004) {
      return calculateTree169(features, 3);
    } else {
      return calculateTree169(features, 4);
    }
  }
  if (node === 5) return -0.0012892796509812302;
  if (node === 111) return 0.17874366101916536;
  if (node === 451) return 0.00918869692619902;
  if (node === 452) return 0.2831719077278293;
  if (node === 225) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree169(features, 451);
    } else if (val <= 36.50000000000001) {
      return calculateTree169(features, 451);
    } else {
      return calculateTree169(features, 452);
    }
  }
  if (node === 226) return -0.09448236264967894;
  if (node === 112) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree169(features, 225);
    } else if (val <= 42.50000000000001) {
      return calculateTree169(features, 225);
    } else {
      return calculateTree169(features, 226);
    }
  }
  if (node === 55) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree169(features, 111);
    } else if (val <= 1.5000000000000002) {
      return calculateTree169(features, 111);
    } else {
      return calculateTree169(features, 112);
    }
  }
  if (node === 56) return -0.25065952888102583;
  if (node === 27) {
    const val = features['is_preference_kitchen'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree169(features, 55);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree169(features, 55);
    } else {
      return calculateTree169(features, 56);
    }
  }
  if (node === 115) return 0.11789109035333678;
  if (node === 116) return 0.29948663943138015;
  if (node === 57) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree169(features, 115);
    } else if (val <= 9.500000000000002) {
      return calculateTree169(features, 115);
    } else {
      return calculateTree169(features, 116);
    }
  }
  if (node === 58) return -0.13308532972829598;
  if (node === 28) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree169(features, 57);
    } else if (val <= 22.500000000000004) {
      return calculateTree169(features, 57);
    } else {
      return calculateTree169(features, 58);
    }
  }
  if (node === 13) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree169(features, 27);
    } else if (val <= 6.500000000000001) {
      return calculateTree169(features, 27);
    } else {
      return calculateTree169(features, 28);
    }
  }
  if (node === 29) return 0.013894278682604356;
  if (node === 61) return -0.3375476246213264;
  if (node === 125) return -0.32951436764943354;
  if (node === 253) return 0.23409048322305737;
  if (node === 509) return -0.28420859228505896;
  if (node === 510) return 0.0027533777866155912;
  if (node === 254) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree169(features, 509);
    } else if (val <= 283.50000000000006) {
      return calculateTree169(features, 509);
    } else {
      return calculateTree169(features, 510);
    }
  }
  if (node === 126) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree169(features, 253);
    } else if (val <= 181.50000000000003) {
      return calculateTree169(features, 253);
    } else {
      return calculateTree169(features, 254);
    }
  }
  if (node === 62) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree169(features, 125);
    } else if (val <= 178.50000000000003) {
      return calculateTree169(features, 125);
    } else {
      return calculateTree169(features, 126);
    }
  }
  if (node === 30) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree169(features, 61);
    } else if (val <= 960.0000000000001) {
      return calculateTree169(features, 61);
    } else {
      return calculateTree169(features, 62);
    }
  }
  if (node === 14) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree169(features, 29);
    } else if (val <= 175.50000000000003) {
      return calculateTree169(features, 29);
    } else {
      return calculateTree169(features, 30);
    }
  }
  if (node === 6) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree169(features, 13);
    } else if (val <= 955.5000000000001) {
      return calculateTree169(features, 13);
    } else {
      return calculateTree169(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree169(features, 5);
    } else if (val <= 954.5000000000001) {
      return calculateTree169(features, 5);
    } else {
      return calculateTree169(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree169(features, 1);
    } else if (val <= 18.500000000000004) {
      return calculateTree169(features, 1);
    } else {
      return calculateTree169(features, 2);
    }
  }
}
function calculateTree170(features, node = 0) {
  if (node === 1) return -0.0005807118291584927;
  if (node === 95) return -0.1543285655359343;
  if (node === 96) return 0.0312718138081115;
  if (node === 47) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree170(features, 95);
    } else if (val <= 8.500000000000002) {
      return calculateTree170(features, 95);
    } else {
      return calculateTree170(features, 96);
    }
  }
  if (node === 97) return 0.024283720570743704;
  if (node === 197) return -0.016485486591571445;
  if (node === 795) return -0.019157497788820397;
  if (node === 1593) return 0.0805576188135083;
  if (node === 1594) return -0.085837539440128;
  if (node === 796) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree170(features, 1593);
    } else if (val <= 932.5000000000001) {
      return calculateTree170(features, 1593);
    } else {
      return calculateTree170(features, 1594);
    }
  }
  if (node === 397) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree170(features, 795);
    } else if (val <= 181.50000000000003) {
      return calculateTree170(features, 795);
    } else {
      return calculateTree170(features, 796);
    }
  }
  if (node === 1595) return 0.10515953190565801;
  if (node === 1596) return -0.16728289066955151;
  if (node === 797) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree170(features, 1595);
    } else if (val <= 485.50000000000006) {
      return calculateTree170(features, 1595);
    } else {
      return calculateTree170(features, 1596);
    }
  }
  if (node === 1597) return -0.2358819115093338;
  if (node === 1598) return -0.026805020133876183;
  if (node === 798) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree170(features, 1597);
    } else if (val <= 112.00000000000001) {
      return calculateTree170(features, 1597);
    } else {
      return calculateTree170(features, 1598);
    }
  }
  if (node === 398) {
    const val = features['is_preference_men_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree170(features, 797);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree170(features, 797);
    } else {
      return calculateTree170(features, 798);
    }
  }
  if (node === 198) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree170(features, 397);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree170(features, 397);
    } else {
      return calculateTree170(features, 398);
    }
  }
  if (node === 98) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree170(features, 197);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree170(features, 197);
    } else {
      return calculateTree170(features, 198);
    }
  }
  if (node === 48) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree170(features, 97);
    } else if (val <= 5.500000000000001) {
      return calculateTree170(features, 97);
    } else {
      return calculateTree170(features, 98);
    }
  }
  if (node === 23) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree170(features, 47);
    } else if (val <= 12.000000000000002) {
      return calculateTree170(features, 47);
    } else {
      return calculateTree170(features, 48);
    }
  }
  if (node === 49) return -0.22572745364583574;
  if (node === 50) return 0.2252987441221982;
  if (node === 24) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree170(features, 49);
    } else if (val <= 17.500000000000004) {
      return calculateTree170(features, 49);
    } else {
      return calculateTree170(features, 50);
    }
  }
  if (node === 11) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree170(features, 23);
    } else if (val <= 662.5000000000001) {
      return calculateTree170(features, 23);
    } else {
      return calculateTree170(features, 24);
    }
  }
  if (node === 51) return 0.17707807186335423;
  if (node === 52) return 0.4869036624785305;
  if (node === 25) {
    const val = features['is_preference_pets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree170(features, 51);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree170(features, 51);
    } else {
      return calculateTree170(features, 52);
    }
  }
  if (node === 26) return 0.020903116356748425;
  if (node === 12) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree170(features, 25);
    } else if (val <= 320.00000000000006) {
      return calculateTree170(features, 25);
    } else {
      return calculateTree170(features, 26);
    }
  }
  if (node === 5) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree170(features, 11);
    } else if (val <= 670.5000000000001) {
      return calculateTree170(features, 11);
    } else {
      return calculateTree170(features, 12);
    }
  }
  if (node === 13) return -0.24424945992568448;
  if (node === 59) return 0.07374765131853654;
  if (node === 60) return -0.09212345651207131;
  if (node === 29) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree170(features, 59);
    } else if (val <= 22.500000000000004) {
      return calculateTree170(features, 59);
    } else {
      return calculateTree170(features, 60);
    }
  }
  if (node === 123) return -0.358044771142447;
  if (node === 249) return -0.15413811017538417;
  if (node === 250) return 0.07013626226301511;
  if (node === 124) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree170(features, 249);
    } else if (val <= 899.5000000000001) {
      return calculateTree170(features, 249);
    } else {
      return calculateTree170(features, 250);
    }
  }
  if (node === 61) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree170(features, 123);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree170(features, 123);
    } else {
      return calculateTree170(features, 124);
    }
  }
  if (node === 503) return 0.08957293760306052;
  if (node === 2019) return -0.27944328851809785;
  if (node === 2020) return -0.09228021974443647;
  if (node === 1009) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree170(features, 2019);
    } else if (val <= 381.50000000000006) {
      return calculateTree170(features, 2019);
    } else {
      return calculateTree170(features, 2020);
    }
  }
  if (node === 2021) return 0.00032808879844405976;
  if (node === 2022) return -0.13727895109182003;
  if (node === 1010) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree170(features, 2021);
    } else if (val <= 57.50000000000001) {
      return calculateTree170(features, 2021);
    } else {
      return calculateTree170(features, 2022);
    }
  }
  if (node === 504) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree170(features, 1009);
    } else if (val <= 816.0000000000001) {
      return calculateTree170(features, 1009);
    } else {
      return calculateTree170(features, 1010);
    }
  }
  if (node === 251) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree170(features, 503);
    } else if (val <= 130.00000000000003) {
      return calculateTree170(features, 503);
    } else {
      return calculateTree170(features, 504);
    }
  }
  if (node === 1011) return -0.004596995136756664;
  if (node === 1012) return 0.06412481357835167;
  if (node === 505) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree170(features, 1011);
    } else if (val <= 857.5000000000001) {
      return calculateTree170(features, 1011);
    } else {
      return calculateTree170(features, 1012);
    }
  }
  if (node === 506) return -0.0638540214949355;
  if (node === 252) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree170(features, 505);
    } else if (val <= 9.500000000000002) {
      return calculateTree170(features, 505);
    } else {
      return calculateTree170(features, 506);
    }
  }
  if (node === 125) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree170(features, 251);
    } else if (val <= 1.5000000000000002) {
      return calculateTree170(features, 251);
    } else {
      return calculateTree170(features, 252);
    }
  }
  if (node === 126) return 0.02472581558312158;
  if (node === 62) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree170(features, 125);
    } else if (val <= 15.500000000000002) {
      return calculateTree170(features, 125);
    } else {
      return calculateTree170(features, 126);
    }
  }
  if (node === 30) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree170(features, 61);
    } else if (val <= 82.50000000000001) {
      return calculateTree170(features, 61);
    } else {
      return calculateTree170(features, 62);
    }
  }
  if (node === 14) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree170(features, 29);
    } else if (val <= 39.50000000000001) {
      return calculateTree170(features, 29);
    } else {
      return calculateTree170(features, 30);
    }
  }
  if (node === 6) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree170(features, 13);
    } else if (val <= 677.5000000000001) {
      return calculateTree170(features, 13);
    } else {
      return calculateTree170(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree170(features, 5);
    } else if (val <= 675.5000000000001) {
      return calculateTree170(features, 5);
    } else {
      return calculateTree170(features, 6);
    }
  }
  if (node === 0) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree170(features, 1);
    } else if (val <= 44.50000000000001) {
      return calculateTree170(features, 1);
    } else {
      return calculateTree170(features, 2);
    }
  }
}
function calculateTree171(features, node = 0) {
  if (node === 1) return -0.0008028539484161702;
  if (node === 95) return -0.08273621411395772;
  if (node === 96) return -0.3254475834223534;
  if (node === 47) {
    const val = features['is_preference_personal_care'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree171(features, 95);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree171(features, 95);
    } else {
      return calculateTree171(features, 96);
    }
  }
  if (node === 48) return -0.023614440771032422;
  if (node === 23) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree171(features, 47);
    } else if (val <= 25.500000000000004) {
      return calculateTree171(features, 47);
    } else {
      return calculateTree171(features, 48);
    }
  }
  if (node === 799) return 0.2432344660183238;
  if (node === 800) return 0.02406269321236839;
  if (node === 399) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree171(features, 799);
    } else if (val <= 26.500000000000004) {
      return calculateTree171(features, 799);
    } else {
      return calculateTree171(features, 800);
    }
  }
  if (node === 801) return 0.021700542155377086;
  if (node === 802) return -0.09668496156354098;
  if (node === 400) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree171(features, 801);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree171(features, 801);
    } else {
      return calculateTree171(features, 802);
    }
  }
  if (node === 199) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree171(features, 399);
    } else if (val <= 181.50000000000003) {
      return calculateTree171(features, 399);
    } else {
      return calculateTree171(features, 400);
    }
  }
  if (node === 200) return 0.23996618469503714;
  if (node === 99) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree171(features, 199);
    } else if (val <= 607.5000000000001) {
      return calculateTree171(features, 199);
    } else {
      return calculateTree171(features, 200);
    }
  }
  if (node === 201) return -0.19213354483587844;
  if (node === 202) return 0.12828125320193515;
  if (node === 100) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree171(features, 201);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree171(features, 201);
    } else {
      return calculateTree171(features, 202);
    }
  }
  if (node === 49) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree171(features, 99);
    } else if (val <= 189.50000000000003) {
      return calculateTree171(features, 99);
    } else {
      return calculateTree171(features, 100);
    }
  }
  if (node === 203) return -0.29426795518515775;
  if (node === 204) return -0.09558064734220985;
  if (node === 101) {
    const val = features['is_preference_women_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree171(features, 203);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree171(features, 203);
    } else {
      return calculateTree171(features, 204);
    }
  }
  if (node === 102) return -0.012038715501842141;
  if (node === 50) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree171(features, 101);
    } else if (val <= 245.00000000000003) {
      return calculateTree171(features, 101);
    } else {
      return calculateTree171(features, 102);
    }
  }
  if (node === 24) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree171(features, 49);
    } else if (val <= 212.50000000000003) {
      return calculateTree171(features, 49);
    } else {
      return calculateTree171(features, 50);
    }
  }
  if (node === 11) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree171(features, 23);
    } else if (val <= 20.500000000000004) {
      return calculateTree171(features, 23);
    } else {
      return calculateTree171(features, 24);
    }
  }
  if (node === 25) return 0.06782413904767441;
  if (node === 53) return -0.2742525662232898;
  if (node === 54) return -0.11393446746251644;
  if (node === 26) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree171(features, 53);
    } else if (val <= 398.00000000000006) {
      return calculateTree171(features, 53);
    } else {
      return calculateTree171(features, 54);
    }
  }
  if (node === 12) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree171(features, 25);
    } else if (val <= 26.500000000000004) {
      return calculateTree171(features, 25);
    } else {
      return calculateTree171(features, 26);
    }
  }
  if (node === 5) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree171(features, 11);
    } else if (val <= 665.0000000000001) {
      return calculateTree171(features, 11);
    } else {
      return calculateTree171(features, 12);
    }
  }
  if (node === 55) return 0.17486417490066952;
  if (node === 56) return 0.017786471894084728;
  if (node === 27) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree171(features, 55);
    } else if (val <= 690.5000000000001) {
      return calculateTree171(features, 55);
    } else {
      return calculateTree171(features, 56);
    }
  }
  if (node === 57) return 0.07651132816467067;
  if (node === 58) return 0.34961622562164424;
  if (node === 28) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree171(features, 57);
    } else if (val <= 891.0000000000001) {
      return calculateTree171(features, 57);
    } else {
      return calculateTree171(features, 58);
    }
  }
  if (node === 13) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree171(features, 27);
    } else if (val <= 898.5000000000001) {
      return calculateTree171(features, 27);
    } else {
      return calculateTree171(features, 28);
    }
  }
  if (node === 119) return 0.046530007629425496;
  if (node === 120) return -0.3105648320670731;
  if (node === 59) {
    const val = features['is_preference_home'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree171(features, 119);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree171(features, 119);
    } else {
      return calculateTree171(features, 120);
    }
  }
  if (node === 121) return -0.38044983651003417;
  if (node === 245) return -0.29498594846189535;
  if (node === 246) return -0.05205895567274312;
  if (node === 122) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree171(features, 245);
    } else if (val <= 35.50000000000001) {
      return calculateTree171(features, 245);
    } else {
      return calculateTree171(features, 246);
    }
  }
  if (node === 60) {
    const val = features['is_preference_household'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree171(features, 121);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree171(features, 121);
    } else {
      return calculateTree171(features, 122);
    }
  }
  if (node === 29) {
    const val = features['is_preference_kitchen'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree171(features, 59);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree171(features, 59);
    } else {
      return calculateTree171(features, 60);
    }
  }
  if (node === 123) return -0.11818873000253043;
  if (node === 249) return 0.12408751753920347;
  if (node === 1003) return -0.019205228268080726;
  if (node === 1004) return 0.17733432259150558;
  if (node === 501) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree171(features, 1003);
    } else if (val <= 847.0000000000001) {
      return calculateTree171(features, 1003);
    } else {
      return calculateTree171(features, 1004);
    }
  }
  if (node === 502) return -0.4138885630551097;
  if (node === 250) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree171(features, 501);
    } else if (val <= 606.5000000000001) {
      return calculateTree171(features, 501);
    } else {
      return calculateTree171(features, 502);
    }
  }
  if (node === 124) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree171(features, 249);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree171(features, 249);
    } else {
      return calculateTree171(features, 250);
    }
  }
  if (node === 61) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree171(features, 123);
    } else if (val <= 25.500000000000004) {
      return calculateTree171(features, 123);
    } else {
      return calculateTree171(features, 124);
    }
  }
  if (node === 62) return 0.1636976993285073;
  if (node === 30) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree171(features, 61);
    } else if (val <= 905.5000000000001) {
      return calculateTree171(features, 61);
    } else {
      return calculateTree171(features, 62);
    }
  }
  if (node === 14) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree171(features, 29);
    } else if (val <= 948.0000000000001) {
      return calculateTree171(features, 29);
    } else {
      return calculateTree171(features, 30);
    }
  }
  if (node === 6) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree171(features, 13);
    } else if (val <= 906.0000000000001) {
      return calculateTree171(features, 13);
    } else {
      return calculateTree171(features, 14);
    }
  }
  if (node === 2) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree171(features, 5);
    } else if (val <= 682.5000000000001) {
      return calculateTree171(features, 5);
    } else {
      return calculateTree171(features, 6);
    }
  }
  if (node === 0) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree171(features, 1);
    } else if (val <= 22.500000000000004) {
      return calculateTree171(features, 1);
    } else {
      return calculateTree171(features, 2);
    }
  }
}
function calculateTree172(features, node = 0) {
  if (node === 3) return 0.000839114571651633;
  if (node === 79) return 0.007410199133930608;
  if (node === 323) return -0.05825222740477001;
  if (node === 324) return 0.18675216316988338;
  if (node === 161) {
    const val = features['is_preference_personal_care'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree172(features, 323);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree172(features, 323);
    } else {
      return calculateTree172(features, 324);
    }
  }
  if (node === 325) return -0.1993172543891575;
  if (node === 326) return 0.07719073822269719;
  if (node === 162) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree172(features, 325);
    } else if (val <= 25.500000000000004) {
      return calculateTree172(features, 325);
    } else {
      return calculateTree172(features, 326);
    }
  }
  if (node === 80) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree172(features, 161);
    } else if (val <= 588.5000000000001) {
      return calculateTree172(features, 161);
    } else {
      return calculateTree172(features, 162);
    }
  }
  if (node === 39) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree172(features, 79);
    } else if (val <= 13.500000000000002) {
      return calculateTree172(features, 79);
    } else {
      return calculateTree172(features, 80);
    }
  }
  if (node === 81) return -0.11999363346129732;
  if (node === 165) return 0.1904096574423824;
  if (node === 333) return -0.057435294822034;
  if (node === 1339) return 0.1378183115568706;
  if (node === 1340) return 0.42511979557852225;
  if (node === 669) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree172(features, 1339);
    } else if (val <= 888.5000000000001) {
      return calculateTree172(features, 1339);
    } else {
      return calculateTree172(features, 1340);
    }
  }
  if (node === 1341) return -0.22340566687218788;
  if (node === 1342) return 0.0641046773036419;
  if (node === 670) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree172(features, 1341);
    } else if (val <= 1.5000000000000002) {
      return calculateTree172(features, 1341);
    } else {
      return calculateTree172(features, 1342);
    }
  }
  if (node === 334) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree172(features, 669);
    } else if (val <= 606.5000000000001) {
      return calculateTree172(features, 669);
    } else {
      return calculateTree172(features, 670);
    }
  }
  if (node === 166) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree172(features, 333);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree172(features, 333);
    } else {
      return calculateTree172(features, 334);
    }
  }
  if (node === 82) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree172(features, 165);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree172(features, 165);
    } else {
      return calculateTree172(features, 166);
    }
  }
  if (node === 40) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree172(features, 81);
    } else if (val <= 580.0000000000001) {
      return calculateTree172(features, 81);
    } else {
      return calculateTree172(features, 82);
    }
  }
  if (node === 19) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree172(features, 39);
    } else if (val <= 2.5000000000000004) {
      return calculateTree172(features, 39);
    } else {
      return calculateTree172(features, 40);
    }
  }
  if (node === 167) return 0.40722569057398333;
  if (node === 337) return 0.10009121709437506;
  if (node === 338) return 0.41758230208242336;
  if (node === 168) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree172(features, 337);
    } else if (val <= 540.0000000000001) {
      return calculateTree172(features, 337);
    } else {
      return calculateTree172(features, 338);
    }
  }
  if (node === 83) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree172(features, 167);
    } else if (val <= 1.5000000000000002) {
      return calculateTree172(features, 167);
    } else {
      return calculateTree172(features, 168);
    }
  }
  if (node === 84) return -0.24874995926395804;
  if (node === 41) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree172(features, 83);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree172(features, 83);
    } else {
      return calculateTree172(features, 84);
    }
  }
  if (node === 171) return -0.31663327749442066;
  if (node === 345) return 0.006403754751370174;
  if (node === 346) return 0.11996295183035266;
  if (node === 172) {
    const val = features['is_preference_kitchen'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree172(features, 345);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree172(features, 345);
    } else {
      return calculateTree172(features, 346);
    }
  }
  if (node === 85) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree172(features, 171);
    } else if (val <= 580.0000000000001) {
      return calculateTree172(features, 171);
    } else {
      return calculateTree172(features, 172);
    }
  }
  if (node === 695) return 0.0827008267631288;
  if (node === 696) return -0.1405036233956253;
  if (node === 347) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree172(features, 695);
    } else if (val <= 50.50000000000001) {
      return calculateTree172(features, 695);
    } else {
      return calculateTree172(features, 696);
    }
  }
  if (node === 348) return -0.32695961342189034;
  if (node === 173) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree172(features, 347);
    } else if (val <= 606.5000000000001) {
      return calculateTree172(features, 347);
    } else {
      return calculateTree172(features, 348);
    }
  }
  if (node === 174) return 0.1507821998933702;
  if (node === 86) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree172(features, 173);
    } else if (val <= 607.5000000000001) {
      return calculateTree172(features, 173);
    } else {
      return calculateTree172(features, 174);
    }
  }
  if (node === 42) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree172(features, 85);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree172(features, 85);
    } else {
      return calculateTree172(features, 86);
    }
  }
  if (node === 20) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree172(features, 41);
    } else if (val <= 575.5000000000001) {
      return calculateTree172(features, 41);
    } else {
      return calculateTree172(features, 42);
    }
  }
  if (node === 9) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree172(features, 19);
    } else if (val <= 30.500000000000004) {
      return calculateTree172(features, 19);
    } else {
      return calculateTree172(features, 20);
    }
  }
  if (node === 87) return 0.026922280756335352;
  if (node === 88) return -0.12366776939757473;
  if (node === 43) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree172(features, 87);
    } else if (val <= 599.0000000000001) {
      return calculateTree172(features, 87);
    } else {
      return calculateTree172(features, 88);
    }
  }
  if (node === 89) return -0.40696316395422083;
  if (node === 181) return -0.22624231798603184;
  if (node === 182) return 0.06210064851776042;
  if (node === 90) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree172(features, 181);
    } else if (val <= 607.5000000000001) {
      return calculateTree172(features, 181);
    } else {
      return calculateTree172(features, 182);
    }
  }
  if (node === 44) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree172(features, 89);
    } else if (val <= 15.500000000000002) {
      return calculateTree172(features, 89);
    } else {
      return calculateTree172(features, 90);
    }
  }
  if (node === 21) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree172(features, 43);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree172(features, 43);
    } else {
      return calculateTree172(features, 44);
    }
  }
  if (node === 45) return 0.2012913766757818;
  if (node === 46) return -0.14838699957201013;
  if (node === 22) {
    const val = features['is_preference_sports'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree172(features, 45);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree172(features, 45);
    } else {
      return calculateTree172(features, 46);
    }
  }
  if (node === 10) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree172(features, 21);
    } else if (val <= 1.5000000000000002) {
      return calculateTree172(features, 21);
    } else {
      return calculateTree172(features, 22);
    }
  }
  if (node === 4) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree172(features, 9);
    } else if (val <= 13.500000000000002) {
      return calculateTree172(features, 9);
    } else {
      return calculateTree172(features, 10);
    }
  }
  if (node === 1) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree172(features, 3);
    } else if (val <= 563.5000000000001) {
      return calculateTree172(features, 3);
    } else {
      return calculateTree172(features, 4);
    }
  }
  if (node === 2) return -0.0012354178335644088;
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree172(features, 1);
    } else if (val <= 611.0000000000001) {
      return calculateTree172(features, 1);
    } else {
      return calculateTree172(features, 2);
    }
  }
}
function calculateTree173(features, node = 0) {
  if (node === 3) return 6.502392674528108e-5;
  if (node === 79) return 0.15924802815520392;
  if (node === 161) return -0.339524985638401;
  if (node === 651) return 0.20389852239242903;
  if (node === 652) return -0.019582007117102337;
  if (node === 325) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree173(features, 651);
    } else if (val <= 253.50000000000003) {
      return calculateTree173(features, 651);
    } else {
      return calculateTree173(features, 652);
    }
  }
  if (node === 326) return 0.18880145088473504;
  if (node === 162) {
    const val = features['is_preference_women_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree173(features, 325);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree173(features, 325);
    } else {
      return calculateTree173(features, 326);
    }
  }
  if (node === 80) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree173(features, 161);
    } else if (val <= 222.50000000000003) {
      return calculateTree173(features, 161);
    } else {
      return calculateTree173(features, 162);
    }
  }
  if (node === 39) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree173(features, 79);
    } else if (val <= 143.50000000000003) {
      return calculateTree173(features, 79);
    } else {
      return calculateTree173(features, 80);
    }
  }
  if (node === 81) return -0.2704622586696154;
  if (node === 165) return 0.7562431442558673;
  if (node === 166) return -0.17913138258005176;
  if (node === 82) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree173(features, 165);
    } else if (val <= 135.50000000000003) {
      return calculateTree173(features, 165);
    } else {
      return calculateTree173(features, 166);
    }
  }
  if (node === 40) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree173(features, 81);
    } else if (val <= 2.5000000000000004) {
      return calculateTree173(features, 81);
    } else {
      return calculateTree173(features, 82);
    }
  }
  if (node === 19) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree173(features, 39);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree173(features, 39);
    } else {
      return calculateTree173(features, 40);
    }
  }
  if (node === 167) return 0.009804454247900747;
  if (node === 168) return 0.09481812858757252;
  if (node === 83) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree173(features, 167);
    } else if (val <= 21.500000000000004) {
      return calculateTree173(features, 167);
    } else {
      return calculateTree173(features, 168);
    }
  }
  if (node === 339) return -0.4933392408254032;
  if (node === 340) return 0.020764132225256405;
  if (node === 169) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree173(features, 339);
    } else if (val <= 3.5000000000000004) {
      return calculateTree173(features, 339);
    } else {
      return calculateTree173(features, 340);
    }
  }
  if (node === 683) return 0.37876809637166015;
  if (node === 684) return -0.038782523585952564;
  if (node === 341) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree173(features, 683);
    } else if (val <= 688.5000000000001) {
      return calculateTree173(features, 683);
    } else {
      return calculateTree173(features, 684);
    }
  }
  if (node === 1371) return 0.14054684414469426;
  if (node === 1372) return -0.14773609255631162;
  if (node === 685) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree173(features, 1371);
    } else if (val <= 12.500000000000002) {
      return calculateTree173(features, 1371);
    } else {
      return calculateTree173(features, 1372);
    }
  }
  if (node === 1373) return -0.08931816885724403;
  if (node === 1374) return -0.553318882843239;
  if (node === 686) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree173(features, 1373);
    } else if (val <= 498.00000000000006) {
      return calculateTree173(features, 1373);
    } else {
      return calculateTree173(features, 1374);
    }
  }
  if (node === 342) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree173(features, 685);
    } else if (val <= 1.5000000000000002) {
      return calculateTree173(features, 685);
    } else {
      return calculateTree173(features, 686);
    }
  }
  if (node === 170) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree173(features, 341);
    } else if (val <= 22.500000000000004) {
      return calculateTree173(features, 341);
    } else {
      return calculateTree173(features, 342);
    }
  }
  if (node === 84) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree173(features, 169);
    } else if (val <= 14.500000000000002) {
      return calculateTree173(features, 169);
    } else {
      return calculateTree173(features, 170);
    }
  }
  if (node === 41) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree173(features, 83);
    } else if (val <= 143.50000000000003) {
      return calculateTree173(features, 83);
    } else {
      return calculateTree173(features, 84);
    }
  }
  if (node === 85) return 0.2182269340136454;
  if (node === 86) return -0.10692180687458387;
  if (node === 42) {
    const val = features['is_preference_men_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree173(features, 85);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree173(features, 85);
    } else {
      return calculateTree173(features, 86);
    }
  }
  if (node === 20) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree173(features, 41);
    } else if (val <= 897.0000000000001) {
      return calculateTree173(features, 41);
    } else {
      return calculateTree173(features, 42);
    }
  }
  if (node === 9) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree173(features, 19);
    } else if (val <= 485.50000000000006) {
      return calculateTree173(features, 19);
    } else {
      return calculateTree173(features, 20);
    }
  }
  if (node === 87) return -0.07115683657893375;
  if (node === 355) return 0.1710521632259688;
  if (node === 713) return -0.2858216375039526;
  if (node === 1429) return 0.24662490375484616;
  if (node === 1430) return -0.07718595656812222;
  if (node === 714) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree173(features, 1429);
    } else if (val <= 6.500000000000001) {
      return calculateTree173(features, 1429);
    } else {
      return calculateTree173(features, 1430);
    }
  }
  if (node === 356) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree173(features, 713);
    } else if (val <= 485.50000000000006) {
      return calculateTree173(features, 713);
    } else {
      return calculateTree173(features, 714);
    }
  }
  if (node === 177) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree173(features, 355);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree173(features, 355);
    } else {
      return calculateTree173(features, 356);
    }
  }
  if (node === 715) return -0.08578501879518088;
  if (node === 716) return -0.44301588290235966;
  if (node === 357) {
    const val = features['is_preference_kids'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree173(features, 715);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree173(features, 715);
    } else {
      return calculateTree173(features, 716);
    }
  }
  if (node === 358) return 0.0034505612031781193;
  if (node === 178) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree173(features, 357);
    } else if (val <= 35.50000000000001) {
      return calculateTree173(features, 357);
    } else {
      return calculateTree173(features, 358);
    }
  }
  if (node === 88) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree173(features, 177);
    } else if (val <= 626.0000000000001) {
      return calculateTree173(features, 177);
    } else {
      return calculateTree173(features, 178);
    }
  }
  if (node === 43) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree173(features, 87);
    } else if (val <= 197.50000000000003) {
      return calculateTree173(features, 87);
    } else {
      return calculateTree173(features, 88);
    }
  }
  if (node === 44) return -0.15617664887460395;
  if (node === 21) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree173(features, 43);
    } else if (val <= 916.0000000000001) {
      return calculateTree173(features, 43);
    } else {
      return calculateTree173(features, 44);
    }
  }
  if (node === 22) return 0.06864153841490661;
  if (node === 10) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree173(features, 21);
    } else if (val <= 29.500000000000004) {
      return calculateTree173(features, 21);
    } else {
      return calculateTree173(features, 22);
    }
  }
  if (node === 4) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree173(features, 9);
    } else if (val <= 30.500000000000004) {
      return calculateTree173(features, 9);
    } else {
      return calculateTree173(features, 10);
    }
  }
  if (node === 1) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree173(features, 3);
    } else if (val <= 948.0000000000001) {
      return calculateTree173(features, 3);
    } else {
      return calculateTree173(features, 4);
    }
  }
  if (node === 2) return 0.01625591139828809;
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree173(features, 1);
    } else if (val <= 973.5000000000001) {
      return calculateTree173(features, 1);
    } else {
      return calculateTree173(features, 2);
    }
  }
}
function calculateTree174(features, node = 0) {
  if (node === 15) return 0.12417966620749817;
  if (node === 16) return -0.23913762961621315;
  if (node === 7) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree174(features, 15);
    } else if (val <= 271.00000000000006) {
      return calculateTree174(features, 15);
    } else {
      return calculateTree174(features, 16);
    }
  }
  if (node === 8) return 0.3745645194311245;
  if (node === 3) {
    const val = features['is_preference_kitchen'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree174(features, 7);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree174(features, 7);
    } else {
      return calculateTree174(features, 8);
    }
  }
  if (node === 319) return 0.25321526433853175;
  if (node === 641) return -0.039237412549660934;
  if (node === 642) return 0.1703176175039605;
  if (node === 320) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree174(features, 641);
    } else if (val <= 300.50000000000006) {
      return calculateTree174(features, 641);
    } else {
      return calculateTree174(features, 642);
    }
  }
  if (node === 159) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree174(features, 319);
    } else if (val <= 132.50000000000003) {
      return calculateTree174(features, 319);
    } else {
      return calculateTree174(features, 320);
    }
  }
  if (node === 160) return -0.0142571708282085;
  if (node === 79) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree174(features, 159);
    } else if (val <= 1.5000000000000002) {
      return calculateTree174(features, 159);
    } else {
      return calculateTree174(features, 160);
    }
  }
  if (node === 323) return -0.11241275206335356;
  if (node === 324) return -0.3949534481137771;
  if (node === 161) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree174(features, 323);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree174(features, 323);
    } else {
      return calculateTree174(features, 324);
    }
  }
  if (node === 325) return -0.21059791194681413;
  if (node === 1307) return 0.1437391920152139;
  if (node === 1308) return -0.22205668319274025;
  if (node === 653) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree174(features, 1307);
    } else if (val <= 175.50000000000003) {
      return calculateTree174(features, 1307);
    } else {
      return calculateTree174(features, 1308);
    }
  }
  if (node === 654) return 0.204190548272789;
  if (node === 326) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree174(features, 653);
    } else if (val <= 270.00000000000006) {
      return calculateTree174(features, 653);
    } else {
      return calculateTree174(features, 654);
    }
  }
  if (node === 162) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree174(features, 325);
    } else if (val <= 17.500000000000004) {
      return calculateTree174(features, 325);
    } else {
      return calculateTree174(features, 326);
    }
  }
  if (node === 80) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree174(features, 161);
    } else if (val <= 660.0000000000001) {
      return calculateTree174(features, 161);
    } else {
      return calculateTree174(features, 162);
    }
  }
  if (node === 39) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree174(features, 79);
    } else if (val <= 271.00000000000006) {
      return calculateTree174(features, 79);
    } else {
      return calculateTree174(features, 80);
    }
  }
  if (node === 81) return -0.25752579843130813;
  if (node === 82) return 0.014156547247046453;
  if (node === 40) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree174(features, 81);
    } else if (val <= 99.50000000000001) {
      return calculateTree174(features, 81);
    } else {
      return calculateTree174(features, 82);
    }
  }
  if (node === 19) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree174(features, 39);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree174(features, 39);
    } else {
      return calculateTree174(features, 40);
    }
  }
  if (node === 20) return -0.3446739196923163;
  if (node === 9) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree174(features, 19);
    } else if (val <= 491.50000000000006) {
      return calculateTree174(features, 19);
    } else {
      return calculateTree174(features, 20);
    }
  }
  if (node === 43) return 0.19025358816119872;
  if (node === 44) return -0.09351954740977882;
  if (node === 21) {
    const val = features['is_preference_personal_care'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree174(features, 43);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree174(features, 43);
    } else {
      return calculateTree174(features, 44);
    }
  }
  if (node === 91) return 0.002998421518046803;
  if (node === 92) return -0.3055634018621098;
  if (node === 45) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree174(features, 91);
    } else if (val <= 123.50000000000001) {
      return calculateTree174(features, 91);
    } else {
      return calculateTree174(features, 92);
    }
  }
  if (node === 375) return 0.049574396448998116;
  if (node === 376) return 1.2821057387247747e-5;
  if (node === 187) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree174(features, 375);
    } else if (val <= 5.500000000000001) {
      return calculateTree174(features, 375);
    } else {
      return calculateTree174(features, 376);
    }
  }
  if (node === 377) return 0.13193394919288984;
  if (node === 378) return -0.3800655438317451;
  if (node === 188) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree174(features, 377);
    } else if (val <= 953.5000000000001) {
      return calculateTree174(features, 377);
    } else {
      return calculateTree174(features, 378);
    }
  }
  if (node === 93) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree174(features, 187);
    } else if (val <= 160.50000000000003) {
      return calculateTree174(features, 187);
    } else {
      return calculateTree174(features, 188);
    }
  }
  if (node === 94) return -0.23944482642358902;
  if (node === 46) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree174(features, 93);
    } else if (val <= 969.5000000000001) {
      return calculateTree174(features, 93);
    } else {
      return calculateTree174(features, 94);
    }
  }
  if (node === 22) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree174(features, 45);
    } else if (val <= 131.50000000000003) {
      return calculateTree174(features, 45);
    } else {
      return calculateTree174(features, 46);
    }
  }
  if (node === 10) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree174(features, 21);
    } else if (val <= 531.0000000000001) {
      return calculateTree174(features, 21);
    } else {
      return calculateTree174(features, 22);
    }
  }
  if (node === 4) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree174(features, 9);
    } else if (val <= 507.00000000000006) {
      return calculateTree174(features, 9);
    } else {
      return calculateTree174(features, 10);
    }
  }
  if (node === 1) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree174(features, 3);
    } else if (val <= 57.50000000000001) {
      return calculateTree174(features, 3);
    } else {
      return calculateTree174(features, 4);
    }
  }
  if (node === 23) return -0.18289070278710845;
  if (node === 49) return 0.13720276208707277;
  if (node === 203) return -0.024806342650386837;
  if (node === 204) return 0.14393734776340703;
  if (node === 101) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree174(features, 203);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree174(features, 203);
    } else {
      return calculateTree174(features, 204);
    }
  }
  if (node === 102) return -0.2947550984820647;
  if (node === 50) {
    const val = features['is_preference_beauty'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree174(features, 101);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree174(features, 101);
    } else {
      return calculateTree174(features, 102);
    }
  }
  if (node === 24) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree174(features, 49);
    } else if (val <= 1.5000000000000002) {
      return calculateTree174(features, 49);
    } else {
      return calculateTree174(features, 50);
    }
  }
  if (node === 11) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree174(features, 23);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree174(features, 23);
    } else {
      return calculateTree174(features, 24);
    }
  }
  if (node === 12) return -0.3129004114973147;
  if (node === 5) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree174(features, 11);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree174(features, 11);
    } else {
      return calculateTree174(features, 12);
    }
  }
  if (node === 6) return -0.0014317167956099572;
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree174(features, 5);
    } else if (val <= 166.50000000000003) {
      return calculateTree174(features, 5);
    } else {
      return calculateTree174(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree174(features, 1);
    } else if (val <= 164.50000000000003) {
      return calculateTree174(features, 1);
    } else {
      return calculateTree174(features, 2);
    }
  }
}
function calculateTree175(features, node = 0) {
  if (node === 1) return -0.0007738226925871266;
  if (node === 47) return 1.0804327847570864e-5;
  if (node === 48) return -0.3678568919556559;
  if (node === 23) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree175(features, 47);
    } else if (val <= 944.5000000000001) {
      return calculateTree175(features, 47);
    } else {
      return calculateTree175(features, 48);
    }
  }
  if (node === 24) return 0.0382267396975175;
  if (node === 11) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree175(features, 23);
    } else if (val <= 105.50000000000001) {
      return calculateTree175(features, 23);
    } else {
      return calculateTree175(features, 24);
    }
  }
  if (node === 25) return -0.266420165652402;
  if (node === 53) return -0.23983449817529173;
  if (node === 439) return 0.003966005031286731;
  if (node === 440) return -0.06969123311039255;
  if (node === 219) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree175(features, 439);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree175(features, 439);
    } else {
      return calculateTree175(features, 440);
    }
  }
  if (node === 220) return -0.034568588152000056;
  if (node === 109) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree175(features, 219);
    } else if (val <= 14.500000000000002) {
      return calculateTree175(features, 219);
    } else {
      return calculateTree175(features, 220);
    }
  }
  if (node === 443) return 0.01040865906304607;
  if (node === 1779) return 0.16501432031081512;
  if (node === 1780) return -0.03256910124151726;
  if (node === 889) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree175(features, 1779);
    } else if (val <= 325.00000000000006) {
      return calculateTree175(features, 1779);
    } else {
      return calculateTree175(features, 1780);
    }
  }
  if (node === 1781) return 0.1993561291712219;
  if (node === 1782) return -0.12363418920585428;
  if (node === 890) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree175(features, 1781);
    } else if (val <= 253.50000000000003) {
      return calculateTree175(features, 1781);
    } else {
      return calculateTree175(features, 1782);
    }
  }
  if (node === 444) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree175(features, 889);
    } else if (val <= 19.500000000000004) {
      return calculateTree175(features, 889);
    } else {
      return calculateTree175(features, 890);
    }
  }
  if (node === 221) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree175(features, 443);
    } else if (val <= 295.00000000000006) {
      return calculateTree175(features, 443);
    } else {
      return calculateTree175(features, 444);
    }
  }
  if (node === 445) return -0.07142283922231824;
  if (node === 893) return 0.30880083281652676;
  if (node === 894) return -0.0004383369209859426;
  if (node === 446) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree175(features, 893);
    } else if (val <= 253.50000000000003) {
      return calculateTree175(features, 893);
    } else {
      return calculateTree175(features, 894);
    }
  }
  if (node === 222) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree175(features, 445);
    } else if (val <= 27.500000000000004) {
      return calculateTree175(features, 445);
    } else {
      return calculateTree175(features, 446);
    }
  }
  if (node === 110) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree175(features, 221);
    } else if (val <= 817.5000000000001) {
      return calculateTree175(features, 221);
    } else {
      return calculateTree175(features, 222);
    }
  }
  if (node === 54) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree175(features, 109);
    } else if (val <= 3.5000000000000004) {
      return calculateTree175(features, 109);
    } else {
      return calculateTree175(features, 110);
    }
  }
  if (node === 26) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree175(features, 53);
    } else if (val <= 877.5000000000001) {
      return calculateTree175(features, 53);
    } else {
      return calculateTree175(features, 54);
    }
  }
  if (node === 12) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree175(features, 25);
    } else if (val <= 217.50000000000003) {
      return calculateTree175(features, 25);
    } else {
      return calculateTree175(features, 26);
    }
  }
  if (node === 5) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree175(features, 11);
    } else if (val <= 212.50000000000003) {
      return calculateTree175(features, 11);
    } else {
      return calculateTree175(features, 12);
    }
  }
  if (node === 55) return 0.025939277335618526;
  if (node === 56) return -0.22257456262046643;
  if (node === 27) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree175(features, 55);
    } else if (val <= 23.500000000000004) {
      return calculateTree175(features, 55);
    } else {
      return calculateTree175(features, 56);
    }
  }
  if (node === 28) return 0.13979819547531347;
  if (node === 13) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree175(features, 27);
    } else if (val <= 25.500000000000004) {
      return calculateTree175(features, 27);
    } else {
      return calculateTree175(features, 28);
    }
  }
  if (node === 119) return 0.012847218401696798;
  if (node === 483) return -0.03771150110820919;
  if (node === 484) return 0.07469659900147685;
  if (node === 241) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree175(features, 483);
    } else if (val <= 845.5000000000001) {
      return calculateTree175(features, 483);
    } else {
      return calculateTree175(features, 484);
    }
  }
  if (node === 485) return 0.05203661571677127;
  if (node === 486) return -0.12404874011468231;
  if (node === 242) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree175(features, 485);
    } else if (val <= 15.500000000000002) {
      return calculateTree175(features, 485);
    } else {
      return calculateTree175(features, 486);
    }
  }
  if (node === 120) {
    const val = features['is_preference_home'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree175(features, 241);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree175(features, 241);
    } else {
      return calculateTree175(features, 242);
    }
  }
  if (node === 59) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree175(features, 119);
    } else if (val <= 4.500000000000001) {
      return calculateTree175(features, 119);
    } else {
      return calculateTree175(features, 120);
    }
  }
  if (node === 121) return -0.1419867996656779;
  if (node === 245) return 0.09499522072256088;
  if (node === 246) return -0.09409291937440191;
  if (node === 122) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree175(features, 245);
    } else if (val <= 905.5000000000001) {
      return calculateTree175(features, 245);
    } else {
      return calculateTree175(features, 246);
    }
  }
  if (node === 60) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree175(features, 121);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree175(features, 121);
    } else {
      return calculateTree175(features, 122);
    }
  }
  if (node === 29) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree175(features, 59);
    } else if (val <= 234.50000000000003) {
      return calculateTree175(features, 59);
    } else {
      return calculateTree175(features, 60);
    }
  }
  if (node === 123) return -0.020364129840876745;
  if (node === 249) return -0.13328089950079605;
  if (node === 250) return 0.2737834166269227;
  if (node === 124) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree175(features, 249);
    } else if (val <= 6.500000000000001) {
      return calculateTree175(features, 249);
    } else {
      return calculateTree175(features, 250);
    }
  }
  if (node === 61) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree175(features, 123);
    } else if (val <= 905.5000000000001) {
      return calculateTree175(features, 123);
    } else {
      return calculateTree175(features, 124);
    }
  }
  if (node === 62) return -0.20423104107908796;
  if (node === 30) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree175(features, 61);
    } else if (val <= 908.5000000000001) {
      return calculateTree175(features, 61);
    } else {
      return calculateTree175(features, 62);
    }
  }
  if (node === 14) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree175(features, 29);
    } else if (val <= 626.0000000000001) {
      return calculateTree175(features, 29);
    } else {
      return calculateTree175(features, 30);
    }
  }
  if (node === 6) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree175(features, 13);
    } else if (val <= 485.50000000000006) {
      return calculateTree175(features, 13);
    } else {
      return calculateTree175(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree175(features, 5);
    } else if (val <= 472.50000000000006) {
      return calculateTree175(features, 5);
    } else {
      return calculateTree175(features, 6);
    }
  }
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree175(features, 1);
    } else if (val <= 850.5000000000001) {
      return calculateTree175(features, 1);
    } else {
      return calculateTree175(features, 2);
    }
  }
}
function calculateTree176(features, node = 0) {
  if (node === 3) return 0.0020087962578672898;
  if (node === 39) return -0.0016859158303288585;
  if (node === 40) return -0.3285475222794635;
  if (node === 19) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree176(features, 39);
    } else if (val <= 185.00000000000003) {
      return calculateTree176(features, 39);
    } else {
      return calculateTree176(features, 40);
    }
  }
  if (node === 83) return -0.014114530230570526;
  if (node === 84) return -0.32435724812232375;
  if (node === 41) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree176(features, 83);
    } else if (val <= 14.500000000000002) {
      return calculateTree176(features, 83);
    } else {
      return calculateTree176(features, 84);
    }
  }
  if (node === 42) return 0.18806452830899795;
  if (node === 20) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree176(features, 41);
    } else if (val <= 42.50000000000001) {
      return calculateTree176(features, 41);
    } else {
      return calculateTree176(features, 42);
    }
  }
  if (node === 9) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree176(features, 19);
    } else if (val <= 670.5000000000001) {
      return calculateTree176(features, 19);
    } else {
      return calculateTree176(features, 20);
    }
  }
  if (node === 21) return 0.058399787908844814;
  if (node === 22) return -0.31377834438724617;
  if (node === 10) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree176(features, 21);
    } else if (val <= 670.5000000000001) {
      return calculateTree176(features, 21);
    } else {
      return calculateTree176(features, 22);
    }
  }
  if (node === 4) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree176(features, 9);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree176(features, 9);
    } else {
      return calculateTree176(features, 10);
    }
  }
  if (node === 1) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree176(features, 3);
    } else if (val <= 316.50000000000006) {
      return calculateTree176(features, 3);
    } else {
      return calculateTree176(features, 4);
    }
  }
  if (node === 95) return 0.08573484821991766;
  if (node === 96) return -0.08341400197533648;
  if (node === 47) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree176(features, 95);
    } else if (val <= 83.50000000000001) {
      return calculateTree176(features, 95);
    } else {
      return calculateTree176(features, 96);
    }
  }
  if (node === 195) return -0.22790180577341423;
  if (node === 196) return 0.04183786191940466;
  if (node === 97) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree176(features, 195);
    } else if (val <= 18.500000000000004) {
      return calculateTree176(features, 195);
    } else {
      return calculateTree176(features, 196);
    }
  }
  if (node === 98) return -0.31226302016648394;
  if (node === 48) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree176(features, 97);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree176(features, 97);
    } else {
      return calculateTree176(features, 98);
    }
  }
  if (node === 23) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree176(features, 47);
    } else if (val <= 338.50000000000006) {
      return calculateTree176(features, 47);
    } else {
      return calculateTree176(features, 48);
    }
  }
  if (node === 49) return -0.2061242886613065;
  if (node === 101) return 0.026143769081783657;
  if (node === 411) return 0.38582081055540457;
  if (node === 412) return 0.08763952467110102;
  if (node === 205) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree176(features, 411);
    } else if (val <= 25.500000000000004) {
      return calculateTree176(features, 411);
    } else {
      return calculateTree176(features, 412);
    }
  }
  if (node === 206) return 0.024448967625186405;
  if (node === 102) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree176(features, 205);
    } else if (val <= 378.50000000000006) {
      return calculateTree176(features, 205);
    } else {
      return calculateTree176(features, 206);
    }
  }
  if (node === 50) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree176(features, 101);
    } else if (val <= 13.500000000000002) {
      return calculateTree176(features, 101);
    } else {
      return calculateTree176(features, 102);
    }
  }
  if (node === 24) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree176(features, 49);
    } else if (val <= 346.00000000000006) {
      return calculateTree176(features, 49);
    } else {
      return calculateTree176(features, 50);
    }
  }
  if (node === 11) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree176(features, 23);
    } else if (val <= 688.5000000000001) {
      return calculateTree176(features, 23);
    } else {
      return calculateTree176(features, 24);
    }
  }
  if (node === 51) return 0.22036627412675888;
  if (node === 52) return 0.020296674775372326;
  if (node === 25) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree176(features, 51);
    } else if (val <= 7.500000000000001) {
      return calculateTree176(features, 51);
    } else {
      return calculateTree176(features, 52);
    }
  }
  if (node === 53) return -0.28767194125606943;
  if (node === 109) return 0.29931248992427784;
  if (node === 110) return -0.04724675247307496;
  if (node === 54) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree176(features, 109);
    } else if (val <= 1.5000000000000002) {
      return calculateTree176(features, 109);
    } else {
      return calculateTree176(features, 110);
    }
  }
  if (node === 26) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree176(features, 53);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree176(features, 53);
    } else {
      return calculateTree176(features, 54);
    }
  }
  if (node === 12) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree176(features, 25);
    } else if (val <= 180.50000000000003) {
      return calculateTree176(features, 25);
    } else {
      return calculateTree176(features, 26);
    }
  }
  if (node === 5) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree176(features, 11);
    } else if (val <= 40.50000000000001) {
      return calculateTree176(features, 11);
    } else {
      return calculateTree176(features, 12);
    }
  }
  if (node === 111) return -0.4704296396981878;
  if (node === 112) return 0.02151853979319109;
  if (node === 55) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree176(features, 111);
    } else if (val <= 15.500000000000002) {
      return calculateTree176(features, 111);
    } else {
      return calculateTree176(features, 112);
    }
  }
  if (node === 113) return 0.06794570413293684;
  if (node === 114) return -0.09677264164083378;
  if (node === 56) {
    const val = features['is_preference_personal_care'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree176(features, 113);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree176(features, 113);
    } else {
      return calculateTree176(features, 114);
    }
  }
  if (node === 27) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree176(features, 55);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree176(features, 55);
    } else {
      return calculateTree176(features, 56);
    }
  }
  if (node === 231) return -0.08135749996782157;
  if (node === 232) return 0.19035860849178596;
  if (node === 115) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree176(features, 231);
    } else if (val <= 234.50000000000003) {
      return calculateTree176(features, 231);
    } else {
      return calculateTree176(features, 232);
    }
  }
  if (node === 116) return -0.3203624766721076;
  if (node === 57) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree176(features, 115);
    } else if (val <= 40.50000000000001) {
      return calculateTree176(features, 115);
    } else {
      return calculateTree176(features, 116);
    }
  }
  if (node === 58) return -0.010477222996650846;
  if (node === 28) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree176(features, 57);
    } else if (val <= 1.5000000000000002) {
      return calculateTree176(features, 57);
    } else {
      return calculateTree176(features, 58);
    }
  }
  if (node === 13) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree176(features, 27);
    } else if (val <= 26.500000000000004) {
      return calculateTree176(features, 27);
    } else {
      return calculateTree176(features, 28);
    }
  }
  if (node === 14) return -0.0008582351678225097;
  if (node === 6) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree176(features, 13);
    } else if (val <= 353.00000000000006) {
      return calculateTree176(features, 13);
    } else {
      return calculateTree176(features, 14);
    }
  }
  if (node === 2) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree176(features, 5);
    } else if (val <= 348.50000000000006) {
      return calculateTree176(features, 5);
    } else {
      return calculateTree176(features, 6);
    }
  }
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree176(features, 1);
    } else if (val <= 327.50000000000006) {
      return calculateTree176(features, 1);
    } else {
      return calculateTree176(features, 2);
    }
  }
}
function calculateTree177(features, node = 0) {
  if (node === 3) return -0.0010315666839506021;
  if (node === 19) return -0.08963558141224444;
  if (node === 167) return 0.24997009546489063;
  if (node === 675) return 0.12621862977582798;
  if (node === 676) return -0.0777204347621739;
  if (node === 337) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree177(features, 675);
    } else if (val <= 1.5000000000000002) {
      return calculateTree177(features, 675);
    } else {
      return calculateTree177(features, 676);
    }
  }
  if (node === 1355) return 0.1069057366215893;
  if (node === 1356) return -0.09479505032875953;
  if (node === 677) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree177(features, 1355);
    } else if (val <= 580.0000000000001) {
      return calculateTree177(features, 1355);
    } else {
      return calculateTree177(features, 1356);
    }
  }
  if (node === 678) return -0.07164340440274469;
  if (node === 338) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree177(features, 677);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree177(features, 677);
    } else {
      return calculateTree177(features, 678);
    }
  }
  if (node === 168) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree177(features, 337);
    } else if (val <= 245.00000000000003) {
      return calculateTree177(features, 337);
    } else {
      return calculateTree177(features, 338);
    }
  }
  if (node === 83) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree177(features, 167);
    } else if (val <= 12.000000000000002) {
      return calculateTree177(features, 167);
    } else {
      return calculateTree177(features, 168);
    }
  }
  if (node === 169) return 0.004078505237550165;
  if (node === 170) return 0.17099165734384356;
  if (node === 84) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree177(features, 169);
    } else if (val <= 839.5000000000001) {
      return calculateTree177(features, 169);
    } else {
      return calculateTree177(features, 170);
    }
  }
  if (node === 41) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree177(features, 83);
    } else if (val <= 643.5000000000001) {
      return calculateTree177(features, 83);
    } else {
      return calculateTree177(features, 84);
    }
  }
  if (node === 85) return -0.16029728587823802;
  if (node === 86) return -0.0292060809027598;
  if (node === 42) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree177(features, 85);
    } else if (val <= 859.5000000000001) {
      return calculateTree177(features, 85);
    } else {
      return calculateTree177(features, 86);
    }
  }
  if (node === 20) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree177(features, 41);
    } else if (val <= 819.5000000000001) {
      return calculateTree177(features, 41);
    } else {
      return calculateTree177(features, 42);
    }
  }
  if (node === 9) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree177(features, 19);
    } else if (val <= 207.00000000000003) {
      return calculateTree177(features, 19);
    } else {
      return calculateTree177(features, 20);
    }
  }
  if (node === 87) return 0.4551888240730207;
  if (node === 177) return 0.2489562258573621;
  if (node === 178) return -0.05993553079043303;
  if (node === 88) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree177(features, 177);
    } else if (val <= 14.500000000000002) {
      return calculateTree177(features, 177);
    } else {
      return calculateTree177(features, 178);
    }
  }
  if (node === 43) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree177(features, 87);
    } else if (val <= 181.50000000000003) {
      return calculateTree177(features, 87);
    } else {
      return calculateTree177(features, 88);
    }
  }
  if (node === 179) return 0.0435772395103371;
  if (node === 180) return 0.3189904984430709;
  if (node === 89) {
    const val = features['is_preference_sports'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree177(features, 179);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree177(features, 179);
    } else {
      return calculateTree177(features, 180);
    }
  }
  if (node === 727) return 0.009439044073731567;
  if (node === 728) return -0.3112067942851821;
  if (node === 363) {
    const val = features['is_preference_women_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree177(features, 727);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree177(features, 727);
    } else {
      return calculateTree177(features, 728);
    }
  }
  if (node === 1459) return 0.024667065962256984;
  if (node === 1460) return 0.2989940951472486;
  if (node === 729) {
    const val = features['is_preference_grocery'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree177(features, 1459);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree177(features, 1459);
    } else {
      return calculateTree177(features, 1460);
    }
  }
  if (node === 1461) return -0.14579001987112278;
  if (node === 1462) return 0.18937267452767967;
  if (node === 730) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree177(features, 1461);
    } else if (val <= 677.5000000000001) {
      return calculateTree177(features, 1461);
    } else {
      return calculateTree177(features, 1462);
    }
  }
  if (node === 364) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree177(features, 729);
    } else if (val <= 16.500000000000004) {
      return calculateTree177(features, 729);
    } else {
      return calculateTree177(features, 730);
    }
  }
  if (node === 181) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree177(features, 363);
    } else if (val <= 275.50000000000006) {
      return calculateTree177(features, 363);
    } else {
      return calculateTree177(features, 364);
    }
  }
  if (node === 365) return 0.06930691935962029;
  if (node === 366) return -0.35252686940814526;
  if (node === 182) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree177(features, 365);
    } else if (val <= 358.50000000000006) {
      return calculateTree177(features, 365);
    } else {
      return calculateTree177(features, 366);
    }
  }
  if (node === 90) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree177(features, 181);
    } else if (val <= 799.5000000000001) {
      return calculateTree177(features, 181);
    } else {
      return calculateTree177(features, 182);
    }
  }
  if (node === 44) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree177(features, 89);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree177(features, 89);
    } else {
      return calculateTree177(features, 90);
    }
  }
  if (node === 21) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree177(features, 43);
    } else if (val <= 131.50000000000003) {
      return calculateTree177(features, 43);
    } else {
      return calculateTree177(features, 44);
    }
  }
  if (node === 91) return 0.07338609599417034;
  if (node === 185) return -0.26657997293213;
  if (node === 186) return 0.007285424614033511;
  if (node === 92) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree177(features, 185);
    } else if (val <= 24.500000000000004) {
      return calculateTree177(features, 185);
    } else {
      return calculateTree177(features, 186);
    }
  }
  if (node === 45) {
    const val = features['is_preference_personal_care'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree177(features, 91);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree177(features, 91);
    } else {
      return calculateTree177(features, 92);
    }
  }
  if (node === 187) return -0.15597153476384049;
  if (node === 188) return 0.2452138107397269;
  if (node === 93) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree177(features, 187);
    } else if (val <= 822.5000000000001) {
      return calculateTree177(features, 187);
    } else {
      return calculateTree177(features, 188);
    }
  }
  if (node === 94) return -0.19434337856076125;
  if (node === 46) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree177(features, 93);
    } else if (val <= 24.500000000000004) {
      return calculateTree177(features, 93);
    } else {
      return calculateTree177(features, 94);
    }
  }
  if (node === 22) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree177(features, 45);
    } else if (val <= 409.50000000000006) {
      return calculateTree177(features, 45);
    } else {
      return calculateTree177(features, 46);
    }
  }
  if (node === 10) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree177(features, 21);
    } else if (val <= 794.0000000000001) {
      return calculateTree177(features, 21);
    } else {
      return calculateTree177(features, 22);
    }
  }
  if (node === 4) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree177(features, 9);
    } else if (val <= 5.500000000000001) {
      return calculateTree177(features, 9);
    } else {
      return calculateTree177(features, 10);
    }
  }
  if (node === 1) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree177(features, 3);
    } else if (val <= 23.500000000000004) {
      return calculateTree177(features, 3);
    } else {
      return calculateTree177(features, 4);
    }
  }
  if (node === 2) return 0.001576375514020194;
  if (node === 0) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree177(features, 1);
    } else if (val <= 25.500000000000004) {
      return calculateTree177(features, 1);
    } else {
      return calculateTree177(features, 2);
    }
  }
}
function calculateTree178(features, node = 0) {
  if (node === 1) return -0.00014396568782315037;
  if (node === 47) return -0.0324759657310866;
  if (node === 97) return 0.19699448893571989;
  if (node === 98) return 0.0010590830440629691;
  if (node === 48) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree178(features, 97);
    } else if (val <= 398.00000000000006) {
      return calculateTree178(features, 97);
    } else {
      return calculateTree178(features, 98);
    }
  }
  if (node === 23) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree178(features, 47);
    } else if (val <= 365.50000000000006) {
      return calculateTree178(features, 47);
    } else {
      return calculateTree178(features, 48);
    }
  }
  if (node === 99) return -0.4536904785142202;
  if (node === 100) return -0.12332314698499723;
  if (node === 49) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree178(features, 99);
    } else if (val <= 197.50000000000003) {
      return calculateTree178(features, 99);
    } else {
      return calculateTree178(features, 100);
    }
  }
  if (node === 101) return -0.11138336620623646;
  if (node === 102) return 0.16159845983789442;
  if (node === 50) {
    const val = features['is_preference_home'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree178(features, 101);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree178(features, 101);
    } else {
      return calculateTree178(features, 102);
    }
  }
  if (node === 24) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree178(features, 49);
    } else if (val <= 58.50000000000001) {
      return calculateTree178(features, 49);
    } else {
      return calculateTree178(features, 50);
    }
  }
  if (node === 11) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree178(features, 23);
    } else if (val <= 485.50000000000006) {
      return calculateTree178(features, 23);
    } else {
      return calculateTree178(features, 24);
    }
  }
  if (node === 415) return -0.3522363854280008;
  if (node === 416) return -0.08353408005282487;
  if (node === 207) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree178(features, 415);
    } else if (val <= 682.5000000000001) {
      return calculateTree178(features, 415);
    } else {
      return calculateTree178(features, 416);
    }
  }
  if (node === 417) return 0.23084016374487556;
  if (node === 418) return -0.13073327373963797;
  if (node === 208) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree178(features, 417);
    } else if (val <= 7.500000000000001) {
      return calculateTree178(features, 417);
    } else {
      return calculateTree178(features, 418);
    }
  }
  if (node === 103) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree178(features, 207);
    } else if (val <= 56.00000000000001) {
      return calculateTree178(features, 207);
    } else {
      return calculateTree178(features, 208);
    }
  }
  if (node === 104) return 0.22299449830692314;
  if (node === 51) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree178(features, 103);
    } else if (val <= 923.5000000000001) {
      return calculateTree178(features, 103);
    } else {
      return calculateTree178(features, 104);
    }
  }
  if (node === 211) return 0.11533131154508386;
  if (node === 851) return -0.07455635612539388;
  if (node === 852) return 0.23557390648108403;
  if (node === 425) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree178(features, 851);
    } else if (val <= 897.0000000000001) {
      return calculateTree178(features, 851);
    } else {
      return calculateTree178(features, 852);
    }
  }
  if (node === 426) return -0.27205201158734876;
  if (node === 212) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree178(features, 425);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree178(features, 425);
    } else {
      return calculateTree178(features, 426);
    }
  }
  if (node === 105) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree178(features, 211);
    } else if (val <= 479.50000000000006) {
      return calculateTree178(features, 211);
    } else {
      return calculateTree178(features, 212);
    }
  }
  if (node === 106) return -0.006671027770304143;
  if (node === 52) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree178(features, 105);
    } else if (val <= 305.50000000000006) {
      return calculateTree178(features, 105);
    } else {
      return calculateTree178(features, 106);
    }
  }
  if (node === 25) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree178(features, 51);
    } else if (val <= 56.50000000000001) {
      return calculateTree178(features, 51);
    } else {
      return calculateTree178(features, 52);
    }
  }
  if (node === 215) return -0.14495952838121395;
  if (node === 433) return 0.3091504586390283;
  if (node === 434) return -0.009625985657032194;
  if (node === 216) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree178(features, 433);
    } else if (val <= 794.0000000000001) {
      return calculateTree178(features, 433);
    } else {
      return calculateTree178(features, 434);
    }
  }
  if (node === 107) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree178(features, 215);
    } else if (val <= 307.00000000000006) {
      return calculateTree178(features, 215);
    } else {
      return calculateTree178(features, 216);
    }
  }
  if (node === 108) return 0.2949009004198986;
  if (node === 53) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree178(features, 107);
    } else if (val <= 733.5000000000001) {
      return calculateTree178(features, 107);
    } else {
      return calculateTree178(features, 108);
    }
  }
  if (node === 54) return 0.2927416760417575;
  if (node === 26) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree178(features, 53);
    } else if (val <= 682.5000000000001) {
      return calculateTree178(features, 53);
    } else {
      return calculateTree178(features, 54);
    }
  }
  if (node === 12) {
    const val = features['is_preference_grocery'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree178(features, 25);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree178(features, 25);
    } else {
      return calculateTree178(features, 26);
    }
  }
  if (node === 5) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree178(features, 11);
    } else if (val <= 507.00000000000006) {
      return calculateTree178(features, 11);
    } else {
      return calculateTree178(features, 12);
    }
  }
  if (node === 55) return -0.09668525155882289;
  if (node === 56) return -0.36491348651199645;
  if (node === 27) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree178(features, 55);
    } else if (val <= 884.0000000000001) {
      return calculateTree178(features, 55);
    } else {
      return calculateTree178(features, 56);
    }
  }
  if (node === 28) return -0.04593643516262368;
  if (node === 13) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree178(features, 27);
    } else if (val <= 56.50000000000001) {
      return calculateTree178(features, 27);
    } else {
      return calculateTree178(features, 28);
    }
  }
  if (node === 239) return 0.004125363160572882;
  if (node === 481) return -0.13476958592938088;
  if (node === 482) return 0.19029745084884334;
  if (node === 240) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree178(features, 481);
    } else if (val <= 253.50000000000003) {
      return calculateTree178(features, 481);
    } else {
      return calculateTree178(features, 482);
    }
  }
  if (node === 119) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree178(features, 239);
    } else if (val <= 4.500000000000001) {
      return calculateTree178(features, 239);
    } else {
      return calculateTree178(features, 240);
    }
  }
  if (node === 120) return 0.34976822294511;
  if (node === 59) {
    const val = features['is_preference_beauty'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree178(features, 119);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree178(features, 119);
    } else {
      return calculateTree178(features, 120);
    }
  }
  if (node === 60) return -0.09690610253059609;
  if (node === 29) {
    const val = features['is_preference_grocery'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree178(features, 59);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree178(features, 59);
    } else {
      return calculateTree178(features, 60);
    }
  }
  if (node === 30) return -0.05652750114584925;
  if (node === 14) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree178(features, 29);
    } else if (val <= 378.50000000000006) {
      return calculateTree178(features, 29);
    } else {
      return calculateTree178(features, 30);
    }
  }
  if (node === 6) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree178(features, 13);
    } else if (val <= 898.5000000000001) {
      return calculateTree178(features, 13);
    } else {
      return calculateTree178(features, 14);
    }
  }
  if (node === 2) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree178(features, 5);
    } else if (val <= 717.0000000000001) {
      return calculateTree178(features, 5);
    } else {
      return calculateTree178(features, 6);
    }
  }
  if (node === 0) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree178(features, 1);
    } else if (val <= 55.50000000000001) {
      return calculateTree178(features, 1);
    } else {
      return calculateTree178(features, 2);
    }
  }
}
function calculateTree179(features, node = 0) {
  if (node === 3) return -0.00046735197094877957;
  if (node === 39) return 0.025883747030922888;
  if (node === 655) return -0.04547486860080746;
  if (node === 1313) return -0.3210956453882744;
  if (node === 1314) return -0.06029196412050013;
  if (node === 656) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree179(features, 1313);
    } else if (val <= 2.5000000000000004) {
      return calculateTree179(features, 1313);
    } else {
      return calculateTree179(features, 1314);
    }
  }
  if (node === 327) {
    const val = features['is_preference_sports'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree179(features, 655);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree179(features, 655);
    } else {
      return calculateTree179(features, 656);
    }
  }
  if (node === 1315) return 0.0014090764154783037;
  if (node === 1316) return -0.07393042079770519;
  if (node === 657) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree179(features, 1315);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree179(features, 1315);
    } else {
      return calculateTree179(features, 1316);
    }
  }
  if (node === 1317) return -0.09026723716759612;
  if (node === 1318) return 0.00631423371365084;
  if (node === 658) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree179(features, 1317);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree179(features, 1317);
    } else {
      return calculateTree179(features, 1318);
    }
  }
  if (node === 328) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree179(features, 657);
    } else if (val <= 37.50000000000001) {
      return calculateTree179(features, 657);
    } else {
      return calculateTree179(features, 658);
    }
  }
  if (node === 163) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree179(features, 327);
    } else if (val <= 217.50000000000003) {
      return calculateTree179(features, 327);
    } else {
      return calculateTree179(features, 328);
    }
  }
  if (node === 659) return 0.016972144755682043;
  if (node === 1321) return -0.17336202622850996;
  if (node === 1322) return 0.12348861694253466;
  if (node === 660) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree179(features, 1321);
    } else if (val <= 59.50000000000001) {
      return calculateTree179(features, 1321);
    } else {
      return calculateTree179(features, 1322);
    }
  }
  if (node === 329) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree179(features, 659);
    } else if (val <= 838.5000000000001) {
      return calculateTree179(features, 659);
    } else {
      return calculateTree179(features, 660);
    }
  }
  if (node === 330) return 0.1316528283802534;
  if (node === 164) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree179(features, 329);
    } else if (val <= 937.5000000000001) {
      return calculateTree179(features, 329);
    } else {
      return calculateTree179(features, 330);
    }
  }
  if (node === 81) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree179(features, 163);
    } else if (val <= 44.50000000000001) {
      return calculateTree179(features, 163);
    } else {
      return calculateTree179(features, 164);
    }
  }
  if (node === 165) return 0.02126644046916935;
  if (node === 333) return -0.18416902593610887;
  if (node === 669) return 0.12518123566622014;
  if (node === 670) return -0.06591225310142074;
  if (node === 334) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree179(features, 669);
    } else if (val <= 660.0000000000001) {
      return calculateTree179(features, 669);
    } else {
      return calculateTree179(features, 670);
    }
  }
  if (node === 166) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree179(features, 333);
    } else if (val <= 606.5000000000001) {
      return calculateTree179(features, 333);
    } else {
      return calculateTree179(features, 334);
    }
  }
  if (node === 82) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree179(features, 165);
    } else if (val <= 577.5000000000001) {
      return calculateTree179(features, 165);
    } else {
      return calculateTree179(features, 166);
    }
  }
  if (node === 40) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree179(features, 81);
    } else if (val <= 271.00000000000006) {
      return calculateTree179(features, 81);
    } else {
      return calculateTree179(features, 82);
    }
  }
  if (node === 19) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree179(features, 39);
    } else if (val <= 158.50000000000003) {
      return calculateTree179(features, 39);
    } else {
      return calculateTree179(features, 40);
    }
  }
  if (node === 167) return -0.2894896410788653;
  if (node === 168) return 0.014201014442840209;
  if (node === 83) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree179(features, 167);
    } else if (val <= 55.50000000000001) {
      return calculateTree179(features, 167);
    } else {
      return calculateTree179(features, 168);
    }
  }
  if (node === 169) return 0.1712432068922548;
  if (node === 683) return -0.19989452084776674;
  if (node === 1369) return 0.02763153335360983;
  if (node === 1370) return 0.4145594596732851;
  if (node === 684) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree179(features, 1369);
    } else if (val <= 969.5000000000001) {
      return calculateTree179(features, 1369);
    } else {
      return calculateTree179(features, 1370);
    }
  }
  if (node === 341) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree179(features, 683);
    } else if (val <= 194.00000000000003) {
      return calculateTree179(features, 683);
    } else {
      return calculateTree179(features, 684);
    }
  }
  if (node === 1371) return 0.4953243682774858;
  if (node === 1372) return 0.09211385873277454;
  if (node === 685) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree179(features, 1371);
    } else if (val <= 38.50000000000001) {
      return calculateTree179(features, 1371);
    } else {
      return calculateTree179(features, 1372);
    }
  }
  if (node === 1373) return -0.06360701078108869;
  if (node === 1374) return 0.10472465372217254;
  if (node === 686) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree179(features, 1373);
    } else if (val <= 414.50000000000006) {
      return calculateTree179(features, 1373);
    } else {
      return calculateTree179(features, 1374);
    }
  }
  if (node === 342) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree179(features, 685);
    } else if (val <= 127.50000000000001) {
      return calculateTree179(features, 685);
    } else {
      return calculateTree179(features, 686);
    }
  }
  if (node === 170) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree179(features, 341);
    } else if (val <= 28.500000000000004) {
      return calculateTree179(features, 341);
    } else {
      return calculateTree179(features, 342);
    }
  }
  if (node === 84) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree179(features, 169);
    } else if (val <= 107.00000000000001) {
      return calculateTree179(features, 169);
    } else {
      return calculateTree179(features, 170);
    }
  }
  if (node === 41) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree179(features, 83);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree179(features, 83);
    } else {
      return calculateTree179(features, 84);
    }
  }
  if (node === 42) return 0.008438929105559965;
  if (node === 20) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree179(features, 41);
    } else if (val <= 801.5000000000001) {
      return calculateTree179(features, 41);
    } else {
      return calculateTree179(features, 42);
    }
  }
  if (node === 9) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree179(features, 19);
    } else if (val <= 657.5000000000001) {
      return calculateTree179(features, 19);
    } else {
      return calculateTree179(features, 20);
    }
  }
  if (node === 10) return -0.0006476831977176105;
  if (node === 4) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree179(features, 9);
    } else if (val <= 4.500000000000001) {
      return calculateTree179(features, 9);
    } else {
      return calculateTree179(features, 10);
    }
  }
  if (node === 1) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree179(features, 3);
    } else if (val <= 1.5000000000000002) {
      return calculateTree179(features, 3);
    } else {
      return calculateTree179(features, 4);
    }
  }
  if (node === 5) return 0.10727127592539426;
  if (node === 13) return 0.03267938035699101;
  if (node === 14) return -0.23834130080706947;
  if (node === 6) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree179(features, 13);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree179(features, 13);
    } else {
      return calculateTree179(features, 14);
    }
  }
  if (node === 2) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree179(features, 5);
    } else if (val <= 222.50000000000003) {
      return calculateTree179(features, 5);
    } else {
      return calculateTree179(features, 6);
    }
  }
  if (node === 0) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree179(features, 1);
    } else if (val <= 104.00000000000001) {
      return calculateTree179(features, 1);
    } else {
      return calculateTree179(features, 2);
    }
  }
}
function calculateTree180(features, node = 0) {
  if (node === 3) return 0.0005761691629946414;
  if (node === 19) return 0.03523269319841996;
  if (node === 167) return -0.1738133907617069;
  if (node === 337) return 0.19518515925305205;
  if (node === 677) return -0.2980365688211076;
  if (node === 678) return -0.03352949740950004;
  if (node === 338) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree180(features, 677);
    } else if (val <= 25.500000000000004) {
      return calculateTree180(features, 677);
    } else {
      return calculateTree180(features, 678);
    }
  }
  if (node === 168) {
    const val = features['is_preference_personal_care'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree180(features, 337);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree180(features, 337);
    } else {
      return calculateTree180(features, 338);
    }
  }
  if (node === 83) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree180(features, 167);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree180(features, 167);
    } else {
      return calculateTree180(features, 168);
    }
  }
  if (node === 169) return -0.05379170584399979;
  if (node === 1367) return 0.1145909868319917;
  if (node === 1368) return 0.5379426496102309;
  if (node === 683) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree180(features, 1367);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree180(features, 1367);
    } else {
      return calculateTree180(features, 1368);
    }
  }
  if (node === 684) return -0.0379243086365361;
  if (node === 341) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree180(features, 683);
    } else if (val <= 6.500000000000001) {
      return calculateTree180(features, 683);
    } else {
      return calculateTree180(features, 684);
    }
  }
  if (node === 342) return -0.02756629598733273;
  if (node === 170) {
    const val = features['is_preference_kids'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree180(features, 341);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree180(features, 341);
    } else {
      return calculateTree180(features, 342);
    }
  }
  if (node === 84) {
    const val = features['is_preference_beauty'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree180(features, 169);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree180(features, 169);
    } else {
      return calculateTree180(features, 170);
    }
  }
  if (node === 41) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree180(features, 83);
    } else if (val <= 560.5000000000001) {
      return calculateTree180(features, 83);
    } else {
      return calculateTree180(features, 84);
    }
  }
  if (node === 171) return -0.2973094373943399;
  if (node === 345) return 0.1576715037050856;
  if (node === 346) return -0.18901676433319797;
  if (node === 172) {
    const val = features['is_preference_men_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree180(features, 345);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree180(features, 345);
    } else {
      return calculateTree180(features, 346);
    }
  }
  if (node === 85) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree180(features, 171);
    } else if (val <= 25.500000000000004) {
      return calculateTree180(features, 171);
    } else {
      return calculateTree180(features, 172);
    }
  }
  if (node === 173) return -0.07927598021052108;
  if (node === 174) return 0.2299715777140334;
  if (node === 86) {
    const val = features['is_preference_personal_care'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree180(features, 173);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree180(features, 173);
    } else {
      return calculateTree180(features, 174);
    }
  }
  if (node === 42) {
    const val = features['is_preference_pets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree180(features, 85);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree180(features, 85);
    } else {
      return calculateTree180(features, 86);
    }
  }
  if (node === 20) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree180(features, 41);
    } else if (val <= 20.500000000000004) {
      return calculateTree180(features, 41);
    } else {
      return calculateTree180(features, 42);
    }
  }
  if (node === 9) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree180(features, 19);
    } else if (val <= 322.00000000000006) {
      return calculateTree180(features, 19);
    } else {
      return calculateTree180(features, 20);
    }
  }
  if (node === 21) return 0.209963073037608;
  if (node === 45) return -0.21937553540763244;
  if (node === 46) return -0.04831525756628047;
  if (node === 22) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree180(features, 45);
    } else if (val <= 191.50000000000003) {
      return calculateTree180(features, 45);
    } else {
      return calculateTree180(features, 46);
    }
  }
  if (node === 10) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree180(features, 21);
    } else if (val <= 60.00000000000001) {
      return calculateTree180(features, 21);
    } else {
      return calculateTree180(features, 22);
    }
  }
  if (node === 4) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree180(features, 9);
    } else if (val <= 41.50000000000001) {
      return calculateTree180(features, 9);
    } else {
      return calculateTree180(features, 10);
    }
  }
  if (node === 1) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree180(features, 3);
    } else if (val <= 880.0000000000001) {
      return calculateTree180(features, 3);
    } else {
      return calculateTree180(features, 4);
    }
  }
  if (node === 47) return 0.05175766922176848;
  if (node === 783) return -0.036913737911219;
  if (node === 784) return -0.32398841187677546;
  if (node === 391) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree180(features, 783);
    } else if (val <= 602.5000000000001) {
      return calculateTree180(features, 783);
    } else {
      return calculateTree180(features, 784);
    }
  }
  if (node === 392) return 0.2623387507782324;
  if (node === 195) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree180(features, 391);
    } else if (val <= 677.5000000000001) {
      return calculateTree180(features, 391);
    } else {
      return calculateTree180(features, 392);
    }
  }
  if (node === 393) return -0.1109432610298245;
  if (node === 394) return -0.4536294079285892;
  if (node === 196) {
    const val = features['is_preference_home'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree180(features, 393);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree180(features, 393);
    } else {
      return calculateTree180(features, 394);
    }
  }
  if (node === 97) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree180(features, 195);
    } else if (val <= 774.0000000000001) {
      return calculateTree180(features, 195);
    } else {
      return calculateTree180(features, 196);
    }
  }
  if (node === 98) return 0.24435686711366883;
  if (node === 48) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree180(features, 97);
    } else if (val <= 21.500000000000004) {
      return calculateTree180(features, 97);
    } else {
      return calculateTree180(features, 98);
    }
  }
  if (node === 23) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree180(features, 47);
    } else if (val <= 2.5000000000000004) {
      return calculateTree180(features, 47);
    } else {
      return calculateTree180(features, 48);
    }
  }
  if (node === 24) return -0.2692392052241256;
  if (node === 11) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree180(features, 23);
    } else if (val <= 972.5000000000001) {
      return calculateTree180(features, 23);
    } else {
      return calculateTree180(features, 24);
    }
  }
  if (node === 51) return 0.3185333513471667;
  if (node === 105) return 0.2652494347715223;
  if (node === 106) return 0.03375153599549973;
  if (node === 52) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree180(features, 105);
    } else if (val <= 2.5000000000000004) {
      return calculateTree180(features, 105);
    } else {
      return calculateTree180(features, 106);
    }
  }
  if (node === 25) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree180(features, 51);
    } else if (val <= 897.0000000000001) {
      return calculateTree180(features, 51);
    } else {
      return calculateTree180(features, 52);
    }
  }
  if (node === 26) return -0.22409148116282743;
  if (node === 12) {
    const val = features['is_preference_household'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree180(features, 25);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree180(features, 25);
    } else {
      return calculateTree180(features, 26);
    }
  }
  if (node === 5) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree180(features, 11);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree180(features, 11);
    } else {
      return calculateTree180(features, 12);
    }
  }
  if (node === 6) return 0.0029658936314236432;
  if (node === 2) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree180(features, 5);
    } else if (val <= 13.500000000000002) {
      return calculateTree180(features, 5);
    } else {
      return calculateTree180(features, 6);
    }
  }
  if (node === 0) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree180(features, 1);
    } else if (val <= 891.0000000000001) {
      return calculateTree180(features, 1);
    } else {
      return calculateTree180(features, 2);
    }
  }
}
function calculateTree181(features, node = 0) {
  if (node === 15) return -0.005375661073188571;
  if (node === 33) return 0.01750692005930189;
  if (node === 69) return -0.342717623882162;
  if (node === 1135) return -0.0842766677496972;
  if (node === 1136) return -0.305813255732997;
  if (node === 567) {
    const val = features['is_preference_home'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree181(features, 1135);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree181(features, 1135);
    } else {
      return calculateTree181(features, 1136);
    }
  }
  if (node === 568) return 0.03774749828875784;
  if (node === 283) {
    const val = features['is_preference_kids'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree181(features, 567);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree181(features, 567);
    } else {
      return calculateTree181(features, 568);
    }
  }
  if (node === 284) return 0.11517079079589347;
  if (node === 141) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree181(features, 283);
    } else if (val <= 20.500000000000004) {
      return calculateTree181(features, 283);
    } else {
      return calculateTree181(features, 284);
    }
  }
  if (node === 142) return -0.2603518630982204;
  if (node === 70) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree181(features, 141);
    } else if (val <= 25.500000000000004) {
      return calculateTree181(features, 141);
    } else {
      return calculateTree181(features, 142);
    }
  }
  if (node === 34) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree181(features, 69);
    } else if (val <= 13.500000000000002) {
      return calculateTree181(features, 69);
    } else {
      return calculateTree181(features, 70);
    }
  }
  if (node === 16) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree181(features, 33);
    } else if (val <= 1.5000000000000002) {
      return calculateTree181(features, 33);
    } else {
      return calculateTree181(features, 34);
    }
  }
  if (node === 7) {
    const val = features['is_preference_sports'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree181(features, 15);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree181(features, 15);
    } else {
      return calculateTree181(features, 16);
    }
  }
  if (node === 71) return 0.0415523462048668;
  if (node === 72) return -0.17567902754308948;
  if (node === 35) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree181(features, 71);
    } else if (val <= 12.500000000000002) {
      return calculateTree181(features, 71);
    } else {
      return calculateTree181(features, 72);
    }
  }
  if (node === 147) return -0.27761194939548667;
  if (node === 297) return -0.26724491602764455;
  if (node === 597) return 0.273935196749731;
  if (node === 598) return -0.06291514498905638;
  if (node === 298) {
    const val = features['is_preference_beauty'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree181(features, 597);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree181(features, 597);
    } else {
      return calculateTree181(features, 598);
    }
  }
  if (node === 148) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree181(features, 297);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree181(features, 297);
    } else {
      return calculateTree181(features, 298);
    }
  }
  if (node === 73) {
    const val = features['is_preference_sports'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree181(features, 147);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree181(features, 147);
    } else {
      return calculateTree181(features, 148);
    }
  }
  if (node === 74) return -0.39867637853152266;
  if (node === 36) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree181(features, 73);
    } else if (val <= 9.500000000000002) {
      return calculateTree181(features, 73);
    } else {
      return calculateTree181(features, 74);
    }
  }
  if (node === 17) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree181(features, 35);
    } else if (val <= 34.00000000000001) {
      return calculateTree181(features, 35);
    } else {
      return calculateTree181(features, 36);
    }
  }
  if (node === 75) return 0.25313977923752845;
  if (node === 615) return 0.11077523953211943;
  if (node === 616) return 0.428368472697965;
  if (node === 307) {
    const val = features['is_preference_household'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree181(features, 615);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree181(features, 615);
    } else {
      return calculateTree181(features, 616);
    }
  }
  if (node === 308) return -0.010042517608510819;
  if (node === 153) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree181(features, 307);
    } else if (val <= 13.500000000000002) {
      return calculateTree181(features, 307);
    } else {
      return calculateTree181(features, 308);
    }
  }
  if (node === 1239) return 0.08008702163962801;
  if (node === 1240) return 0.3270070806305762;
  if (node === 619) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree181(features, 1239);
    } else if (val <= 46.50000000000001) {
      return calculateTree181(features, 1239);
    } else {
      return calculateTree181(features, 1240);
    }
  }
  if (node === 620) return -0.022709871454458696;
  if (node === 309) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree181(features, 619);
    } else if (val <= 880.0000000000001) {
      return calculateTree181(features, 619);
    } else {
      return calculateTree181(features, 620);
    }
  }
  if (node === 621) return -0.2286181045710625;
  if (node === 622) return 0.02681520163796752;
  if (node === 310) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree181(features, 621);
    } else if (val <= 54.50000000000001) {
      return calculateTree181(features, 621);
    } else {
      return calculateTree181(features, 622);
    }
  }
  if (node === 154) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree181(features, 309);
    } else if (val <= 49.50000000000001) {
      return calculateTree181(features, 309);
    } else {
      return calculateTree181(features, 310);
    }
  }
  if (node === 76) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree181(features, 153);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree181(features, 153);
    } else {
      return calculateTree181(features, 154);
    }
  }
  if (node === 37) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree181(features, 75);
    } else if (val <= 143.50000000000003) {
      return calculateTree181(features, 75);
    } else {
      return calculateTree181(features, 76);
    }
  }
  if (node === 77) return -0.21263488884209014;
  if (node === 157) return 0.2246166907732232;
  if (node === 158) return -0.0724137179494998;
  if (node === 78) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree181(features, 157);
    } else if (val <= 6.500000000000001) {
      return calculateTree181(features, 157);
    } else {
      return calculateTree181(features, 158);
    }
  }
  if (node === 38) {
    const val = features['is_preference_beauty'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree181(features, 77);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree181(features, 77);
    } else {
      return calculateTree181(features, 78);
    }
  }
  if (node === 18) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree181(features, 37);
    } else if (val <= 926.0000000000001) {
      return calculateTree181(features, 37);
    } else {
      return calculateTree181(features, 38);
    }
  }
  if (node === 8) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree181(features, 17);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree181(features, 17);
    } else {
      return calculateTree181(features, 18);
    }
  }
  if (node === 3) {
    const val = features['is_preference_men_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree181(features, 7);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree181(features, 7);
    } else {
      return calculateTree181(features, 8);
    }
  }
  if (node === 19) return 0.06521088293604256;
  if (node === 20) return 0.24434269201048786;
  if (node === 9) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree181(features, 19);
    } else if (val <= 1.5000000000000002) {
      return calculateTree181(features, 19);
    } else {
      return calculateTree181(features, 20);
    }
  }
  if (node === 21) return -0.10897055744651982;
  if (node === 22) return 0.10698821412947634;
  if (node === 10) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree181(features, 21);
    } else if (val <= 10.500000000000002) {
      return calculateTree181(features, 21);
    } else {
      return calculateTree181(features, 22);
    }
  }
  if (node === 4) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree181(features, 9);
    } else if (val <= 19.000000000000004) {
      return calculateTree181(features, 9);
    } else {
      return calculateTree181(features, 10);
    }
  }
  if (node === 1) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree181(features, 3);
    } else if (val <= 62.50000000000001) {
      return calculateTree181(features, 3);
    } else {
      return calculateTree181(features, 4);
    }
  }
  if (node === 2) return 0.0006040012265701156;
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree181(features, 1);
    } else if (val <= 39.50000000000001) {
      return calculateTree181(features, 1);
    } else {
      return calculateTree181(features, 2);
    }
  }
}
function calculateTree182(features, node = 0) {
  if (node === 255) return -0.19629973910452334;
  if (node === 1027) return 0.014465014974421058;
  if (node === 1028) return 0.3142184843659193;
  if (node === 513) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree182(features, 1027);
    } else if (val <= 660.0000000000001) {
      return calculateTree182(features, 1027);
    } else {
      return calculateTree182(features, 1028);
    }
  }
  if (node === 514) return -0.05891747467453732;
  if (node === 256) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree182(features, 513);
    } else if (val <= 606.5000000000001) {
      return calculateTree182(features, 513);
    } else {
      return calculateTree182(features, 514);
    }
  }
  if (node === 127) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree182(features, 255);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree182(features, 255);
    } else {
      return calculateTree182(features, 256);
    }
  }
  if (node === 515) return -0.30400543403720437;
  if (node === 1033) return -0.08769445474986774;
  if (node === 1034) return 0.18565753737087481;
  if (node === 516) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree182(features, 1033);
    } else if (val <= 804.0000000000001) {
      return calculateTree182(features, 1033);
    } else {
      return calculateTree182(features, 1034);
    }
  }
  if (node === 257) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree182(features, 515);
    } else if (val <= 18.500000000000004) {
      return calculateTree182(features, 515);
    } else {
      return calculateTree182(features, 516);
    }
  }
  if (node === 258) return 0.14022016093621276;
  if (node === 128) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree182(features, 257);
    } else if (val <= 659.5000000000001) {
      return calculateTree182(features, 257);
    } else {
      return calculateTree182(features, 258);
    }
  }
  if (node === 63) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree182(features, 127);
    } else if (val <= 16.500000000000004) {
      return calculateTree182(features, 127);
    } else {
      return calculateTree182(features, 128);
    }
  }
  if (node === 129) return 0.2071197820062361;
  if (node === 130) return 0.00303613079681867;
  if (node === 64) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree182(features, 129);
    } else if (val <= 2.5000000000000004) {
      return calculateTree182(features, 129);
    } else {
      return calculateTree182(features, 130);
    }
  }
  if (node === 31) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree182(features, 63);
    } else if (val <= 26.500000000000004) {
      return calculateTree182(features, 63);
    } else {
      return calculateTree182(features, 64);
    }
  }
  if (node === 65) return -0.3366803086574943;
  if (node === 1071) return -0.03301242416881182;
  if (node === 1072) return 0.16464389373471558;
  if (node === 535) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree182(features, 1071);
    } else if (val <= 794.0000000000001) {
      return calculateTree182(features, 1071);
    } else {
      return calculateTree182(features, 1072);
    }
  }
  if (node === 1073) return 0.08474521782813606;
  if (node === 1074) return -0.15629688028030322;
  if (node === 536) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree182(features, 1073);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree182(features, 1073);
    } else {
      return calculateTree182(features, 1074);
    }
  }
  if (node === 267) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree182(features, 535);
    } else if (val <= 8.500000000000002) {
      return calculateTree182(features, 535);
    } else {
      return calculateTree182(features, 536);
    }
  }
  if (node === 268) return -0.2067927727157995;
  if (node === 133) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree182(features, 267);
    } else if (val <= 695.5000000000001) {
      return calculateTree182(features, 267);
    } else {
      return calculateTree182(features, 268);
    }
  }
  if (node === 1079) return 0.048134499101447285;
  if (node === 1080) return 0.39394636729928517;
  if (node === 539) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree182(features, 1079);
    } else if (val <= 4.500000000000001) {
      return calculateTree182(features, 1079);
    } else {
      return calculateTree182(features, 1080);
    }
  }
  if (node === 1081) return 0.19148967239190537;
  if (node === 1082) return -0.030464064365513598;
  if (node === 540) {
    const val = features['is_preference_kids'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree182(features, 1081);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree182(features, 1081);
    } else {
      return calculateTree182(features, 1082);
    }
  }
  if (node === 269) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree182(features, 539);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree182(features, 539);
    } else {
      return calculateTree182(features, 540);
    }
  }
  if (node === 270) return -0.14374485400898068;
  if (node === 134) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree182(features, 269);
    } else if (val <= 804.0000000000001) {
      return calculateTree182(features, 269);
    } else {
      return calculateTree182(features, 270);
    }
  }
  if (node === 66) {
    const val = features['is_preference_beauty'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree182(features, 133);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree182(features, 133);
    } else {
      return calculateTree182(features, 134);
    }
  }
  if (node === 32) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree182(features, 65);
    } else if (val <= 30.500000000000004) {
      return calculateTree182(features, 65);
    } else {
      return calculateTree182(features, 66);
    }
  }
  if (node === 15) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree182(features, 31);
    } else if (val <= 29.500000000000004) {
      return calculateTree182(features, 31);
    } else {
      return calculateTree182(features, 32);
    }
  }
  if (node === 33) return -0.36263905876222463;
  if (node === 34) return -0.09961833958115247;
  if (node === 16) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree182(features, 33);
    } else if (val <= 320.00000000000006) {
      return calculateTree182(features, 33);
    } else {
      return calculateTree182(features, 34);
    }
  }
  if (node === 7) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree182(features, 15);
    } else if (val <= 10.500000000000002) {
      return calculateTree182(features, 15);
    } else {
      return calculateTree182(features, 16);
    }
  }
  if (node === 35) return 0.11669041938063518;
  if (node === 36) return 0.34354679974425956;
  if (node === 17) {
    const val = features['is_preference_sports'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree182(features, 35);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree182(features, 35);
    } else {
      return calculateTree182(features, 36);
    }
  }
  if (node === 37) return -0.17657692841313827;
  if (node === 77) return -0.04941699729202881;
  if (node === 78) return 0.21096136361115422;
  if (node === 38) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree182(features, 77);
    } else if (val <= 32.50000000000001) {
      return calculateTree182(features, 77);
    } else {
      return calculateTree182(features, 78);
    }
  }
  if (node === 18) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree182(features, 37);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree182(features, 37);
    } else {
      return calculateTree182(features, 38);
    }
  }
  if (node === 8) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree182(features, 17);
    } else if (val <= 3.5000000000000004) {
      return calculateTree182(features, 17);
    } else {
      return calculateTree182(features, 18);
    }
  }
  if (node === 3) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree182(features, 7);
    } else if (val <= 12.500000000000002) {
      return calculateTree182(features, 7);
    } else {
      return calculateTree182(features, 8);
    }
  }
  if (node === 9) return 0.02044666306932397;
  if (node === 10) return -0.4029685612050456;
  if (node === 4) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree182(features, 9);
    } else if (val <= 749.0000000000001) {
      return calculateTree182(features, 9);
    } else {
      return calculateTree182(features, 10);
    }
  }
  if (node === 1) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree182(features, 3);
    } else if (val <= 28.500000000000004) {
      return calculateTree182(features, 3);
    } else {
      return calculateTree182(features, 4);
    }
  }
  if (node === 5) return -0.000668474694780206;
  if (node === 6) return 0.004511742456627081;
  if (node === 2) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree182(features, 5);
    } else if (val <= 775.5000000000001) {
      return calculateTree182(features, 5);
    } else {
      return calculateTree182(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree182(features, 1);
    } else if (val <= 10.000000000000002) {
      return calculateTree182(features, 1);
    } else {
      return calculateTree182(features, 2);
    }
  }
}
function calculateTree183(features, node = 0) {
  if (node === 3) return 0.0005309805136446176;
  if (node === 9) return -0.06363446250854438;
  if (node === 87) return 0.15433361981855262;
  if (node === 88) return -0.23836606787166129;
  if (node === 43) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree183(features, 87);
    } else if (val <= 675.5000000000001) {
      return calculateTree183(features, 87);
    } else {
      return calculateTree183(features, 88);
    }
  }
  if (node === 44) return 0.2726811254304722;
  if (node === 21) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree183(features, 43);
    } else if (val <= 6.500000000000001) {
      return calculateTree183(features, 43);
    } else {
      return calculateTree183(features, 44);
    }
  }
  if (node === 91) return 0.04996783622128838;
  if (node === 92) return 0.3713638934612015;
  if (node === 45) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree183(features, 91);
    } else if (val <= 20.500000000000004) {
      return calculateTree183(features, 91);
    } else {
      return calculateTree183(features, 92);
    }
  }
  if (node === 46) return -0.14927841387695934;
  if (node === 22) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree183(features, 45);
    } else if (val <= 26.500000000000004) {
      return calculateTree183(features, 45);
    } else {
      return calculateTree183(features, 46);
    }
  }
  if (node === 10) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree183(features, 21);
    } else if (val <= 467.50000000000006) {
      return calculateTree183(features, 21);
    } else {
      return calculateTree183(features, 22);
    }
  }
  if (node === 4) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree183(features, 9);
    } else if (val <= 171.00000000000003) {
      return calculateTree183(features, 9);
    } else {
      return calculateTree183(features, 10);
    }
  }
  if (node === 1) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree183(features, 3);
    } else if (val <= 339.50000000000006) {
      return calculateTree183(features, 3);
    } else {
      return calculateTree183(features, 4);
    }
  }
  if (node === 47) return -0.017868837755203645;
  if (node === 391) return -0.07510523400168885;
  if (node === 392) return 0.09410405176249699;
  if (node === 195) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree183(features, 391);
    } else if (val <= 12.500000000000002) {
      return calculateTree183(features, 391);
    } else {
      return calculateTree183(features, 392);
    }
  }
  if (node === 196) return -0.26298493334165407;
  if (node === 97) {
    const val = features['is_preference_household'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree183(features, 195);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree183(features, 195);
    } else {
      return calculateTree183(features, 196);
    }
  }
  if (node === 395) return -0.2577543319117669;
  if (node === 396) return 0.01725670996267212;
  if (node === 197) {
    const val = features['is_preference_household'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree183(features, 395);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree183(features, 395);
    } else {
      return calculateTree183(features, 396);
    }
  }
  if (node === 397) return -0.4724502911542737;
  if (node === 797) return -0.23779520037552596;
  if (node === 798) return 0.07641592176486893;
  if (node === 398) {
    const val = features['is_preference_sports'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree183(features, 797);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree183(features, 797);
    } else {
      return calculateTree183(features, 798);
    }
  }
  if (node === 198) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree183(features, 397);
    } else if (val <= 680.0000000000001) {
      return calculateTree183(features, 397);
    } else {
      return calculateTree183(features, 398);
    }
  }
  if (node === 98) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree183(features, 197);
    } else if (val <= 7.500000000000001) {
      return calculateTree183(features, 197);
    } else {
      return calculateTree183(features, 198);
    }
  }
  if (node === 48) {
    const val = features['is_preference_personal_care'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree183(features, 97);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree183(features, 97);
    } else {
      return calculateTree183(features, 98);
    }
  }
  if (node === 23) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree183(features, 47);
    } else if (val <= 555.5000000000001) {
      return calculateTree183(features, 47);
    } else {
      return calculateTree183(features, 48);
    }
  }
  if (node === 399) return -0.14039010328633386;
  if (node === 400) return 0.19735479090974098;
  if (node === 199) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree183(features, 399);
    } else if (val <= 954.5000000000001) {
      return calculateTree183(features, 399);
    } else {
      return calculateTree183(features, 400);
    }
  }
  if (node === 200) return -0.21220154809044264;
  if (node === 99) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree183(features, 199);
    } else if (val <= 5.500000000000001) {
      return calculateTree183(features, 199);
    } else {
      return calculateTree183(features, 200);
    }
  }
  if (node === 100) return 0.3881437005732175;
  if (node === 49) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree183(features, 99);
    } else if (val <= 9.500000000000002) {
      return calculateTree183(features, 99);
    } else {
      return calculateTree183(features, 100);
    }
  }
  if (node === 101) return -0.3533507713554037;
  if (node === 102) return 0.054878049204215455;
  if (node === 50) {
    const val = features['is_preference_kids'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree183(features, 101);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree183(features, 101);
    } else {
      return calculateTree183(features, 102);
    }
  }
  if (node === 24) {
    const val = features['is_preference_household'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree183(features, 49);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree183(features, 49);
    } else {
      return calculateTree183(features, 50);
    }
  }
  if (node === 11) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree183(features, 23);
    } else if (val <= 923.5000000000001) {
      return calculateTree183(features, 23);
    } else {
      return calculateTree183(features, 24);
    }
  }
  if (node === 25) return -0.004754790286125261;
  if (node === 26) return -0.036182774813164925;
  if (node === 12) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree183(features, 25);
    } else if (val <= 661.0000000000001) {
      return calculateTree183(features, 25);
    } else {
      return calculateTree183(features, 26);
    }
  }
  if (node === 5) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree183(features, 11);
    } else if (val <= 437.50000000000006) {
      return calculateTree183(features, 11);
    } else {
      return calculateTree183(features, 12);
    }
  }
  if (node === 13) return 0.035057443449898744;
  if (node === 239) return 0.033001107915564855;
  if (node === 481) return -0.3925972056679144;
  if (node === 482) return -0.06976686254988755;
  if (node === 240) {
    const val = features['is_preference_home'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree183(features, 481);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree183(features, 481);
    } else {
      return calculateTree183(features, 482);
    }
  }
  if (node === 119) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree183(features, 239);
    } else if (val <= 3.5000000000000004) {
      return calculateTree183(features, 239);
    } else {
      return calculateTree183(features, 240);
    }
  }
  if (node === 120) return 0.2403645617905299;
  if (node === 59) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree183(features, 119);
    } else if (val <= 902.0000000000001) {
      return calculateTree183(features, 119);
    } else {
      return calculateTree183(features, 120);
    }
  }
  if (node === 60) return -0.15716605332393582;
  if (node === 29) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree183(features, 59);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree183(features, 59);
    } else {
      return calculateTree183(features, 60);
    }
  }
  if (node === 30) return 0.002098283235175821;
  if (node === 14) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree183(features, 29);
    } else if (val <= 771.0000000000001) {
      return calculateTree183(features, 29);
    } else {
      return calculateTree183(features, 30);
    }
  }
  if (node === 6) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree183(features, 13);
    } else if (val <= 755.0000000000001) {
      return calculateTree183(features, 13);
    } else {
      return calculateTree183(features, 14);
    }
  }
  if (node === 2) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree183(features, 5);
    } else if (val <= 712.5000000000001) {
      return calculateTree183(features, 5);
    } else {
      return calculateTree183(features, 6);
    }
  }
  if (node === 0) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree183(features, 1);
    } else if (val <= 378.50000000000006) {
      return calculateTree183(features, 1);
    } else {
      return calculateTree183(features, 2);
    }
  }
}
function calculateTree184(features, node = 0) {
  if (node === 7) return 0.0009212580645099372;
  if (node === 17) return -0.05183608252047903;
  if (node === 18) return 0.12808373972054204;
  if (node === 8) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree184(features, 17);
    } else if (val <= 52.50000000000001) {
      return calculateTree184(features, 17);
    } else {
      return calculateTree184(features, 18);
    }
  }
  if (node === 3) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree184(features, 7);
    } else if (val <= 898.5000000000001) {
      return calculateTree184(features, 7);
    } else {
      return calculateTree184(features, 8);
    }
  }
  if (node === 39) return -0.15231534227704294;
  if (node === 40) return 0.11963429604038309;
  if (node === 19) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree184(features, 39);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree184(features, 39);
    } else {
      return calculateTree184(features, 40);
    }
  }
  if (node === 41) return 0.08842339194962867;
  if (node === 42) return -0.136832791569466;
  if (node === 20) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree184(features, 41);
    } else if (val <= 1.5000000000000002) {
      return calculateTree184(features, 41);
    } else {
      return calculateTree184(features, 42);
    }
  }
  if (node === 9) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree184(features, 19);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree184(features, 19);
    } else {
      return calculateTree184(features, 20);
    }
  }
  if (node === 21) return 0.251929209132249;
  if (node === 367) return 0.044260353840074776;
  if (node === 368) return 0.2187869315041374;
  if (node === 183) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree184(features, 367);
    } else if (val <= 339.50000000000006) {
      return calculateTree184(features, 367);
    } else {
      return calculateTree184(features, 368);
    }
  }
  if (node === 184) return -0.1687268182533143;
  if (node === 91) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree184(features, 183);
    } else if (val <= 880.0000000000001) {
      return calculateTree184(features, 183);
    } else {
      return calculateTree184(features, 184);
    }
  }
  if (node === 185) return -0.3358622926316933;
  if (node === 186) return -0.023447018029077414;
  if (node === 92) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree184(features, 185);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree184(features, 185);
    } else {
      return calculateTree184(features, 186);
    }
  }
  if (node === 45) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree184(features, 91);
    } else if (val <= 48.50000000000001) {
      return calculateTree184(features, 91);
    } else {
      return calculateTree184(features, 92);
    }
  }
  if (node === 93) return 0.04761740413878565;
  if (node === 94) return 0.25865831158768376;
  if (node === 46) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree184(features, 93);
    } else if (val <= 13.500000000000002) {
      return calculateTree184(features, 93);
    } else {
      return calculateTree184(features, 94);
    }
  }
  if (node === 22) {
    const val = features['is_preference_kitchen'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree184(features, 45);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree184(features, 45);
    } else {
      return calculateTree184(features, 46);
    }
  }
  if (node === 10) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree184(features, 21);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree184(features, 21);
    } else {
      return calculateTree184(features, 22);
    }
  }
  if (node === 4) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree184(features, 9);
    } else if (val <= 15.500000000000002) {
      return calculateTree184(features, 9);
    } else {
      return calculateTree184(features, 10);
    }
  }
  if (node === 1) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree184(features, 3);
    } else if (val <= 900.5000000000001) {
      return calculateTree184(features, 3);
    } else {
      return calculateTree184(features, 4);
    }
  }
  if (node === 5) return -0.3456599630076802;
  if (node === 27) return 0.03249978804007286;
  if (node === 57) return -0.3436603460226212;
  if (node === 117) return -0.2348837515194897;
  if (node === 118) return 0.03799264523097268;
  if (node === 58) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree184(features, 117);
    } else if (val <= 27.500000000000004) {
      return calculateTree184(features, 117);
    } else {
      return calculateTree184(features, 118);
    }
  }
  if (node === 28) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree184(features, 57);
    } else if (val <= 932.5000000000001) {
      return calculateTree184(features, 57);
    } else {
      return calculateTree184(features, 58);
    }
  }
  if (node === 13) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree184(features, 27);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree184(features, 27);
    } else {
      return calculateTree184(features, 28);
    }
  }
  if (node === 59) return -0.022186940611713436;
  if (node === 121) return 0.4341844334593063;
  if (node === 122) return 0.09764041231763225;
  if (node === 60) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree184(features, 121);
    } else if (val <= 42.50000000000001) {
      return calculateTree184(features, 121);
    } else {
      return calculateTree184(features, 122);
    }
  }
  if (node === 29) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree184(features, 59);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree184(features, 59);
    } else {
      return calculateTree184(features, 60);
    }
  }
  if (node === 247) return -0.0019422483616660163;
  if (node === 248) return -0.05613965218867768;
  if (node === 123) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree184(features, 247);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree184(features, 247);
    } else {
      return calculateTree184(features, 248);
    }
  }
  if (node === 1999) return 0.03066897748203405;
  if (node === 2000) return 0.18144090457406112;
  if (node === 999) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree184(features, 1999);
    } else if (val <= 35.50000000000001) {
      return calculateTree184(features, 1999);
    } else {
      return calculateTree184(features, 2000);
    }
  }
  if (node === 1000) return -0.17711348479712222;
  if (node === 499) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree184(features, 999);
    } else if (val <= 42.50000000000001) {
      return calculateTree184(features, 999);
    } else {
      return calculateTree184(features, 1000);
    }
  }
  if (node === 500) return 0.21596962804807496;
  if (node === 249) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree184(features, 499);
    } else if (val <= 45.50000000000001) {
      return calculateTree184(features, 499);
    } else {
      return calculateTree184(features, 500);
    }
  }
  if (node === 250) return -0.003416926814785186;
  if (node === 124) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree184(features, 249);
    } else if (val <= 941.5000000000001) {
      return calculateTree184(features, 249);
    } else {
      return calculateTree184(features, 250);
    }
  }
  if (node === 61) {
    const val = features['is_preference_men_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree184(features, 123);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree184(features, 123);
    } else {
      return calculateTree184(features, 124);
    }
  }
  if (node === 125) return -0.1843504109011425;
  if (node === 126) return -0.01966599933553422;
  if (node === 62) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree184(features, 125);
    } else if (val <= 937.5000000000001) {
      return calculateTree184(features, 125);
    } else {
      return calculateTree184(features, 126);
    }
  }
  if (node === 30) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree184(features, 61);
    } else if (val <= 13.500000000000002) {
      return calculateTree184(features, 61);
    } else {
      return calculateTree184(features, 62);
    }
  }
  if (node === 14) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree184(features, 29);
    } else if (val <= 935.5000000000001) {
      return calculateTree184(features, 29);
    } else {
      return calculateTree184(features, 30);
    }
  }
  if (node === 6) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree184(features, 13);
    } else if (val <= 934.0000000000001) {
      return calculateTree184(features, 13);
    } else {
      return calculateTree184(features, 14);
    }
  }
  if (node === 2) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree184(features, 5);
    } else if (val <= 925.5000000000001) {
      return calculateTree184(features, 5);
    } else {
      return calculateTree184(features, 6);
    }
  }
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree184(features, 1);
    } else if (val <= 911.0000000000001) {
      return calculateTree184(features, 1);
    } else {
      return calculateTree184(features, 2);
    }
  }
}
function calculateTree185(features, node = 0) {
  if (node === 1) return 0.0009160890989195554;
  if (node === 23) return 0.021790901063672402;
  if (node === 399) return -0.020506473617133557;
  if (node === 400) return -0.2431955442503368;
  if (node === 199) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree185(features, 399);
    } else if (val <= 745.5000000000001) {
      return calculateTree185(features, 399);
    } else {
      return calculateTree185(features, 400);
    }
  }
  if (node === 401) return 0.019179748767585287;
  if (node === 402) return 0.25240938570701954;
  if (node === 200) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree185(features, 401);
    } else if (val <= 14.500000000000002) {
      return calculateTree185(features, 401);
    } else {
      return calculateTree185(features, 402);
    }
  }
  if (node === 99) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree185(features, 199);
    } else if (val <= 794.0000000000001) {
      return calculateTree185(features, 199);
    } else {
      return calculateTree185(features, 200);
    }
  }
  if (node === 100) return -0.27108709674739817;
  if (node === 49) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree185(features, 99);
    } else if (val <= 835.5000000000001) {
      return calculateTree185(features, 99);
    } else {
      return calculateTree185(features, 100);
    }
  }
  if (node === 203) return -0.35782465801004204;
  if (node === 409) return 0.4284810480056223;
  if (node === 410) return 0.20463100420900843;
  if (node === 204) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree185(features, 409);
    } else if (val <= 656.5000000000001) {
      return calculateTree185(features, 409);
    } else {
      return calculateTree185(features, 410);
    }
  }
  if (node === 101) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree185(features, 203);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree185(features, 203);
    } else {
      return calculateTree185(features, 204);
    }
  }
  if (node === 411) return -0.34884342946611385;
  if (node === 412) return -0.012630345236976823;
  if (node === 205) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree185(features, 411);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree185(features, 411);
    } else {
      return calculateTree185(features, 412);
    }
  }
  if (node === 206) return 0.001824146986872734;
  if (node === 102) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree185(features, 205);
    } else if (val <= 954.5000000000001) {
      return calculateTree185(features, 205);
    } else {
      return calculateTree185(features, 206);
    }
  }
  if (node === 50) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree185(features, 101);
    } else if (val <= 916.5000000000001) {
      return calculateTree185(features, 101);
    } else {
      return calculateTree185(features, 102);
    }
  }
  if (node === 24) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree185(features, 49);
    } else if (val <= 908.5000000000001) {
      return calculateTree185(features, 49);
    } else {
      return calculateTree185(features, 50);
    }
  }
  if (node === 11) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree185(features, 23);
    } else if (val <= 127.00000000000001) {
      return calculateTree185(features, 23);
    } else {
      return calculateTree185(features, 24);
    }
  }
  if (node === 12) return 0.11298362465516926;
  if (node === 5) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree185(features, 11);
    } else if (val <= 61.50000000000001) {
      return calculateTree185(features, 11);
    } else {
      return calculateTree185(features, 12);
    }
  }
  if (node === 111) return -0.3309000414856837;
  if (node === 225) return -0.0011983805112840192;
  if (node === 226) return -0.34463070523428996;
  if (node === 112) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree185(features, 225);
    } else if (val <= 34.50000000000001) {
      return calculateTree185(features, 225);
    } else {
      return calculateTree185(features, 226);
    }
  }
  if (node === 55) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree185(features, 111);
    } else if (val <= 282.50000000000006) {
      return calculateTree185(features, 111);
    } else {
      return calculateTree185(features, 112);
    }
  }
  if (node === 56) return -0.015392005068829817;
  if (node === 27) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree185(features, 55);
    } else if (val <= 678.5000000000001) {
      return calculateTree185(features, 55);
    } else {
      return calculateTree185(features, 56);
    }
  }
  if (node === 57) return 0.08714862568486557;
  if (node === 117) return -0.2806272016325288;
  if (node === 1903) return 0.10823705333954699;
  if (node === 1904) return -0.2395794836700574;
  if (node === 951) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree185(features, 1903);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree185(features, 1903);
    } else {
      return calculateTree185(features, 1904);
    }
  }
  if (node === 1905) return 0.6266628746458008;
  if (node === 1906) return 0.11534670855111227;
  if (node === 952) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree185(features, 1905);
    } else if (val <= 40.50000000000001) {
      return calculateTree185(features, 1905);
    } else {
      return calculateTree185(features, 1906);
    }
  }
  if (node === 475) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree185(features, 951);
    } else if (val <= 35.50000000000001) {
      return calculateTree185(features, 951);
    } else {
      return calculateTree185(features, 952);
    }
  }
  if (node === 953) return -0.3027387818279815;
  if (node === 954) return 0.15156341238305382;
  if (node === 476) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree185(features, 953);
    } else if (val <= 882.5000000000001) {
      return calculateTree185(features, 953);
    } else {
      return calculateTree185(features, 954);
    }
  }
  if (node === 237) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree185(features, 475);
    } else if (val <= 774.0000000000001) {
      return calculateTree185(features, 475);
    } else {
      return calculateTree185(features, 476);
    }
  }
  if (node === 955) return 0.070327224391204;
  if (node === 956) return -0.30085175098519984;
  if (node === 477) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree185(features, 955);
    } else if (val <= 740.5000000000001) {
      return calculateTree185(features, 955);
    } else {
      return calculateTree185(features, 956);
    }
  }
  if (node === 957) return 0.0824093893086557;
  if (node === 1917) return -0.13653190058723763;
  if (node === 1918) return 0.1135285163860813;
  if (node === 958) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree185(features, 1917);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree185(features, 1917);
    } else {
      return calculateTree185(features, 1918);
    }
  }
  if (node === 478) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree185(features, 957);
    } else if (val <= 155.50000000000003) {
      return calculateTree185(features, 957);
    } else {
      return calculateTree185(features, 958);
    }
  }
  if (node === 238) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree185(features, 477);
    } else if (val <= 774.0000000000001) {
      return calculateTree185(features, 477);
    } else {
      return calculateTree185(features, 478);
    }
  }
  if (node === 118) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree185(features, 237);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree185(features, 237);
    } else {
      return calculateTree185(features, 238);
    }
  }
  if (node === 58) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree185(features, 117);
    } else if (val <= 709.5000000000001) {
      return calculateTree185(features, 117);
    } else {
      return calculateTree185(features, 118);
    }
  }
  if (node === 28) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree185(features, 57);
    } else if (val <= 566.0000000000001) {
      return calculateTree185(features, 57);
    } else {
      return calculateTree185(features, 58);
    }
  }
  if (node === 13) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree185(features, 27);
    } else if (val <= 398.00000000000006) {
      return calculateTree185(features, 27);
    } else {
      return calculateTree185(features, 28);
    }
  }
  if (node === 14) return -0.0022862932432620876;
  if (node === 6) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree185(features, 13);
    } else if (val <= 708.5000000000001) {
      return calculateTree185(features, 13);
    } else {
      return calculateTree185(features, 14);
    }
  }
  if (node === 2) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree185(features, 5);
    } else if (val <= 677.5000000000001) {
      return calculateTree185(features, 5);
    } else {
      return calculateTree185(features, 6);
    }
  }
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree185(features, 1);
    } else if (val <= 611.0000000000001) {
      return calculateTree185(features, 1);
    } else {
      return calculateTree185(features, 2);
    }
  }
}
function calculateTree186(features, node = 0) {
  if (node === 1) return 0.0003847902079164612;
  if (node === 191) return -0.013854323665925043;
  if (node === 192) return 0.04412576518589305;
  if (node === 95) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree186(features, 191);
    } else if (val <= 31.500000000000004) {
      return calculateTree186(features, 191);
    } else {
      return calculateTree186(features, 192);
    }
  }
  if (node === 96) return -0.07334030140798445;
  if (node === 47) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree186(features, 95);
    } else if (val <= 40.50000000000001) {
      return calculateTree186(features, 95);
    } else {
      return calculateTree186(features, 96);
    }
  }
  if (node === 97) return 0.187258839096483;
  if (node === 197) return -0.31624668244033544;
  if (node === 198) return 0.06120936277140794;
  if (node === 98) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree186(features, 197);
    } else if (val <= 40.50000000000001) {
      return calculateTree186(features, 197);
    } else {
      return calculateTree186(features, 198);
    }
  }
  if (node === 48) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree186(features, 97);
    } else if (val <= 15.500000000000002) {
      return calculateTree186(features, 97);
    } else {
      return calculateTree186(features, 98);
    }
  }
  if (node === 23) {
    const val = features['is_preference_women_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree186(features, 47);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree186(features, 47);
    } else {
      return calculateTree186(features, 48);
    }
  }
  if (node === 199) return 0.11157003825224752;
  if (node === 803) return 0.17328474309905625;
  if (node === 804) return -0.1659546678376195;
  if (node === 401) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree186(features, 803);
    } else if (val <= 21.500000000000004) {
      return calculateTree186(features, 803);
    } else {
      return calculateTree186(features, 804);
    }
  }
  if (node === 402) return -0.33109901085378124;
  if (node === 200) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree186(features, 401);
    } else if (val <= 3.5000000000000004) {
      return calculateTree186(features, 401);
    } else {
      return calculateTree186(features, 402);
    }
  }
  if (node === 99) {
    const val = features['is_preference_beauty'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree186(features, 199);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree186(features, 199);
    } else {
      return calculateTree186(features, 200);
    }
  }
  if (node === 403) return 0.5473164334003158;
  if (node === 404) return 0.22103471336294392;
  if (node === 201) {
    const val = features['is_preference_pets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree186(features, 403);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree186(features, 403);
    } else {
      return calculateTree186(features, 404);
    }
  }
  if (node === 202) return 0.11973250324585469;
  if (node === 100) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree186(features, 201);
    } else if (val <= 8.500000000000002) {
      return calculateTree186(features, 201);
    } else {
      return calculateTree186(features, 202);
    }
  }
  if (node === 49) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree186(features, 99);
    } else if (val <= 234.50000000000003) {
      return calculateTree186(features, 99);
    } else {
      return calculateTree186(features, 100);
    }
  }
  if (node === 203) return 0.02561207325385253;
  if (node === 204) return -0.2888467425205895;
  if (node === 101) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree186(features, 203);
    } else if (val <= 5.500000000000001) {
      return calculateTree186(features, 203);
    } else {
      return calculateTree186(features, 204);
    }
  }
  if (node === 411) return -0.029019591516147324;
  if (node === 825) return 0.06718369957474055;
  if (node === 826) return 0.29827014846304345;
  if (node === 412) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree186(features, 825);
    } else if (val <= 5.500000000000001) {
      return calculateTree186(features, 825);
    } else {
      return calculateTree186(features, 826);
    }
  }
  if (node === 205) {
    const val = features['is_preference_pets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree186(features, 411);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree186(features, 411);
    } else {
      return calculateTree186(features, 412);
    }
  }
  if (node === 413) return -0.3139056533535138;
  if (node === 414) return 0.04567898915848351;
  if (node === 206) {
    const val = features['is_preference_sports'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree186(features, 413);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree186(features, 413);
    } else {
      return calculateTree186(features, 414);
    }
  }
  if (node === 102) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree186(features, 205);
    } else if (val <= 725.5000000000001) {
      return calculateTree186(features, 205);
    } else {
      return calculateTree186(features, 206);
    }
  }
  if (node === 50) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree186(features, 101);
    } else if (val <= 35.50000000000001) {
      return calculateTree186(features, 101);
    } else {
      return calculateTree186(features, 102);
    }
  }
  if (node === 24) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree186(features, 49);
    } else if (val <= 31.500000000000004) {
      return calculateTree186(features, 49);
    } else {
      return calculateTree186(features, 50);
    }
  }
  if (node === 11) {
    const val = features['is_preference_home'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree186(features, 23);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree186(features, 23);
    } else {
      return calculateTree186(features, 24);
    }
  }
  if (node === 25) return 0.25900418349635373;
  if (node === 26) return -0.009448751407177356;
  if (node === 12) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree186(features, 25);
    } else if (val <= 3.5000000000000004) {
      return calculateTree186(features, 25);
    } else {
      return calculateTree186(features, 26);
    }
  }
  if (node === 5) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree186(features, 11);
    } else if (val <= 45.50000000000001) {
      return calculateTree186(features, 11);
    } else {
      return calculateTree186(features, 12);
    }
  }
  if (node === 13) return -0.2906660162279858;
  if (node === 59) return -0.09777365793476038;
  if (node === 60) return 0.11863822472828864;
  if (node === 29) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree186(features, 59);
    } else if (val <= 62.50000000000001) {
      return calculateTree186(features, 59);
    } else {
      return calculateTree186(features, 60);
    }
  }
  if (node === 495) return 0.21996910553219873;
  if (node === 496) return -0.03336199439485273;
  if (node === 247) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree186(features, 495);
    } else if (val <= 51.50000000000001) {
      return calculateTree186(features, 495);
    } else {
      return calculateTree186(features, 496);
    }
  }
  if (node === 248) return 0.28781204061936505;
  if (node === 123) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree186(features, 247);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree186(features, 247);
    } else {
      return calculateTree186(features, 248);
    }
  }
  if (node === 124) return -0.08648117129855572;
  if (node === 61) {
    const val = features['is_preference_personal_care'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree186(features, 123);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree186(features, 123);
    } else {
      return calculateTree186(features, 124);
    }
  }
  if (node === 125) return -0.24322685822243298;
  if (node === 126) return 0.058292525970777775;
  if (node === 62) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree186(features, 125);
    } else if (val <= 52.50000000000001) {
      return calculateTree186(features, 125);
    } else {
      return calculateTree186(features, 126);
    }
  }
  if (node === 30) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree186(features, 61);
    } else if (val <= 437.50000000000006) {
      return calculateTree186(features, 61);
    } else {
      return calculateTree186(features, 62);
    }
  }
  if (node === 14) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree186(features, 29);
    } else if (val <= 4.500000000000001) {
      return calculateTree186(features, 29);
    } else {
      return calculateTree186(features, 30);
    }
  }
  if (node === 6) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree186(features, 13);
    } else if (val <= 283.50000000000006) {
      return calculateTree186(features, 13);
    } else {
      return calculateTree186(features, 14);
    }
  }
  if (node === 2) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree186(features, 5);
    } else if (val <= 46.50000000000001) {
      return calculateTree186(features, 5);
    } else {
      return calculateTree186(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree186(features, 1);
    } else if (val <= 986.5000000000001) {
      return calculateTree186(features, 1);
    } else {
      return calculateTree186(features, 2);
    }
  }
}
function calculateTree187(features, node = 0) {
  if (node === 1) return -0.0009569705267147851;
  if (node === 191) return 0.01977804957362016;
  if (node === 192) return 0.2755776347803775;
  if (node === 95) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree187(features, 191);
    } else if (val <= 982.5000000000001) {
      return calculateTree187(features, 191);
    } else {
      return calculateTree187(features, 192);
    }
  }
  if (node === 387) return -0.08063708480667789;
  if (node === 388) return 0.21409015063203543;
  if (node === 193) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree187(features, 387);
    } else if (val <= 599.5000000000001) {
      return calculateTree187(features, 387);
    } else {
      return calculateTree187(features, 388);
    }
  }
  if (node === 194) return -0.32800609442026196;
  if (node === 96) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree187(features, 193);
    } else if (val <= 804.5000000000001) {
      return calculateTree187(features, 193);
    } else {
      return calculateTree187(features, 194);
    }
  }
  if (node === 47) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree187(features, 95);
    } else if (val <= 17.500000000000004) {
      return calculateTree187(features, 95);
    } else {
      return calculateTree187(features, 96);
    }
  }
  if (node === 97) return 0.4269992437506767;
  if (node === 197) return 0.23073769357121798;
  if (node === 198) return -0.12981634230078512;
  if (node === 98) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree187(features, 197);
    } else if (val <= 338.50000000000006) {
      return calculateTree187(features, 197);
    } else {
      return calculateTree187(features, 198);
    }
  }
  if (node === 48) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree187(features, 97);
    } else if (val <= 24.500000000000004) {
      return calculateTree187(features, 97);
    } else {
      return calculateTree187(features, 98);
    }
  }
  if (node === 23) {
    const val = features['is_preference_pets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree187(features, 47);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree187(features, 47);
    } else {
      return calculateTree187(features, 48);
    }
  }
  if (node === 49) return 0.0922746554556406;
  if (node === 101) return -0.23578065909409143;
  if (node === 102) return 0.013658642789219102;
  if (node === 50) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree187(features, 101);
    } else if (val <= 42.50000000000001) {
      return calculateTree187(features, 101);
    } else {
      return calculateTree187(features, 102);
    }
  }
  if (node === 24) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree187(features, 49);
    } else if (val <= 251.00000000000003) {
      return calculateTree187(features, 49);
    } else {
      return calculateTree187(features, 50);
    }
  }
  if (node === 11) {
    const val = features['is_preference_sports'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree187(features, 23);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree187(features, 23);
    } else {
      return calculateTree187(features, 24);
    }
  }
  if (node === 415) return 0.46155439785715424;
  if (node === 416) return 0.18655448286053575;
  if (node === 207) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree187(features, 415);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree187(features, 415);
    } else {
      return calculateTree187(features, 416);
    }
  }
  if (node === 208) return 0.015009989820870841;
  if (node === 103) {
    const val = features['is_preference_men_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree187(features, 207);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree187(features, 207);
    } else {
      return calculateTree187(features, 208);
    }
  }
  if (node === 209) return -0.019714542199580697;
  if (node === 210) return 0.2929193424421094;
  if (node === 104) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree187(features, 209);
    } else if (val <= 817.5000000000001) {
      return calculateTree187(features, 209);
    } else {
      return calculateTree187(features, 210);
    }
  }
  if (node === 51) {
    const val = features['is_preference_kids'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree187(features, 103);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree187(features, 103);
    } else {
      return calculateTree187(features, 104);
    }
  }
  if (node === 52) return -0.05581033517851073;
  if (node === 25) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree187(features, 51);
    } else if (val <= 1.5000000000000002) {
      return calculateTree187(features, 51);
    } else {
      return calculateTree187(features, 52);
    }
  }
  if (node === 863) return 0.24072817730423762;
  if (node === 864) return -0.16443632166284375;
  if (node === 431) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree187(features, 863);
    } else if (val <= 30.500000000000004) {
      return calculateTree187(features, 863);
    } else {
      return calculateTree187(features, 864);
    }
  }
  if (node === 432) return 0.3292501338166275;
  if (node === 215) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree187(features, 431);
    } else if (val <= 11.500000000000002) {
      return calculateTree187(features, 431);
    } else {
      return calculateTree187(features, 432);
    }
  }
  if (node === 216) return -0.1821965703836784;
  if (node === 107) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree187(features, 215);
    } else if (val <= 417.00000000000006) {
      return calculateTree187(features, 215);
    } else {
      return calculateTree187(features, 216);
    }
  }
  if (node === 435) return 0.42240799652700173;
  if (node === 436) return 0.11577046011864232;
  if (node === 217) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree187(features, 435);
    } else if (val <= 33.50000000000001) {
      return calculateTree187(features, 435);
    } else {
      return calculateTree187(features, 436);
    }
  }
  if (node === 218) return -0.08278705947715229;
  if (node === 108) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree187(features, 217);
    } else if (val <= 868.0000000000001) {
      return calculateTree187(features, 217);
    } else {
      return calculateTree187(features, 218);
    }
  }
  if (node === 53) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree187(features, 107);
    } else if (val <= 576.5000000000001) {
      return calculateTree187(features, 107);
    } else {
      return calculateTree187(features, 108);
    }
  }
  if (node === 54) return -0.10256223771738573;
  if (node === 26) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree187(features, 53);
    } else if (val <= 43.50000000000001) {
      return calculateTree187(features, 53);
    } else {
      return calculateTree187(features, 54);
    }
  }
  if (node === 12) {
    const val = features['is_preference_pets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree187(features, 25);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree187(features, 25);
    } else {
      return calculateTree187(features, 26);
    }
  }
  if (node === 5) {
    const val = features['is_preference_household'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree187(features, 11);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree187(features, 11);
    } else {
      return calculateTree187(features, 12);
    }
  }
  if (node === 27) return -0.01109532014257409;
  if (node === 28) return -0.14361536033664002;
  if (node === 13) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree187(features, 27);
    } else if (val <= 57.50000000000001) {
      return calculateTree187(features, 27);
    } else {
      return calculateTree187(features, 28);
    }
  }
  if (node === 59) return 0.4216899155135537;
  if (node === 60) return 0.11145572438618019;
  if (node === 29) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree187(features, 59);
    } else if (val <= 4.500000000000001) {
      return calculateTree187(features, 59);
    } else {
      return calculateTree187(features, 60);
    }
  }
  if (node === 61) return -0.3142757513655361;
  if (node === 62) return 0.002013835534947481;
  if (node === 30) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree187(features, 61);
    } else if (val <= 83.50000000000001) {
      return calculateTree187(features, 61);
    } else {
      return calculateTree187(features, 62);
    }
  }
  if (node === 14) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree187(features, 29);
    } else if (val <= 75.50000000000001) {
      return calculateTree187(features, 29);
    } else {
      return calculateTree187(features, 30);
    }
  }
  if (node === 6) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree187(features, 13);
    } else if (val <= 70.00000000000001) {
      return calculateTree187(features, 13);
    } else {
      return calculateTree187(features, 14);
    }
  }
  if (node === 2) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree187(features, 5);
    } else if (val <= 847.0000000000001) {
      return calculateTree187(features, 5);
    } else {
      return calculateTree187(features, 6);
    }
  }
  if (node === 0) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree187(features, 1);
    } else if (val <= 804.0000000000001) {
      return calculateTree187(features, 1);
    } else {
      return calculateTree187(features, 2);
    }
  }
}
function calculateTree188(features, node = 0) {
  if (node === 3) return -0.000900121198247985;
  if (node === 79) return 0.595173691183745;
  if (node === 80) return 0.0021624493546863396;
  if (node === 39) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree188(features, 79);
    } else if (val <= 36.50000000000001) {
      return calculateTree188(features, 79);
    } else {
      return calculateTree188(features, 80);
    }
  }
  if (node === 163) return -0.16448665004708113;
  if (node === 164) return 0.09748595389723269;
  if (node === 81) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree188(features, 163);
    } else if (val <= 435.00000000000006) {
      return calculateTree188(features, 163);
    } else {
      return calculateTree188(features, 164);
    }
  }
  if (node === 82) return -0.31235162492597507;
  if (node === 40) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree188(features, 81);
    } else if (val <= 25.500000000000004) {
      return calculateTree188(features, 81);
    } else {
      return calculateTree188(features, 82);
    }
  }
  if (node === 19) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree188(features, 39);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree188(features, 39);
    } else {
      return calculateTree188(features, 40);
    }
  }
  if (node === 20) return -0.2851096781327026;
  if (node === 9) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree188(features, 19);
    } else if (val <= 781.0000000000001) {
      return calculateTree188(features, 19);
    } else {
      return calculateTree188(features, 20);
    }
  }
  if (node === 10) return 0.1324238346764445;
  if (node === 4) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree188(features, 9);
    } else if (val <= 934.0000000000001) {
      return calculateTree188(features, 9);
    } else {
      return calculateTree188(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree188(features, 3);
    } else if (val <= 693.0000000000001) {
      return calculateTree188(features, 3);
    } else {
      return calculateTree188(features, 4);
    }
  }
  if (node === 1535) return 0.17499069990902286;
  if (node === 1536) return -0.0411309073487586;
  if (node === 767) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree188(features, 1535);
    } else if (val <= 715.5000000000001) {
      return calculateTree188(features, 1535);
    } else {
      return calculateTree188(features, 1536);
    }
  }
  if (node === 768) return 0.21221655067041043;
  if (node === 383) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree188(features, 767);
    } else if (val <= 745.5000000000001) {
      return calculateTree188(features, 767);
    } else {
      return calculateTree188(features, 768);
    }
  }
  if (node === 769) return -0.3192354002688845;
  if (node === 1541) return 0.16805835489082802;
  if (node === 1542) return -0.12550981148023146;
  if (node === 770) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree188(features, 1541);
    } else if (val <= 740.5000000000001) {
      return calculateTree188(features, 1541);
    } else {
      return calculateTree188(features, 1542);
    }
  }
  if (node === 384) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree188(features, 769);
    } else if (val <= 733.5000000000001) {
      return calculateTree188(features, 769);
    } else {
      return calculateTree188(features, 770);
    }
  }
  if (node === 191) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree188(features, 383);
    } else if (val <= 584.5000000000001) {
      return calculateTree188(features, 383);
    } else {
      return calculateTree188(features, 384);
    }
  }
  if (node === 192) return 0.3248640463944477;
  if (node === 95) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree188(features, 191);
    } else if (val <= 776.0000000000001) {
      return calculateTree188(features, 191);
    } else {
      return calculateTree188(features, 192);
    }
  }
  if (node === 193) return -0.341754874825453;
  if (node === 194) return 0.26791007775820586;
  if (node === 96) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree188(features, 193);
    } else if (val <= 800.5000000000001) {
      return calculateTree188(features, 193);
    } else {
      return calculateTree188(features, 194);
    }
  }
  if (node === 47) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree188(features, 95);
    } else if (val <= 781.0000000000001) {
      return calculateTree188(features, 95);
    } else {
      return calculateTree188(features, 96);
    }
  }
  if (node === 1567) return -0.3552394277294936;
  if (node === 1568) return 0.0625663697074714;
  if (node === 783) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree188(features, 1567);
    } else if (val <= 685.0000000000001) {
      return calculateTree188(features, 1567);
    } else {
      return calculateTree188(features, 1568);
    }
  }
  if (node === 1569) return 0.024584834519850593;
  if (node === 1570) return -0.1469082643660114;
  if (node === 784) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree188(features, 1569);
    } else if (val <= 21.500000000000004) {
      return calculateTree188(features, 1569);
    } else {
      return calculateTree188(features, 1570);
    }
  }
  if (node === 391) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree188(features, 783);
    } else if (val <= 715.5000000000001) {
      return calculateTree188(features, 783);
    } else {
      return calculateTree188(features, 784);
    }
  }
  if (node === 392) return -0.4014348464504813;
  if (node === 195) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree188(features, 391);
    } else if (val <= 776.0000000000001) {
      return calculateTree188(features, 391);
    } else {
      return calculateTree188(features, 392);
    }
  }
  if (node === 196) return -0.2697619843898714;
  if (node === 97) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree188(features, 195);
    } else if (val <= 745.5000000000001) {
      return calculateTree188(features, 195);
    } else {
      return calculateTree188(features, 196);
    }
  }
  if (node === 197) return 0.042578117791298935;
  if (node === 198) return 0.22537663364767255;
  if (node === 98) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree188(features, 197);
    } else if (val <= 4.500000000000001) {
      return calculateTree188(features, 197);
    } else {
      return calculateTree188(features, 198);
    }
  }
  if (node === 48) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree188(features, 97);
    } else if (val <= 781.0000000000001) {
      return calculateTree188(features, 97);
    } else {
      return calculateTree188(features, 98);
    }
  }
  if (node === 23) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree188(features, 47);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree188(features, 47);
    } else {
      return calculateTree188(features, 48);
    }
  }
  if (node === 24) return -0.0306973941193708;
  if (node === 11) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree188(features, 23);
    } else if (val <= 839.5000000000001) {
      return calculateTree188(features, 23);
    } else {
      return calculateTree188(features, 24);
    }
  }
  if (node === 12) return 0.032970442912232674;
  if (node === 5) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree188(features, 11);
    } else if (val <= 24.500000000000004) {
      return calculateTree188(features, 11);
    } else {
      return calculateTree188(features, 12);
    }
  }
  if (node === 111) return 0.06710695158542877;
  if (node === 112) return -0.19234148712556912;
  if (node === 55) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree188(features, 111);
    } else if (val <= 23.500000000000004) {
      return calculateTree188(features, 111);
    } else {
      return calculateTree188(features, 112);
    }
  }
  if (node === 56) return 0.22507513931384784;
  if (node === 27) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree188(features, 55);
    } else if (val <= 3.5000000000000004) {
      return calculateTree188(features, 55);
    } else {
      return calculateTree188(features, 56);
    }
  }
  if (node === 28) return -0.3019091882835638;
  if (node === 13) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree188(features, 27);
    } else if (val <= 144.50000000000003) {
      return calculateTree188(features, 27);
    } else {
      return calculateTree188(features, 28);
    }
  }
  if (node === 14) return -3.3591442728334527e-6;
  if (node === 6) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree188(features, 13);
    } else if (val <= 757.5000000000001) {
      return calculateTree188(features, 13);
    } else {
      return calculateTree188(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree188(features, 5);
    } else if (val <= 752.5000000000001) {
      return calculateTree188(features, 5);
    } else {
      return calculateTree188(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree188(features, 1);
    } else if (val <= 709.5000000000001) {
      return calculateTree188(features, 1);
    } else {
      return calculateTree188(features, 2);
    }
  }
}
function calculateTree189(features, node = 0) {
  if (node === 1) return 0.0013567626773373558;
  if (node === 11) return -0.09767307996819384;
  if (node === 12) return -0.01386721227184072;
  if (node === 5) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree189(features, 11);
    } else if (val <= 41.50000000000001) {
      return calculateTree189(features, 11);
    } else {
      return calculateTree189(features, 12);
    }
  }
  if (node === 55) return -0.0008526615436364303;
  if (node === 113) return -0.01297623530205699;
  if (node === 1839) return -0.10132266105828922;
  if (node === 1840) return -0.031055611695498617;
  if (node === 919) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree189(features, 1839);
    } else if (val <= 799.5000000000001) {
      return calculateTree189(features, 1839);
    } else {
      return calculateTree189(features, 1840);
    }
  }
  if (node === 920) return 0.2428861923980109;
  if (node === 459) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree189(features, 919);
    } else if (val <= 22.500000000000004) {
      return calculateTree189(features, 919);
    } else {
      return calculateTree189(features, 920);
    }
  }
  if (node === 460) return -0.14188788565340438;
  if (node === 229) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree189(features, 459);
    } else if (val <= 868.0000000000001) {
      return calculateTree189(features, 459);
    } else {
      return calculateTree189(features, 460);
    }
  }
  if (node === 230) return -0.2529986447077779;
  if (node === 114) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree189(features, 229);
    } else if (val <= 23.500000000000004) {
      return calculateTree189(features, 229);
    } else {
      return calculateTree189(features, 230);
    }
  }
  if (node === 56) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree189(features, 113);
    } else if (val <= 4.500000000000001) {
      return calculateTree189(features, 113);
    } else {
      return calculateTree189(features, 114);
    }
  }
  if (node === 27) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree189(features, 55);
    } else if (val <= 745.5000000000001) {
      return calculateTree189(features, 55);
    } else {
      return calculateTree189(features, 56);
    }
  }
  if (node === 57) return 0.026405043983268532;
  if (node === 117) return -0.09655708001446815;
  if (node === 237) return -0.03596515595541378;
  if (node === 238) return 0.07199826785543981;
  if (node === 118) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree189(features, 237);
    } else if (val <= 5.500000000000001) {
      return calculateTree189(features, 237);
    } else {
      return calculateTree189(features, 238);
    }
  }
  if (node === 58) {
    const val = features['is_preference_women_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree189(features, 117);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree189(features, 117);
    } else {
      return calculateTree189(features, 118);
    }
  }
  if (node === 28) {
    const val = features['is_preference_home'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree189(features, 57);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree189(features, 57);
    } else {
      return calculateTree189(features, 58);
    }
  }
  if (node === 13) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree189(features, 27);
    } else if (val <= 899.5000000000001) {
      return calculateTree189(features, 27);
    } else {
      return calculateTree189(features, 28);
    }
  }
  if (node === 479) return -0.2268897720397273;
  if (node === 1923) return 0.09348015839908168;
  if (node === 1924) return -0.2014047992334721;
  if (node === 961) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree189(features, 1923);
    } else if (val <= 560.5000000000001) {
      return calculateTree189(features, 1923);
    } else {
      return calculateTree189(features, 1924);
    }
  }
  if (node === 962) return -0.19166162371453832;
  if (node === 480) {
    const val = features['is_preference_home'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree189(features, 961);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree189(features, 961);
    } else {
      return calculateTree189(features, 962);
    }
  }
  if (node === 239) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree189(features, 479);
    } else if (val <= 43.50000000000001) {
      return calculateTree189(features, 479);
    } else {
      return calculateTree189(features, 480);
    }
  }
  if (node === 240) return 0.1329133059926698;
  if (node === 119) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree189(features, 239);
    } else if (val <= 693.0000000000001) {
      return calculateTree189(features, 239);
    } else {
      return calculateTree189(features, 240);
    }
  }
  if (node === 241) return -0.0789294748718577;
  if (node === 242) return -0.3763567434408371;
  if (node === 120) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree189(features, 241);
    } else if (val <= 938.5000000000001) {
      return calculateTree189(features, 241);
    } else {
      return calculateTree189(features, 242);
    }
  }
  if (node === 59) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree189(features, 119);
    } else if (val <= 49.50000000000001) {
      return calculateTree189(features, 119);
    } else {
      return calculateTree189(features, 120);
    }
  }
  if (node === 121) return 0.11723632248232269;
  if (node === 122) return -0.01498651582785766;
  if (node === 60) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree189(features, 121);
    } else if (val <= 305.50000000000006) {
      return calculateTree189(features, 121);
    } else {
      return calculateTree189(features, 122);
    }
  }
  if (node === 29) {
    const val = features['is_preference_beauty'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree189(features, 59);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree189(features, 59);
    } else {
      return calculateTree189(features, 60);
    }
  }
  if (node === 247) return 0.4746026679128661;
  if (node === 248) return 0.1056740457120691;
  if (node === 123) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree189(features, 247);
    } else if (val <= 635.0000000000001) {
      return calculateTree189(features, 247);
    } else {
      return calculateTree189(features, 248);
    }
  }
  if (node === 249) return -0.2794964930730129;
  if (node === 250) return 0.1012295448195671;
  if (node === 124) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree189(features, 249);
    } else if (val <= 188.00000000000003) {
      return calculateTree189(features, 249);
    } else {
      return calculateTree189(features, 250);
    }
  }
  if (node === 61) {
    const val = features['is_preference_home'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree189(features, 123);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree189(features, 123);
    } else {
      return calculateTree189(features, 124);
    }
  }
  if (node === 251) return -0.2745273440536501;
  if (node === 252) return 0.007765738374496207;
  if (node === 125) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree189(features, 251);
    } else if (val <= 43.50000000000001) {
      return calculateTree189(features, 251);
    } else {
      return calculateTree189(features, 252);
    }
  }
  if (node === 507) return 0.11761741204898012;
  if (node === 508) return 0.357204742117656;
  if (node === 253) {
    const val = features['is_preference_sports'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree189(features, 507);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree189(features, 507);
    } else {
      return calculateTree189(features, 508);
    }
  }
  if (node === 254) return 0.026472219753209483;
  if (node === 126) {
    const val = features['is_preference_personal_care'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree189(features, 253);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree189(features, 253);
    } else {
      return calculateTree189(features, 254);
    }
  }
  if (node === 62) {
    const val = features['is_preference_household'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree189(features, 125);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree189(features, 125);
    } else {
      return calculateTree189(features, 126);
    }
  }
  if (node === 30) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree189(features, 61);
    } else if (val <= 42.50000000000001) {
      return calculateTree189(features, 61);
    } else {
      return calculateTree189(features, 62);
    }
  }
  if (node === 14) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree189(features, 29);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree189(features, 29);
    } else {
      return calculateTree189(features, 30);
    }
  }
  if (node === 6) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree189(features, 13);
    } else if (val <= 24.500000000000004) {
      return calculateTree189(features, 13);
    } else {
      return calculateTree189(features, 14);
    }
  }
  if (node === 2) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree189(features, 5);
    } else if (val <= 41.50000000000001) {
      return calculateTree189(features, 5);
    } else {
      return calculateTree189(features, 6);
    }
  }
  if (node === 0) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree189(features, 1);
    } else if (val <= 38.50000000000001) {
      return calculateTree189(features, 1);
    } else {
      return calculateTree189(features, 2);
    }
  }
}
function calculateTree190(features, node = 0) {
  if (node === 7) return 0.004095173357698155;
  if (node === 71) return -0.14203466526763447;
  if (node === 72) return -0.0238234330251649;
  if (node === 35) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree190(features, 71);
    } else if (val <= 531.0000000000001) {
      return calculateTree190(features, 71);
    } else {
      return calculateTree190(features, 72);
    }
  }
  if (node === 73) return 0.06317928521606622;
  if (node === 74) return -0.08767068426301006;
  if (node === 36) {
    const val = features['is_preference_kids'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree190(features, 73);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree190(features, 73);
    } else {
      return calculateTree190(features, 74);
    }
  }
  if (node === 17) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree190(features, 35);
    } else if (val <= 437.50000000000006) {
      return calculateTree190(features, 35);
    } else {
      return calculateTree190(features, 36);
    }
  }
  if (node === 18) return -0.0012965184767937648;
  if (node === 8) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree190(features, 17);
    } else if (val <= 36.50000000000001) {
      return calculateTree190(features, 17);
    } else {
      return calculateTree190(features, 18);
    }
  }
  if (node === 3) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree190(features, 7);
    } else if (val <= 34.50000000000001) {
      return calculateTree190(features, 7);
    } else {
      return calculateTree190(features, 8);
    }
  }
  if (node === 19) return 0.3098146094246419;
  if (node === 20) return 0.11717979397316139;
  if (node === 9) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree190(features, 19);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree190(features, 19);
    } else {
      return calculateTree190(features, 20);
    }
  }
  if (node === 10) return -0.3060501592469181;
  if (node === 4) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree190(features, 9);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree190(features, 9);
    } else {
      return calculateTree190(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree190(features, 3);
    } else if (val <= 296.50000000000006) {
      return calculateTree190(features, 3);
    } else {
      return calculateTree190(features, 4);
    }
  }
  if (node === 191) return 0.03798605946365862;
  if (node === 385) return -0.4735050397957108;
  if (node === 386) return 0.10669265845282074;
  if (node === 192) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree190(features, 385);
    } else if (val <= 39.50000000000001) {
      return calculateTree190(features, 385);
    } else {
      return calculateTree190(features, 386);
    }
  }
  if (node === 95) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree190(features, 191);
    } else if (val <= 25.500000000000004) {
      return calculateTree190(features, 191);
    } else {
      return calculateTree190(features, 192);
    }
  }
  if (node === 96) return 0.27399293578257666;
  if (node === 47) {
    const val = features['is_preference_household'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree190(features, 95);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree190(features, 95);
    } else {
      return calculateTree190(features, 96);
    }
  }
  if (node === 48) return -0.37708216528999355;
  if (node === 23) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree190(features, 47);
    } else if (val <= 750.0000000000001) {
      return calculateTree190(features, 47);
    } else {
      return calculateTree190(features, 48);
    }
  }
  if (node === 49) return -0.34567244847174106;
  if (node === 407) return 0.07889087795061962;
  if (node === 1635) return 0.019747477271684015;
  if (node === 1636) return -0.3037833603351896;
  if (node === 817) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree190(features, 1635);
    } else if (val <= 5.500000000000001) {
      return calculateTree190(features, 1635);
    } else {
      return calculateTree190(features, 1636);
    }
  }
  if (node === 818) return 0.1369748301316076;
  if (node === 408) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree190(features, 817);
    } else if (val <= 13.500000000000002) {
      return calculateTree190(features, 817);
    } else {
      return calculateTree190(features, 818);
    }
  }
  if (node === 203) {
    const val = features['is_preference_personal_care'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree190(features, 407);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree190(features, 407);
    } else {
      return calculateTree190(features, 408);
    }
  }
  if (node === 409) return 0.25157604094935404;
  if (node === 410) return 0.006445707400845304;
  if (node === 204) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree190(features, 409);
    } else if (val <= 14.500000000000002) {
      return calculateTree190(features, 409);
    } else {
      return calculateTree190(features, 410);
    }
  }
  if (node === 101) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree190(features, 203);
    } else if (val <= 804.0000000000001) {
      return calculateTree190(features, 203);
    } else {
      return calculateTree190(features, 204);
    }
  }
  if (node === 411) return -0.01545435724904486;
  if (node === 412) return -0.17022361635431046;
  if (node === 205) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree190(features, 411);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree190(features, 411);
    } else {
      return calculateTree190(features, 412);
    }
  }
  if (node === 413) return 0.2145731244480098;
  if (node === 414) return -0.015580265481001124;
  if (node === 206) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree190(features, 413);
    } else if (val <= 591.0000000000001) {
      return calculateTree190(features, 413);
    } else {
      return calculateTree190(features, 414);
    }
  }
  if (node === 102) {
    const val = features['is_preference_women_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree190(features, 205);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree190(features, 205);
    } else {
      return calculateTree190(features, 206);
    }
  }
  if (node === 50) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree190(features, 101);
    } else if (val <= 941.5000000000001) {
      return calculateTree190(features, 101);
    } else {
      return calculateTree190(features, 102);
    }
  }
  if (node === 24) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree190(features, 49);
    } else if (val <= 715.5000000000001) {
      return calculateTree190(features, 49);
    } else {
      return calculateTree190(features, 50);
    }
  }
  if (node === 11) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree190(features, 23);
    } else if (val <= 305.50000000000006) {
      return calculateTree190(features, 23);
    } else {
      return calculateTree190(features, 24);
    }
  }
  if (node === 25) return -0.3376539334122675;
  if (node === 107) return -0.005169253417948429;
  if (node === 435) return 1.0827546648565176;
  if (node === 436) return 0.56733461788344;
  if (node === 217) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree190(features, 435);
    } else if (val <= 660.0000000000001) {
      return calculateTree190(features, 435);
    } else {
      return calculateTree190(features, 436);
    }
  }
  if (node === 218) return 0.008449642795470995;
  if (node === 108) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree190(features, 217);
    } else if (val <= 34.50000000000001) {
      return calculateTree190(features, 217);
    } else {
      return calculateTree190(features, 218);
    }
  }
  if (node === 53) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree190(features, 107);
    } else if (val <= 1.5000000000000002) {
      return calculateTree190(features, 107);
    } else {
      return calculateTree190(features, 108);
    }
  }
  if (node === 54) return -0.30957261055805785;
  if (node === 26) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree190(features, 53);
    } else if (val <= 948.0000000000001) {
      return calculateTree190(features, 53);
    } else {
      return calculateTree190(features, 54);
    }
  }
  if (node === 12) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree190(features, 25);
    } else if (val <= 750.0000000000001) {
      return calculateTree190(features, 25);
    } else {
      return calculateTree190(features, 26);
    }
  }
  if (node === 5) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree190(features, 11);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree190(features, 11);
    } else {
      return calculateTree190(features, 12);
    }
  }
  if (node === 6) return -0.0004965004129603353;
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree190(features, 5);
    } else if (val <= 311.50000000000006) {
      return calculateTree190(features, 5);
    } else {
      return calculateTree190(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree190(features, 1);
    } else if (val <= 300.50000000000006) {
      return calculateTree190(features, 1);
    } else {
      return calculateTree190(features, 2);
    }
  }
}
function calculateTree191(features, node = 0) {
  if (node === 3) return 0.000781109860358562;
  if (node === 4) return 0.22921714606035795;
  if (node === 1) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree191(features, 3);
    } else if (val <= 73.00000000000001) {
      return calculateTree191(features, 3);
    } else {
      return calculateTree191(features, 4);
    }
  }
  if (node === 11) return -0.011369172340065173;
  if (node === 25) return 0.18216498905725365;
  if (node === 107) return 0.11266796391905412;
  if (node === 108) return -0.2772957582441893;
  if (node === 53) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree191(features, 107);
    } else if (val <= 24.500000000000004) {
      return calculateTree191(features, 107);
    } else {
      return calculateTree191(features, 108);
    }
  }
  if (node === 109) return -0.11885278660428476;
  if (node === 110) return 0.14312293959408434;
  if (node === 54) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree191(features, 109);
    } else if (val <= 18.500000000000004) {
      return calculateTree191(features, 109);
    } else {
      return calculateTree191(features, 110);
    }
  }
  if (node === 26) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree191(features, 53);
    } else if (val <= 3.0000000000000004) {
      return calculateTree191(features, 53);
    } else {
      return calculateTree191(features, 54);
    }
  }
  if (node === 12) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree191(features, 25);
    } else if (val <= 576.0000000000001) {
      return calculateTree191(features, 25);
    } else {
      return calculateTree191(features, 26);
    }
  }
  if (node === 5) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree191(features, 11);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree191(features, 11);
    } else {
      return calculateTree191(features, 12);
    }
  }
  if (node === 111) return -0.313765455390873;
  if (node === 112) return 0.010915728287535688;
  if (node === 55) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree191(features, 111);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree191(features, 111);
    } else {
      return calculateTree191(features, 112);
    }
  }
  if (node === 455) return 0.06019741550020287;
  if (node === 913) return -0.18999654685114034;
  if (node === 914) return -0.47059818499200373;
  if (node === 456) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree191(features, 913);
    } else if (val <= 99.50000000000001) {
      return calculateTree191(features, 913);
    } else {
      return calculateTree191(features, 914);
    }
  }
  if (node === 227) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree191(features, 455);
    } else if (val <= 271.00000000000006) {
      return calculateTree191(features, 455);
    } else {
      return calculateTree191(features, 456);
    }
  }
  if (node === 457) return -0.08962554877704408;
  if (node === 458) return 0.11769398150297823;
  if (node === 228) {
    const val = features['is_preference_kitchen'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree191(features, 457);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree191(features, 457);
    } else {
      return calculateTree191(features, 458);
    }
  }
  if (node === 113) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree191(features, 227);
    } else if (val <= 660.0000000000001) {
      return calculateTree191(features, 227);
    } else {
      return calculateTree191(features, 228);
    }
  }
  if (node === 114) return -0.012113766396394634;
  if (node === 56) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree191(features, 113);
    } else if (val <= 13.500000000000002) {
      return calculateTree191(features, 113);
    } else {
      return calculateTree191(features, 114);
    }
  }
  if (node === 27) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree191(features, 55);
    } else if (val <= 155.50000000000003) {
      return calculateTree191(features, 55);
    } else {
      return calculateTree191(features, 56);
    }
  }
  if (node === 231) return 0.045811028074880696;
  if (node === 931) return -0.04060220982508717;
  if (node === 932) return -0.339528286695483;
  if (node === 465) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree191(features, 931);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree191(features, 931);
    } else {
      return calculateTree191(features, 932);
    }
  }
  if (node === 466) return -0.005275513588385751;
  if (node === 232) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree191(features, 465);
    } else if (val <= 141.50000000000003) {
      return calculateTree191(features, 465);
    } else {
      return calculateTree191(features, 466);
    }
  }
  if (node === 115) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree191(features, 231);
    } else if (val <= 132.50000000000003) {
      return calculateTree191(features, 231);
    } else {
      return calculateTree191(features, 232);
    }
  }
  if (node === 467) return -0.1093888063355167;
  if (node === 937) return 0.0778790418499245;
  if (node === 1877) return -0.18050829689464473;
  if (node === 1878) return 0.045487192765956364;
  if (node === 938) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree191(features, 1877);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree191(features, 1877);
    } else {
      return calculateTree191(features, 1878);
    }
  }
  if (node === 468) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree191(features, 937);
    } else if (val <= 916.0000000000001) {
      return calculateTree191(features, 937);
    } else {
      return calculateTree191(features, 938);
    }
  }
  if (node === 233) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree191(features, 467);
    } else if (val <= 162.50000000000003) {
      return calculateTree191(features, 467);
    } else {
      return calculateTree191(features, 468);
    }
  }
  if (node === 234) return 0.009937666379229023;
  if (node === 116) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree191(features, 233);
    } else if (val <= 441.00000000000006) {
      return calculateTree191(features, 233);
    } else {
      return calculateTree191(features, 234);
    }
  }
  if (node === 57) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree191(features, 115);
    } else if (val <= 414.50000000000006) {
      return calculateTree191(features, 115);
    } else {
      return calculateTree191(features, 116);
    }
  }
  if (node === 943) return 0.0822603607282301;
  if (node === 944) return -0.3217029191348562;
  if (node === 471) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree191(features, 943);
    } else if (val <= 31.500000000000004) {
      return calculateTree191(features, 943);
    } else {
      return calculateTree191(features, 944);
    }
  }
  if (node === 472) return 0.13041946954142028;
  if (node === 235) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree191(features, 471);
    } else if (val <= 660.0000000000001) {
      return calculateTree191(features, 471);
    } else {
      return calculateTree191(features, 472);
    }
  }
  if (node === 236) return -0.2722194320932942;
  if (node === 117) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree191(features, 235);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree191(features, 235);
    } else {
      return calculateTree191(features, 236);
    }
  }
  if (node === 237) return -0.02186463756325933;
  if (node === 238) return -0.001335212192898218;
  if (node === 118) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree191(features, 237);
    } else if (val <= 718.5000000000001) {
      return calculateTree191(features, 237);
    } else {
      return calculateTree191(features, 238);
    }
  }
  if (node === 58) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree191(features, 117);
    } else if (val <= 507.00000000000006) {
      return calculateTree191(features, 117);
    } else {
      return calculateTree191(features, 118);
    }
  }
  if (node === 28) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree191(features, 57);
    } else if (val <= 504.50000000000006) {
      return calculateTree191(features, 57);
    } else {
      return calculateTree191(features, 58);
    }
  }
  if (node === 13) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree191(features, 27);
    } else if (val <= 127.50000000000001) {
      return calculateTree191(features, 27);
    } else {
      return calculateTree191(features, 28);
    }
  }
  if (node === 14) return 0.006927188193384035;
  if (node === 6) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree191(features, 13);
    } else if (val <= 47.50000000000001) {
      return calculateTree191(features, 13);
    } else {
      return calculateTree191(features, 14);
    }
  }
  if (node === 2) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree191(features, 5);
    } else if (val <= 6.000000000000001) {
      return calculateTree191(features, 5);
    } else {
      return calculateTree191(features, 6);
    }
  }
  if (node === 0) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree191(features, 1);
    } else if (val <= 90.50000000000001) {
      return calculateTree191(features, 1);
    } else {
      return calculateTree191(features, 2);
    }
  }
}
function calculateTree192(features, node = 0) {
  if (node === 3) return 0.00040069563535016675;
  if (node === 9) return -0.1951567029338361;
  if (node === 43) return -0.3383025022976921;
  if (node === 179) return 0.19385001178289896;
  if (node === 180) return 0.07106103547477906;
  if (node === 89) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree192(features, 179);
    } else if (val <= 628.5000000000001) {
      return calculateTree192(features, 179);
    } else {
      return calculateTree192(features, 180);
    }
  }
  if (node === 363) return 0.003353860859876669;
  if (node === 364) return -0.31423207770611783;
  if (node === 181) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree192(features, 363);
    } else if (val <= 308.00000000000006) {
      return calculateTree192(features, 363);
    } else {
      return calculateTree192(features, 364);
    }
  }
  if (node === 365) return 0.17107092788138664;
  if (node === 733) return -0.3220241513295489;
  if (node === 1469) return 0.11035190532936032;
  if (node === 1470) return 0.01071051906135599;
  if (node === 734) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree192(features, 1469);
    } else if (val <= 789.5000000000001) {
      return calculateTree192(features, 1469);
    } else {
      return calculateTree192(features, 1470);
    }
  }
  if (node === 366) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree192(features, 733);
    } else if (val <= 782.0000000000001) {
      return calculateTree192(features, 733);
    } else {
      return calculateTree192(features, 734);
    }
  }
  if (node === 182) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree192(features, 365);
    } else if (val <= 693.0000000000001) {
      return calculateTree192(features, 365);
    } else {
      return calculateTree192(features, 366);
    }
  }
  if (node === 90) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree192(features, 181);
    } else if (val <= 539.5000000000001) {
      return calculateTree192(features, 181);
    } else {
      return calculateTree192(features, 182);
    }
  }
  if (node === 44) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree192(features, 89);
    } else if (val <= 584.5000000000001) {
      return calculateTree192(features, 89);
    } else {
      return calculateTree192(features, 90);
    }
  }
  if (node === 21) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree192(features, 43);
    } else if (val <= 230.50000000000003) {
      return calculateTree192(features, 43);
    } else {
      return calculateTree192(features, 44);
    }
  }
  if (node === 91) return 0.32620705886317247;
  if (node === 185) return -0.34442196617530213;
  if (node === 186) return 0.04462753256380803;
  if (node === 92) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree192(features, 185);
    } else if (val <= 575.5000000000001) {
      return calculateTree192(features, 185);
    } else {
      return calculateTree192(features, 186);
    }
  }
  if (node === 45) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree192(features, 91);
    } else if (val <= 571.5000000000001) {
      return calculateTree192(features, 91);
    } else {
      return calculateTree192(features, 92);
    }
  }
  if (node === 46) return -0.32425828522289707;
  if (node === 22) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree192(features, 45);
    } else if (val <= 762.5000000000001) {
      return calculateTree192(features, 45);
    } else {
      return calculateTree192(features, 46);
    }
  }
  if (node === 10) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree192(features, 21);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree192(features, 21);
    } else {
      return calculateTree192(features, 22);
    }
  }
  if (node === 4) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree192(features, 9);
    } else if (val <= 131.50000000000003) {
      return calculateTree192(features, 9);
    } else {
      return calculateTree192(features, 10);
    }
  }
  if (node === 1) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree192(features, 3);
    } else if (val <= 563.5000000000001) {
      return calculateTree192(features, 3);
    } else {
      return calculateTree192(features, 4);
    }
  }
  if (node === 191) return 0.369163878561342;
  if (node === 385) return -0.06479472525418668;
  if (node === 386) return -0.32494509123609155;
  if (node === 192) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree192(features, 385);
    } else if (val <= 127.00000000000001) {
      return calculateTree192(features, 385);
    } else {
      return calculateTree192(features, 386);
    }
  }
  if (node === 95) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree192(features, 191);
    } else if (val <= 643.5000000000001) {
      return calculateTree192(features, 191);
    } else {
      return calculateTree192(features, 192);
    }
  }
  if (node === 775) return -0.056284648369743145;
  if (node === 776) return 0.3236978609616224;
  if (node === 387) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree192(features, 775);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree192(features, 775);
    } else {
      return calculateTree192(features, 776);
    }
  }
  if (node === 777) return -0.00026073676858605994;
  if (node === 778) return -0.1235257083972191;
  if (node === 388) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree192(features, 777);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree192(features, 777);
    } else {
      return calculateTree192(features, 778);
    }
  }
  if (node === 193) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree192(features, 387);
    } else if (val <= 648.5000000000001) {
      return calculateTree192(features, 387);
    } else {
      return calculateTree192(features, 388);
    }
  }
  if (node === 194) return -0.34268825700013544;
  if (node === 96) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree192(features, 193);
    } else if (val <= 455.50000000000006) {
      return calculateTree192(features, 193);
    } else {
      return calculateTree192(features, 194);
    }
  }
  if (node === 47) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree192(features, 95);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree192(features, 95);
    } else {
      return calculateTree192(features, 96);
    }
  }
  if (node === 48) return -0.32606603175186727;
  if (node === 23) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree192(features, 47);
    } else if (val <= 517.5000000000001) {
      return calculateTree192(features, 47);
    } else {
      return calculateTree192(features, 48);
    }
  }
  if (node === 49) return 0.13094906084676786;
  if (node === 203) return -0.23885781237349804;
  if (node === 204) return 0.11164702310225262;
  if (node === 101) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree192(features, 203);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree192(features, 203);
    } else {
      return calculateTree192(features, 204);
    }
  }
  if (node === 102) return -0.2274172972207156;
  if (node === 50) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree192(features, 101);
    } else if (val <= 643.5000000000001) {
      return calculateTree192(features, 101);
    } else {
      return calculateTree192(features, 102);
    }
  }
  if (node === 24) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree192(features, 49);
    } else if (val <= 763.5000000000001) {
      return calculateTree192(features, 49);
    } else {
      return calculateTree192(features, 50);
    }
  }
  if (node === 11) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree192(features, 23);
    } else if (val <= 733.5000000000001) {
      return calculateTree192(features, 23);
    } else {
      return calculateTree192(features, 24);
    }
  }
  if (node === 51) return -0.23779264624234753;
  if (node === 52) return 0.09789203387964321;
  if (node === 25) {
    const val = features['is_preference_grocery'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree192(features, 51);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree192(features, 51);
    } else {
      return calculateTree192(features, 52);
    }
  }
  if (node === 26) return 0.17354937716456628;
  if (node === 12) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree192(features, 25);
    } else if (val <= 648.5000000000001) {
      return calculateTree192(features, 25);
    } else {
      return calculateTree192(features, 26);
    }
  }
  if (node === 5) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree192(features, 11);
    } else if (val <= 15.500000000000002) {
      return calculateTree192(features, 11);
    } else {
      return calculateTree192(features, 12);
    }
  }
  if (node === 6) return -0.0009455833260359115;
  if (node === 2) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree192(features, 5);
    } else if (val <= 656.5000000000001) {
      return calculateTree192(features, 5);
    } else {
      return calculateTree192(features, 6);
    }
  }
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree192(features, 1);
    } else if (val <= 619.5000000000001) {
      return calculateTree192(features, 1);
    } else {
      return calculateTree192(features, 2);
    }
  }
}
function calculateTree193(features, node = 0) {
  if (node === 3) return -0.0024937187092480078;
  if (node === 4) return 0.0006886988892990209;
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree193(features, 3);
    } else if (val <= 455.50000000000006) {
      return calculateTree193(features, 3);
    } else {
      return calculateTree193(features, 4);
    }
  }
  if (node === 5) return 0.10757948589761619;
  if (node === 27) return -0.2700698984037596;
  if (node === 115) return 0.4948692061146801;
  if (node === 116) return 0.09375615030433926;
  if (node === 57) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree193(features, 115);
    } else if (val <= 1.5000000000000002) {
      return calculateTree193(features, 115);
    } else {
      return calculateTree193(features, 116);
    }
  }
  if (node === 58) return -0.3778364844364858;
  if (node === 28) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree193(features, 57);
    } else if (val <= 693.0000000000001) {
      return calculateTree193(features, 57);
    } else {
      return calculateTree193(features, 58);
    }
  }
  if (node === 13) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree193(features, 27);
    } else if (val <= 497.50000000000006) {
      return calculateTree193(features, 27);
    } else {
      return calculateTree193(features, 28);
    }
  }
  if (node === 239) return -0.12253899561559106;
  if (node === 1927) return 0.004164387713304696;
  if (node === 1928) return 0.14126276519021744;
  if (node === 963) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree193(features, 1927);
    } else if (val <= 733.5000000000001) {
      return calculateTree193(features, 1927);
    } else {
      return calculateTree193(features, 1928);
    }
  }
  if (node === 964) return -0.11492702635951953;
  if (node === 481) {
    const val = features['is_preference_sports'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree193(features, 963);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree193(features, 963);
    } else {
      return calculateTree193(features, 964);
    }
  }
  if (node === 482) return 0.13356742278666325;
  if (node === 240) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree193(features, 481);
    } else if (val <= 18.500000000000004) {
      return calculateTree193(features, 481);
    } else {
      return calculateTree193(features, 482);
    }
  }
  if (node === 119) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree193(features, 239);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree193(features, 239);
    } else {
      return calculateTree193(features, 240);
    }
  }
  if (node === 120) return 0.11602740483631396;
  if (node === 59) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree193(features, 119);
    } else if (val <= 3.5000000000000004) {
      return calculateTree193(features, 119);
    } else {
      return calculateTree193(features, 120);
    }
  }
  if (node === 121) return -0.24486919580249655;
  if (node === 122) return -0.05077006790836947;
  if (node === 60) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree193(features, 121);
    } else if (val <= 8.500000000000002) {
      return calculateTree193(features, 121);
    } else {
      return calculateTree193(features, 122);
    }
  }
  if (node === 29) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree193(features, 59);
    } else if (val <= 5.500000000000001) {
      return calculateTree193(features, 59);
    } else {
      return calculateTree193(features, 60);
    }
  }
  if (node === 495) return 0.08217246876272473;
  if (node === 993) return -0.23871412362139066;
  if (node === 994) return 0.027219245855654956;
  if (node === 496) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree193(features, 993);
    } else if (val <= 37.50000000000001) {
      return calculateTree193(features, 993);
    } else {
      return calculateTree193(features, 994);
    }
  }
  if (node === 247) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree193(features, 495);
    } else if (val <= 28.500000000000004) {
      return calculateTree193(features, 495);
    } else {
      return calculateTree193(features, 496);
    }
  }
  if (node === 248) return -0.24550457359280348;
  if (node === 123) {
    const val = features['is_preference_personal_care'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree193(features, 247);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree193(features, 247);
    } else {
      return calculateTree193(features, 248);
    }
  }
  if (node === 124) return 0.15019480491950435;
  if (node === 61) {
    const val = features['is_preference_men_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree193(features, 123);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree193(features, 123);
    } else {
      return calculateTree193(features, 124);
    }
  }
  if (node === 503) return 0.20279864201435088;
  if (node === 2019) return 0.018652797546767935;
  if (node === 2020) return 0.1826159908705411;
  if (node === 1009) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree193(features, 2019);
    } else if (val <= 5.500000000000001) {
      return calculateTree193(features, 2019);
    } else {
      return calculateTree193(features, 2020);
    }
  }
  if (node === 2021) return -0.189509775704822;
  if (node === 2022) return 0.06509401474133988;
  if (node === 1010) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree193(features, 2021);
    } else if (val <= 22.500000000000004) {
      return calculateTree193(features, 2021);
    } else {
      return calculateTree193(features, 2022);
    }
  }
  if (node === 504) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree193(features, 1009);
    } else if (val <= 15.500000000000002) {
      return calculateTree193(features, 1009);
    } else {
      return calculateTree193(features, 1010);
    }
  }
  if (node === 251) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree193(features, 503);
    } else if (val <= 24.500000000000004) {
      return calculateTree193(features, 503);
    } else {
      return calculateTree193(features, 504);
    }
  }
  if (node === 252) return 0.35166013390602796;
  if (node === 125) {
    const val = features['is_preference_sports'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree193(features, 251);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree193(features, 251);
    } else {
      return calculateTree193(features, 252);
    }
  }
  if (node === 1015) return 0.14732911547413183;
  if (node === 2033) return -0.04288728739286106;
  if (node === 2034) return -0.24672759576765965;
  if (node === 1016) {
    const val = features['is_preference_kitchen'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree193(features, 2033);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree193(features, 2033);
    } else {
      return calculateTree193(features, 2034);
    }
  }
  if (node === 507) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree193(features, 1015);
    } else if (val <= 30.500000000000004) {
      return calculateTree193(features, 1015);
    } else {
      return calculateTree193(features, 1016);
    }
  }
  if (node === 1017) return 0.17095441319323823;
  if (node === 1018) return -0.019571823560080134;
  if (node === 508) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree193(features, 1017);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree193(features, 1017);
    } else {
      return calculateTree193(features, 1018);
    }
  }
  if (node === 253) {
    const val = features['is_preference_grocery'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree193(features, 507);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree193(features, 507);
    } else {
      return calculateTree193(features, 508);
    }
  }
  if (node === 254) return -0.19342499604425054;
  if (node === 126) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree193(features, 253);
    } else if (val <= 28.500000000000004) {
      return calculateTree193(features, 253);
    } else {
      return calculateTree193(features, 254);
    }
  }
  if (node === 62) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree193(features, 125);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree193(features, 125);
    } else {
      return calculateTree193(features, 126);
    }
  }
  if (node === 30) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree193(features, 61);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree193(features, 61);
    } else {
      return calculateTree193(features, 62);
    }
  }
  if (node === 14) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree193(features, 29);
    } else if (val <= 21.500000000000004) {
      return calculateTree193(features, 29);
    } else {
      return calculateTree193(features, 30);
    }
  }
  if (node === 6) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree193(features, 13);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree193(features, 13);
    } else {
      return calculateTree193(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree193(features, 5);
    } else if (val <= 253.50000000000003) {
      return calculateTree193(features, 5);
    } else {
      return calculateTree193(features, 6);
    }
  }
  if (node === 0) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree193(features, 1);
    } else if (val <= 973.5000000000001) {
      return calculateTree193(features, 1);
    } else {
      return calculateTree193(features, 2);
    }
  }
}
function calculateTree194(features, node = 0) {
  if (node === 7) return 0.00042250761322122605;
  if (node === 35) return 0.15402913888111458;
  if (node === 36) return -0.22384069669514073;
  if (node === 17) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree194(features, 35);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree194(features, 35);
    } else {
      return calculateTree194(features, 36);
    }
  }
  if (node === 18) return -0.3220615683391359;
  if (node === 8) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree194(features, 17);
    } else if (val <= 556.0000000000001) {
      return calculateTree194(features, 17);
    } else {
      return calculateTree194(features, 18);
    }
  }
  if (node === 3) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree194(features, 7);
    } else if (val <= 278.50000000000006) {
      return calculateTree194(features, 7);
    } else {
      return calculateTree194(features, 8);
    }
  }
  if (node === 4) return 0.0071866577365641135;
  if (node === 1) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree194(features, 3);
    } else if (val <= 665.0000000000001) {
      return calculateTree194(features, 3);
    } else {
      return calculateTree194(features, 4);
    }
  }
  if (node === 383) return 0.05024226396640274;
  if (node === 769) return -0.2616417997075747;
  if (node === 770) return -0.012459073870028183;
  if (node === 384) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree194(features, 769);
    } else if (val <= 447.00000000000006) {
      return calculateTree194(features, 769);
    } else {
      return calculateTree194(features, 770);
    }
  }
  if (node === 191) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree194(features, 383);
    } else if (val <= 171.00000000000003) {
      return calculateTree194(features, 383);
    } else {
      return calculateTree194(features, 384);
    }
  }
  if (node === 771) return 0.13991069653109942;
  if (node === 772) return -0.04917297167501008;
  if (node === 385) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree194(features, 771);
    } else if (val <= 46.50000000000001) {
      return calculateTree194(features, 771);
    } else {
      return calculateTree194(features, 772);
    }
  }
  if (node === 386) return 0.4179714053320304;
  if (node === 192) {
    const val = features['is_preference_beauty'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree194(features, 385);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree194(features, 385);
    } else {
      return calculateTree194(features, 386);
    }
  }
  if (node === 95) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree194(features, 191);
    } else if (val <= 40.50000000000001) {
      return calculateTree194(features, 191);
    } else {
      return calculateTree194(features, 192);
    }
  }
  if (node === 96) return -0.26037544002969937;
  if (node === 47) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree194(features, 95);
    } else if (val <= 51.50000000000001) {
      return calculateTree194(features, 95);
    } else {
      return calculateTree194(features, 96);
    }
  }
  if (node === 97) return -0.3660545609190527;
  if (node === 395) return -0.23422598385998303;
  if (node === 793) return -0.07174399827594659;
  if (node === 794) return 0.1889351351524274;
  if (node === 396) {
    const val = features['is_preference_men_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree194(features, 793);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree194(features, 793);
    } else {
      return calculateTree194(features, 794);
    }
  }
  if (node === 197) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree194(features, 395);
    } else if (val <= 17.500000000000004) {
      return calculateTree194(features, 395);
    } else {
      return calculateTree194(features, 396);
    }
  }
  if (node === 198) return -0.30012190025054514;
  if (node === 98) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree194(features, 197);
    } else if (val <= 531.0000000000001) {
      return calculateTree194(features, 197);
    } else {
      return calculateTree194(features, 198);
    }
  }
  if (node === 48) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree194(features, 97);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree194(features, 97);
    } else {
      return calculateTree194(features, 98);
    }
  }
  if (node === 23) {
    const val = features['is_preference_pets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree194(features, 47);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree194(features, 47);
    } else {
      return calculateTree194(features, 48);
    }
  }
  if (node === 24) return 0.16328118856541696;
  if (node === 11) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree194(features, 23);
    } else if (val <= 53.50000000000001) {
      return calculateTree194(features, 23);
    } else {
      return calculateTree194(features, 24);
    }
  }
  if (node === 103) return -0.001168195338461464;
  if (node === 209) return -0.30159497075305053;
  if (node === 210) return -0.0027014320533525005;
  if (node === 104) {
    const val = features['is_preference_pets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree194(features, 209);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree194(features, 209);
    } else {
      return calculateTree194(features, 210);
    }
  }
  if (node === 51) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree194(features, 103);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree194(features, 103);
    } else {
      return calculateTree194(features, 104);
    }
  }
  if (node === 52) return 0.07675147357413072;
  if (node === 25) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree194(features, 51);
    } else if (val <= 877.5000000000001) {
      return calculateTree194(features, 51);
    } else {
      return calculateTree194(features, 52);
    }
  }
  if (node === 26) return -0.31625746003204297;
  if (node === 12) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree194(features, 25);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree194(features, 25);
    } else {
      return calculateTree194(features, 26);
    }
  }
  if (node === 5) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree194(features, 11);
    } else if (val <= 288.50000000000006) {
      return calculateTree194(features, 11);
    } else {
      return calculateTree194(features, 12);
    }
  }
  if (node === 55) return -0.3490699357622829;
  if (node === 455) return 0.17639238402115706;
  if (node === 456) return 0.029590915092431697;
  if (node === 227) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree194(features, 455);
    } else if (val <= 13.500000000000002) {
      return calculateTree194(features, 455);
    } else {
      return calculateTree194(features, 456);
    }
  }
  if (node === 457) return 0.4026855177861664;
  if (node === 458) return 0.08457687550821166;
  if (node === 228) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree194(features, 457);
    } else if (val <= 804.0000000000001) {
      return calculateTree194(features, 457);
    } else {
      return calculateTree194(features, 458);
    }
  }
  if (node === 113) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree194(features, 227);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree194(features, 227);
    } else {
      return calculateTree194(features, 228);
    }
  }
  if (node === 114) return 0.03371745694549189;
  if (node === 56) {
    const val = features['is_preference_kitchen'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree194(features, 113);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree194(features, 113);
    } else {
      return calculateTree194(features, 114);
    }
  }
  if (node === 27) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree194(features, 55);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree194(features, 55);
    } else {
      return calculateTree194(features, 56);
    }
  }
  if (node === 57) return 0.06352070767630591;
  if (node === 58) return -0.33965255760993307;
  if (node === 28) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree194(features, 57);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree194(features, 57);
    } else {
      return calculateTree194(features, 58);
    }
  }
  if (node === 13) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree194(features, 27);
    } else if (val <= 778.5000000000001) {
      return calculateTree194(features, 27);
    } else {
      return calculateTree194(features, 28);
    }
  }
  if (node === 14) return -0.0016137994217253731;
  if (node === 6) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree194(features, 13);
    } else if (val <= 294.50000000000006) {
      return calculateTree194(features, 13);
    } else {
      return calculateTree194(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree194(features, 5);
    } else if (val <= 291.50000000000006) {
      return calculateTree194(features, 5);
    } else {
      return calculateTree194(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree194(features, 1);
    } else if (val <= 282.50000000000006) {
      return calculateTree194(features, 1);
    } else {
      return calculateTree194(features, 2);
    }
  }
}
function calculateTree195(features, node = 0) {
  if (node === 31) return 0.007521923341613045;
  if (node === 32) return 0.18138554221841383;
  if (node === 15) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree195(features, 31);
    } else if (val <= 58.50000000000001) {
      return calculateTree195(features, 31);
    } else {
      return calculateTree195(features, 32);
    }
  }
  if (node === 33) return -0.005190605214363201;
  if (node === 34) return -0.08595336319935602;
  if (node === 16) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree195(features, 33);
    } else if (val <= 57.50000000000001) {
      return calculateTree195(features, 33);
    } else {
      return calculateTree195(features, 34);
    }
  }
  if (node === 7) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree195(features, 15);
    } else if (val <= 1.5000000000000002) {
      return calculateTree195(features, 15);
    } else {
      return calculateTree195(features, 16);
    }
  }
  if (node === 143) return -0.3566090487385589;
  if (node === 289) return 0.238918978359523;
  if (node === 1163) return -0.10047625313350422;
  if (node === 1164) return 0.14270969879165016;
  if (node === 581) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree195(features, 1163);
    } else if (val <= 27.500000000000004) {
      return calculateTree195(features, 1163);
    } else {
      return calculateTree195(features, 1164);
    }
  }
  if (node === 1165) return -0.43345225295852563;
  if (node === 1166) return -0.10019862912387016;
  if (node === 582) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree195(features, 1165);
    } else if (val <= 10.000000000000002) {
      return calculateTree195(features, 1165);
    } else {
      return calculateTree195(features, 1166);
    }
  }
  if (node === 290) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree195(features, 581);
    } else if (val <= 1.5000000000000002) {
      return calculateTree195(features, 581);
    } else {
      return calculateTree195(features, 582);
    }
  }
  if (node === 144) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree195(features, 289);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree195(features, 289);
    } else {
      return calculateTree195(features, 290);
    }
  }
  if (node === 71) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree195(features, 143);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree195(features, 143);
    } else {
      return calculateTree195(features, 144);
    }
  }
  if (node === 72) return 0.0986891387989664;
  if (node === 35) {
    const val = features['is_preference_sports'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree195(features, 71);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree195(features, 71);
    } else {
      return calculateTree195(features, 72);
    }
  }
  if (node === 36) return -0.23982867007008685;
  if (node === 17) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree195(features, 35);
    } else if (val <= 18.500000000000004) {
      return calculateTree195(features, 35);
    } else {
      return calculateTree195(features, 36);
    }
  }
  if (node === 303) return 0.1157545708914898;
  if (node === 304) return -0.24571266395146807;
  if (node === 151) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree195(features, 303);
    } else if (val <= 850.5000000000001) {
      return calculateTree195(features, 303);
    } else {
      return calculateTree195(features, 304);
    }
  }
  if (node === 1223) return -0.024077616943464848;
  if (node === 1224) return -0.31625682482282574;
  if (node === 611) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree195(features, 1223);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree195(features, 1223);
    } else {
      return calculateTree195(features, 1224);
    }
  }
  if (node === 1225) return -0.07812700197101798;
  if (node === 1226) return 0.22669768271500387;
  if (node === 612) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree195(features, 1225);
    } else if (val <= 10.500000000000002) {
      return calculateTree195(features, 1225);
    } else {
      return calculateTree195(features, 1226);
    }
  }
  if (node === 305) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree195(features, 611);
    } else if (val <= 28.500000000000004) {
      return calculateTree195(features, 611);
    } else {
      return calculateTree195(features, 612);
    }
  }
  if (node === 306) return -0.29839952015428184;
  if (node === 152) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree195(features, 305);
    } else if (val <= 75.50000000000001) {
      return calculateTree195(features, 305);
    } else {
      return calculateTree195(features, 306);
    }
  }
  if (node === 75) {
    const val = features['is_preference_household'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree195(features, 151);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree195(features, 151);
    } else {
      return calculateTree195(features, 152);
    }
  }
  if (node === 76) return 0.31473025936769755;
  if (node === 37) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree195(features, 75);
    } else if (val <= 935.5000000000001) {
      return calculateTree195(features, 75);
    } else {
      return calculateTree195(features, 76);
    }
  }
  if (node === 311) return 0.15318626570599728;
  if (node === 312) return -0.12495833387532199;
  if (node === 155) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree195(features, 311);
    } else if (val <= 6.500000000000001) {
      return calculateTree195(features, 311);
    } else {
      return calculateTree195(features, 312);
    }
  }
  if (node === 313) return -0.10815783309891325;
  if (node === 314) return -0.3796504171054083;
  if (node === 156) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree195(features, 313);
    } else if (val <= 916.0000000000001) {
      return calculateTree195(features, 313);
    } else {
      return calculateTree195(features, 314);
    }
  }
  if (node === 77) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree195(features, 155);
    } else if (val <= 338.50000000000006) {
      return calculateTree195(features, 155);
    } else {
      return calculateTree195(features, 156);
    }
  }
  if (node === 157) return -0.20505556409886394;
  if (node === 635) return 0.22539578443865876;
  if (node === 636) return -0.09890093136848918;
  if (node === 317) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree195(features, 635);
    } else if (val <= 17.500000000000004) {
      return calculateTree195(features, 635);
    } else {
      return calculateTree195(features, 636);
    }
  }
  if (node === 318) return 0.3765361581531129;
  if (node === 158) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree195(features, 317);
    } else if (val <= 705.5000000000001) {
      return calculateTree195(features, 317);
    } else {
      return calculateTree195(features, 318);
    }
  }
  if (node === 78) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree195(features, 157);
    } else if (val <= 47.50000000000001) {
      return calculateTree195(features, 157);
    } else {
      return calculateTree195(features, 158);
    }
  }
  if (node === 38) {
    const val = features['is_preference_personal_care'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree195(features, 77);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree195(features, 77);
    } else {
      return calculateTree195(features, 78);
    }
  }
  if (node === 18) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree195(features, 37);
    } else if (val <= 44.50000000000001) {
      return calculateTree195(features, 37);
    } else {
      return calculateTree195(features, 38);
    }
  }
  if (node === 8) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree195(features, 17);
    } else if (val <= 33.50000000000001) {
      return calculateTree195(features, 17);
    } else {
      return calculateTree195(features, 18);
    }
  }
  if (node === 3) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree195(features, 7);
    } else if (val <= 891.0000000000001) {
      return calculateTree195(features, 7);
    } else {
      return calculateTree195(features, 8);
    }
  }
  if (node === 19) return 0.42022519394601454;
  if (node === 20) return 0.0724392466851183;
  if (node === 9) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree195(features, 19);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree195(features, 19);
    } else {
      return calculateTree195(features, 20);
    }
  }
  if (node === 10) return -0.04963674155559129;
  if (node === 4) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree195(features, 9);
    } else if (val <= 51.50000000000001) {
      return calculateTree195(features, 9);
    } else {
      return calculateTree195(features, 10);
    }
  }
  if (node === 1) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree195(features, 3);
    } else if (val <= 960.0000000000001) {
      return calculateTree195(features, 3);
    } else {
      return calculateTree195(features, 4);
    }
  }
  if (node === 2) return 0.0010424546846957577;
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree195(features, 1);
    } else if (val <= 105.50000000000001) {
      return calculateTree195(features, 1);
    } else {
      return calculateTree195(features, 2);
    }
  }
}
function calculateTree196(features, node = 0) {
  if (node === 3) return 0.0003076115338446995;
  if (node === 39) return -0.3215531081211224;
  if (node === 327) return 0.09869636631974513;
  if (node === 328) return -0.15275176381468072;
  if (node === 163) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree196(features, 327);
    } else if (val <= 758.5000000000001) {
      return calculateTree196(features, 327);
    } else {
      return calculateTree196(features, 328);
    }
  }
  if (node === 164) return 0.36767232525148913;
  if (node === 81) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree196(features, 163);
    } else if (val <= 715.5000000000001) {
      return calculateTree196(features, 163);
    } else {
      return calculateTree196(features, 164);
    }
  }
  if (node === 331) return 0.4912368710135037;
  if (node === 665) return 0.2419641639605281;
  if (node === 666) return -0.15738203028088535;
  if (node === 332) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree196(features, 665);
    } else if (val <= 7.500000000000001) {
      return calculateTree196(features, 665);
    } else {
      return calculateTree196(features, 666);
    }
  }
  if (node === 165) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree196(features, 331);
    } else if (val <= 558.5000000000001) {
      return calculateTree196(features, 331);
    } else {
      return calculateTree196(features, 332);
    }
  }
  if (node === 333) return -0.12357992580048856;
  if (node === 1339) return -0.0578941434802822;
  if (node === 1340) return 0.18138432827243836;
  if (node === 669) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree196(features, 1339);
    } else if (val <= 11.500000000000002) {
      return calculateTree196(features, 1339);
    } else {
      return calculateTree196(features, 1340);
    }
  }
  if (node === 1341) return -0.14709750396688967;
  if (node === 1342) return 0.20283765590350913;
  if (node === 670) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree196(features, 1341);
    } else if (val <= 880.0000000000001) {
      return calculateTree196(features, 1341);
    } else {
      return calculateTree196(features, 1342);
    }
  }
  if (node === 334) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree196(features, 669);
    } else if (val <= 422.00000000000006) {
      return calculateTree196(features, 669);
    } else {
      return calculateTree196(features, 670);
    }
  }
  if (node === 166) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree196(features, 333);
    } else if (val <= 6.500000000000001) {
      return calculateTree196(features, 333);
    } else {
      return calculateTree196(features, 334);
    }
  }
  if (node === 82) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree196(features, 165);
    } else if (val <= 139.50000000000003) {
      return calculateTree196(features, 165);
    } else {
      return calculateTree196(features, 166);
    }
  }
  if (node === 40) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree196(features, 81);
    } else if (val <= 3.5000000000000004) {
      return calculateTree196(features, 81);
    } else {
      return calculateTree196(features, 82);
    }
  }
  if (node === 19) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree196(features, 39);
    } else if (val <= 12.000000000000002) {
      return calculateTree196(features, 39);
    } else {
      return calculateTree196(features, 40);
    }
  }
  if (node === 83) return -0.19014612756086485;
  if (node === 84) return 0.19866501481029458;
  if (node === 41) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree196(features, 83);
    } else if (val <= 461.50000000000006) {
      return calculateTree196(features, 83);
    } else {
      return calculateTree196(features, 84);
    }
  }
  if (node === 171) return 0.41806069260036877;
  if (node === 172) return 0.10197305580629461;
  if (node === 85) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree196(features, 171);
    } else if (val <= 447.00000000000006) {
      return calculateTree196(features, 171);
    } else {
      return calculateTree196(features, 172);
    }
  }
  if (node === 173) return -0.03143694963811911;
  if (node === 174) return 0.22048954512942898;
  if (node === 86) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree196(features, 173);
    } else if (val <= 639.5000000000001) {
      return calculateTree196(features, 173);
    } else {
      return calculateTree196(features, 174);
    }
  }
  if (node === 42) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree196(features, 85);
    } else if (val <= 470.50000000000006) {
      return calculateTree196(features, 85);
    } else {
      return calculateTree196(features, 86);
    }
  }
  if (node === 20) {
    const val = features['is_preference_household'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree196(features, 41);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree196(features, 41);
    } else {
      return calculateTree196(features, 42);
    }
  }
  if (node === 9) {
    const val = features['is_preference_men_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree196(features, 19);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree196(features, 19);
    } else {
      return calculateTree196(features, 20);
    }
  }
  if (node === 43) return -0.006757438128643266;
  if (node === 44) return -0.2397841278181145;
  if (node === 21) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree196(features, 43);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree196(features, 43);
    } else {
      return calculateTree196(features, 44);
    }
  }
  if (node === 735) return -0.23756917465034816;
  if (node === 736) return 0.0061880640685392264;
  if (node === 367) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree196(features, 735);
    } else if (val <= 278.50000000000006) {
      return calculateTree196(features, 735);
    } else {
      return calculateTree196(features, 736);
    }
  }
  if (node === 368) return 0.21309685933938174;
  if (node === 183) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree196(features, 367);
    } else if (val <= 143.50000000000003) {
      return calculateTree196(features, 367);
    } else {
      return calculateTree196(features, 368);
    }
  }
  if (node === 184) return -0.21974743022223234;
  if (node === 91) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree196(features, 183);
    } else if (val <= 880.0000000000001) {
      return calculateTree196(features, 183);
    } else {
      return calculateTree196(features, 184);
    }
  }
  if (node === 185) return 0.41840015855470253;
  if (node === 186) return 0.03017256360703835;
  if (node === 92) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree196(features, 185);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree196(features, 185);
    } else {
      return calculateTree196(features, 186);
    }
  }
  if (node === 45) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree196(features, 91);
    } else if (val <= 742.5000000000001) {
      return calculateTree196(features, 91);
    } else {
      return calculateTree196(features, 92);
    }
  }
  if (node === 46) return -0.13602761476850656;
  if (node === 22) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree196(features, 45);
    } else if (val <= 29.500000000000004) {
      return calculateTree196(features, 45);
    } else {
      return calculateTree196(features, 46);
    }
  }
  if (node === 10) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree196(features, 21);
    } else if (val <= 3.5000000000000004) {
      return calculateTree196(features, 21);
    } else {
      return calculateTree196(features, 22);
    }
  }
  if (node === 4) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree196(features, 9);
    } else if (val <= 763.0000000000001) {
      return calculateTree196(features, 9);
    } else {
      return calculateTree196(features, 10);
    }
  }
  if (node === 1) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree196(features, 3);
    } else if (val <= 54.50000000000001) {
      return calculateTree196(features, 3);
    } else {
      return calculateTree196(features, 4);
    }
  }
  if (node === 5) return -0.019707666705489776;
  if (node === 13) return 0.03290214483605386;
  if (node === 29) return 0.006023564252254744;
  if (node === 30) return -0.07552919737092716;
  if (node === 14) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree196(features, 29);
    } else if (val <= 734.0000000000001) {
      return calculateTree196(features, 29);
    } else {
      return calculateTree196(features, 30);
    }
  }
  if (node === 6) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree196(features, 13);
    } else if (val <= 2.5000000000000004) {
      return calculateTree196(features, 13);
    } else {
      return calculateTree196(features, 14);
    }
  }
  if (node === 2) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree196(features, 5);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree196(features, 5);
    } else {
      return calculateTree196(features, 6);
    }
  }
  if (node === 0) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree196(features, 1);
    } else if (val <= 55.50000000000001) {
      return calculateTree196(features, 1);
    } else {
      return calculateTree196(features, 2);
    }
  }
}
function calculateTree197(features, node = 0) {
  if (node === 3) return -0.0007080564452275885;
  if (node === 39) return -0.12138594066146051;
  if (node === 40) return 0.09286529813451934;
  if (node === 19) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree197(features, 39);
    } else if (val <= 86.50000000000001) {
      return calculateTree197(features, 39);
    } else {
      return calculateTree197(features, 40);
    }
  }
  if (node === 20) return -0.08953676489944365;
  if (node === 9) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree197(features, 19);
    } else if (val <= 211.50000000000003) {
      return calculateTree197(features, 19);
    } else {
      return calculateTree197(features, 20);
    }
  }
  if (node === 1407) return 0.1047719408372199;
  if (node === 1408) return -0.06856554107910191;
  if (node === 703) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree197(features, 1407);
    } else if (val <= 66.00000000000001) {
      return calculateTree197(features, 1407);
    } else {
      return calculateTree197(features, 1408);
    }
  }
  if (node === 1409) return -0.19260609343957186;
  if (node === 1410) return 0.10233492312802488;
  if (node === 704) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree197(features, 1409);
    } else if (val <= 105.50000000000001) {
      return calculateTree197(features, 1409);
    } else {
      return calculateTree197(features, 1410);
    }
  }
  if (node === 351) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree197(features, 703);
    } else if (val <= 15.500000000000002) {
      return calculateTree197(features, 703);
    } else {
      return calculateTree197(features, 704);
    }
  }
  if (node === 705) return 0.2646442593336072;
  if (node === 1413) return 0.12714818928321533;
  if (node === 1414) return -0.05658979539284707;
  if (node === 706) {
    const val = features['is_preference_home'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree197(features, 1413);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree197(features, 1413);
    } else {
      return calculateTree197(features, 1414);
    }
  }
  if (node === 352) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree197(features, 705);
    } else if (val <= 148.50000000000003) {
      return calculateTree197(features, 705);
    } else {
      return calculateTree197(features, 706);
    }
  }
  if (node === 175) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree197(features, 351);
    } else if (val <= 817.5000000000001) {
      return calculateTree197(features, 351);
    } else {
      return calculateTree197(features, 352);
    }
  }
  if (node === 353) return 0.0033267017232088587;
  if (node === 354) return -0.24218787866492664;
  if (node === 176) {
    const val = features['is_preference_household'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree197(features, 353);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree197(features, 353);
    } else {
      return calculateTree197(features, 354);
    }
  }
  if (node === 87) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree197(features, 175);
    } else if (val <= 480.00000000000006) {
      return calculateTree197(features, 175);
    } else {
      return calculateTree197(features, 176);
    }
  }
  if (node === 88) return 0.09978591432133979;
  if (node === 43) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree197(features, 87);
    } else if (val <= 503.50000000000006) {
      return calculateTree197(features, 87);
    } else {
      return calculateTree197(features, 88);
    }
  }
  if (node === 179) return -0.3280291563263232;
  if (node === 180) return 0.10183995205038777;
  if (node === 89) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree197(features, 179);
    } else if (val <= 6.500000000000001) {
      return calculateTree197(features, 179);
    } else {
      return calculateTree197(features, 180);
    }
  }
  if (node === 363) return 0.07141638808216211;
  if (node === 364) return -0.23157622704958875;
  if (node === 181) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree197(features, 363);
    } else if (val <= 880.0000000000001) {
      return calculateTree197(features, 363);
    } else {
      return calculateTree197(features, 364);
    }
  }
  if (node === 182) return -0.2956508610285669;
  if (node === 90) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree197(features, 181);
    } else if (val <= 4.500000000000001) {
      return calculateTree197(features, 181);
    } else {
      return calculateTree197(features, 182);
    }
  }
  if (node === 44) {
    const val = features['is_preference_women_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree197(features, 89);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree197(features, 89);
    } else {
      return calculateTree197(features, 90);
    }
  }
  if (node === 21) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree197(features, 43);
    } else if (val <= 548.5000000000001) {
      return calculateTree197(features, 43);
    } else {
      return calculateTree197(features, 44);
    }
  }
  if (node === 45) return 0.2792828086386697;
  if (node === 187) return -0.21634246267681162;
  if (node === 1511) return 0.25593975825186965;
  if (node === 1512) return 0.015129597589986714;
  if (node === 755) {
    const val = features['is_preference_sports'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree197(features, 1511);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree197(features, 1511);
    } else {
      return calculateTree197(features, 1512);
    }
  }
  if (node === 1513) return -0.04208450431598118;
  if (node === 1514) return 0.07181162756514477;
  if (node === 756) {
    const val = features['is_preference_sports'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree197(features, 1513);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree197(features, 1513);
    } else {
      return calculateTree197(features, 1514);
    }
  }
  if (node === 377) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree197(features, 755);
    } else if (val <= 141.50000000000003) {
      return calculateTree197(features, 755);
    } else {
      return calculateTree197(features, 756);
    }
  }
  if (node === 1515) return 0.0059974368995512955;
  if (node === 1516) return -0.2603058470129376;
  if (node === 757) {
    const val = features['is_preference_sports'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree197(features, 1515);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree197(features, 1515);
    } else {
      return calculateTree197(features, 1516);
    }
  }
  if (node === 1517) return -0.05761650420597913;
  if (node === 1518) return 0.2909769095990732;
  if (node === 758) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree197(features, 1517);
    } else if (val <= 13.500000000000002) {
      return calculateTree197(features, 1517);
    } else {
      return calculateTree197(features, 1518);
    }
  }
  if (node === 378) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree197(features, 757);
    } else if (val <= 56.00000000000001) {
      return calculateTree197(features, 757);
    } else {
      return calculateTree197(features, 758);
    }
  }
  if (node === 188) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree197(features, 377);
    } else if (val <= 951.5000000000001) {
      return calculateTree197(features, 377);
    } else {
      return calculateTree197(features, 378);
    }
  }
  if (node === 93) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree197(features, 187);
    } else if (val <= 34.00000000000001) {
      return calculateTree197(features, 187);
    } else {
      return calculateTree197(features, 188);
    }
  }
  if (node === 189) return -0.2986215054674592;
  if (node === 190) return -0.05264883760846615;
  if (node === 94) {
    const val = features['is_preference_personal_care'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree197(features, 189);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree197(features, 189);
    } else {
      return calculateTree197(features, 190);
    }
  }
  if (node === 46) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree197(features, 93);
    } else if (val <= 916.0000000000001) {
      return calculateTree197(features, 93);
    } else {
      return calculateTree197(features, 94);
    }
  }
  if (node === 22) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree197(features, 45);
    } else if (val <= 649.5000000000001) {
      return calculateTree197(features, 45);
    } else {
      return calculateTree197(features, 46);
    }
  }
  if (node === 10) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree197(features, 21);
    } else if (val <= 647.0000000000001) {
      return calculateTree197(features, 21);
    } else {
      return calculateTree197(features, 22);
    }
  }
  if (node === 4) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree197(features, 9);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree197(features, 9);
    } else {
      return calculateTree197(features, 10);
    }
  }
  if (node === 1) {
    const val = features['is_preference_kitchen'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree197(features, 3);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree197(features, 3);
    } else {
      return calculateTree197(features, 4);
    }
  }
  if (node === 2) return 0.002066420642486609;
  if (node === 0) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree197(features, 1);
    } else if (val <= 18.500000000000004) {
      return calculateTree197(features, 1);
    } else {
      return calculateTree197(features, 2);
    }
  }
}
function calculateTree198(features, node = 0) {
  if (node === 127) return -0.10083366687635954;
  if (node === 1031) return 0.031570387093707825;
  if (node === 1032) return -0.0545677123041322;
  if (node === 515) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree198(features, 1031);
    } else if (val <= 13.500000000000002) {
      return calculateTree198(features, 1031);
    } else {
      return calculateTree198(features, 1032);
    }
  }
  if (node === 1033) return 0.2448017157054453;
  if (node === 1034) return 0.04251901475434499;
  if (node === 516) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree198(features, 1033);
    } else if (val <= 41.50000000000001) {
      return calculateTree198(features, 1033);
    } else {
      return calculateTree198(features, 1034);
    }
  }
  if (node === 257) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree198(features, 515);
    } else if (val <= 36.50000000000001) {
      return calculateTree198(features, 515);
    } else {
      return calculateTree198(features, 516);
    }
  }
  if (node === 258) return -0.12209403571408356;
  if (node === 128) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree198(features, 257);
    } else if (val <= 50.50000000000001) {
      return calculateTree198(features, 257);
    } else {
      return calculateTree198(features, 258);
    }
  }
  if (node === 63) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree198(features, 127);
    } else if (val <= 1.5000000000000002) {
      return calculateTree198(features, 127);
    } else {
      return calculateTree198(features, 128);
    }
  }
  if (node === 259) return 0.12109692290091588;
  if (node === 260) return 0.3355919554184432;
  if (node === 129) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree198(features, 259);
    } else if (val <= 36.50000000000001) {
      return calculateTree198(features, 259);
    } else {
      return calculateTree198(features, 260);
    }
  }
  if (node === 1047) return -0.11435280698395209;
  if (node === 1048) return -0.5159550002634845;
  if (node === 523) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree198(features, 1047);
    } else if (val <= 40.50000000000001) {
      return calculateTree198(features, 1047);
    } else {
      return calculateTree198(features, 1048);
    }
  }
  if (node === 1049) return -0.08770574637498912;
  if (node === 1050) return 0.10114199224584874;
  if (node === 524) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree198(features, 1049);
    } else if (val <= 38.50000000000001) {
      return calculateTree198(features, 1049);
    } else {
      return calculateTree198(features, 1050);
    }
  }
  if (node === 261) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree198(features, 523);
    } else if (val <= 2.5000000000000004) {
      return calculateTree198(features, 523);
    } else {
      return calculateTree198(features, 524);
    }
  }
  if (node === 262) return 0.22115585134556326;
  if (node === 130) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree198(features, 261);
    } else if (val <= 50.50000000000001) {
      return calculateTree198(features, 261);
    } else {
      return calculateTree198(features, 262);
    }
  }
  if (node === 64) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree198(features, 129);
    } else if (val <= 1.5000000000000002) {
      return calculateTree198(features, 129);
    } else {
      return calculateTree198(features, 130);
    }
  }
  if (node === 31) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree198(features, 63);
    } else if (val <= 1.5000000000000002) {
      return calculateTree198(features, 63);
    } else {
      return calculateTree198(features, 64);
    }
  }
  if (node === 65) return 0.28329985859350443;
  if (node === 66) return 0.07465874491342817;
  if (node === 32) {
    const val = features['is_preference_beauty'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree198(features, 65);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree198(features, 65);
    } else {
      return calculateTree198(features, 66);
    }
  }
  if (node === 15) {
    const val = features['is_preference_home'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree198(features, 31);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree198(features, 31);
    } else {
      return calculateTree198(features, 32);
    }
  }
  if (node === 67) return -0.20538676872761305;
  if (node === 137) return 0.024826699091769384;
  if (node === 277) return -0.05078085345709289;
  if (node === 278) return -0.2959518920977001;
  if (node === 138) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree198(features, 277);
    } else if (val <= 8.500000000000002) {
      return calculateTree198(features, 277);
    } else {
      return calculateTree198(features, 278);
    }
  }
  if (node === 68) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree198(features, 137);
    } else if (val <= 42.50000000000001) {
      return calculateTree198(features, 137);
    } else {
      return calculateTree198(features, 138);
    }
  }
  if (node === 33) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree198(features, 67);
    } else if (val <= 5.500000000000001) {
      return calculateTree198(features, 67);
    } else {
      return calculateTree198(features, 68);
    }
  }
  if (node === 69) return 0.3175246037680889;
  if (node === 141) return 0.12190666218596866;
  if (node === 285) return -0.28579938025943913;
  if (node === 286) return 0.04047963079824266;
  if (node === 142) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree198(features, 285);
    } else if (val <= 43.50000000000001) {
      return calculateTree198(features, 285);
    } else {
      return calculateTree198(features, 286);
    }
  }
  if (node === 70) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree198(features, 141);
    } else if (val <= 33.50000000000001) {
      return calculateTree198(features, 141);
    } else {
      return calculateTree198(features, 142);
    }
  }
  if (node === 34) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree198(features, 69);
    } else if (val <= 295.00000000000006) {
      return calculateTree198(features, 69);
    } else {
      return calculateTree198(features, 70);
    }
  }
  if (node === 16) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree198(features, 33);
    } else if (val <= 41.00000000000001) {
      return calculateTree198(features, 33);
    } else {
      return calculateTree198(features, 34);
    }
  }
  if (node === 7) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree198(features, 15);
    } else if (val <= 4.500000000000001) {
      return calculateTree198(features, 15);
    } else {
      return calculateTree198(features, 16);
    }
  }
  if (node === 35) return 0.36656780975925335;
  if (node === 73) return 0.034142476780289255;
  if (node === 74) return 0.3541736262980955;
  if (node === 36) {
    const val = features['is_preference_kitchen'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree198(features, 73);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree198(features, 73);
    } else {
      return calculateTree198(features, 74);
    }
  }
  if (node === 17) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree198(features, 35);
    } else if (val <= 15.500000000000002) {
      return calculateTree198(features, 35);
    } else {
      return calculateTree198(features, 36);
    }
  }
  if (node === 18) return 0.02455933344391562;
  if (node === 8) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree198(features, 17);
    } else if (val <= 50.50000000000001) {
      return calculateTree198(features, 17);
    } else {
      return calculateTree198(features, 18);
    }
  }
  if (node === 3) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree198(features, 7);
    } else if (val <= 12.500000000000002) {
      return calculateTree198(features, 7);
    } else {
      return calculateTree198(features, 8);
    }
  }
  if (node === 4) return -0.3019507789706884;
  if (node === 1) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree198(features, 3);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree198(features, 3);
    } else {
      return calculateTree198(features, 4);
    }
  }
  if (node === 5) return 0.0006471284278998945;
  if (node === 27) return -0.11007726361632933;
  if (node === 28) return -0.3473109262455628;
  if (node === 13) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree198(features, 27);
    } else if (val <= 295.00000000000006) {
      return calculateTree198(features, 27);
    } else {
      return calculateTree198(features, 28);
    }
  }
  if (node === 14) return -0.0020023924767208754;
  if (node === 6) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree198(features, 13);
    } else if (val <= 899.5000000000001) {
      return calculateTree198(features, 13);
    } else {
      return calculateTree198(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree198(features, 5);
    } else if (val <= 896.5000000000001) {
      return calculateTree198(features, 5);
    } else {
      return calculateTree198(features, 6);
    }
  }
  if (node === 0) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree198(features, 1);
    } else if (val <= 3.5000000000000004) {
      return calculateTree198(features, 1);
    } else {
      return calculateTree198(features, 2);
    }
  }
}
function calculateTree199(features, node = 0) {
  if (node === 1023) return -0.0016147749378573795;
  if (node === 1024) return 0.100061795161418;
  if (node === 511) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree199(features, 1023);
    } else if (val <= 709.5000000000001) {
      return calculateTree199(features, 1023);
    } else {
      return calculateTree199(features, 1024);
    }
  }
  if (node === 1025) return -0.37489660502582967;
  if (node === 1026) return -0.05491285704998446;
  if (node === 512) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree199(features, 1025);
    } else if (val <= 507.50000000000006) {
      return calculateTree199(features, 1025);
    } else {
      return calculateTree199(features, 1026);
    }
  }
  if (node === 255) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree199(features, 511);
    } else if (val <= 497.50000000000006) {
      return calculateTree199(features, 511);
    } else {
      return calculateTree199(features, 512);
    }
  }
  if (node === 1027) return 0.12169547673463374;
  if (node === 1028) return 0.44051206993635456;
  if (node === 513) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree199(features, 1027);
    } else if (val <= 723.5000000000001) {
      return calculateTree199(features, 1027);
    } else {
      return calculateTree199(features, 1028);
    }
  }
  if (node === 1029) return -0.0030504261672394104;
  if (node === 1030) return 0.3837303478495757;
  if (node === 514) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree199(features, 1029);
    } else if (val <= 859.5000000000001) {
      return calculateTree199(features, 1029);
    } else {
      return calculateTree199(features, 1030);
    }
  }
  if (node === 256) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree199(features, 513);
    } else if (val <= 729.5000000000001) {
      return calculateTree199(features, 513);
    } else {
      return calculateTree199(features, 514);
    }
  }
  if (node === 127) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree199(features, 255);
    } else if (val <= 654.0000000000001) {
      return calculateTree199(features, 255);
    } else {
      return calculateTree199(features, 256);
    }
  }
  if (node === 128) return -0.1823338621846674;
  if (node === 63) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree199(features, 127);
    } else if (val <= 937.5000000000001) {
      return calculateTree199(features, 127);
    } else {
      return calculateTree199(features, 128);
    }
  }
  if (node === 64) return 0.18401215298275253;
  if (node === 31) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree199(features, 63);
    } else if (val <= 969.5000000000001) {
      return calculateTree199(features, 63);
    } else {
      return calculateTree199(features, 64);
    }
  }
  if (node === 263) return -0.0014247453874511797;
  if (node === 264) return -0.138581043459542;
  if (node === 131) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree199(features, 263);
    } else if (val <= 932.5000000000001) {
      return calculateTree199(features, 263);
    } else {
      return calculateTree199(features, 264);
    }
  }
  if (node === 1063) return -0.07282805063974222;
  if (node === 1064) return 0.06711284728501149;
  if (node === 531) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree199(features, 1063);
    } else if (val <= 575.5000000000001) {
      return calculateTree199(features, 1063);
    } else {
      return calculateTree199(features, 1064);
    }
  }
  if (node === 1065) return 0.07889363163802124;
  if (node === 1066) return -0.16661215880457073;
  if (node === 532) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree199(features, 1065);
    } else if (val <= 863.0000000000001) {
      return calculateTree199(features, 1065);
    } else {
      return calculateTree199(features, 1066);
    }
  }
  if (node === 265) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree199(features, 531);
    } else if (val <= 331.50000000000006) {
      return calculateTree199(features, 531);
    } else {
      return calculateTree199(features, 532);
    }
  }
  if (node === 266) return -0.24629222876365636;
  if (node === 132) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree199(features, 265);
    } else if (val <= 903.5000000000001) {
      return calculateTree199(features, 265);
    } else {
      return calculateTree199(features, 266);
    }
  }
  if (node === 65) {
    const val = features['is_preference_grocery'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree199(features, 131);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree199(features, 131);
    } else {
      return calculateTree199(features, 132);
    }
  }
  if (node === 66) return 0.013316222781674504;
  if (node === 32) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree199(features, 65);
    } else if (val <= 934.0000000000001) {
      return calculateTree199(features, 65);
    } else {
      return calculateTree199(features, 66);
    }
  }
  if (node === 15) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree199(features, 31);
    } else if (val <= 1.5000000000000002) {
      return calculateTree199(features, 31);
    } else {
      return calculateTree199(features, 32);
    }
  }
  if (node === 16) return 0.10164221484765854;
  if (node === 7) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree199(features, 15);
    } else if (val <= 922.5000000000001) {
      return calculateTree199(features, 15);
    } else {
      return calculateTree199(features, 16);
    }
  }
  if (node === 8) return -0.0887732277472189;
  if (node === 3) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree199(features, 7);
    } else if (val <= 923.5000000000001) {
      return calculateTree199(features, 7);
    } else {
      return calculateTree199(features, 8);
    }
  }
  if (node === 19) return 0.12252939132960682;
  if (node === 20) return -0.3093695492400052;
  if (node === 9) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree199(features, 19);
    } else if (val <= 781.0000000000001) {
      return calculateTree199(features, 19);
    } else {
      return calculateTree199(features, 20);
    }
  }
  if (node === 351) return 0.01594279545807125;
  if (node === 352) return -0.24981178246529714;
  if (node === 175) {
    const val = features['is_preference_grocery'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree199(features, 351);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree199(features, 351);
    } else {
      return calculateTree199(features, 352);
    }
  }
  if (node === 176) return 0.2849481411975462;
  if (node === 87) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree199(features, 175);
    } else if (val <= 155.50000000000003) {
      return calculateTree199(features, 175);
    } else {
      return calculateTree199(features, 176);
    }
  }
  if (node === 177) return -0.05560960920565702;
  if (node === 178) return 0.29235297804424265;
  if (node === 88) {
    const val = features['super_offer_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree199(features, 177);
    } else if (val <= 808.5000000000001) {
      return calculateTree199(features, 177);
    } else {
      return calculateTree199(features, 178);
    }
  }
  if (node === 43) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree199(features, 87);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree199(features, 87);
    } else {
      return calculateTree199(features, 88);
    }
  }
  if (node === 179) return 0.08620627836520867;
  if (node === 180) return 0.4373675323468151;
  if (node === 89) {
    const val = features['is_preference_gadgets'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree199(features, 179);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree199(features, 179);
    } else {
      return calculateTree199(features, 180);
    }
  }
  if (node === 90) return -0.1715367777265646;
  if (node === 44) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree199(features, 89);
    } else if (val <= 378.50000000000006) {
      return calculateTree199(features, 89);
    } else {
      return calculateTree199(features, 90);
    }
  }
  if (node === 21) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree199(features, 43);
    } else if (val <= 7.500000000000001) {
      return calculateTree199(features, 43);
    } else {
      return calculateTree199(features, 44);
    }
  }
  if (node === 22) return -0.2048961310471205;
  if (node === 10) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree199(features, 21);
    } else if (val <= 18.500000000000004) {
      return calculateTree199(features, 21);
    } else {
      return calculateTree199(features, 22);
    }
  }
  if (node === 4) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree199(features, 9);
    } else if (val <= 940.0000000000001) {
      return calculateTree199(features, 9);
    } else {
      return calculateTree199(features, 10);
    }
  }
  if (node === 1) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree199(features, 3);
    } else if (val <= 938.5000000000001) {
      return calculateTree199(features, 3);
    } else {
      return calculateTree199(features, 4);
    }
  }
  if (node === 2) return -0.002009632263781358;
  if (node === 0) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree199(features, 1);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree199(features, 1);
    } else {
      return calculateTree199(features, 2);
    }
  }
}
function calculateTree200(features, node = 0) {
  if (node === 1) return -0.00196654418950561;
  if (node === 11) return 0.002363628847741266;
  if (node === 25) return 0.17710153802936157;
  if (node === 26) return -0.3436303205233955;
  if (node === 12) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree200(features, 25);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree200(features, 25);
    } else {
      return calculateTree200(features, 26);
    }
  }
  if (node === 5) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree200(features, 11);
    } else if (val <= 846.5000000000001) {
      return calculateTree200(features, 11);
    } else {
      return calculateTree200(features, 12);
    }
  }
  if (node === 223) return 0.04577336532294005;
  if (node === 224) return 0.2541580742688603;
  if (node === 111) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree200(features, 223);
    } else if (val <= 55.50000000000001) {
      return calculateTree200(features, 223);
    } else {
      return calculateTree200(features, 224);
    }
  }
  if (node === 225) return -0.32425196946859564;
  if (node === 1815) return 0.060839154771103326;
  if (node === 1816) return -0.17520995269590664;
  if (node === 907) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree200(features, 1815);
    } else if (val <= 888.0000000000001) {
      return calculateTree200(features, 1815);
    } else {
      return calculateTree200(features, 1816);
    }
  }
  if (node === 1817) return 0.09683752912248678;
  if (node === 1818) return -0.059661491329137686;
  if (node === 908) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree200(features, 1817);
    } else if (val <= 905.5000000000001) {
      return calculateTree200(features, 1817);
    } else {
      return calculateTree200(features, 1818);
    }
  }
  if (node === 453) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree200(features, 907);
    } else if (val <= 903.5000000000001) {
      return calculateTree200(features, 907);
    } else {
      return calculateTree200(features, 908);
    }
  }
  if (node === 454) return -0.224479048935316;
  if (node === 226) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree200(features, 453);
    } else if (val <= 916.5000000000001) {
      return calculateTree200(features, 453);
    } else {
      return calculateTree200(features, 454);
    }
  }
  if (node === 112) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree200(features, 225);
    } else if (val <= 882.5000000000001) {
      return calculateTree200(features, 225);
    } else {
      return calculateTree200(features, 226);
    }
  }
  if (node === 55) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree200(features, 111);
    } else if (val <= 868.0000000000001) {
      return calculateTree200(features, 111);
    } else {
      return calculateTree200(features, 112);
    }
  }
  if (node === 227) return 0.09113182354694492;
  if (node === 228) return -0.10191221309250878;
  if (node === 113) {
    const val = features['is_preference_women_fashion'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree200(features, 227);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree200(features, 227);
    } else {
      return calculateTree200(features, 228);
    }
  }
  if (node === 229) return 0.08767664677126863;
  if (node === 230) return 0.22702694791773345;
  if (node === 114) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree200(features, 229);
    } else if (val <= 923.5000000000001) {
      return calculateTree200(features, 229);
    } else {
      return calculateTree200(features, 230);
    }
  }
  if (node === 56) {
    const val = features['user_gender'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree200(features, 113);
    } else if (val <= 1.5000000000000002) {
      return calculateTree200(features, 113);
    } else {
      return calculateTree200(features, 114);
    }
  }
  if (node === 27) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree200(features, 55);
    } else if (val <= 919.5000000000001) {
      return calculateTree200(features, 55);
    } else {
      return calculateTree200(features, 56);
    }
  }
  if (node === 115) return 0.4338338728464917;
  if (node === 116) return -0.2383310333082161;
  if (node === 57) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree200(features, 115);
    } else if (val <= 28.500000000000004) {
      return calculateTree200(features, 115);
    } else {
      return calculateTree200(features, 116);
    }
  }
  if (node === 235) return -0.06943789596468602;
  if (node === 236) return 0.0013706898127263045;
  if (node === 117) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree200(features, 235);
    } else if (val <= 38.50000000000001) {
      return calculateTree200(features, 235);
    } else {
      return calculateTree200(features, 236);
    }
  }
  if (node === 237) return -9.17442379966555e-5;
  if (node === 238) return 0.08508961234870743;
  if (node === 118) {
    const val = features['user_state_id'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree200(features, 237);
    } else if (val <= 511.00000000000006) {
      return calculateTree200(features, 237);
    } else {
      return calculateTree200(features, 238);
    }
  }
  if (node === 58) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree200(features, 117);
    } else if (val <= 5.500000000000001) {
      return calculateTree200(features, 117);
    } else {
      return calculateTree200(features, 118);
    }
  }
  if (node === 28) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree200(features, 57);
    } else if (val <= 948.5000000000001) {
      return calculateTree200(features, 57);
    } else {
      return calculateTree200(features, 58);
    }
  }
  if (node === 13) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree200(features, 27);
    } else if (val <= 938.5000000000001) {
      return calculateTree200(features, 27);
    } else {
      return calculateTree200(features, 28);
    }
  }
  if (node === 29) return -0.1644509435407211;
  if (node === 247) return -0.006028743460121977;
  if (node === 995) return -0.3516101725343989;
  if (node === 996) return 0.07463958298354895;
  if (node === 497) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree200(features, 995);
    } else if (val <= 922.5000000000001) {
      return calculateTree200(features, 995);
    } else {
      return calculateTree200(features, 996);
    }
  }
  if (node === 498) return -0.371549039755153;
  if (node === 248) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree200(features, 497);
    } else if (val <= 923.5000000000001) {
      return calculateTree200(features, 497);
    } else {
      return calculateTree200(features, 498);
    }
  }
  if (node === 123) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree200(features, 247);
    } else if (val <= 919.5000000000001) {
      return calculateTree200(features, 247);
    } else {
      return calculateTree200(features, 248);
    }
  }
  if (node === 499) return 0.46302230910863124;
  if (node === 500) return 0.12238512712007009;
  if (node === 249) {
    const val = features['user_active_days'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree200(features, 499);
    } else if (val <= 3.5000000000000004) {
      return calculateTree200(features, 499);
    } else {
      return calculateTree200(features, 500);
    }
  }
  if (node === 501) return -0.33307471584868403;
  if (node === 502) return 0.004786213923860677;
  if (node === 250) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree200(features, 501);
    } else if (val <= 942.0000000000001) {
      return calculateTree200(features, 501);
    } else {
      return calculateTree200(features, 502);
    }
  }
  if (node === 124) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree200(features, 249);
    } else if (val <= 940.0000000000001) {
      return calculateTree200(features, 249);
    } else {
      return calculateTree200(features, 250);
    }
  }
  if (node === 61) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree200(features, 123);
    } else if (val <= 938.5000000000001) {
      return calculateTree200(features, 123);
    } else {
      return calculateTree200(features, 124);
    }
  }
  if (node === 62) return -0.23959681506934116;
  if (node === 30) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree200(features, 61);
    } else if (val <= 992.5000000000001) {
      return calculateTree200(features, 61);
    } else {
      return calculateTree200(features, 62);
    }
  }
  if (node === 14) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree200(features, 29);
    } else if (val <= 868.0000000000001) {
      return calculateTree200(features, 29);
    } else {
      return calculateTree200(features, 30);
    }
  }
  if (node === 6) {
    const val = features['is_ios'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree200(features, 13);
    } else if (val <= 1.0000000180025095e-35) {
      return calculateTree200(features, 13);
    } else {
      return calculateTree200(features, 14);
    }
  }
  if (node === 2) {
    const val = features['offer_id_mod'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree200(features, 5);
    } else if (val <= 852.5000000000001) {
      return calculateTree200(features, 5);
    } else {
      return calculateTree200(features, 6);
    }
  }
  if (node === 0) {
    const val = features['user_age'];
    if (val === undefined || val === null || isNaN(val)) {
      return calculateTree200(features, 1);
    } else if (val <= 25.500000000000004) {
      return calculateTree200(features, 1);
    } else {
      return calculateTree200(features, 2);
    }
  }
}

function predict(features) {
  let sum = 0;
  sum += calculateTree0(features);
  sum += calculateTree1(features);
  sum += calculateTree2(features);
  sum += calculateTree3(features);
  sum += calculateTree4(features);
  sum += calculateTree5(features);
  sum += calculateTree6(features);
  sum += calculateTree7(features);
  sum += calculateTree8(features);
  sum += calculateTree9(features);
  sum += calculateTree10(features);
  sum += calculateTree11(features);
  sum += calculateTree12(features);
  sum += calculateTree13(features);
  sum += calculateTree14(features);
  sum += calculateTree15(features);
  sum += calculateTree16(features);
  sum += calculateTree17(features);
  sum += calculateTree18(features);
  sum += calculateTree19(features);
  sum += calculateTree20(features);
  sum += calculateTree21(features);
  sum += calculateTree22(features);
  sum += calculateTree23(features);
  sum += calculateTree24(features);
  sum += calculateTree25(features);
  sum += calculateTree26(features);
  sum += calculateTree27(features);
  sum += calculateTree28(features);
  sum += calculateTree29(features);
  sum += calculateTree30(features);
  sum += calculateTree31(features);
  sum += calculateTree32(features);
  sum += calculateTree33(features);
  sum += calculateTree34(features);
  sum += calculateTree35(features);
  sum += calculateTree36(features);
  sum += calculateTree37(features);
  sum += calculateTree38(features);
  sum += calculateTree39(features);
  sum += calculateTree40(features);
  sum += calculateTree41(features);
  sum += calculateTree42(features);
  sum += calculateTree43(features);
  sum += calculateTree44(features);
  sum += calculateTree45(features);
  sum += calculateTree46(features);
  sum += calculateTree47(features);
  sum += calculateTree48(features);
  sum += calculateTree49(features);
  sum += calculateTree50(features);
  sum += calculateTree51(features);
  sum += calculateTree52(features);
  sum += calculateTree53(features);
  sum += calculateTree54(features);
  sum += calculateTree55(features);
  sum += calculateTree56(features);
  sum += calculateTree57(features);
  sum += calculateTree58(features);
  sum += calculateTree59(features);
  sum += calculateTree60(features);
  sum += calculateTree61(features);
  sum += calculateTree62(features);
  sum += calculateTree63(features);
  sum += calculateTree64(features);
  sum += calculateTree65(features);
  sum += calculateTree66(features);
  sum += calculateTree67(features);
  sum += calculateTree68(features);
  sum += calculateTree69(features);
  sum += calculateTree70(features);
  sum += calculateTree71(features);
  sum += calculateTree72(features);
  sum += calculateTree73(features);
  sum += calculateTree74(features);
  sum += calculateTree75(features);
  sum += calculateTree76(features);
  sum += calculateTree77(features);
  sum += calculateTree78(features);
  sum += calculateTree79(features);
  sum += calculateTree80(features);
  sum += calculateTree81(features);
  sum += calculateTree82(features);
  sum += calculateTree83(features);
  sum += calculateTree84(features);
  sum += calculateTree85(features);
  sum += calculateTree86(features);
  sum += calculateTree87(features);
  sum += calculateTree88(features);
  sum += calculateTree89(features);
  sum += calculateTree90(features);
  sum += calculateTree91(features);
  sum += calculateTree92(features);
  sum += calculateTree93(features);
  sum += calculateTree94(features);
  sum += calculateTree95(features);
  sum += calculateTree96(features);
  sum += calculateTree97(features);
  sum += calculateTree98(features);
  sum += calculateTree99(features);
  sum += calculateTree100(features);
  sum += calculateTree101(features);
  sum += calculateTree102(features);
  sum += calculateTree103(features);
  sum += calculateTree104(features);
  sum += calculateTree105(features);
  sum += calculateTree106(features);
  sum += calculateTree107(features);
  sum += calculateTree108(features);
  sum += calculateTree109(features);
  sum += calculateTree110(features);
  sum += calculateTree111(features);
  sum += calculateTree112(features);
  sum += calculateTree113(features);
  sum += calculateTree114(features);
  sum += calculateTree115(features);
  sum += calculateTree116(features);
  sum += calculateTree117(features);
  sum += calculateTree118(features);
  sum += calculateTree119(features);
  sum += calculateTree120(features);
  sum += calculateTree121(features);
  sum += calculateTree122(features);
  sum += calculateTree123(features);
  sum += calculateTree124(features);
  sum += calculateTree125(features);
  sum += calculateTree126(features);
  sum += calculateTree127(features);
  sum += calculateTree128(features);
  sum += calculateTree129(features);
  sum += calculateTree130(features);
  sum += calculateTree131(features);
  sum += calculateTree132(features);
  sum += calculateTree133(features);
  sum += calculateTree134(features);
  sum += calculateTree135(features);
  sum += calculateTree136(features);
  sum += calculateTree137(features);
  sum += calculateTree138(features);
  sum += calculateTree139(features);
  sum += calculateTree140(features);
  sum += calculateTree141(features);
  sum += calculateTree142(features);
  sum += calculateTree143(features);
  sum += calculateTree144(features);
  sum += calculateTree145(features);
  sum += calculateTree146(features);
  sum += calculateTree147(features);
  sum += calculateTree148(features);
  sum += calculateTree149(features);
  sum += calculateTree150(features);
  sum += calculateTree151(features);
  sum += calculateTree152(features);
  sum += calculateTree153(features);
  sum += calculateTree154(features);
  sum += calculateTree155(features);
  sum += calculateTree156(features);
  sum += calculateTree157(features);
  sum += calculateTree158(features);
  sum += calculateTree159(features);
  sum += calculateTree160(features);
  sum += calculateTree161(features);
  sum += calculateTree162(features);
  sum += calculateTree163(features);
  sum += calculateTree164(features);
  sum += calculateTree165(features);
  sum += calculateTree166(features);
  sum += calculateTree167(features);
  sum += calculateTree168(features);
  sum += calculateTree169(features);
  sum += calculateTree170(features);
  sum += calculateTree171(features);
  sum += calculateTree172(features);
  sum += calculateTree173(features);
  sum += calculateTree174(features);
  sum += calculateTree175(features);
  sum += calculateTree176(features);
  sum += calculateTree177(features);
  sum += calculateTree178(features);
  sum += calculateTree179(features);
  sum += calculateTree180(features);
  sum += calculateTree181(features);
  sum += calculateTree182(features);
  sum += calculateTree183(features);
  sum += calculateTree184(features);
  sum += calculateTree185(features);
  sum += calculateTree186(features);
  sum += calculateTree187(features);
  sum += calculateTree188(features);
  sum += calculateTree189(features);
  sum += calculateTree190(features);
  sum += calculateTree191(features);
  sum += calculateTree192(features);
  sum += calculateTree193(features);
  sum += calculateTree194(features);
  sum += calculateTree195(features);
  sum += calculateTree196(features);
  sum += calculateTree197(features);
  sum += calculateTree198(features);
  sum += calculateTree199(features);
  sum += calculateTree200(features);
  const score = sum;
  return 1 / (1 + Math.exp(-score));
}

module.exports = predict;

// 使用示例:
// const features = {
//   super_offer_id: 0.5,
//   offer_id_mod: 2.0
// };
// console.log(predict(features));
